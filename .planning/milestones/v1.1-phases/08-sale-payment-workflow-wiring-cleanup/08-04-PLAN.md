---
phase: 08-sale-payment-workflow-wiring-cleanup
plan: 04
type: execute
wave: 2
depends_on: ["08-02"]
files_modified:
  - src/features/process-refund/model/useProcessRefund.ts
  - src/features/process-refund/ui/RefundSheet.tsx
  - e2e/35-refund.spec.ts
autonomous: true
requirements: [SALE-05]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "useProcessRefund.ts's SUPABASE_ERROR fallback (the confirmed leak site, ROADMAP SC3's literal example) returns a translated processRefund.genericError message, never error.message as string (D-09)."
    - "RefundSheet.tsx's dead '!== \"\"' ternary is simplified to a plain toast.error(result.error.message) — safe now that the hook never returns an empty or raw string — matching the actual leak-fix pattern rather than leaving misleading defensive code in place."
    - "REFUND_EXCEEDS_ORIGINAL, ITEM_NOT_IN_ORIGINAL_ORDER, AUTH_FORBIDDEN, and (from 08-02) VALIDATION_ERROR mappings are byte-identical to their pre-change form — only the SUPABASE_ERROR branch changes."
    - "A new e2e/35-refund.spec.ts test forces the unmapped-error path and asserts the rendered toast text matches the translated generic string, not a substring like /relation|column|syntax error/i."
  artifacts:
    - e2e/35-refund.spec.ts (1 new test)
  key_links:
    - "useProcessRefund's SUPABASE_ERROR branch → i18n.t('featOrders:processRefund.genericError') → RefundSheet.tsx's simplified toast.error(result.error.message) → e2e/35-refund.spec.ts new test asserts the visible text"
  prohibitions:
    - "The generic fallback must not replace REFUND_EXCEEDS_ORIGINAL's specific, already-actionable message ('exceeds the remaining refundable balance') — a manager needs that distinction to know the refund is genuinely over-limit, not just 'something went wrong'."
---

<objective>
Close SALE-05's headline example — ROADMAP SC3: "Triggering a refund failure ... shows a translated, staff-facing message instead of a raw Postgres/RPC error string." `useProcessRefund.ts:53` (`return err({ code: 'SUPABASE_ERROR' as AppErrorCode, message: error.message as string, raw: error })`) is the exact confirmed leak — verified by reading the source this session and cited directly in RESEARCH.md's Code Examples. This plan runs after 08-02 (SALE-06) because both touch `useProcessRefund.ts` and cannot execute in parallel against the same file.

Purpose: A cashier/manager who triggers a genuinely unmapped refund failure (e.g. a transient DB error, not REFUND_EXCEEDS_ORIGINAL) currently sees raw Postgres text in the toast today — this plan closes that specific, named gap and proves it with a real E2E assertion.
Output: `useProcessRefund.ts`'s SUPABASE_ERROR branch is translated; `RefundSheet.tsx`'s dead empty-string ternary is simplified; `e2e/35-refund.spec.ts` gains a test forcing and asserting the fix.
</objective>

## Artifacts this plan produces

- No new files besides the one new test case appended to `e2e/35-refund.spec.ts`.
- Reuses the already-existing `featOrders.json` key `processRefund.genericError` ("Could not process refund. Check your connection and try again." / "No se pudo procesar el reembolso. Revisa tu conexión e intenta de nuevo.") — no new i18n key needed.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-sale-payment-workflow-wiring-cleanup/08-CONTEXT.md
@.planning/phases/08-sale-payment-workflow-wiring-cleanup/08-RESEARCH.md
.planning/phases/08-sale-payment-workflow-wiring-cleanup/08-02-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Translate useProcessRefund's SUPABASE_ERROR fallback, simplify RefundSheet's ternary, extend 35-refund.spec.ts</name>
  <files>src/features/process-refund/model/useProcessRefund.ts, src/features/process-refund/ui/RefundSheet.tsx, e2e/35-refund.spec.ts</files>
  <read_first>
    src/features/process-refund/model/useProcessRefund.ts (post-08-02 state — the file now has ProcessRefundInputSchema validation added; this task only touches the RPC-error branch, currently `return err({ code: 'SUPABASE_ERROR' as AppErrorCode, message: error.message as string, raw: error });`)
    src/features/process-refund/ui/RefundSheet.tsx lines 175-183 (the `result.error.message !== "" ? result.error.message : t("processRefund.genericError")` ternary to simplify)
    e2e/35-refund.spec.ts (full file — T1-T6 structure, `seedPaidTab` helper, `getServiceClient()` pattern for forcing a DB-level failure)
  </read_first>
  <behavior>
    - When `process_refund` RPC's `error` is not matched by the existing `REFUND_EXCEEDS_ORIGINAL`/`ITEM_NOT_IN_ORIGINAL_ORDER`/`AUTH_FORBIDDEN` string checks, the returned message is `i18n.t('featOrders:processRefund.genericError')`, never `error.message`.
    - `RefundSheet.tsx`'s toast always renders `result.error.message` directly — no `!== ""` branch, since the hook is now guaranteed to never return an empty or raw string.
  </behavior>
  <action>
    In `useProcessRefund.ts`, change the final fallback line from `return err({ code: 'SUPABASE_ERROR' as AppErrorCode, message: error.message as string, raw: error });` to `return err({ code: 'SUPABASE_ERROR' as AppErrorCode, message: i18n.t('featOrders:processRefund.genericError'), raw: error });` (`i18n` is already imported in this file; `raw: error` is preserved unchanged for logging/debugging, only the user-facing `message` field changes).

    In `RefundSheet.tsx` lines 175-183, replace:
    `toast.error(result.error.message !== "" ? result.error.message : t("processRefund.genericError"));`
    with:
    `toast.error(result.error.message);`
    (the ternary's fallback branch is now unreachable dead code — the hook never returns an empty string — so removing it is a correctness cleanup, not a behavior change).

    Add a new test to `e2e/35-refund.spec.ts` after the existing T6 test, in the same file's style: seed a paid tab via `seedPaidTab(db, 1, 10.0)`, then force an unmapped RPC failure by deleting the referenced payment row via the service client BEFORE opening the RefundSheet and requesting approval (e.g. `await db.from('payments').delete().eq('id', paymentId)` right after seeding, so `process_refund`'s lookup of `original_payment_id` fails with a genuinely unmapped Postgres error, not REFUND_EXCEEDS_ORIGINAL) — OR, if the RefundButton's own UI-level guard (per T5's existing comment) prevents opening RefundSheet for a payment that no longer resolves, instead force the failure via `page.route()` intercepting the `process_refund` RPC call's underlying HTTP request and returning a 500 with an unmapped Postgres-shaped error body. Prefer the `page.route()` interception approach if the DB-delete approach is blocked by the UI guard — it does not require any RPC/DB behavior change and directly exercises the exact client-side code path this task fixes. Assert the rendered toast text equals `i18n.t('featOrders:processRefund.genericError')` and does NOT match `/relation|column|syntax error|does not exist/i` (proving no raw Postgres text leaked).
  </action>
  <verify>
    <automated>npx playwright test e2e/35-refund.spec.ts -g "generic"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `useProcessRefund.ts`'s `SUPABASE_ERROR` branch no longer passes `error.message` into the returned `AppError.message`.
    - Source: `RefundSheet.tsx` no longer contains the `!== ""` empty-string ternary.
    - Behavior: the new E2E test's toast assertion passes — visible text matches the translated generic string and does not match a raw-Postgres-text pattern.
    - `npx playwright test e2e/35-refund.spec.ts` (full file, all tests) passes.
  </acceptance_criteria>
  <done>useProcessRefund's confirmed SALE-05 leak site is fixed; e2e/35-refund.spec.ts proves the fix with a forced-failure assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `process_refund` RPC error text → staff-facing toast | Same boundary as 08-03, scoped specifically to the refund flow named in ROADMAP SC3 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-09 | Information Disclosure | `useProcessRefund.ts`'s `SUPABASE_ERROR` fallback | high | mitigate | The confirmed, ROADMAP-cited raw-error leak site is replaced with a translated generic message; `raw: error` still flows to `logger.error`-adjacent debugging paths, never to the toast |
</threat_model>

<verification>
`npx playwright test e2e/35-refund.spec.ts` (all tests, including the new one). `npm run typecheck && npm run lint` clean.
</verification>

<success_criteria>
- `useProcessRefund.ts`'s SUPABASE_ERROR branch is translated.
- `RefundSheet.tsx`'s dead ternary is simplified.
- New E2E test proves the generic-fallback path renders translated text, not raw Postgres text.
- Existing T1-T6 tests in `35-refund.spec.ts` remain green (REFUND_EXCEEDS_ORIGINAL/AUTH_FORBIDDEN paths unaffected).
</success_criteria>

<output>
Create `.planning/phases/08-sale-payment-workflow-wiring-cleanup/08-04-SUMMARY.md` when done
</output>
