---
phase: 08-sale-payment-workflow-wiring-cleanup
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/tauri.conf.json
autonomous: true
requirements: [OPS-01]

estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "src-tauri/tauri.conf.json's identifier field reads exactly \"com.tajhouseofspices.supermarketpos\" (D-15 — value already decided by the user, recorded in STATE.md)."
    - "The placeholder value \"com.yourcompany.barpos\" no longer appears anywhere in tauri.conf.json."
    - { statement: "No other field in tauri.conf.json (productName, version, bundle config, etc.) is touched — this is a single scalar-value edit, not a broader config pass.", verification: backstop }
  artifacts:
    - src-tauri/tauri.conf.json (identifier field updated)
  key_links: []
  prohibitions: []
---

<objective>
Implement OPS-01: replace the placeholder Tauri app identifier `com.yourcompany.barpos` (deliberately left untouched during Phase 1's rebrand as an OS-level, out-of-scope item at the time) with the real value `com.tajhouseofspices.supermarketpos`, already decided by the user and recorded in `.planning/STATE.md`.

Purpose: The shipped Windows/WebView2 desktop app must carry a real, unique OS-level application identifier before it goes to the customer's store — a placeholder identifier risks collision with any other app on the same machine still using `com.yourcompany.*`, and is a visible sign of an unfinished rebrand.
Output: `src-tauri/tauri.conf.json`'s `identifier` field is updated. No runtime code path is affected (this is a build-time value read by the Tauri bundler); per RESEARCH.md's Runtime State Inventory, there is no existing installed instance of this app to migrate, so no data-loss/migration concern applies.
</objective>

## Artifacts this plan produces

- No new files. `src-tauri/tauri.conf.json` line 5's `identifier` value changes.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-sale-payment-workflow-wiring-cleanup/08-CONTEXT.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Set the real Tauri app identifier</name>
  <files>src-tauri/tauri.conf.json</files>
  <read_first>
    src-tauri/tauri.conf.json (full file — line 5's current `"identifier": "com.yourcompany.barpos",`)
    .planning/STATE.md (Decisions section — confirms target value `com.tajhouseofspices.supermarketpos` and that this was deliberately deferred from Phase 1's rebrand)
  </read_first>
  <action>
    In `src-tauri/tauri.conf.json`, change line 5 from `"identifier": "com.yourcompany.barpos",` to `"identifier": "com.tajhouseofspices.supermarketpos",` (D-15). Do not modify any other field in the file (productName, version, bundle targets, etc. are all out of scope for OPS-01).
  </action>
  <verify>
    <automated>grep -c 'com.tajhouseofspices.supermarketpos' src-tauri/tauri.conf.json</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'com.tajhouseofspices.supermarketpos' src-tauri/tauri.conf.json` returns `1`.
    - Source: `grep -c 'com.yourcompany.barpos' src-tauri/tauri.conf.json` returns `0`.
    - `npx @tauri-apps/cli info` (or `cat src-tauri/tauri.conf.json | python3 -m json.tool`) confirms the file is still valid JSON after the edit.
  </acceptance_criteria>
  <done>tauri.conf.json's identifier field reads com.tajhouseofspices.supermarketpos, confirmed by direct file inspection (config value, not E2E-testable — consistent with this project's documented exception for OPS-01/OPS-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Build-time config value only, no runtime trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-10 | Spoofing (OS-level app-identity collision) | `src-tauri/tauri.conf.json` `identifier` | low | mitigate | Setting a real, unique reverse-DNS identifier avoids a future collision with another locally-installed app still using the generic `com.yourcompany.*` placeholder (e.g. shared local-storage/keychain scope on Windows) |
</threat_model>

<verification>
Direct file inspection: `grep -c 'com.tajhouseofspices.supermarketpos' src-tauri/tauri.conf.json` returns 1; JSON remains valid.
</verification>

<success_criteria>
- `tauri.conf.json`'s `identifier` field is exactly `com.tajhouseofspices.supermarketpos`.
- No other field in the file changed.
</success_criteria>

<output>
Create `.planning/phases/08-sale-payment-workflow-wiring-cleanup/08-05-SUMMARY.md` when done
</output>
