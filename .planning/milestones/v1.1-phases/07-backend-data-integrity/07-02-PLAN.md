---
phase: 07-backend-data-integrity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/settings-backup/index.ts
  - supabase/functions/settings-restore/index.ts
  - e2e/56-settings-backup-restore.spec.ts
autonomous: true
requirements: [DATA-02]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "settings-backup and settings-restore edge functions complete successfully end-to-end with zero pool_tables references — proven by a Playwright E2E test exercising both functions via the existing Settings > Backup tab UI (ROADMAP Phase 7 Success Criterion #3)."
    - "A repo-wide grep for 'pool_tables' inside supabase/functions/settings-backup/ and supabase/functions/settings-restore/ returns zero matches after this plan."
  artifacts:
    - path: "supabase/functions/settings-backup/index.ts"
      provides: "Backup snapshot collection with the pool_tables SELECT/field removed — settings/categories/products/modifiers/product_modifiers only"
    - path: "supabase/functions/settings-restore/index.ts"
      provides: "Restore logic and Snapshot type with the pool_tables field/read/upsert block removed"
    - path: "e2e/56-settings-backup-restore.spec.ts"
      provides: "Automated end-to-end proof: create a backup, then restore it, both via the live edge functions, both succeed"
  key_links:
    - "BackupSettingsTab.tsx (existing, unmodified) is the only UI entry point for both edge functions — useMutationCreateSettingsBackup()/useMutationRestoreSettingsBackup() -> callCreateSettingsBackup()/callRestoreSettingsBackup() -> supabase.functions.invoke('settings-backup'|'settings-restore') -> this plan's edited Deno functions."
    - "Both edge functions are bind-mounted from supabase/functions/ into the supabase-edge-functions container (confirmed via docker inspect this session) — editing the source file requires a container restart to take effect, since the edge-runtime does not reliably hot-reload changed function source for already-served functions."
  prohibitions:
    - statement: "Must not leave any residual pool_tables reference in either file after the edit — including a shifted Promise.all destructuring position (removing one array element without also removing its corresponding destructured variable name and its slot in the later `.error ||` chain is a silent off-by-one, not a clean deletion)."
      disposition: flagged-unverified
---

<objective>
Remove all `pool_tables` references from `supabase/functions/settings-backup/index.ts` and `supabase/functions/settings-restore/index.ts` — both currently query/upsert a table dropped from the schema in Phase 1, so both functions hard-fail against the live schema today (confirmed by reading both files this session: `settings-backup`'s `Promise.all` includes `serviceClient.from('pool_tables').select('*')...`, which throws against a live DB with no `pool_tables` table, making `poolTablesRes.error` truthy and the whole backup call return 500 `DB_ERROR`).

Purpose: per D-04 (locked), hard-remove with no backward-compat tolerance for old snapshots that still contain a `pool_tables` key — this is pre-production data, no real backup depends on restoring pool_tables.

Output: both edge functions edited and restarted live; one new Playwright E2E spec proving both functions succeed end-to-end via the existing Backup Settings tab UI.

**Specless-probe note (flagged assumption, not silently dropped):** the deterministic edge-probe run before this plan classified DATA-02 as `unclassified`/`unresolved` (no automated backstop applied). The `must_haves.truths` above are derived directly from ROADMAP.md's Phase 7 Success Criterion #3 and CONTEXT.md's D-04, not from the probe output.

**Tracer-first note:** this plan has no meaningfully "thinner" slice than the full fix — both files are pure, symmetric deletions inside an already-existing, already-deployed request/response scaffold (per 07-PATTERNS.md, "no pattern change needed"). Task 1 makes both edits and restarts the container in one pass; Task 2 is the end-to-end proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-backend-data-integrity/07-CONTEXT.md
@.planning/phases/07-backend-data-integrity/07-PATTERNS.md
@supabase/functions/settings-backup/index.ts
@supabase/functions/settings-restore/index.ts
@src/widgets/SettingsTabsPanel/tabs/BackupSettingsTab.tsx
@e2e/08-settings-receipt.spec.ts
@e2e/helpers/auth.ts
</context>

<interfaces>
`BackupSettingsTab.tsx` (existing, not modified by this plan) renders under `/settings` -> "Backup" tab (`t('tabs.backup')` = `"Backup"`), gated by `manage_settings` (admin/manager). Button text (en-US, from `wAdmin.json`): create = `"Create Manual Backup"`, success toast = `"Backup created."`; restore row button = `"Restore"`, confirm-dialog button = `"Restore backup"`, success toast = `"Backup restored."`.

`e2e/helpers/auth.ts` exports `loginAs(page, role)`/`logout(page)`. `e2e/helpers/supabase.ts` exports `resetTestState()`/`openCaja(openingCash)`. `e2e/helpers/requireEnv.ts` exports `requireIntegrationEnv()`. Mirror `e2e/08-settings-receipt.spec.ts`'s `beforeEach` (requireIntegrationEnv -> resetTestState -> openCaja(540) -> page.goto('/')) exactly.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: [BLOCKING] Remove pool_tables from settings-backup and settings-restore, restart the live edge functions</name>
  <files>supabase/functions/settings-backup/index.ts, supabase/functions/settings-restore/index.ts</files>
  <read_first>Both files in full (already read this session — settings-backup 113 lines, settings-restore 167 lines); .planning/phases/07-backend-data-integrity/07-PATTERNS.md lines 112-158 (exact line-by-line deletions for both files); .planning/phases/07-backend-data-integrity/07-CONTEXT.md D-04 (line 26, the locked hard-remove decision, no backward-compat).</read_first>
  <action>
    In `supabase/functions/settings-backup/index.ts`: remove `serviceClient.from('pool_tables').select('*').order('number'),` from the `Promise.all` array and its destructured name `poolTablesRes` from `const [settingsRes, poolTablesRes, categoriesRes, productsRes, modifiersRes, productModifiersRes] = ...` (becomes `const [settingsRes, categoriesRes, productsRes, modifiersRes, productModifiersRes] = ...`); remove `poolTablesRes.error ||` from the combined error check; remove `pool_tables: poolTablesRes.data,` from the `snapshot` object literal.
    In `supabase/functions/settings-restore/index.ts`: remove the `pool_tables?: Array<{...}>` field from the `Snapshot` type; remove `const poolTables = snapshot.pool_tables ?? [];`; remove the entire `if (poolTables.length > 0) { ... serviceClient.from('pool_tables').upsert(...) ... }` block. Leave every other field/block in both files (categories/products/modifiers/product_modifiers/settings, the auth split, the `json()` helper, `recordAudit`) untouched.
    After both edits, confirm zero remaining references: `grep -n pool_tables supabase/functions/settings-backup/index.ts supabase/functions/settings-restore/index.ts` must return nothing.
    [BLOCKING] Restart the live edge-runtime container so the edited source is served: `docker restart supabase-edge-functions`, then wait for it to be ready (`docker logs supabase-edge-functions --tail 20` should show no crash loop; the container's bind mount at `/home/deno/functions` already points at this repo's `supabase/functions/`, confirmed via `docker inspect` this session — no `supabase functions deploy` step exists or is needed for this self-hosted stack).
  </action>
  <verify>
    <automated>grep -c pool_tables supabase/functions/settings-backup/index.ts supabase/functions/settings-restore/index.ts | grep -v ':0' | grep -v '^$' ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>The grep verify command finds no `pool_tables` occurrence in either file (exit confirms zero matches). `docker restart supabase-edge-functions` completes and `docker logs supabase-edge-functions --tail 20` shows the service listening, no crash/error referencing either function.</acceptance_criteria>
  <done>Both edge functions no longer reference the dropped pool_tables table, and the live container is serving the updated code.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Playwright E2E — create then restore a settings backup end-to-end (SC3)</name>
  <files>e2e/56-settings-backup-restore.spec.ts</files>
  <precondition>Task 1's edge-function restart has completed and the live container is serving the updated settings-backup/settings-restore code.</precondition>
  <read_first>src/widgets/SettingsTabsPanel/tabs/BackupSettingsTab.tsx in full (already read this session — button/toast copy, ConfirmDialog flow); e2e/08-settings-receipt.spec.ts lines 1-30 (the beforeEach scaffold and tab-navigation pattern to mirror); src/shared/lib/i18n/locales/en-US/wAdmin.json lines 244-261 (`backupSettingsTab` copy — already confirmed this session: title "Backup", create button "Create Manual Backup", success toast "Backup created.", restore button "Restore", confirm button "Restore backup", success toast "Backup restored.").</read_first>
  <action>
    Create `e2e/56-settings-backup-restore.spec.ts` mirroring `e2e/08-settings-receipt.spec.ts`'s imports and `beforeEach` (`requireIntegrationEnv()` -> `resetTestState()` -> `openCaja(540)` -> `page.goto('/')`). One test: `loginAs(page, 'admin')`, `page.goto('/settings')`, click `page.getByRole('tab', { name: 'Backup' })`, click `page.getByRole('button', { name: 'Create Manual Backup' })`, assert a toast/text containing `'Backup created.'` becomes visible (this call exercises `settings-backup` end-to-end — before Task 1's fix it would 500 on the `pool_tables` SELECT). Then locate the newly-created backup row and click its `page.getByRole('button', { name: 'Restore' })`, confirm via `page.getByRole('button', { name: 'Restore backup' })` in the `ConfirmDialog`, assert a toast/text containing `'Backup restored.'` becomes visible (this call exercises `settings-restore` end-to-end — before Task 1's fix it would 500 on the `pool_tables` upsert whenever a legacy snapshot contained that key, and unconditionally referenced a dropped table's upsert path). End with `logout(page)`.
  </action>
  <verify>
    <automated>npx playwright test e2e/56-settings-backup-restore.spec.ts</automated>
  </verify>
  <acceptance_criteria>The spec passes: both the create-backup and restore-backup calls succeed (toasts visible), proving both edge functions complete end-to-end against the current schema with no pool_tables error.</acceptance_criteria>
  <done>ROADMAP Phase 7 Success Criterion #3 is proven by an automated Playwright E2E test exercising both edge functions live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (admin session) -> `settings-backup`/`settings-restore` edge functions (Bearer token, admin-role-gated by `serviceClient.from('profiles').select('role')`) | Unchanged by this plan — only the internal snapshot field list changes, not who can call these functions |
| `settings-restore` -> arbitrary JSON `snapshot` column upserted back into `categories`/`products`/`modifiers`/`product_modifiers`/`settings` | Pre-existing trust boundary (a stored snapshot's contents are trusted once it's inside `settings_backups`); this plan narrows it by one table (`pool_tables`), does not widen it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Tampering | `settings-backup`/`settings-restore` admin-role gate | medium | accept | The existing `profileError || profile?.role !== 'admin'` 403 check in both files is untouched by this plan — the pool_tables deletion happens strictly inside the already-authenticated/authorized code path; no new trust boundary is introduced. |
| T-07-05 | Tampering | Silent partial removal (dangling reference) | medium | mitigate | Task 1's verify step is a literal `grep -c pool_tables` across both files with a non-zero-match failure condition — a shifted `Promise.all` destructuring position or a missed field is caught mechanically, not by review alone. |
| T-07-06 | Information Disclosure | `settings_backups.snapshot` no longer includes `pool_tables` data | low | accept | Strictly a reduction in what gets backed up (pre-Phase-1 data that no longer exists in the schema); cannot leak data that is never collected. |

No package-manager installs occur in this plan (edge-function TypeScript edits + one Playwright spec, no new npm/pip/cargo dependency) — the package-legitimacy gate does not apply.
</threat_model>

<verification>
1. `grep -rn pool_tables supabase/functions/settings-backup/ supabase/functions/settings-restore/` returns no matches.
2. `npx playwright test e2e/56-settings-backup-restore.spec.ts` passes.
3. `npx playwright test e2e/08-settings-receipt.spec.ts` still passes unchanged (confirms the unrelated Hardware/Receipt settings tab is unaffected by the container restart or the edit).
</verification>

<success_criteria>
ROADMAP Phase 7 Success Criterion #3 is met: both edge functions complete successfully end-to-end with zero pool_tables references, proven by a live, automated Playwright test.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-02)

- `e2e/56-settings-backup-restore.spec.ts` (new file) — one Playwright test proving both `settings-backup` and `settings-restore` succeed end-to-end.
- No new exported symbols — `supabase/functions/settings-backup/index.ts` and `supabase/functions/settings-restore/index.ts` are edited in place (deletions only, no new fields/exports); both are outside `tsconfig.json`'s `include` (`src`, `scripts`) and outside `npm run lint`'s scope (`eslint src`), so neither typecheck nor lint cover them.
</artifacts_produced>

<output>
Create `.planning/phases/07-backend-data-integrity/07-02-SUMMARY.md` when done
</output>
