---
phase: 07-backend-data-integrity
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/shared/lib/supabase.types.ts
  - src/entities/settings/model/queries.ts
  - src/entities/supplier/model/queries.ts
autonomous: true
requirements: [DATA-03]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "supabase.types.ts includes generated types for suppliers, shipments, receipt_settings, and the receive_shipment RPC (ROADMAP Phase 7 Success Criterion #4)."
    - "npm run typecheck passes with zero errors, repo-wide, after the regen (not just on the files this plan directly touches)."
    - "No `as any`/type-workaround cast remains for supabase.from('receipt_settings'|'suppliers'|'supplier_products') anywhere in src/ (ROADMAP Phase 7 Success Criterion #4's 'no remaining as any workaround casts for those tables')."
  artifacts:
    - path: "src/shared/lib/supabase.types.ts"
      provides: "Full regenerated Database type including suppliers, supplier_products, shipments, receipt_settings tables and the receive_shipment RPC signature"
    - path: "src/entities/settings/model/queries.ts"
      provides: "useReceiptSettings()/useMutationUpdateReceiptSettings() reading/writing receipt_settings via the plain typed supabase client, no cast"
    - path: "src/entities/supplier/model/queries.ts"
      provides: "Supplier CRUD hooks reading/writing suppliers/supplier_products via the plain typed supabase client, no cast"
  key_links:
    - "Regeneration requires network reachability to Postgres that the host does not have directly (supabase-db's 5432/tcp is not published to the host, confirmed via docker inspect this session) — Task 1's socat proxy container (attached to the supabase_default docker network, publishing a host port) is the verified working mechanism, not a hypothetical fallback."
    - "This plan depends on 07-01 (Wave 1) landing first per D-05's own stated rationale ('avoids a second types-regen pass') even though receive_shipment's function signature is unchanged by 07-01 — the regen captures the schema in its final Phase-7 state in one pass."
  prohibitions:
    - statement: "Must not regenerate supabase.types.ts and then leave the as any casts in place 'just in case' — removing the cast at every call site for suppliers/supplier_products/receipt_settings is the point of DATA-03, not an optional follow-up."
      disposition: flagged-unverified
    - statement: "The full-schema regen must not be treated as scoped only to the 4 named tables/RPC in a way that silently breaks other already-typed consumers elsewhere in the repo — npm run typecheck must be run and pass repo-wide, not just on the 3 files this plan directly edits."
      disposition: flagged-unverified
---

<objective>
Regenerate `src/shared/lib/supabase.types.ts` so it includes `suppliers`, `supplier_products`, `shipments`, `receipt_settings`, and the `receive_shipment` RPC (none of which are in the currently-committed types file — confirmed by grep this session: zero matches for any of them), then remove the `as any`/type-workaround casts at every call site for the tables DATA-03 names.

Purpose: `receipt_settings` (added in Phase 6) and `suppliers`/`supplier_products`/`shipments`/`receive_shipment` (added in Phase 3) were never captured by a types regen — three files currently carry manual `as any` casts + scoped eslint-disables to work around this (`src/entities/settings/model/queries.ts` for `receipt_settings`, `src/entities/supplier/model/queries.ts` for `suppliers`/`supplier_products` — the latter found by this session's own re-grep per CONTEXT.md's explicit instruction to "check for others... at plan time", beyond the single file CONTEXT.md had already confirmed).

Output: a regenerated `supabase.types.ts`; both cast sites removed; `npm run typecheck && npm run lint` clean repo-wide.

**Out of explicit scope (not an oversight):** `src/entities/inventory/model/queries.ts` has its own pre-existing `row as unknown as {cost_price?, expiry_date?}` casts (added in Phase 3, predating this phase) for the `inventory.cost_price`/`expiry_date` columns. CONTEXT.md's DATA-03 scope names exactly four things — `suppliers`/`shipments`/`receipt_settings`/`receive_shipment` — and ROADMAP Success Criterion #4 says "no remaining as any workaround casts for those tables" (those four). `inventory`'s casts are not one of the four and are left untouched here; the regen will incidentally produce correct `inventory` types as a side effect of being a full-schema dump, but cleaning up `entities/inventory/model/queries.ts`'s own casts is not required by this phase's locked scope and is not done by this plan.

**Specless-probe note (flagged assumption, not silently dropped):** the deterministic edge-probe run before this plan classified DATA-03 as `unclassified`/`unresolved` (no automated backstop applied). The `must_haves.truths` above are derived directly from ROADMAP.md's Phase 7 Success Criterion #4, not from the probe output.

**Tracer-first note:** Task 1 is the tracer — it proves the full regen mechanism end-to-end against the live DB (the genuinely novel/risky part of this plan, since the host cannot reach the DB directly) before Tasks 2-3 do the mechanical cast removal that the regen unlocks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-backend-data-integrity/07-CONTEXT.md
@.planning/phases/07-backend-data-integrity/07-PATTERNS.md
@src/entities/settings/model/queries.ts
@src/entities/supplier/model/queries.ts
@CLAUDE.md
</context>

<interfaces>
Live self-hosted stack facts (all confirmed by direct testing this session, not assumed):
- `supabase-db`'s Postgres port (5432/tcp) is NOT published to the host — direct `--db-url postgresql://...@localhost:5432/...` from the host fails.
- `docker exec supabase-db env` exposes `POSTGRES_PASSWORD` (user `supabase_admin`, db `postgres`).
- `supabase-db` is on the `supabase_default` docker bridge network, reachable by container DNS name `supabase-db` from any container attached to that same network.
- `node_modules/.bin/supabase` (v2.91.1, already a devDependency) works fine on the host — the blocker is DB network reachability, not the CLI binary.
- Verified working command sequence (run and confirmed against this exact live DB this session — produced correct output including `suppliers:`, `shipments:`, `receipt_settings:`, `receive_shipment:` in the generated file): spin up a short-lived `alpine:3.22` container attached to `supabase_default`, install `socat` in it, have it forward a published host port to `supabase-db:5432`, then run the host's `npx supabase gen types typescript --db-url` against that published port.

`Database['public']['Functions']['receive_shipment']` — the RPC's typed signature the regen adds; no client code currently calls `supabase.rpc('receive_shipment', ...)` directly (it's called only from `supabase/functions/receive-shipment/index.ts`'s Deno `admin.rpc(...)`, outside this repo's generated-types surface), so no call-site update is needed for it beyond the type existing.
</interfaces>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: [BLOCKING] Regenerate supabase.types.ts against the live self-hosted DB</name>
  <files>src/shared/lib/supabase.types.ts</files>
  <precondition>Plan 07-01's migration (20260819000003_receive_shipment_weighted_avg_cost.sql) is already live on the self-hosted stack — confirmed by `docker exec supabase-db psql -U supabase_admin -d postgres -c "select count(*) from pg_proc where proname='receive_shipment'"` returning 1 (the function exists; this task does not depend on which body revision is live, only that the table/RPC surface DATA-03 needs is already in the schema).</precondition>
  <read_first>CLAUDE.md "Missing generated types workaround" section (the documented `npx supabase gen types typescript --local` command, which does NOT work as-is against this self-hosted stack — `--local` expects a `supabase start`-managed stack, this one is a hand-run docker-compose stack with different container names); supabase/config.toml (the D-06-REVISED comment confirming this is a self-hosted, unlinked stack — no cloud `project_id` to `--linked` against); current src/shared/lib/supabase.types.ts lines 1-20 and the `inventory` table block (confirms today's file predates Phase 3 — `inventory` has no `cost_price`/`expiry_date` columns yet, and has zero occurrences of `suppliers`/`shipments`/`receipt_settings`/`receive_shipment`).</read_first>
  <action>
    1. Get the DB password: `docker exec supabase-db env | grep POSTGRES_PASSWORD` (format `POSTGRES_PASSWORD=<value>`).
    2. Start the proxy container: `docker run -d --rm --name pg-typegen-proxy --network supabase_default -p 127.0.0.1:55432:5432 alpine:3.22 sh -c "apk add --no-cache socat >/dev/null 2>&1 && socat TCP-LISTEN:5432,fork,reuseaddr TCP:supabase-db:5432"`. Wait ~3s, then confirm it's listening: `docker ps --filter name=pg-typegen-proxy --format '{{.Ports}}'` should show `127.0.0.1:55432->5432/tcp`.
    3. Regenerate: `npx supabase gen types typescript --db-url "postgresql://supabase_admin:<PASSWORD>@localhost:55432/postgres" --schema public > src/shared/lib/supabase.types.ts` (this call itself pulls a small `supabase/postgres-meta` helper image on first run — expected, not an error).
    4. Stop the proxy: `docker stop pg-typegen-proxy` (started with `--rm`, so it self-removes on stop — confirm with `docker ps -a --filter name=pg-typegen-proxy` returning nothing).
    5. Confirm the four named surfaces are present: `grep -n "suppliers:\|shipments:\|receipt_settings:\|receive_shipment:" src/shared/lib/supabase.types.ts` must show all four (this exact sequence was run and verified this session — all four were present in the output).
  </action>
  <verify>
    <automated>grep -c "suppliers:\|shipments:\|receipt_settings:\|receive_shipment:" src/shared/lib/supabase.types.ts</automated>
  </verify>
  <acceptance_criteria>The grep count is at least 4 (one match per named table/RPC, possibly more if a name appears in a Relationships block too). `git diff --stat src/shared/lib/supabase.types.ts` shows a substantial diff (hundreds of lines — this file has not been regenerated since before Phase 3). No `pg-typegen-proxy` container remains running or exists afterward (`docker ps -a --filter name=pg-typegen-proxy` returns nothing).</acceptance_criteria>
  <done>supabase.types.ts is regenerated from the live schema and includes suppliers, supplier_products, shipments, receipt_settings, and receive_shipment.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove the receipt_settings as any cast in entities/settings/model/queries.ts</name>
  <files>src/entities/settings/model/queries.ts</files>
  <read_first>src/entities/settings/model/queries.ts lines 300-390 (already read this session — `useReceiptSettings()` lines 322-352, `useMutationUpdateReceiptSettings()` lines 354-387, the two `(supabase as any).from('receipt_settings')` cast sites and their `eslint-disable-next-line` comments and explanatory code comments above each).</read_first>
  <action>
    In `useReceiptSettings()`'s `queryFn`: replace `(await (supabase as any).from('receipt_settings').select('*').maybeSingle()) as { data: ReceiptSettingsRow | null; error: PostgrestError | null }` with the plain typed call `await supabase.from('receipt_settings').select('*').maybeSingle()`; delete the `eslint-disable-next-line @typescript-eslint/no-explicit-any, ...` comment and the "not yet in generated Supabase types" explanatory comment above it (both are now stale).
    In `useMutationUpdateReceiptSettings()`'s `mutationFn`: replace `(await (supabase as any).from('receipt_settings').upsert(payload, { onConflict: 'id' }).select('id').single()) as { error: PostgrestError | null }` with the plain typed call `await supabase.from('receipt_settings').upsert(payload, { onConflict: 'id' }).select('id').single()`; delete its matching `eslint-disable-next-line`/comment pair too.
    If the regenerated `receipt_settings` Row type's field names don't exactly match `ReceiptSettingsRow`'s existing shape (e.g. nullability differs from what `mapReceiptRow` expects), adjust `mapReceiptRow`/`toReceiptPayload`'s types minimally to match — do not re-introduce a cast to paper over a mismatch.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>`grep -n "receipt_settings" src/entities/settings/model/queries.ts` shows no `as any` on the same line as any `receipt_settings` reference. `npm run typecheck` (repo-wide) passes with zero errors.</acceptance_criteria>
  <done>useReceiptSettings()/useMutationUpdateReceiptSettings() use the plain typed supabase client with no workaround cast.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Remove the suppliers/supplier_products as any cast in entities/supplier/model/queries.ts, full typecheck/lint</name>
  <files>src/entities/supplier/model/queries.ts</files>
  <read_first>src/entities/supplier/model/queries.ts in full (already read this session, 184 lines — the file-level `/* eslint-disable @typescript-eslint/no-explicit-any, ... */` at line 1 and `const db = supabase as unknown as { from: (table: string) => any };` at line 15, then every `db.from('suppliers'|'supplier_products')` call site through the file).</read_first>
  <action>
    Delete the file-level `/* eslint-disable ... */` comment at line 1 (keep `i18next/no-literal-string` disabled if this file still has literal strings the regen doesn't affect — re-check after the other rules are removed; if no literal-string violations remain, drop that too) and the `const db = supabase as unknown as { from: (table: string) => any };` line. Replace every `db.from('suppliers')` / `db.from('supplier_products')` call site (in `useSuppliers`, `useSupplierProductIds`, `useProductSupplierIds`, `syncSupplierProducts`, `syncProductSuppliers`, `useMutationCreateSupplier`, `useMutationUpdateSupplier`, `useMutationDeleteSupplier`) with the plain typed `supabase.from('suppliers')` / `supabase.from('supplier_products')`. The `supabaseQuery<any[]>(...)`/`supabaseMutation<any>(...)` generic type parameters can likely be narrowed or dropped now that the underlying call is typed — narrow them if it's a mechanical change (e.g. to the regenerated `Tables<'suppliers'>['Row'][]` shape), but do not spend extra effort re-deriving `map()`'s manual field mapping, which stays as-is (it already validates via `SupplierSchema.parse`, independent of the raw DB row's TS type).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>Both commands exit 0, repo-wide (not scoped to this file only). `grep -n "as any\|as unknown as" src/entities/supplier/model/queries.ts` returns no matches.</acceptance_criteria>
  <done>All three DATA-03 cast sites (receipt_settings x2, suppliers/supplier_products x1 file-level) are removed, and the whole repo typechecks and lints clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host machine -> live Postgres (via a temporary socat proxy container, Task 1 only) | The proxy container is short-lived (`--rm`, explicitly stopped at the end of Task 1) and binds only to `127.0.0.1` on the host — not reachable from outside this machine |
| Regenerated `Database` type -> every existing typed consumer repo-wide | A full-schema regen can change shapes for tables unrelated to this phase's 3 named files; Task 3's repo-wide typecheck is the guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07 | Elevation of Privilege | Temporary `pg-typegen-proxy` container | medium | mitigate | Binds only `127.0.0.1:55432` (not `0.0.0.0`), runs with `--rm` (self-deletes), and is explicitly `docker stop`-ped at the end of Task 1 — it does not persist past this plan's execution, and is never part of the shipped application. |
| T-07-08 | Tampering | Regenerated types silently breaking other consumers | low | mitigate | Task 3's `npm run typecheck` runs repo-wide (not scoped to the 3 edited files), per the frontmatter prohibition against a narrowly-scoped regen check. |
| T-07-09 | Information Disclosure | `supabase.types.ts` exposing schema shape client-side | low | accept | Pre-existing, unchanged risk profile — generated types are compile-time-only and this file is already committed/shipped for every other table; this plan adds 4 more tables/1 RPC to an already-public type-info surface, no new category of exposure. |

No package-manager installs occur in this plan (the `supabase` CLI is already a devDependency; `socat` is installed only inside the temporary, non-shipped proxy container) — the package-legitimacy gate does not apply.
</threat_model>

<verification>
1. `grep -c "suppliers:\|shipments:\|receipt_settings:\|receive_shipment:" src/shared/lib/supabase.types.ts` >= 4.
2. `npm run typecheck` exits 0, repo-wide.
3. `npm run lint` exits 0, repo-wide.
4. `grep -rn "as any" src/entities/settings/model/queries.ts src/entities/supplier/model/queries.ts` returns no matches referencing `receipt_settings`/`suppliers`/`supplier_products`.
</verification>

<success_criteria>
ROADMAP Phase 7 Success Criterion #4 is met: supabase.types.ts includes suppliers/shipments/receipt_settings/receive_shipment, and a clean repo-wide `npm run typecheck` confirms no remaining as any workaround cast for those tables.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-03)

- `src/shared/lib/supabase.types.ts` — regenerated in place (not a new file); adds `suppliers`, `supplier_products`, `shipments`, `receipt_settings` table types and the `receive_shipment` function type to the existing `Database` type. No new exported symbol name (same `Database`/`Json` exports as before).
- No new files. `src/entities/settings/model/queries.ts` and `src/entities/supplier/model/queries.ts` are edited in place (cast removal only — same exported hook names: `useReceiptSettings`, `useMutationUpdateReceiptSettings`, `useSuppliers`, `useSupplierProductIds`, `useProductSupplierIds`, `syncSupplierProducts`, `syncProductSuppliers`, `useMutationCreateSupplier`, `useMutationUpdateSupplier`, `useMutationDeleteSupplier`).
</artifacts_produced>

<output>
Create `.planning/phases/07-backend-data-integrity/07-03-SUMMARY.md` when done
</output>
