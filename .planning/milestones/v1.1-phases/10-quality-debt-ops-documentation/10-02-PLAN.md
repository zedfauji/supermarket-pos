---
phase: 10-quality-debt-ops-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .storybook/main.ts
  - .storybook/preview.ts
  - src/shared/ui/EmptyState.stories.tsx
  - src/shared/ui/ConfirmDialog.stories.tsx
  - src/shared/ui/POSButton.stories.tsx
  - src/shared/ui/DataTable.stories.tsx
  - src/shared/ui/MoneyDisplay.stories.tsx
  - src/shared/ui/MoneyInput.stories.tsx
autonomous: true
requirements: [QA-02]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Running the Storybook build produces a working static Storybook with zero errors"
    - "Each of the 6 named shared/ui primitives (EmptyState, ConfirmDialog, POSButton, DataTable, MoneyDisplay, MoneyInput) has a story that renders without error"
  artifacts:
    - path: ".storybook/main.ts"
      provides: "Storybook framework/stories-glob/addons config — the repo has never had this file (confirmed absent on disk, gitignored); it is a hard blocker for every other file in this plan"
    - path: ".storybook/preview.ts"
      provides: "Global decorators/parameters — imports Tailwind globals.css and the i18next singleton so translated component text resolves without a wrapper"
    - path: "src/shared/ui/EmptyState.stories.tsx"
      provides: "Default + action-variant stories for EmptyState"
    - path: "src/shared/ui/ConfirmDialog.stories.tsx"
      provides: "Default + destructive-variant stories for ConfirmDialog"
    - path: "src/shared/ui/POSButton.stories.tsx"
      provides: "Default + touchSize-variant stories for POSButton"
    - path: "src/shared/ui/DataTable.stories.tsx"
      provides: "Default + loading + empty-state stories for DataTable"
    - path: "src/shared/ui/MoneyDisplay.stories.tsx"
      provides: "Positive/negative/size-variant stories for MoneyDisplay"
    - path: "src/shared/ui/MoneyInput.stories.tsx"
      provides: "Default + labeled-variant stories for MoneyInput"
  key_links:
    - from: ".storybook/main.ts"
      to: "src/shared/ui/*.stories.tsx"
      via: "stories glob '../src/**/*.stories.@(ts|tsx)' picks up all 21 story files (15 existing + 6 new)"
      pattern: "stories:.*src/\\*\\*/\\*\\.stories"
---

<objective>
Close QA-02 by (1) hand-authoring the `.storybook/` config this repo has never actually had — despite 15 pre-existing `.stories.tsx` files and `package.json`/`vitest.config.ts` scripts that assume it exists — and (2) adding the 6 missing stories for `EmptyState`, `ConfirmDialog`, `POSButton`, `DataTable`, `MoneyDisplay`, `MoneyInput`. `npm run build-storybook` is the concrete automated gate: it must exit 0 with all 21 stories (15 existing + 6 new) included.

Purpose: Storybook coverage for the 6 core shared/ui primitives currently missing it, and — as a prerequisite discovered by research — a working Storybook build at all. Per D-07, this is the described mechanical approach with no changes requested: 6 new `.stories.tsx` files next to their components, following existing story conventions.
Output: `.storybook/main.ts`, `.storybook/preview.ts`, 6 new `.stories.tsx` files, a green `npm run build-storybook`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@package.json
@vitest.config.ts
@src/shared/ui/Button.stories.tsx
@src/shared/ui/EmptyState.tsx
@src/shared/ui/ConfirmDialog.tsx
@src/shared/ui/POSButton.tsx
@src/shared/ui/DataTable.tsx
@src/shared/ui/MoneyDisplay.tsx
@src/shared/ui/MoneyInput.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold .storybook/main.ts + preview.ts (Wave-0 gap)</name>
  <files>.storybook/main.ts, .storybook/preview.ts</files>
  <read_first>
    - package.json devDependencies (verify exact installed versions: `@storybook/react-vite@^10.3.5`, `@storybook/addon-a11y@^10.3.5`, `@storybook/addon-docs@^10.3.5`, `@storybook/addon-onboarding@^10.3.5`, `@chromatic-com/storybook@^5.1.2`, `storybook@^10.3.5`)
    - vitest.config.ts:1-15 (`configDir: path.join(dirname, '.storybook')` — confirms the opt-in Vitest storybook project already expects this exact path)
    - .gitignore:59 (`.storybook/` is gitignored — this is intentional per the repo's existing convention; do not remove that line)
    - src/app/globals.css (Tailwind entry point, for preview.ts to import)
    - src/shared/lib/i18n/index.ts (the i18next singleton — `i18n.use(initReactI18next).init(...)` runs at module scope; importing it for its side effect is the same technique `src/shared/lib/test-setup.ts` already uses for Vitest, no `<I18nextProvider>` wrapper needed)
  </read_first>
  <action>
    Hand-author `.storybook/main.ts` (do NOT run `npx storybook init` — Anti-Pattern per research, it may prompt interactively or duplicate the 15 existing `.stories.tsx` files' assumptions): export a config object with `framework: '@storybook/react-vite'`, `stories: ['../src/**/*.stories.@(ts|tsx)']`, and `addons` listing `@storybook/addon-a11y`, `@storybook/addon-docs`, `@storybook/addon-onboarding`, `@chromatic-com/storybook` (matching the exact addon set already in `package.json` devDependencies — do not add addons that aren't installed).

    Hand-author `.storybook/preview.ts`: a default-exported `Preview` object with `parameters` (empty or minimal `controls.matchers` is fine) and two top-of-file side-effect imports — `import '../src/app/globals.css';` (Tailwind) and `import '../src/shared/lib/i18n/index.ts';` (i18next singleton, so every component using `useTranslation` resolves real strings without a provider wrapper, mirroring `src/shared/lib/test-setup.ts`'s approach).
  </action>
  <verify>
    <automated>npm run build-storybook</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build-storybook` exits 0 against the 15 pre-existing `.stories.tsx` files alone (before this task's later tasks add the 6 new ones) — proves the config itself is correct, isolating the Wave-0 gap fix from new story content
    - `.storybook/main.ts` and `.storybook/preview.ts` exist and reference only addon packages present in `package.json`
  </acceptance_criteria>
  <done>`npm run build-storybook` succeeds; `storybook-static/` is produced with zero errors.</done>
  <reversibility rating="reversible">Dev-tooling config only — never ships in the Tauri bundle (per RESEARCH.md's Architectural Responsibility Map); trivially deletable/re-authorable.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add stories for EmptyState, ConfirmDialog, POSButton</name>
  <files>src/shared/ui/EmptyState.stories.tsx, src/shared/ui/ConfirmDialog.stories.tsx, src/shared/ui/POSButton.stories.tsx</files>
  <read_first>
    - src/shared/ui/Button.stories.tsx:1-36 (the exact story shape to follow: `title: 'Shared/UI/<Name>'`, `component`, `parameters: { layout: 'centered' }`, `tags: ['autodocs']`, `argTypes`, `export const Default`)
    - src/shared/ui/EmptyState.tsx:12-26 (props: `icon: LucideIcon` (required, e.g. import `Truck` or `FileQuestion` from `lucide-react`), `title: string`, `description?: string`, `action?: { label: string; onClick: () => void }`)
    - src/shared/ui/ConfirmDialog.tsx:25-50 (props: `open: boolean`, `title: string`, `description: string`, `confirmLabel?`, `cancelLabel?`, `variant?: 'default' | 'destructive'`, `onConfirm`, `onCancel`, `isLoading?`)
    - src/shared/ui/POSButton.tsx:12-15,37-59 (extends `ButtonProps`; `touchSize?: 'default' | 'large' | 'xl'`)
  </read_first>
  <action>
    `EmptyState.stories.tsx`: `title: 'Shared/UI/EmptyState'`, `component: EmptyState`. `Default` story with `icon` (import a lucide icon, e.g. `FileQuestion`), `title: 'No results'`, `description: 'Try adjusting your filters'`. A second `WithAction` story adding `action: { label: 'Add item', onClick: () => {} }`.

    `ConfirmDialog.stories.tsx`: `title: 'Shared/UI/ConfirmDialog'`, `component: ConfirmDialog`. `Default` story with `open: true`, `title: 'Confirm action'`, `description: 'Are you sure?'`, `onConfirm: () => {}`, `onCancel: () => {}`. A `Destructive` story adding `variant: 'destructive'`, `confirmLabel: 'Delete'`.

    `POSButton.stories.tsx`: `title: 'Shared/UI/POSButton'`, `component: POSButton`, `argTypes.touchSize` as a `select` control with options `['default', 'large', 'xl']` (mirroring `Button.stories.tsx`'s `argTypes.variant` shape). `Default` story with `children: 'Process Payment'`, `touchSize: 'default'`. An `XLarge` story with `touchSize: 'xl'`.

    Every story file must satisfy `Meta<typeof Component>` (`satisfies` keyword, matching `Button.stories.tsx`'s pattern exactly) so TypeScript catches any prop mismatch.
  </action>
  <verify>
    <automated>npm run build-storybook</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build-storybook` exits 0 with these 3 new files plus the 15 existing ones (18 total)
    - `npm run typecheck` remains clean (each `meta` object type-checks against its component's real prop signature)
  </acceptance_criteria>
  <done>3 of 6 required stories exist and build cleanly.</done>
  <reversibility rating="reversible">Dev-only story files, additive, no runtime app impact.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add stories for DataTable, MoneyDisplay, MoneyInput — full QA-02 verification</name>
  <files>src/shared/ui/DataTable.stories.tsx, src/shared/ui/MoneyDisplay.stories.tsx, src/shared/ui/MoneyInput.stories.tsx</files>
  <read_first>
    - src/shared/ui/DataTable.tsx:29-72 (props: `columns: ColumnDef<T>[]`, `data: T[]`, `isLoading?`, `emptyState?`, `searchable?`, `searchPlaceholder?` — generic component, story needs a concrete row type)
    - src/shared/ui/MoneyDisplay.tsx:12-21,42-66 (props: `amount: number`, `negative?: boolean`, `size?: 'sm' | 'md' | 'lg' | 'xl'`)
    - src/shared/ui/MoneyInput.tsx:16-31,64-72 (props: `value: number`, `onChange: (value: number) => void`, `label?`, `placeholder?`, `disabled?`)
  </read_first>
  <action>
    `DataTable.stories.tsx`: `title: 'Shared/UI/DataTable'`, `component: DataTable`. Define a small local `type Row = { id: string; name: string; amount: number }` and 3 sample rows plus a `columns: ColumnDef<Row>[]` with `name`/`amount` accessors, matching `AuditLogTable.tsx`'s `ColumnDef` shape. `Default` story with those `columns`/`data`. A `Loading` story with `isLoading: true`, `data: []`. An `Empty` story with `data: []`, `isLoading: false`.

    `MoneyDisplay.stories.tsx`: `title: 'Shared/UI/MoneyDisplay'`, `component: MoneyDisplay`. `Positive` story `{ amount: 42.5 }`. `Negative` story `{ amount: -12.99 }`. `Large` story `{ amount: 199.99, size: 'xl' }`.

    `MoneyInput.stories.tsx`: `title: 'Shared/UI/MoneyInput'`, `component: MoneyInput`. `Default` story with `value: 0, onChange: () => {}, placeholder: '0.00'`. `Labeled` story adding `label: 'Amount'`.

    After all three files exist, run the full verification: `npm run build-storybook` must succeed with all 21 stories (15 pre-existing + 6 new from this plan) and zero errors — this satisfies QA-02's ROADMAP acceptance criterion in full.
  </action>
  <verify>
    <automated>npm run build-storybook</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build-storybook` exits 0 with all 21 `.stories.tsx` files present, zero errors in the build log
    - `npm run typecheck` and `npm run lint` remain clean
  </acceptance_criteria>
  <done>All 6 required Storybook stories (EmptyState, ConfirmDialog, POSButton, DataTable, MoneyDisplay, MoneyInput) exist and the full Storybook static build succeeds with zero errors — QA-02 satisfied.</done>
  <reversibility rating="reversible">Dev-only story files, additive, no runtime app impact.</reversibility>
</task>

</tasks>

<verification>
`npm run build-storybook` exits 0 with 21 total stories. `npm run typecheck && npm run lint` clean.
</verification>

<success_criteria>
QA-02 (ROADMAP Success Criterion 2) satisfied: Storybook stories exist and render without error for EmptyState, ConfirmDialog, POSButton, DataTable, MoneyDisplay, and MoneyInput, verified by the automated Storybook build check.
</success_criteria>

<output>
Create `.planning/phases/10-quality-debt-ops-documentation/10-02-SUMMARY.md` when done
</output>
