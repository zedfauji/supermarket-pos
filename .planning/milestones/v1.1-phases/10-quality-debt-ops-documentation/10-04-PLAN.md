---
phase: 10-quality-debt-ops-documentation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/backup-db.sh
  - docs/database-backup-and-disaster-recovery.md
autonomous: true
requirements: [OPS-02]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A documented database backup/disaster-recovery plan exists, stating actual verified facts about this project's current hosting posture rather than assuming a hosting decision that hasn't been made"
    - "A runnable pg_dump backup script exists for the self-hosted fallback path, ready to be wired into a cron job later"
    - "The doc explicitly distinguishes itself from the existing settings-only BackupSettingsTab feature so nobody mistakes that feature for full-database DR"
  artifacts:
    - path: "scripts/backup-db.sh"
      provides: "pg_dump wrapper reading DATABASE_URL from the environment, guarded by a command -v pg_dump check, writing timestamped dumps to backups/"
    - path: "docs/database-backup-and-disaster-recovery.md"
      provides: "Two-section (self-hosted / Supabase Cloud) DR doc grounded in supabase/config.toml's D-06 comment and settings-backup's verified 5-table scope"
  key_links:
    - from: "docs/database-backup-and-disaster-recovery.md"
      to: "scripts/backup-db.sh"
      via: "doc references the script by path as the self-hosted mechanism"
      pattern: "scripts/backup-db\\.sh"
---

<objective>
Close OPS-02: document the store's actual database backup/disaster-recovery posture (currently silently dependent on an unconfirmed default) and ship a runnable `pg_dump` script for the self-hosted fallback path. Production hosting (self-hosted Docker stack vs. Supabase Cloud) is genuinely undecided per D-08 — the doc covers both scenarios rather than picking one, and explicitly states the existing `BackupSettingsTab`/`settings-backup` feature is NOT a substitute (it backs up 5 config-ish tables only, never `orders`/`payments`/`inventory`/`suppliers`/`shipments`/`caja_sessions`/`profiles`/`audit_logs`).

Purpose: Production data-loss protection should be an explicit, verified statement, not a silent assumption.
Output: `scripts/backup-db.sh`, `docs/database-backup-and-disaster-recovery.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@supabase/config.toml
@supabase/functions/settings-backup/index.ts
@scripts/setup-ubuntu.sh
@src/widgets/SettingsTabsPanel/tabs/BackupSettingsTab.tsx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `scripts/backup-db.sh` execution environment → committed repo files | The script must never cause a real Postgres connection string (with embedded credentials) to land in a committed file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Information Disclosure | `scripts/backup-db.sh` | medium | mitigate | Script reads `DATABASE_URL` from the environment only (`: "${DATABASE_URL:?...}"` guard, no hardcoded default connection string with credentials); the DR doc explicitly instructs never to commit a real connection string, consistent with this repo's existing `.env.local`-based secret convention |
| T-10-02 | Information Disclosure | `backups/*.dump` output directory | low | accept | Dump files land in a local `backups/` directory outside version control by convention (not gitignored by this plan, since the directory doesn't exist yet and is a deploy-time/operator concern) — the doc calls this out explicitly rather than silently assuming it |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: scripts/backup-db.sh — pg_dump wrapper for the self-hosted fallback path</name>
  <files>scripts/backup-db.sh</files>
  <read_first>
    - scripts/setup-ubuntu.sh:1-25 (header/guard style to mirror: `#!/usr/bin/env bash`, `set -euo pipefail`, `cd "$(dirname "$0")/.."`, a fail-fast guard-and-exit pattern rather than silent failure)
    - supabase/config.toml:1-10 (confirms this is a self-hosted, non-Cloud-linked project — the script targets the self-hosted fallback path per D-09, not a Cloud-specific mechanism)
  </read_first>
  <action>
    Create `scripts/backup-db.sh`: `#!/usr/bin/env bash`, `set -euo pipefail`. Guard: `: "${DATABASE_URL:?Set DATABASE_URL to the target Postgres connection string}"` (fails fast with a clear message if unset, never a hardcoded fallback connection string). Add a `command -v pg_dump >/dev/null 2>&1 || { echo "scripts/backup-db.sh: pg_dump not found. Install postgresql-client (apt) or the Supabase CLI." >&2; exit 1; }` guard mirroring `setup-ubuntu.sh`'s dependency-check convention. `cd "$(dirname "$0")/.."`, `mkdir -p backups`, compute `timestamp=$(date -u +%Y%m%dT%H%M%SZ)`, run `pg_dump "$DATABASE_URL" --format=custom --file="backups/backup-${timestamp}.dump"`, echo the output path on success. Make the file executable (`chmod +x scripts/backup-db.sh`). Do NOT wire this into a cron job or any CI workflow — that is an explicit deploy-time decision left out of scope per D-09.
  </action>
  <verify>
    <automated>bash -n scripts/backup-db.sh && test -x scripts/backup-db.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/backup-db.sh` reports no syntax errors
    - `test -x scripts/backup-db.sh` succeeds (executable bit set)
    - Running the script with `DATABASE_URL` unset exits non-zero with the guard message, not a raw `pg_dump` error
  </acceptance_criteria>
  <done>A runnable, guarded pg_dump backup script exists at scripts/backup-db.sh.</done>
  <reversibility rating="reversible">Standalone script, not wired into any automation; deletable/editable with zero blast radius.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: docs/database-backup-and-disaster-recovery.md — two-scenario DR doc</name>
  <files>docs/database-backup-and-disaster-recovery.md</files>
  <read_first>
    - supabase/config.toml:5-10 (D-06 comment, quote verbatim: self-hosted Supabase stack on localhost:8000, no linked Supabase Cloud `project_id` — "the prior value ('shsrhxleopmovzpzqmex') was the live production bar project and must never be restored here"; local CLI dev stack's Postgres runs on port 54322 per `[db] port = 54322` in the same file — cite both facts precisely, do not conflate the local-dev CLI stack's ports with an as-yet-undecided production self-hosted deployment's ports)
    - supabase/functions/settings-backup/index.ts:68-86 (verified scope: `settings`, `categories`, `products`, `modifiers`, `product_modifiers` only — 5 tables, none of which are `orders`/`payments`/`inventory`/`suppliers`/`shipments`/`caja_sessions`/`profiles`/`audit_logs`)
    - src/widgets/SettingsTabsPanel/tabs/BackupSettingsTab.tsx (the UI surface for that settings-only feature, so the doc can name it precisely and say plainly it is not a substitute)
    - scripts/backup-db.sh (from Task 1, to reference by path)
  </read_first>
  <action>
    Write `docs/database-backup-and-disaster-recovery.md` with these sections: (1) a short intro stating this doc's scope is full-database DR (orders, payments, inventory, suppliers, shipments, caja sessions, profiles, audit logs — everything), explicitly distinct from the existing `BackupSettingsTab`/`settings-backup` feature, which the doc must name and state backs up only `settings`/`categories`/`products`/`modifiers`/`product_modifiers` (5 tables) and is NOT a substitute for this document's scope (D-10); (2) a "Production hosting: undecided" callout stating self-hosted vs. Supabase Cloud is not yet decided (cite `supabase/config.toml`'s D-06 comment verbatim) and that this doc intentionally covers both paths rather than assuming one; (3) a "Self-hosted" section: state plainly that no managed point-in-time-recovery (PITR) exists today for a self-hosted stack, the recommended mechanism is the `pg_dump`-based `scripts/backup-db.sh` (link/reference it by path), and that scheduling it (cron, systemd timer, etc.) is a deploy-time decision not yet made; (4) a "Supabase Cloud" section: state that IF Supabase Cloud is chosen, PITR coverage depends on the project's paid tier, and mark any specific tier name or day-count as `[ASSUMED — verify against Supabase's current pricing page before relying on it]` rather than stating a specific tier as fact (do not fabricate a tier/day-count — this project's research explicitly flagged this as unverified, Assumption A3); (5) a short "Restore" note: `pg_restore` is the counterpart to the script's `--format=custom` dump, and restoring should always be tested against a non-production instance first.
  </action>
  <verify>
    <automated>test -f docs/database-backup-and-disaster-recovery.md && grep -q "BackupSettingsTab\|settings-backup" docs/database-backup-and-disaster-recovery.md && grep -qi "self-hosted" docs/database-backup-and-disaster-recovery.md && grep -qi "supabase cloud" docs/database-backup-and-disaster-recovery.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/database-backup-and-disaster-recovery.md` exists and contains both "self-hosted" and "Supabase Cloud" sections (case-insensitive grep)
    - The doc names `BackupSettingsTab` or `settings-backup` and states it is not a substitute for full-database DR
    - No specific Supabase Cloud tier/day-count is stated as unqualified fact — any such claim is marked `[ASSUMED — verify against Supabase's current pricing page before relying on it]`
    - The doc references `scripts/backup-db.sh` by path
  </acceptance_criteria>
  <done>A two-scenario (self-hosted / Supabase Cloud) DR doc exists, grounded in verified repo facts, distinguishing itself from the settings-only backup feature — OPS-02 satisfied.</done>
  <reversibility rating="reversible">Documentation only; no code/schema/config change.</reversibility>
</task>

</tasks>

<verification>
`test -f docs/database-backup-and-disaster-recovery.md && test -x scripts/backup-db.sh`. Doc content grep-checks above pass. `bash -n scripts/backup-db.sh` clean.
</verification>

<success_criteria>
OPS-02 (ROADMAP Success Criterion 5) satisfied: a database backup/disaster-recovery plan document exists in the repo, confirming the Supabase project tier's actual PITR/backup coverage — verified by direct inspection (doc + config fact, not E2E-testable, consistent with this project's established treatment of OPS-01/OPS-02 per STATE.md's decision log).
</success_criteria>

<output>
Create `.planning/phases/10-quality-debt-ops-documentation/10-04-SUMMARY.md` when done
</output>
