---
phase: 10-quality-debt-ops-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widgets/SupplierListPanel.tsx
  - e2e/57-suppliers-loading-error.spec.ts
autonomous: true
requirements: [QA-01]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "A user opening /suppliers while the network is slow sees a visible loading skeleton, not a blank panel"
    - "A user opening /suppliers when the fetch fails sees a visible, accessible error message, not a silently blank panel"
  artifacts:
    - path: "src/widgets/SupplierListPanel.tsx"
      provides: "isLoading skeleton + resultError alert wired ahead of the existing supplier <ul>"
    - path: "e2e/57-suppliers-loading-error.spec.ts"
      provides: "Playwright E2E proof of both states via page.route interception of the suppliers REST call"
  key_links:
    - from: "src/widgets/SupplierListPanel.tsx"
      to: "src/entities/supplier/model/queries.ts"
      via: "useSuppliers() destructure now includes isLoading and resultError (already returned by the query, unchanged)"
      pattern: "useSuppliers\\(\\)"
---

<objective>
Wire the Suppliers list panel to show a loading skeleton while `useSuppliers()` is fetching, and a `role="alert"` error message when the fetch fails — matching the exact pattern already established in `InventoryPagePanel.tsx`, with zero query-layer changes. This is Phase 10's tracer: the thinnest, most self-contained requirement (QA-01), proving the "quality-debt UI wiring" pattern end-to-end (query state → conditional render → E2E proof) before the rest of the phase's plans build out from it.

Purpose: Close QA-01 — the Suppliers page currently renders blank during fetch/error instead of giving the user any signal.
Output: `SupplierListPanel.tsx` wired for loading/error, plus a new Playwright spec proving both states via network interception (no manual verification, per project policy).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/widgets/InventoryPagePanel.tsx
@src/entities/supplier/model/queries.ts
@src/shared/ui/LoadingSkeletons.tsx
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Suppliers loading + error states, wired end-to-end and E2E-proven</name>
  <files>src/widgets/SupplierListPanel.tsx, e2e/57-suppliers-loading-error.spec.ts</files>
  <read_first>
    - src/widgets/SupplierListPanel.tsx (current file — only destructures `{ data: suppliers }` from `useSuppliers()` at line 21; the `<ul>` list starts at line 83)
    - src/entities/supplier/model/queries.ts:73-79 (`useSuppliers()` already returns `isLoading`, `resultError`, `isEmpty` alongside `data` — zero changes needed here)
    - src/widgets/InventoryPagePanel.tsx:223-227 (the exact `resultError ? <p role="alert" className="text-sm text-destructive">{resultError.message}</p> : null` pattern to replicate verbatim, per D-06)
    - src/shared/ui/LoadingSkeletons.tsx:67-80 (`TableRowSkeleton({ columns, className })` — reuse, do not create a new skeleton component, per D-05)
    - e2e/18-modifier-notes-kds.spec.ts:81-110 (the `page.route('**/rest/v1/<table>*', ...)` interception pattern to mirror for `suppliers`)
    - e2e/helpers/auth.ts (`gotoAuthed`/`loginAs` signatures already used by every other spec)
  </read_first>
  <action>
    In `SupplierListPanel.tsx`: change the destructure at line 21 from `const { data: suppliers } = useSuppliers();` to `const { data: suppliers, isLoading, resultError } = useSuppliers();`. Add an `isLoading` early-return rendering 3 `<TableRowSkeleton columns={2} />` rows inside a `<div className="space-y-4">` wrapper, placed before the existing `if (suppliers?.length === 0)` empty-state branch (columns=2 matches the current row's two visual columns: name + action buttons). Inside the main `<section className="space-y-4">` return path, insert the `resultError ? <p className="text-sm text-destructive" role="alert">{resultError.message}</p> : null` block immediately after the opening `<section>` tag, above the `<POSButton>`/`<ul>` — do not alter the existing empty-state (`suppliers?.length === 0`) branch, which already works and is out of QA-01's scope per D-05/D-06/UI-SPEC. No changes to `useSuppliers()` itself.

    Create `e2e/57-suppliers-loading-error.spec.ts` with two tests inside a `test.describe('Suppliers loading/error states', ...)` block: (1) intercepts `page.route('**/rest/v1/suppliers*', ...)` to delay the response (`await new Promise(r => setTimeout(r, 3000)); await route.continue();`), navigates to `/suppliers` as a logged-in manager (`loginAs`/`gotoAuthed`), and asserts a `role="status"` skeleton element is visible before the delayed response resolves; (2) intercepts the same route to `route.fulfill({ status: 500, body: JSON.stringify({ message: 'Internal Server Error' }) })`, navigates to `/suppliers`, and asserts a `role="alert"` element is visible. Do not assert on exact `resultError.message` text (translated, brittle) — assert on the `role="alert"` element's presence/visibility only.
  </action>
  <verify>
    <automated>npx playwright test e2e/57-suppliers-loading-error.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/57-suppliers-loading-error.spec.ts` passes both tests headless
    - `npm run typecheck` and `npm run lint` remain clean after the edit
    - `SupplierListPanel.tsx`'s existing empty-state and CRUD behavior (create/edit/delete dialogs) is untouched — confirm by re-reading the diff contains only the destructure line, the new loading branch, and the new error `<p>` block
  </acceptance_criteria>
  <done>A slow suppliers fetch shows a skeleton; a failed suppliers fetch shows a `role="alert"` message; both are proven by a real headless Playwright run, not manual inspection.</done>
  <reversibility rating="reversible">UI-only wiring reusing existing components/patterns verbatim (D-05/D-06); no schema, contract, or query-layer change — trivially revertable.</reversibility>
</task>

</tasks>

<verification>
`npx playwright test e2e/57-suppliers-loading-error.spec.ts` passes headless. `npm run typecheck && npm run lint` clean.
</verification>

<success_criteria>
QA-01 (ROADMAP Success Criterion 1) satisfied: Suppliers page shows a visible loading indicator while fetching and a visible error state on fetch failure, verified by Playwright E2E mocking a slow/failed request.
</success_criteria>

<output>
Create `.planning/phases/10-quality-debt-ops-documentation/10-01-SUMMARY.md` when done
</output>
