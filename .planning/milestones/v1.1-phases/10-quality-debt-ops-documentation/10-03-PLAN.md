---
phase: 10-quality-debt-ops-documentation
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/features/checkout-sale/model/useCheckoutSale.test.ts
autonomous: true
requirements: [QA-04]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "useCheckoutSale's success and failure paths are exercised by a dedicated, automated Vitest test, not left uncovered"
  artifacts:
    - path: "src/features/checkout-sale/model/useCheckoutSale.test.ts"
      provides: "Vitest unit test mocking cartStore/staffStore/cajaStore/callProcessDirectSale/isOnline, covering offline, caja-closed, cash/card/split success, and edge-function-failure paths"
  key_links:
    - from: "src/features/checkout-sale/model/useCheckoutSale.test.ts"
      to: "src/features/checkout-sale/model/useCheckoutSale.ts"
      via: "renderHook(() => useCheckoutSale()) against the real hook, with its 3 store dependencies + edge-function call mocked at the exact module specifiers the hook itself imports"
      pattern: "renderHook\\(\\(\\) => useCheckoutSale\\(\\)\\)"
---

<objective>
Give `useCheckoutSale` (the direct-sale payment mutation hook) a dedicated Vitest unit test covering its success and failure paths — it currently has none. Since the hook already exists and works (it's exercised indirectly by E2E specs), this is a characterization test: write it, run it, and if a genuine hook bug surfaces, fix the hook minimally — do not weaken the test to make it pass.

Purpose: Close QA-04 — the payment mutation hook has no dedicated unit test. Per D-07, this follows the described mechanical approach with no changes requested: mirror `useProcessRefund.test.ts`'s mocking style, adjusted for `useCheckoutSale`'s three-Zustand-store dependency.
Output: `useCheckoutSale.test.ts`, green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/features/checkout-sale/model/useCheckoutSale.ts
@src/features/process-refund/model/useProcessRefund.test.ts
@src/features/scan-barcode-to-cart/model/useScanBarcodeToCart.test.ts
@src/entities/caja/model/store.ts
@src/entities/staff/model/store.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: useCheckoutSale.test.ts — success + failure paths</name>
  <files>src/features/checkout-sale/model/useCheckoutSale.test.ts</files>
  <read_first>
    - src/features/checkout-sale/model/useCheckoutSale.ts (full file, already read this session — imports `useCajaStore` from `@entities/caja`, `useStaffStore` from `@entities/staff`, `useCartStore` from `@entities/tab/model/cartStore`, `isOnline` from `@shared/lib/connectivity`, `callProcessDirectSale` from `@shared/lib/edge-function-contracts`; `submit()` returns `err(networkOfflineError())` when `!isOnline()`, `err({code:'CAJA_CLOSED',...})` when `!staff || !shift || !isCajaOpen || !caja`; `processCashPayment`/`processCardPayment` synthesize `err({code:'UNKNOWN_ERROR',...})` when the edge-function result lacks `paymentId`/`receiptData`; `processSplitPayment` synthesizes the same when it lacks `paymentGroupId`/`paymentIds`/`receiptData`)
    - src/features/process-refund/model/useProcessRefund.test.ts (overall file structure: `makeQueryClient`/`makeWrapper` helpers, `renderHook` + `QueryClientProvider` wrapper, `vi.clearAllMocks()` in `afterEach`)
    - src/features/scan-barcode-to-cart/model/useScanBarcodeToCart.test.ts:11-15 (the exact Zustand selector-passthrough `vi.mock` shape: `useXStore: (selector) => selector({ ...mockState })`)
    - src/entities/caja/model/store.ts:10-12 and src/entities/staff/model/store.ts:11-12 (confirmed field names: `currentCaja`, `isCajaOpen`, `currentStaff`, `currentShift`)
    - Confirmed this session: `@entities/staff`'s barrel (`src/entities/staff/index.ts`) exports `useStaffStore`, and `@entities/caja`'s barrel (`src/entities/caja/index.ts`) exports `useCajaStore` — these are the exact module specifiers `useCheckoutSale.ts` imports, so `vi.mock('@entities/staff', ...)` / `vi.mock('@entities/caja', ...)` (not the underlying `/model/store` paths) is what actually intercepts the hook's real calls
  </read_first>
  <behavior>
    - `submit()` returns `err(networkOfflineError())` (code `NETWORK_OFFLINE`) when the mocked `isOnline()` returns `false`, and `callProcessDirectSale` is never called in that case
    - `submit()` returns `err({code:'CAJA_CLOSED'})` when `isOnline()` is `true` but the mocked staff store has `currentStaff: null` (or `currentShift: null`, or the mocked caja store has `isCajaOpen: false` / `currentCaja: null`) — `callProcessDirectSale` is never called in that case either
    - Given `isOnline()` true, a valid staff/shift/caja, and `callProcessDirectSale` mocked to resolve `ok({ paymentId: 'p1', receiptData: { changeAmount: 0 } })`, calling `processors.processCashPayment(tabId, 10, 0, 10)` returns `ok({ paymentId: 'p1', changeAmount: 0, receiptData: {...} })`
    - Given the same setup, calling `processors.processCardPayment(tabId, 10, 0)` returns `ok({ paymentId: 'p1', receiptData: {...} })`
    - Given `callProcessDirectSale` mocked to resolve `ok({ paymentGroupId: 'g1', paymentIds: ['p1','p2'], receiptData: {...} })`, calling `processors.processSplitPayment(tabId, [{method:'cash',amount:5,tipAmount:0},{method:'card',amount:5,tipAmount:0}], 10)` returns `ok({ paymentGroupId: 'g1', paymentIds: ['p1','p2'], receipts: [{...}] })` (one receipt wrapped in an array — see the hook's D-09 comment)
    - Given `callProcessDirectSale` mocked to resolve `ok({})` (success envelope missing `paymentId`/`receiptData`), `processCashPayment` returns `err({code:'UNKNOWN_ERROR'})`, not a thrown exception
    - Given `callProcessDirectSale` mocked to resolve `err({code:'SUPABASE_ERROR', message:'...'})`, `processCashPayment` propagates that same `err` unchanged
  </behavior>
  <action>
    Create `useCheckoutSale.test.ts` mirroring `useProcessRefund.test.ts`'s file structure (imports, `describe`/`it`, `afterEach(() => vi.clearAllMocks())`). Mock 5 module specifiers at the top, matching exactly what `useCheckoutSale.ts` imports: `@entities/tab/model/cartStore` (`useCartStore` selector-passthrough, `items: []` by default), `@entities/staff` (`useStaffStore` selector-passthrough, mutable `currentStaff`/`currentShift` mock state), `@entities/caja` (`useCajaStore` selector-passthrough, mutable `currentCaja`/`isCajaOpen` mock state), `@shared/lib/edge-function-contracts` (`callProcessDirectSale: vi.fn()`), `@shared/lib/connectivity` (`isOnline: vi.fn(() => true)` by default, overridden per-test). Use `renderHook(() => useCheckoutSale())` (no QueryClientProvider needed — the hook itself uses no TanStack Query) and call `result.current.processors.processCashPayment(...)` / `processCardPayment(...)` / `processSplitPayment(...)` directly per the Behavior cases above, resetting each store mock's mutable state in `beforeEach`. If any assertion reveals the hook does not match its own documented behavior (a real bug, not a test-authoring mistake), fix `useCheckoutSale.ts` minimally and note the fix in the plan SUMMARY — do not adjust the test to paper over a real defect.
  </action>
  <verify>
    <automated>npx vitest run src/features/checkout-sale/model/useCheckoutSale.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/features/checkout-sale/model/useCheckoutSale.test.ts` passes with at least 7 test cases (one per Behavior bullet above)
    - `npm run typecheck` remains clean (mocked store shapes match the real store field names)
    - `npm run test` (full unit suite) still passes — no regression to `useProcessRefund.test.ts` or `useScanBarcodeToCart.test.ts` from shared-module mocking bleed
  </acceptance_criteria>
  <done>useCheckoutSale has a dedicated Vitest test exercising offline, caja-closed, cash/card/split success, and edge-function-failure paths — all green.</done>
  <reversibility rating="reversible">Pure test addition; if a hook bug is found and fixed, that fix is a small, isolated behavior correction with its own test as regression guard.</reversibility>
</task>

</tasks>

<verification>
`npx vitest run src/features/checkout-sale/model/useCheckoutSale.test.ts` green. `npm run test` (full unit suite) green.
</verification>

<success_criteria>
QA-04 (ROADMAP Success Criterion 4) satisfied: the payment mutation hook has a dedicated Vitest unit test exercising its success and failure paths.
</success_criteria>

<output>
Create `.planning/phases/10-quality-debt-ops-documentation/10-03-SUMMARY.md` when done
</output>
