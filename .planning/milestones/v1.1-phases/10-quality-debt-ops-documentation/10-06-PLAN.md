---
phase: 10-quality-debt-ops-documentation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widgets/PaymentPane/ui/PaymentPane.tsx
  - src/widgets/PaymentPane/ui/PaymentPane.test.tsx
  - src/widgets/StaffDashboard/StaffDashboard.tsx
  - src/widgets/StaffDashboard/StaffDashboard.test.tsx
  - src/shared/lib/i18n/locales/en-US/wPanels.json
  - src/shared/lib/i18n/locales/es-MX/wPanels.json
  - e2e/58-entity-id-crosslink.spec.ts
autonomous: true
requirements: [QA-03]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Visiting /payments?id={paymentId} filters the payment list down to that ID"
    - "Visiting /staff?id={staffId} scrolls to and visually highlights that staff member's row"
  artifacts:
    - path: "src/widgets/PaymentPane/ui/PaymentPane.tsx"
      provides: "A client-side ID filter box (SearchInput) above PaymentHistoryList, seeded from useSearchParams().get('id')"
    - path: "src/widgets/StaffDashboard/StaffDashboard.tsx"
      provides: "useSearchParams().get('id') drives DataTable's existing getRowClassName highlight + a scrollIntoView effect, without touching DataTable's internal search state"
    - path: "e2e/58-entity-id-crosslink.spec.ts"
      provides: "Playwright proof that both query-param-driven behaviors work end-to-end"
  key_links:
    - from: "src/widgets/PaymentPane/ui/PaymentPane.tsx"
      to: "src/entities/payment (usePayments)"
      via: "client-side Array.prototype.filter on the already-fetched payments array, matched against the trimmed ?id= value — never sent to Supabase as a query"
      pattern: "payments\\.filter\\("
    - from: "src/widgets/StaffDashboard/StaffDashboard.tsx"
      to: "src/shared/ui/DataTable.tsx"
      via: "existing getRowClassName prop (already supported, unused elsewhere for this purpose) — no DataTable.tsx API change (Pitfall 4: its internal search state cannot be externally controlled)"
      pattern: "getRowClassName="
---

<objective>
Give `/payments` and `/staff` the read side of QA-03's cross-linking: reading a `?id=` query param to filter (Payments) or scroll+highlight (Staff) the matching record. Neither widget imports `EntityIdCell` — this is independent, parallel-safe foundation work that a later, dependent plan's `EntityIdCell`-driven links will point at. This is the first use of `useSearchParams` anywhere in this codebase (RESEARCH.md Pattern 5) — both widgets currently have zero query-param handling.

Purpose: Foundation for QA-03 (D-03/D-04) — the destination side of the payment/tab/staff navigation links.
Output: `PaymentPane.tsx` filter, `StaffDashboard.tsx` scroll+highlight, both E2E-proven directly (no dependency on EntityIdCell existing yet).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/shared/ui/SearchInput.tsx
@src/shared/ui/DataTable.tsx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser URL `?id=` query param → `useSearchParams().get('id')` | Untrusted client-controlled input entering both widgets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Tampering / Information Disclosure | `PaymentPane.tsx`'s `?id=` filter | low | mitigate | The value is only ever used for `.trim()` + `String.prototype.includes()` comparison against the already-fetched, RBAC-scoped `payments` array (never interpolated into a Supabase query string or RPC call) — no injection surface. React's default JSX escaping covers any incidental render of the raw value; no `dangerouslySetInnerHTML` is introduced |
| T-10-06 | Tampering | `StaffDashboard.tsx`'s `?id=` row-match | low | accept | Same treatment: `===` comparison against `row.staff.id`, never rendered as raw HTML, never sent to a query |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: PaymentPane.tsx — ID filter seeded from ?id=</name>
  <files>src/widgets/PaymentPane/ui/PaymentPane.tsx, src/widgets/PaymentPane/ui/PaymentPane.test.tsx, src/shared/lib/i18n/locales/en-US/wPanels.json, src/shared/lib/i18n/locales/es-MX/wPanels.json</files>
  <read_first>
    - src/widgets/PaymentPane/ui/PaymentPane.tsx (full file, already read this session — `PaymentHistoryList` renders payments as plain `<div data-testid="payment-row-{id}">` rows, NOT `DataTable`; `usePayments()` is called inside `PaymentHistoryList`)
    - src/shared/ui/SearchInput.tsx:16-27,48-54 (controlled `value`/`onChange` component — reuse directly, do not build a new filter input)
    - src/widgets/PaymentPane/ui/PaymentPane.test.tsx (confirmed this session: 8 call sites of `renderWithProviders(<PaymentPane />)` with NO Router wrapper — adding `useSearchParams()` to `PaymentPane.tsx` will break every one of them with "useSearchParams() may be used only in the context of a &lt;Router&gt;")
    - src/widgets/CajaDashboard/CajaDashboard.test.tsx:7,167-170 (the exact fix pattern: `import { MemoryRouter } from 'react-router-dom';` + `renderWithProviders(<MemoryRouter><Component /></MemoryRouter>)`)
    - src/shared/lib/i18n/locales/en-US/wPanels.json (existing `paymentPane.*` key group, to see where a new `filterByIdPlaceholder` key fits)
  </read_first>
  <action>
    In `PaymentPane.tsx`: import `useSearchParams` from `react-router-dom` and `SearchInput` from `@shared/ui/SearchInput`. Inside `PaymentHistoryList`, add `const [searchParams] = useSearchParams();` and `const [filterValue, setFilterValue] = useState(() => (searchParams.get('id') ?? '').trim());` — seed once on mount only, do not re-sync on every searchParams change (the user may then type/clear the filter freely). Render `<SearchInput value={filterValue} onChange={setFilterValue} placeholder={t('wPanels:filterByIdPlaceholder')} />` above the existing payments list. Compute `const visiblePayments = filterValue ? payments.filter(p => p.id.includes(filterValue.trim())) : payments;` and map over `visiblePayments` instead of `payments` in the render — keep every existing button/behavior (`RefundButton`, `EditTicketButton`, etc.) unchanged, just filter the source array. If `visiblePayments.length === 0` while `payments.length > 0` (filter yields zero matches), fall through to the SAME existing "no payment records" empty copy already used when `payments` itself is empty (per UI-SPEC's "zero-one-many" row — reuses the existing empty-state pattern, no new copy).

    Add `filterByIdPlaceholder` to `wPanels.json` (both locales): en "Filter by ID…", es "Filtrar por ID…".

    Fix `PaymentPane.test.tsx`: add `import { MemoryRouter } from 'react-router-dom';` at the top, then wrap every `renderWithProviders(<PaymentPane />)` call site with `<MemoryRouter><PaymentPane /></MemoryRouter>` (use `replace_all` on the exact string `renderWithProviders(<PaymentPane />)` → `renderWithProviders(<MemoryRouter><PaymentPane /></MemoryRouter>)` across all 8 occurrences). Add one new test case: render with `<MemoryRouter initialEntries={['/payments?id=<a-payment-id-from-the-mocked-list>']}>`, assert only the matching `data-testid="payment-row-{id}"` is visible and other seeded rows are not.
  </action>
  <verify>
    <automated>npx vitest run src/widgets/PaymentPane/ui/PaymentPane.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/widgets/PaymentPane/ui/PaymentPane.test.tsx` passes, including the new ?id= filter test case
    - All 8 pre-existing `PaymentPane.test.tsx` cases still pass (Router-wrap fix did not change their behavior)
    - `npm run typecheck && npm run lint` clean
  </acceptance_criteria>
  <done>Visiting /payments?id=X seeds the filter box to X and shows only the matching payment row(s); the filter box remains freely editable afterward.</done>
  <reversibility rating="reversible">Additive client-side filter, no new backend query; reusing existing SearchInput and payments data already fetched.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: StaffDashboard.tsx — scroll+highlight seeded from ?id=</name>
  <files>src/widgets/StaffDashboard/StaffDashboard.tsx, src/widgets/StaffDashboard/StaffDashboard.test.tsx</files>
  <read_first>
    - src/widgets/StaffDashboard/StaffDashboard.tsx (full file, already read this session — `DataTable` is rendered with `searchable` at ~line 204-210; `columns` is a `useMemo<ColumnDef<StaffShiftRow>[]>`)
    - src/shared/ui/DataTable.tsx:29-54,204-208 (`getRowClassName?: (row: T) => string | undefined` prop already exists and is passed through to each `<TableRow className={cn(..., getRowClassName?.(row.original))}>` — reuse this, do NOT add a controlled `value` prop to `DataTable`'s internal `globalFilter` state, per Pitfall 4)
    - src/widgets/StaffDashboard/StaffDashboard.test.tsx (confirmed this session: 3 call sites of `renderWithProviders(<StaffDashboard />)` with NO Router wrapper — same regression risk as PaymentPane.test.tsx, same fix)
  </read_first>
  <action>
    In `StaffDashboard.tsx`: import `useSearchParams` from `react-router-dom`. Add `const [searchParams] = useSearchParams();` and `const targetStaffId = searchParams.get('id');`. Pass `getRowClassName={row => (targetStaffId && row.staff.id === targetStaffId ? 'bg-accent/40' : undefined)}` to the existing `<DataTable>` element (do not modify `DataTable.tsx` itself). Add a `useEffect` that, when `targetStaffId` is set and `rows` contains a matching entry, scrolls the matching row into view — the simplest correct approach: after render, `document.querySelector('[data-state]')`-style row targeting is fragile; instead add a `ref` callback on each `<TableRow>`... since `DataTable.tsx` itself is not being modified, achieve the scroll via `document.querySelector` scoped to a stable attribute already present on table rows (`data-state` is not stable enough) — simplest reliable option: wrap the `<DataTable>` in a container `<div ref={containerRef}>` and, once `targetStaffId` matches a row, call `containerRef.current?.querySelector('tr.bg-accent\\/40')?.scrollIntoView({ behavior: 'smooth', block: 'center' })` inside the `useEffect` (depends on `[targetStaffId, rows]`) — this only runs when a highlight class is actually present, so it never scrolls on a false match.

    Fix `StaffDashboard.test.tsx`: add `import { MemoryRouter } from 'react-router-dom';`, wrap all 3 `renderWithProviders(<StaffDashboard />)` call sites in `<MemoryRouter>` (`replace_all`). Add one new test case: render with `<MemoryRouter initialEntries={['/staff?id=<a-staff-id-from-the-mocked-list>']}>`, assert the matching row has the highlight class (query the row by staff name text, then check `closest('tr')` has `bg-accent/40` in its class list).
  </action>
  <verify>
    <automated>npx vitest run src/widgets/StaffDashboard/StaffDashboard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/widgets/StaffDashboard/StaffDashboard.test.tsx` passes, including the new ?id= highlight test case
    - All 3 pre-existing `StaffDashboard.test.tsx` cases still pass
    - `DataTable.tsx` itself is NOT modified (grep the diff — zero changes to `src/shared/ui/DataTable.tsx`)
    - `npm run typecheck && npm run lint` clean
  </acceptance_criteria>
  <done>Visiting /staff?id=X highlights the matching staff row and scrolls it into view, without any change to DataTable's public API or internal search state.</done>
  <reversibility rating="reversible">Additive read-only highlight; DataTable itself untouched, so this is trivially removable with no ripple effect on other DataTable consumers.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 3: E2E proof — both query-param behaviors, driven directly (not via a link click yet)</name>
  <files>e2e/58-entity-id-crosslink.spec.ts</files>
  <read_first>
    - e2e/helpers/auth.ts (`gotoAuthed`/`loginAs` signatures)
    - e2e/helpers/supabase.ts (`getServiceClient`, seeding conventions used by other specs to create a known payment/staff row for the URL param)
    - e2e/35-refund.spec.ts (an example of seeding a paid tab + payment directly via the service-role client, for a stable `payment.id` to navigate with)
  </read_first>
  <action>
    Create `e2e/58-entity-id-crosslink.spec.ts` with a `test.describe('Entity ID cross-link read side (D-03/D-04)', ...)` block and two tests: (1) seed a payment via the service-role client (or reuse an existing seeding helper), log in as manager, `page.goto('/payments?id=' + paymentId)`, and assert `page.getByTestId('payment-row-' + paymentId)` is visible while asserting the filter input's value equals the id (or a distinguishing substring) — confirms Task 1's seeding; (2) log in as manager, navigate to `/staff`, read one real staff id from the rendered table (e.g. via the service-role client's `profiles` table), then `page.goto('/staff?id=' + staffId)` and assert the corresponding row has entered view (use `expect(locator).toBeInViewport()` or assert the highlighted row's `class` attribute contains `bg-accent`) — confirms Task 2's highlight. Both tests navigate directly by URL — they do NOT yet click a link from AuditLogTable/EditHistoryTable (that full click-through E2E lives in the dependent wiring plan, once EntityIdCell is composed into those tables).
  </action>
  <verify>
    <automated>npx playwright test e2e/58-entity-id-crosslink.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/58-entity-id-crosslink.spec.ts` passes both tests headless
  </acceptance_criteria>
  <done>Both /payments?id= filtering and /staff?id= highlighting are proven end-to-end via direct URL navigation.</done>
  <reversibility rating="reversible">New, additive E2E spec; no production code touched.</reversibility>
</task>

</tasks>

<verification>
`npx vitest run src/widgets/PaymentPane/ui/PaymentPane.test.tsx src/widgets/StaffDashboard/StaffDashboard.test.tsx` green. `npx playwright test e2e/58-entity-id-crosslink.spec.ts` green. `npm run typecheck && npm run lint` clean.
</verification>

<success_criteria>
The destination half of QA-03's D-03/D-04 cross-linking works and is E2E-proven, independent of whether the source-side links exist yet.
</success_criteria>

<output>
Create `.planning/phases/10-quality-debt-ops-documentation/10-06-SUMMARY.md` when done
</output>
