---
phase: 10-quality-debt-ops-documentation
plan: 07
type: execute
wave: 2
depends_on: ["10-05", "10-06"]
files_modified:
  - src/widgets/AuditLogTable/AuditLogTable.tsx
  - src/widgets/AuditLogTable/AuditLogTable.test.tsx
  - src/widgets/EditHistoryTable/EditHistoryTable.tsx
  - src/widgets/DeletionsPostCloseReport/DeletionsPostCloseReport.tsx
  - src/widgets/DeletionsPreSendPanel/DeletionsPreSendPanel.tsx
  - src/widgets/DeletionsPreSendPanel/DeletionsPreSendPanel.test.tsx
  - src/shared/lib/i18n/locales/en-US/wAdmin.json
  - src/shared/lib/i18n/locales/es-MX/wAdmin.json
  - e2e/38-audit-logs.spec.ts
  - e2e/47-edit-paid-tab.spec.ts
autonomous: true
requirements: [QA-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Audit Log shows a copyable/clickable entity ID column that did not exist before this phase"
    - "Edit History's existing truncated-ID ticket column is now copyable and clickable (it was previously plain text)"
    - "Reports' two Deletions surfaces (post-close tabId, pre-send orderId) both show the ID cross-linking treatment — not just the one the UI-SPEC named by name"
    - "Clicking a payment/tab/staff entity-ID link actually navigates to a filtered/highlighted destination page, proven end-to-end"
  artifacts:
    - path: "src/widgets/AuditLogTable/AuditLogTable.tsx"
      provides: "New entityId ColumnDef using EntityIdCell, added without disturbing the existing action/entityType/actor/createdAt/source columns or the sr-only diff-trigger DOM contract locked by e2e/38-audit-logs.spec.ts"
    - path: "src/widgets/EditHistoryTable/EditHistoryTable.tsx"
      provides: "ticket column's cell swapped from raw .slice(0,8) text to <EntityIdCell entityType=\"tab\" .../>"
    - path: "src/widgets/DeletionsPostCloseReport/DeletionsPostCloseReport.tsx"
      provides: "tabId column now renders EntityIdCell (linkable, entityType=\"tab\")"
    - path: "src/widgets/DeletionsPreSendPanel/DeletionsPreSendPanel.tsx"
      provides: "orderId column now renders EntityIdCell (copy-only, entityType=\"order_item\", not in D-02's allowlist)"
  key_links:
    - from: "src/widgets/AuditLogTable/AuditLogTable.tsx"
      to: "src/shared/ui/EntityIdCell.tsx"
      via: "new ColumnDef cell renders <EntityIdCell entityType={row.original.entityType} entityId={row.original.entityId ?? undefined} />"
      pattern: "EntityIdCell entityType={row\\.original\\.entityType}"
    - from: "e2e/38-audit-logs.spec.ts"
      to: "src/widgets/PaymentPane/ui/PaymentPane.tsx (from plan 10-06)"
      via: "clicking the payment.process row's entity-ID link navigates to /payments?id=<id>, which 10-06's filter already handles"
      pattern: "/payments\\?id="
---

<objective>
Compose `EntityIdCell` (built in plan 10-05) into the four widgets that actually display entity IDs — `AuditLogTable` (new column), `EditHistoryTable` (upgrade the existing truncated-text column), and BOTH Reports surfaces that show a raw ID today: `DeletionsPostCloseReport`'s `tabId` and `DeletionsPreSendPanel`'s `orderId` (RESEARCH.md Pitfall 3 — the UI-SPEC only named one of these two by name during discussion; both must get the treatment). Extend the two existing E2E specs that already visit these surfaces (`38-audit-logs.spec.ts`, `47-edit-paid-tab.spec.ts`) to prove the full click-through: link click → navigate → filtered/highlighted destination, now that plan 10-06 provides the destination-side handling.

Purpose: Complete QA-03 — every place an entity ID appears now offers copy and, where a destination exists, a real link.
Output: 4 widgets wired to EntityIdCell, 1 new component test, 2 extended E2E specs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/shared/ui/EntityIdCell.tsx
@src/widgets/AuditLogTable/AuditLogTable.tsx
@src/widgets/EditHistoryTable/EditHistoryTable.tsx
@src/widgets/DeletionsPostCloseReport/DeletionsPostCloseReport.tsx
@src/widgets/DeletionsPreSendPanel/DeletionsPreSendPanel.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: AuditLogTable new entityId column + EditHistoryTable ticket column upgrade</name>
  <files>src/widgets/AuditLogTable/AuditLogTable.tsx, src/widgets/AuditLogTable/AuditLogTable.test.tsx, src/widgets/EditHistoryTable/EditHistoryTable.tsx, src/shared/lib/i18n/locales/en-US/wAdmin.json, src/shared/lib/i18n/locales/es-MX/wAdmin.json</files>
  <read_first>
    - src/shared/ui/EntityIdCell.tsx (from plan 10-05 — exact prop signature: `entityType: string`, `entityId: string | undefined`)
    - src/widgets/AuditLogTable/AuditLogTable.tsx:63-116 (existing `columns: ColumnDef<AuditLog>[]` array — no `entityId` column exists today, only `entityType` as plain text at ~line 91-95; this task ADDS a new column, does not touch the existing `entityType` text column)
    - src/widgets/AuditLogTable/AuditLogTable.test.tsx (confirmed this session: `renderWithProviders(<AuditLogTable />)` at 2 call sites, NEITHER wrapped in Router — adding a `<Link>`-rendering column will throw "You should not use &lt;Link&gt; outside a &lt;Router&gt;" on both, even the "No matches" test since it renders the table shell regardless of row count)
    - src/widgets/CajaDashboard/CajaDashboard.test.tsx:7,167-170 (the exact Router-wrap fix pattern to replicate)
    - src/widgets/EditHistoryTable/EditHistoryTable.tsx:97-101 (existing `ticket` column: `cell: ({ row }) => row.original.entityId?.slice(0, 8) ?? '—'` — Edit History is always `tab.edit_paid`-scoped, so `entityType` here is always literally `'tab'`, always linkable per D-02's allowlist)
  </read_first>
  <action>
    In `AuditLogTable.tsx`: add `import { EntityIdCell } from '@shared/ui/EntityIdCell';` and a new `wAdmin.json` key `auditLogTable.columnEntityId` (en: "Entity ID", es: "ID de entidad") in both locale files. Insert a new `ColumnDef` object into the `columns` array (after the existing `entityType` column, before `actor`): `{ id: 'entityId', header: t('auditLogTable.columnEntityId'), cell: ({ row }) => <EntityIdCell entityType={row.original.entityType} entityId={row.original.entityId ?? undefined} /> }`. Do not remove or alter the existing `action`/`entityType`/`actor`/`createdAt`/`source` columns, the sr-only diff-trigger button, or `onRowClick` — those are locked by `e2e/38-audit-logs.spec.ts`'s existing DOM/a11y contract per the file's own doc comment.

    In `EditHistoryTable.tsx`: add the same `EntityIdCell` import. Replace the `ticket` column's `cell` body from `row.original.entityId?.slice(0, 8) ?? '—'` to `<EntityIdCell entityType="tab" entityId={row.original.entityId ?? undefined} />` — the header (`t('editHistoryTable.columnTicket')`) is unchanged.

    Fix `AuditLogTable.test.tsx`: add `import { MemoryRouter } from 'react-router-dom';`, wrap both `renderWithProviders(<AuditLogTable />)` call sites in `<MemoryRouter>` (`replace_all`). Extend the first test ("renders the action cell as plain text...") with an assertion that `screen.getByRole('link', { name: /view payment/i })` is present for `ROW_A` (entityType `'payment'`, a valid `entityId`) and resolves to an `href` containing `/payments?id=${ROW_A.entityId}`.
  </action>
  <verify>
    <automated>npx vitest run src/widgets/AuditLogTable/AuditLogTable.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/widgets/AuditLogTable/AuditLogTable.test.tsx` passes, including the new entityId-link assertion
    - Both pre-existing `AuditLogTable.test.tsx` cases still pass unchanged in behavior (Router-wrap is purely additive)
    - `npm run typecheck && npm run lint` clean
  </acceptance_criteria>
  <done>AuditLogTable has a new, copyable/linkable entityId column; EditHistoryTable's existing ticket column is upgraded from plain truncated text to the same copy/link treatment.</done>
  <reversibility rating="reversible">Additive column / cell-renderer swap; existing columns and RBAC/route gating unchanged.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DeletionsPostCloseReport + DeletionsPreSendPanel — both Reports surfaces</name>
  <files>src/widgets/DeletionsPostCloseReport/DeletionsPostCloseReport.tsx, src/widgets/DeletionsPreSendPanel/DeletionsPreSendPanel.tsx, src/widgets/DeletionsPreSendPanel/DeletionsPreSendPanel.test.tsx</files>
  <read_first>
    - src/shared/ui/EntityIdCell.tsx (from plan 10-05)
    - src/widgets/DeletionsPostCloseReport/DeletionsPostCloseReport.tsx:15-53 (existing `tabId` column cell: `info => <span className="tabular-nums">{info.getValue&lt;string&gt;()}</span>` — `DeletionsPostRowSchema.tabId: UuidSchema`, a real `tab` id, per D-03 linkable)
    - src/widgets/DeletionsPreSendPanel/DeletionsPreSendPanel.tsx:22-59 (existing `orderId` column, same raw-span pattern — `DeletionsPreRowSchema.orderId: UuidSchema` is `order_items`-scoped per its own type, NOT in D-02's `payment`/`tab`/`staff` allowlist — Pitfall 3: this is the surface the discussion-phase UI-SPEC did not name, do not skip it)
    - src/widgets/AuditLogTable/AuditLogTable.test.tsx (Vitest/RTL mocking-hook pattern to mirror for the new `DeletionsPreSendPanel.test.tsx` — mock `@entities/tab/model/queries-reports`'s `useDeletionsPreReport` the same way `AuditLogTable.test.tsx` mocks `@entities/audit-log`'s `useAuditLogs`)
  </read_first>
  <action>
    In `DeletionsPostCloseReport.tsx`: import `EntityIdCell`, change the `tabId` column's `cell` from `info => <span className="tabular-nums">{info.getValue<string>()}</span>` to `info => <EntityIdCell entityType="tab" entityId={info.getValue<string>()} />` (linkable — a closed tab is payment-history-adjacent per D-03).

    In `DeletionsPreSendPanel.tsx`: same import, change the `orderId` column's `cell` to `info => <EntityIdCell entityType="order_item" entityId={info.getValue<string>()} />` — `"order_item"` is not in `LINKABLE_TYPES`, so `EntityIdCell` renders copy-only plain text automatically (no `Link`, no `/order-items/:id` route needed or built — per RESEARCH.md's explicit anti-pattern warning against inventing routes for non-allowlisted types).

    Create `DeletionsPreSendPanel.test.tsx` (new — this widget currently has no test file): mock `useDeletionsPreReport` (from `@entities/tab/model/queries-reports`) to return one row with a known `orderId`, `renderWithProviders(<MemoryRouter><DeletionsPreSendPanel dateRange={...} /></MemoryRouter>)`, and assert (a) NO `role="link"` element is present for that row (confirms copy-only, per D-02's allowlist excluding `order_item`) and (b) a copy button IS present. This is a lighter-weight, deterministic substitute for a fragile full E2E fixture — `EntityIdCell`'s copy-only branch is already exhaustively unit-tested generically in plan 10-05; this test only confirms the correct `entityType` prop is actually wired through from `DeletionsPreSendPanel`.
  </action>
  <verify>
    <automated>npx vitest run src/widgets/DeletionsPreSendPanel/DeletionsPreSendPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/widgets/DeletionsPreSendPanel/DeletionsPreSendPanel.test.tsx` passes
    - `npm run typecheck && npm run lint` clean
    - Grep confirms `DeletionsPreSendPanel.tsx` uses `entityType="order_item"` (not `"tab"` or `"payment"`) — this row must render copy-only, never a link, since no `/order-items/:id` destination exists
  </acceptance_criteria>
  <done>Both Reports surfaces that render a raw entity ID (post-close tabId, pre-send orderId) now use EntityIdCell — the linkable one gets a real link, the non-allowlisted one gets copy-only, matching D-02/D-03 exactly.</done>
  <reversibility rating="reversible">Cell-renderer swap only; report data/columns/export behavior unchanged.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 3: E2E full click-through — AuditLogTable and EditHistoryTable links actually navigate</name>
  <files>e2e/38-audit-logs.spec.ts, e2e/47-edit-paid-tab.spec.ts</files>
  <read_first>
    - e2e/38-audit-logs.spec.ts:144-199 ("should display audit entries after processing a payment" — already seeds a real `payment.process` audit row and navigates to `/audit`; this task extends it, not a new test)
    - e2e/47-edit-paid-tab.spec.ts:295-320 ("manager sees the edit in /edit-history and row click opens the JsonDiffViewer" — already seeds a real `tab.edit_paid` audit row via `editPaidTabViaUi` and navigates to `/edit-history`; this task extends it AND adds a follow-on visit to /reports' Deletions-post tab using the SAME seeded tab, since `DeletionsPostCloseReport` reads `tab.edit_paid` rows too)
    - src/widgets/PaymentPane/ui/PaymentPane.tsx (from plan 10-06 — confirms the `/payments?id=` filter Task 3 relies on already exists)
  </read_first>
  <action>
    In `e2e/38-audit-logs.spec.ts`'s "should display audit entries after processing a payment" test: after the existing assertion that `paymentProcessCell` is visible, locate the row (`paymentProcessCell.locator('..')` or `page.getByRole('row').filter({ has: paymentProcessCell })`) and assert a `role="link"` element with an accessible name matching `/view payment/i` is present in that row. Also grant clipboard permissions (`await context.grantPermissions(['clipboard-read', 'clipboard-write'])` before navigation) and assert clicking the row's copy button shows the `idCopied` success toast (match on translated text or a stable `data-testid` if one is easier to add to the copy button). Then click the entity-ID link itself and assert the browser navigates to a URL matching `/\/payments\?id=/` and that a `payment-row-{id}` testid becomes visible on the destination page (proves plan 10-06's filter actually receives and honors the param end-to-end, not just that the link's `href` looks right).

    In `e2e/47-edit-paid-tab.spec.ts`'s SC-4 test: after the existing `reason` text assertion, locate the `tab.edit_paid` row and assert its ticket-column entity-ID link is present, click it, and assert navigation to `/payments?id=<seeded.tabId>` succeeds. Then, still in the same test, navigate to `/reports`, click the `Deletions` tab, and assert the `tabId` column for the same seeded tab shows a link/copy affordance (confirms Task 2's `DeletionsPostCloseReport` wiring using a fixture this test already produced — no new seeding needed).
  </action>
  <verify>
    <automated>npx playwright test e2e/38-audit-logs.spec.ts e2e/47-edit-paid-tab.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/38-audit-logs.spec.ts e2e/47-edit-paid-tab.spec.ts` passes headless, including the new link/copy/navigation assertions
    - The full click path (source table link → destination page filter/highlight) is exercised at least once for `payment`/`tab` entityType, proving plans 10-05, 10-06, and 10-07 compose correctly end-to-end
  </acceptance_criteria>
  <done>Clicking a real entity-ID link in AuditLogTable or EditHistoryTable navigates to the correct filtered/highlighted destination — QA-03 fully closed and E2E-proven, not just unit-tested in isolation.</done>
  <reversibility rating="reversible">Test-only additions to existing, already-passing specs.</reversibility>
</task>

</tasks>

<verification>
`npx vitest run src/widgets/AuditLogTable/AuditLogTable.test.tsx src/widgets/DeletionsPreSendPanel/DeletionsPreSendPanel.test.tsx` green. `npx playwright test e2e/38-audit-logs.spec.ts e2e/47-edit-paid-tab.spec.ts` green. `npm run typecheck && npm run lint` clean. `npm run test` (full unit suite) green (no regression from the AuditLogTable/EditHistoryTable/Deletions edits).
</verification>

<success_criteria>
QA-03 (ROADMAP Success Criterion 3) fully satisfied: Audit Log, Edit History, and Reports display entity IDs as copyable and/or clickable links to the related record — verified by Playwright E2E (copy action / navigation).
</success_criteria>

<output>
Create `.planning/phases/10-quality-debt-ops-documentation/10-07-SUMMARY.md` when done
</output>
