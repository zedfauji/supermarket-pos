---
phase: 10-quality-debt-ops-documentation
plan: 05
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/shared/ui/EntityIdCell.tsx
  - src/shared/ui/EntityIdCell.test.tsx
  - src/shared/lib/i18n/locales/en-US/common.json
  - src/shared/lib/i18n/locales/es-MX/common.json
  - src/shared/lib/i18n/locales/en-US/wAdmin.json
  - src/shared/lib/i18n/locales/es-MX/wAdmin.json
autonomous: true
requirements: [QA-03]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A payment/tab/staff entity ID renders as a real navigation link (D-01/D-02); every other entityType renders as copy-only plain text"
    - "The full (untruncated) entity ID — never the 8-char truncated display string — is what actually gets copied and linked"
    - "A clipboard-write rejection surfaces a visible error toast, not a silent no-op (UI-SPEC backstop row)"
  artifacts:
    - path: "src/shared/ui/EntityIdCell.tsx"
      provides: "Shared shared/ui primitive: entityType/entityId -> Link (payment/tab -> /payments?id=, staff -> /staff?id=) or plain copy-only text, plus a copy-to-clipboard icon button and full-ID tooltip"
    - path: "src/shared/ui/EntityIdCell.test.tsx"
      provides: "Vitest/RTL coverage of the allowlist branching, copy behavior, and clipboard-rejection backstop"
  key_links:
    - from: "src/shared/ui/EntityIdCell.tsx"
      to: "react-router-dom Link"
      via: "payment/tab entityType -> `/payments?id={entityId}`, staff entityType -> `/staff?id={entityId}`, exact-Set allowlist match (Pitfall 2)"
      pattern: "LINKABLE_TYPES\\.has\\(entityType\\)"
---

<objective>
Build `EntityIdCell` — the shared `src/shared/ui/` primitive every QA-03 table/report widget will compose (AuditLogTable, EditHistoryTable, DeletionsPostCloseReport, DeletionsPreSendPanel — wired in a later, dependent plan). It renders an entity ID as a real navigation link for `payment`/`tab`/`staff` types (D-01/D-02) or plain copy-only mono text for everything else, always with a copy-to-clipboard affordance and a full-ID tooltip. This is a dedicated TDD plan: write the test first, then the component, since the allowlist branching and copy behavior are exactly the kind of `expect(fn(input)).toBe(output)` logic TDD suits.

Purpose: Foundation for QA-03 — no direct-analog component exists yet in this codebase (RESEARCH.md's "No Analog Found" table).
Output: `EntityIdCell.tsx`, its test, and the `common`/`wAdmin` i18n keys it needs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/shared/ui/ProtectedAction.tsx
@src/shared/ui/ProtectedAction.test.tsx
@src/shared/ui/tooltip.tsx
@src/shared/lib/i18n/locales/en-US/common.json
@src/shared/lib/i18n/locales/en-US/wAdmin.json
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `navigator.clipboard.writeText()` call | Browser Clipboard API permission can be denied/unavailable (non-secure context, browser policy) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03 | Denial of Service (UX) | `EntityIdCell`'s copy button | low | mitigate | Clipboard write wrapped in try/catch; rejection shows `toast.error` (UI-SPEC's explicit backstop for the "partial" clipboard-unavailable row) instead of throwing or silently doing nothing — verified by a Vitest test mocking a rejected `navigator.clipboard.writeText` |
| T-10-04 | Information Disclosure | `entityId` rendered in DOM/tooltip | low | accept | Entity IDs are opaque UUIDs already visible elsewhere in the same admin-gated screens (Audit Log, Edit History, Reports — all already RBAC-restricted to manager/admin); no new exposure surface |
</threat_model>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): EntityIdCell.test.tsx — full behavior spec against a not-yet-built component</name>
  <files>src/shared/ui/EntityIdCell.test.tsx</files>
  <read_first>
    - src/shared/ui/ProtectedAction.test.tsx (RTL test structure to mirror: `renderWithProviders` from `@shared/lib/test-utils`, `userEvent.setup()`, `screen.getByRole`)
    - src/shared/ui/ProtectedAction.tsx:1-3,45-54 (confirmed this session: `TooltipProvider`/`Tooltip`/`TooltipTrigger`/`TooltipContent` are imported from `./tooltip`, wrapped locally with `<TooltipProvider delayDuration={300}>` — no app-root provider exists, replicate this exact local-wrap shape in the new component, not the test)
    - Confirmed this session: `src/shared/lib/test-utils.tsx`'s `renderWithProviders` wraps ONLY `QueryClientProvider` — it does NOT include a Router. `EntityIdCell` renders `react-router-dom`'s `<Link>`, so every test render MUST be wrapped in `<MemoryRouter>` (`renderWithProviders(<MemoryRouter>{...}</MemoryRouter>)`) exactly like `src/widgets/CajaDashboard/CajaDashboard.test.tsx:167-170` already does for a Link-using component — omitting this throws "You should not use &lt;Link&gt; outside a &lt;Router&gt;"
  </read_first>
  <behavior>
    - Given `entityType="payment"` and a valid `entityId`, renders a `role="link"` element whose accessible name matches the payment aria-label pattern, `href` resolves to `/payments?id={entityId}` (full id, not truncated), and its visible text is the first 8 characters of `entityId`
    - Given `entityType="tab"` and a valid `entityId`, also renders a link to `/payments?id={entityId}` (D-03: payment AND tab both route to `/payments`)
    - Given `entityType="staff"` and a valid `entityId`, renders a link to `/staff?id={entityId}` with the staff aria-label pattern
    - Given `entityType="settings"` (or any other non-allowlisted string, e.g. `"caja_session"`, `"order_item"`) and a valid `entityId`, renders NO `role="link"` element — plain muted mono text showing the first 8 characters
    - Given `entityId={undefined}`, renders an em dash placeholder and NO copy button, regardless of `entityType`
    - Given a valid `entityId` (any `entityType`), a copy button (`role="button"`, accessible name = the copy-ID label) is always present; clicking it calls a mocked `navigator.clipboard.writeText` with the FULL `entityId` string, not the truncated 8-char display text
    - Given `navigator.clipboard.writeText` mocked to resolve, clicking copy shows a success toast (mock `sonner`'s `toast.success` and assert it was called)
    - Given `navigator.clipboard.writeText` mocked to reject, clicking copy shows an error toast (mock `sonner`'s `toast.error` and assert it was called) — never a thrown/unhandled rejection
  </behavior>
  <action>
    Create `EntityIdCell.test.tsx` importing the not-yet-existing `EntityIdCell` from `./EntityIdCell` (this import failing, or the file being a stub, is an acceptable RED state). Mock `sonner` (`vi.mock('sonner', () => ({ toast: { success: vi.fn(), error: vi.fn() } }))`) and stub `navigator.clipboard.writeText` via `Object.assign(navigator, { clipboard: { writeText: vi.fn() } })` in a `beforeEach`, reconfigurable per test (`.mockResolvedValue(undefined)` / `.mockRejectedValue(new Error('denied'))`). Wrap every render in `<MemoryRouter>`. Write one `it()` per Behavior bullet above, using `userEvent` for the click interactions. Run the suite and confirm it fails (component doesn't exist yet) before moving to Task 2.
  </action>
  <verify>
    <automated>npx vitest run src/shared/ui/EntityIdCell.test.tsx 2>&1 | grep -qE "fail|Cannot find module|Error" || (echo "expected RED state before EntityIdCell.tsx exists" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `EntityIdCell.test.tsx` exists with at least 8 test cases, one per Behavior bullet
    - Running it fails (RED) because `./EntityIdCell` does not yet exist
  </acceptance_criteria>
  <done>Full behavior spec is written and confirmed RED.</done>
  <reversibility rating="reversible">Test-only file; no production behavior yet.</reversibility>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Implement EntityIdCell.tsx + required i18n keys</name>
  <files>src/shared/ui/EntityIdCell.tsx, src/shared/lib/i18n/locales/en-US/common.json, src/shared/lib/i18n/locales/es-MX/common.json, src/shared/lib/i18n/locales/en-US/wAdmin.json, src/shared/lib/i18n/locales/es-MX/wAdmin.json</files>
  <read_first>
    - src/shared/ui/EntityIdCell.test.tsx (from Task 1 — the exact contract to satisfy)
    - src/shared/lib/i18n/locales/en-US/common.json (top-level flat-key convention — e.g. `searchInput.search`, `moneyDisplay.negativePrefix`; this task adds two NEW flat top-level keys, `copyId` and `idCopied`, plus `copyIdFailed` for the rejection path, matching the Copywriting Contract's literal `t('common.copyId', ...)`/`t('common.idCopied', ...)` notation)
    - src/shared/lib/i18n/locales/en-US/wAdmin.json (this task adds two NEW flat top-level keys, `viewPaymentAriaLabel` and `viewStaffAriaLabel`, matching the Copywriting Contract's `t('wAdmin.viewPaymentAriaLabel', { id })` → "View payment {id}" / `t('wAdmin.viewStaffAriaLabel', { id })` → "View staff member {id}")
    - src/shared/lib/i18n/locales/es-MX/common.json and es-MX/wAdmin.json (existing keys are genuine Spanish translations, NOT byte-identical to English — e.g. `searchInput.search`: en "Search" / es "Buscar" — this is normal post-migration i18n practice; only the original i18n-migration phase used byte-identical es-MX values)
  </read_first>
  <action>
    Add to `common.json` (both locales) two flat top-level keys: `copyId` (en: "Copy ID", es: "Copiar ID"), `idCopied` (en: "ID copied to clipboard", es: "ID copiado al portapapeles"), `copyIdFailed` (en: "Could not copy ID", es: "No se pudo copiar el ID") — insert alphabetically among existing top-level keys.

    Add to `wAdmin.json` (both locales) two flat top-level keys: `viewPaymentAriaLabel` (en: "View payment {{id}}", es: "Ver pago {{id}}"), `viewStaffAriaLabel` (en: "View staff member {{id}}", es: "Ver miembro del personal {{id}}") — `{{id}}` interpolation syntax matches the existing `{{action}}`/`{{date}}` pattern already in `auditLogTable.viewDiffAriaLabel`.

    Create `EntityIdCell.tsx`: `export type EntityIdCellProps = { entityType: string; entityId: string | undefined; className?: string }` (note `entityId: string | undefined`, not `entityId?:`, per `exactOptionalPropertyTypes`). Define `const LINKABLE_TYPES = new Set(['payment', 'tab', 'staff']);` (module scope, exact-string Set match per Pitfall 2 — never fuzzy/case-insensitive). If `entityId` is undefined, render `<span className="text-xs text-muted-foreground">—</span>` and stop. Otherwise compute `truncated = entityId.slice(0, 8)` and `route = entityType === 'staff' ? \`/staff?id=${entityId}\` : \`/payments?id=${entityId}\`` (payment and tab share the `/payments` target per D-03). Render a flex row: the ID element — `<Link to={route} aria-label={t('wAdmin:viewPaymentAriaLabel', { id: truncated })}>` (or the staff variant) wrapped in `<TooltipProvider delayDuration={300}><Tooltip><TooltipTrigger asChild>{idElement}</TooltipTrigger><TooltipContent side="top">{entityId}</TooltipContent></Tooltip></TooltipProvider>` when `LINKABLE_TYPES.has(entityType)`, else the same Tooltip wrap around a plain `<span className="font-mono text-xs text-muted-foreground">{truncated}</span>` (no Link) — plus a copy icon `<Button variant="ghost" size="icon" className="h-8 w-8" aria-label={t('common:copyId')}>` using `Copy`/`Check` from `lucide-react` (swap to `Check` for ~1.5s after a successful copy via local `useState`), `onClick` calling `navigator.clipboard.writeText(entityId)` (the FULL id, never `truncated`), `.then(() => toast.success(t('common:idCopied')))`, `.catch(() => toast.error(t('common:copyIdFailed')))`. Import `toast` from `sonner` (matches `SupplierListPanel.tsx`'s existing usage). Use `useTranslation()` with no namespace argument and address keys via the `namespace:key` colon syntax (matches `i18n.t('featOrders:checkoutSale.defaultCustomerName')`'s existing usage elsewhere in the codebase) so both `common` and `wAdmin` namespaces resolve from one hook call.
  </action>
  <verify>
    <automated>npx vitest run src/shared/ui/EntityIdCell.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/shared/ui/EntityIdCell.test.tsx` passes all 8+ cases (GREEN)
    - `npm run typecheck` clean (no `any`, `entityId: string | undefined` used correctly)
    - `npm run lint` clean (no hardcoded literal strings in `EntityIdCell.tsx` — every string routes through `t()`)
  </acceptance_criteria>
  <done>EntityIdCell exists, is fully tested, and satisfies D-01/D-02/D-03/D-04, Pitfall 2 (exact-match allowlist), Pitfall 5 (local TooltipProvider), and the UI-SPEC's clipboard-rejection backstop.</done>
  <reversibility rating="reversible">New additive shared/ui component; not yet wired into any consumer widget (that happens in a later, dependent plan) — zero blast radius if reverted.</reversibility>
</task>

</tasks>

<verification>
`npx vitest run src/shared/ui/EntityIdCell.test.tsx` green. `npm run typecheck && npm run lint` clean.
</verification>

<success_criteria>
Foundation for QA-03 (ROADMAP Success Criterion 3) laid: a fully-tested, reusable EntityIdCell exists satisfying D-01 (copy+link), D-02 (payment/tab/staff-only allowlist), D-03/D-04 (route targets), and the UI-SPEC's long-text/overflow and clipboard-backstop rows — ready for composition into AuditLogTable/EditHistoryTable/Reports in a dependent plan.
</success_criteria>

<output>
Create `.planning/phases/10-quality-debt-ops-documentation/10-05-SUMMARY.md` when done
</output>
