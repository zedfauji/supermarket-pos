---
phase: 09-reopen-and-edit-a-completed-sale
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/edit-paid-tab/model/edit-paid-tab-rpc.integration.test.ts
autonomous: true
requirements: [SALE-03]

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "edit_paid_tab's existing status guard (v_status NOT IN ('paid', 'closed')) already structurally excludes status='open' (a reopened sale); this plan adds automated regression coverage proving it, with zero production code changes."
  artifacts:
    - src/features/edit-paid-tab/model/edit-paid-tab-rpc.integration.test.ts
  key_links:
    - "New it() case seeds a status='open' tab (via the file's existing seedPaidTab helper, then a direct tabs.status update) -> managerClient.rpc('edit_paid_tab', ...) -> asserts { ok: false, code: 'TAB_NOT_EDITABLE' }"
---

<objective>
Close the one genuine test gap RESEARCH.md's Pitfall 2 identified for SC-5: `edit_paid_tab`'s `TAB_NOT_EDITABLE` guard has no existing automated coverage for the `status='open'` (reopened-sale) case specifically, even though the guard already excludes it.

Purpose: SC-5 requires this be "confirmed by its existing/updated test coverage," not just "confirmed by reading the source." This plan is a pure regression-test addition — it runs independently of Plan 09-01/09-03's UI work and touches a completely different file, so it is parallel-safe in Wave 1.
Output: One new `it()` case in the existing `edit_paid_tab RPC (integration)` describe block.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-reopen-and-edit-a-completed-sale/09-CONTEXT.md
@.planning/phases/09-reopen-and-edit-a-completed-sale/09-RESEARCH.md
@src/features/edit-paid-tab/model/edit-paid-tab-rpc.integration.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the SC-5 TAB_NOT_EDITABLE(status='open') regression test</name>
  <files>src/features/edit-paid-tab/model/edit-paid-tab-rpc.integration.test.ts</files>
  <read_first>
    src/features/edit-paid-tab/model/edit-paid-tab-rpc.integration.test.ts (full file — env guards lines 1-34, `seedPaidTab` helper lines 100-139, `managerClient`/`bartenderClient` setup lines 141-258, the existing AUTH_FORBIDDEN test at lines 305-318 as the direct template for seed -> RPC call -> assert shape)
    supabase/migrations/20260719000001_edit_paid_tab_rpc.sql lines 85-92 (the `TAB_NOT_EDITABLE` guard this test proves, already unchanged: `IF v_status NOT IN ('paid', 'closed') THEN RETURN jsonb_build_object('ok', false, 'code', 'TAB_NOT_EDITABLE', ...)`)
  </read_first>
  <action>
    Add one new `it('SC-5: TAB_NOT_EDITABLE is returned when the tab is status=open (a reopened sale, not paid/closed)', ...)` inside the existing `describe.skipIf(skip)('edit_paid_tab RPC (integration)', ...)` block, placed directly after the existing AUTH_FORBIDDEN test (around line 318). Reuse the file's own `seedPaidTab(10.0, 1)` helper to create a `status='paid'` tab with one order_item, then flip it to a reopened state with a direct `db.from('tabs').update({ status: 'open' }).eq('id', seed.tabId)` call (mirrors the same direct-update pattern `e2e/48-reopen-closed-ticket.spec.ts`'s own seed helper already uses to move a tab between statuses; this avoids pulling in `reopen_tab`'s full caja/reason/audit side effects, which are irrelevant to this guard). Then call `managerClient.rpc('edit_paid_tab', { p_tab_id: seed.tabId, p_expected_version: seed.version, p_order_item_patches: [{ id: seed.orderItemId, op: 'update', notes: 'attempt on reopened tab' }], p_notes: null, p_reason: 'Integration test: reopened tab' })`. The `TAB_NOT_EDITABLE` branch returns a normal `{ ok: false, code: ... }` payload (a `RETURN jsonb_build_object(...)`, not a raised Postgres exception — unlike `STALE_VERSION`/`AUTH_FORBIDDEN`, which are exceptions caught via `.error`), so assert `error` is `null` and `data.ok === false` and `data.code === 'TAB_NOT_EDITABLE'`. Add `seed.tabId` cleanup is already handled by the file's existing `afterEach` (`cleanupTabIds` array — `seedPaidTab` already pushes onto it), no extra teardown needed.
  </action>
  <verify>
    <automated>npx vitest run src/features/edit-paid-tab/model/edit-paid-tab-rpc.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New `it()` block named with the literal prefix "SC-5:" exists in the file.
    - `npx vitest run src/features/edit-paid-tab/model/edit-paid-tab-rpc.integration.test.ts` — the new test and all pre-existing tests in the file pass.
    - The new test asserts `data.code === 'TAB_NOT_EDITABLE'` (source assertion — grep the test body for that literal).
    - No production code file is modified by this plan (`git diff --name-only` for this plan touches only the test file).
  </acceptance_criteria>
  <done>SC-5 is proven by an automated regression test, not just source inspection; the guard itself is confirmed unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `edit_paid_tab` RPC's own `v_status` guard | Server-side check inside a `SECURITY DEFINER` Postgres function — the only boundary this plan touches, and only to add test coverage, not to change behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Tampering | `edit_paid_tab`'s `TAB_NOT_EDITABLE` status guard | high | mitigate | This plan adds a regression test asserting `TAB_NOT_EDITABLE` for `status='open'`. Any future change that accidentally loosens the guard to accept reopened sales (which would let a correction-only tool bypass the reopen/add/remove flow's own audit trail) is caught by CI (`npm run test`) before merge, not discovered in production. No production code change in this plan — the guard is already correct. |

No new package installs in this plan — no `T-*-SC` package-legitimacy row required.
</threat_model>

<verification>
- `npx vitest run src/features/edit-paid-tab/model/edit-paid-tab-rpc.integration.test.ts` green (new test + all pre-existing tests in the file).
- `npm run typecheck` clean.
</verification>

<success_criteria>
SC-5 from ROADMAP.md Phase 9 is met: `edit_paid_tab`'s status guard still structurally excludes reopened (`status='open'`) sales, confirmed by new automated test coverage rather than source inspection alone.
</success_criteria>

<output>
Create `.planning/phases/09-reopen-and-edit-a-completed-sale/09-02-SUMMARY.md` when done
</output>
