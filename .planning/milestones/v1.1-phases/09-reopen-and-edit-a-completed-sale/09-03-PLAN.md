---
phase: 09-reopen-and-edit-a-completed-sale
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/widgets/PaymentPane/ui/EditReopenedItemsPanel.tsx
  - e2e/48-reopen-closed-ticket.spec.ts
  - src/shared/lib/i18n/locales/en-US/featOrders.json
  - src/shared/lib/i18n/locales/es-MX/featOrders.json
autonomous: true
requirements: [SALE-03]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "On a reopened sale, a manager can remove an existing line item from EditReopenedItemsPanel: clicking 'Quitar' on a row opens a manager PIN gate, PIN success opens RemoveTabItemDialog (reused unmodified, D-05), confirming with a reason calls remove_tab_item, and the item disappears from the panel and the sale total updates."
    - "Removing the last remaining item from a reopened sale succeeds through the same per-item remove_tab_item flow (no client-side 'last item' guard is added) — the parent order-void-when-empty behavior is already handled server-side by remove_tab_item per RESEARCH.md's Architectural Responsibility Map, so no additional Save-disable logic is needed in this panel for the zero-items-remaining case (UI-SPEC backstop item resolved: defer to existing RPC behavior, not a new client guard)."
    - "e2e/48-reopen-closed-ticket.spec.ts's SC-1 and SC-2 groups still pass unmodified after the SC-3 block and full-file run are added."
  artifacts: []
  key_links:
    - "EditReopenedItemsPanel existing-item row 'Quitar' button -> showPinForRemoval -> ManagerPinDialog requiredAction=\"reopen_tab\" -> onSuccess -> showRemoveConfirm -> RemoveTabItemDialog (reused, unmodified) -> useRemoveTabItem -> supabase.rpc('remove_tab_item', ...)"
---

<objective>
Wire the already-built, already-tested `RemoveTabItemDialog`/`useRemoveTabItem` into `EditReopenedItemsPanel` (SALE-03 / SC-3), reproducing the deleted `TableStatusPanel`'s two-step PIN-then-confirm orchestration with a still-valid `requiredAction`, and extend `e2e/48-reopen-closed-ticket.spec.ts` with the SC-3 coverage this whole phase's ROADMAP success criteria require.

Purpose: This is the phase's second (and final) expansion slice, building on Plan 09-01's proven add-item tracer. It resolves the open wiring question flagged in STATE.md/ROADMAP: `RemoveTabItemDialog`/`useRemoveTabItem` are fully functional and tested, just never composed into any live UI — this plan is that composition, not a rewrite (D-05).
Output: `EditReopenedItemsPanel` gains a per-row remove affordance; `e2e/48-reopen-closed-ticket.spec.ts` gains an SC-3 test block and a full-file combined-flow run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-reopen-and-edit-a-completed-sale/09-CONTEXT.md
@.planning/phases/09-reopen-and-edit-a-completed-sale/09-RESEARCH.md
@.planning/phases/09-reopen-and-edit-a-completed-sale/09-PATTERNS.md
@src/widgets/PaymentPane/ui/EditReopenedItemsPanel.tsx
@src/features/remove-tab-item/useRemoveTabItem.ts
@src/features/remove-tab-item/ui/RemoveTabItemDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose RemoveTabItemDialog into EditReopenedItemsPanel (D-05 two-step orchestration)</name>
  <precondition>src/widgets/PaymentPane/ui/EditReopenedItemsPanel.tsx exists with the add-item Sheet shell and a read-only existing-items list (Plan 09-01, Task 3).</precondition>
  <files>src/widgets/PaymentPane/ui/EditReopenedItemsPanel.tsx, src/shared/lib/i18n/locales/en-US/featOrders.json, src/shared/lib/i18n/locales/es-MX/featOrders.json</files>
  <read_first>
    src/widgets/PaymentPane/ui/EditReopenedItemsPanel.tsx (as built by 09-01 — the existing-items read-only list and the addedRows/PIN-gate state to extend, not replace)
    src/features/remove-tab-item/ui/RemoveTabItemDialog.tsx (full file — props `open`, `item: OrderItem | null`, `tabId`, `orderId`, `onClose`; its own doc comment explicitly delegates the PIN step to its parent)
    src/features/remove-tab-item/useRemoveTabItem.ts (full file — confirms `TAB_NOT_OPEN`/`NOT_FOUND` error mapping already exists, no changes needed)
    .planning/phases/09-reopen-and-edit-a-completed-sale/09-PATTERNS.md section "Two-step PIN -> confirm orchestration for remove (D-05)" (the exact recovered `TableStatusPanel` orchestration to reproduce, with `requiredAction` swapped)
  </read_first>
  <action>
    In src/widgets/PaymentPane/ui/EditReopenedItemsPanel.tsx: import `RemoveTabItemDialog` via the deep import `@features/remove-tab-item/ui/RemoveTabItemDialog` (this folder has no barrel — do not add one, do not modify `useRemoveTabItem.ts`/`RemoveTabItemDialog.tsx` themselves, D-05 reuse-as-is). Add local state `showPinForRemoval: boolean`, `showRemoveConfirm: boolean`, `selectedItemForRemoval: OrderItem | null` (import `OrderItem` type from `@shared/lib/domain`). On each existing-item row (already rendered read-only by 09-01), add a "Quitar"/"Remove" trigger button (`t('editReopenedItems.removeItem')`, `POSButton variant="ghost" size="sm" className="text-destructive"`) that sets `selectedItemForRemoval = item; showPinForRemoval = true`. Mount a second `<ManagerPinDialog open={showPinForRemoval} onOpenChange={open => { if (!open) { setShowPinForRemoval(false); setSelectedItemForRemoval(null); } }} requiredAction="reopen_tab" onSuccess={() => { setShowPinForRemoval(false); setShowRemoveConfirm(true); }} />` — a second, independent `ManagerPinDialog` instance from the add-side's `addPinOpen` one added in 09-01 (D-03: every removal gets its own PIN prompt, one-item-per-prompt, not shared with the add-side's single PIN). Mount `<RemoveTabItemDialog open={showRemoveConfirm} item={selectedItemForRemoval} tabId={tab?.id ?? ''} orderId={selectedItemForRemoval?.orderId ?? ''} onClose={() => { setShowRemoveConfirm(false); setSelectedItemForRemoval(null); }} />` directly (not gated further — its own internal `ConfirmDialog` and reason-required validation already exist unmodified). Add `showPinForRemoval`/`showRemoveConfirm`/`selectedItemForRemoval` to the panel's existing `onOpenChange(false)` reset block (added in 09-01) so closing the whole Sheet also clears any in-flight remove attempt. Apply `className="min-w-0 truncate"` to the existing-item row's product-name label if 09-01 did not already add it (UI-SPEC overflow backstop, matching `RemoveTabItemDialog`'s own row treatment). Add `"removeItem": "Remove"` (en-US) / `"removeItem": "Quitar"` (es-MX) to the `editReopenedItems` object already added to both `featOrders.json` locale files in 09-01 — do not touch `removeTabItem.*` keys (D-05, `RemoveTabItemDialog`'s own copy is reused unchanged). Explicitly do NOT add any "last item / zero remaining items" guard disabling anything on this remove flow — `remove_tab_item` already voids the parent order server-side when it empties out (RESEARCH.md's Architectural Responsibility Map), so no new client-side check is needed for that case.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `EditReopenedItemsPanel.tsx` imports `RemoveTabItemDialog` from `@features/remove-tab-item/ui/RemoveTabItemDialog` and does not modify that file or `useRemoveTabItem.ts` (`git diff --name-only` for this task excludes both).
    - Source assertion: the file contains two distinct `ManagerPinDialog` mounts, both `requiredAction="reopen_tab"`, neither `requiredAction="void_order"`.
    - `npm run typecheck && npm run lint` both clean.
    - No new "zero items" / "last item" conditional is added around the remove trigger or `RemoveTabItemDialog` mount (source inspection — grep for a length-based disable on the remove path finds none).
  </acceptance_criteria>
  <done>EditReopenedItemsPanel composes RemoveTabItemDialog behind its own manager PIN gate, reproducing the deleted TableStatusPanel's orchestration with a valid StaffAction.</done>
</task>

<task type="auto">
  <name>Task 2: SC-3 E2E coverage + full-spec combined-flow run</name>
  <files>e2e/48-reopen-closed-ticket.spec.ts</files>
  <read_first>
    e2e/48-reopen-closed-ticket.spec.ts (as extended by 09-01 — `seedPaidTabViaDirectSale`, `enterManagerPin`, and the SC-2 describe block to follow as the template)
    src/shared/lib/i18n/locales/es-MX/featOrders.json `removeTabItem` block (confirm literal copy: title "¿Eliminar artículo?", confirmLabel "Eliminar artículo", reasonLabel "Motivo")
  </read_first>
  <action>
    Add `test.describe('SC-3: remove a line item from a reopened sale', ...)` after the SC-2 describe block in e2e/48-reopen-closed-ticket.spec.ts. Seed a tab via `seedPaidTabViaDirectSale`, log in as manager, reopen it (same steps as SC-2's setup), click "Editar artículos", click the existing item row's "Quitar" button, complete the manager PIN gate (`enterManagerPin`), fill `RemoveTabItemDialog`'s reason input (`#remove-tab-item-reason`) with a test reason, click "Eliminar artículo" to confirm, then assert a success toast matching the `removeTabItem.removedFromOrder` pattern (product name + "eliminado del pedido") and that the item no longer appears in the panel's list. Query the DB directly to assert the `order_items` row for that item id no longer exists (`remove_tab_item` hard-deletes, per its migration). After adding the SC-3 block, run the full spec file once (not just `-g` filters) to confirm SC-1 through SC-3 all pass together in a single run sharing the same `process_direct_sale_atomic` fixture path (SC-4 confirmed structurally — every seed in this file now goes through that fixture, none through hand-built rows).
  </action>
  <verify>
    <automated>npx playwright test e2e/48-reopen-closed-ticket.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/48-reopen-closed-ticket.spec.ts -g "SC-3"` passes.
    - `npx playwright test e2e/48-reopen-closed-ticket.spec.ts` (full file, no `-g` filter) passes end-to-end — SC-1, SC-2, SC-3 all green in one run.
    - `grep -c "seedPaidTab(db" e2e/48-reopen-closed-ticket.spec.ts` returns 0 (old hand-built helper call fully retired; only `seedPaidTabViaDirectSale` call sites remain).
  </acceptance_criteria>
  <done>SC-3 is proven end-to-end by Playwright; the full spec file (SC-1 through SC-4) is green together, closing out SALE-03 for this phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (any authenticated staff session) -> `remove_tab_item` RPC | `remove_tab_item` deliberately has no server-side role check (existing D-07 design note in its own migration, matching `useRemoveTabItem`'s pre-existing bartender-accessible level) — the new PIN gate added by this plan is the only enforcement point for the remove action reachable through this specific UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05 | Elevation of Privilege | `remove_tab_item` RPC (via `EditReopenedItemsPanel`'s new remove composition) | medium | accept | `remove_tab_item` has no server-side role check by deliberate design (its own migration's D-07 note). This plan's PIN gate matches the risk profile `RemoveTabItemDialog`/`useRemoveTabItem` already carried before this phase (RESEARCH.md Open Question 2) — adding a server-side role check is explicitly out of scope per D-05's "reuse as-is, no rewrite" boundary. Not a regression introduced by this phase; flagged for a future phase/ticket if the team wants defense-in-depth here. |
| T-09-06 | Repudiation | `remove_tab_item`'s required `reason` field | low | accept | `RemoveTabItemDialog` already requires a non-empty reason (`canConfirm = trimmedReason.length > 0`) and the RPC logs it via `logger.error`/`logger.info` in `useRemoveTabItem.ts` — unchanged by this plan. |

No new package installs in this plan — no `T-*-SC` package-legitimacy row required.
</threat_model>

<verification>
- `npm run typecheck && npm run lint` clean.
- `npx playwright test e2e/48-reopen-closed-ticket.spec.ts` — full file green (SC-1, SC-2, SC-3 together).
- `npm run test` (full unit suite) still passes — no regression in `RemoveTabItemDialog.test.tsx`/`useRemoveTabItem.test.ts` (both untouched) or `PaymentPane.test.tsx`.
</verification>

<success_criteria>
SC-3 from ROADMAP.md Phase 9 is met: a manager can remove a line item from a reopened sale via `remove_tab_item`, with `RemoveTabItemDialog`/`useRemoveTabItem` rewiring confirmed complete (they were functional-but-unwired, not broken, per this phase's CONTEXT.md domain boundary). Combined with Plan 09-01 and 09-02, all five of Phase 9's success criteria (SC-1 through SC-5) are satisfied.
</success_criteria>

<output>
Create `.planning/phases/09-reopen-and-edit-a-completed-sale/09-03-SUMMARY.md` when done
</output>
