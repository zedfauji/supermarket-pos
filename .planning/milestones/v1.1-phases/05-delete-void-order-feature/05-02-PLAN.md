---
phase: 05-delete-void-order-feature
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - supabase/migrations/20260818000006_drop_void_order_permissions.sql
  - supabase/functions/void-order/index.ts
  - src/shared/lib/i18n/locales/es-MX/featOrders.json
  - src/shared/lib/i18n/locales/en-US/featOrders.json
autonomous: true
requirements: [SALE-01]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The role_permissions table no longer has ('manager', 'void_order') or ('admin', 'void_order') rows after a new forward migration runs — the historical seeding migration (20260510000001_rls_rewrite_phase13.sql) is never edited, per D-01."
    - "supabase/functions/void-order/ no longer exists as a deployable directory — the network-reachable POST /functions/v1/void-order endpoint is gone, per D-02."
    - "Neither locale's featOrders.json catalog has a voidOrder key block anymore, and both locale files remain valid JSON with identical key sets to each other (this project's i18n key-parity convention)."
  artifacts:
    - "supabase/migrations/20260818000006_drop_void_order_permissions.sql"
  key_links:
    - "This migration's DELETE target ('manager'/'admin', 'void_order') is the exact DB-side mirror of 05-01-PLAN.md's rbac.ts STAFF_ACTIONS/MANAGER_EXTRA removal — both plans drop the same permission grant, one client-side, one DB-side, closing D-01's two-place removal."
    - "orders.status='voided' and its report-RPC consumers (get_voids_report, close_caja_session) are a completely separate schema object from the role_permissions rows this migration deletes — the migration's WHERE clause only ever matches action = 'void_order', never touches the orders table or its enum."
  prohibitions:
    - statement: "Must not edit supabase/migrations/20260510000001_rls_rewrite_phase13.sql in place — it is the historical seeding migration for these rows; removal is always a new forward migration in this codebase's convention, never a rewrite of history."
      disposition: flagged-unverified
    - statement: "Must not touch the orders.status='voided' enum value, any row carrying it, get_voids_report, or close_caja_session — these are explicitly out of scope and unrelated to the role_permissions rows this migration targets."
      disposition: flagged-unverified
---

<objective>
Delete the void-order feature's backend/DB surface: a new forward migration removes its two role_permissions grant rows, its Supabase Edge Function directory is deleted outright, and its i18n copy is removed from both locale catalogs — per SALE-01 and CONTEXT.md D-01/D-02, sequenced after 05-01-PLAN.md so the client-side callers of both the edge function and the permission are already gone before this plan removes what they would have called.

Purpose: this is the phase's backend/DB half of the RBAC lockstep decision (D-01) and the removal of the last network-reachable artifact (the edge function) and the last user-visible copy (i18n) tied to the deleted feature. Landing it after 05-01-PLAN.md means no window exists where a live client caller points at a permission or endpoint this plan is about to delete.

Output: a new migration file deleting the two role_permissions rows (with a documentation-only, non-executable DOWN comment per this repo's convention); supabase/functions/void-order/ no longer exists; both featOrders.json locale catalogs have no voidOrder key block.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-delete-void-order-feature/05-CONTEXT.md
@.planning/phases/05-delete-void-order-feature/05-RESEARCH.md
@.planning/phases/05-delete-void-order-feature/05-PATTERNS.md
@supabase/migrations/20260703000006_role_permissions_view_audit_log.sql
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: New forward migration — DELETE the role_permissions grant rows</name>
  <files>supabase/migrations/20260818000006_drop_void_order_permissions.sql</files>
  <precondition>05-01-PLAN.md's rbac.ts change has landed (the client-side Action union no longer admits the permission this migration removes at the DB layer) — confirmed by 05-01-PLAN.md's SUMMARY or, if running out of order, by `grep -c "'void_order'" src/shared/lib/rbac.ts` returning 0.</precondition>
  <read_first>supabase/migrations/20260703000006_role_permissions_view_audit_log.sql in full (18 lines — the direct structural precedent: header comment explaining what/why, UP block wrapped in BEGIN/COMMIT, then a commented-out, non-executable DOWN block showing the mirror-image statement); 05-RESEARCH.md's Code Examples section (the exact DELETE statement and DOWN-comment text this migration should use); `ls supabase/migrations/ | tail -5` at execute time to confirm the next available timestamp is still after the latest existing file (research found 20260818000005 as the latest at research time — re-verify since new migrations may have landed since).</read_first>
  <action>Re-run `ls supabase/migrations/ | tail -5` first and confirm no migration with a timestamp of 20260818000006 or later already exists; if one does, pick the next available lexicographic timestamp instead and rename the file accordingly. Create the migration file with a header comment stating this closes SALE-01/D-01 and explicitly noting that supabase/migrations/20260510000001_rls_rewrite_phase13.sql (the original seeding migration) is historical and is not edited. The UP block, wrapped in BEGIN;/COMMIT;, deletes the two role_permissions rows for roles 'manager' and 'admin' where the action column matches the void-order permission name — use a single DELETE ... WHERE role IN ('manager', 'admin') AND action = <the permission name>, matching 05-RESEARCH.md's Code Examples exactly. Follow it with a commented-out (non-executable, `-- `-prefixed) DOWN block showing the mirror-image INSERT with ON CONFLICT DO NOTHING, per this repo's documentation-only-DOWN convention (CLAUDE.md's "Migration DOWN scripts" note) — do not make the DOWN block a live statement.

[BLOCKING] Apply the migration to the local self-hosted Supabase stack before verifying: try `supabase db push --db-url "postgresql://postgres.<POOLER_TENANT_ID>:<POSTGRES_PASSWORD>@localhost:5432/postgres?sslmode=disable" --debug` first (credentials via `docker exec supabase-db env` / `docker exec supabase-pooler env`); if the pooler is unreachable, fall back to `docker exec -i supabase-db psql -U supabase_admin -d postgres < supabase/migrations/20260818000006_drop_void_order_permissions.sql` then register it with `docker exec supabase-db psql -U supabase_admin -d postgres -c "INSERT INTO supabase_migrations.schema_migrations (version, name) VALUES ('20260818000006', 'drop_void_order_permissions') ON CONFLICT (version) DO NOTHING"`.</action>
  <verify>
    <automated>docker exec supabase-db psql -U supabase_admin -d postgres -c "select role, action from role_permissions where action = 'void_order'"</automated>
  </verify>
  <acceptance_criteria>The verify query returns zero rows. The new migration file exists, its UP block is a real executable DELETE wrapped in BEGIN/COMMIT, and its DOWN block is a commented-out (non-executable) INSERT. supabase/migrations/20260510000001_rls_rewrite_phase13.sql is byte-for-byte unchanged (confirm via `git diff --stat supabase/migrations/20260510000001_rls_rewrite_phase13.sql` showing no output).</acceptance_criteria>
  <done>The role_permissions table no longer grants the deleted feature's action to any role, applied via a new forward migration that leaves the historical seeding migration untouched.</done>
  <reversibility rating="costly">D-01: undoing this DELETE requires a new migration re-inserting the two rows, coordinated with reverting 05-01-PLAN.md's rbac.ts type change — costly but not one-way, since role_permissions rows are pure permission grants with no user data, and this exact decision (including its costly rating) was already locked in CONTEXT.md; no checkpoint is inserted here because the door was already walked through at discuss-phase, not because the operation is trivial.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete the void-order Edge Function directory</name>
  <files>supabase/functions/void-order/index.ts</files>
  <read_first>supabase/functions/void-order/index.ts in full (116 lines — confirm no other file in supabase/functions/ imports from this directory before deleting; RESEARCH.md's Runtime State Inventory already confirmed no dedicated secret/env var is scoped to this function beyond the shared ambient Supabase auth pattern).</read_first>
  <action>Delete the entire supabase/functions/void-order/ directory (index.ts and the directory itself). No live client caller remains after 05-01-PLAN.md removed callVoidOrder and its registry entry, and no other edge function imports from this directory, so this is a standalone deletion with no coordinated edit elsewhere. This removes the last network-reachable POST /functions/v1/void-order endpoint per D-02 — it was reachable by any caller holding a valid session Bearer token regardless of client-UI RBAC gating, since the function itself never re-checked role_permissions server-side.</action>
  <verify>
    <automated>test ! -d supabase/functions/void-order && npx vitest run src/shared/lib/__tests__/audit-edge-coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>supabase/functions/void-order/ no longer exists on disk. audit-edge-coverage.test.ts (already updated in 05-01-PLAN.md to drop this directory from its allowlist) still passes — it no longer expects this directory to exist.</acceptance_criteria>
  <done>The deployable edge function artifact is gone; the only remaining reversal-capable network endpoint reachable from the app is process-refund.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Remove the voidOrder i18n key block from both locale catalogs</name>
  <files>src/shared/lib/i18n/locales/es-MX/featOrders.json, src/shared/lib/i18n/locales/en-US/featOrders.json</files>
  <read_first>src/shared/lib/i18n/locales/en-US/featOrders.json lines 85-96 (the voidOrder key block, immediately after removeTabItem and immediately before editPaidTab) and src/shared/lib/i18n/locales/es-MX/featOrders.json lines 85-96 (the same block, same position, Spanish values) — both files are 169 lines with identical key structure/ordering per this project's i18n key-parity convention.</read_first>
  <action>Delete the voidOrder key block (the object with keys reasonRequired/title/description/confirmLabel/orderVoided/orderTime/unknownItem/totalVoided/voidReasonLabel/requiredReasonPlaceholder) from both es-MX/featOrders.json and en-US/featOrders.json, in the same task so the two catalogs never drift out of key-parity even momentarily. After deletion, removeTabItem's closing brace should be followed directly by editPaidTab's opening key in both files. Confirm both files remain valid JSON after the edit (a trailing comma left behind from the deleted block, or a missing comma before editPaidTab, is the most likely mistake).</action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('src/shared/lib/i18n/locales/es-MX/featOrders.json','utf8')); JSON.parse(require('fs').readFileSync('src/shared/lib/i18n/locales/en-US/featOrders.json','utf8')); console.log('valid')" && node -e "const a=Object.keys(require('./src/shared/lib/i18n/locales/es-MX/featOrders.json')).sort(); const b=Object.keys(require('./src/shared/lib/i18n/locales/en-US/featOrders.json')).sort(); if (JSON.stringify(a)!==JSON.stringify(b)) { console.error('key mismatch'); process.exit(1); } console.log('parity ok')"</automated>
  </verify>
  <acceptance_criteria>Both featOrders.json locale files parse as valid JSON. Both files' top-level key sets are identical (no key-parity drift introduced by the deletion). Neither file's raw text contains a "voidOrder" key anymore.</acceptance_criteria>
  <done>Both locale catalogs have the void-order copy removed, remain valid JSON, and stay in key-parity with each other.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `role_permissions` table (server-trusted RBAC state) → any RPC/edge function that reads it | Plain-`text` action column, no FK/CHECK constraint — a stale row is a dormant permission grant that nothing currently checks, but nothing prevents a future caller from checking it |
| Supabase Edge Function `/functions/v1/void-order` (network-reachable endpoint) → Postgres (service-role write path) | Deployed function reachable by any caller holding a valid Supabase session Bearer token, independent of whether any client UI currently calls it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Elevation of Privilege | `role_permissions` rows `('manager','void_order')` / `('admin','void_order')` | medium | mitigate | New forward migration DELETEs both rows so no dormant server-trusted grant remains for a feature with no live enforcement point — closes the stale-grant/privilege-creep pattern RESEARCH.md identified, verified via a direct `psql` query confirming zero matching rows post-migration. |
| T-05-04 | Elevation of Privilege | `supabase/functions/void-order/` (deployed edge function, network-reachable) | medium | mitigate | Delete the function directory outright (D-02) — this was a live `POST /functions/v1/void-order` endpoint reachable by anyone with a valid session Bearer token regardless of client-UI RBAC gating (the function itself never re-checked `role_permissions` server-side, per RESEARCH.md's Runtime State Inventory); removing it eliminates that reachable surface entirely rather than leaving an un-called-but-deployed function live. |

No package-manager installs occur in this plan — no `T-05-SC` supply-chain row applies.
</threat_model>

<verification>
- After Task 1: the direct `psql` query for `action = 'void_order'` in `role_permissions` returns zero rows; the historical seeding migration is unchanged (`git diff --stat` empty).
- After Task 2: `supabase/functions/void-order/` no longer exists; `audit-edge-coverage.test.ts` passes.
- After Task 3: both locale files parse as valid JSON and remain key-parity-identical.
- Full-plan gate: `npm run typecheck && npm run lint && npm run test` exits 0 with all three tasks' changes applied together (05-01-PLAN.md already landed per `depends_on`).
</verification>

<success_criteria>
The role_permissions DB grant is gone via a new forward migration (historical migration untouched), the edge function directory no longer exists, and both i18n catalogs are copy-clean and still key-parity-matched — the backend/DB half of SALE-01 is complete and 05-03-PLAN.md can proceed with the E2E sweep and the SC1/SC2 verification gates.
</success_criteria>

## Artifacts this plan produces

- `supabase/migrations/20260818000006_drop_void_order_permissions.sql` — the phase's one newly-created file; every other change in this plan and 05-01-PLAN.md is an edit or deletion of an existing file.

<output>
Create `.planning/phases/05-delete-void-order-feature/05-02-SUMMARY.md` when done
</output>