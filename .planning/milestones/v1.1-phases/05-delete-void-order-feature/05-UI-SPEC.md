---
phase: 5
slug: delete-void-order-feature
status: approved
shadcn_initialized: true
preset: radix-nova
created: 2026-08-16
reviewed_at: 2026-08-16
---

# Phase 5 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

**Phase type: deletion-only.** This phase removes the orphaned `void-order` feature end-to-end (client component, edge function, RBAC seed rows, i18n keys, E2E spec). No new UI is designed, no new copy is written, no new spacing/typography/color decisions are needed. This contract exists to document what disappears and confirm zero visual regression, per project CLAUDE.md instruction to scope UI-SPEC to what the phase's file changes actually touch.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (already initialized — `components.json` present) |
| Preset | `radix-nova` (existing project preset, unchanged by this phase) |
| Component library | Radix primitives via shadcn |
| Icon library | lucide |
| Font | project default (unchanged by this phase — no typography added or removed) |

This phase makes **zero changes** to `tailwind.config.ts`, `components.json`, or any design token. All fields above are recorded for reference only, not because this phase modifies them.

---

## Spacing Scale

Not applicable — no layout is added, resized, or reflowed by this phase.

**Reflow check (confirmed, not assumed):** `VoidOrderDialog`/`useVoidOrder` have **zero live importers** outside `src/features/void-order/` itself (confirmed via grep, recorded in `05-CONTEXT.md` code_context section). The void-order button/dialog is not currently mounted in any toolbar, menu, or panel in the running app — it is dead code, not a rendered-but-hidden control. Therefore deleting the feature folder removes no visible element from any screen, and there is **no adjacent control to reflow, no gap to fill, no spacing token to reapply**. `PaymentPane`/`/payments` (where a reversal action would live) already renders only the refund action; nothing changes there.

Only two files outside the feature folder reference `void_order`/`void-order` at all: `src/shared/lib/rbac.ts` (a TypeScript union member + permission-action array — not a UI file) and `src/shared/lib/edge-function-contracts.ts` (a schema/registry file — not a UI file). Neither renders anything.

Exceptions: none.

---

## Typography

Not applicable — no text styling is added, changed, or removed. The only text deleted is i18n keys (`featOrders.json` → `voidOrder` block) that were never rendered (no live importer of the component that would have consumed them).

---

## Color

Not applicable — no color, including destructive/danger tokens, is added, changed, or removed. The void confirmation dialog being deleted (`VoidOrderDialog`) used the existing destructive-button styling from `shared/ui`; that shared primitive is untouched and remains available for other destructive flows (e.g. refund's existing manager-PIN-gated confirm).

---

## Copywriting Contract

Not applicable in the standard sense — this phase **removes** copy, it adds none.

| Element | Copy |
|---------|------|
| Primary CTA | n/a — no new CTA added this phase |
| Empty state heading | n/a — no new empty state added this phase |
| Empty state body | n/a |
| Error state | n/a — no new error state added this phase |
| Destructive confirmation | n/a — the void-order destructive confirmation dialog is being **deleted**, not added. No new confirmation UI is introduced. Refund's existing manager-PIN-gated confirmation flow (already in `PaymentPane`) is the sole surviving reversal-confirmation UI and is explicitly out of scope for this phase (must keep passing `e2e/35-refund.spec.ts` unchanged per CONTEXT.md/ROADMAP.md success criteria). |

**Copy actually being deleted this phase** (for completeness, not because it needs replacement):
- `src/shared/lib/i18n/locales/es-MX/featOrders.json` → `voidOrder` key block
- `src/shared/lib/i18n/locales/en-US/featOrders.json` → `voidOrder` key block

No adjacent label, menu item, or copy elsewhere in the app references or depends on these keys (confirmed: `VoidOrderDialog` had zero live importers, so nothing else was consuming this copy at render time). No replacement copy is needed.

---

## UI Considerations

Applicable state considerations resolved via `ui-consideration-probe.cjs` against element E1 (VoidOrderDialog trigger + confirmation dialog): 4 applicable, 4 dismissed, 0 unresolved.

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| loading | E1 — VoidOrderDialog trigger/dialog | dismissed | Component has zero live importers (confirmed via grep, `05-CONTEXT.md`) — never mounted, never rendered, being deleted outright with no replacement. There is no loading state to design because there is no render path that would ever show one. |
| error | E1 — VoidOrderDialog trigger/dialog | dismissed | Same reason — dead code being removed, not built. No submit/load path exists to fail. |
| overflow | E1 — VoidOrderDialog trigger/dialog | dismissed | Same reason — no rendered container exists to overflow. |
| long-text | E1 — VoidOrderDialog trigger/dialog | dismissed | Same reason — no rendered text exists to wrap/truncate. |

The one state genuinely relevant to this phase — "does the remaining UI regress after deletion" — is answered in Spacing Scale above: no, because there was no rendered UI to begin with. E2E verification (Success Criteria #1: "no void-order button or menu action is reachable from any screen") should assert **absence** across screens that plausibly could have hosted it (`/pos`, `/payments`), which the phase's Playwright coverage already accounts for — a test-authoring concern for the planner, not a new UI-SPEC state.

The one state that IS relevant to this phase — "does the remaining UI regress after deletion" — is answered directly in Spacing Scale above: no, because there was no rendered UI to begin with. E2E verification for this (Success Criteria #1: "no void-order button or menu action is reachable from any screen") should assert **absence** across screens that plausibly could have hosted it (`/pos`, `/payments`), which the phase's Playwright coverage (per ROADMAP.md Success Criteria) already accounts for — this is a test-authoring concern for the planner, not a new UI-SPEC state.

---

## Registry Safety

Not applicable — no shadcn component is added, and none is removed (the destructive-button/dialog primitives used by `VoidOrderDialog` are shared `shared/ui` components that remain in use elsewhere, e.g. refund's confirmation flow; only the feature-level wrapper component is deleted).

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none added/removed this phase | not required |

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS — n/a, no copy added; deleted copy has no live consumer
- [x] Dimension 2 Visuals: PASS — n/a, no visual element added or reflowed; deleted element had zero live importers
- [x] Dimension 3 Color: PASS — n/a, no color token touched
- [x] Dimension 4 Typography: PASS — n/a, no typography touched
- [x] Dimension 5 Spacing: PASS — confirmed no reflow needed (dead code, not a mounted control)
- [x] Dimension 6 Registry Safety: PASS — n/a, no registry component added/removed

**Approval:** APPROVED by gsd-ui-checker (all 6 dimensions PASS, 2026-08-16)
