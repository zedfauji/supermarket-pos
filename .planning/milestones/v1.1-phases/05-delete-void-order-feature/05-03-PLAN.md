---
phase: 05-delete-void-order-feature
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - e2e/09-rbac.spec.ts
  - e2e/18-void-order.spec.ts
  - e2e/helpers/supabase.ts
  - e2e/global-teardown.ts
autonomous: true
requirements: [SALE-01]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "e2e/18-void-order.spec.ts no longer exists, and the full Playwright suite passes without it (ROADMAP Phase 5 SC3)."
    - "A new automated Playwright test proves no void-order button, menu action, or confirmation dialog is reachable from /pos or /payments (ROADMAP Phase 5 SC1) — replacing the old dialog-behavior spec, which tested UI that no longer exists, with a direct absence assertion."
    - "e2e/07-reports.spec.ts and e2e/02-caja.spec.ts (voids report + caja close) still pass unchanged, proving orders.status='voided' and its report-RPC consumers survived the deletion untouched (ROADMAP Phase 5 SC4)."
    - "e2e/35-refund.spec.ts still passes unchanged, confirming refund remains the sole staff-facing reversal path (ROADMAP Phase 5 SC5)."
    - "A single repo-wide case-insensitive grep for the void-order feature's name variants, scoped to exclude .planning/ (append-only historical record) and this plan's own new migration's DOWN-comment text, returns zero matches (ROADMAP Phase 5 SC2)."
  artifacts:
    - "e2e/09-rbac.spec.ts (T8/T9 deleted; new absence-assertion test added)"
  key_links:
    - "The new absence-assertion test in e2e/09-rbac.spec.ts is the concrete Playwright proof for ROADMAP Phase 5 SC1 — it is the only place in this phase's plan set where SC1 is directly exercised, not merely implied by the deletion of other files."
    - "The final repo-wide grep in Task 2 is the concrete proof for ROADMAP Phase 5 SC2's 'automated grep/lint check returns zero matches' requirement — a one-off command run during this task's verification, not a persisted script, per CONTEXT.md's Claude's Discretion note."
  prohibitions:
    - statement: "Must not delete or reword e2e/38-audit-logs.spec.ts's unskipped 'order.void filter' test or src/shared/lib/audit-actions.ts's historical audit-action enum entry it depends on — both stay exactly as they are; this plan's grep gate excludes comment-only, harmless references in e2e/38-audit-logs.spec.ts and src/entities/tab/model/queries.concurrent.test.ts rather than editing them."
      disposition: flagged-unverified
    - statement: "Must not seed a voidable order or reconstruct any part of the deleted VoidOrderDialog's behavior (reason validation, inventory restore, subtotal update) in the new absence-assertion test — SC1 only requires proving the trigger control is absent, which needs no seeded order at all."
      disposition: flagged-unverified
---

<objective>
Close out SALE-01's E2E surface: delete the dead void-order spec and its now-unused seed helper, remove the two already-skipped RBAC tests that still drive the deleted dialog, add the one new Playwright test that actually proves SC1 (absence), and run the phase's full verification gate (grep-zero-matches for SC2, plus the untouched-dependency specs for SC4/SC5) — per SALE-01 and CONTEXT.md D-03/D-04, after 05-01-PLAN.md and 05-02-PLAN.md have removed every production reference this plan's tests would otherwise still exercise.

Purpose: this is the phase's final layer — proving, with automated Playwright/Vitest runs (per this project's CLAUDE.md testing policy), that the deletion is both complete (SC2, SC3) and safe (SC1, SC4, SC5). Nothing in 05-01/05-02 is itself proof that the feature is unreachable from a real browser session or that its still-live dependents survived; this plan supplies that proof.

Output: e2e/18-void-order.spec.ts deleted; e2e/09-rbac.spec.ts's T8/T9 deleted and a new absence-assertion test added; e2e/helpers/supabase.ts's seedVoidableOrder deleted; e2e/global-teardown.ts's stale SUITE_MAP label removed; a clean repo-wide grep; and green runs of the full unit suite, the full Playwright suite, and the specific voids-report/caja-close/refund specs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-delete-void-order-feature/05-CONTEXT.md
@.planning/phases/05-delete-void-order-feature/05-RESEARCH.md
@e2e/helpers/auth.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete the dead spec/helper/skipped-tests and add the SC1 absence-assertion test</name>
  <files>e2e/09-rbac.spec.ts, e2e/18-void-order.spec.ts, e2e/helpers/supabase.ts, e2e/global-teardown.ts</files>
  <read_first>e2e/09-rbac.spec.ts lines 1-16 (imports, the payButton helper, and the describe/beforeEach block using loginAs/resetTestState/openCaja — the pattern the new test should reuse) and lines 149-207 (the two test.skip blocks to delete, T8 lines 149-177 and T9 lines 179-207, each with its own leading explanatory comment; T10 at line 215 onward is unrelated to the deleted feature and must not be touched); e2e/18-void-order.spec.ts in full (confirms the file self-skips and has no other spec depending on it); e2e/helpers/supabase.ts lines 397-447 (the seedVoidableOrder function and its own JSDoc — the "New helpers added for specs 18-26" section comment above it at line 398 applies to multiple helpers and must stay; only the seedVoidableOrder function itself, lines 402-447, is deleted); e2e/global-teardown.ts lines 31-57 (the SUITE_MAP array — the deleted-spec label sits between the 17-payment-pane and 19-caja-entries entries, alongside several other already-stale Phase-1-era entries that remain unmaintained by convention).</read_first>
  <action>Delete e2e/18-void-order.spec.ts entirely. In e2e/09-rbac.spec.ts, delete both T8 and T9 (each including its leading comment block) — both are already test.skip()'d and both drive the deleted dialog through the long-gone /pos tab-based flow; leave every other test in the file, including T10, untouched. In the same file, in the gap where T8/T9 used to sit, add one new, unskipped test named "void-order control is absent from every screen it could plausibly appear on": log in as manager (a role that previously had the permission), navigate to /pos and then /payments, and assert zero count for both a button with an accessible name matching a void-order pattern and an alertdialog with the same pattern — no seeded order, no tab setup, since this only needs to prove the trigger control's absence, not exercise any order lifecycle. Log out at the end.

In e2e/helpers/supabase.ts, delete the seedVoidableOrder function (its own JSDoc plus the function body) — it is used only by the file just deleted; leave the "New helpers added for specs 18-26" section comment above it in place since other helpers in that section are still used elsewhere. In e2e/global-teardown.ts, remove the SUITE_MAP entry whose regex matches the deleted spec's filename prefix — this is cosmetic (an unmatched regex is silently ignored by the report generator, confirmed by several other already-stale Phase-1-era entries in the same array), but removing it keeps this task's own grep-based acceptance criteria genuinely clean.</action>
  <verify>
    <automated>test ! -f e2e/18-void-order.spec.ts && npx playwright test e2e/09-rbac.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>e2e/18-void-order.spec.ts no longer exists. `npx playwright test e2e/09-rbac.spec.ts --list` succeeds (file parses, no syntax errors) and shows one fewer test.skip entry than before plus one new non-skipped test. e2e/helpers/supabase.ts no longer exports seedVoidableOrder. e2e/global-teardown.ts's SUITE_MAP no longer has an entry matching the deleted spec's filename prefix.</acceptance_criteria>
  <done>The dead spec, its two already-skipped dependent tests, and its now-unused seed helper are gone, and a real, unskipped Playwright test exists proving the void-order control is unreachable from /pos and /payments.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Full verification gate — SC1-SC5</name>
  <files>None (verification-only task; no files created or modified).</files>
  <read_first>05-RESEARCH.md's Validation Architecture section (the Phase Requirements → Test Map table mapping each of ROADMAP Phase 5's 5 success criteria to a concrete automated command) and its Code Examples section (the exact repo-wide grep command this task's SC2 check should run, including its `.planning/`-exclusion rationale).</read_first>
  <action>Run, in order, and record the result of each: (1) `npm run typecheck && npm run lint && npm run test` — the full local pipeline, covering every edit across 05-01/05-02/05-03-PLAN.md together for the first time in one pass; (2) the repo-wide case-insensitive grep from 05-RESEARCH.md's Code Examples, scoped to exclude node_modules/.git/.planning (the last because it is an append-only historical record that legitimately still narrates the deleted feature, per CONTEXT.md/RESEARCH.md) — the only acceptable surviving match is the new migration's own commented DOWN-block text documenting the rollback shape, which the grep command itself should exclude or which must be manually confirmed as the sole hit if not excluded; (3) `npx playwright test e2e/09-rbac.spec.ts` for SC1; (4) `npx playwright test e2e/07-reports.spec.ts e2e/02-caja.spec.ts` for SC4 (voids report + caja close, unchanged); (5) `npx playwright test e2e/35-refund.spec.ts` for SC5 (refund unchanged); (6) the full `npm run test:e2e` suite for SC3 (green without e2e/18-void-order.spec.ts in the run at all, since it no longer exists). Do not persist the grep command as a new npm script or CI job — CONTEXT.md's Claude's Discretion note explicitly allows a one-off command here.</action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run test</automated>
  </verify>
  <acceptance_criteria>All six checks in the action above pass: typecheck/lint/unit-test pipeline exits 0; the repo-wide grep returns no output outside the new migration's own DOWN-comment text; e2e/09-rbac.spec.ts's new absence test passes; e2e/07-reports.spec.ts and e2e/02-caja.spec.ts pass unchanged; e2e/35-refund.spec.ts passes unchanged; the full npm run test:e2e suite is green.</acceptance_criteria>
  <done>Every one of ROADMAP Phase 5's 5 success criteria (SC1-SC5) is proven by a concrete automated command run in this task, closing SALE-01 end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A — this plan modifies only Playwright specs, an E2E seed helper, and a teardown-report label; no production code path or trust boundary is touched | — |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering (test-only) | e2e/helpers/supabase.ts (seedVoidableOrder removal), e2e/09-rbac.spec.ts (T8/T9 removal + new absence test) | low | accept | Test-fixture/spec-only changes; no production RLS/auth path is touched, and the new absence-assertion test is itself the automated proof that 05-01/05-02's RBAC surface reduction holds true end-to-end in a real browser session — accepted as the phase's terminal verification layer, not a new attack surface. |

No package-manager installs occur in this plan — no `T-05-SC` supply-chain row applies.
</threat_model>

<verification>
See Task 2's action list — it is this plan's (and the phase's) full verification gate, covering all 5 ROADMAP Phase 5 success criteria in one task.
</verification>

<success_criteria>
All 5 of ROADMAP Phase 5's success criteria are proven true by automated commands run in this plan: no void-order control is reachable (SC1), a repo-wide grep confirms zero remaining references outside the append-only `.planning/` history and this phase's own migration DOWN-comment (SC2), the dead spec is gone and the full suite is green without it (SC3), the voids-report/caja-close dependents survive unchanged (SC4), and refund remains the sole reversal path (SC5). SALE-01 is complete.
</success_criteria>

## Artifacts this plan produces

None — every task in this plan edits or deletes existing files, or runs verification commands only. This plan produces no new files.

<output>
Create `.planning/phases/05-delete-void-order-feature/05-03-SUMMARY.md` when done
</output>
