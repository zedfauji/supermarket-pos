---
phase: 05-delete-void-order-feature
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/void-order/model/useVoidOrder.ts
  - src/features/void-order/model/useVoidOrder.test.tsx
  - src/features/void-order/ui/VoidOrderDialog.tsx
  - src/features/void-order/ui/VoidOrderDialog.test.tsx
  - src/features/void-order/index.ts
  - src/shared/lib/rbac.ts
  - src/shared/ui/ProtectedAction.test.tsx
  - src/shared/lib/rbac.test.ts
  - src/shared/lib/edge-function-contracts.ts
  - src/shared/lib/__tests__/audit-edge-coverage.test.ts
autonomous: true
requirements: [SALE-01]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "src/features/void-order/ no longer exists in the repo, and npm run typecheck / npm run lint / npm run test all exit 0 immediately after its deletion, proving the folder had zero live importers (per SALE-01, matching CONTEXT.md's and RESEARCH.md's confirmed-orphaned finding)."
    - "The Action union in src/shared/lib/rbac.ts no longer accepts the removed permission as a valid value in either STAFF_ACTIONS or MANAGER_EXTRA (D-01's client-side half — the DB half lands in 05-02-PLAN.md's migration)."
    - "src/shared/lib/edge-function-contracts.ts no longer exports the request/response schemas or caller function for the deleted feature, and its central edge-function registry object has no entry for it."
    - "npm run typecheck exits 0 after the rbac.ts change, because ProtectedAction.test.tsx's now-invalid action literal was swapped to a still-existing MANAGER_EXTRA member (process_refund) in the same task, not left broken for a later plan to fix."
  artifacts:
    - "src/shared/lib/rbac.ts (STAFF_ACTIONS/MANAGER_EXTRA with the removed action gone)"
    - "src/shared/lib/edge-function-contracts.ts (without the deleted feature's schemas/caller/registry entry)"
  key_links:
    - "src/shared/lib/rbac.ts's STAFF_ACTIONS (client-side type source) stays structurally in sync with the role_permissions DB rows removed in 05-02-PLAN.md's migration — both drop the same action name together, closing D-01's lockstep requirement across plans."
    - "src/shared/ui/ProtectedAction.test.tsx's action prop literal must always be a live StaffAction union member — the swap to process_refund keeps this test compiling against the narrower post-deletion union while preserving its 'manager-tier action denied to cashier' intent."
  prohibitions:
    - statement: "SALE-01 is a deletion requirement outside the specless-probe's standard edge-shape taxonomy (add/update/query shapes); its correctness is instead verified directly via the phase's 5 ROADMAP success criteria (absence assertions + untouched-dependency assertions), which supersede a generic edge-shape check for this phase."
      disposition: flagged-unverified
    - statement: "Must not delete or alter src/shared/lib/audit-actions.ts's historical audit-action enum entry for the deleted feature — it has no future writer once the edge function is deleted (05-02-PLAN.md), but it is historical audit-trail data actively exercised by e2e/38-audit-logs.spec.ts's unskipped filter test, and out of scope for every task in this plan."
      disposition: flagged-unverified
    - statement: "Must not remove RBAC coverage for any StaffAction other than the one being deleted while editing rbac.ts's STAFF_ACTIONS/MANAGER_EXTRA — the cashier-tier and kitchen-tier action sets never contained it and must be left untouched."
      disposition: flagged-unverified
---

<objective>
Delete the orphaned void-order client feature end-to-end from the frontend/shared-lib tier — the feature folder itself, its client-side RBAC action, and its edge-function contract exports — while keeping the whole build/lint/unit-test pipeline green after every task, per SALE-01 and CONTEXT.md D-01/D-02/D-03/D-04.

Purpose: this is the phase's frontend/shared-lib half of the RBAC lockstep decision (D-01) and the client contract surface for the edge function being deleted in 05-02-PLAN.md. Landing it first (before the DB migration and edge function deletion) proves the deletion methodology on the lowest-risk, zero-live-importer layer before touching state that requires a database migration.

Output: `src/features/void-order/` no longer exists; `src/shared/lib/rbac.ts`'s Action union no longer admits the deleted action; `src/shared/lib/edge-function-contracts.ts` no longer exports its schemas/caller or lists it in the registry; every test file that referenced any of the above (`ProtectedAction.test.tsx`, `rbac.test.ts`, `audit-edge-coverage.test.ts`) is updated to match, and `npm run typecheck && npm run lint && npm run test` all pass.

Note on UI-SPEC.md: its 4 state-consideration rows (loading/error/overflow/long-text) for the deleted dialog element were all resolved `dismissed` — not because no UI work was needed, but because 05-UI-SPEC.md's own grep evidence (mirroring CONTEXT.md's code_context finding) confirmed the component had zero live importers and was never actually rendered. That "no visual regression from deleting this" claim is therefore grep-verified fact, not an assumption this plan takes on faith — Task 1 independently re-confirms the same zero-importers fact before deleting.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-delete-void-order-feature/05-CONTEXT.md
@.planning/phases/05-delete-void-order-feature/05-RESEARCH.md
@.planning/phases/05-delete-void-order-feature/05-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1 (tracer): Delete the void-order feature folder end-to-end and prove zero regressions</name>
  <files>src/features/void-order/model/useVoidOrder.ts, src/features/void-order/model/useVoidOrder.test.tsx, src/features/void-order/ui/VoidOrderDialog.tsx, src/features/void-order/ui/VoidOrderDialog.test.tsx, src/features/void-order/index.ts</files>
  <read_first>05-CONTEXT.md's code_context section (confirms zero live importers of VoidOrderDialog/useVoidOrder outside their own folder) and 05-RESEARCH.md's Summary + Sources (confirms this was re-verified via a full-repo grep this session, not assumed from an earlier session).</read_first>
  <action>Before deleting, re-run the orphan check yourself (do not trust CONTEXT.md's timestamp blindly — confirm nothing has changed since research): grep the whole src/ tree for the component and hook export names and confirm the only hits are inside the feature's own folder. Once confirmed, delete the entire feature folder — all 5 files: both files under model/, both files under ui/, and index.ts — per D-02/D-03. Do not touch any file outside this folder in this task; this is deliberately the lowest-risk removal first (zero external importers), establishing that the deletion methodology (delete, then run the full local pipeline) is sound before Task 2/3 touch shared risk-bearing files that other code still imports from.</action>
  <verify>
    <automated>test ! -d src/features/void-order && npm run typecheck && npm run lint && npm run test</automated>
  </verify>
  <acceptance_criteria>`src/features/void-order/` no longer exists on disk. `npm run typecheck`, `npm run lint`, and `npm run test` all exit 0 with only this folder removed and no other files modified in this task.</acceptance_criteria>
  <done>The client feature is fully deleted and the entire local build/lint/unit-test pipeline is green, proving the deletion is safe before Task 2/3 touch shared files other code still imports.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove the void action from rbac.ts's Action union and fix the now-invalid ProtectedAction.test.tsx literal</name>
  <files>src/shared/lib/rbac.ts, src/shared/ui/ProtectedAction.test.tsx, src/shared/lib/rbac.test.ts</files>
  <read_first>src/shared/lib/rbac.ts lines 13-35 (STAFF_ACTIONS array — the action to remove sits between 'close_tab' and 'view_reports' — and the derived StaffAction type at line 35) and lines 46-58 (MANAGER_EXTRA set — the same action sits between 'close_tab' and 'view_reports'); src/shared/ui/ProtectedAction.test.tsx lines 18-30 ("disables child and shows manager denial tooltip when cashier lacks action" — the action prop on line 21 and its 'Manager access required' assertion on line 28); src/shared/lib/rbac.test.ts lines 11-35 (the ALLOWED.manager mirror Set, which also contains the action being removed) and lines 76-79 (the rbacDenialMessage assertion for the same action, alongside the sibling close_tab assertion in the same it block).</read_first>
  <action>In src/shared/lib/rbac.ts, delete the void-order permission's entry from both the STAFF_ACTIONS array and the MANAGER_EXTRA set in this same task — removing only one desyncs the client-side type union from the runtime manager grant set, and D-01 requires both move together. Do not touch CASHIER_ACTIONS or KITCHEN_ACTIONS; neither ever contained this action. In src/shared/ui/ProtectedAction.test.tsx, swap the now-invalid action literal on line 21 (currently the void-order permission name) to process_refund — process_refund remains in MANAGER_EXTRA after this edit, so the existing assertion (a cashier sees the 'Manager access required' tooltip) still holds with no other line in that test changed. In src/shared/lib/rbac.test.ts, remove the void-order permission from the ALLOWED.manager mirror Set and delete its dedicated rbacDenialMessage expectation in the 'uses manager copy for manager-tier actions' test block, leaving the sibling close_tab assertion in that same block untouched.</action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/shared/lib/rbac.test.ts src/shared/ui/ProtectedAction.test.tsx</automated>
  </verify>
  <acceptance_criteria>npm run typecheck exits 0 (proves ProtectedAction.test.tsx compiles against the narrowed StaffAction union). npx vitest run src/shared/lib/rbac.test.ts src/shared/ui/ProtectedAction.test.tsx exits 0 with no failing tests. STAFF_ACTIONS and MANAGER_EXTRA in rbac.ts each have exactly one fewer entry than before this task, and both removed entries are the same permission name.</acceptance_criteria>
  <done>rbac.ts's Action union no longer admits the void-order permission, ProtectedAction.test.tsx compiles and passes against the narrower union using a different manager-tier action, and rbac.test.ts no longer asserts on the removed permission.</done>
  <reversibility rating="costly">D-01: re-adding this client-side removal later requires reverting the type change in rbac.ts and, together with 05-02-PLAN.md's migration, a new migration re-inserting the DB grant rows — costly but not one-way, since it is a pure permission-grant change with no user data at risk (CONTEXT.md D-01's own rationale; the decision is already locked, this tag records the weight for the record without gating execution).</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Remove the edge-function contract exports and its coverage-gate registration</name>
  <files>src/shared/lib/edge-function-contracts.ts, src/shared/lib/__tests__/audit-edge-coverage.test.ts</files>
  <read_first>src/shared/lib/edge-function-contracts.ts lines 704-787 (the request schema, response schema, and caller function for the deleted feature — three exports total) and lines 1224-1228 (the registry object entry mapping the deleted feature's kebab-case action name to those three exports, positioned between the 'send-receipt-email' and 'settings-backup' entries); src/shared/lib/__tests__/audit-edge-coverage.test.ts in full (25 lines — the SENSITIVE_EDGE_FUNCTIONS allowlist on line 25 includes the deleted feature's directory name, and the header JSDoc on lines 8 and 13 names it too; this test asserts every listed function's supabase/functions/<name>/index.ts exists and calls recordAudit — it will fail once 05-02-PLAN.md deletes that directory unless the name is removed from the allowlist here first).</read_first>
  <action>In src/shared/lib/edge-function-contracts.ts, delete the deleted feature's request schema export, response schema export, request type export, response type export, and caller function — the full block between the 'send-receipt-email' schemas above it and the 'settings-backup' schemas below it — and delete its one-entry object in the central edge-function registry (the entry sits between the 'send-receipt-email' and 'settings-backup' registry entries). In src/shared/lib/__tests__/audit-edge-coverage.test.ts, remove the deleted feature's directory name from the SENSITIVE_EDGE_FUNCTIONS array on line 25 (leaving 'create-staff' and 'settings-restore') and update the header JSDoc on lines 8 and 13 so it no longer describes the deleted feature as a pending/expected-RED coverage item — this array drives an existsSync check against supabase/functions/<name>/index.ts, and 05-02-PLAN.md deletes that directory, so this file must stop naming it in this task, before that directory disappears. NOTE: src/shared/lib/edge-function-contracts.test.ts is deliberately NOT edited in this task — CONTEXT.md D-04 and RESEARCH.md both list it as needing a void-order sweep, but that claim is stale: the file (verified by direct read, 289 lines) only imports and tests `ProcessPaymentRequestSchema`, `ReceiptDataSchema`, and `SendReceiptEmailRequestSchema` by name — it contains zero references to VoidOrder/void-order anything and does not iterate the registry generically, so deleting the void-order schemas/registry entry above cannot break it. It is still run in this task's verify command as a regression check, unmodified.</action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/shared/lib/__tests__/audit-edge-coverage.test.ts && npx vitest run src/shared/lib/edge-function-contracts.test.ts</automated>
  </verify>
  <acceptance_criteria>npm run typecheck exits 0. npx vitest run for both listed test files exits 0. SENSITIVE_EDGE_FUNCTIONS in audit-edge-coverage.test.ts has exactly 2 entries (create-staff, settings-restore). The edge-function registry object in edge-function-contracts.ts has no entry for the deleted feature.</acceptance_criteria>
  <done>The client-side contract surface for the deleted edge function no longer exists, and the audit-coverage allowlist no longer names a directory that 05-02-PLAN.md is about to delete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client RBAC gate (`ProtectedAction` + `rbac.ts`) → any future direct RPC/API caller | `STAFF_ACTIONS`/`MANAGER_EXTRA` is the client-side mirror of the DB-side `role_permissions` grant list; a stale action name in either place is a permission surface even when no UI currently renders a control for it |
| `edge-function-contracts.ts` registry → Supabase Edge Functions (network boundary) | The registry maps action names to request/response Zod schemas and `fetch()` caller functions — each entry is a client-constructible path toward a network-invocable serverless function |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | `src/shared/lib/rbac.ts` (`STAFF_ACTIONS`/`MANAGER_EXTRA`) | medium | mitigate | Remove the action from both the type source and the manager grant set in the same task (D-01), so no client-typeable action name maps to a now-nonexistent server-side grant. |
| T-05-02 | Tampering | `src/shared/lib/edge-function-contracts.ts` (registry entry + schemas + caller) | low | mitigate | Delete the request/response schemas, the caller function, and the registry entry together so no client code path can construct or send a request toward the (separately deleted, 05-02-PLAN.md) edge function. |

No package-manager installs occur in this plan — no `T-05-SC` supply-chain row applies (Package Legitimacy Audit is N/A per RESEARCH.md).
</threat_model>

<verification>
- After Task 1: `npm run typecheck && npm run lint && npm run test` all exit 0 with the feature folder gone.
- After Task 2: `npm run typecheck` exits 0; `npx vitest run src/shared/lib/rbac.test.ts src/shared/ui/ProtectedAction.test.tsx` exits 0.
- After Task 3: `npm run typecheck` exits 0; `npx vitest run src/shared/lib/__tests__/audit-edge-coverage.test.ts src/shared/lib/edge-function-contracts.test.ts` exits 0.
- Full-plan gate: `npm run typecheck && npm run lint && npm run test` exits 0 with all three tasks' changes applied together.
</verification>

<success_criteria>
`src/features/void-order/` is gone, `rbac.ts`'s Action union no longer admits the deleted permission, `edge-function-contracts.ts` no longer exports or registers its schemas/caller, and every dependent test file compiles and passes — the frontend/shared-lib half of SALE-01 is complete and ready for 05-02-PLAN.md's DB/backend half.
</success_criteria>

## Artifacts this plan produces

None — every task in this plan edits or deletes existing files. The phase's one newly-created artifact (the forward-only RBAC-drop migration) is produced by 05-02-PLAN.md, not this plan.

<output>
Create `.planning/phases/05-delete-void-order-feature/05-01-SUMMARY.md` when done
</output>
