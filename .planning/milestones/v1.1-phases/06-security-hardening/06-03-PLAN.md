---
phase: 06-security-hardening
plan: 03
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - src/widgets/SettingsTabsPanel/tabs/HardwareSettingsTab.tsx
  - src/features/upload-logo/model/useUploadLogo.ts
  - src/widgets/LogoImage/index.tsx
  - src/widgets/CajaDashboard/CajaDashboard.tsx
  - src/entities/settings/model/receipt-settings-rls.integration.test.ts
autonomous: true
requirements: [SEC-02]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "HardwareSettingsTab, LogoUploader (via useUploadLogo), LogoImage, and CajaDashboard read/write receipt settings exclusively through the new receipt_settings table — no code path anywhere in src/ reads or writes the old settings table's key='receipt' row anymore."
    - "A cashier-role session can SELECT the single receipt_settings row but any INSERT/UPDATE/DELETE attempt is rejected by Postgres; a manager/admin-role session can perform all four operations (D-05's reinterpretation of ROADMAP Success Criterion #4)."
  artifacts:
    - path: "src/entities/settings/model/receipt-settings-rls.integration.test.ts"
      provides: "Service-role integration test proving role-scoped RLS write isolation (Success Criterion #4)"
  key_links:
    - "HardwareSettingsTab.tsx / LogoUploader (useUploadLogo.ts) / LogoImage / CajaDashboard all call useReceiptSettings()/useMutationUpdateReceiptSettings() from 06-02-PLAN.md's queries.ts additions, which query/upsert supabase.from('receipt_settings') directly via PostgREST — RLS-enforced at the DB layer, not just the /settings route's admin-only RBAC gate."
    - "receipt-settings-rls.integration.test.ts mirrors src/features/reopen-tab/model/reopen-tab-rpc.integration.test.ts's structure (service-role seed client, temp auth users, signInWithPassword) because /settings is RBAC-gated admin-only at the route layer — a Playwright browser test can never reach the write path as a cashier to prove RLS blocks it."
  prohibitions:
    - statement: "Must not test SC4 (role-scoped RLS) via a Playwright browser test through the /settings UI — the route is RBAC-gated admin-only, so a cashier session can never reach the form to attempt a write; the only test that can actually exercise the RLS policy is a service-role Vitest integration test hitting Supabase directly (RESEARCH.md Pitfall 1)."
      disposition: flagged-unverified
---

<objective>
Complete SEC-02: repoint every remaining client consumer of receipt settings from the generic `settings` table onto the new `receipt_settings` table (06-02-PLAN.md's `useReceiptSettings`/`useMutationUpdateReceiptSettings`), and prove role-scoped RLS write isolation with a service-role integration test — this is the direct evidence for ROADMAP Success Criterion #4.

Purpose: RESEARCH.md and 06-PATTERNS.md named `HardwareSettingsTab.tsx` and `useUploadLogo.ts` as the two receipt-settings consumers, but a fuller sweep of this session found two more read-only consumers of `useSettings().data.receipt` that would otherwise silently break at typecheck once 06-02-PLAN.md removed the `receipt` field from `SettingsSnapshot`: `src/widgets/LogoImage/index.tsx` (app-wide logo display) and `src/widgets/CajaDashboard/CajaDashboard.tsx` (caja-close receipt auto-cut setting). All four are repointed together in this plan's first task so no file is left half-migrated.

Output: four consumer files repointed to the new hooks; a new service-role RLS integration test proving cashier=read-only / manager+admin=full-CRUD on the single `receipt_settings` row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-security-hardening/06-CONTEXT.md
@.planning/phases/06-security-hardening/06-RESEARCH.md
@.planning/phases/06-security-hardening/06-PATTERNS.md
@src/widgets/SettingsTabsPanel/tabs/HardwareSettingsTab.tsx
@src/features/upload-logo/model/useUploadLogo.ts
@src/features/reopen-tab/model/reopen-tab-rpc.integration.test.ts
@.planning/phases/06-security-hardening/06-02-PLAN.md
</context>

<interfaces>
From 06-02-PLAN.md (this plan's dependency — must have landed first):
```
useReceiptSettings(): { data: ReceiptSettings | undefined; resultError; isIdleOrLoading; ... }
useMutationUpdateReceiptSettings(): UseMutationResult whose mutate/mutateAsync takes a ReceiptSettings value directly (not {key,value})
```
Both exported from `@entities/settings`.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Repoint HardwareSettingsTab, useUploadLogo, LogoImage, CajaDashboard onto receipt_settings</name>
  <files>src/widgets/SettingsTabsPanel/tabs/HardwareSettingsTab.tsx, src/features/upload-logo/model/useUploadLogo.ts, src/widgets/LogoImage/index.tsx, src/widgets/CajaDashboard/CajaDashboard.tsx</files>
  <precondition>useReceiptSettings/useMutationUpdateReceiptSettings are exported from @entities/settings (06-02-PLAN.md landed) — confirmed by `grep -c "useReceiptSettings" src/entities/settings/index.ts` returning a nonzero count.</precondition>
  <read_first>src/widgets/SettingsTabsPanel/tabs/HardwareSettingsTab.tsx in full (already read this session — `useSettings()`/`useMutationUpdateSetting()` used ONLY for receipt fields in this file, lines 27-56: `settings?.receipt`, `patchReceipt`'s `updateSetting.mutate({key:'receipt', value: next}, ...)`); src/features/upload-logo/model/useUploadLogo.ts in full (already read — `apply()`'s `update.mutateAsync({key:'receipt', value: next})` at line 87); src/widgets/LogoImage/index.tsx in full (25 lines — `const { data } = useSettings(); const logoDataUrl = data?.receipt.logoDataUrl ?? null;`); src/widgets/CajaDashboard/CajaDashboard.tsx around line 66 and line 189 (`const settings = useSettings();` used ONLY for `settings.data?.receipt.autoCut` in this whole file — confirmed by grep this session, no other `settings.` usage exists).</read_first>
  <action>
    1. In HardwareSettingsTab.tsx: replace the `useSettings()`/`useMutationUpdateSetting()` calls with `useReceiptSettings()`/`useMutationUpdateReceiptSettings()`; replace every `settings?.receipt` reference with the new hook's `data` directly; in `patchReceipt`, replace `updateSetting.mutate({key:'receipt', value: next}, {...})` with `updateReceiptSettings.mutate(next, {...})` — same optimistic-update/rollback logic, unchanged.
    2. In useUploadLogo.ts: replace the `useMutationUpdateSetting` import/usage with `useMutationUpdateReceiptSettings`; `apply()`'s `update.mutateAsync({key:'receipt', value: next})` becomes `update.mutateAsync(next)`.
    3. In LogoImage/index.tsx: replace `useSettings()` with `useReceiptSettings()`; `data?.receipt.logoDataUrl ?? null` becomes `data?.logoDataUrl ?? null`.
    4. In CajaDashboard.tsx: replace `const settings = useSettings();` with `const receiptSettings = useReceiptSettings();`; the one usage at `printRawText(lines, { autoCut: settings.data?.receipt.autoCut })` becomes `printRawText(lines, { autoCut: receiptSettings.data?.autoCut })`.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npx playwright test e2e/08-settings-receipt.spec.ts</automated>
  </verify>
  <acceptance_criteria>typecheck and lint both pass with zero errors/warnings across the whole repo (not just these 4 files — this proves no other consumer was missed). e2e/08-settings-receipt.spec.ts's existing 5 tests (paper width save, cashier-name toggle, reload persistence, reset to defaults, auto-cut persistence) all still pass unmodified against the repointed persistence layer, proving identical UI behavior. A repo-wide grep for `key: 'receipt'` and `.receipt.` returns zero matches in src/ (confirms no consumer was left half-migrated).</acceptance_criteria>
  <done>Every client consumer of receipt settings reads/writes exclusively through receipt_settings; the old settings-table receipt path no longer exists anywhere in the client.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Service-role integration test — cashier read-only, manager/admin full CRUD (SC4)</name>
  <files>src/entities/settings/model/receipt-settings-rls.integration.test.ts</files>
  <precondition>VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY are set in the environment (already present in .env.local per prior-phase setup, same env guard reopen-tab-rpc.integration.test.ts uses) and receipt_settings exists with RLS active on the target DB (06-02-PLAN.md's Task 1) — confirmed by the same `describe.skipIf(skip)` pattern and by `docker exec supabase-db psql -U supabase_admin -d postgres -c "select 1 from receipt_settings limit 0"` succeeding.</precondition>
  <read_first>src/features/reopen-tab/model/reopen-tab-rpc.integration.test.ts in full (724 lines — the exact structural pattern to copy: env guards at the top, `createAuthStaff(role)` helper using `db.auth.admin.createUser` + a `profiles` upsert, `signInClient(email,password)` helper creating a fresh anon-key client and calling `signInWithPassword`, `describe.skipIf(skip)`, `beforeAll`/`afterAll` teardown, the `safe()` best-effort-cleanup wrapper); 06-02-PLAN.md's Task 1 (the exact receipt_settings column names for building INSERT/UPDATE payloads in this test).</read_first>
  <action>
    Create `src/entities/settings/model/receipt-settings-rls.integration.test.ts` mirroring reopen-tab-rpc.integration.test.ts's structure (service-role `db` client via `createClient(url, serviceKey)`, `createAuthStaff('manager'|'cashier')`, `signInClient`, `describe.skipIf(skip)` env guard) but scoped to `receipt_settings` instead of tabs/reopen. Test cases:
    1. A cashier-signed-in client `select('*')` from `receipt_settings` succeeds (`error` is null) — proves the `_select_authenticated` policy's `USING (true)` grants read to every authenticated role.
    2. A cashier-signed-in client attempting `.insert({ paper_width_chars: 40 })` on `receipt_settings` is rejected by RLS (`error` is not null).
    3. A cashier-signed-in client attempting `.update({ paper_width_chars: 40 })` on `receipt_settings` is rejected by RLS (`error` is not null); a follow-up service-role read confirms no row was actually changed.
    4. A manager-signed-in client can `.insert(...)`, then `.update(...)`, then `.delete(...)` a row on `receipt_settings` — all three succeed (`error` is null each time).
    Clean up every row this test creates in `afterEach` via the service-role `db` client (mirroring reopen-tab's `safe()` pattern), and tear down the two temp auth users/profiles in `afterAll`.
  </action>
  <verify>
    <automated>npx vitest run src/entities/settings/model/receipt-settings-rls.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>All 4 test cases pass — cashier SELECT succeeds, cashier INSERT/UPDATE both rejected by RLS with no row mutated, manager INSERT/UPDATE/DELETE all succeed. This matches ROADMAP Success Criterion #4 as reinterpreted by D-05 (role-scoped write isolation on the single row, not multi-terminal isolation, since D-04 established there is only ever one row).</acceptance_criteria>
  <done>An automated, service-role-driven integration test proves receipt_settings' RLS policies enforce cashier=read-only / manager+admin=full-CRUD — the SC4 evidence CLAUDE.md's testing policy requires (a Vitest integration test, not a Playwright/manual UAT scenario, since /settings is RBAC-gated admin-only and a cashier session can never reach the write path through the UI).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cashier-role session -> receipt_settings write path | The single defense here is DB-level RLS (06-02-PLAN.md), not client-side RBAC — this plan's Task 2 is the proof that the DB layer actually holds when a client bypasses the admin-only /settings route entirely (e.g. a compromised or modified client binary calling PostgREST directly) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-08 | Elevation of Privilege | A cashier-role client bypassing the admin-only /settings UI route and calling supabase.from('receipt_settings') directly | high | mitigate | Proven closed by Task 2's integration test, which signs in as an actual cashier-role Supabase session (not just a UI-route check) and asserts every write attempt is rejected by Postgres RLS, not merely hidden by the client. |

No `T-06-SC` supply-chain row: this plan installs no packages.
</threat_model>

<verification>
- After Task 1: typecheck/lint pass repo-wide; e2e/08-settings-receipt.spec.ts's existing 5 tests pass unmodified against the repointed persistence layer.
- After Task 2: the 4 RLS role-scoped integration test cases pass, proving SC4.
- Full-plan gate: `npm run typecheck && npm run lint && npm run test` exits 0 with both tasks' changes applied together.
</verification>

<success_criteria>
receipt_settings is the sole persistence layer for receipt configuration across the entire client (no remaining consumer of the generic settings table for this data); a cashier-role session can read but never write the row, while manager/admin can fully manage it, proven by an automated service-role integration test — SEC-02 is complete.
</success_criteria>

## Artifacts this phase produces

- `src/entities/settings/model/receipt-settings-rls.integration.test.ts` — new service-role integration test proving role-scoped RLS (Success Criterion #4)

<output>
Create `.planning/phases/06-security-hardening/06-03-SUMMARY.md` when done
</output>
