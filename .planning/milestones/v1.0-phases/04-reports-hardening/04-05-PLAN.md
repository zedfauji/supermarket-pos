---
phase: 04-reports-hardening
plan: 05
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - src/entities/tab/model/queries-reports.ts
  - src/entities/tab/model/product-sales-report.integration.test.ts
  - src/entities/staff/model/queries.ts
  - src/entities/staff/model/queries.staff-report.test.ts
  - e2e/07-reports.spec.ts
autonomous: true
gap_closure: true
requirements: [REP-02]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Product Sales margin is accurate for every supported sale, including loose-weight (weighted) products — the weight_grams/1000 factor process_direct_sale_atomic applies to unit_price at checkout is applied identically to the historical cost_price_snapshot when the report computes cost/margin (closes 04-VERIFICATION.md Gap 1)"
    - "Staff Performance's revenue/void aggregation queries real order_items/orders columns (unit_price, orders.staff_id, orders.status, orders.tab_id) instead of order_items.created_by/price/tab_id/is_voided, which never existed on that table — root-causing 3 of the 4 e2e/07-reports.spec.ts failures, not just their locators"
    - "The retained Reports E2E suite (e2e/07-reports.spec.ts, excluding only the pre-existing pool-tables-dependent bartender-initiated test already documented as out-of-scope) passes green end-to-end (closes 04-VERIFICATION.md Gap 2)"
  artifacts:
    - "src/entities/tab/model/queries-reports.ts"
    - "src/entities/staff/model/queries.ts"
    - "e2e/07-reports.spec.ts"
  key_links:
    - "useProductSalesReport's cost aggregation now applies the same weight_grams/1000 factor to cost_price_snapshot that process_direct_sale_atomic already applies to unit_price at checkout time, so a weighted line's revenue and cost stay proportionally consistent"
    - "useStaffMetrics's fetchOrderItemsInRange now joins order_items to orders!inner(staff_id, status, tab_id) instead of selecting nonexistent order_items.created_by/price/tab_id/is_voided columns, mirroring the same orders!inner join pattern useProductSalesReport already uses for order-level status"
  prohibitions:
    - statement: "Do not touch close_caja_session, the full-day soak spec (e2e/55-full-day-soak.spec.ts), or the bartender-initiated pool-tables test — REP-01 and the soak are already 04-VERIFICATION.md VERIFIED, and the bartender test is a separate, pre-existing, already-documented pool-tables defect outside this gap closure's scope."
      disposition: flagged
---

<objective>
Close the 2 remaining BLOCKER gaps from 04-VERIFICATION.md's re-verification: (1) Product Sales margin is wrong for weighted/loose products — `useProductSalesReport` multiplies each weighted line's full unit `cost_price_snapshot` by `quantity` without the `weight_grams/1000` factor checkout already applies to that same line's `unit_price`; (2) the retained `e2e/07-reports.spec.ts` suite is not green (22/26) — one ambiguous "Voids & Refunds" empty-state locator and three "Staff Performance" failures.

Investigation this session (live Playwright reruns + a live network trace) found the three Staff Performance failures are not merely bad locators: `useStaffMetrics`'s `fetchOrderItemsInRange` selects `order_items.created_by, price, tab_id, is_voided` — none of which exist on `order_items` (confirmed against `src/shared/lib/supabase.types.ts`) — producing a live `400 Bad Request` on every real run, silently swallowed into `rows = []` by `StaffSalesPanel`, so Staff Performance has been rendering its empty state regardless of real activity. All 4 failures additionally share one ambiguous-locator bug: `getByText(/no (voids or refunds|staff activity)/i)` matches both the EmptyState's `<h3>` heading and its `<p>` description, a Playwright strict-mode violation — this file already has a working precedent for the fix (line 452's `/no voids or refunds in this range/i`, which matches only the longer paragraph text).

Purpose: These are the last 2 blockers between 04-VERIFICATION.md's `gaps_found` (15/18) and REP-02 being re-provable as VERIFIED. No new capability — corrections to already-shipped Phase 4 work (04-02 margin, 04-01/04-04 report suite) plus one pre-existing, previously-masked staff-metrics query bug this suite's failures exposed.

Also resolves, by explicit deferral (not silent drop): the WARNING-severity `src/entities/caja/model/tip-distribution-rpc.integration.test.ts` `p_closed_by: null` issue noted in 04-VERIFICATION.md's Anti-Patterns table. Investigation confirmed this entire file predates this project's Phase 1 strip and exercises `tip_distribution_entries`, a table Phase 1 already dropped (absent from `src/shared/lib/supabase.types.ts`) — it is already fully broken independent of the `p_closed_by` nuance, and fixing it is Phase-1-strip cleanup debt entangled with an unrelated dropped feature, not Phase 4 REP-01/REP-02 scope (dependency-conflict deferral per planner authority limits). Recorded in `deferred-items.md`.

Output: A byte-correct Product Sales margin for weighted lines with a live-DB regression test; a corrected `useStaffMetrics` query (and updated unit-test fixtures) that reflects real staff activity instead of unconditionally rendering empty; a fully green retained Reports E2E suite (26/26, excluding the documented pool-tables-dependent test).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reports-hardening/04-VERIFICATION.md
@.planning/phases/04-reports-hardening/04-04-SUMMARY.md
@.planning/phases/04-reports-hardening/deferred-items.md
@supabase/migrations/20260818000003_process_direct_sale_atomic_cost_snapshot.sql
@src/shared/lib/supabase.types.ts
@src/entities/tab/model/queries-reports.ts
@src/entities/staff/model/queries.ts
@e2e/07-reports.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply the checkout weight factor to Product Sales margin cost</name>
  <files>src/entities/tab/model/queries-reports.ts, src/entities/tab/model/product-sales-report.integration.test.ts</files>
  <read_first>src/entities/tab/model/queries-reports.ts (lines 231-245 — the `useProductSalesReport` `.select()` call, currently selecting `quantity, unit_price, modifier_price_delta, cost_price_snapshot, products(...), orders!inner(...)` with no `weight_grams`; lines 268-302 — the aggregation loop computing `lineRev`/`costPrice`/`existing.costTotal`/the new-entry `costTotal` initializer, all currently ignoring weight); supabase/migrations/20260818000003_process_direct_sale_atomic_cost_snapshot.sql (line 71 — `ROUND(v_catalog_price * (v_weight_grams / 1000.0), 2)`, the exact factor checkout applies to `unit_price` for a weighted line; line 95/117-121 — confirms `cost_price_snapshot` is inserted as the raw, unweighted `v_cost_price` straight from `inventory.cost_price`, so a weighted line's `unit_price` is already weight-adjusted but its `cost_price_snapshot` is not — the report must apply the same factor itself); src/entities/tab/model/product-sales-report.integration.test.ts (lines 184-234 — the AC-5 fixture's existing `IT_ITEM_BEER_A`/`IT_ITEM_BEER_B`/`IT_ITEM_SPIRITS` seed into `IT_ORDER_BEER`/`IT_ORDER_SPIRITS`, and lines 262-304 — the AC-5 test's existing assertions `beerRow.revenue=50, costTotal=8, margin=12`, the baseline this task's new test adds one weighted row on top of)</read_first>
  <behavior>
    - New test in `product-sales-report.integration.test.ts`: seed one additional `order_items` row into the existing `IT_ORDER_BEER` order for `PRODUCT_ID_BEER` with `quantity: 1, unit_price: 5, modifier_price_delta: 0, weight_grams: 500, cost_price_snapshot: 4` (mirrors checkout's own weighted-line shape: a 500g sale of a $10/kg-cost product prices at $5 and should cost 4 * 0.5 = $2, not the full $4).
    - Assert the aggregated beer row now has `revenue = 55` (existing $50 + this line's $5), `costTotal = 10` (existing $8 + this line's weight-adjusted $2, NOT $8 + $4 = $12), and `margin = 15` (existing AC-5 fixture's `marginRevenue` of $20 + this line's $5 = $25, minus `costTotal` $10).
    - Test must self-clean (delete the new `order_items` row in a `finally`/local cleanup) since it is not part of the shared `beforeEach`/`afterEach` fixture array.
  </behavior>
  <action>Add `weight_grams,` to the `.select()` template string in `useProductSalesReport` (`src/entities/tab/model/queries-reports.ts`), immediately after the existing `cost_price_snapshot,` line, so each fetched row carries its weight alongside its cost snapshot.

In the aggregation loop, after the existing `costPrice` derivation, add a `weightFactor` local: `typeof item.weight_grams === 'number' ? item.weight_grams / 1000 : 1` (defaulting to 1 for non-weighted lines, matching checkout's own no-op case). Derive a `lineCost` local as `costPrice === null ? 0 : quantity * costPrice * weightFactor`. Replace both existing raw-cost expressions with this weight-adjusted value: the `existing` branch's `existing.costTotal += quantity * costPrice` becomes `existing.costTotal += lineCost`, and the new-entry branch's `costTotal: costPrice === null ? 0 : quantity * costPrice` becomes `costTotal: lineCost`. `marginRevenue`/`revenue`/`hasCost` are untouched — only the cost side of the calculation was wrong; `unit_price` (and therefore `revenue`) was already correctly weight-adjusted at checkout time, per RESEARCH.md/04-VERIFICATION.md's gap description.

Then write the regression test in `<behavior>` into `product-sales-report.integration.test.ts`, placed after the existing `'AC-5: aggregates order_items by product...'` test in the same `describe` block.</action>
  <verify>
    <automated>npx vitest run src/entities/tab/model/product-sales-report.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>The new weighted-line integration test passes against the live test DB with `revenue=55, costTotal=10, margin=15` for the beer product row. The pre-existing AC-5/null-cost/voided/date-range/empty-array tests in the same file still pass unchanged (they contain no weighted lines, so `weightFactor` is 1 for all of them and their expected values are unaffected).</acceptance_criteria>
  <done>Product Sales margin for a weighted (loose-kg/g) sale reflects the same weight-adjusted cost checkout charged, not the full per-unit cost — proven by a live-DB regression test distinguishing the fixed value (costTotal=10) from the buggy one (costTotal=12).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix useStaffMetrics's stale order_items column query (root cause of 3 e2e failures)</name>
  <files>src/entities/staff/model/queries.ts, src/entities/staff/model/queries.staff-report.test.ts</files>
  <read_first>src/entities/staff/model/queries.ts (lines 598-629 — the `OrderItemRow` type and `fetchOrderItemsInRange`'s `.select('created_by, quantity, price, tab_id, is_voided')` call; lines 663-674 — the aggregation loop reading `item.created_by`/`item.is_voided`/`item.price`/`item.tab_id`); src/shared/lib/supabase.types.ts (lines 504-522 — `order_items` Row type: no `created_by`, `price`, `tab_id`, or `is_voided` columns exist on this table, only `quantity`, `unit_price`, `cost_price_snapshot`, `weight_grams`, etc.; lines 583-597 — `orders` Row type: `staff_id`, `status` (`order_status` enum: `"pending" | "served" | "voided"`), and `tab_id` live here); src/entities/tab/model/queries-reports.ts (lines 231-245 — the established in-repo pattern `useProductSalesReport` already uses: `order_items.select(...).orders!inner(status, tabs!inner(created_at))` — mirror this join shape rather than inventing a new one); src/entities/staff/model/queries.staff-report.test.ts (full file — all 5 tests mock `order_items` rows in the stale flat shape `{ created_by, quantity, price, tab_id, is_voided }` at lines 112-117 and 264-274, which must be updated to the corrected nested shape to keep testing real production behavior instead of the bug's own shape)</read_first>
  <behavior>
    - `queries.staff-report.test.ts` fixtures updated to the corrected row shape: `{ quantity, unit_price, orders: { staff_id, status, tab_id } }`, using `status: 'pending'` for non-voided rows (matches `process_direct_sale_atomic`'s own `orders.status` insert value and the real `order_status` enum) and `status: 'voided'` for the voided-item test case.
    - All 5 existing `useStaffMetrics` test assertions (S10-01.1 revenue-sort, S10-01.2 date-range-passed, S10-01.3 zero-metrics-for-idle-staff, S10-01.4/.4b error paths, S10-01.x voided-item) keep their existing expected values — this task fixes field names/shape only, not the aggregation math or Result/error semantics.
  </behavior>
  <action>Root cause (confirmed live this session via a reproduced `400 Bad Request` from `GET /rest/v1/order_items?select=created_by,quantity,price,tab_id,is_voided`): `order_items` never had `created_by`, `price`, `tab_id`, or `is_voided` columns. `fetchOrderItemsInRange`'s query has always failed against the real DB, and `StaffSalesPanel` silently maps any error `Result` to `rows = []` (its `useMemo(() => (result?.ok ? result.data : []), [result])`), so Staff Performance has rendered its empty state unconditionally regardless of real staff activity — not just an E2E-locator problem.

In `src/entities/staff/model/queries.ts`: change the `OrderItemRow` type from the flat `{ created_by, quantity, price, tab_id, is_voided }` shape to `{ quantity: number; unit_price: number; orders: { staff_id: string | null; status: string; tab_id: string } | null }`. Change `fetchOrderItemsInRange`'s `.select(...)` argument from `'created_by, quantity, price, tab_id, is_voided'` to `'quantity, unit_price, orders!inner(staff_id, status, tab_id)'` — leave the existing `.gte('created_at', ...).lte('created_at', ...)` date filter unchanged (it already correctly targets `order_items.created_at`, which does exist on that table). In the `useStaffMetrics` aggregation loop, replace `item.created_by` with `item.orders?.staff_id ?? null`, replace `item.is_voided` with `item.orders?.status === 'voided'`, replace `item.price` with `item.unit_price`, and replace `item.tab_id` with `item.orders?.tab_id ?? ''` — same control flow (skip rows with no matching staff entry; accumulate `voidCount` on voided, `revenue`/`tabIds` otherwise), only the field-access paths change.

Update `queries.staff-report.test.ts`'s mocked `order_items` fixtures in the 5 existing tests to the corrected nested shape from `<behavior>`.</action>
  <verify>
    <automated>npx vitest run src/entities/staff/model/queries.staff-report.test.ts</automated>
  </verify>
  <acceptance_criteria>All 5 existing `useStaffMetrics` unit tests pass with the corrected mock shape and identical expected outputs (revenue sums, sort order, void counts, error propagation). `OrderItemRow`/`fetchOrderItemsInRange` are grep-confirmed private to this file (no other caller needs updating).</acceptance_criteria>
  <done>Staff Performance's revenue/void aggregation reads real `order_items`/`orders` columns — the live `400` this session reproduced (`order_items?select=created_by,quantity,price,tab_id,is_voided`) no longer occurs, and the report reflects real staff activity instead of unconditionally falling back to its empty state.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Disambiguate the 4 empty-state locators and prove the retained suite is green</name>
  <files>e2e/07-reports.spec.ts</files>
  <read_first>e2e/07-reports.spec.ts (line 452 — the already-passing precedent in this same file, `getByText(/no voids or refunds in this range/i)`, which disambiguates by matching only the EmptyState's longer `<p>` description text, not its shorter `<h3>` heading; lines 582, 642, 665, 713 — the four ambiguous `getByText(/no voids or refunds/i)` / `getByText(/no staff activity/i)` call sites that Playwright strict-mode-fails because they also match the sibling heading — confirmed live this session: `<h3>No voids or refunds</h3>` + `<p>No voids or refunds in this range.</p>` both match `/no voids or refunds/i`, and `<h3>No staff activity</h3>` + `<p>No staff activity in this date range.</p>` both match `/no staff activity/i`); src/shared/ui/EmptyState.tsx (confirms the shared `<h3>` title + `<p>` description structure underlying every report panel's empty state, so this is one bug pattern repeated 4 times, not 4 distinct bugs)</read_first>
  <action>In `e2e/07-reports.spec.ts`, replace the regex in each of the 4 ambiguous locators with the longer, paragraph-only phrasing already proven at line 452 — no app code or locator strategy changes, purely widening the match text so it excludes the sibling heading:
- Line 582 (`'Voids & Refunds: table renders columns...'` test): `getByText(/no voids or refunds/i)` becomes `getByText(/no voids or refunds in this range/i)`.
- Line 642 (`'Sprint 10: Staff Performance tab shows column headers or empty state'` test): `getByText(/no staff activity/i)` becomes `getByText(/no staff activity in this date range/i)`.
- Line 665 (`'Sprint 10: Staff Performance tab shows empty state for year 2020 date range'` test): same replacement as line 642.
- Line 713 (`'Sprint 10: Export button appears in Staff Performance tab when data rows exist'` test): same replacement as line 642.

After all 3 tasks land, run the full retained-suite command to confirm the phase's declared regression proof is actually green (not just the 4 previously-failing tests in isolation) — Task 1 and Task 2's production fixes may change which branch (`hasRows` vs. empty-state) some of these tests take on a live dataset, and both branches must now resolve correctly either way.</action>
  <verify>
    <automated>npx playwright test e2e/07-reports.spec.ts --grep-invert "bartender-initiated" --retries=0</automated>
  </verify>
  <acceptance_criteria>`npx playwright test e2e/07-reports.spec.ts --grep-invert "bartender-initiated" --retries=0` reports 26/26 passing, 0 failures (up from the 22/26 recorded in 04-04-SUMMARY.md and reproduced again this session before this task's fix).</acceptance_criteria>
  <done>The retained Reports E2E suite is fully green, excluding only the pre-existing, unrelated pool-tables-dependent `bartender-initiated` test already documented in deferred-items.md — closing 04-VERIFICATION.md Gap 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manager/admin session → Product Sales / Staff Performance reports | Both reports are read-only, role-gated (`view_reports`) financial data surfaces — this plan changes only their query/aggregation correctness, not their access control |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-09 | Tampering (data integrity) | `useProductSalesReport`'s margin cost aggregation | low | mitigate | Task 1 applies the same `weight_grams/1000` factor to historical cost that checkout already applies to price, so weighted-product margin no longer overstates cost (understating profitability) to the store owner; a live-DB regression test proves the corrected value |
| T-04-10 | Tampering (data integrity) | `useStaffMetrics`'s `fetchOrderItemsInRange` | low | mitigate | The query selected 4 columns that never existed on `order_items`, causing a silent `400` swallowed into an always-empty report; Task 2 corrects the join to real `order_items`/`orders` columns so Staff Performance reflects actual activity instead of silently masking a broken query as "no data" |

Both threats are internal reporting-correctness issues visible only to already-authorized manager/admin roles, not a new external attack surface — severity is `low` because no trust boundary is crossed, only report accuracy is at stake.
</threat_model>

<verification>
- `npx vitest run src/entities/tab/model/product-sales-report.integration.test.ts` passes, including the new weighted-line regression test.
- `npx vitest run src/entities/staff/model/queries.staff-report.test.ts` passes with corrected fixtures.
- `npx playwright test e2e/07-reports.spec.ts --grep-invert "bartender-initiated" --retries=0` passes 26/26.
- `npm run typecheck` passes (both production files touched are already under file-level `any`/unsafe-access lint disables, so no new type errors are expected).
</verification>

<success_criteria>
Product Sales margin is byte-correct for weighted/loose-weight products, Staff Performance reflects real activity instead of a silently-broken query's forced empty state, and the retained Reports E2E suite is fully green (26/26) — closing both remaining 04-VERIFICATION.md blockers so REP-02 is re-provable as VERIFIED and the phase goal is achieved.
</success_criteria>

## Artifacts this plan produces

- `src/entities/tab/model/queries-reports.ts` (fixed — weight-adjusted margin cost) + `src/entities/tab/model/product-sales-report.integration.test.ts` (new weighted-line regression test)
- `src/entities/staff/model/queries.ts` (fixed — real `order_items`/`orders` column query) + `src/entities/staff/model/queries.staff-report.test.ts` (fixtures updated to match)
- `e2e/07-reports.spec.ts` (4 ambiguous empty-state locators disambiguated)
- `.planning/phases/04-reports-hardening/deferred-items.md` (records the explicit, reasoned deferral of `tip-distribution-rpc.integration.test.ts`'s `p_closed_by: null` WARNING — that file predates Phase 1's strip and already fails independent of this issue, since it exercises `tip_distribution_entries`, a table Phase 1 already dropped)

<output>
Create `.planning/phases/04-reports-hardening/04-05-SUMMARY.md` when done
</output>
