---
phase: 04-reports-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/reports/index.tsx
  - src/widgets/TipDistributionPanel/TipDistributionPanel.tsx
  - src/widgets/TipDistributionPanel/TipDistributionPanel.test.tsx
  - src/widgets/TipDistributionPanel/index.ts
  - src/widgets/ModifierPopularityReport/ModifierPopularityReport.tsx
  - src/widgets/ModifierPopularityReport/index.ts
  - src/entities/staff/model/queries.ts
  - src/entities/staff/model/queries.staff-report.test.ts
  - src/entities/staff/index.ts
  - src/entities/staff/model/index.ts
  - src/entities/tab/model/queries-reports.ts
  - src/entities/tab/model/modifier-popularity-report.integration.test.ts
  - src/shared/lib/domain.ts
  - src/features/export-report/model/useExportReport.ts
  - src/features/export-report/ui/ExportButtons.tsx
  - src/features/export-report/ui/ExportButtons.test.tsx
  - src/shared/lib/exporters/excel.ts
  - src/shared/lib/exporters/pdf.tsx
  - src/shared/lib/i18n/locales/es-MX/pages.json
  - src/shared/lib/i18n/locales/en-US/pages.json
  - src/shared/lib/i18n/locales/es-MX/wAdmin.json
  - src/shared/lib/i18n/locales/en-US/wAdmin.json
  - src/shared/lib/i18n/locales/es-MX/receipt.json
  - src/shared/lib/i18n/locales/en-US/receipt.json
  - supabase/migrations/20260818000001_drop_modifier_popularity_report_rpc.sql
autonomous: true
requirements: [REP-02]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Reports page shows no 'Tips' or 'Modifier Popularity' TabsTrigger/TabsContent; the 'Menu & Promos' group div is removed entirely (its only child was Modifier Popularity) and the 'Staff & Tips' group renders as 'Staff' only (D-01, D-02, D-04)"
    - "get_modifier_popularity_report is dropped from the database (DROP FUNCTION), not just unwired from the UI — no EXECUTE grant to authenticated remains (D-04)"
    - "All 10 kept report tabs (session, products, hourly, categories, payment-methods, staff, voids, deletions-pre, deletions-post, refunds-reg) still render, export, and pass their existing test suites unchanged after the deletion (resolves REP-02 specless-probe item — tab-removal correctness)"
    - "Reports page tab-group label text after the 'Menu & Promos' removal and 'Staff & Tips'→'Staff' rename introduces no new wrap/overflow risk — both changes shorten label text, never lengthen it (UI-SPEC 'long-text' consideration)"
  artifacts:
    - "supabase/migrations/20260818000001_drop_modifier_popularity_report_rpc.sql"
  key_links:
    - "src/pages/reports/index.tsx no longer imports TipDistributionPanel or ModifierPopularityReport"
    - "useExportReport.ts's ExportType union and switch statement have no tips-*/modifier-popularity-csv members — TypeScript's `never` exhaustiveness check (line ~489) enforces this at compile time"
    - "src/entities/staff/model/queries.ts's fetchActiveProfiles remains exported/callable by useStaffMetrics after useStaffTips is deleted"
  prohibitions:
    - statement: "Report-tab deletion must never remove a shared query/function still relied on by a KEPT report (e.g. deleting fetchActiveProfiles would silently break Staff Sales/useStaffMetrics) — verify via full npm run typecheck plus the existing Staff Sales/Category Revenue e2e coverage, not just the deleted tabs' own tests."
      disposition: flagged
---

<objective>
Delete the "Tips" and "Modifier Popularity" report tabs and every layer they touch (widget, query hook, export-type plumbing, i18n strings, and the `get_modifier_popularity_report` RPC), per CONTEXT.md D-01/D-02/D-04. Neither tab applies to a supermarket checkout — no tip culture, no restaurant-style modifiers. "Remove" means delete the code entirely, not hide it from nav (D-04).

Purpose: REP-02 requires bar/pool-specific report tabs removed from the Reports page. This is the phase's only pure-deletion track — it has no new capability of its own, so the executor must prove each deletion is *complete* (no orphaned import, no dangling DB grant, no broken shared helper) rather than just visually absent.

Output: Reports page with 10 tabs (no Tips, no Modifier Popularity, no "Menu & Promos" group), `get_modifier_popularity_report` dropped from the database, and every file in `files_modified` free of dead references to either report.

Per D-03, "Staff Sales" (`StaffSalesPanel`) and "Category Revenue" (`CategoryRevenuePanel`) are explicitly OUT of scope for this plan — neither tab, its hook, nor its export path is touched by either task below, even though D-03 notes they aren't explicitly named in REP-02's report list. Task 2's `<verify>` running `e2e/22-staff-management.spec.ts` is the regression proof that Staff Sales survives untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reports-hardening/04-CONTEXT.md
@.planning/phases/04-reports-hardening/04-RESEARCH.md
@.planning/phases/04-reports-hardening/04-UI-SPEC.md
@supabase/migrations/20260721000003_modifier_popularity_rpc.sql
@supabase/migrations/20260810000010_drop_tip_distribution.sql
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Delete Modifier Popularity end-to-end (widget → hook → schema → export → RPC drop)</name>
  <files>src/pages/reports/index.tsx, src/widgets/ModifierPopularityReport/ModifierPopularityReport.tsx, src/widgets/ModifierPopularityReport/index.ts, src/entities/tab/model/queries-reports.ts, src/entities/tab/model/modifier-popularity-report.integration.test.ts, src/shared/lib/domain.ts, src/features/export-report/model/useExportReport.ts, src/features/export-report/ui/ExportButtons.tsx, src/features/export-report/ui/ExportButtons.test.tsx, src/shared/lib/i18n/locales/es-MX/pages.json, src/shared/lib/i18n/locales/en-US/pages.json, src/shared/lib/i18n/locales/es-MX/wAdmin.json, src/shared/lib/i18n/locales/en-US/wAdmin.json, supabase/migrations/20260818000001_drop_modifier_popularity_report_rpc.sql</files>
  <read_first>src/pages/reports/index.tsx; src/widgets/ModifierPopularityReport/ModifierPopularityReport.tsx; src/entities/tab/model/queries-reports.ts (lines 1-48, 569-578); src/features/export-report/model/useExportReport.ts (ExportType union lines 45-76, ModifierPopularityContext type lines 118-121, MODIFIER_POPULARITY_CSV_COLUMNS lines 246-250, switch case 'modifier-popularity-csv' lines 479-483, exhaustiveness check lines 489-492); src/features/export-report/ui/ExportButtons.tsx (ModifierPopularityProps lines 85-88, handleExport branch line 163, isCsvOnly line 188); src/shared/lib/domain.ts (ModifierPopularityRowSchema lines 1051-1058); supabase/migrations/20260721000003_modifier_popularity_rpc.sql (exact signature: `get_modifier_popularity_report(p_from timestamptz, p_to timestamptz)`, granted to `authenticated`); supabase/migrations/20260810000010_drop_tip_distribution.sql (DROP-migration house style — header comment explaining what/why, `NOTIFY pgrst, 'reload schema'` after the DDL)</read_first>
  <action>This is the thinnest full-stack deletion path (fewest layers of the two removed tabs — Modifier Popularity has no dedicated Excel/PDF exporter, CSV-only), proving the removal pattern once before Task 2 applies it to the wider Tips surface.

Delete `src/widgets/ModifierPopularityReport/` (both files). In `src/pages/reports/index.tsx` remove the `ModifierPopularityReport` import, the entire `reports.groups.menu` wrapper `<div>` (its `TabsList` group — the only child is the `modifier-popularity` `TabsTrigger`), and the `TabsContent value="modifier-popularity"` block, per UI-SPEC's "Tab Removal — Structural Contract."

In `src/entities/tab/model/queries-reports.ts` delete `useModifierPopularityReport` (the function using `useReportRpc('modifier-popularity', 'get_modifier_popularity_report', ...)`), remove `ModifierPopularityRow`/`ModifierPopularityRowSchema` from both the top `import type`/`import` blocks and the `export type { ... }` re-export block. Delete `src/entities/tab/model/modifier-popularity-report.integration.test.ts` entirely.

In `src/shared/lib/domain.ts` delete `ModifierPopularityRowSchema` and its `ModifierPopularityRow` type export (verify no other consumer first via grep — none exist besides the files in this task's `<files>` list).

In `src/features/export-report/model/useExportReport.ts` delete the `'modifier-popularity-csv'` member from the `ExportType` union, the `ModifierPopularityContext` type, the `MODIFIER_POPULARITY_CSV_COLUMNS` const, the `'modifier-popularity-csv':` switch case, and the now-unused `ModifierPopularityRow` import. In `src/features/export-report/ui/ExportButtons.tsx` delete `ModifierPopularityProps`, its member in the `Props` union, the `else if (props.reportType === 'modifier-popularity')` branch in `handleExport`, and its entry in the `isCsvOnly` check. In `src/features/export-report/ui/ExportButtons.test.tsx` delete the `MODIFIER_POPULARITY_ROWS` const, its `ModifierPopularityRow` import, and the `modifier-popularity` entry in the `CASES` array.

Delete the i18n keys: `pages.json` → `reports.tabs.modifierPopularity` and the whole `reports.groups.menu` key (both es-MX and en-US); `wAdmin.json` → the whole `modifierPopularityReport` block (both locales).

Create migration `supabase/migrations/20260818000001_drop_modifier_popularity_report_rpc.sql`: `DROP FUNCTION IF EXISTS get_modifier_popularity_report(timestamptz, timestamptz);` wrapped in `BEGIN;`/`COMMIT;`, followed by `NOTIFY pgrst, 'reload schema';`, with a header comment stating this closes D-02/D-04 and a DOWN comment pointing back to `20260721000003_modifier_popularity_rpc.sql` for manual re-creation (not recommended — no re-derivation of historical modifier-attach data once dropped).

[BLOCKING] Apply the migration to the local self-hosted Supabase stack before verifying: try `supabase db push --db-url "postgresql://postgres.<POOLER_TENANT_ID>:<POSTGRES_PASSWORD>@localhost:5432/postgres?sslmode=disable" --debug` first (credentials via `docker exec supabase-db env` / `docker exec supabase-pooler env`); if the pooler is unreachable, fall back to `docker exec -i supabase-db psql -U supabase_admin -d postgres < supabase/migrations/20260818000001_drop_modifier_popularity_report_rpc.sql` then register it with `docker exec supabase-db psql -U supabase_admin -d postgres -c "INSERT INTO supabase_migrations.schema_migrations (version, name) VALUES ('20260818000001', 'drop_modifier_popularity_report_rpc') ON CONFLICT (version) DO NOTHING"`. Confirm via `docker exec supabase-db psql -U supabase_admin -d postgres -c "select proname from pg_proc where proname = 'get_modifier_popularity_report'"` that zero rows return.</action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/features/export-report/ui/ExportButtons.test.tsx src/entities/staff/model/queries.staff-report.test.ts</automated>
  </verify>
  <acceptance_criteria>`npm run typecheck` passes with zero errors (the `ExportType` union's `never` exhaustiveness check is the compile-time proof the deletion is complete across `useExportReport.ts`/`ExportButtons.tsx`). `git grep -c "ModifierPopularityReport\|ModifierPopularityRow\|modifier-popularity-csv\|get_modifier_popularity_report" -- src supabase/migrations/2026081*` returns zero matches outside the new drop-migration file itself. The live database has no `get_modifier_popularity_report` function.</acceptance_criteria>
  <done>Modifier Popularity is unreachable from the UI, its RPC is dropped from the database with no lingering grant, and the codebase has zero remaining references to it.</done>
  <reversibility rating="costly">D-02: dropping `get_modifier_popularity_report` and its historical modifier-attach aggregation means re-adding this report later requires a new migration and, since the deletion doesn't touch source data (`order_items.modifier_ids`, `modifiers`), the report itself can be re-derived, but the RPC/UI must be rebuilt from scratch.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete Tips end-to-end (widget → hook → schema → excel/pdf/csv export → i18n)</name>
  <files>src/pages/reports/index.tsx, src/widgets/TipDistributionPanel/TipDistributionPanel.tsx, src/widgets/TipDistributionPanel/TipDistributionPanel.test.tsx, src/widgets/TipDistributionPanel/index.ts, src/entities/staff/model/queries.ts, src/entities/staff/model/queries.staff-report.test.ts, src/entities/staff/index.ts, src/entities/staff/model/index.ts, src/shared/lib/domain.ts, src/features/export-report/model/useExportReport.ts, src/features/export-report/ui/ExportButtons.tsx, src/features/export-report/ui/ExportButtons.test.tsx, src/shared/lib/exporters/excel.ts, src/shared/lib/exporters/pdf.tsx, src/shared/lib/i18n/locales/es-MX/pages.json, src/shared/lib/i18n/locales/en-US/pages.json, src/shared/lib/i18n/locales/es-MX/wAdmin.json, src/shared/lib/i18n/locales/en-US/wAdmin.json, src/shared/lib/i18n/locales/es-MX/receipt.json, src/shared/lib/i18n/locales/en-US/receipt.json</files>
  <read_first>src/entities/staff/model/queries.ts (lines 28-35 staffKeys, 609-650 fetchActiveProfiles + useStaffMetrics — KEEP both verbatim, 715-753 useStaffTips — DELETE); src/entities/staff/index.ts; src/entities/staff/model/index.ts; src/widgets/TipDistributionPanel/TipDistributionPanel.tsx; src/shared/lib/domain.ts (lines 973-980 StaffTipsSchema/StaffTips, line 1228 registry entry, line 1323 types entry); src/shared/lib/exporters/excel.ts (lines 202-223 staffTipsToWorkbook); src/shared/lib/exporters/pdf.tsx (lines 389-425 StaffTipsDoc + staffTipsToPdfBytes); src/shared/lib/i18n/locales/en-US/receipt.json (lines 104-108 pdf.staffTips — this file is NOT in 04-RESEARCH.md's file list, found this session via grep of pdfT's `receipt` namespace usage; delete it in both locales alongside the rest of this deletion)</read_first>
  <action>Expansion of Task 1's proven pattern to the wider Tips surface (this one touches Excel/PDF exporters, which Modifier Popularity did not).

Delete `src/widgets/TipDistributionPanel/` (all 3 files). In `src/pages/reports/index.tsx` remove the `TipDistributionPanel` import, the `tips` `TabsTrigger` inside the `reports.groups.staffTips` group (keep the group `<div>` and the `staff` `TabsTrigger`/`TabsContent`), and the `TabsContent value="tips"` block, per UI-SPEC's Structural Contract.

In `src/entities/staff/model/queries.ts` delete `useStaffTips` and the `staffKeys.staffTips` key factory entry; do NOT touch `fetchActiveProfiles` (also called by the kept `useStaffMetrics`, verified both call sites this session) or `fetchPaymentsWithTipsInRange` unless grep confirms `useStaffTips` was its only caller. Remove the `useStaffTips` export from `src/entities/staff/index.ts` and `src/entities/staff/model/index.ts`. In `src/entities/staff/model/queries.staff-report.test.ts` delete only the `describe('useStaffTips', ...)` block; keep every `useStaffMetrics` test.

In `src/shared/lib/domain.ts` delete `StaffTipsSchema`, the `StaffTips` type export, and both registry entries (`StaffTips: StaffTipsSchema` in the schemas map, `StaffTips: StaffTips` in the types map).

In `src/features/export-report/model/useExportReport.ts` delete the `'tips-excel' | 'tips-pdf' | 'tips-csv'` members from `ExportType`, the `TipsContext` type, `TIPS_CSV_COLUMNS`, the three `'tips-*'` switch cases, the `staffTipsToWorkbook`/`staffTipsToPdfBytes` imports, and the now-unused `StaffTips` type import. In `src/features/export-report/ui/ExportButtons.tsx` delete `TipsProps`, its `Props` union member, and the `else if (props.reportType === 'tips')` branch. In `ExportButtons.test.tsx` delete `TIPS_ROWS`, its `StaffTips` import, and the `tips` entry in `CASES`.

In `src/shared/lib/exporters/excel.ts` delete `staffTipsToWorkbook` and its now-unused `StaffTips` import. In `src/shared/lib/exporters/pdf.tsx` delete `StaffTipsDoc` and `staffTipsToPdfBytes` and its now-unused `StaffTips` import.

Delete i18n keys: `pages.json` → `reports.tabs.tips` (both locales); change `reports.groups.staffTips` value from "Staff & Tips" to "Staff" (en-US) and its es-MX parallel from "Personal y propinas" to "Personal". `wAdmin.json` → the whole `tipDistributionPanel` block (both locales). `receipt.json` → the whole `pdf.staffTips` block (both locales, lines ~104-108).</action>
  <verify>
    <automated>npm run typecheck && npm run lint && npx vitest run src/entities/staff/model/queries.staff-report.test.ts src/features/export-report/ui/ExportButtons.test.tsx && npx playwright test e2e/02-caja.spec.ts e2e/22-staff-management.spec.ts e2e/07-reports.spec.ts</automated>
  </verify>
  <acceptance_criteria>`npm run typecheck`/`npm run lint` pass with zero errors. `git grep -rc "TipDistributionPanel\|StaffTips\|staffTipsToWorkbook\|staffTipsToPdfBytes\|useStaffTips" -- src` returns zero matches. `useStaffMetrics`'s existing tests still pass (proving `fetchActiveProfiles` survived intact). The kept e2e specs covering Reports/Staff/Caja pass unchanged.</acceptance_criteria>
  <done>Tips is unreachable from the UI and every code layer, `fetchActiveProfiles`/`useStaffMetrics` are untouched, and the Reports page has 10 tabs across Sales/Staff/Operations groups with no "Menu & Promos" group.</done>
  <reversibility rating="costly">D-01: deleting the Tips widget/hook/exporters entirely (not just hiding the tab) means re-adding this report later requires rebuilding the whole layer stack from scratch, per CONTEXT.md's own reversibility note on D-01.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (Reports page) → Supabase RPC | `get_modifier_popularity_report` was callable by any authenticated staff with `view_reports`; this task removes that surface entirely |
| Codebase → shared export/report plumbing | `useExportReport`/`ExportButtons` is a generic feature every report tab routes through — deletion must be surgical within it, not wholesale |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | `get_modifier_popularity_report` RPC (dangling attack surface after UI removal) | medium | mitigate | `DROP FUNCTION` in the same migration wave as the UI deletion (Task 1) — no client can invoke a report RPC whose UI no longer exists; do not merely stop calling it from the client |
| T-04-02 | Tampering | Incomplete deletion leaving orphaned `ExportType` members | low | accept | TypeScript's `never` exhaustiveness check in `useExportReport.ts`'s switch statement fails the build if any `ExportType` member deletion is incomplete — compile-time gate via `npm run typecheck`, not a runtime risk |
</threat_model>

<verification>
- `npm run typecheck` and `npm run lint` pass with zero errors after both tasks.
- `git grep` for every deleted symbol (`TipDistributionPanel`, `ModifierPopularityReport`, `StaffTips`, `ModifierPopularityRow`, `get_modifier_popularity_report`) returns zero matches outside this plan's own migration/summary files.
- `get_modifier_popularity_report` is absent from the live database (`pg_proc` check).
- Kept reports (`e2e/02-caja.spec.ts`, `e2e/07-reports.spec.ts`, `e2e/22-staff-management.spec.ts`, `e2e/19-product-sales-report.spec.ts`, `e2e/37-analytics-reports.spec.ts`) pass unchanged.
</verification>

<success_criteria>
Reports page shows exactly the 10 kept tabs across Sales/Staff/Operations groups. Neither Tips nor Modifier Popularity is reachable via UI, hook, schema, export path, or database RPC. Every kept report continues to function exactly as before.
</success_criteria>

## Artifacts this phase produces

- `supabase/migrations/20260818000001_drop_modifier_popularity_report_rpc.sql` (new migration, DROP FUNCTION)

<output>
Create `.planning/phases/04-reports-hardening/04-01-SUMMARY.md` when done
</output>
