---
phase: 04-reports-hardening
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - supabase/migrations/20260818000005_close_caja_session_authoritative_closed_by.sql
  - e2e/55-full-day-soak.spec.ts
  - e2e/07-reports.spec.ts
autonomous: true
gap_closure: true
requirements: [REP-01, REP-02]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A caja close is attributed to the authenticated manager/admin who performed it — close_caja_session rejects any p_closed_by that does not equal auth.uid() with a PERMISSION_DENIED error and never writes an unverified caller-supplied identity into caja_sessions.closed_by (closes 04-VERIFICATION.md Gap 1 / 04-REVIEW.md CR-01)"
    - "All retained report regression suites pass after the obsolete report tabs are deleted — e2e/07-reports.spec.ts no longer asserts the removed Tip Distribution tab or Modifier Popularity tab (closes 04-VERIFICATION.md Gap 2)"
    - "The full-day soak proves its UI receiving shipment committed before using the near-expiry result as evidence — e2e/55-full-day-soak.spec.ts awaits the receive-shipment edge function's own success response and asserts the generated inventory row's quantity and expiry date before checking the near-expiry badge (closes 04-VERIFICATION.md Gap 3 / 04-REVIEW.md WR-02)"
    - "The soak spec no longer relies on the flaky full-page-reload navigation pattern after mutating product state — it uses the retrying gotoAuthed helper instead of page.reload() (closes 04-REVIEW.md WR-01)"
  artifacts:
    - "supabase/migrations/20260818000005_close_caja_session_authoritative_closed_by.sql"
    - "e2e/55-full-day-soak.spec.ts"
    - "e2e/07-reports.spec.ts"
  key_links:
    - "close_caja_session's UPDATE now writes closed_by = auth.uid() directly, never the client-supplied p_closed_by parameter, closing the identity-forgery gap CR-01 identified"
    - "e2e/55-full-day-soak.spec.ts's receiving step now awaits the receive-shipment edge function's own success response and re-queries the resulting inventory row before the near-expiry badge assertion runs, so the badge can no longer pass on a pre-existing unrelated row"
  prohibitions:
    - statement: "The fix must never remove, rename, or repurpose the close_caja_session p_closed_by parameter — only add server-side validation against auth.uid() and rebind the UPDATE's closed_by write. Changing the signature would break the existing frontend caller (CajaDashboard.tsx) and the edge-function contract for no benefit."
      disposition: flagged
---

<objective>
Close the 3 blocking gaps 04-VERIFICATION.md found: (1) `close_caja_session` trusts a caller-supplied `p_closed_by` instead of binding the audit record to the authenticated identity (CR-01, security/audit-integrity); (2) `e2e/07-reports.spec.ts` still asserts the removed Tip Distribution and Modifier Popularity tabs, so the retained reports regression suite fails; (3) `e2e/55-full-day-soak.spec.ts`'s receiving step doesn't wait for or verify the receipt actually committed before treating the near-expiry badge as proof, so the soak can pass without the receiving action having happened. Also folds in 04-REVIEW.md's WR-01 (flaky `page.reload()` in the same soak spec) while that file is already open for the WR-02 fix.

Purpose: These are the 3 remaining blockers between 04-VERIFICATION.md's `gaps_found` (9/12) and REP-01/REP-02 being re-provable as VERIFIED. No new capability is being built — every fix is a correction to already-shipped Phase 4 work (04-01/04-02/04-03).

Output: A new forward migration binding `close_caja_session`'s audit attribution to the authenticated caller; a trimmed `e2e/07-reports.spec.ts` with no obsolete tab assertions; a hardened `e2e/55-full-day-soak.spec.ts` that proves its own receiving step committed and no longer uses a flaky reload.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reports-hardening/04-CONTEXT.md
@.planning/phases/04-reports-hardening/04-VERIFICATION.md
@.planning/phases/04-reports-hardening/04-REVIEW.md
@.planning/phases/04-reports-hardening/04-01-SUMMARY.md
@.planning/phases/04-reports-hardening/04-03-SUMMARY.md
@supabase/migrations/20260818000004_close_caja_reconciliation_summary.sql
@e2e/55-full-day-soak.spec.ts
@e2e/07-reports.spec.ts
@e2e/helpers/auth.ts
@e2e/helpers/supabase.ts
@src/shared/lib/edge-function-contracts.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bind close_caja_session's audit attribution to the authenticated caller</name>
  <files>supabase/migrations/20260818000005_close_caja_session_authoritative_closed_by.sql, e2e/55-full-day-soak.spec.ts</files>
  <read_first>supabase/migrations/20260818000004_close_caja_reconciliation_summary.sql (full file — the current live body being extended; lines 24-30 the `v_caller_role NOT IN ('manager','admin')` role check, lines 54-62 the `UPDATE caja_sessions ... closed_by = p_closed_by` statement being fixed); .planning/phases/04-reports-hardening/04-REVIEW.md (lines 67-83 — CR-01 finding and its exact fix snippet); src/entities/caja/model/queries.ts (lines 246-266 — confirms `closedBy` is always `p_closed_by = closedBy` and `result.error.code` flows through as a raw string with no fixed union, so a new `PERMISSION_DENIED` response needs no frontend change); src/widgets/CajaDashboard/CajaDashboard.tsx (line 141 `closedBy: currentStaff.id` — confirms the frontend already always passes the logged-in user's own id, so this fix changes no legitimate-caller behavior); e2e/55-full-day-soak.spec.ts (lines 344-390 — the existing "closing an already-closed caja session is a no-op" test, the exact `page.evaluate`/localStorage-token/`fetch` pattern to mirror for the new regression test); e2e/helpers/supabase.ts (`openCaja`, lines 192+ — confirms the `caja_sessions_one_open` unique constraint and that `openCaja()` self-closes any leftover open session, so a new test's open session needs no manual cleanup)</read_first>
  <behavior>
    - Test (Playwright, e2e/55-full-day-soak.spec.ts, new test "closing a caja session with a mismatched p_closed_by is rejected" inside the existing `test.describe.serial('Full-day soak', ...)` block): open a fresh caja via `openCaja(100)`; log in as `manager`; via the service-role admin client fetch the logged-in manager's own profile id AND a different profile's id (a different role, e.g. `admin`); using the same `page.evaluate` + localStorage-auth-token-extraction + `fetch('${url}/rest/v1/rpc/close_caja_session', ...)` pattern as the existing "closing an already-closed caja session is a no-op" test, POST `{ p_caja_id: <the fresh open caja>, p_closed_by: <the OTHER profile's id, NOT the logged-in manager's own>, p_closing_cash: 0 }`; assert the JSON response matches `{ ok: false, error: { code: 'PERMISSION_DENIED' } }`; then query `caja_sessions` for that id via the admin client and assert `status` is still `'open'` and `closed_by` is still `null` — proving the UPDATE never ran for a mismatched identity.
  </behavior>
  <action>Write the test in `<behavior>` FIRST, appended after the existing "closing an already-closed caja session is a no-op" test inside the same `test.describe.serial('Full-day soak', ...)` block (it will FAIL against the currently-deployed function — the mismatched call currently returns `{ ok: true }` and closes the caja under the wrong identity), then fix the RPC to make it pass.

Create `supabase/migrations/20260818000005_close_caja_session_authoritative_closed_by.sql`: `CREATE OR REPLACE FUNCTION close_caja_session(p_caja_id UUID, p_closed_by UUID, p_closing_cash NUMERIC(12,2), p_notes TEXT DEFAULT NULL)` — copy the full live body verbatim from `20260818000004_close_caja_reconciliation_summary.sql` (same signature, same DECLARE list, same cash-reconciliation `SELECT` and `RETURN` payload shape), then apply exactly two edits per 04-REVIEW.md CR-01: (1) immediately after the existing role-check block's `END IF;` (the `v_caller_role NOT IN ('manager', 'admin')` check) and before the `SELECT COUNT(*) INTO v_open_tab_count` open-tabs check, insert a new guard: `IF p_closed_by IS DISTINCT FROM auth.uid() THEN RETURN json_build_object('ok', false, 'error', json_build_object('code', 'PERMISSION_DENIED', 'message', 'Caja close must be attributed to the authenticated caller.')); END IF;` — this must run before any write path is reached; (2) in the `UPDATE caja_sessions SET ...` statement, change the `closed_by = p_closed_by` line to `closed_by = auth.uid()`, binding the actual write to the server-verified identity as defense-in-depth even though the guard above already proves equality. Do not change the parameter list, the `record_audit` call, the cash-reconciliation calculation, or the JSON return shape — the frontend and every other existing caller keep working unchanged since they already always pass the logged-in user's own id. Wrap in `BEGIN;`/`COMMIT;` + `NOTIFY pgrst, 'reload schema';`, matching house style.

[BLOCKING] Apply immediately after writing the file, before rerunning the regression test: try `supabase db push --db-url "postgresql://postgres.<POOLER_TENANT_ID>:<POSTGRES_PASSWORD>@localhost:5432/postgres?sslmode=disable" --debug` first (credentials via `docker exec supabase-db env` / `docker exec supabase-pooler env`); if the pooler is unreachable, fall back to `docker exec -i supabase-db psql -U supabase_admin -d postgres < supabase/migrations/20260818000005_close_caja_session_authoritative_closed_by.sql`, then register it with `docker exec supabase-db psql -U supabase_admin -d postgres -c "INSERT INTO supabase_migrations.schema_migrations (version, name) VALUES ('20260818000005', 'close_caja_session_authoritative_closed_by') ON CONFLICT (version) DO NOTHING"`. Confirm via `docker exec supabase-db psql -U supabase_admin -d postgres -c "SELECT pg_get_functiondef('close_caja_session'::regproc)"` that the live function body now contains the new guard and `closed_by = auth.uid()` before rerunning the test.</action>
  <verify>
    <automated>npx playwright test e2e/55-full-day-soak.spec.ts -g "mismatched p_closed_by is rejected" --retries=0</automated>
  </verify>
  <acceptance_criteria>The new regression test passes against the live database. `pg_get_functiondef('close_caja_session'::regproc)` on the live database contains both the new `IS DISTINCT FROM auth.uid()` guard and `closed_by = auth.uid()`. Migration `20260818000005` is registered in `supabase_migrations.schema_migrations`.</acceptance_criteria>
  <done>A manager/admin can no longer close a caja under a forged identity — a mismatched `p_closed_by` is rejected with `PERMISSION_DENIED` and the caja session is left untouched, proven by a live regression test.</done>
  <reversibility rating="reversible">Pure server-side logic change to an existing SECURITY DEFINER function's authorization/write path — no data migration, no signature change; revertible with another `CREATE OR REPLACE` restoring the prior body.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete stale Tip Distribution and Modifier Popularity assertions from the reports regression suite</name>
  <files>e2e/07-reports.spec.ts</files>
  <read_first>e2e/07-reports.spec.ts (lines 696-774 — the full "Sprint 10 — Tip Distribution tab" comment header and its 4 tests; lines 803-834 — the "Phase 24: all 4 new report tabs render without crash" test containing the Modifier Popularity assertions; lines 879-927 — the unrelated pre-existing "Phase 24: bartender-initiated reason-required removal succeeds" test, DO NOT touch this one); src/pages/reports/index.tsx (lines 57-100 — confirms the exact 10 remaining `TabsTrigger` values: session, products, hourly, categories, payment-methods, staff, voids, deletions-pre, deletions-post, refunds-reg — no tips/modifier-popularity trigger exists); .planning/phases/04-reports-hardening/04-01-SUMMARY.md (confirms the Tips and Modifier Popularity tabs/widgets were deleted end-to-end in Plan 01 per D-01/D-02/D-04, so these E2E assertions can never pass again)</read_first>
  <action>Delete the entire "Sprint 10 — Tip Distribution tab (TipDistributionPanel)" block from `e2e/07-reports.spec.ts`: the `// ---` comment header and all four `test('Sprint 10: Tip Distribution tab ...')` blocks that follow it ("is present and navigable", "shows DateRangePicker with today's date", "shows column headers or empty state", "shows empty state for year 2020 date range") — that report tab and its widget no longer exist. Leave exactly one blank line before the next test ("Sprint 10: Export button appears in Staff Performance tab when data rows exist"), which is unaffected and stays.

In the same file's `test('Phase 24: all 4 new report tabs render without crash', ...)` test, remove the `TabsTrigger` visibility assertion for the "Modifiers" tab and the `// modifier-popularity: chart+table or empty state` block that clicks that tab and asserts its panel — that report tab and its widget no longer exist. Rename the test to `'Phase 24: retained new report tabs render without crash'` and keep the remaining Deletions/Corrections/Payment Methods assertions unchanged (three tabs, not four).

Leave the unrelated "Phase 24: bartender-initiated reason-required removal succeeds" test untouched — it navigates to `/pool-tables/:tableId` and seeds via the `pool_tables` table, both dropped in Phase 1 (`supabase/migrations/20260810000003_drop_pool_resources.sql`), so it is already broken for a reason unconnected to this phase's report-tab removal. This is a distinct, pre-existing defect outside this gap closure's scope — flag it for a separate follow-up rather than fixing it here.</action>
  <verify>
    <automated>npx playwright test e2e/07-reports.spec.ts --grep-invert "bartender-initiated" --retries=0</automated>
  </verify>
  <acceptance_criteria>Every retained test in `e2e/07-reports.spec.ts` passes, excluding only the one pre-existing, unrelated pool-tables-dependent test named above. No test references a "Tip Distribution" or "Modifiers" tab.</acceptance_criteria>
  <done>The reports regression suite no longer asserts tabs that Plan 01 already deleted; the retained-tab coverage passes end-to-end.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Prove the soak's receiving step committed before trusting the near-expiry badge, and remove the flaky reload</name>
  <files>e2e/55-full-day-soak.spec.ts</files>
  <read_first>e2e/55-full-day-soak.spec.ts (lines 237-256 — the `page.reload()` call after mutating `sold_by_weight`/inventory to fix per WR-01; lines 292-323 — the supplier-create → receiving-UI → Confirm Receipt → near-expiry-badge sequence to harden per WR-02); src/shared/lib/edge-function-contracts.ts (lines 438-487 — `callReceiveShipment`, `ReceiveShipmentEnvelopeSchema`/`ReceiveShipmentSuccessSchema`: the edge function posts to `${VITE_SUPABASE_URL}/functions/v1/receive-shipment` and its JSON response is `{ success: boolean, shipmentId?: string, error?: {...} }`); supabase/migrations/20260817000002_receive_shipment_atomicity.sql (lines 36-50 — confirms the RPC's `INSERT ... ON CONFLICT (product_id) DO UPDATE` adds the received quantity onto existing `inventory.quantity_on_hand` and overwrites `inventory.expiry_date`); e2e/helpers/auth.ts (lines 98-113 — `gotoAuthed`, the retrying authenticated-navigation helper already imported in this file, used elsewhere at lines 261/268/309/321)</read_first>
  <action>WR-01: Replace `await page.reload();` (after the `sold_by_weight`/`inventory.quantity_on_hand` admin mutations) with `await gotoAuthed(page, '/pos');` followed by `await expect(page.getByPlaceholder(/search products/i)).toBeVisible({ timeout: 15_000 });` before the existing `.fill('Corona')` call — removes the documented full-reload session-loss flakiness in this test's main hardening sequence.

WR-02: Before clicking "Confirm Receipt", capture the corona product's current `inventory.quantity_on_hand` via the admin client (reuse the already-in-scope `coronaProduct.id` from the earlier weighted-sale section) into a local variable. Wrap the "Confirm Receipt" click in a `page.waitForResponse` that matches `response.request().method() === 'POST' && response.url().includes('/functions/v1/receive-shipment')`, await and parse its JSON body as `{ success?: boolean; shipmentId?: string }`, and assert `success` is `true` and `shipmentId` is a truthy string — this is the receiving success signal that was previously never awaited. Then, before navigating to `/home` for the near-expiry badge check, re-query `inventory` for `coronaProduct.id` via the admin client and assert `quantity_on_hand` equals the captured before-value plus 2 (the line item's quantity) and `expiry_date` equals the exact date string already computed for the form input (`expiry.toISOString().slice(0, 10)`) — proving the specific shipment this test submitted is what produced the near-expiry signal, not a pre-existing unrelated row. Only after these assertions pass does the existing `gotoAuthed(page, '/home')` / near-expiry-badge check run, unchanged.</action>
  <verify>
    <automated>npx playwright test e2e/55-full-day-soak.spec.ts --retries=0</automated>
  </verify>
  <acceptance_criteria>The full soak spec (all 3 tests, including Task 1's new regression test) passes end-to-end. The receiving assertions fail loudly if `success` were ever false or the inventory row's quantity/expiry didn't reflect this test's own shipment — no longer able to pass on a coincidental pre-existing near-expiry row.</acceptance_criteria>
  <done>The soak proves its own UI receiving action committed (edge-function success response + persisted inventory quantity/expiry) before treating the near-expiry badge as evidence, and no longer depends on the flaky full-page-reload navigation pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated manager/admin session → `close_caja_session` RPC (SECURITY DEFINER) | Any valid manager/admin session can call this RPC; the parameter `p_closed_by` previously let that identity write an arbitrary UUID into a privileged audit field |
| E2E test harness (service-role admin client + manager session) → `close_caja_session`/`receive_shipment` RPCs | Test-only boundary — Task 1's new test deliberately sends a mismatched-identity payload to prove the RPC's own authority check holds |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Spoofing | `close_caja_session`'s `p_closed_by` parameter | high | mitigate | Task 1's migration rejects any `p_closed_by IS DISTINCT FROM auth.uid()` with `PERMISSION_DENIED` and binds the actual `UPDATE`'s `closed_by` write to `auth.uid()` server-side, not the client-supplied value; a live regression test proves a forged identity is rejected and the caja session row is left unmodified |
| T-04-08 | Repudiation | `e2e/55-full-day-soak.spec.ts`'s receiving step (pre-fix) | medium | mitigate | The soak previously could pass its near-expiry assertion without the receiving action having committed. Task 3 now awaits the `receive-shipment` edge function's own success response and verifies the exact inventory quantity/expiry it produced before trusting the downstream badge, closing the evidentiary gap |
</threat_model>

<verification>
- `npx playwright test e2e/55-full-day-soak.spec.ts --retries=0` passes in full (3 tests: happy-path soak, idempotent-close no-op, mismatched-`p_closed_by` rejection).
- `npx playwright test e2e/07-reports.spec.ts --grep-invert "bartender-initiated" --retries=0` passes in full.
- Live database: `pg_get_functiondef('close_caja_session'::regproc)` contains the new `auth.uid()` guard and binding; migration `20260818000005` is registered in `supabase_migrations.schema_migrations`.
</verification>

<success_criteria>
Caja close attribution can no longer be forged, the retained reports E2E suite passes without asserting tabs Plan 01 already removed, and the full-day soak proves its own receiving step committed rather than assuming it did — closing all 3 blockers from 04-VERIFICATION.md so REP-01/REP-02 are re-provable as VERIFIED.
</success_criteria>

## Artifacts this phase produces

- `supabase/migrations/20260818000005_close_caja_session_authoritative_closed_by.sql` (new migration — binds `closed_by` to `auth.uid()`, rejects a mismatched `p_closed_by` with `PERMISSION_DENIED`)
- `e2e/55-full-day-soak.spec.ts` (extended — new mismatched-identity regression test; receiving-commit proof via edge-function response + inventory assertion; `gotoAuthed` replacing `page.reload()`)
- `e2e/07-reports.spec.ts` (trimmed — obsolete Tip Distribution/Modifier Popularity assertions removed)
- No new test helpers — reuses existing `openCaja`/`gotoAuthed`/`getServiceClient` from `e2e/helpers/`

<output>
Create `.planning/phases/04-reports-hardening/04-04-SUMMARY.md` when done
</output>
