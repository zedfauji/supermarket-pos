---
phase: 04-reports-hardening
plan: 06
type: execute
wave: 6
depends_on: ["04-05"]
files_modified:
  - e2e/07-reports.spec.ts
autonomous: true
gap_closure: true
gap_ids: ["G-04-1", "G-04-2"]
requirements: [REP-02]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "e2e/07-reports.spec.ts's six date-string computations for 'today'/'yesterday' use the same local-calendar-date algorithm as the app's own default (src/pages/reports/index.tsx's toDateStr), so date-based assertions can never disagree with the app regardless of host timezone or time of day (closes 04-VERIFICATION.md Gap G-04-1)."
    - "The retained e2e/07-reports.spec.ts suite (excluding the pre-existing, already-documented bartender-initiated pool-tables test) is fully green end-to-end, growing from 26 to 27 passing tests with the addition of the new Margin-column layout test."
    - "A Playwright test proves the Product Sales Margin column renders without header/body clipping or overlap at both the app's default desktop viewport (1280x800) and its documented minimum supported window size (1024x700, per src-tauri/tauri.conf.json), covering both a populated-margin row and the accessible '—' unknown-cost placeholder row (closes 04-VERIFICATION.md's behavior_unverified backstop item G-04-2)."
  artifacts:
    - "e2e/07-reports.spec.ts"
  key_links:
    - "The new localDateStr() test helper reproduces src/pages/reports/index.tsx's toDateStr() algorithm (getFullYear/getMonth+1/getDate, zero-padded, hyphen-joined) exactly, so the test's notion of 'today'/'yesterday' is structurally guaranteed to match the app's own default date range on any machine, in any timezone — not just the one this gap happened to reproduce in."
    - "The new layout test's two seeded order_items (one with cost_price_snapshot set, one left null via seedOpenTab's default) drive ProductSalesPanel's two Margin-cell rendering branches (MoneyDisplay vs. the aria-labelled '—' placeholder), proving both branches lay out correctly side-by-side with the other five columns, not just one."
  prohibitions:
    - statement: "Do not modify src/entities/tab/model/queries-reports.ts, src/pages/reports/index.tsx, or any other e2e spec file — both gaps are provable with test-only changes to e2e/07-reports.spec.ts; the underlying production margin/date logic is already correct and already covered by 04-02/04-05's own regression tests."
      disposition: flagged
---

<objective>
Close the 2 remaining items from 04-VERIFICATION.md's re-verification (score 13/15): (G-04-1) the retained `e2e/07-reports.spec.ts` suite is not reliably green — 6 tests compute "today"/"yesterday" via `new Date().toISOString().slice(0, 10)` (UTC calendar date), while the app's actual default date range (`src/pages/reports/index.tsx`'s `toDateStr(new Date())`) uses the LOCAL calendar date, so the two disagree during the daily window between local midnight and UTC midnight; (G-04-2) 04-02-PLAN.md's `verification: backstop` truth — "the Margin column has no layout breakage in Product Sales" — was never converted into an automated Playwright assertion by any Phase 4 plan, which this project's CLAUDE.md testing policy does not accept as a terminal state.

Both gaps are closed with test-only changes to `e2e/07-reports.spec.ts`. No production code changes: the margin math (04-02/04-05) and the app's own local-date default (`toDateStr`) are already correct — only the test's own date-computation algorithm and test coverage are incomplete.

Purpose: these are the last 2 items between 04-VERIFICATION.md's `gaps_found` (13/15) and the phase goal being achieved — REP-02 depends on the retained report suite being a trustworthy, time-of-day-independent regression proof, and on every truth from 04-01..04-05 (including the Margin column's layout truth) being provably closed, not left as an accepted backstop.

Output: `e2e/07-reports.spec.ts` with (1) all 6 date-string computations rewritten to match the app's own local-calendar-date algorithm, and (2) one new Playwright test proving the Product Sales Margin column lays out correctly at both a supported desktop and the app's minimum supported window size, for both a populated-margin row and an unknown-cost row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reports-hardening/04-VERIFICATION.md
@.planning/phases/04-reports-hardening/04-05-SUMMARY.md
@src/pages/reports/index.tsx
@src/widgets/ProductSalesPanel/ProductSalesPanel.tsx
@e2e/07-reports.spec.ts
@e2e/helpers/supabase.ts
@src-tauri/tauri.conf.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute e2e/07-reports.spec.ts's date assertions using local calendar date, not UTC</name>
  <files>e2e/07-reports.spec.ts</files>
  <read_first>e2e/07-reports.spec.ts lines 187-194 (the end of the `enterManagerPin` helper and the start of `test.describe('Reports Page', ...)` — the insertion point for a new module-level helper, alongside the file's other local helpers like `seedCashPayment`/`seedRemovableItem`/`enterManagerPin`); e2e/07-reports.spec.ts lines 298, 417, 504, 610 (four `const today = new Date().toISOString().slice(0, 10);` sites) and lines 531-534, 703-706 (two sites where a `yesterday` Date is computed via `setDate(getDate() - 1)` then converted to `yesterdayStr` the same UTC-ISO way) — all 6 sites to fix; src/pages/reports/index.tsx lines 16-19 (`toDateStr(d: Date)` — the exact local-calendar-date algorithm: `d.getFullYear()`, `String(d.getMonth() + 1).padStart(2, '0')`, `String(d.getDate()).padStart(2, '0')`, joined with hyphens) and line 32 (`toDateStr(new Date())` as the app's real default date range) — the algorithm the test must mirror byte-for-byte so the two sides can never disagree, in any timezone.</read_first>
  <action>Add a module-level helper function `localDateStr(d: Date): string` to `e2e/07-reports.spec.ts`, placed after the existing `enterManagerPin` helper and before `test.describe('Reports Page', ...)`. Reproduce `src/pages/reports/index.tsx`'s `toDateStr` algorithm exactly: year from `getFullYear()`, month from `getMonth() + 1` zero-padded to 2 digits, day from `getDate()` zero-padded to 2 digits, joined as `YYYY-MM-DD` — a local-calendar-date computation, not an ISO/UTC-based one.

Replace each of the 6 existing date-string computations with a call to this new helper, keeping every existing variable name and every downstream assertion unchanged: the four `const today = ...` assignments (lines 298, 417, 504, 610) become `const today = localDateStr(new Date());`, and the two `yesterdayStr` assignments (lines 534, 706) — each already preceded by a `yesterday` Date computed via `new Date(); yesterday.setDate(yesterday.getDate() - 1);` — become `const yesterdayStr = localDateStr(yesterday);`. Do not touch the preceding `yesterday.setDate(...)` lines; only the final string-conversion line changes at each site.

This is a pure date-computation swap — no other line in any of the 6 affected tests changes. Because both the app (`toDateStr`) and the test (`localDateStr`) now derive their date string from the identical local-calendar-date algorithm, evaluated on the same machine, the two can never disagree regardless of host timezone or time of day: the fix removes the whole UTC-vs-local class of bug by construction, not just the specific offset 04-VERIFICATION.md happened to reproduce it in. In the plan's SUMMARY, state this equivalence-by-construction reasoning explicitly (per 04-VERIFICATION.md's instruction not to rely on a single lucky run) rather than only citing one green run.</action>
  <verify>
    <automated>npx playwright test e2e/07-reports.spec.ts --grep-invert "bartender-initiated" --retries=0</automated>
  </verify>
  <acceptance_criteria>All 6 sites use the new local-calendar-date helper; none compute a date string via the old ISO/UTC-based approach anymore. The full retained suite passes (26/26 at this point, before Task 2 adds the 27th test). Because the fix makes the test's date algorithm structurally identical to the app's own `toDateStr`, the equivalence holds for any timezone or time of day — not only the one this session happens to run in.</acceptance_criteria>
  <done>e2e/07-reports.spec.ts's date-based assertions can never disagree with the app's actual default date range again, independent of UTC offset or time of day — closing 04-VERIFICATION.md's Gap G-04-1.</done>
</task>

<task type="auto">
  <name>Task 2: Add a Playwright layout backstop for the Product Sales Margin column</name>
  <files>e2e/07-reports.spec.ts</files>
  <read_first>src/widgets/ProductSalesPanel/ProductSalesPanel.tsx lines 44-91 (the 6 column defs in render order — productName, categoryName, units, revenue, pctTotal, margin — and the margin cell's two branches: a `MoneyDisplay` for a known cost, an aria-labelled "—" span when `margin === null`); src/shared/lib/i18n/locales/en-US/wAdmin.json lines 101-107 (exact header labels for this tabpanel: "Product", "Category", "Units Sold", "Revenue", "% of Total", "Margin", and `marginUnavailableAriaLabel`: "Margin unavailable — no recorded cost for this period"); src-tauri/tauri.conf.json lines 16-19 (window default 1280x800, `minWidth`/`minHeight` 1024x700 — the app's own documented supported-viewport bounds; `playwright.config.ts`'s default viewport is already 1280x800, so only the 1024x700 "narrow" case needs an explicit resize); e2e/helpers/supabase.ts lines 456-535 (`seedOpenTab` — returns `{ tabId, orderId }`; its own `order_items` insert never sets `cost_price_snapshot` or `weight_grams`, so a row seeded through it renders the unknown-cost "—" placeholder for free) and lines 46-100 (`seedCashPayment` in this same file — the existing local pattern for a one-off `getServiceClient()` direct insert, to mirror for the second, populated-margin `order_items` row); e2e/07-reports.spec.ts lines 273-289 ("Product Sales tab shows at least one product row..." — the existing precedent for `seedOpenTab` + login + `page.goto('/reports')` + Product Sales tab click) and line 681 (`tabPanel.locator('tbody tr td:nth-child(2)')` — the file's own existing precedent for targeting a specific column by `nth-child`, reused here for the 6th/Margin column).</read_first>
  <action>Add one new test to `e2e/07-reports.spec.ts`'s "Sprint 1 Feature #12 — Product Sales" section, immediately after the existing "Product Sales: date range filter to far past shows empty state" test, named "Product Sales: Margin column has no layout breakage at desktop and narrow viewports". Give it a generous `test.setTimeout` (e.g. 90 seconds) since it seeds data and asserts at two viewports.

Seed two representative Product Sales rows sharing one tab/order: call `seedOpenTab({ customerName: 'E2E-Margin-Layout', withItem: true, productName: 'Budweiser' })` for the unknown-cost row (its `orderId` return value is reused below; Budweiser's row will render the "—" placeholder since `seedOpenTab` never sets `cost_price_snapshot`). Then, via `getServiceClient()` directly (mirroring `seedCashPayment`'s local-insert shape), fetch one other product's `id`/`base_price` (`.neq('name', 'Budweiser').limit(1).single()`) and insert a second `order_items` row on the same `orderId` with `quantity: 1`, `unit_price` set to that product's `base_price`, `modifier_price_delta: 0`, and `cost_price_snapshot` set to a plausible fraction of `base_price` (e.g. 40% of it, rounded to 2 decimals) — this is the populated-margin row. Throw on any insert error, matching this file's existing seeding helpers.

`loginAs(page, 'admin')`, `page.goto('/reports')`, click the Product Sales tab, wait for its tabpanel to be visible — same shape as the existing Product Sales tests. Then, at the default viewport (1280x800, Playwright's already-configured default — no resize needed for this pass):
- Assert all six `columnheader`-role locators ("Product", "Category", "Units Sold", "Revenue", "% of Total", "Margin") inside the tabpanel are visible.
- Assert both representative rows render: the Budweiser row's 6th (`nth-child(6)`) cell has an `aria-label` matching `marginUnavailableAriaLabel`'s text, and the other seeded product's row's 6th cell contains money-formatted text (a `$` prefix).
- Get each of the 6 columnheader locators' bounding boxes, sort them by `x`, and assert no adjacent pair overlaps — each header's `x + width` is less than or equal to the next header's `x` (allow ~1-2px rounding tolerance). This is the core "no layout breakage" signal: a real CSS/flex regression collapses or overlaps header cells, and this check catches it structurally rather than visually.
- Assert the header row and the first body row don't vertically overlap either: the header row's `y + height` is less than or equal to the first body row's `y` (same small tolerance) — catches sticky/absolute-position regressions.

Then call `page.setViewportSize({ width: 1024, height: 700 })` (the app's documented minimum supported window size) and repeat the same 4 assertions (all 6 headers visible, both rows' Margin cells visible/readable, no header-to-header x-overlap, no header-to-body y-overlap) to prove the narrow supported width doesn't break the layout either. Finish with `logout(page)`.

No new imports are needed — `getServiceClient`, `seedOpenTab`, `loginAs`, and `logout` are already imported at the top of this file.</action>
  <verify>
    <automated>npx playwright test e2e/07-reports.spec.ts --grep-invert "bartender-initiated" --retries=0</automated>
  </verify>
  <acceptance_criteria>The new test passes at both viewports (1280x800 desktop, 1024x700 narrow/minimum-supported), proving via bounding-box assertions (no header-to-header x-overlap, no header-to-body y-overlap) that all six Product Sales columns — including the new Margin column — render without clipping or overlap for both a populated-margin row and an unknown-cost "—" row. The retained suite grows from 26 to 27 passing tests (the 26 from Task 1 plus this new one), all green, excluding only the pre-existing `bartender-initiated` test already documented as out of scope.</acceptance_criteria>
  <done>The Product Sales Margin column's 04-02-PLAN.md backstop truth ("no layout breakage at supported desktop/narrow viewports") is now proven by an automated Playwright assertion instead of remaining an unresolved human/backstop item — closing 04-VERIFICATION.md's behavior_unverified gap G-04-2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test harness (`getServiceClient()`, service-role Supabase client) → `order_items`/`products` tables | Pre-existing test-seeding pattern already used by every spec in this suite (e.g. this file's own `seedCashPayment`) to bypass RLS and seed fixtures; this plan adds one more service-role insert of the same kind for the new layout test — no new boundary, no new caller of production code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-11 | Tampering (test-only) | Task 2's new `order_items` seed insert in `e2e/07-reports.spec.ts` | low | accept | Uses the exact same `getServiceClient()` service-role seeding pattern already present throughout this suite; no production RLS/auth path or new attack surface is introduced — it is test fixture data for an already-role-gated (`view_reports`), read-only report |
| T-04-12 | Repudiation (N/A — correctness only) | Task 1's date-computation fix | low | accept | Pure test-assertion correctness fix (UTC-based to local-calendar-date algorithm); touches no production code path, no data exposure, no new attack surface |

Both threats are internal, test-only changes with no production code modified and no new attack surface — severity is `low` because no trust boundary is crossed and no authorization/access-control logic is touched.
</threat_model>

<verification>
- `npx playwright test e2e/07-reports.spec.ts --grep-invert "bartender-initiated" --retries=0` passes 27/27 (26 pre-existing plus the 1 new layout test) after both tasks land.
- `npm run typecheck` passes (test-only TypeScript changes).
- `npx eslint e2e/07-reports.spec.ts` — 0 new errors beyond this file's existing pre-04-05-documented baseline.
</verification>

<success_criteria>
The retained Reports E2E suite is fully green and structurally timezone-independent (no UTC-vs-local date mismatch remains), and the Product Sales Margin column's layout is proven correct by an automated Playwright assertion at both supported viewport sizes — closing both remaining 04-VERIFICATION.md items so the Phase 4 goal (REP-02: trustworthy, fully-automated report verification) is achieved with no outstanding backstop/human-verification items.
</success_criteria>

## Artifacts this plan produces

- `e2e/07-reports.spec.ts` (6 date-computation sites fixed to local-calendar-date; 1 new Margin-column layout test added)

<output>
Create `.planning/phases/04-reports-hardening/04-06-SUMMARY.md` when done
</output>
</output>
