---
phase: 01-strip-rebrand
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src-tauri/tauri.conf.json
  - index.html
  - src/shared/lib/i18n/locales/es-MX/receipt.json
  - src/shared/lib/i18n/locales/en-US/receipt.json
  - src/shared/lib/i18n/locales/es-MX/wPanels.json
  - src/shared/lib/i18n/locales/en-US/wPanels.json
  - README.md
autonomous: true
requirements: []

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The app window title, browser tab title, and installer productName all read Supermarket POS instead of any Bar POS / Bar & Pool Parlor POS variant"
    - "Printed receipts and PDF exports show Supermarket POS as the header in both es-MX and en-US locales"
    - "package.json's name field is supermarket-pos"
  artifacts:
    - path: "src-tauri/tauri.conf.json"
      provides: "productName and window title renamed to Supermarket POS"
    - path: "src/shared/lib/i18n/locales/es-MX/receipt.json"
      provides: "pdf.appTitle renamed to Supermarket POS"
    - path: "src/shared/lib/i18n/locales/en-US/receipt.json"
      provides: "pdf.appTitle renamed to Supermarket POS"
---

<objective>
Rebrand every user-visible and config identity string from "bar-pos"/"Bar POS"/"Bar &amp; Pool Parlor POS" to "Supermarket POS" per D-01/D-02/D-03/D-04 — strings-only, zero visual/theme change, zero touch to internal OS-level identifiers (Rust crate name, Tauri `identifier`) which stay out of scope per RESEARCH.md Open Question 3.

Purpose: D-01 requires the app to present as "Supermarket POS" everywhere a user or the OS sees it; D-02 scopes this to strings only (no logo/theme/color change) for Phase 1.
Output: Renamed identity strings across package config, Tauri window config, browser title, and printed receipts in both locales.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-strip-rebrand/01-CONTEXT.md
@.planning/phases/01-strip-rebrand/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename app identity in config, window, and browser title (D-01/D-03)</name>
  <files>package.json, src-tauri/tauri.conf.json, index.html</files>
  <read_first>
    - package.json line 2 (current `"name": "bar-pos"`)
    - src-tauri/tauri.conf.json lines 1-20 (`productName`, `app.windows[0].title`, `identifier`)
    - index.html line 7 (`<title>Bar POS</title>`)
  </read_first>
  <action>
    In package.json, change the top-level `"name"` field from `"bar-pos"` to `"supermarket-pos"` (kebab-case package identifier, D-03). In src-tauri/tauri.conf.json, change `productName` from `"Bar &amp; Pool Parlor POS"` to `"Supermarket POS"` and `app.windows[0].title` from `"Bar POS"` to `"Supermarket POS"` — leave `identifier` (`com.yourcompany.barpos`) and every other field untouched (OS-level app identifier controlling the installed data directory path, explicitly out of D-03's scope per RESEARCH.md Open Question 3). In index.html, change the `&lt;title&gt;` element text from `Bar POS` to `Supermarket POS`. Do not touch src-tauri/Cargo.toml's crate names (`bar-pos`, `bar_pos_lib`) — internal Rust identifiers, out of scope for the same reason as the Tauri `identifier`.
  </action>
  <verify>
    <automated>grep -c "Supermarket POS" package.json src-tauri/tauri.conf.json index.html | awk -F: '{s+=$2} END {exit (s>=3)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `"name"` reads `"supermarket-pos"`
    - `src-tauri/tauri.conf.json` `productName` and `app.windows[0].title` both read `"Supermarket POS"`
    - `index.html`'s `&lt;title&gt;` reads `Supermarket POS`
    - `src-tauri/tauri.conf.json`'s `identifier` field is unchanged (`com.yourcompany.barpos`)
  </acceptance_criteria>
  <done>Every OS/browser-surfaced app title reads Supermarket POS; internal identifiers (Rust crate, Tauri identifier) are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Rename receipt/PDF header in both locales, remove orphaned bartender i18n label, grep README (D-01/D-03/D-22)</name>
  <files>src/shared/lib/i18n/locales/es-MX/receipt.json, src/shared/lib/i18n/locales/en-US/receipt.json, src/shared/lib/i18n/locales/es-MX/wPanels.json, src/shared/lib/i18n/locales/en-US/wPanels.json, README.md</files>
  <read_first>
    - src/shared/lib/i18n/locales/es-MX/receipt.json line 36 (`pdf.appTitle`)
    - src/shared/lib/i18n/locales/en-US/receipt.json line 36 (`pdf.appTitle`)
    - src/shared/lib/i18n/locales/es-MX/wPanels.json line 359 (`homeDashboard.managerLabels.bartender` key)
  </read_first>
  <action>
    In both `receipt.json` locale files, change `pdf.appTitle` from `"Bar &amp; Pool Parlor POS"` to `"Supermarket POS"` — identical value in both es-MX and en-US (this is an app-identity string, not a translated phrase). In both `wPanels.json` locale files, delete the `homeDashboard.managerLabels.bartender` key entirely (its only consumer was the `/kds-bar` HomeDashboard tile being removed in a later plan in this phase — deleting it now is safe since nothing else reads this key). Run `grep -in "bar-pos\|bar pos\|bar & pool parlor" README.md` and replace any hit with the equivalent Supermarket POS phrasing, preserving surrounding sentence structure.
  </action>
  <verify>
    <automated>grep -c "Supermarket POS" src/shared/lib/i18n/locales/es-MX/receipt.json src/shared/lib/i18n/locales/en-US/receipt.json | awk -F: '{s+=$2} END {exit (s>=2)?0:1}' &amp;&amp; ! grep -q '"bartender":' src/shared/lib/i18n/locales/es-MX/wPanels.json src/shared/lib/i18n/locales/en-US/wPanels.json</automated>
  </verify>
  <acceptance_criteria>
    - Both `receipt.json` locale files' `pdf.appTitle` read `"Supermarket POS"`
    - Neither `wPanels.json` locale file contains a `homeDashboard.managerLabels.bartender` key
    - `README.md` has zero remaining case-insensitive hits for "bar-pos", "bar pos", or "bar &amp; pool parlor"
  </acceptance_criteria>
  <done>Printed receipts/PDFs show Supermarket POS in both locales; the orphaned bartender i18n label is gone; README is free of stale bar-pos branding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None — this plan only edits static string literals in config/i18n JSON, no runtime data path or trust boundary is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | N/A — no code logic, only literal string content | low | accept | Pure string-literal edits in config/i18n files carry no execution-path risk; changes are visually verifiable and covered by the grep-based `&lt;verify&gt;` gates above |
</threat_model>

<verification>
`grep -c "Supermarket POS" package.json src-tauri/tauri.conf.json index.html` finds all three; both receipt.json locales' `pdf.appTitle` read "Supermarket POS"; neither wPanels.json locale has the orphaned `bartender` key; README has zero stale bar-pos references.
</verification>

<success_criteria>
Every user-visible and config app-identity string reads "Supermarket POS"; no internal OS-level identifier (Tauri `identifier`, Rust crate names) was touched.
</success_criteria>

<output>
Create `.planning/phases/01-strip-rebrand/01-02-SUMMARY.md` when done
</output>
