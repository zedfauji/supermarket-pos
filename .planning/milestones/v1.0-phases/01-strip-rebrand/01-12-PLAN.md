---
phase: 01-strip-rebrand
plan: 12
type: execute
wave: 3
depends_on: ["01-04"]
files_modified:
  - src/widgets/TipBucketDistributionPanel/**
  - src/widgets/SettingsTabsPanel/tabs/TipDistributionSettingsTab.tsx
  - src/widgets/SettingsTabsPanel/tabs/TipDistributionSettingsTab.test.tsx
  - supabase/migrations/2026081008xx_drop_tip_distribution.sql
  - e2e/42-tip-distribution.spec.ts
autonomous: true
requirements: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No file under src/widgets/TipBucketDistributionPanel/ or TipDistributionSettingsTab.tsx/.test.tsx exists"
    - "tip_distribution_entries table no longer exists on the new Supabase project"
    - "close_caja_session's live function source contains the version = version + 1 fix but no INSERT INTO tip_distribution_entries block — every caja close still works"
    - "src/widgets/TipDistributionPanel/ (the pre-existing, generic per-staff tip report, distinct from TipBucketDistributionPanel) is UNCHANGED"
  artifacts:
    - path: "supabase/migrations/2026081008xx_drop_tip_distribution.sql"
      provides: "DROP tip_distribution_entries; close_caja_session restored to the version-bump-fixed body without the tip-distribution INSERT block"
---

<objective>
Remove the bar-specific floor/bar/kitchen tip-bucket distribution feature entirely — the "Tip Split" Settings tab, the "Tip Split" Reports tab widget (`TipBucketDistributionPanel`), and its DB schema/logic — per D-21 ("bar tip-splitting doesn't apply to grocery cashiers... together with their feature code").

Purpose: this feature is distinct from the pre-existing, GENERIC per-staff "Tip Distribution" report (`TipDistributionPanel`, ReportsPage's `tips` tab) — that one is kept, unaffected by this plan.

**Highest-risk part of this plan (Pitfall 4):** `close_caja_session` — a shared function every caja close depends on (REP-01-critical, generic, kept feature) — was modified by `20260709000002_close_caja_session_tip_distribution.sql` to (a) fix a pre-existing STALE_VERSION bug (`version = version + 1`, unrelated to tip-distribution, must be KEPT) and (b) INSERT a `tip_distribution_entries` row (must be REMOVED). Dropping only the table without restoring the function would make every caja close start erroring.
Output: no tip-bucket-distribution code or its Settings/Reports UI remains; `close_caja_session` still closes a caja correctly, with the version-bump fix intact and the tip-distribution INSERT removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-strip-rebrand/01-CONTEXT.md
@.planning/phases/01-strip-rebrand/01-RESEARCH.md
@.planning/phases/01-strip-rebrand/01-PATTERNS.md
@supabase/migrations/20260709000002_close_caja_session_tip_distribution.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete TipBucketDistributionPanel widget, TipDistributionSettingsTab, and the tip-split E2E spec (D-21)</name>
  <files>src/widgets/TipBucketDistributionPanel/, src/widgets/SettingsTabsPanel/tabs/TipDistributionSettingsTab.tsx, src/widgets/SettingsTabsPanel/tabs/TipDistributionSettingsTab.test.tsx, e2e/42-tip-distribution.spec.ts</files>
  <read_first>
    - src/pages/reports/index.tsx (post-01-04 state — confirm the `tips` tab still renders `TipDistributionPanel`, a DIFFERENT widget than the one this task deletes)
  </read_first>
  <action>
    Delete the directory `src/widgets/TipBucketDistributionPanel/` in full (`git rm -r`). Delete the two files `src/widgets/SettingsTabsPanel/tabs/TipDistributionSettingsTab.tsx` and `src/widgets/SettingsTabsPanel/tabs/TipDistributionSettingsTab.test.tsx` (registration already removed from `SettingsTabsPanel/index.tsx` and the `tip-split` tab already removed from `pages/reports/index.tsx` in 01-04). Do NOT touch `src/widgets/TipDistributionPanel/` — the distinct, generic, kept per-staff tip report. Delete `e2e/42-tip-distribution.spec.ts` per D-11 (delete, not archive). Grep i18n locale files for `tipSplit`/`tipDistributionSettings`-namespaced keys (distinct from a plain `tipDistribution`/`tips` key used by the kept per-staff report) and remove entries whose only consumer was code deleted in this task, per D-22.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; test ! -d src/widgets/TipBucketDistributionPanel &amp;&amp; test ! -f src/widgets/SettingsTabsPanel/tabs/TipDistributionSettingsTab.tsx &amp;&amp; test -d src/widgets/TipDistributionPanel &amp;&amp; test ! -f e2e/42-tip-distribution.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/widgets/TipBucketDistributionPanel/` and `TipDistributionSettingsTab.tsx`/`.test.tsx` no longer exist
    - `src/widgets/TipDistributionPanel/` (the distinct generic widget) still exists, unmodified
    - `e2e/42-tip-distribution.spec.ts` no longer exists
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>Tip-bucket-distribution UI is fully deleted; the generic per-staff tip report is confirmed untouched.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Drop tip_distribution_entries and restore close_caja_session without the tip-distribution INSERT (Pitfall 4)</name>
  <precondition>supabase/config.toml's project_id is not shsrhxleopmovzpzqmex (Plan 01-01 completed)</precondition>
  <files>supabase/migrations/2026081008xx_drop_tip_distribution.sql</files>
  <read_first>
    - supabase/migrations/20260709000001_tip_distribution_entries_table.sql (table creation)
    - supabase/migrations/20260709000002_close_caja_session_tip_distribution.sql (full body already loaded in context — the function to restore: keep the `version = version + 1` fix in the `UPDATE caja_sessions` statement and the `caja.close` audit call; remove everything under the "Phase 19: tip-bucket distribution" comment block — the `v_tab_ids`/`v_total_tips`/`v_config`/`v_floor_pct`/`v_bar_pct`/`v_kitchen_pct`/`v_floor_amount`/`v_bar_amount`/`v_kitchen_amount`/`v_remainder`/`v_entry_id` local variable declarations and every statement computing/inserting a tip_distribution_entries row and its `tip_distribution.compute` audit call)
  </read_first>
  <action>
    Author a new forward migration following the repo's DROP-migration convention (analog: `supabase/migrations/20260711000001_drop_happy_hour_columns.sql`). First, `CREATE OR REPLACE FUNCTION close_caja_session(p_caja_id UUID, p_closed_by UUID, p_closing_cash NUMERIC(12,2), p_notes TEXT DEFAULT NULL) RETURNS JSON ...` with the SAME signature, permission check, open-tabs guard, and the `UPDATE caja_sessions SET closed_at=now(), closed_by=p_closed_by, closing_cash=p_closing_cash, notes=COALESCE(p_notes, notes), status='closed', version=version+1 WHERE id=p_caja_id AND status='open'` statement (the version-bump fix — KEEP, it is an unrelated bug fix riding along in the same migration, not tip-distribution logic) and the `record_audit('caja.close', ...)` call, ending with `RETURN json_build_object('ok', true); END; $$;` — i.e. everything from the current live body EXCEPT the "Phase 19: tip-bucket distribution" block (tab-id collection, tip pooling, config read, largest-remainder split, `INSERT INTO tip_distribution_entries`, and the `tip_distribution.compute` audit call). Then `DROP TABLE IF EXISTS tip_distribution_entries CASCADE;`. Include a DOWN section per D-19 noting rollback is not recommended (matches the existing migration's own DOWN comment). Before applying, enforce the project-id gate as a runnable command, not just the precondition note: run `grep -q 'project_id = "shsrhxleopmovzpzqmex"' supabase/config.toml &amp;&amp; { echo "REFUSING: linked to production bar project"; exit 1; }; supabase db push`.
  </action>
  <verify>
    <automated>grep -q 'project_id = "shsrhxleopmovzpzqmex"' supabase/config.toml &amp;&amp; { echo "REFUSING: linked to production bar project"; exit 1; }; supabase db push 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` completes successfully against the new project
    - `tip_distribution_entries` no longer appears in `information_schema.tables`
    - `close_caja_session`'s live function source (`pg_get_functiondef`) contains `version = version + 1` (or `version=version+1`) — the bug fix is preserved
    - `close_caja_session`'s live function source contains no reference to `tip_distribution_entries` or `tip_distribution.compute`
    - A test caja close (via the existing E2E flow in `02-caja.spec.ts`, run manually or as part of Plan 01-13's final full-suite verification) succeeds with no error
  </acceptance_criteria>
  <done>tip_distribution_entries is gone; close_caja_session closes a caja correctly with the version-bump fix intact and zero tip-distribution logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `close_caja_session` (SECURITY DEFINER) | Every caja close (REP-01-critical, generic, kept) routes through this single function — an incorrect rewrite that drops the version-bump fix alongside the tip-distribution logic would silently reintroduce the STALE_VERSION bug that made every caja close fail before Phase 19's fix |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-26 | Denial of Service | `supabase db push` against the wrong project | critical | mitigate | Task 2's `&lt;precondition&gt;` re-asserts project separation before the push |
| T-01-27 | Denial of Service | Rewrite accidentally drops the `version = version + 1` fix along with the tip-distribution block, reintroducing the STALE_VERSION bug for every caja close | critical | mitigate | Task 2's acceptance criteria explicitly assert (via `pg_get_functiondef`) that `version = version + 1` is present in the live function source, and that a real caja-close E2E test succeeds |
</threat_model>

<verification>
`npx tsc --noEmit` passes; `supabase db push` succeeds; `tip_distribution_entries` is gone; `close_caja_session`'s live source retains the version-bump fix and has zero tip-distribution logic; a real caja close succeeds.
</verification>

<success_criteria>
Tip-bucket distribution is fully removed at the code and SQL level with zero regression to the shared, generic caja-close path.
</success_criteria>

<output>
Create `.planning/phases/01-strip-rebrand/01-12-SUMMARY.md` when done
</output>
