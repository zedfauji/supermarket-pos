---
phase: 01-strip-rebrand
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/shared/lib/domain.ts
  - src/shared/lib/rbac.ts
  - e2e/helpers/auth.ts
  - e2e/helpers/supabase.ts
  - scripts/setup-dev-users.ts
  - supabase/migrations/2026081000xx_rename_bartender_role_to_cashier.sql
autonomous: true
requirements: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every persisted staff record with role bartender now has role cashier, and the DB CHECK constraint / RLS policies accept cashier, not bartender"
    - "domain.ts's UserRoleSchema and UserRole constants use cashier, not bartender"
    - "e2e/helpers/auth.ts's StaffRole type and loginAs() branch on cashier, reading the same E2E_BARTENDER_NAME/PIN env vars internally (no CI secret rename required)"
    - "Every E2E spec file that previously called loginAs(page, 'bartender') now calls loginAs(page, 'cashier') and still type-checks"
    - "HomeDashboard's role badge (which renders the raw currentStaff.role value) displays Cashier automatically post-rename with zero additional i18n change"
  artifacts:
    - path: "src/shared/lib/domain.ts"
      provides: "UserRoleSchema = z.enum(['cashier', 'manager', 'admin', 'kitchen'])"
    - path: "src/shared/lib/rbac.ts"
      provides: "CASHIER_ACTIONS constant (renamed from BARTENDER_ACTIONS), ROLE_SET keyed by cashier"
    - path: "supabase/migrations/2026081000xx_rename_bartender_role_to_cashier.sql"
      provides: "UPDATE profiles SET role='cashier' WHERE role='bartender', plus CHECK constraint / RLS policy literal updates"
  key_links:
    - from: "src/shared/lib/rbac.ts"
      to: "src/shared/lib/domain.ts"
      via: "import type { UserRole } from '@shared/lib/domain'"
      pattern: "StaffRole = UserRole"
---

<objective>
Rename the `bartender` role to `cashier` everywhere, fully and atomically, per D-16: TypeScript single source of truth (`domain.ts`), RBAC constants (`rbac.ts`), the DB `user_role` enum + every RLS policy/function that embeds the literal string, the E2E auth helper, seed scripts, and every E2E spec calling `loginAs(page, 'bartender')`. No partial rename — D-16 explicitly forbids a UI-only rename that leaves a code/DB naming mismatch, and RESEARCH.md's ASVS V4 threat notes a stale RLS policy still checking `role = 'bartender'` silently locks the renamed role out of formerly-accessible rows.

Purpose: "bartender" is bar-domain terminology; "cashier" matches the supermarket pivot. D-17's only action-list exception (deleting `transfer_tab`) and the STAFF_ACTIONS pruning for now-orphaned bar-specific actions are deliberately deferred to the phase's final consolidation plan (01-13) — removing action entries here, before the features that reference them are deleted in later waves, would break the build mid-phase.
Output: `cashier` is the live role everywhere — DB, TS types, RBAC constants, E2E helpers and specs — verified end to end.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-strip-rebrand/01-CONTEXT.md
@.planning/phases/01-strip-rebrand/01-RESEARCH.md
@.planning/phases/01-strip-rebrand/01-PATTERNS.md
@src/shared/lib/domain.ts
@src/shared/lib/rbac.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename bartender to cashier in the TypeScript single source of truth (D-16)</name>
  <files>src/shared/lib/domain.ts, src/shared/lib/rbac.ts</files>
  <read_first>
    - src/shared/lib/domain.ts lines 42-50 (`UserRoleSchema`, `UserRole` const)
    - src/shared/lib/rbac.ts lines 1-96 (`STAFF_ROLES`, `BARTENDER_ACTIONS`, `MANAGER_ACTIONS`, `ROLE_SET`)
  </read_first>
  <action>
    In domain.ts, change `UserRoleSchema = z.enum(['bartender', 'manager', 'admin', 'kitchen'])` to `z.enum(['cashier', 'manager', 'admin', 'kitchen'])`, and the `UserRole` const's `BARTENDER: 'bartender'` entry to `CASHIER: 'cashier'` (per CLAUDE.md, this file is the single source of truth — edit it first). In rbac.ts, change `STAFF_ROLES`'s `'bartender'` entry to `'cashier'`; rename the `BARTENDER_ACTIONS` constant to `CASHIER_ACTIONS` (keep its Set contents unchanged in this task — action-list pruning is deferred to 01-13); update the `MANAGER_ACTIONS` spread from `...BARTENDER_ACTIONS` to `...CASHIER_ACTIONS`; change the `ROLE_SET` record key from `bartender:` to `cashier:`. Do not remove any action from the STAFF_ACTIONS list or any *_ACTIONS set in this task.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "'cashier'" src/shared/lib/domain.ts src/shared/lib/rbac.ts | awk -F: '{s+=$2} END {exit (s>=3)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - `UserRoleSchema` and `UserRole.CASHIER` in domain.ts use `'cashier'`, not `'bartender'`
    - `rbac.ts` has no remaining `BARTENDER_ACTIONS` identifier — renamed to `CASHIER_ACTIONS`, referenced correctly in `MANAGER_ACTIONS` and `ROLE_SET`
    - `npx tsc --noEmit` passes with zero new errors
  </acceptance_criteria>
  <done>domain.ts and rbac.ts use cashier as the single source of truth; the repo still type-checks (other files referencing the old `BARTENDER_ACTIONS` name, if any, are caught here).</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Rename the DB user_role enum value and every live policy/function literal, push to the new project (D-16)</name>
  <precondition>supabase/config.toml's project_id is not shsrhxleopmovzpzqmex (Plan 01-01 completed) and the new project already holds the full baseline schema</precondition>
  <files>supabase/migrations/2026081000xx_rename_bartender_role_to_cashier.sql</files>
  <read_first>
    - supabase/migrations/20260414000001_enums.sql (`CREATE TYPE user_role AS ENUM ('bartender', 'manager', 'admin')` — original enum definition)
    - supabase/migrations/20260422000004_kds_core.sql line 2 (`ALTER TYPE user_role ADD VALUE IF NOT EXISTS 'kitchen'` — confirms 'kitchen' was added later, not part of the original three)
    - 01-RESEARCH.md "RBAC Role Rename Fallout" section (ASVS V4 threat: a stale RLS policy checking `role = 'bartender'` silently locks the renamed role out)
  </read_first>
  <action>
    Author a single new forward migration. First statement: `ALTER TYPE user_role RENAME VALUE 'bartender' TO 'cashier';` — this is atomic, cannot run inside the same transaction block as other DDL (Postgres restriction on enum renames), and automatically migrates every existing `profiles.role = 'bartender'` row's stored value along with it (enum values are stored by OID, not string). This does NOT fix embedded string literals inside policy/function bodies (`get_user_role() = 'bartender'` casts the literal at evaluation time and will raise "invalid input value for enum" once the label no longer exists). Query the LIVE new project (not migration file history — most historical bartender-referencing migrations are superseded) for every currently-active object still containing the literal: run `SELECT schemaname, tablename, policyname FROM pg_policies WHERE qual::text ILIKE '%bartender%' OR with_check::text ILIKE '%bartender%';` and `SELECT proname FROM pg_proc WHERE prosrc ILIKE '%bartender%' AND pronamespace = 'public'::regnamespace;` against the new project. For each policy found, use `ALTER POLICY &lt;name&gt; ON &lt;table&gt; USING (...) WITH CHECK (...)` with the literal replaced; for each function found, `CREATE OR REPLACE FUNCTION` with the literal replaced (including the `deplete_for_order_item` role-guard error message text, which currently reads "bartender or higher required" — update to "cashier or higher required"). Include a DOWN section per D-19's convention (rename value back, best-effort literal restoration noted as non-recoverable for any newly-cashier data). Before applying, enforce the project-id gate as a runnable command, not just the precondition note: run `grep -q 'project_id = "shsrhxleopmovzpzqmex"' supabase/config.toml &amp;&amp; { echo "REFUSING: linked to production bar project"; exit 1; }; supabase db push` to apply.
  </action>
  <verify>
    <automated>grep -q 'project_id = "shsrhxleopmovzpzqmex"' supabase/config.toml &amp;&amp; { echo "REFUSING: linked to production bar project"; exit 1; }; supabase db push --dry-run 2>&amp;1 | tail -5; SUPABASE_ACCESS_TOKEN="${SUPABASE_ACCESS_TOKEN:-}" psql "$(supabase status -o env 2>/dev/null | grep DB_URL | cut -d= -f2-)" -c "SELECT proname FROM pg_proc WHERE prosrc ILIKE '%bartender%' AND pronamespace='public'::regnamespace;" 2>/dev/null | grep -qv "0 rows" &amp;&amp; echo "STILL_HAS_LITERAL" || echo "CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` completes successfully against the new project (confirmed by config.toml project_id != shsrhxleopmovzpzqmex per the precondition)
    - `SELECT unnest(enum_range(NULL::user_role))` on the new project returns `cashier`, not `bartender`
    - No row is returned by `pg_policies`/`pg_proc` queries for `%bartender%` on the new project after the migration
    - Every existing `profiles` row that had `role = 'bartender'` now has `role = 'cashier'`
  </acceptance_criteria>
  <done>The DB's role enum, every RLS policy, and every function literal use cashier; the migration is pushed and live on the new project only.</done>
</task>

<task type="auto">
  <name>Task 3: Rename bartender in E2E auth helpers, seed scripts, and every spec call site (D-16, Pitfall 5)</name>
  <files>e2e/helpers/auth.ts, e2e/helpers/supabase.ts, scripts/setup-dev-users.ts</files>
  <read_first>
    - e2e/helpers/auth.ts (`StaffRole` type, `staffForRole()` branch)
    - e2e/helpers/supabase.ts line 613 (`role: 'bartender' | 'manager' | 'admin'` type)
    - scripts/setup-dev-users.ts (`ROLES` array with `role: 'bartender'`)
  </read_first>
  <action>
    In e2e/helpers/auth.ts, change `type StaffRole = 'bartender' | 'manager' | 'admin' | 'kitchen'` to use `'cashier'`, and `staffForRole()`'s `if (role === 'bartender')` branch condition to `if (role === 'cashier')` — per RESEARCH.md Pitfall 5 recommendation (b), keep reading the SAME env var names (`E2E_BARTENDER_NAME`/`E2E_BARTENDER_PIN`) inside that branch; only the role literal and type change, not the env var names (avoids a CI secret-store rename). Apply the same type/literal change to e2e/helpers/supabase.ts's `role: 'bartender' | 'manager' | 'admin'` type (line 613) and scripts/setup-dev-users.ts's `ROLES` array `role: 'bartender'` entries (env prefix stays `E2E_BARTENDER` per the same rationale). Then run a repo-wide sweep across every E2E spec file EXCEPT `e2e/15-home-navigation.spec.ts`: `grep -rl "'bartender'" e2e/*.spec.ts | grep -v '15-home-navigation.spec.ts' | xargs sed -i "s/'bartender'/'cashier'/g"` — this updates every `loginAs(page, 'bartender')` call site and any other bartender-role-literal usage across all matched spec files in one pass (some of these files will be deleted wholesale by later plans in this phase; updating them first is harmless). `e2e/15-home-navigation.spec.ts` is deliberately excluded here — Plan 01-04 (same wave, runs in parallel) also edits that exact file to add new removed-route-redirect tests; touching it from both plans in the same wave would be a same-wave file conflict, so 01-04's own edit handles that one file's `'bartender'` literal instead (see 01-04's Task 3).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p e2e/tsconfig.json 2>/dev/null || npx tsc --noEmit; ! grep -rl "'bartender'" e2e/*.spec.ts e2e/helpers/*.ts | grep -v '15-home-navigation.spec.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/helpers/auth.ts`'s `StaffRole` type and `staffForRole()` branch use `'cashier'`, while still reading `E2E_BARTENDER_NAME`/`E2E_BARTENDER_PIN` env vars
    - Zero remaining `'bartender'` string literals across `e2e/*.spec.ts` and `e2e/helpers/*.ts`, EXCEPT `e2e/15-home-navigation.spec.ts` (handled by Plan 01-04, same wave)
    - `scripts/setup-dev-users.ts`'s role literal is `'cashier'`
    - The E2E suite still type-checks (no spec references an invalid `StaffRole` value)
  </acceptance_criteria>
  <done>Every E2E spec except 15-home-navigation.spec.ts compiles cleanly calling loginAs(page, 'cashier'); env var names are unchanged so no CI secret update is required; 15-home-navigation.spec.ts is deliberately left to Plan 01-04 to avoid a same-wave file conflict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RLS policy evaluation | Every `get_user_role() = '&lt;role&gt;'` comparison inside a Postgres RLS policy is the actual enforcement boundary between an authenticated staff session and row access |
| DB enum vs. TS union | domain.ts's `UserRoleSchema` and the DB `user_role` enum must agree, or client-side validation and server-side RLS diverge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Elevation of Privilege | Stale RLS policy still checking `role = 'bartender'` after the enum rename | high | mitigate | Task 2 queries the LIVE `pg_policies`/`pg_proc` on the new project (not migration file history) for every remaining literal and rewrites each one in the same migration as the enum rename — no partial-rename window |
| T-01-06 | Denial of Service | `ALTER TYPE ... RENAME VALUE` run against the wrong project | critical | mitigate | Task 2's `&lt;precondition&gt;` re-asserts `project_id != shsrhxleopmovzpzqmex` before the push, matching Plan 01-01's gate |
| T-01-07 | Repudiation | Existing `profiles` rows silently reassigned role without an audit trail | low | accept | This is a schema-baseline rename on freshly-seeded new-project data (Plan 01-01 seeded dev users, no real production history exists on this project yet) — acceptable for a Phase 1 dev-environment migration |
</threat_model>

<verification>
`npx tsc --noEmit` passes after Task 1 and Task 3; Task 2's `pg_policies`/`pg_proc` sweep returns zero rows for `%bartender%` on the new project; `grep -rl "'bartender'" e2e/*.spec.ts e2e/helpers/*.ts` returns nothing.
</verification>

<success_criteria>
`cashier` is the single, consistent role name across DB enum, RLS policies, function bodies, TS types, RBAC constants, E2E helpers, and every E2E spec — no partial-rename gap remains anywhere.
</success_criteria>

<output>
Create `.planning/phases/01-strip-rebrand/01-03-SUMMARY.md` when done
</output>
