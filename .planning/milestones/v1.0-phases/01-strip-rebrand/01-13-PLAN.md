---
phase: 01-strip-rebrand
plan: 13
type: execute
wave: 4
depends_on: ["01-03", "01-05", "01-06", "01-07", "01-08", "01-09", "01-10", "01-11", "01-12"]
files_modified:
  - src/shared/lib/domain.ts
  - src/shared/lib/domain-helpers.ts
  - src/shared/lib/domain.test.ts
  - src/shared/lib/domain-helpers.test.ts
  - src/shared/lib/domain.table-type.test.ts
  - src/shared/ui/StatusBadge.tsx
  - src/shared/lib/mocks.ts
  - src/shared/lib/rbac.ts
  - src/shared/lib/i18n/locales/es-MX/**
  - src/shared/lib/i18n/locales/en-US/**
autonomous: true
requirements: []

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "domain.ts has zero remaining bar/pool-specific Zod schema (Kds/Resource/Combo/Recipe/Ingredient/ModifierInventoryRule/Prep/Waitlist/Promotion families) — every schema removed had zero remaining consumer outside its own now-deleted entity dir, confirmed by grep before deletion"
    - "TabSchema retains only generic fields — the pool/split-linkage fields (tableNumber, poolCharges, hasActivePoolSession, activePoolTableNumber, parentTabId, splitMode, splitLabel) are gone; rappiOrderId is deliberately retained (deferred per 01-05's documented rationale)"
    - "StatusBadge.tsx no longer imports ResourceStatusSchema or has a Pool table statuses config block, and still correctly renders TabStatus/OrderStatus/duration/inventory badges"
    - "rbac.ts's STAFF_ACTIONS list has zero orphaned action (transfer_tab, start_pool_timer, stop_pool_timer, view_kds, view_kds_bar, produce_prep_batch, manage_waitlist) — every entry left has a live consumer"
    - "npm run test (Vitest unit suite) passes with zero failures after the schema/helper pruning"
    - "The full retained E2E suite (auth, RBAC, caja, staff, base inventory, and every other kept spec) passes against the new Supabase project — ROADMAP Success Criterion 3 (no hidden SQL-side breakage)"
    - "npx playwright test e2e/15-home-navigation.spec.ts still passes, confirming the removed-route redirects still work after this final pass"
  artifacts:
    - path: "src/shared/lib/domain.ts"
      provides: "Domain schema file with zero bar/pool-specific schema remaining"
    - path: "src/shared/lib/rbac.ts"
      provides: "STAFF_ACTIONS with zero orphaned bar/pool action"
---

<objective>
Close out the strip: prune every remaining bar/pool-specific Zod schema and helper function from the single-source-of-truth `domain.ts`/`domain-helpers.ts` files (deferred until now because these are shared files every wave-3 feature-strip plan would otherwise collide on), prune the now-orphaned RBAC action entries (deferred for the same reason — removing them earlier would have broken the build while wave-3 plans still had consuming code in place), sweep the remaining orphaned i18n keys across all namespaces, and run the full retained E2E suite to prove ROADMAP Success Criteria 2 and 3.

Purpose: every wave-3 feature-strip plan has now deleted its entity/feature/widget/page code. What remains is the shared "single source of truth" files (`domain.ts` per CLAUDE.md, `rbac.ts`) that would have conflicted if 8 different plans tried to edit them in the same wave — this plan is that consolidation pass, run only once every consumer is confirmed gone.
Output: `domain.ts`/`domain-helpers.ts`/`rbac.ts` contain zero bar/pool-specific residue; the full E2E suite proves the strip introduced no hidden SQL-side breakage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-strip-rebrand/01-CONTEXT.md
@.planning/phases/01-strip-rebrand/01-RESEARCH.md
@.planning/phases/01-strip-rebrand/01-PATTERNS.md
@src/shared/lib/domain.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prune bar/pool-specific schemas from domain.ts and their ripple into domain-helpers.ts, StatusBadge.tsx, mocks.ts</name>
  <files>src/shared/lib/domain.ts, src/shared/lib/domain-helpers.ts, src/shared/lib/domain.test.ts, src/shared/lib/domain-helpers.test.ts, src/shared/lib/domain.table-type.test.ts, src/shared/ui/StatusBadge.tsx, src/shared/lib/mocks.ts</files>
  <read_first>
    - src/shared/lib/domain.ts (full file — schemas to remove, listed below, verified present this session at these approximate line numbers pre-strip: `KdsStatusSchema` ~90, `ResourceStatusSchema`/`ResourceTypeSchema` ~98-100, `PoolSessionSummarySchema` ~416, `ResourceSchema` ~571, `PoolTableTransferSchema` ~1158, `ComboSlotTypeSchema`/`ComboSlotSchema`/`ComboSlotOptionSchema`/`ComboAvailabilitySchema`/`AddComboToTabInputSchema` ~1617-1714, `IngredientSchema` ~1714, `RecipeItemSchema` ~1786, `ModifierInventoryRuleSchema` ~1857, `PrepProductionSchema`/`PrepProductionCreateSchema` ~1873-1896, `WaitlistEntryStatusSchema`/`WaitlistEntrySchema`/`WaitlistNotificationSchema` ~1896-1954, `ComboMixRowSchema`/`RecipeVarianceRowSchema`/`WaitlistMetricsRowSchema`/`ComboOverrideRowSchema` ~1954-1996, `PromotionDiscountTypeSchema`/`PromotionTargetTypeSchema`/`PromotionAvailabilitySchema`/`AppliedPromotionSchema` ~2069-2144 — exact line numbers will have shifted after wave-3 plans' own edits, locate by name)
    - src/shared/lib/domain-helpers.ts (has `calculatePoolCharge` — zero consumers outside itself/its test, confirmed this session; `calculateTabSubtotal(orders, poolCharges: PoolSessionSummary[])` takes a `poolCharges` param that must be removed along with its arithmetic contribution once `TabSchema.poolCharges` and `PoolSessionSummarySchema` are gone)
    - src/shared/ui/StatusBadge.tsx (imports `ResourceStatusSchema` for its prop type union; has a "Pool table statuses" block in `statusConfig` with `available`/`occupied`/`reserved`/`maintenance` entries — confirmed this session these 4 status values are pool-exclusive, not shared with TabStatus/OrderStatus)
    - src/shared/lib/mocks.ts (has `generateMockOccupiedPoolTable`, imports `ResourceSchema`/`Resource` type, an `openTables: Resource[]` field somewhere in a larger mock-data shape — confirm the surrounding shape before editing, since `mocks.ts` is shared test-support infra for KEPT tests too)
  </read_first>
  <action>
    In domain.ts, remove every schema listed in `&lt;read_first&gt;` above by name — for EACH one, first `grep -rn "&lt;SchemaName&gt;" src/` to confirm zero remaining consumer outside domain.ts itself (every wave-3 plan already deleted the entity dir that would have consumed it; if a consumer is unexpectedly still found, resolve that consumer first, do not delete the schema out from under live code). In `TabSchema`, remove the 7 fields `tableNumber`, `poolCharges`, `hasActivePoolSession`, `activePoolTableNumber`, `parentTabId`, `splitMode`, `splitLabel` — leave `rappiOrderId` in place (deliberately deferred, per 01-05's documented rationale: `isRappiTab` is permanently unreachable without it, and removing it would require a risky rewrite of the core retained `PaymentForm.tsx`). Explicitly do NOT remove `DiscountScopeSchema` (used by `edge-function-contracts.ts`'s payment discount contract — its `discountScope` field is optional/nullable and simply goes permanently unused once promotions' evaluate RPC is gone, same deferral rationale as `rappiOrderId`) or `ModifierSchema`/`ModifierGroupSchema`/`ModifierGroupItemSchema`/`ProductModifierGroupSchema`/`ModifierPopularityRowSchema` (all confirmed generic/kept). In domain-helpers.ts, remove `calculatePoolCharge` in full; update `calculateTabSubtotal`'s signature to drop the `poolCharges: PoolSessionSummary[]` parameter and remove its contribution to the returned total (confirm no caller still passes a second argument — fix forward on any `npx tsc --noEmit` error this produces). In StatusBadge.tsx, remove the `ResourceStatusSchema` import and its member in the `StatusBadgeProps.status` union type, and remove the "Pool table statuses" block (`available`/`occupied`/`reserved`/`maintenance` entries) from `statusConfig`. In mocks.ts, remove `generateMockOccupiedPoolTable`, the `ResourceSchema`/`Resource` type imports, and any mock-data field/shape that only existed to carry pool-table mock data (confirm via `npx tsc --noEmit` which callers break and fix forward — mocks.ts is shared test infra, other mock builders for Product/Category/Tab/Staff/Shift/Inventory must remain intact). Delete `src/shared/lib/domain.table-type.test.ts` in full (100% ResourceTypeSchema/ResourceSchema-specific, both removed). In `domain.test.ts`, remove only the `describe` blocks for `ModifierInventoryRuleSchema`, `PromotionDiscountTypeSchema`, `PromotionTargetTypeSchema`, `PromotionSchema / PromotionCreateSchema`, `AppliedPromotionSchema`, and `AuditActionSchema — promotion.apply` — leave every other describe block (`CategorySchema`, `ProductSchema`, `ModifierGroupSchema`, `ModifierPopularityRowSchema`, etc.) untouched. In `domain-helpers.test.ts`, remove the `calculatePoolCharge` describe block and update the `calculateTabSubtotal` describe block's test cases to match the new signature (no `poolCharges` argument).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm test -- --run 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Every listed schema is absent from `domain.ts`, confirmed by `grep -c` returning 0 for each schema name
    - `TabSchema` retains `rappiOrderId` and has none of the 7 removed pool/split fields
    - `DiscountScopeSchema` and the generic modifier schemas are unchanged, still present
    - `src/shared/lib/domain.table-type.test.ts` no longer exists
    - `npx tsc --noEmit` passes with zero errors
    - `npm test -- --run` passes with zero failures
  </acceptance_criteria>
  <done>domain.ts, domain-helpers.ts, and their ripple files (StatusBadge.tsx, mocks.ts, domain.test.ts, domain-helpers.test.ts) contain zero bar/pool-specific residue; the unit test suite proves nothing broke.</done>
</task>

<task type="auto">
  <name>Task 2: Prune orphaned RBAC actions and sweep remaining orphaned i18n keys across all namespaces (D-17, D-22)</name>
  <files>src/shared/lib/rbac.ts, src/shared/lib/i18n/locales/es-MX/, src/shared/lib/i18n/locales/en-US/</files>
  <read_first>
    - src/shared/lib/rbac.ts (post-01-03 state — `STAFF_ACTIONS` array still has `transfer_tab`, `start_pool_timer`, `stop_pool_timer`, `view_kds`, `view_kds_bar`, `produce_prep_batch`, `manage_waitlist`, deliberately left in place by 01-03 to avoid breaking the build while wave-3 plans still had consuming code)
  </read_first>
  <action>
    In rbac.ts, remove `transfer_tab`, `start_pool_timer`, `stop_pool_timer`, `view_kds`, `view_kds_bar`, `produce_prep_batch`, `manage_waitlist` from the `STAFF_ACTIONS` array and from whichever `*_ACTIONS` Set(s) currently contain them (`CASHIER_ACTIONS`, `MANAGER_EXTRA`, `KITCHEN_ACTIONS`, `ADMIN_EXTRA`) — first `grep -rn "'&lt;action&gt;'" src/` for each to confirm zero remaining call site (every consuming feature/page was deleted by a wave-3 plan; if a consumer is unexpectedly found, resolve it before removing the action). Keep every other action (`create_order`, `view_own_tabs`, `view_all_tabs`, `clock_in`, `clock_out`, `close_tab`, `void_order`, `view_reports`, `adjust_inventory`, `manage_products`, `manage_staff`, `manage_settings`, `delete_tab`, `view_all_shifts`, `manage_caja`, `process_refund`, `view_audit_log`, `edit_paid_tab`, `reopen_tab`) untouched. Then, across all 10 i18n namespace files in both `es-MX` and `en-US` locales, grep for any remaining key whose only historical consumer was code deleted anywhere in this phase (search by feature-name substrings: `pool`, `resource`, `kds`, `prep`, `waitlist`, `rappi`, `combo`, `recipe`, `ingredient`, `modifierInventoryRule`, `promotion`, `happyHour`, `tipSplit`, `transferTab`, `splitTab` — cross-reference each hit against the deleted directories from Plans 01-05 through 01-12 before removing; do NOT remove a key sharing a substring with a kept feature, e.g. `modifierPopularity`/`modifierGroup`/`tipDistribution`/`splitPayment`/`discountScope`/`refund` must survive).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; ! grep -q "'transfer_tab'\|'start_pool_timer'\|'stop_pool_timer'\|'view_kds'\|'view_kds_bar'\|'produce_prep_batch'\|'manage_waitlist'" src/shared/lib/rbac.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rbac.ts`'s `STAFF_ACTIONS` array and every `*_ACTIONS` set has none of the 7 orphaned action strings
    - `npx tsc --noEmit` passes with zero errors
    - No kept-feature i18n key (`modifierPopularity`, `modifierGroup`, `tipDistribution`, `splitPayment`, `discountScope`, `refund`) was accidentally removed — spot-checked
  </acceptance_criteria>
  <done>rbac.ts has zero orphaned bar/pool action; i18n locale files have no dangling key for deleted features.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full retained E2E suite against the new Supabase project (ROADMAP Success Criteria 2 &amp; 3)</name>
  <precondition>supabase/config.toml's project_id is not shsrhxleopmovzpzqmex and every wave-3 SQL migration has been pushed (Plans 01-05 through 01-12 completed)</precondition>
  <files>none — verification only</files>
  <read_first>
    - CLAUDE.md's "E2E Test Suite" list (61 spec files, cross-reference against what this phase deleted to confirm the expected remaining count)
  </read_first>
  <action>
    Run the full retained Playwright E2E suite (`npx playwright test`) against the dev server pointed at the new Supabase project (headless, per CLAUDE.md's testing policy — no manual verification). This includes the infra/build specs (`01-ci`, `11-offline`, `12-infrastructure`, `13-tauri-build`, `18-updater`) that D-13 explicitly left untouched throughout this phase — confirm they still pass unmodified, proving D-13's "not tied to the bar/pool domain" judgment held. This proves ROADMAP Success Criterion 2 (login/RBAC/staff clock-in/out are unaffected by the strip) and Success Criterion 3 (existing E2E coverage for retained infrastructure — auth, RBAC, caja, staff, base inventory — still passes, confirming no hidden SQL-side breakage from removing bar/pool code). If any spec fails, diagnose whether it is a genuine regression from this phase's edits (fix forward) or a pre-existing flake unrelated to the strip (note in the SUMMARY, do not silently mark the phase complete over an unexplained failure).
  </action>
  <verify>
    <automated>npx playwright test 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test` completes with zero failing specs (skips for genuinely environment-gated tests, e.g. `13-tauri-build.spec.ts`, are acceptable per CLAUDE.md's existing scoping)
    - `e2e/15-home-navigation.spec.ts`'s removed-route-redirect assertions (added in 01-04) pass
    - `e2e/02-caja.spec.ts`/`19-caja-entries.spec.ts`/`23-caja-entries.spec.ts` (caja close, dependent on 01-12's close_caja_session rewrite) pass
    - `e2e/09-rbac.spec.ts` (dependent on 01-03's role rename) passes
  </acceptance_criteria>
  <done>The full retained E2E suite passes against the new Supabase project — the phase's exit criteria (ROADMAP Success Criteria 1-3) are proven by automated evidence, not a manual click-through.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Single-source-of-truth files | `domain.ts` and `rbac.ts` are read by every layer of the app (client validation, RBAC gating) — an incorrect prune here has the widest possible blast radius of any file in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-28 | Tampering | Removing a schema/action still referenced by a not-yet-discovered consumer, silently breaking a retained feature | high | mitigate | Task 1/2's action mandates a `grep -rn` check before EACH removal, and both tasks gate on `npx tsc --noEmit` — TypeScript's strict mode makes a missed reference a hard compile error, not a silent runtime gap |
| T-01-29 | Elevation of Privilege | Orphaned RBAC action strings left in `STAFF_ACTIONS` after their consuming route is gone are dead weight, not a security hole (no code path can grant an action nothing checks) — but confirming their removal closes any doubt | low | mitigate | Task 2 explicitly removes them once verified orphaned |
| T-01-30 | Repudiation | A genuine E2E regression from this phase's edits is silently accepted as "pre-existing flake" | high | mitigate | Task 3's action explicitly requires diagnosing failure cause before treating any failure as acceptable, and the SUMMARY must document the reasoning |
</threat_model>

<verification>
`npx tsc --noEmit` and `npm test -- --run` pass after Task 1-2's pruning; `npx playwright test` passes in full against the new Supabase project after Task 3, including the removed-route-redirect and caja-close specs.
</verification>

<success_criteria>
domain.ts, domain-helpers.ts, and rbac.ts are free of bar/pool-specific residue; the full retained E2E suite proves the strip introduced zero hidden SQL-side breakage — the phase is complete and verified by automated evidence.
</success_criteria>

<output>
Create `.planning/phases/01-strip-rebrand/01-13-SUMMARY.md` when done
</output>
