---
phase: 01-strip-rebrand
plan: 05
type: execute
wave: 3
depends_on: ["01-04"]
files_modified:
  - src/entities/rappi-order/**
  - src/widgets/RappiOrdersPanel/**
  - src/pages/rappi/**
  - supabase/functions/rappi-sync-menu/**
  - supabase/functions/rappi-webhook/**
  - supabase/migrations/2026081001xx_drop_rappi_orders.sql
  - e2e/25-rappi-orders.spec.ts
autonomous: true
requirements: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No file under src/entities/rappi-order/, src/widgets/RappiOrdersPanel/, src/pages/rappi/, supabase/functions/rappi-sync-menu/, or supabase/functions/rappi-webhook/ exists"
    - "The rappi_orders table and its supabase_realtime publication membership no longer exist on the new Supabase project"
    - "The rest of the retained E2E suite still passes after the Rappi strip (no import errors, no orphaned reference)"
  artifacts:
    - path: "supabase/migrations/2026081001xx_drop_rappi_orders.sql"
      provides: "ALTER PUBLICATION supabase_realtime DROP TABLE rappi_orders; DROP TABLE rappi_orders CASCADE; plus any rappi_orders-referencing function/trigger"
---

<objective>
Remove the Rappi delivery integration entirely — entity, widget, page, both edge functions, and its DB table + Realtime publication membership — per D-09/Phase Boundary ("Rappi delivery... entirely out of scope for grocery").

Purpose: the /rappi route and its nav tile were already severed in 01-04; this plan physically deletes the code and data those pointed at.
Output: no file, table, or edge function related to Rappi delivery remains.

Scope note (Claude's Discretion, documented not silently dropped): `PaymentMethodSchema`'s `'rappi'` enum value, `PaymentForm.tsx`'s `isRappiTab`/rappi-branch logic (30+ references across a core, retained payment widget), and `CajaReportPanel`'s "Rappi Sales" breakdown row are deliberately NOT touched by this plan. `isRappiTab = Boolean(tab.rappiOrderId)` — once this plan deletes the entity/page that ever set `rappiOrderId` on a tab, that condition is permanently false, so the Rappi payment button is unreachable dead code without needing to rewrite `PaymentForm.tsx` (1000+ lines of payment logic STATE.md flags as must-stay-stable for Phase 2 to build checkout on). `07-reports.spec.ts`'s "Revenue breakdown shows cash, card, rappi" test and `23-payment-edge-cases.spec.ts`'s PE5 Rappi-payment test both continue to pass unmodified (the UI elements they assert on still render, unaffected by this plan). Revisit in a later phase if full removal of the payment-method value is desired.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-strip-rebrand/01-CONTEXT.md
@.planning/phases/01-strip-rebrand/01-RESEARCH.md
@.planning/phases/01-strip-rebrand/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Rappi entity, widget, page, edge functions, and its E2E spec (D-09)</name>
  <files>src/entities/rappi-order/, src/widgets/RappiOrdersPanel/, src/pages/rappi/, supabase/functions/rappi-sync-menu/, supabase/functions/rappi-webhook/, e2e/25-rappi-orders.spec.ts</files>
  <read_first>
    - src/entities/rappi-order/ directory listing (confirm no file outside this dir imports from it, aside from providers.tsx's `useRappiOrdersRealtimeBridge` import already removed in 01-04, and PaymentForm.tsx's `tab.rappiOrderId` field reference which reads the TabSchema field directly, not this entity module)
  </read_first>
  <action>
    Delete the directories `src/entities/rappi-order/`, `src/widgets/RappiOrdersPanel/`, `src/pages/rappi/`, `supabase/functions/rappi-sync-menu/`, `supabase/functions/rappi-webhook/` in full (`git rm -r`). Delete `e2e/25-rappi-orders.spec.ts` per D-11 (delete, not archive — git history preserves it if ever needed). Grep the remaining `src/` tree for `rappi-order` import specifiers (`@entities/rappi-order`) to confirm zero remaining consumers post-01-04's providers.tsx edit — none are expected. Grep the i18n locale files for a `rappiOrders`-namespaced key block (likely in `wPanels.json`/`entities.json`) and remove it per D-22, since its only consumer (`RappiOrdersPanel`) is now deleted.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; test ! -d src/entities/rappi-order &amp;&amp; test ! -d src/widgets/RappiOrdersPanel &amp;&amp; test ! -d src/pages/rappi &amp;&amp; test ! -d supabase/functions/rappi-sync-menu &amp;&amp; test ! -d supabase/functions/rappi-webhook &amp;&amp; test ! -f e2e/25-rappi-orders.spec.ts &amp;&amp; ! grep -rq "@entities/rappi-order" src/</automated>
  </verify>
  <acceptance_criteria>
    - None of the 5 listed directories exist
    - `e2e/25-rappi-orders.spec.ts` no longer exists
    - Zero remaining `@entities/rappi-order` import specifiers anywhere in `src/`
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>Every Rappi-specific code file and edge function is deleted; the codebase type-checks with no dangling import.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Drop rappi_orders table and its Realtime publication membership, push to the new project (D-09)</name>
  <precondition>supabase/config.toml's project_id is not shsrhxleopmovzpzqmex (Plan 01-01 completed)</precondition>
  <files>supabase/migrations/2026081001xx_drop_rappi_orders.sql</files>
  <read_first>
    - supabase/migrations/20260417100000_rappi_orders.sql (table creation; line 39 adds `rappi_orders` to the `supabase_realtime` publication)
  </read_first>
  <action>
    Author a new forward migration following the repo's existing DROP-migration convention (analog: `supabase/migrations/20260711000001_drop_happy_hour_columns.sql`). First: `ALTER PUBLICATION supabase_realtime DROP TABLE IF EXISTS rappi_orders;` — must run before the table drop. Then `DROP TABLE IF EXISTS rappi_orders CASCADE;`. Grep the live new project (`pg_proc`/`pg_trigger` WHERE source/definition ILIKE '%rappi_orders%') for any function or trigger referencing the table beyond what CASCADE already removes, and drop those explicitly first if CASCADE alone would leave an orphaned definition. Include a DOWN section per D-19 recreating the table shape only (no data restoration, matching the repo convention). Before applying, enforce the project-id gate as a runnable command, not just the precondition note: run `grep -q 'project_id = "shsrhxleopmovzpzqmex"' supabase/config.toml &amp;&amp; { echo "REFUSING: linked to production bar project"; exit 1; }; supabase db push`.
  </action>
  <verify>
    <automated>grep -q 'project_id = "shsrhxleopmovzpzqmex"' supabase/config.toml &amp;&amp; { echo "REFUSING: linked to production bar project"; exit 1; }; supabase db push 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` completes successfully against the new project
    - `rappi_orders` no longer appears in `information_schema.tables` on the new project
    - `rappi_orders` no longer appears in `pg_publication_tables` for the `supabase_realtime` publication
  </acceptance_criteria>
  <done>rappi_orders and its Realtime membership are gone from the new project's schema; no other retained table/function/trigger references it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase Realtime publication | `rappi_orders`'s publication membership controlled which clients received live inserts/updates for delivery orders |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Denial of Service | `supabase db push` against the wrong project | critical | mitigate | Task 2's `&lt;precondition&gt;` re-asserts project separation before the push, matching every other SQL-pushing plan in this phase |
| T-01-11 | Information Disclosure | `rappi-webhook` edge function accepted unauthenticated inbound webhook payloads from Rappi's platform | n/a — removed | mitigate | The entire edge function is deleted in Task 1; no residual attack surface |
</threat_model>

<verification>
`npx tsc --noEmit` passes; `supabase db push` succeeds; `rappi_orders` is absent from both `information_schema.tables` and the `supabase_realtime` publication on the new project.
</verification>

<success_criteria>
Rappi delivery integration is fully removed at the code and SQL level; the deliberately-deferred payment-method cleanup is documented, not silently dropped.
</success_criteria>

<output>
Create `.planning/phases/01-strip-rebrand/01-05-SUMMARY.md` when done
</output>
