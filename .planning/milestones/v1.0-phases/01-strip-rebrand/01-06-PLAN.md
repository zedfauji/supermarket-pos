---
phase: 01-strip-rebrand
plan: 06
type: execute
wave: 3
depends_on: ["01-04"]
files_modified:
  - src/entities/resource/**
  - src/features/assign-pool-session-to-tab/**
  - src/features/start-pool-timer/**
  - src/features/stop-pool-timer/**
  - src/features/stop-and-move-table/**
  - src/features/edit-session-start-time/**
  - src/widgets/PoolTableGrid/**
  - src/widgets/PoolTableOccupancyPanel/**
  - src/widgets/TableStatusPanel/**
  - src/pages/pool-tables/**
  - src/pages/pool-table-status/**
  - src/widgets/SettingsTabsPanel/tabs/PoolTablesSettingsTab.tsx
  - supabase/migrations/2026081002xx_drop_pool_resources.sql
  - e2e/04-pool-timer.spec.ts
  - e2e/21-carom-billing.spec.ts
  - e2e/24-pool-advanced.spec.ts
  - e2e/22-sprint3-billing.spec.ts
  - e2e/24-sprint5-pool-accuracy.spec.ts
  - e2e/23-payment-edge-cases.spec.ts
autonomous: true
requirements: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No file under src/entities/resource/, the 5 pool feature dirs, the 3 pool widget dirs, or the 2 pool page dirs exists"
    - "The resources and pool_sessions tables, and every RPC/trigger operating on them, no longer exist on the new Supabase project"
    - "23-payment-edge-cases.spec.ts no longer has PE6/PE7 tests that depend on constructing a tab with a running pool session"
    - "24-sprint5-pool-accuracy.spec.ts, 04-pool-timer.spec.ts, 21-carom-billing.spec.ts, 24-pool-advanced.spec.ts, 22-sprint3-billing.spec.ts no longer exist"
  artifacts:
    - path: "supabase/migrations/2026081002xx_drop_pool_resources.sql"
      provides: "DROP of resources, pool_sessions tables and their RPCs/triggers, ALTER PUBLICATION DROP TABLE resources"
---

<objective>
Remove the pool table / billiards session domain entirely — the `resource` entity (renamed from `pool_tables` in an earlier phase, 100% pool-table domain despite its generic name per RESEARCH.md's verified finding), its 5 lifecycle features, 3 widgets, 2 pages, the Settings tab, and its DB tables/RPCs/triggers.

Purpose: pool tables have no meaning for a grocery store (Phase Boundary). The `/pool-tables` route, its nav tile, and the `PoolRealtimeListener` mount were already severed in 01-04; this plan deletes the underlying code and data.
Output: no pool-table code, widget, page, or Settings tab remains; `resources`/`pool_sessions` and their Realtime membership are dropped from the new project.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-strip-rebrand/01-CONTEXT.md
@.planning/phases/01-strip-rebrand/01-RESEARCH.md
@.planning/phases/01-strip-rebrand/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete pool entity/features/widgets/pages/settings-tab, trim mixed E2E specs, delete pure-pool E2E specs (D-09)</name>
  <files>src/entities/resource/, src/features/assign-pool-session-to-tab/, src/features/start-pool-timer/, src/features/stop-pool-timer/, src/features/stop-and-move-table/, src/features/edit-session-start-time/, src/widgets/PoolTableGrid/, src/widgets/PoolTableOccupancyPanel/, src/widgets/TableStatusPanel/, src/pages/pool-tables/, src/pages/pool-table-status/, src/widgets/SettingsTabsPanel/tabs/PoolTablesSettingsTab.tsx, e2e/04-pool-timer.spec.ts, e2e/21-carom-billing.spec.ts, e2e/24-pool-advanced.spec.ts, e2e/22-sprint3-billing.spec.ts, e2e/24-sprint5-pool-accuracy.spec.ts, e2e/23-payment-edge-cases.spec.ts</files>
  <read_first>
    - src/entities/resource/ directory listing (`usePoolTimer.ts`, `ResourceIllustration.tsx`, `ResourceCard.tsx`)
    - e2e/23-payment-edge-cases.spec.ts (PE6 "tab with only pool charge" and PE7 "paying tab with running pool session — error shown" tests — both construct pool-session-linked tabs that can no longer exist once this task removes pool session creation)
  </read_first>
  <action>
    Delete the directories `src/entities/resource/`, `src/features/assign-pool-session-to-tab/`, `src/features/start-pool-timer/`, `src/features/stop-pool-timer/`, `src/features/stop-and-move-table/`, `src/features/edit-session-start-time/`, `src/widgets/PoolTableGrid/`, `src/widgets/PoolTableOccupancyPanel/`, `src/widgets/TableStatusPanel/`, `src/pages/pool-tables/`, `src/pages/pool-table-status/` in full (`git rm -r`). Delete the single file `src/widgets/SettingsTabsPanel/tabs/PoolTablesSettingsTab.tsx` (its registration was already removed from `SettingsTabsPanel/index.tsx` in 01-04). Delete (per D-11 — delete, not archive) the fully pool-domain E2E spec files: `e2e/04-pool-timer.spec.ts`, `e2e/21-carom-billing.spec.ts`, `e2e/24-pool-advanced.spec.ts`, `e2e/22-sprint3-billing.spec.ts`, `e2e/24-sprint5-pool-accuracy.spec.ts` (this last one confirmed by direct read this session — all 8 tests exercise Edit Start Time / pool session mechanics, not flagged in RESEARCH.md's original list but verified 100% pool-domain). In `e2e/23-payment-edge-cases.spec.ts`, remove only the `PE6` and `PE7` `test()` blocks (the file is otherwise generic — do not delete the whole file, matching RESEARCH.md Pitfall 2's "mixed file" guidance). Grep the i18n locale files for pool/resource-namespaced keys (likely in `wPanels.json`/`entities.json`/`settings.json`) and remove entries whose only consumer was code deleted in this task, per D-22.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; test ! -d src/entities/resource &amp;&amp; test ! -d src/widgets/TableStatusPanel &amp;&amp; test ! -f e2e/04-pool-timer.spec.ts &amp;&amp; test ! -f e2e/24-sprint5-pool-accuracy.spec.ts &amp;&amp; ! grep -q "PE6:\|PE7:" e2e/23-payment-edge-cases.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - None of the listed entity/feature/widget/page directories or the Settings tab file exist
    - The 5 pure-pool E2E spec files no longer exist
    - `e2e/23-payment-edge-cases.spec.ts` still exists but has no `PE6:` or `PE7:` test title
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>Every pool-table code file, widget, page, and Settings tab is deleted; mixed-content E2E files are trimmed, not wholesale-deleted, matching D-12's methodology.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Drop resources and pool_sessions tables/RPCs/triggers, push to the new project (D-09)</name>
  <precondition>supabase/config.toml's project_id is not shsrhxleopmovzpzqmex (Plan 01-01 completed)</precondition>
  <files>supabase/migrations/2026081002xx_drop_pool_resources.sql</files>
  <read_first>
    - supabase/migrations/20260414000005_pool_tables.sql, 20260728000001_rename_pool_tables_to_resources.sql (line 113-115 adds `resources` to `supabase_realtime` publication), 20260728000002_resources_is_temp_floating.sql, 20260728000003_deactivate_floating_resource_trigger.sql, 20260807000001_pool_session_atomic_rpcs.sql, 20260710000006_stop_pool_session_rpc.sql, 20260713000002_fix_transfer_pool_session_version_bump.sql (full list of pool/resources migrations to reverse-engineer from, per 01-RESEARCH.md's SQL removal table)
  </read_first>
  <action>
    Author a new forward migration following the repo's DROP-migration convention (analog: `supabase/migrations/20260711000001_drop_happy_hour_columns.sql`). First `ALTER PUBLICATION supabase_realtime DROP TABLE IF EXISTS resources;`. Then query the live new project (`pg_proc` WHERE `prosrc ILIKE '%resources%' OR prosrc ILIKE '%pool_session%'` and `pronamespace = 'public'::regnamespace`) to enumerate every pool-specific function (session start/stop/transfer/atomic RPCs) and `DROP FUNCTION IF EXISTS ... CASCADE` each; drop the `deactivate_floating_resource` trigger and its function; `DROP TABLE IF EXISTS pool_sessions CASCADE;` then `DROP TABLE IF EXISTS resources CASCADE;` (sessions reference the resource table via FK — drop child before parent, or rely on CASCADE from the resources drop and confirm no orphaned pool_sessions rows remain referenced elsewhere). `evaluate_promotions_pool_grant` (from the Promotions migration group, `20260710000005`) also references pool sessions — confirm via `pg_proc` query whether it still exists on the new project after Plan 01-10 (Promotions strip) has run; if 01-10 has not yet run when this task executes, note in the SUMMARY that a residual reference may need a follow-up DROP once both plans have landed (both plans push independently — this is expected transient state within the same wave, not a defect). Include a DOWN section per D-19. Before applying, enforce the project-id gate as a runnable command, not just the precondition note: run `grep -q 'project_id = "shsrhxleopmovzpzqmex"' supabase/config.toml &amp;&amp; { echo "REFUSING: linked to production bar project"; exit 1; }; supabase db push`.
  </action>
  <verify>
    <automated>grep -q 'project_id = "shsrhxleopmovzpzqmex"' supabase/config.toml &amp;&amp; { echo "REFUSING: linked to production bar project"; exit 1; }; supabase db push 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` completes successfully against the new project
    - `resources` and `pool_sessions` no longer appear in `information_schema.tables` on the new project
    - `resources` no longer appears in `pg_publication_tables` for `supabase_realtime`
    - No `pg_proc` function with `prosrc ILIKE '%pool_session%'` remains in the `public` schema
  </acceptance_criteria>
  <done>resources and pool_sessions, their RPCs, triggers, and Realtime membership are gone from the new project.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase Realtime publication | `resources`'s publication membership drove live pool-table occupancy updates across terminals |
| Manager PIN gate | `edit-session-start-time` required a manager PIN — its removal eliminates that specific gated action, not a general RBAC gap |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Denial of Service | `supabase db push` against the wrong project | critical | mitigate | Task 2's `&lt;precondition&gt;` re-asserts project separation before the push |
| T-01-13 | Tampering | Cross-plan dependency: `evaluate_promotions_pool_grant` (authored by the Promotions migration group) references pool session RPCs this plan drops | medium | mitigate | Task 2's action explicitly checks for this residual reference via a live `pg_proc` query and documents the expected transient state if Plan 01-10 hasn't landed yet in the same wave |
</threat_model>

<verification>
`npx tsc --noEmit` passes; `supabase db push` succeeds; `resources`/`pool_sessions` are absent from `information_schema.tables` and the `supabase_realtime` publication on the new project.
</verification>

<success_criteria>
Pool-table domain is fully removed at the code and SQL level; mixed E2E files are surgically trimmed, not deleted wholesale, per D-12's methodology.
</success_criteria>

<output>
Create `.planning/phases/01-strip-rebrand/01-06-SUMMARY.md` when done
</output>
