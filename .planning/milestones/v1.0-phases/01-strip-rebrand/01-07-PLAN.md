---
phase: 01-strip-rebrand
plan: 07
type: execute
wave: 3
depends_on: ["01-04"]
files_modified:
  - src/entities/kds/**
  - src/entities/prep/**
  - src/features/bump-kds-item/**
  - src/features/produce-prep-batch/**
  - src/widgets/KdsBoard/**
  - src/widgets/KitchenPrepDashboard/**
  - src/pages/kds/**
  - src/pages/kds-bar/**
  - src/pages/kitchen-prep/**
  - supabase/migrations/2026081003xx_drop_kds_and_prep.sql
  - e2e/28-kds.spec.ts
  - e2e/40-kds-bar.spec.ts
  - e2e/21-prep.spec.ts
  - e2e/18-modifier-notes-kds.spec.ts
autonomous: true
requirements: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No file under src/entities/kds/, src/entities/prep/, the 2 KDS/prep feature dirs, the 2 KDS/prep widget dirs, or the 3 kds/kds-bar/kitchen-prep page dirs exists"
    - "The kds_enabled setting, prep_productions table + trigger, KDS-core tables/RPCs, and caja_open_prep_batch RPCs no longer exist on the new Supabase project"
    - "18-modifier-notes-kds.spec.ts still exists and still passes its T1/T2 modifier-sheet tests — only its T3 KDS-card test is removed (the file is mixed-domain, not pure-KDS)"
  artifacts:
    - path: "supabase/migrations/2026081003xx_drop_kds_and_prep.sql"
      provides: "DROP of KDS-core tables/RPCs, kds_enabled setting removal, prep_productions table+trigger drop, caja_open_prep_batch RPC drop"
---

<objective>
Remove the Kitchen Display System (KDS/KDS-bar boards) and batch chef prep production domain — 2 entities, 2 features, 2 widgets, 3 pages, and their DB tables/RPCs/triggers.

Purpose: neither KDS boards nor batch prep production apply to a grocery store (Phase Boundary). The `/kds`, `/kds-bar`, `/kitchen-prep` routes and nav tiles were already severed in 01-04; this plan deletes the underlying code and data.
Output: no KDS or prep code, widget, or page remains; the corresponding DB objects are dropped from the new project.

Correction to RESEARCH.md's E2E classification (verified by direct read this session): `e2e/18-modifier-notes-kds.spec.ts` is NOT a pure-KDS spec despite its filename — it has 4 tests, and only T3 ("KDS card shows modifier name and note") touches KDS. T1/T2 test the generic modifier-sheet + notes flow (retained, part of `add-item-to-tab`) and T4 is already skipped. This plan trims only T3, matching D-12's mixed-file methodology rather than deleting the whole file as RESEARCH.md's original inventory assumed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-strip-rebrand/01-CONTEXT.md
@.planning/phases/01-strip-rebrand/01-RESEARCH.md
@.planning/phases/01-strip-rebrand/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete KDS/prep entities/features/widgets/pages, trim mixed spec, delete pure KDS/prep specs (D-Phase Boundary)</name>
  <files>src/entities/kds/, src/entities/prep/, src/features/bump-kds-item/, src/features/produce-prep-batch/, src/widgets/KdsBoard/, src/widgets/KitchenPrepDashboard/, src/pages/kds/, src/pages/kds-bar/, src/pages/kitchen-prep/, e2e/28-kds.spec.ts, e2e/40-kds-bar.spec.ts, e2e/21-prep.spec.ts, e2e/18-modifier-notes-kds.spec.ts</files>
  <read_first>
    - src/entities/kds/ and src/entities/prep/ directory listings
    - e2e/18-modifier-notes-kds.spec.ts (confirm T3's exact block boundaries — "T3: KDS card shows modifier name and note for a seeded food order" — before removing)
  </read_first>
  <action>
    Delete the directories `src/entities/kds/`, `src/entities/prep/`, `src/features/bump-kds-item/`, `src/features/produce-prep-batch/`, `src/widgets/KdsBoard/`, `src/widgets/KitchenPrepDashboard/`, `src/pages/kds/`, `src/pages/kds-bar/`, `src/pages/kitchen-prep/` in full (`git rm -r`). Delete (per D-11 — delete, not archive) the fully KDS/prep-domain E2E spec files `e2e/28-kds.spec.ts`, `e2e/40-kds-bar.spec.ts`, `e2e/21-prep.spec.ts`. In `e2e/18-modifier-notes-kds.spec.ts`, remove only the `T3` `test()` block ("KDS card shows modifier name and note for a seeded food order") — leave T1, T2, T4 and the file's `test.describe` wrapper untouched. Grep i18n locale files for `kds`/`prep`-namespaced keys and remove entries whose only consumer was code deleted in this task, per D-22.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; test ! -d src/entities/kds &amp;&amp; test ! -d src/entities/prep &amp;&amp; test ! -f e2e/28-kds.spec.ts &amp;&amp; test ! -f e2e/21-prep.spec.ts &amp;&amp; test -f e2e/18-modifier-notes-kds.spec.ts &amp;&amp; ! grep -q "T3: KDS card" e2e/18-modifier-notes-kds.spec.ts &amp;&amp; grep -q "T1: modifier sheet opens" e2e/18-modifier-notes-kds.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - None of the listed entity/feature/widget/page directories exist
    - `e2e/28-kds.spec.ts`, `e2e/40-kds-bar.spec.ts`, `e2e/21-prep.spec.ts` no longer exist
    - `e2e/18-modifier-notes-kds.spec.ts` still exists, still has its T1/T2 tests, and no longer has T3
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>KDS and prep code is fully deleted; the mixed modifier-notes-kds spec keeps its generic tests and loses only the KDS-specific one.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Drop KDS-core, kds_enabled setting, prep_productions, and caja_open_prep_batch RPCs, push to the new project</name>
  <precondition>supabase/config.toml's project_id is not shsrhxleopmovzpzqmex (Plan 01-01 completed)</precondition>
  <files>supabase/migrations/2026081003xx_drop_kds_and_prep.sql</files>
  <read_first>
    - supabase/migrations/20260422000004_kds_core.sql, 20260422000006_kds_enabled_setting.sql (KDS-core tables/RPCs, `kds_enabled` settings key)
    - supabase/migrations/20260429000001_prep_productions_table.sql, 20260429000002_recipes_prep_extension.sql, 20260429000003_prep_productions_trigger.sql, 20260703000003_caja_open_prep_batch_rpcs.sql (prep production tables/trigger/RPCs)
  </read_first>
  <action>
    Author a new forward migration following the repo's DROP-migration convention (analog: `supabase/migrations/20260711000001_drop_happy_hour_columns.sql`). Query the live new project's `pg_proc`/`pg_trigger`/`information_schema.tables` for everything KDS-core created (`20260422000004_kds_core.sql`'s table set + RPCs) and drop each with `DROP FUNCTION IF EXISTS ... CASCADE` / `DROP TABLE IF EXISTS ... CASCADE`; delete the `kds_enabled` row from the `settings` table (`DELETE FROM settings WHERE key = 'kds_enabled';`) rather than a schema drop, since `settings` itself is a generic retained table. Drop the `prep_productions` table and its trigger (`DROP TRIGGER IF EXISTS ... ON prep_productions;` before `DROP TABLE IF EXISTS prep_productions CASCADE;`), and the two `caja_open_prep_batch` RPCs from `20260703000003`. Note `20260429000002_recipes_prep_extension.sql` extends the `recipes` table (owned by Plan 01-09's combos/recipes strip, not this plan) — only drop the prep-specific extension columns/logic this migration added if they are exclusive to prep, cross-checking against Plan 01-09's scope to avoid dropping something 01-09 also expects to find; if ambiguous, leave the `recipes` table itself untouched here and note the extension column for 01-09 to confirm in its own SUMMARY. Include a DOWN section per D-19. Before applying, enforce the project-id gate as a runnable command, not just the precondition note: run `grep -q 'project_id = "shsrhxleopmovzpzqmex"' supabase/config.toml &amp;&amp; { echo "REFUSING: linked to production bar project"; exit 1; }; supabase db push`.
  </action>
  <verify>
    <automated>grep -q 'project_id = "shsrhxleopmovzpzqmex"' supabase/config.toml &amp;&amp; { echo "REFUSING: linked to production bar project"; exit 1; }; supabase db push 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` completes successfully against the new project
    - No table from the KDS-core migration remains in `information_schema.tables`
    - `settings` has no row with `key = 'kds_enabled'`
    - `prep_productions` no longer appears in `information_schema.tables`
    - The two `caja_open_prep_batch` RPC functions no longer exist in `pg_proc`
  </acceptance_criteria>
  <done>KDS-core, kds_enabled, prep_productions, and caja_open_prep_batch RPCs are gone from the new project.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `kitchen` role RLS policies | Several RLS policies gate on `get_user_role() = 'kitchen'` for KDS access — removing the KDS tables does not remove the `kitchen` role itself (still used by `produce_prep_batch`... no, that action is deferred-pruned in 01-13; the `kitchen` role remains a valid StaffRole regardless, unaffected by this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-14 | Denial of Service | `supabase db push` against the wrong project | critical | mitigate | Task 2's `&lt;precondition&gt;` re-asserts project separation before the push |
| T-01-15 | Tampering | `recipes_prep_extension` migration modified the shared `recipes` table, owned by a different wave-3 plan (01-09) | medium | mitigate | Task 2's action explicitly flags the cross-plan boundary and defers the `recipes` table itself to 01-09, only removing prep-exclusive extension logic here |
</threat_model>

<verification>
`npx tsc --noEmit` passes; `supabase db push` succeeds; KDS-core tables, `kds_enabled`, `prep_productions`, and `caja_open_prep_batch` RPCs are absent from the new project.
</verification>

<success_criteria>
KDS and prep production are fully removed at the code and SQL level; the mixed modifier-notes-kds spec correctly retains its generic coverage.
</success_criteria>

<output>
Create `.planning/phases/01-strip-rebrand/01-07-SUMMARY.md` when done
</output>
