---
phase: 01-strip-rebrand
plan: 10
type: execute
wave: 3
depends_on: ["01-04"]
files_modified:
  - src/entities/promotion/**
  - src/features/manage-promotions/**
  - src/widgets/OrderPanel/HappyHourBanner.tsx
  - src/widgets/OrderPanel/HappyHourBanner.test.tsx
  - src/widgets/OrderPanel/ProductGrid.tsx
  - supabase/migrations/2026081006xx_drop_promotions.sql
  - e2e/43-promotions.spec.ts
autonomous: true
requirements: []

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No file under src/entities/promotion/ or src/features/manage-promotions/ exists"
    - "promotions, applied_promotions tables and evaluate_promotions_rpc/is_promotion_available_fn no longer exist on the new Supabase project"
    - "ProductGrid.tsx (part of the retained, Phase-2-critical OrderPanel widget) no longer imports useActivePromotions or renders HappyHourBanner"
  artifacts:
    - path: "supabase/migrations/2026081006xx_drop_promotions.sql"
      provides: "DROP of promotions, applied_promotions tables and their RPCs"
---

<objective>
Remove the promotions/happy-hour discount engine entirely — 1 entity, 1 feature, its live consumer inside a retained widget, and its DB schema.

Purpose: the bar-pos promotions engine (percentage/fixed-amount/fixed-price discounts targeting items, categories, or pool billing/grants) has no evidence of grocery need and is explicitly out of scope per REQUIREMENTS.md's "Loyalty/rewards program, coupon/promotion engine" exclusion. Its Settings-tab registration and SettingsTabsPanel import were already severed in 01-04.

**Hidden dependency found by direct code read this session:** `src/widgets/OrderPanel/ProductGrid.tsx` — part of the retained, Phase-2-critical `OrderPanel` widget (the product grid `add-item-to-tab` uses) — calls `useActivePromotions()` and renders `&lt;HappyHourBanner activePromotions={...} /&gt;`. This is a real, live consumer inside kept code, not inside the deleted `src/entities/promotion/` or `src/features/manage-promotions/` directories, so it must be handled explicitly in this plan or `npx tsc --noEmit` fails after the entity is deleted.
Output: no promotions code, DB object, or dangling reference in retained widgets remains.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-strip-rebrand/01-CONTEXT.md
@.planning/phases/01-strip-rebrand/01-RESEARCH.md
@.planning/phases/01-strip-rebrand/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete promotions entity/feature, remove its consumer in ProductGrid.tsx, delete its E2E spec</name>
  <files>src/entities/promotion/, src/features/manage-promotions/, src/widgets/OrderPanel/HappyHourBanner.tsx, src/widgets/OrderPanel/HappyHourBanner.test.tsx, src/widgets/OrderPanel/ProductGrid.tsx, e2e/43-promotions.spec.ts</files>
  <read_first>
    - src/entities/promotion/ directory listing
    - src/widgets/OrderPanel/ProductGrid.tsx (line ~123: `const { data: activePromotions } = useActivePromotions();`; line ~180: `&lt;HappyHourBanner activePromotions={activePromotions ?? []} /&gt;`)
  </read_first>
  <action>
    Delete the directories `src/entities/promotion/` and `src/features/manage-promotions/` in full (`git rm -r`). In `src/widgets/OrderPanel/ProductGrid.tsx`, remove the `useActivePromotions` import and its call, and remove the `&lt;HappyHourBanner ... /&gt;` JSX line — this widget is retained (Phase-2-critical `add-item-to-tab` product grid) and must compile without any `@entities/promotion` reference. Delete `src/widgets/OrderPanel/HappyHourBanner.tsx` and `src/widgets/OrderPanel/HappyHourBanner.test.tsx` as files. Delete `e2e/43-promotions.spec.ts` per D-11 (delete, not archive). Grep the remaining `src/` tree for `@entities/promotion` import specifiers to confirm zero remaining consumers after this task — none expected. Grep i18n locale files for `promotion`/`happyHour`-namespaced keys and remove entries whose only consumer was code deleted in this task, per D-22.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; test ! -d src/entities/promotion &amp;&amp; test ! -d src/features/manage-promotions &amp;&amp; test ! -f src/widgets/OrderPanel/HappyHourBanner.tsx &amp;&amp; test ! -f e2e/43-promotions.spec.ts &amp;&amp; ! grep -rq "@entities/promotion\|useActivePromotions\|HappyHourBanner" src/</automated>
  </verify>
  <acceptance_criteria>
    - Neither `src/entities/promotion/` nor `src/features/manage-promotions/` exists
    - `src/widgets/OrderPanel/HappyHourBanner.tsx`/`.test.tsx` no longer exist
    - `ProductGrid.tsx` has no reference to `useActivePromotions` or `HappyHourBanner`
    - `e2e/43-promotions.spec.ts` no longer exists
    - Zero remaining `@entities/promotion` import specifiers anywhere in `src/`
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>Every promotions-specific code file is deleted, including its live consumer inside the retained OrderPanel widget; the codebase type-checks with no dangling import.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Drop promotions, applied_promotions, and their RPCs, push to the new project</name>
  <precondition>supabase/config.toml's project_id is not shsrhxleopmovzpzqmex (Plan 01-01 completed)</precondition>
  <files>supabase/migrations/2026081006xx_drop_promotions.sql</files>
  <read_first>
    - supabase/migrations/20260710000001_promotions_schema.sql, 20260710000002_is_promotion_available_fn.sql, 20260710000003_applied_promotions_table.sql, 20260710000004_evaluate_promotions_rpc.sql (the promotions schema/RPCs to drop — do NOT touch 20260710000005_evaluate_promotions_pool_grant.sql or 20260710000006_stop_pool_session_rpc.sql, both of which modify Pool-domain RPCs owned by Plan 01-06, not this plan; whichever plan's migration lands second on the new project naturally supersedes the other's residual reference since Pool's tables/RPCs are dropped wholesale by 01-06 regardless of grant history)
    - supabase/migrations/20260710000007_migrate_happy_hour_data.sql, 20260711000001_drop_happy_hour_columns.sql (already-historical happy_hour migration/cleanup that promotions replaced — no further action needed, these are already applied as part of the baseline)
  </read_first>
  <action>
    Author a new forward migration following the repo's DROP-migration convention (analog: `supabase/migrations/20260711000001_drop_happy_hour_columns.sql`). Drop `evaluate_promotions_rpc` and `is_promotion_available_fn` via `DROP FUNCTION IF EXISTS ... CASCADE`, then `DROP TABLE IF EXISTS applied_promotions CASCADE;` then `DROP TABLE IF EXISTS promotions CASCADE;`. Do not attempt to drop or modify `stop_pool_session` or any pool-session RPC here — that function is dropped wholesale by Plan 01-06 regardless of what this plan's migration history shows. Include a DOWN section per D-19. Before applying, enforce the project-id gate as a runnable command, not just the precondition note: run `grep -q 'project_id = "shsrhxleopmovzpzqmex"' supabase/config.toml &amp;&amp; { echo "REFUSING: linked to production bar project"; exit 1; }; supabase db push`.
  </action>
  <verify>
    <automated>grep -q 'project_id = "shsrhxleopmovzpzqmex"' supabase/config.toml &amp;&amp; { echo "REFUSING: linked to production bar project"; exit 1; }; supabase db push 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` completes successfully against the new project
    - `promotions` and `applied_promotions` no longer appear in `information_schema.tables` on the new project
    - `evaluate_promotions_rpc` and `is_promotion_available_fn` no longer exist in `pg_proc`
  </acceptance_criteria>
  <done>Promotions schema and RPCs are gone from the new project.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `evaluate_promotions_rpc` (SECURITY DEFINER) | Computed discount amounts server-side at checkout time — removal eliminates that computation path entirely, no replacement needed since promotions are out of scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-21 | Denial of Service | `supabase db push` against the wrong project | critical | mitigate | Task 2's `&lt;precondition&gt;` re-asserts project separation before the push |
| T-01-22 | Tampering | Cross-plan overlap: `20260710000005/6` migrations touch Pool-domain RPCs owned by Plan 01-06 | low | accept | Both plans' migrations independently converge on the same end state (the pool RPCs are gone) regardless of push order within the wave; no coordination is required since neither plan modifies the other's owned schema |
</threat_model>

<verification>
`npx tsc --noEmit` passes; `supabase db push` succeeds; `promotions`/`applied_promotions` and their RPCs are absent from the new project.
</verification>

<success_criteria>
Promotions engine is fully removed at the code and SQL level.
</success_criteria>

<output>
Create `.planning/phases/01-strip-rebrand/01-10-SUMMARY.md` when done
</output>