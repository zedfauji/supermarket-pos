---
phase: 01-strip-rebrand
plan: 08
type: execute
wave: 3
depends_on: ["01-04"]
files_modified:
  - src/entities/waitlist/**
  - src/features/add-waitlist-entry/**
  - src/features/notify-waitlist/**
  - src/features/seat-waitlist-party/**
  - src/features/mark-waitlist-entry-cancelled/**
  - src/features/mark-waitlist-no-show/**
  - src/widgets/WaitlistQueue/**
  - src/widgets/WaitlistAnalyticsReport/**
  - src/pages/waitlist/**
  - supabase/functions/send-waitlist-notification/**
  - supabase/migrations/2026081004xx_drop_waitlist.sql
  - e2e/24-waitlist.spec.ts
autonomous: true
requirements: []

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No file under src/entities/waitlist/, the 5 waitlist feature dirs, the 2 waitlist widget dirs, src/pages/waitlist/, or supabase/functions/send-waitlist-notification/ exists"
    - "waitlist_entries, waitlist_notifications, and the notify trigger no longer exist on the new Supabase project"
  artifacts:
    - path: "supabase/migrations/2026081004xx_drop_waitlist.sql"
      provides: "DROP of waitlist_entries, waitlist_notifications tables and their notify trigger"
---

<objective>
Remove the walk-in waitlist queue domain entirely — entity, 5 lifecycle features, 2 widgets, page, the WhatsApp-notification edge function, and its DB tables/trigger.

Purpose: a walk-in waitlist has no equivalent for a grocery direct-sale checkout (Phase Boundary). The `/waitlist` route, nav tile, `WaitlistRealtimeListener` mount, and its ReportsPage analytics tab were already severed in 01-04; this plan deletes the underlying code and data.
Output: no waitlist code, widget, page, or edge function remains; `waitlist_entries`/`waitlist_notifications` and their notify trigger are dropped from the new project.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-strip-rebrand/01-CONTEXT.md
@.planning/phases/01-strip-rebrand/01-RESEARCH.md
@.planning/phases/01-strip-rebrand/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete waitlist entity/features/widgets/page/edge-function and its E2E spec (D-Phase Boundary)</name>
  <files>src/entities/waitlist/, src/features/add-waitlist-entry/, src/features/notify-waitlist/, src/features/seat-waitlist-party/, src/features/mark-waitlist-entry-cancelled/, src/features/mark-waitlist-no-show/, src/widgets/WaitlistQueue/, src/widgets/WaitlistAnalyticsReport/, src/pages/waitlist/, supabase/functions/send-waitlist-notification/, e2e/24-waitlist.spec.ts</files>
  <read_first>
    - src/entities/waitlist/ directory listing (confirm `useWaitlistWaitingCount` export, already de-referenced from HomeDashboard.tsx in 01-04)
  </read_first>
  <action>
    Delete the directories `src/entities/waitlist/`, `src/features/add-waitlist-entry/`, `src/features/notify-waitlist/`, `src/features/seat-waitlist-party/`, `src/features/mark-waitlist-entry-cancelled/`, `src/features/mark-waitlist-no-show/`, `src/widgets/WaitlistQueue/`, `src/widgets/WaitlistAnalyticsReport/`, `src/pages/waitlist/`, `supabase/functions/send-waitlist-notification/` in full (`git rm -r`). Delete `e2e/24-waitlist.spec.ts` per D-11 (delete, not archive). Grep the remaining `src/` tree for `@entities/waitlist` import specifiers to confirm zero remaining consumers post-01-04's HomeDashboard edit — none expected. Grep i18n locale files for `waitlist`-namespaced keys and remove entries whose only consumer was code deleted in this task, per D-22.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; test ! -d src/entities/waitlist &amp;&amp; test ! -d src/widgets/WaitlistQueue &amp;&amp; test ! -f e2e/24-waitlist.spec.ts &amp;&amp; ! grep -rq "@entities/waitlist" src/</automated>
  </verify>
  <acceptance_criteria>
    - None of the listed entity/feature/widget/page/edge-function directories exist
    - `e2e/24-waitlist.spec.ts` no longer exists
    - Zero remaining `@entities/waitlist` import specifiers anywhere in `src/`
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>Every waitlist-specific code file and edge function is deleted; the codebase type-checks with no dangling import.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Drop waitlist_entries, waitlist_notifications, and the notify trigger, push to the new project</name>
  <precondition>supabase/config.toml's project_id is not shsrhxleopmovzpzqmex (Plan 01-01 completed)</precondition>
  <files>supabase/migrations/2026081004xx_drop_waitlist.sql</files>
  <read_first>
    - supabase/migrations/20260501000001_waitlist_entries.sql through 20260501000004_waitlist_trigger_url.sql (table creation, notifications table, notify trigger, trigger URL config)
  </read_first>
  <action>
    Author a new forward migration following the repo's DROP-migration convention (analog: `supabase/migrations/20260711000001_drop_happy_hour_columns.sql`). Drop the notify trigger first (`DROP TRIGGER IF EXISTS ... ON waitlist_entries;`), then its underlying function, then `DROP TABLE IF EXISTS waitlist_notifications CASCADE;` and `DROP TABLE IF EXISTS waitlist_entries CASCADE;` (drop the referencing table before or via CASCADE from the referenced one — confirm FK direction via the live schema before ordering). Confirm via a live `pg_publication_tables` query on the new project whether `waitlist_entries` was ever added to `supabase_realtime` outside a tracked migration file (RESEARCH.md Assumption A4 — the app had a dedicated `WaitlistRealtimeListener.tsx`, suggesting it likely subscribed via the trigger's notify mechanism rather than table-level publication membership; if the live query shows it IS in the publication, add the `ALTER PUBLICATION supabase_realtime DROP TABLE IF EXISTS waitlist_entries;` statement before the table drop). Include a DOWN section per D-19. Before applying, enforce the project-id gate as a runnable command, not just the precondition note: run `grep -q 'project_id = "shsrhxleopmovzpzqmex"' supabase/config.toml &amp;&amp; { echo "REFUSING: linked to production bar project"; exit 1; }; supabase db push`.
  </action>
  <verify>
    <automated>grep -q 'project_id = "shsrhxleopmovzpzqmex"' supabase/config.toml &amp;&amp; { echo "REFUSING: linked to production bar project"; exit 1; }; supabase db push 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` completes successfully against the new project
    - `waitlist_entries` and `waitlist_notifications` no longer appear in `information_schema.tables` on the new project
    - No trigger referencing `waitlist_entries` remains in `pg_trigger`
  </acceptance_criteria>
  <done>waitlist_entries, waitlist_notifications, and the notify trigger are gone from the new project.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Waitlist notify trigger -> edge function | The notify trigger fired an HTTP call to `send-waitlist-notification` for WhatsApp delivery — a webhook-style outbound integration |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-16 | Denial of Service | `supabase db push` against the wrong project | critical | mitigate | Task 2's `&lt;precondition&gt;` re-asserts project separation before the push |
| T-01-17 | Information Disclosure | `send-waitlist-notification` edge function held WhatsApp API credentials for outbound messaging | n/a — removed | mitigate | The entire edge function is deleted in Task 1; no residual credential surface |
</threat_model>

<verification>
`npx tsc --noEmit` passes; `supabase db push` succeeds; `waitlist_entries`/`waitlist_notifications` and the notify trigger are absent from the new project.
</verification>

<success_criteria>
Waitlist queue domain is fully removed at the code and SQL level.
</success_criteria>

<output>
Create `.planning/phases/01-strip-rebrand/01-08-SUMMARY.md` when done
</output>
