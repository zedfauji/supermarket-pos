---
phase: 01-strip-rebrand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/config.toml
  - .env
  - .env.local
autonomous: false
requirements: []
user_setup:
  - service: supabase
    why: "D-06 requires a brand-new, separate Supabase project for the supermarket pivot — the currently-linked project (shsrhxleopmovzpzqmex) is live production data for an operating bar (D-05) and must never receive a DROP migration."
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens (or `supabase login` interactively)"
    dashboard_config:
      - task: "Confirm target Supabase organization has capacity for a new project (or designate an existing empty project's ref + db password)"
        location: "Supabase Dashboard -> Organization"

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The app's local Supabase config (supabase/config.toml project_id) and .env/.env.local values point at a project that is NOT shsrhxleopmovzpzqmex"
    - "The new Supabase project has the full 144-file migration history applied (identical baseline schema to the current linked project, before any DROP migration)"
    - "A user can PIN-login and reach /home against the new project (auth/RBAC/staff infra is live on the new project)"
  artifacts:
    - path: "supabase/config.toml"
      provides: "project_id repointed away from shsrhxleopmovzpzqmex"
    - path: ".env.local"
      provides: "SUPABASE_URL / SUPABASE_ANON_KEY (or equivalent) values for the new project"
  key_links:
    - from: "supabase/config.toml"
      to: "new Supabase project"
      via: "supabase link --project-ref <new-ref>"
      pattern: "project_id = "
---

<objective>
Provision a brand-new, separate Supabase project for the supermarket pivot (D-05/D-06), replay the full existing 144-file migration history onto it to establish baseline schema, repoint the app's local Supabase config/env at the new project, and prove the pipeline works end-to-end (PIN login + role-gated navigation + caja live) before any bar/pool DROP migration is authored against it. This is the phase's tracer: the thinnest path touching every layer the phase modifies (Supabase project infra -> local config -> DB schema -> running app -> automated test).

Purpose: D-05 identifies the currently-linked project (`shsrhxleopmovzpzqmex`) as live production data for an operating bar; a DROP migration against it is a one-way, unrecoverable data-loss event. Every other plan in this phase depends on a safe, separate target existing before it authors or pushes any SQL. This plan establishes the migration convention every wave-3 plan follows: per D-18, every bar/pool removal in this phase is a NEW forward DROP migration (never a squashed/rewritten history), applied only against this new project; per D-20, each feature group (pool/resources, KDS, waitlist, rappi, combos/recipes/ingredients/modifier-rules, promotions, tab-linkage, tip-distribution) gets its own migration file rather than one combined migration, so a future E2E regression can be bisected to the exact removal that caused it.
Output: A new Supabase project, linked locally, with baseline schema live and the app connecting to it — verified by a real Playwright run, not a manual check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-strip-rebrand/01-CONTEXT.md
@.planning/phases/01-strip-rebrand/01-RESEARCH.md
@supabase/config.toml
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Obtain Supabase credentials for the new project (D-06)</name>
  <what-built>Nothing yet — this gathers the one credential Claude cannot obtain on its own.</what-built>
  <action>Authenticate the Supabase CLI and designate the new project.</action>
  <instructions>
    Per D-06, provide ONE of:
    1. Run `supabase login` interactively in a terminal Claude does not have access to, OR set `SUPABASE_ACCESS_TOKEN` (Supabase Dashboard -> Account -> Access Tokens) as an environment variable Claude's shell can read.
    2. Confirm the target Supabase organization ID Claude should create the new project under (`supabase projects create <name> --org-id <org> --db-password <pw> --region <region>`), OR provide the ref + db password of an existing EMPTY Supabase project reserved for this pivot.
    3. Confirm the new project's name (suggested: a name distinct from any existing bar-pos project, e.g. `supermarket-pos`).
  </instructions>
  <verification>Claude confirms `SUPABASE_ACCESS_TOKEN` is readable in its shell environment (or `supabase login`'s local session is active) and an org-id/project ref has been provided, before proceeding to Task 2.</verification>
  <resume-signal>Reply with the access token/org-id (or existing empty project ref+password) once available, or type "done" once `supabase login` has been run in your own terminal.</resume-signal>
</task>

<task type="tracer">
  <name>Task: Provision new Supabase project, push baseline schema, repoint config, prove the app boots on it</name>
  <precondition>SUPABASE_ACCESS_TOKEN is set, or `supabase login` has been completed (checkpoint above resolved)</precondition>
  <files>supabase/config.toml, .env, .env.local</files>
  <read_first>
    - supabase/config.toml (current project_id = shsrhxleopmovzpzqmex — the linked live bar project, D-05)
    - .env.local (current SUPABASE_URL / SUPABASE_ANON_KEY / SUPABASE_SERVICE_ROLE_KEY values, if present)
    - 01-RESEARCH.md "SQL-Side Removal Approach" section (exact provisioning sequence, D-06)
  </read_first>
  <action>
    Per D-06's locked sequence: (1) run `supabase projects create &lt;name&gt; --org-id &lt;org&gt; --db-password &lt;pw&gt; --region &lt;region&gt;` (or reuse the human-designated existing empty project from the checkpoint above), capturing the new project ref. (2) Run `supabase link --project-ref &lt;new-ref&gt;` from the repo root — this rewrites `supabase/config.toml`'s `project_id` field. Verify the file no longer reads `project_id = "shsrhxleopmovzpzqmex"` before continuing (hard stop if it still does — the link failed). (3) Run `supabase db push` to replay the full 144-file migration history onto the new project, establishing baseline schema (every bar/pool table exists at this point — the DROP migrations in later plans remove them, not this one). (4) Update `.env`/`.env.local` (and any Tauri-side runtime config reading Supabase URL/keys) to the new project's `SUPABASE_URL`, `SUPABASE_ANON_KEY` values (from `supabase projects api-keys --project-ref &lt;new-ref&gt;` or the Dashboard) — the app and Playwright E2E suite must read the new project from this point forward. (5) Set the new project's E2E seed users: run `npm run setup:dev` against the new project to create the four PIN-login accounts (admin/manager/bartender/kitchen — role rename to cashier happens in 01-03, not here) so the smoke test below has accounts to log in with.
  </action>
  <verify>
    <automated>grep -c 'project_id = "shsrhxleopmovzpzqmex"' supabase/config.toml | grep -qx 0 && npx playwright test e2e/01-ci.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/config.toml`'s `project_id` is a value other than `shsrhxleopmovzpzqmex`
    - `supabase db push` completed with the full existing migration set applied (no errors) against the new project
    - `.env.local` (or equivalent runtime config) points the app's Supabase client at the new project's URL/anon key
    - `npx playwright test e2e/01-ci.spec.ts` passes against the dev server pointed at the new project (proves PIN login + basic navigation work end-to-end on the new baseline)
  </acceptance_criteria>
  <done>New Supabase project exists, holds the full pre-strip baseline schema, the app connects to it exclusively, and a real Playwright run confirms login works — all other Phase 1 plans can now safely push DROP migrations against this project.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local CLI -> Supabase project | `supabase db push`/`supabase link` execute destructive DDL against whichever project `supabase/config.toml` currently names |
| Developer env -> app runtime | `.env`/`.env.local` values determine which Supabase project the running app and E2E suite talk to |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Denial of Service / Repudiation | `supabase db push` against the wrong project | critical | mitigate | Task 2's `<verify>` hard-gates on `project_id != shsrhxleopmovzpzqmex` before any push is considered successful; the checkpoint before it ensures Claude never guesses at credentials for the live bar project |
| T-01-02 | Information Disclosure | `SUPABASE_ACCESS_TOKEN` / db password in shell history or committed files | high | mitigate | Token is supplied via environment variable only, never written to a tracked file; `.env`/`.env.local` are already gitignored per existing repo convention |
| T-01-03 | Tampering | Stale `.env` still pointing at the old project after `config.toml` is repointed | medium | mitigate | Task 2 step 4 explicitly updates `.env`/`.env.local` in the same task as the `supabase link`, verified transitively by the Playwright smoke test only passing if the app can actually reach the new project |
</threat_model>

<verification>
`supabase/config.toml`'s `project_id` differs from `shsrhxleopmovzpzqmex`; `supabase db push` output shows the full migration set applied with zero errors; `npx playwright test e2e/01-ci.spec.ts` passes against the new project.
</verification>

<success_criteria>
A new, separate Supabase project holds the complete pre-strip baseline schema, the app and E2E suite are repointed at it exclusively, and a real automated test proves login/navigation works — unblocking every subsequent Phase 1 plan's SQL work.
</success_criteria>

<output>
Create `.planning/phases/01-strip-rebrand/01-01-SUMMARY.md` when done
</output>
