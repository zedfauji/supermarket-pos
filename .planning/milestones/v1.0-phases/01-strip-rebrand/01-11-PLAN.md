---
phase: 01-strip-rebrand
plan: 11
type: execute
wave: 3
depends_on: ["01-04"]
files_modified:
  - src/features/split-tab/**
  - src/features/transfer-tab/**
  - src/pages/pos/**
  - supabase/migrations/2026081007xx_drop_split_transfer_tab_linkage.sql
  - e2e/03-tab-order.spec.ts
  - e2e/06-transfer.spec.ts
  - e2e/29-panel-toggle.spec.ts
autonomous: true
requirements: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No file under src/features/split-tab/, src/features/transfer-tab/, or src/pages/pos/ exists"
    - "tabs.parent_tab_id, tabs.split_mode, tabs.split_label columns, the split_tab_by_item/person/amount RPCs, and tab_transfers table no longer exist on the new Supabase project"
    - "refunds, refund_items tables and payments.is_refund/refund_id columns are UNCHANGED — process-refund is kept per D-09 despite living in the same historical migration file as the split-tab schema"
    - "split-payment (multi-tender cash+card in one sale) is UNCHANGED — it is a distinct feature from split-tab and is kept per D-09"
  artifacts:
    - path: "supabase/migrations/2026081007xx_drop_split_transfer_tab_linkage.sql"
      provides: "DROP of tabs.parent_tab_id/split_mode/split_label columns, split_tab RPCs, tab_transfers table and transfer_tab RPC"
---

<objective>
Remove the split-tab (splitting a shared bar tab by seat/item) and transfer-tab (pure pool-table concept) features entirely, and delete the now-orphaned `/pos` tab-based checkout page — per D-07 (no stub, nothing links to `/pos` until Phase 2) and D-09 (strip split-tab and transfer-tab, keep split-payment and process-refund).

Purpose: neither splitting a shared tab by seat nor transferring a tab between tables has a grocery equivalent. `/pos`'s route and nav tile were already severed in 01-04; this plan deletes its page directory. Critical distinction from two similarly-named KEPT features (D-09): **split-payment** (multi-tender cash+card in one checkout, `process-payment`/`PaymentForm.tsx`) is untouched — do not confuse it with split-**tab**. **process-refund** is untouched — its `refunds`/`refund_items` schema was authored in the SAME historical migration file as split-tab's schema (`20260427000001_split_bill_schema.sql`) and must be surgically separated, not dropped wholesale.
Output: no split-tab or transfer-tab code remains; the `/pos` page directory is gone; the `tabs` table's pool/split linkage columns are dropped while `refunds`/`refund_items`/`payments.is_refund`/`payments.refund_id` survive untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-strip-rebrand/01-CONTEXT.md
@.planning/phases/01-strip-rebrand/01-RESEARCH.md
@.planning/phases/01-strip-rebrand/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete split-tab/transfer-tab features and the /pos page, trim/delete their E2E specs (D-07, D-09)</name>
  <files>src/features/split-tab/, src/features/transfer-tab/, src/pages/pos/, e2e/03-tab-order.spec.ts, e2e/06-transfer.spec.ts, e2e/29-panel-toggle.spec.ts</files>
  <read_first>
    - src/features/split-tab/ directory listing (`useSplitTab.ts`, `SplitTabSheet.tsx`, `split-tab-rpc.integration.test.ts`)
    - src/features/transfer-tab/ directory listing (`useTransferTab.ts`, `useTransferPoolSession.ts`, `TransferTabDialog.tsx`, `TransferPoolDialog.tsx` — the whole folder goes per D-09 regardless of whether an individual file is pool-session-specific or generic-tab-transfer-specific)
    - src/pages/pos/index.tsx (already unreachable since 01-04 removed its route)
  </read_first>
  <action>
    Delete the directories `src/features/split-tab/`, `src/features/transfer-tab/`, `src/pages/pos/` in full (`git rm -r`). Delete (per D-11 — delete, not archive) `e2e/03-tab-order.spec.ts`, `e2e/06-transfer.spec.ts`, `e2e/29-panel-toggle.spec.ts` (D-12 — all three drive their flow through the now-deleted tab-based `/pos` checkout page). Do NOT delete `e2e/41-split-payment.spec.ts` or `e2e/35-refund.spec.ts` — both test the KEPT split-payment and process-refund features respectively, unaffected by this plan. Grep i18n locale files for `splitTab`/`transferTab`-namespaced keys and remove entries whose only consumer was code deleted in this task, per D-22 — do NOT remove `splitPayment`/`refund`-namespaced keys.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; test ! -d src/features/split-tab &amp;&amp; test ! -d src/features/transfer-tab &amp;&amp; test ! -d src/pages/pos &amp;&amp; test ! -f e2e/03-tab-order.spec.ts &amp;&amp; test ! -f e2e/06-transfer.spec.ts &amp;&amp; test ! -f e2e/29-panel-toggle.spec.ts &amp;&amp; test -f e2e/41-split-payment.spec.ts &amp;&amp; test -f e2e/35-refund.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - None of `src/features/split-tab/`, `src/features/transfer-tab/`, `src/pages/pos/` exist
    - `e2e/03-tab-order.spec.ts`, `e2e/06-transfer.spec.ts`, `e2e/29-panel-toggle.spec.ts` no longer exist
    - `e2e/41-split-payment.spec.ts` and `e2e/35-refund.spec.ts` still exist, unmodified
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>Split-tab and transfer-tab code is fully deleted; the orphaned /pos page is gone; split-payment and refund E2E coverage is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Drop tab split/transfer linkage columns and RPCs — surgically preserve refunds schema, push to the new project (D-08, D-09)</name>
  <precondition>supabase/config.toml's project_id is not shsrhxleopmovzpzqmex (Plan 01-01 completed)</precondition>
  <files>supabase/migrations/2026081007xx_drop_split_transfer_tab_linkage.sql</files>
  <read_first>
    - supabase/migrations/20260427000000_tab_status_split_enum.sql (`ALTER TYPE tab_status ADD VALUE 'split'` — Postgres has no direct DROP VALUE for enums; leave this value in the type as permanently-unreachable dead weight rather than recreating the whole `tab_status` type, a disproportionate-risk operation for one unused label)
    - supabase/migrations/20260427000001_split_bill_schema.sql sections 1-2 (lines 1-36: `tabs.parent_tab_id`/`split_mode`/`split_label` columns + `closed_at_requires_closed_status` CHECK constraint fix — DROP these) vs. sections 3-6+ (lines 38+: `refunds`/`refund_items` tables, `payments.is_refund`/`refund_id` columns, `amount_positive` CHECK fix — KEEP these, confirmed by direct read this session to be the process-refund feature's schema bundled into the same historical file)
    - supabase/migrations/20260427000002_split_tab_rpcs.sql, 20260708000001_fix_split_tab_rpcs_version_bump.sql (split_tab_by_item/person/amount RPCs to drop)
    - supabase/migrations/20260420000003_transfers.sql (`tab_transfers` table — audit trail exclusively for transfer-tab, drop), 20260713000001_fix_transfer_tab_version_bump.sql (transfer_tab RPC fix, drop the RPC this modified)
  </read_first>
  <action>
    Author a new forward migration following the repo's DROP-migration convention (analog: `supabase/migrations/20260711000001_drop_happy_hour_columns.sql`). Drop the `transfer_tab` RPC (`DROP FUNCTION IF EXISTS transfer_tab(...) CASCADE;` — confirm exact signature via a live `pg_proc` query for `proname = 'transfer_tab'` on the new project) and `DROP TABLE IF EXISTS tab_transfers CASCADE;`. Drop the `split_tab_by_item`/`split_tab_by_person`/`split_tab_by_amount` RPCs (confirm exact names/signatures via `pg_proc` WHERE `proname ILIKE 'split_tab%'`). Then, restore `closed_at_requires_closed_status` to its pre-split form: `ALTER TABLE tabs DROP CONSTRAINT IF EXISTS closed_at_requires_closed_status; ALTER TABLE tabs ADD CONSTRAINT closed_at_requires_closed_status CHECK ((closed_at IS NULL AND status = 'open') OR (closed_at IS NOT NULL AND status IN ('closed', 'paid', 'voided')));` (matches the pre-`20260427000001` shape — verify against the live constraint definition before writing, since later migrations may have layered on top). Then `ALTER TABLE tabs DROP COLUMN IF EXISTS parent_tab_id, DROP COLUMN IF EXISTS split_mode, DROP COLUMN IF EXISTS split_label;`. Do NOT touch `refunds`, `refund_items`, `payments.is_refund`, or `payments.refund_id` — confirmed KEPT (process-refund, D-09). Do NOT attempt to remove `'split'` from the `tab_status` enum. Include a DOWN section per D-19 (recreate columns/constraint/tables only, no data restoration). Before applying, enforce the project-id gate as a runnable command, not just the precondition note: run `grep -q 'project_id = "shsrhxleopmovzpzqmex"' supabase/config.toml &amp;&amp; { echo "REFUSING: linked to production bar project"; exit 1; }; supabase db push`.
  </action>
  <verify>
    <automated>grep -q 'project_id = "shsrhxleopmovzpzqmex"' supabase/config.toml &amp;&amp; { echo "REFUSING: linked to production bar project"; exit 1; }; supabase db push 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` completes successfully against the new project
    - `tabs` no longer has `parent_tab_id`, `split_mode`, or `split_label` columns
    - `tab_transfers` no longer appears in `information_schema.tables`
    - No `pg_proc` function named `transfer_tab` or `split_tab_by_*` remains
    - `refunds`, `refund_items` tables and `payments.is_refund`/`payments.refund_id` columns STILL exist unchanged
  </acceptance_criteria>
  <done>Split-tab and transfer-tab linkage is gone from the tabs schema; refunds/split-payment schema is verified untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `tabs.closed_at_requires_closed_status` CHECK | Enforces a data-integrity invariant tying `closed_at` to `status`; must be correctly restored to its pre-split shape, not left silently permissive or overly strict |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-23 | Denial of Service | `supabase db push` against the wrong project | critical | mitigate | Task 2's `&lt;precondition&gt;` re-asserts project separation before the push |
| T-01-24 | Tampering | Migration accidentally drops `refunds`/`refund_items`/`payments.is_refund`/`payments.refund_id`, bundled in the same historical file as the schema this plan removes | high | mitigate | Task 2's action explicitly cites the line-range split within `20260427000001_split_bill_schema.sql` and the acceptance criteria assert the refund schema still exists post-push |
| T-01-25 | Repudiation | Restoring `closed_at_requires_closed_status` to the wrong shape could silently permit an invalid closed_at/status combination | medium | mitigate | Task 2's action specifies the exact pre-split CHECK expression and instructs verifying against the live constraint definition before writing, not assuming the file's original text still matches |
</threat_model>

<verification>
`npx tsc --noEmit` passes; `supabase db push` succeeds; tab split/transfer columns, RPCs, and `tab_transfers` are gone; refunds/split-payment schema is confirmed unchanged.
</verification>

<success_criteria>
Split-tab and transfer-tab are fully removed at the code and SQL level with zero collateral damage to the kept refunds/split-payment schema; the orphaned /pos page is deleted.
</success_criteria>

<output>
Create `.planning/phases/01-strip-rebrand/01-11-SUMMARY.md` when done
</output>
