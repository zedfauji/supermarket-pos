---
phase: 03-supplier-receiving-expiry-tracking
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/shared/lib/domain.ts
  - src/entities/settings/model/queries.ts
  - src/entities/settings/model/types.ts
  - src/entities/inventory/model/queries.ts
  - src/entities/inventory/ui/NearExpiryBadge.tsx
  - src/entities/inventory/index.ts
  - src/widgets/SettingsTabsPanel/tabs/NearExpirySettingsTab.tsx
  - src/widgets/SettingsTabsPanel/index.tsx
  - src/widgets/InventoryPagePanel.tsx
  - src/pages/inventory/index.tsx
  - src/widgets/HomeDashboard/ui/HomeDashboard.tsx
  - src/entities/tab/ui/CartItem.tsx
  - src/shared/lib/i18n/locales/es-MX/entities.json
  - src/shared/lib/i18n/locales/en-US/entities.json
  - src/shared/lib/i18n/locales/es-MX/wAdmin.json
  - src/shared/lib/i18n/locales/en-US/wAdmin.json
  - src/shared/lib/i18n/locales/es-MX/pages.json
  - src/shared/lib/i18n/locales/en-US/pages.json
  - e2e/54-near-expiry-alerts.spec.ts
autonomous: true
requirements: [EXP-02]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Owner sees a near-expiry count badge on the Home dashboard's inventory tile when at least one product is within the configured threshold"
    - "Owner sees a near-expiry list/tab on the Inventory page, next to the low-stock signal, when at least one product is within the threshold"
    - "A cashier ringing up a near-expiring product at checkout sees a signal on that cart line"
    - "The near-expiry threshold (default 14 days) is configurable in Settings, admin-only, and the setting persists"
    - "A manager-role account cannot write the near_expiry settings key (server-side RLS rejection, not just a hidden UI control)"
    - statement: "Near-expiry list/tab and badge render nothing (or an explicit 'Nothing expiring soon' EmptyState) when no items are within the threshold"
      verification: explicit
  artifacts:
    - path: "src/entities/inventory/ui/NearExpiryBadge.tsx"
      provides: "Amber near-expiry count badge, structural mirror of LowStockBadge.tsx"
    - path: "src/widgets/SettingsTabsPanel/tabs/NearExpirySettingsTab.tsx"
      provides: "Admin-only threshold-days settings form"
  key_links:
    - from: "src/entities/inventory/model/queries.ts"
      to: "src/entities/settings/model/queries.ts"
      via: "useNearExpiryAlerts() reads the threshold via useSettings().data.nearExpiry.thresholdDays"
      pattern: "useSettings"
    - from: "src/entities/tab/ui/CartItem.tsx"
      to: "src/entities/inventory/model/queries.ts"
      via: "CartItem looks up item.product.id against useNearExpiryAlerts() data"
      pattern: "useNearExpiryAlerts"
---

<objective>
Deliver EXP-02: a near-expiry alert query filtered against a configurable threshold (default 14 days, D-07), surfaced in the three places D-06 locks in — a Home dashboard badge, an Inventory-page tab next to low-stock, and a per-line signal on the POS checkout cart — plus the admin-only Settings tab that controls the threshold.

Purpose: Phase 03's last requirement. Depends on Plan 03-01's `inventory.expiry_date` column.
Output: `useNearExpiryAlerts`/`NearExpiryBadge`/`useNearExpiryToast` trio (structural copy of the existing low-stock trio), `NearExpirySettingsTab`, and the 3 D-06 surfaces wired.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-supplier-receiving-expiry-tracking/03-CONTEXT.md
@.planning/phases/03-supplier-receiving-expiry-tracking/03-RESEARCH.md
@.planning/phases/03-supplier-receiving-expiry-tracking/03-PATTERNS.md
@.planning/phases/03-supplier-receiving-expiry-tracking/03-UI-SPEC.md
@src/entities/inventory/model/queries.ts
@src/entities/inventory/ui/LowStockBadge.tsx
@src/entities/settings/model/queries.ts
@src/widgets/SettingsTabsPanel/tabs/BillingSettingsTab.tsx
@src/widgets/InventoryPagePanel.tsx
@src/pages/inventory/index.tsx
@src/entities/tab/ui/CartItem.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Near-expiry alert query + Settings threshold tab (D-07)</name>
  <files>src/shared/lib/domain.ts, src/entities/settings/model/queries.ts, src/entities/settings/model/types.ts, src/entities/inventory/model/queries.ts, src/entities/inventory/ui/NearExpiryBadge.tsx, src/entities/inventory/index.ts, src/widgets/SettingsTabsPanel/tabs/NearExpirySettingsTab.tsx, src/widgets/SettingsTabsPanel/index.tsx, src/shared/lib/i18n/locales/es-MX/entities.json, src/shared/lib/i18n/locales/en-US/entities.json, src/shared/lib/i18n/locales/es-MX/wAdmin.json, src/shared/lib/i18n/locales/en-US/wAdmin.json, e2e/54-near-expiry-alerts.spec.ts</files>
  <read_first>
    03-RESEARCH.md Pattern 3 (low-stock-alert trio) and Common Pitfalls #3/#4, 03-PATTERNS.md "useNearExpiryAlerts()" and "NearExpirySettingsTab.tsx" sections, src/entities/inventory/model/queries.ts lines 156-231 (`useInventoryAlerts` — structure to mirror verbatim), src/entities/inventory/ui/LowStockBadge.tsx (full file — structure to mirror), src/entities/settings/model/queries.ts (full file — `SettingsSnapshot`/`SETTINGS_KEYS`/`toSnapshot`/`useMutationUpdateSetting` — the exact places a new key must be threaded through), src/widgets/SettingsTabsPanel/tabs/BillingSettingsTab.tsx (dirty-state settings-form pattern), src/widgets/SettingsTabsPanel/index.tsx (tab registration, `canManageSettings` gate), supabase/migrations/20260419000001_settings_and_backups.sql lines 41-71 (the fixed `key IN ('billing', 'pool_tables')` manager allowlist — `near_expiry` must NOT be added here, Pitfall #3).
  </read_first>
  <action>
    **`src/shared/lib/domain.ts`:** add `'near_expiry'` to `SettingsKeySchema`'s enum array (domain.ts:690-698). Add `NearExpirySettingsSchema = z.object({ thresholdDays: z.number().int().min(1).max(365).default(14) })` next to `TipDistributionSettingsSchema`, register both in the bottom `domain.schemas` namespace object. Add `NearExpiryAlertSchema = z.object({ productId: UuidSchema, productName: z.string().min(1), expiryDate: z.string(), daysUntilExpiry: z.number().int() })` next to `InventoryAlertSchema` (domain.ts:677-684).

    **`src/entities/settings/model/queries.ts`:** add `DEFAULT_NEAR_EXPIRY: NearExpirySettings = { thresholdDays: 14 }`, `parseNearExpiry(value: unknown): NearExpirySettings` (same `safeParse`-or-default shape as `parseTipDistribution`), add `nearExpiry: NearExpirySettings` to the `SettingsSnapshot` type, add `'near_expiry'` to the `SETTINGS_KEYS` array, add `nearExpiry: parseNearExpiry(byKey.get('near_expiry'))` inside `toSnapshot`, and add `NearExpirySettings` to `useMutationUpdateSetting`'s `value` union type. Re-export `NearExpirySettings`/`NearExpirySettingsSchema` from `src/entities/settings/model/types.ts` alongside the existing re-exports.

    **`src/entities/inventory/model/queries.ts`:** add `nearExpiry: (thresholdDays: number) => [...inventoryKeys.all, 'near-expiry', thresholdDays] as const` to `inventoryKeys`. Add `useNearExpiryAlerts()`: call `useSettings()` (from `@entities/settings`) at the top to get `thresholdDays = data?.nearExpiry.thresholdDays ?? 14`, then a `useQuery` with `queryKey: inventoryKeys.nearExpiry(thresholdDays)` whose `queryFn` computes a cutoff date string (`YYYY-MM-DD`, today + thresholdDays) and selects `expiry_date, product:products!inner(id, name)` from `inventory` filtered `.not('expiry_date', 'is', null).lte('expiry_date', cutoff)`, mapping each row into `NearExpiryAlertSchema.parse({ productId, productName, expiryDate, daysUntilExpiry: Math.ceil((expiryDate - today) / 86400000) })` — same `Result`-unwrap return shape (`data`, `resultError`, `isEmpty`, `isIdleOrLoading`) as `useInventoryAlerts` (queries.ts:223-231). `staleTime: 30 * 1000`, matching `useInventoryAlerts`.

    **`src/entities/inventory/ui/NearExpiryBadge.tsx`:** copy `LowStockBadge.tsx` near-verbatim — swap `useInventoryAlerts` → `useNearExpiryAlerts`, `variant="destructive"` → `className="ml-3 tabular-nums bg-pos-warning text-amber-950 dark:text-amber-100"` (amber `--pos-warning` token per UI-SPEC Color section — near-expiry is advisory, not blocking, must not reuse the red destructive token), `data-testid="near-expiry-badge"`, i18n key `nearExpiryBadge.count`. Export from `src/entities/inventory/index.ts` alongside `LowStockBadge`.

    **`src/widgets/SettingsTabsPanel/tabs/NearExpirySettingsTab.tsx`:** copy `BillingSettingsTab.tsx`'s dirty-state form pattern for a single numeric field (`thresholdDays`, default '14', `Input` with `min=1 max=365`), `settings` key `'near_expiry'`, gated by `ProtectedAction action="manage_settings"` (admin-only per D-07 — do NOT add `'near_expiry'` to the manager-scoped `key IN ('billing', 'pool_tables')` RLS allowlist, Pitfall #3). Register it in `src/widgets/SettingsTabsPanel/index.tsx` inside the existing `canManageSettings` push block (same admin-only group as `general`/`hardware`/`email`/`backup`).

    **i18n:** add `nearExpiryBadge.count` to `entities.json` (both locales, mirrors `lowStockBadge.count`'s `"{{count}} ..."` shape). Add `nearExpirySettingsTab.title`/`thresholdLabel`/`thresholdHint`/`saveButton`/`saved` to `wAdmin.json` (both locales).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npx playwright test e2e/54-near-expiry-alerts.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Setting the threshold to a new value in Settings → Near Expiry, then reloading, shows the saved value (persisted via the `settings` table `near_expiry` key).
    - A `manager`-role authenticated client-side `supabase.from('settings').upsert({ key: 'near_expiry', ... })` call is rejected by RLS (asserted directly against `getServiceClient()`'s anon-scoped equivalent, or via a manager-authenticated Supabase client in the E2E spec) — proves Pitfall #3's negative path, not just a hidden UI control.
    - `useNearExpiryAlerts()` returns a product seeded with `inventory.expiry_date` inside the threshold window and excludes one outside it.
  </acceptance_criteria>
  <done>The near-expiry alert query and its threshold setting exist, are admin-only, and are proven correct against seeded data — nothing renders yet (Task 2 wires the 3 D-06 surfaces).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire the 3 D-06 surfaces — Inventory tab, Home badge, POS cart signal</name>
  <files>src/widgets/InventoryPagePanel.tsx, src/pages/inventory/index.tsx, src/widgets/HomeDashboard/ui/HomeDashboard.tsx, src/entities/tab/ui/CartItem.tsx, src/shared/lib/i18n/locales/es-MX/entities.json, src/shared/lib/i18n/locales/en-US/entities.json, src/shared/lib/i18n/locales/es-MX/pages.json, src/shared/lib/i18n/locales/en-US/pages.json, e2e/54-near-expiry-alerts.spec.ts</files>
  <read_first>
    03-CONTEXT.md D-06, 03-PATTERNS.md "CartItem.tsx — near-expiry signal" and "HomeDashboard.tsx" sections (Common Pitfall #5 — no existing Home badge-overlay slot, this is new UI construction), src/widgets/InventoryPagePanel.tsx lines 211-216, 420-424 (the existing `Tabs`/`TabsTrigger`/`TabsContent` structure — "stock" and "open-units" — to extend with a third "near-expiry" tab), src/pages/inventory/index.tsx lines 16-47 (`useLowStockToast` — pattern to mirror for `useNearExpiryToast`), src/widgets/HomeDashboard/ui/HomeDashboard.tsx lines 120-155 (the `ITEMS.map` render loop, `Lock` icon absolute-positioning technique, `data-testid={item.path === '/audit' ? ... : undefined}` per-path special-casing), src/entities/tab/ui/CartItem.tsx (full file — the existing modifier `Badge`s and `Zap` happy-hour icon placement, lines 45-54 and 69-75, to extend with an amber near-expiry `Badge`).
  </read_first>
  <action>
    **`src/widgets/InventoryPagePanel.tsx`:** add a third `<TabsTrigger value="near-expiry">` next to `stock`/`open-units` (line ~215) and a matching `<TabsContent value="near-expiry">` rendering: a simple list/table of `useNearExpiryAlerts()` results (product name, expiry date, days-until-expiry), or `EmptyState` ("Nothing expiring soon" / "No products are within the near-expiry window.") when `isEmpty`.

    **`src/pages/inventory/index.tsx`:** add `useNearExpiryToast()` (copy `useLowStockToast`'s structure lines 16-47 verbatim, swap `useInventoryAlerts` → `useNearExpiryAlerts`, toast copy keyed off product name + days-until-expiry) and call it in `InventoryPageInner` alongside `useLowStockToast()`. Add `<NearExpiryBadge />` next to `<LowStockBadge />` in the `PageContainer`'s `actions`.

    **`src/widgets/HomeDashboard/ui/HomeDashboard.tsx`:** inside the `ITEMS.map` render loop, when `item.path === '/inventory'`, render a `NearExpiryBadge`-driven count overlay absolutely positioned top-left of the tile (the `Lock` icon already occupies top-right when gated, lines 137-142) — reuse the same `absolute` positioning technique, new UI construction per Pitfall #5, not a wire-up of an existing slot. Only render when the alert count is > 0 (same conditional-null pattern as `NearExpiryBadge` itself; do not add a second data-fetch — reuse `useNearExpiryAlerts()` directly in `HomeDashboard.tsx` for the count, since `NearExpiryBadge` itself is a self-contained badge instance, not a bare count).

    **`src/entities/tab/ui/CartItem.tsx`:** call `useNearExpiryAlerts()` inside `CartItem`, find the alert matching `item.product.id`, and if found render an amber `Badge` ("Expires {date}" or "Expires in {n} days", per UI-SPEC — use `t('cartItem.nearExpiry', { days: alert.daysUntilExpiry })`) inline next to the product name, following the existing modifier-`Badge` placement pattern (lines 45-54) — no `CartItemSchema`/domain-type change needed (Open Question #2 resolved: separate lookup, not a schema extension, keeps this a refinement not an architectural change).

    **i18n:** add `inventoryPagePanel.nearExpiryTabLabel`, `nothingExpiringSoonTitle`/`Body` to... (these are `wAdmin` namespace per `InventoryPagePanel`'s existing `useTranslation('wAdmin')`; correct: add to `wAdmin.json`, not `entities.json` — verify the namespace each file actually uses via its own `useTranslation()` call before adding keys). Add `inventory.nearExpiryToast` to `pages.json` (matches `inventory.lowStockToast`'s existing key shape). Add `cartItem.nearExpiry` to `entities.json` (both locales, `{{days}}`-interpolated).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npx playwright test e2e/54-near-expiry-alerts.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - E2E: seed a product's `inventory.expiry_date` inside the configured threshold (via `getServiceClient()`), then assert all 3 surfaces show the signal in one spec run: (1) `/home` renders a near-expiry count on the inventory tile, (2) `/inventory`'s near-expiry tab lists the product and the badge in `PageContainer` actions shows the count, (3) adding that product to the POS cart at `/pos` shows the amber near-expiry `Badge` on its `CartItem`.
    - A second E2E case: no products within the threshold — the Home tile shows no overlay, the Inventory near-expiry tab shows the "Nothing expiring soon" `EmptyState`, and `NearExpiryBadge` renders nothing (`data-testid="near-expiry-badge"` is absent from the DOM).
  </acceptance_criteria>
  <done>All three D-06 surfaces (Home dashboard, Inventory page, POS checkout) show the near-expiry signal when applicable and stay silent/empty-stated when not.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → settings table (near_expiry key) | Untrusted client JSON for the threshold-days config crosses here, gated only by RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-03-08 | Elevation of Privilege | `settings` table, `near_expiry` key | high | mitigate | The fixed manager-scoped RLS allowlist (`key IN ('billing', 'pool_tables')`) already default-denies any key not explicitly listed — `near_expiry` is deliberately left OUT of that list so only `admin` can write it, per D-07. Task 1's acceptance criteria includes a negative-path E2E proving a manager write is rejected, not just inferred from omission. |
| T-03-09 | Information Disclosure | Near-expiry alert data | low | accept | Expiry dates are operational inventory data, already visible to any authenticated staff member on the Inventory page today (same trust level as `quantity_on_hand`) — no new sensitive data exposed. |

</threat_model>

<verification>
- `npm run typecheck` and `npm run lint` pass with zero errors/warnings after both tasks.
- `npx playwright test e2e/54-near-expiry-alerts.spec.ts` passes end to end (3-surface positive case, empty-state negative case, RLS negative path).
- Manual settings-persistence check: threshold value survives a page reload.
</verification>

<success_criteria>
The near-expiry threshold is admin-configurable (default 14 days) and a product within that window is visible via a Home dashboard badge, an Inventory-page tab, and a POS cart-line signal — all three driven by one query and one setting, never a hardcoded constant.
</success_criteria>

<output>
Create `.planning/phases/03-supplier-receiving-expiry-tracking/03-02-SUMMARY.md` when done
</output>
