---
phase: 02-core-direct-sale-checkout
plan: 05
type: execute
wave: 2
depends_on: ["02-04"]
files_modified:
  - src/widgets/PaymentModal/ui/PaymentForm.tsx
  - src/widgets/PaymentModal/ui/PaymentForm.test.tsx
  - src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx
  - src/features/checkout-sale/model/useCheckoutSale.ts
  - src/shared/lib/i18n/locales/en-US/featOrders.json
  - src/shared/lib/i18n/locales/es-MX/featOrders.json
  - src/widgets/ProductGrid/ui/ProductGrid.tsx
  - src/features/scan-barcode-to-cart/model/useScanBarcodeToCart.ts
  - src/entities/tab/model/cartStore.ts
  - src/entities/tab/model/cartStore.test.ts
  - src/shared/lib/receipt-format.ts
  - src/shared/lib/receipt-format.test.ts
  - src/shared/lib/edge-function-contracts.ts
  - supabase/functions/process-direct-sale/index.ts
  - e2e/50-direct-sale-checkout.spec.ts
  - e2e/51-barcode-scan-search.spec.ts
  - e2e/52-loose-weight-hold-sale.spec.ts
autonomous: true
requirements: [CHK-01, CHK-02, CHK-05]
gap_closure: true

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Clicking Cancel on the payment screen before any payment attempt leaves the active cart and its items completely intact, and shows no sale-complete confirmation"
    - "Only a confirmed successful payment's receipt Done button clears the cart and shows the sale-complete confirmation"
    - "npm run lint passes with zero errors in src/features/checkout-sale/model/useCheckoutSale.ts and src/widgets/PaymentModal/ui/PaymentForm.tsx"
    - "Scanning a sold-by-weight product's barcode opens the weight-entry dialog instead of adding a quantity-1 unit line the server would reject at payment time"
    - "Clicking Resume while a new active cart is in progress swaps the active and held carts instead of discarding the active one"
    - "A receipt line for a weighted item displays its weight in kg, not a misleading quantity of 1 at the full line price"
  artifacts:
    - src/widgets/PaymentModal/ui/PaymentForm.tsx
    - src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx
    - src/widgets/ProductGrid/ui/ProductGrid.tsx
    - src/features/scan-barcode-to-cart/model/useScanBarcodeToCart.ts
    - src/entities/tab/model/cartStore.ts
    - src/shared/lib/receipt-format.ts
  key_links:
    - "PaymentForm Cancel button -> onClose (pure, non-destructive); ReceiptPreview onDone -> onDone (post-payment success, falls back to onClose when unset)"
    - "useScanBarcodeToCart -> weightEntry.openFor (lifted from ProductGrid into CheckoutPanel) -> WeightEntryDialog"
    - "HoldSaleBanner Resume -> cartStore.resumeHeld (active/held swap when the active cart is non-empty)"
    - "process-direct-sale buildReceipt -> order_items.weight_grams -> ReceiptData.items[].weightGrams -> buildThermalReceiptText"
  prohibitions:
    - statement: "MUST NOT silently discard a cashier's in-progress, uncommitted active cart when Resume is pressed while a sale is held"
      status: resolved
      verification: test
    - statement: "MUST NOT create an unpayable cart line for a sold-by-weight product scanned via barcode"
      status: resolved
      verification: test
    - statement: "MUST NOT auto-create a catalog product record from an unmatched barcode scan"
      status: resolved
      verification: test
      reason: "Already shipped and verified in Plan 02-02 (audit-logged, no product insert); unaffected by this plan's changes — recorded here for CHK-01/CHK-02 coverage completeness."
---

<objective>
Close the CHK-01/CHK-02/CHK-05 verification blockers from `.planning/phases/02-core-direct-sale-checkout/02-VERIFICATION.md` (CR-01, CR-04, CR-05 in `02-REVIEW.md`, plus WR-01): `CheckoutPanel`'s single `onClose` handler destroys the cart on both Cancel-before-payment and receipt-Done, the barcode scanner bypasses weight entry for sold-by-weight products and creates an unpayable cart line, `cartStore.resumeHeld` silently overwrites a new active cart instead of swapping it with the held one, and weighted receipt lines print a misleading "1× Product" instead of the actual weight. This plan also fixes the 12 pre-existing Phase-02 ESLint errors blocking `npm run lint`.

Purpose: A cashier must be able to cancel a payment attempt, scan any product (including loose-weight items), and hold/resume sales without the checkout UI silently destroying their work or producing an unpayable/misleading state.
Output: A correctly-separated Cancel/Done contract on `PaymentForm`, a scanner that routes weighted products through the same weight-entry dialog `ProductGrid` uses, an atomic hold/resume swap, an accurate weighted-receipt line format, a clean `npm run lint`, and E2E/unit coverage for all of the above.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-direct-sale-checkout/02-VERIFICATION.md
@.planning/phases/02-core-direct-sale-checkout/02-REVIEW.md
@src/widgets/PaymentModal/ui/PaymentForm.tsx
@src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx
@src/features/checkout-sale/model/useCheckoutSale.ts
@src/widgets/ProductGrid/ui/ProductGrid.tsx
@src/features/scan-barcode-to-cart/model/useScanBarcodeToCart.ts
@src/features/add-loose-weight-item/model/useAddLooseWeightItem.ts
@src/entities/tab/model/cartStore.ts
@src/entities/tab/model/cartStore.test.ts
@src/shared/lib/receipt-format.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Separate Cancel from post-payment Done (CR-01) and fix the 12 pre-existing lint errors</name>
  <files>src/widgets/PaymentModal/ui/PaymentForm.tsx, src/widgets/PaymentModal/ui/PaymentForm.test.tsx, src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx, src/features/checkout-sale/model/useCheckoutSale.ts, src/shared/lib/i18n/locales/en-US/featOrders.json, src/shared/lib/i18n/locales/es-MX/featOrders.json, e2e/50-direct-sale-checkout.spec.ts</files>
  <behavior>
    - `PaymentForm` given both `onClose` and `onDone`: clicking Cancel (pre-payment) calls `onClose` and never calls `onDone`; clicking Done after a successful single or split payment's receipt calls `onDone` and never calls `onClose`.
    - `PaymentForm` given only `onClose` (no `onDone`, matching `PaymentModal`/`PaymentPane`'s existing callers): Done falls back to calling `onClose`, preserving today's behavior exactly (the existing `PaymentForm.test.tsx` "Done reaches onClose" test must keep passing unmodified).
    - In `CheckoutPanel`, clicking Cancel before any payment leaves `cartStore.items` unchanged and shows no "Sale complete" toast; clicking Done after a real successful payment clears the cart and shows the toast (E2E, `e2e/50-direct-sale-checkout.spec.ts`).
  </behavior>
  <action>
    Add an optional `onDone?: () => void` prop to `PaymentFormProps` in `src/widgets/PaymentModal/ui/PaymentForm.tsx`, placed next to the existing `onClose?: () => void`. Introduce one small helper (e.g. `const handleReceiptDone = () => { (onDone ?? onClose)?.(); };`) and use it in place of the two existing `onClose?.()` calls inside the `ReceiptPreview`'s `onDone` callbacks — both the single-receipt branch (~line 530) and the split-receipt-queue branch's final-receipt case (~line 516, the `else` arm after `if (receiptIndex + 1 < receiptQueue.length)`). Do not change the Cancel button (~line 1106-1117): it must keep calling `onClose` directly, unconditionally, so Cancel and Done never resolve to the same intent when both props are supplied.

    In `src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx`, split the current single `onClose` handler passed to `<PaymentForm>` into two: `onClose` becomes a pure cancel — call `resetIdempotencyKey()` (so a subsequent payment attempt after Cancel gets a fresh idempotency key rather than replaying a stale/failed one) and `setPaymentOpen(false)`, with no toast and no `clearCart()`. Add a new `onDone` prop carrying exactly today's destructive behavior (the "Sale complete" toast via `t('checkoutPanel.saleComplete', ...)`, `resetIdempotencyKey()`, `clearCart()`, `setPaymentOpen(false)`) unchanged.

    Fix the 12 pre-existing ESLint errors flagged in `02-VERIFICATION.md`'s Anti-Patterns table (all in Phase-02-introduced files, not external legacy debt):
    - `useCheckoutSale.ts` `import/order`: swap the `@shared/lib/domain-helpers` and `@shared/lib/edge-function-contracts` import lines so `domain-helpers` precedes `edge-function-contracts`.
    - `useCheckoutSale.ts` line ~23 `customerName: 'Walk-in'`: this literal is genuinely rendered as UI copy (`PaymentForm`'s `<h3>{tab.customerName}</h3>` heading), so translate it rather than suppress the rule — add a `checkoutSale.defaultCustomerName` key ("Walk-in" / "Cliente de mostrador") to both `src/shared/lib/i18n/locales/en-US/featOrders.json` and `es-MX/featOrders.json` (new top-level group, following the existing `weightEntry`/`processPayment` group pattern in that file) and call `i18n.t('featOrders:checkoutSale.defaultCustomerName')` (the file already imports `i18n` from `@shared/lib/i18n`).
    - `useCheckoutSale.ts`'s three `message: 'Payment could not be completed'` literals (lines ~152, ~179, ~210): add a `checkoutSale.paymentIncomplete` key ("Payment could not be completed." / "No se pudo completar el pago.") to both locale files and replace each literal with `i18n.t('featOrders:checkoutSale.paymentIncomplete')`, matching the established `i18n.t('featOrders:...')` pattern already used elsewhere in this file and in sibling features like `useVoidOrder.ts`/`useReopenTab.ts`.
    - `useCheckoutSale.ts` line ~106 `generateIdempotencyKey('direct_sale')` and `PaymentForm.tsx`'s three `generateIdempotencyKey('payment_cash'|'payment_card'|'payment_split')` calls: these are internal idempotency-key prefixes, not UI copy — add an `// eslint-disable-next-line i18next/no-literal-string -- idempotency-key prefix, not UI copy` comment above each call site, matching the established codebase convention for non-copy technical strings (see the existing disables in `ProductGrid.tsx` and `lookup-product-by-barcode`'s cache-key/column-name disables).
    - `useCheckoutSale.ts` line ~183 `processRappiPayment: async () => err(...)`: remove the unnecessary `async` keyword (the function body has no `await`) and wrap the return value in `Promise.resolve(...)` so it still satisfies the `PaymentProcessors['processRappiPayment']` interface; add a `checkoutSale.rappiUnavailable` key ("Not yet available" / "Aún no disponible") to both locale files and use `i18n.t('featOrders:checkoutSale.rappiUnavailable')` for the message.
    - `useCheckoutSale.ts` line ~195 `message: 'Rappi is not available for direct sales'`: add a `checkoutSale.rappiNotSupported` key ("Rappi is not available for direct sales" / "Rappi no está disponible para ventas directas") to both locale files and use `i18n.t('featOrders:checkoutSale.rappiNotSupported')`.

    In `e2e/50-direct-sale-checkout.spec.ts`, add a new test `'cancelling before payment preserves the cart'`: navigate to `/pos`, search and select Budweiser, click "Process payment" to open `PaymentForm`, click the `Cancel` button (`getByRole('button', { name: /^cancel$/i })`), then assert the Budweiser line is still visible in the cart (`page.locator('aside').getByText('Budweiser')`) and that no "Sale complete" text is visible.
  </action>
  <verify>
    <automated>npx eslint src/features/checkout-sale/model/useCheckoutSale.ts src/widgets/PaymentModal/ui/PaymentForm.tsx && npm run typecheck && npx vitest run src/widgets/PaymentModal/ui/PaymentForm.test.tsx && npx playwright test e2e/50-direct-sale-checkout.spec.ts -g "cancelling before payment"</automated>
  </verify>
  <done>ESLint reports zero errors in both files, the existing "Done reaches onClose" unit test still passes unmodified, a new unit test proves onDone/onClose are called for the correct trigger, and the new E2E cancel test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route scanned sold-by-weight products through the shared weight-entry dialog (CR-04)</name>
  <files>src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx, src/widgets/ProductGrid/ui/ProductGrid.tsx, src/features/scan-barcode-to-cart/model/useScanBarcodeToCart.ts, e2e/51-barcode-scan-search.spec.ts</files>
  <behavior>
    - Scanning a barcode whose matched product has `soldByWeight: true` opens the weight-entry dialog (does not call `cartStore.addItem`); the cart remains empty until a weight is entered and confirmed.
    - Scanning a barcode whose matched product has `soldByWeight: false` behaves exactly as before (`addItem(product, [])`).
    - Selecting a sold-by-weight product from the product grid (the existing, already-working path) is unaffected.
  </behavior>
  <action>
    `ProductGrid.tsx` currently owns its own `useAddLooseWeightItem()` instance (state: `product`/`isOpen`/`openFor`/`close`) and renders `<WeightEntryDialog>` itself; `useScanBarcodeToCart` (mounted separately in `CheckoutPanel`) has no access to that state, so it always calls `addItem(product, [])` regardless of `soldByWeight`, producing a line the server's `WEIGHT_OUT_OF_RANGE` check rejects at payment time. Fix by lifting the single `useAddLooseWeightItem()` call from `ProductGrid` up to `CheckoutPanel` so both the grid's card-select path and the scanner path share one instance:

    - In `CheckoutPanel.tsx`, import and call `useAddLooseWeightItem()` once (e.g. `const weightEntry = useAddLooseWeightItem();`), before the `useScanBarcodeToCart(...)` call.
    - Change `useScanBarcodeToCart`'s call site to `useScanBarcodeToCart(true, weightEntry.openFor)`.
    - Pass `weightEntry` down to `<ProductGrid weightEntry={weightEntry} onSelect={...} />` (keep the existing `onSelect` prop unchanged).
    - In `ProductGrid.tsx`: remove its own `const weightEntry = useAddLooseWeightItem();` line and the now-unused `useAddLooseWeightItem` import; accept `weightEntry: ReturnType<typeof useAddLooseWeightItem>` as a new prop in the component's parameter type. Keep the `<WeightEntryDialog>` render and the `if (selectedProduct.soldByWeight) weightEntry.openFor(selectedProduct); else onSelect(selectedProduct);` branch exactly as-is — only the state's *source* moves, not where it's consumed or rendered.
    - In `useScanBarcodeToCart.ts`: add a required second parameter `onWeightedProduct: (product: Product) => void` (import `type { Product }` from `@shared/lib/domain`). In `handleScan`, after a successful `lookup(code)`, branch on `product.soldByWeight`: if true, call `onWeightedProduct(product)` and `logger.debug('barcode_scan.weight_entry_opened', { barcode: code, productId: product.id })` (do not call `addItem`); if false, keep the existing `addItem(product, [])` + `logger.debug('barcode_scan.added', ...)` path unchanged. `useScanBarcodeToCart` has exactly one call site (`CheckoutPanel.tsx`) and no test/story file, so this is not a breaking change for any other consumer.

    Add a new test to `e2e/51-barcode-scan-search.spec.ts`: `'scan of a loose-weight product opens weight entry instead of an unpayable line'` — reuse the same "first active product with a barcode" lookup pattern the existing `'scan adds a catalog product to the cart'` test uses, set that product's `sold_by_weight` to `true` via the admin client before scanning, scan its barcode, assert the weight-entry dialog's title ("Enter weight") becomes visible and the cart stays empty (`getByText(/cart is empty/i)`), then reset `sold_by_weight` back to `false` on that product at the end of the test so it doesn't leak into other specs.
  </action>
  <verify>
    <automated>npm run typecheck && npx eslint src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx src/widgets/ProductGrid/ui/ProductGrid.tsx src/features/scan-barcode-to-cart/model/useScanBarcodeToCart.ts && npx playwright test e2e/51-barcode-scan-search.spec.ts</automated>
  </verify>
  <done>A scanned sold-by-weight product opens the weight-entry dialog and adds no cart line until weight is confirmed; a scanned unit-sold product behaves exactly as before; the new e2e/51 test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Swap active/held carts on Resume (CR-05) and show weight instead of quantity on weighted receipt lines (WR-01)</name>
  <files>src/entities/tab/model/cartStore.ts, src/entities/tab/model/cartStore.test.ts, src/shared/lib/receipt-format.ts, src/shared/lib/receipt-format.test.ts, src/shared/lib/edge-function-contracts.ts, supabase/functions/process-direct-sale/index.ts, e2e/52-loose-weight-hold-sale.spec.ts</files>
  <behavior>
    - `cartStore.resumeHeld()` called while the active cart is empty: held cart becomes active, `heldCart` becomes `null` (today's existing, correct behavior — unchanged).
    - `cartStore.resumeHeld()` called while the active cart is non-empty: the active cart becomes the new `heldCart` and the previously-held cart becomes active — no cart contents are lost, and calling `resumeHeld()` again swaps back.
    - `buildThermalReceiptText` given an item with `weightGrams` set: the item's left-hand label shows the weight in kg (e.g. "0.375kg × Jamón"), not `1× Jamón`.
  </behavior>
  <action>
    CR-05 fix — in `src/entities/tab/model/cartStore.ts`, change `resumeHeld` from unconditionally overwriting `items`/`heldCart` to an atomic swap: when `state.heldCart` is set, return `{ items: state.heldCart, heldCart: state.items.length > 0 ? state.items : null }` (i.e. the current active items become the new held cart only when there were any; an empty active cart still resolves to `heldCart: null`, preserving today's normal-resume behavior exactly). Keep the existing `logger.info('cart.resumed', ...)` call, extending its payload with whether an active cart was swapped (e.g. `{ swappedActiveCart: state.items.length > 0 }`).

    Update the existing `cartStore.test.ts` test `'holds the active cart without blocking a new sale, then resumes it'` (in the `describe('held cart', ...)` block) — this test currently asserts the OLD buggy behavior (`resumeHeld()` after adding a new active-cart item asserts `{ items: [heldItem], heldCart: null }`, i.e. it currently codifies CR-05 as expected). Change its final assertion to `{ items: [heldItem], heldCart: [activeItem] }` (capture `activeItem` from `useCartStore.getState().items[0]` right after the second `addItem` call, mirroring how `heldItem` is already captured), and add one more `resumeHeld()` call + assertion proving the swap-back: `{ items: [activeItem], heldCart: [heldItem] }`. Add a second, separate test confirming the unchanged empty-active-cart path still resolves to `heldCart: null` (hold an item, immediately `resumeHeld()` with nothing else added, assert `{ items: [heldItem], heldCart: null }`).

    Add a new E2E test to `e2e/52-loose-weight-hold-sale.spec.ts`: `'resuming a held sale swaps instead of discarding a new active cart'` — log in as cashier, select Budweiser and click Hold (cart A held), select Corona (a different fixture product from `scripts/seed-dev-data.ts`) into the now-empty active cart WITHOUT paying it (cart B active), click Resume and assert `Budweiser` is now visible in the cart (`page.locator('aside')`) while `Corona` is not, and the "Sale held" badge is still visible (Corona is now held); click Resume again and assert `Corona` is now visible while `Budweiser` is not.

    WR-01 fix — in `src/shared/lib/edge-function-contracts.ts`'s `ReceiptDataSchema`, add `weightGrams: z.number().int().positive().max(50000).nullable().optional()` to the `items` array's object schema (alongside the existing `categoryId`/`categoryName`/`modifierNames` optional fields). In `supabase/functions/process-direct-sale/index.ts`'s `buildReceipt` function: extend the `orders` select string to also fetch `order_items.weight_grams` (add `weight_grams` to the existing `order_items(...)` select list), extend the inline TS type annotation for the `order_items` array to include `weight_grams: number | null;`, and in the `.map(item => ({...}))` that builds receipt line items, add `weightGrams: item.weight_grams ?? null`.

    In `src/shared/lib/receipt-format.ts`'s `buildThermalReceiptText`, change the item-line `left` label construction (inside the `for (const item of group.items)` loop) to branch on `item.weightGrams`: when non-null, render `` `${(item.weightGrams / 1000).toFixed(3)}kg × ${sanitize(item.name)}` `` instead of `` `${String(item.quantity)}× ${sanitize(item.name)}` ``; keep the price side (`formatMoneyIn(locale, item.lineTotal)`) unchanged since the line total was already correct — only the misleading quantity label changes. Do not touch `buildPreChequeText` or `PreChequeData` (out of WR-01's scope — that flow isn't used by direct-sale checkout).

    Add a new unit test to `src/shared/lib/receipt-format.test.ts` (in the `describe('buildThermalReceiptText', ...)` block, following the existing `baseReceipt()` helper pattern): construct a receipt whose single item has `weightGrams: 375` and assert the corresponding output line contains `0.375kg` and does not contain `1×`.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/entities/tab/model/cartStore.test.ts src/shared/lib/receipt-format.test.ts && npx playwright test e2e/52-loose-weight-hold-sale.spec.ts</automated>
  </verify>
  <done>cartStore.test.ts's updated and new held-cart tests pass; receipt-format.test.ts's new weighted-line test passes; the new e2e/52 swap test passes alongside the file's existing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cashier UI (PaymentForm Cancel/Done) → `cartStore.clearCart` | A single ambiguous callback previously conflated a non-destructive Cancel with a destructive post-payment clear, risking silent loss of an unpaid cart. |
| Barcode scanner (HID keystroke-wedge) → `cartStore.addItem` | Scanner input reaches the cart with no server round-trip; a sold-by-weight product scanned without weight entry would otherwise reach payment as an unpayable line. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05-01 | Tampering (unintended state mutation) | `CheckoutPanel`/`PaymentForm` onClose | medium | mitigate | Split `onClose` (pure cancel) from `onDone` (post-payment success) so cancelling never clears an unpaid cart or shows a false success toast (Task 1, CR-01). |
| T-02-05-02 | Tampering (unintended data loss) | `cartStore.resumeHeld` | medium | mitigate | Swap active/held carts atomically instead of overwriting, so no in-progress sale is silently discarded (Task 3, CR-05). |
| T-02-05-03 | Denial of Service (unpayable cart line) | `useScanBarcodeToCart` | low | mitigate | Route `soldByWeight` scans through the shared weight-entry dialog so no line reaches payment without required `weight_grams` (Task 2, CR-04). |
</threat_model>

<verification>
- `npx eslint src/features/checkout-sale/model/useCheckoutSale.ts src/widgets/PaymentModal/ui/PaymentForm.tsx` reports zero errors.
- `npm run typecheck` passes.
- `npx vitest run src/widgets/PaymentModal/ui/PaymentForm.test.tsx src/entities/tab/model/cartStore.test.ts src/shared/lib/receipt-format.test.ts` passes.
- `npx playwright test e2e/50-direct-sale-checkout.spec.ts e2e/51-barcode-scan-search.spec.ts e2e/52-loose-weight-hold-sale.spec.ts` passes in full.
</verification>

<success_criteria>
- Cancelling a payment attempt never clears the cart or shows a false success confirmation; only a real successful payment's Done does.
- A scanned sold-by-weight product always routes through weight entry; no unpayable cart line can be created via scanning.
- Resuming a held sale while a new active cart is in progress swaps the two carts instead of discarding either.
- A weighted receipt line shows the actual weight, not a misleading unit quantity.
- `npm run lint` passes with zero errors in the two Phase-02 files VERIFICATION.md flagged.
</success_criteria>

<output>
Create `.planning/phases/02-core-direct-sale-checkout/02-05-SUMMARY.md` when done
</output>