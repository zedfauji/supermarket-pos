---
phase: 02-core-direct-sale-checkout
plan: 09
type: execute
wave: 3
depends_on: [02-08]
files_modified:
  - src/entities/tab/model/cartStore.ts
  - src/entities/tab/model/cartStore.test.ts
  - src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx
  - e2e/52-loose-weight-hold-sale.spec.ts
autonomous: true
gap_closure: true
requirements: [CHK-05]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "One held sale survives a Tauri/WebView restart, while the active cart remains a fresh session-only cart (D-01, D-02)."
    - "A held slot cannot be overwritten: Checkout disables Hold while one exists, and the store preserves both carts if its action is invoked anyway (D-01)."
    - "The restarted held sale retains every cart-line value needed to show and resume it, including integer grams and its captured price snapshot for a loose-weight line."
    - "The held-sale lifecycle is still one slot and manual-only: a cashier can resume/swap or discard it, with no auto-expiry (D-01, D-03, D-05)."
    - "Holding, rehydrating, resuming, and discarding a sale creates no order, payment, inventory, or stock-movement effect before an actual payment (D-04)."
  artifacts:
    - path: "src/entities/tab/model/cartStore.ts"
      provides: "Versioned, validated local persistence of only the held-cart slot"
      contains: "persist("
    - path: "src/entities/tab/model/cartStore.test.ts"
      provides: "Colocated persisted-state, migration, and clear-on-resume/discard coverage"
      contains: "heldCart"
    - path: "src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx"
      provides: "Hold control unavailable while the single held-cart slot is occupied"
      contains: "isHeld"
    - path: "e2e/52-loose-weight-hold-sale.spec.ts"
      provides: "Reload/restart, weighted-held-sale, resume/discard, and no-reservation browser proof"
      contains: "reload"
  key_links:
    - from: "src/entities/tab/model/cartStore.ts"
      to: "window.localStorage"
      via: "Zustand persist middleware serializes only heldCart under a versioned store key"
      pattern: "partialize"
    - from: "src/entities/tab/model/cartStore.ts"
      to: "src/shared/lib/domain.ts"
      via: "CartItemSchema validates the persisted held-cart payload before hydration"
      pattern: "CartItemSchema"
    - from: "e2e/52-loose-weight-hold-sale.spec.ts"
      to: "src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx"
      via: "a browser reload rehydrates the held banner, keeps Hold disabled with a new active cart, then resumes the original held cart"
      pattern: "Sale held|hold|resume"
---

<objective>
Close the held-sale restart-persistence gap: one held cart must safely survive an application restart without becoming a database sale or reserving stock.

Purpose: The user resolved Research Open Question 2 in favor of persistence. The implementation must retain full loose-weight and pricing data for the one held slot while preserving D-01 through D-05's single-slot, manual, payment-time-only contract.
Output: A versioned native-Zustand persisted held slot, colocated serialization/migration regression tests, and Playwright reload proof for a weighted held sale.
</objective>

<execution_context>
@/home/widowsvail/.codex/gsd-core/workflows/execute-plan.md
@/home/widowsvail/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-direct-sale-checkout/02-CONTEXT.md
@.planning/phases/02-core-direct-sale-checkout/02-RESEARCH.md
@.planning/phases/02-core-direct-sale-checkout/02-03-SUMMARY.md
@.planning/phases/02-core-direct-sale-checkout/02-05-SUMMARY.md
@.planning/phases/02-core-direct-sale-checkout/02-08-PLAN.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Persist and safely rehydrate one complete held-cart snapshot</name>
  <reversibility rating="costly">The local persisted payload becomes a versioned checkout recovery contract; it remains recoverable through a forward migration or by clearing this local-only key.</reversibility>
  <read_first>src/entities/tab/model/cartStore.ts; src/entities/tab/model/cartStore.test.ts; src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx; src/entities/tab/model/store.ts; src/shared/lib/domain.ts; .planning/phases/02-core-direct-sale-checkout/02-05-SUMMARY.md</read_first>
  <files>src/entities/tab/model/cartStore.ts, src/entities/tab/model/cartStore.test.ts, src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx</files>
  <behavior>
    - Start with one held cart containing a sold-by-weight line, then simulate a fresh store hydration: its product, selected modifiers, quantity, `weightGrams`, `unitPrice`, `lineTotal`, and notes restore exactly.
    - The serialized state contains only `heldCart`; an active `items` cart never becomes restart state.
    - A missing, malformed, or obsolete persisted payload hydrates to no held cart rather than an incomplete cart; a valid versioned payload remains readable through the store migration path.
    - `resumeHeld()` and `discardHeld()` clear the persisted held slot while preserving the existing active/held swap behavior.
    - Calling `holdCart()` while `heldCart` already contains a sale leaves the existing held cart and current active cart unchanged; the Checkout Hold button is disabled whenever that slot is occupied.
  </behavior>
  <action>Write the Vitest cases first, using the jsdom local-storage API and a fresh module/store hydration boundary to model a WebView restart. Add a store regression case that puts different carts in `items` and `heldCart`, calls `holdCart()`, and asserts neither array is changed. In `cartStore.ts`, make `holdCart()` return the current state without writing when `heldCart !== null`; this is the authoritative D-01 one-slot guard and must retain the original held sale instead of replacing it. In `CheckoutPanel.tsx`, select `heldCart` and keep the existing Hold control disabled when `heldCart !== null`, in addition to its empty-cart condition, so the UI exposes the same one-slot contract.

Extend `useCartStore` with the already-installed `zustand/middleware` `persist` wrapper, using one dedicated stable key such as `direct-sale-held-cart`, a numeric store version, and `partialize` that serializes only `heldCart`. Keep `items`, actions, derived selectors, payment-attempt state, and all server state out of storage.

Treat the held cart as a complete `CartItem[]` snapshot: import the existing `CartItemSchema` and use a narrow local persisted-envelope schema/normalizer to accept only `{ heldCart: CartItem[] | null }`. Apply that normalizer in the version migration and hydration merge so missing, malformed, or incompatible browser storage resolves to `heldCart: null` and logs only a technical event. Do not add a dependency, database table, RPC, stock reservation, or payment path. The guarded `holdCart`, `resumeHeld`, and `discardHeld` actions remain the only transitions; persist middleware must automatically write `null` after resume/discard and retain the lossless non-empty-cart swap from Plan 02-05. Preserve the product and captured price snapshot for display/resume, while leaving Plan 02-06's server-derived pricing as the authority when payment is later attempted (D-04).</action>
  <verify><automated>npx vitest run src/entities/tab/model/cartStore.test.ts &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>A fresh store instance rehydrates exactly one valid held weighted cart from local storage, ignores invalid/obsolete data safely, does not restore an active cart, and writes no held slot after manual resume or discard. A direct second `holdCart()` call preserves the original held cart and active cart, while Checkout disables Hold whenever the slot is occupied.</acceptance_criteria>
  <done>The client has a versioned, schema-validated one-slot held-sale recovery record without any database or inventory side effect (D-01, D-03, D-04, D-05).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove reload, weighted resume, manual clear, and no-reservation behavior</name>
  <precondition>The local Supabase stack, seeded cashier, open Caja, and cashier shift required by `e2e/52-loose-weight-hold-sale.spec.ts` are available after Plan 02-08.</precondition>
  <read_first>e2e/52-loose-weight-hold-sale.spec.ts; e2e/helpers/auth.ts; e2e/helpers/supabase.ts; src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx; src/features/hold-sale/ui/HoldSaleBanner.tsx; src/entities/tab/model/cartStore.ts; src/entities/tab/model/cartStore.test.ts</read_first>
  <files>e2e/52-loose-weight-hold-sale.spec.ts</files>
  <behavior>
    - A cashier adds a weighted product, holds it, performs a full document reload, and sees the held badge plus the unchanged weighted line and price after Resume.
    - After reload, a different active cart can be started but Hold is disabled while the original slot is occupied; Resume still returns the original held cart rather than replacing it with the active cart.
    - Resume returns the original cart to active state and swaps the second cart into the held slot; resuming again returns the original cart to the held slot, then Discard clears it and another reload shows no held banner.
    - Before payment, the hold/reload/resume/discard sequence leaves the weighted product inventory and the relevant order, payment, and stock-movement counts unchanged.
  </behavior>
  <action>Add one focused Playwright scenario to the existing loose-weight suite. Seed a sold-by-weight fixture product and gram inventory, capture its inventory plus order/payment/stock-movement baselines, then use the existing checkout UI to enter a non-zero decimal weight and Hold it. Use `page.reload()` as the WebView document-restart boundary; wait for persisted-state hydration and assert the held badge is visible. Start a different active cart, assert that its visible Hold button is disabled while the original slot is occupied, and then Resume. Assert that the original weighted product, exact kilogram label, and captured price/total are now active while the second cart is held; this proves a second Hold cannot replace the rehydrated slot. Resume again so the original cart is held, use the existing non-destructive Discard confirmation, reload again, and assert that no held banner remains.

After each pre-payment phase, query the service-role fixtures to prove inventory is unchanged and no order, payment, or stock-movement record was created by persistence or hydration. Do not make a payment in this scenario and do not add a reservation assertion that requires a new database model; D-04 is proven by the absence of those effects. Keep the existing in-memory hold/swap specs and broaden neither the checkout flow nor the number of hold slots.</action>
  <verify><automated>npx playwright test e2e/52-loose-weight-hold-sale.spec.ts -g "survives.*reload|resuming a held sale swaps" &amp;&amp; npm run lint &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>The targeted browser test proves a weighted held sale survives reload, keeps Hold disabled for a second active cart, resumes the original slot without replacement, can only be manually resumed/swapped or discarded, and produces no pre-payment stock or financial records.</acceptance_criteria>
  <done>Playwright provides an end-to-end restart/recovery proof for the full held weighted cart and its manual-only no-reservation lifecycle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser local storage → Zustand cart store | A persisted browser value can be stale, malformed, or modified before checkout renders it. |
| Rehydrated held cart → payment RPC | Client-side snapshot prices and grams cross into the already-authoritative direct-sale payment boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09-01 | Tampering | `direct-sale-held-cart` local record | medium | mitigate | Persist only the one slot and validate the complete envelope with `CartItemSchema`; invalid/obsolete records restore as no held sale (Task 1). |
| T-02-09-02 | Tampering | rehydrated price/weight fields | high | mitigate | Preserve snapshot fields only for client resume; keep Plan 02-06 server-derived product price, weight bounds, and atomic payment as the write authority (Task 1). |
| T-02-09-03 | Information disclosure | shared-terminal local storage | low | accept | The user explicitly requires restart recovery; store only one uncommitted cart, no credentials or payment data, and clear it through existing manual transitions (Tasks 1–2). |
| T-02-09-04 | Repudiation | pre-payment held lifecycle | medium | mitigate | Playwright snapshots prove hold/reload/resume/discard creates no order, payment, inventory, or stock movement before payment (Task 2, D-04). |
| T-02-09-SC | Tampering | package installs | high | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run the cart-store Vitest suite, the focused reload/swap Playwright cases with local Supabase, then lint and type-checking. The browser test is the restart-equivalent WebView proof because it tears down and reinitializes the SPA document while retaining native local storage.
</verification>

<success_criteria>
The only held sale survives an app restart with its full weighted cart data, remains a single manually resume/clear slot, and never reserves or mutates stock before an actual payment.
</success_criteria>

<output>
Create `.planning/phases/02-core-direct-sale-checkout/02-09-SUMMARY.md` when done.
</output>
