---
phase: 02-core-direct-sale-checkout
plan: 08
type: execute
wave: 2
depends_on: [02-06]
files_modified:
  - src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx
  - src/widgets/CheckoutPanel/ui/CheckoutPanel.test.tsx
  - src/features/scan-barcode-to-cart/model/useScanBarcodeToCart.ts
  - src/features/scan-barcode-to-cart/model/useScanBarcodeToCart.test.ts
  - src/features/lookup-product-by-barcode/model/useLookupProductByBarcode.ts
  - e2e/51-barcode-scan-search.spec.ts
  - e2e/52-loose-weight-hold-sale.spec.ts
autonomous: true
gap_closure: true
requirements: [CHK-01, CHK-05]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A scan received while payment, the completed-sale receipt, or either weight-entry dialog is active cannot be added to a cart that is about to clear."
    - "The complete loose-weight add, edit, and payment flow is runnable through an unambiguous decimal-key selector."
    - "A paid loose-weight sale persists integer grams and decrements weighted inventory and its stock-movement record by those exact grams."
    - "A barcode cache miss returns only an active catalogue product."
  artifacts:
    - path: "src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx"
      provides: "Checkout-state scanner gate"
      contains: "useScanBarcodeToCart"
    - path: "e2e/52-loose-weight-hold-sale.spec.ts"
      provides: "Direct-RPC gram-decrement and browser add/edit/pay checkout regressions"
      contains: "WEIGHT_GRAMS"
  key_links:
    - from: "CheckoutPanel"
      to: "useScanBarcodeToCart"
      via: "enabled argument derived from payment and both weight-dialog states"
      pattern: "useScanBarcodeToCart\\("
    - from: "useLookupProductByBarcode"
      to: "products"
      via: "cache-miss barcode query constrained to active products"
      pattern: "is_active"
    - from: "process_direct_sale_atomic"
      to: "decrement_inventory_on_order_item"
      via: "the paid transaction inserts an order item with integer `weight_grams`, which the existing weighted trigger applies directly to inventory and stock_movements"
      pattern: "weight_grams"
---

<objective>
Prevent destructive checkout-state scanner races and restore automated loose-weight proof.

Purpose: a next-sale scan can currently enter the previous cart during payment or receipt completion, then be cleared; the required weighted browser flow fails before its assertions.
Output: State-gated scanner behavior plus focused scanner and loose-weight regressions at both the database-RPC and browser-flow levels.

Artifacts this phase produces:
- scanner-state-safe `CheckoutPanel`
- `e2e/51-barcode-scan-search.spec.ts` receipt-state race coverage
- repaired `e2e/52-loose-weight-hold-sale.spec.ts`
</objective>

<execution_context>
@/home/widowsvail/.codex/gsd-core/workflows/execute-plan.md
@/home/widowsvail/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-direct-sale-checkout/02-CONTEXT.md
@.planning/phases/02-core-direct-sale-checkout/02-VERIFICATION.md
@.planning/phases/02-core-direct-sale-checkout/02-05-SUMMARY.md
@.planning/phases/02-core-direct-sale-checkout/02-06-SUMMARY.md
@src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx
@src/features/scan-barcode-to-cart/model/useScanBarcodeToCart.ts
@src/features/lookup-product-by-barcode/model/useLookupProductByBarcode.ts
@src/features/add-loose-weight-item/model/useAddLooseWeightItem.ts
@src/features/add-loose-weight-item/ui/WeightEntryDialog.tsx
@e2e/51-barcode-scan-search.spec.ts
@e2e/52-loose-weight-hold-sale.spec.ts
@supabase/migrations/20260814000001_loose_weight_items.sql
@supabase/migrations/20260816000001_direct_sale_authoritative_totals.sql

<interfaces>
`useAddLooseWeightItem()` exposes `isOpen`; `CheckoutPanel` separately holds `paymentOpen` and `editingWeightItemId`. `useScanBarcodeToCart(enabled, onWeightedProduct)` forwards scans immediately when enabled. `WeightEntryDialog`'s keypad decimal button has visible name `.` while its back button has the localized `common:actions.back` accessible label.

For `products.sold_by_weight = true`, `inventory.quantity_on_hand` is an integer gram count. `process_direct_sale_atomic` stores the browser's bounded integer `weight_grams`; its price calculation converts grams to kilograms before rounding currency to cents, while `decrement_inventory_on_order_item` subtracts the stored integer grams and writes the same negative integer to `stock_movements`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Gate one receipt-screen scanner attempt before cart completion</name>
  <read_first>src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx; src/features/scan-barcode-to-cart/model/useScanBarcodeToCart.ts; src/features/add-loose-weight-item/model/useAddLooseWeightItem.ts; e2e/51-barcode-scan-search.spec.ts</read_first>
  <files>src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx, src/widgets/CheckoutPanel/ui/CheckoutPanel.test.tsx, src/features/scan-barcode-to-cart/model/useScanBarcodeToCart.ts, src/features/scan-barcode-to-cart/model/useScanBarcodeToCart.test.ts, e2e/51-barcode-scan-search.spec.ts</files>
  <behavior>
    - While PaymentForm or its receipt view is mounted, a simulated scanner burst does not add an item to the active cart.
    - While add-weight or edit-weight dialog is open, a simulated scanner burst does not add an item or open a second dialog.
    - Returning to the normal checkout state restores ordinary known-barcode cart addition.
    - A lookup that began while scanning was enabled cannot add an item or open weight entry after checkout disables scanning before the lookup resolves.
  </behavior>
  <action>Derive the scanner `enabled` argument from checkout state: it is false while payment/receipt UI is active, while `weightEntry.isOpen`, and while an edit-weight dialog is selected; it is true only for the ordinary cart screen. Keep the hook mounted and do not queue scans, because discarding input while a modal owns the register prevents it from being added to a cart that completion clears. Ensure the hook ignores an already-started async lookup when the current enabled state becomes false before it calls cart or weight-entry actions. Add colocated Vitest coverage in `CheckoutPanel.test.tsx` that verifies the modal-derived scanner gate and normal-state re-enable, and in `useScanBarcodeToCart.test.ts` that resolves a pending lookup after disable and proves neither cart nor weight-entry action fires. Add the retained Playwright regression that reaches a receipt state, emits a scanner-style barcode input, selects Done, and proves the cleared/fresh cart contains no scanned line; add equivalent state coverage for weight entry and normal-state scanning.</action>
  <verify><automated>npx vitest run src/widgets/CheckoutPanel/ui/CheckoutPanel.test.tsx src/features/scan-barcode-to-cart/model/useScanBarcodeToCart.test.ts &amp;&amp; npx playwright test e2e/51-barcode-scan-search.spec.ts -g "receipt|weight|scan" &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>Checkout completion cannot discard a scan that was accepted while a payment or modal state was active, because that state accepts no scanner cart action.</acceptance_criteria>
  <done>The receipt-screen and weight-dialog scanner regressions pass alongside the existing normal scan path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make weighted checkout and cache-miss catalogue filtering deterministic</name>
  <read_first>e2e/52-loose-weight-hold-sale.spec.ts; src/features/add-loose-weight-item/ui/WeightEntryDialog.tsx; src/features/lookup-product-by-barcode/model/useLookupProductByBarcode.ts; e2e/51-barcode-scan-search.spec.ts</read_first>
  <files>e2e/52-loose-weight-hold-sale.spec.ts, src/features/lookup-product-by-barcode/model/useLookupProductByBarcode.ts, e2e/51-barcode-scan-search.spec.ts</files>
  <behavior>
    - The loose-weight scenario selects only the keypad decimal control and completes add, edit, cash payment, and inventory assertion.
    - A service-role direct sale of 375 grams stores `weight_grams = 375`, changes 1,000 grams on hand to 625, and records a `-375` stock movement; deleting that order item restores 1,000 grams.
    - A barcode absent from the TanStack cache does not map a product whose active flag is false.
  </behavior>
  <action>After 02-06 has applied its forward migration, repair the loose-weight Playwright selector by using the decimal keypad button's exact accessible name, leaving the back-button control untouched. Preserve and update the existing service-role direct-RPC test so its tender total follows the configured billing tax contract introduced by 02-06; seed a weighted product with 1,000 grams, sell 375 grams, and assert the persisted `order_items.weight_grams`, `inventory.quantity_on_hand`, and `stock_movements.quantity_delta` are exactly 375, 625, and -375 respectively before the existing restore assertion. This is the database-level proof: the client supplies bounded integer grams, the SQL price calculation converts grams to kilograms and rounds currency cents, and the existing trigger applies raw integer grams with no stock-unit conversion or rounding. Run the full browser sequence of distinct weighted lines, edit, payment, receipt, and exact 10,000-to-5,490 gram inventory decrement; retain its already-opened piece-product boundary from D-09 rather than adding case-opening behavior. On the barcode fallback query, add the active-product predicate alongside barcode matching and add a cache-miss E2E fixture proving an inactive barcode is treated as unmatched and takes the existing D-10 toast/audit path.</action>
  <verify><automated>supabase db push --local &amp;&amp; npx playwright test e2e/52-loose-weight-hold-sale.spec.ts -g "decrements and restores inventory by the exact grams sold|adds distinct weighted lines and edits one before checkout" &amp;&amp; npx playwright test e2e/51-barcode-scan-search.spec.ts -g "inactive|cache|not found" &amp;&amp; npm run lint &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>The applied forward RPC retains the persisted gram-decrement contract under an exact direct-RPC assertion, the browser flow reaches its 5,490-gram inventory assertion, and an inactive cache-miss product is not sellable by barcode.</acceptance_criteria>
  <done>CHK-05's exact database and add/edit/pay regressions, plus the small CHK-01 inactive-product closure, have automated passing proof against the applied schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| USB-HID scanner → checkout cart | External keyboard-wedge input can arrive during a destructive payment transition. |
| Barcode lookup → product catalogue | A direct database fallback converts an external code into a sellable product. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08-01 | Tampering | scanner/cart transition | high | mitigate | Gate scanner actions by payment, receipt, and both weight-dialog states; prove the receipt race in Playwright. |
| T-02-08-02 | Tampering | barcode fallback lookup | medium | mitigate | Filter the cache-miss query to active products and prove inactive codes follow the unmatched flow. |
| T-02-08-03 | Denial | scanner lookup completion | low | accept | The existing one-at-a-time scanner hook has bounded barcode input; modal gating prevents destructive state changes. |
| T-02-08-SC | Tampering | package installs | high | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run the targeted scanner, inactive fallback, and loose-weight Playwright cases followed by lint and type-checking.
</verification>

<success_criteria>
CHK-01 and CHK-05 remain safe across modal transitions, and the once-unrunnable full loose-weight checkout E2E is automated.
</success_criteria>

<output>
Create `.planning/phases/02-core-direct-sale-checkout/02-08-SUMMARY.md` when done.
</output>
