---
phase: 02-core-direct-sale-checkout
plan: 07
type: execute
wave: 2
depends_on: [02-06]
files_modified:
  - supabase/functions/process-direct-sale/index.ts
  - src/shared/lib/edge-function-contracts.ts
  - src/features/checkout-sale/model/useCheckoutSale.ts
  - src/widgets/PaymentModal/ui/PaymentForm.tsx
  - src/shared/lib/receipt-format.ts
  - e2e/50-direct-sale-checkout.spec.ts
autonomous: true
gap_closure: true
requirements: [CHK-03, CHK-04]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The authenticated cashier receives receipt data only for a sale returned by their authorized direct-sale invocation."
    - "A split payment prints and displays one sale-level receipt containing the basket once and every tender leg."
    - "A receipt's sale total is distinct from tender amounts, cash tendered, change, and tips."
  artifacts:
    - path: "supabase/functions/process-direct-sale/index.ts"
      provides: "Authenticated and attribution-filtered sale-level receipt response"
      contains: "buildSaleReceipt"
    - path: "src/shared/lib/edge-function-contracts.ts"
      provides: "Receipt wire contract with tender-leg details"
      contains: "tenders"
  key_links:
    - from: "supabase/functions/process-direct-sale/index.ts"
      to: "process_direct_sale_atomic"
      via: "authorized RPC result followed by staff/shift/Caja-filtered receipt reads"
      pattern: "process_direct_sale_atomic"
    - from: "src/widgets/PaymentModal/ui/PaymentForm.tsx"
      to: "src/shared/lib/receipt-format.ts"
      via: "one ReceiptData passed once to printReceipt and ReceiptPreview"
      pattern: "printReceipt"
---

<objective>
Return an authorized, truthful sale-level receipt for direct and split payment.

Purpose: the edge wrapper currently re-reads service-role data without owner filters and generates contradictory basket totals for each split leg.
Output: Sale-level receipt contract, authorized Edge Function response, and a single-print checkout flow.

Artifacts this phase produces:
- `supabase/functions/process-direct-sale/index.ts`
- `src/shared/lib/edge-function-contracts.ts`
- direct-sale receipt and Playwright regression coverage
</objective>

<execution_context>
@/home/widowsvail/.codex/gsd-core/workflows/execute-plan.md
@/home/widowsvail/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-direct-sale-checkout/02-VERIFICATION.md
@.planning/phases/02-core-direct-sale-checkout/02-06-SUMMARY.md
@supabase/functions/process-direct-sale/index.ts
@src/shared/lib/edge-function-contracts.ts
@src/features/checkout-sale/model/useCheckoutSale.ts
@src/widgets/PaymentModal/ui/PaymentForm.tsx
@src/shared/lib/receipt-format.ts
@e2e/50-direct-sale-checkout.spec.ts

<interfaces>
`ReceiptDataSchema` currently contains one basket and one `paymentMethod`. `ProcessDirectSaleSuccessSchema` currently permits a `receipts` array for split sales. `PaymentForm` queues that array and calls `printReceipt` once per element. Plan 02-06 makes the RPC replay result authorized before this plan consumes it.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Return a cashier-scoped sale receipt with all split tender legs</name>
  <reversibility rating="costly">The internal Edge Function receipt payload gains sale-level tender data consumed by checkout, printing, and email paths; changing it later requires coordinated client updates.</reversibility>
  <read_first>supabase/functions/process-direct-sale/index.ts; src/shared/lib/edge-function-contracts.ts; src/features/void-order/model/useVoidOrder.ts; supabase/migrations/20260816000001_direct_sale_authoritative_totals.sql; e2e/50-direct-sale-checkout.spec.ts</read_first>
  <files>supabase/functions/process-direct-sale/index.ts, src/shared/lib/edge-function-contracts.ts, src/features/checkout-sale/model/useCheckoutSale.ts, e2e/50-direct-sale-checkout.spec.ts</files>
  <behavior>
    - A valid split sale returns one receipt whose basket subtotal/total is calculated once and whose tender list contains every payment leg with method, amount, tip, tendered value, change, and terminal reference where applicable.
    - Replaying a known key under another cashier cannot obtain receipt data or IDs from the Edge Function.
    - Replaying a key for the original cashier with a currently valid Caja and owned shift returns the same sale-level receipt without another payment or stock decrement.
  </behavior>
  <action>Replace per-leg `buildReceipt` calls with one `buildSaleReceipt` path. After the authorized RPC result, make every service-role receipt query filter by the authenticated staff ID plus the supplied shift and Caja attribution, and treat an empty filtered result as an authorization failure without returning tab, payment, or receipt identifiers. Load all payment rows in split-index order and compute sale merchandise subtotal from the persisted order items, sale tip from all legs, and sale total from the authoritative persisted payment group; include an optional `tenders` array in the shared receipt contract. Preserve existing single-tender fields for compatibility by deriving them from the sole tender, but omit the old split `receipts` array from direct-sale success responses. Do not trust the request's items, total, tender legs, or receipt fields when composing the response.

Add focused Edge/live-Supabase checks for original-cashier replay, other-cashier replay denial, and a two-leg receipt whose basket appears once while tender rows sum to its paid total. Update `useCheckoutSale` to consume the one direct-sale `receiptData` value for both direct and split processor adapters, preserving the mapped `useVoidOrder` Result return, logger, and mutation error-handling pattern.</action>
  <verify><automated>npx playwright test e2e/50-direct-sale-checkout.spec.ts -g "split.*receipt|replay" &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>A split sale has a single internally consistent receipt payload, and a receipt response is reachable only from the matching cashier's current authorized checkout context.</acceptance_criteria>
  <done>Focused tests prove truthful split receipt composition and block cross-cashier replay disclosure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Display and print the sale receipt once</name>
  <read_first>src/widgets/PaymentModal/ui/PaymentForm.tsx; src/shared/lib/receipt-format.ts; src/shared/lib/edge-function-contracts.ts; src/widgets/PaymentModal/ui/PaymentForm.test.tsx</read_first>
  <files>src/widgets/PaymentModal/ui/PaymentForm.tsx, src/shared/lib/receipt-format.ts, src/widgets/PaymentModal/ui/PaymentForm.test.tsx</files>
  <behavior>
    - Completing a split tender invokes printing once with the sale-level receipt, not once per tender.
    - The receipt presentation shows every tender and keeps basket total separate from paid/tendered/change values.
  </behavior>
  <action>Remove the split receipt queue/index state and render the single direct-sale receipt through the existing `ReceiptPreview` path. Print that receipt once after successful split payment, retaining the cash-drawer behavior when at least one tender is cash. Extend receipt formatting with concise tender-leg lines so methods and amounts are observable without repeating items or basket totals. Keep D-12's automatic print plus email/skip behavior and D-14's existing multi-tender entry UI; do not introduce a new payment flow.</action>
  <verify><automated>npx vitest run src/widgets/PaymentModal/ui/PaymentForm.test.tsx src/shared/lib/receipt-format.test.ts &amp;&amp; npm run lint &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>The completed-sale screen and print invocation have exactly one sale-level basket representation and enumerate every tender leg.</acceptance_criteria>
  <done>Unit tests and the direct-sale E2E suite prove one truthful receipt is shown and attempted for a split sale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edge Function service-role reads → receipt JSON | Privileged database reads become data sent to a cashier browser. |
| Payment group → receipt formatter | Multiple tender records must not be represented as duplicate merchandise sales. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07-01 | Information disclosure | `process-direct-sale` receipt reads | high | mitigate | Filter every receipt query by authenticated staff and valid shift/Caja attribution following the authorized RPC result. |
| T-02-07-02 | Tampering | split receipt calculation | high | mitigate | Construct one sale receipt from persisted items and all persisted tender legs, never request payload values. |
| T-02-07-03 | Repudiation | printed sale receipt | medium | mitigate | Include all persisted tender methods/amounts and preserve payment identifiers in the backend response. |
| T-02-07-SC | Tampering | package installs | high | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run the focused replay/split Playwright tests, receipt-format and PaymentForm Vitest suites, lint, and type-checking. Physical printer output remains the only post-automation hardware check from the verification report.
</verification>

<success_criteria>
CHK-04's completed split payment produces one truthful receipt that can be automatically printed without leaking another cashier's sale.
</success_criteria>

<output>
Create `.planning/phases/02-core-direct-sale-checkout/02-07-SUMMARY.md` when done.
</output>
