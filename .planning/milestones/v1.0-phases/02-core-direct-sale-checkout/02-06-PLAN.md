---
phase: 02-core-direct-sale-checkout
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260816000001_direct_sale_authoritative_totals.sql
  - e2e/50-direct-sale-checkout.spec.ts
autonomous: false
gap_closure: true
requirements: [CHK-03, CHK-04]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A direct cash, card, or split sale derives catalogue prices, allowed modifiers, tax, and tender required by the sale inside the transaction; every direct-sale client discount field is rejected because no server-owned discount eligibility source exists."
    - "A submitted amount, expected split amount, or cash tender that disagrees with the derived values leaves no sale, payment, or inventory effect."
    - "An idempotency replay succeeds only for the same cashier after their current Caja and owned shift have been validated."
  artifacts:
    - path: "supabase/migrations/20260816000001_direct_sale_authoritative_totals.sql"
      provides: "Authoritative direct-sale calculation and replay authorization in process_direct_sale_atomic"
      contains: "CREATE OR REPLACE FUNCTION public.process_direct_sale_atomic"
    - path: "e2e/50-direct-sale-checkout.spec.ts"
      provides: "Adversarial live-Supabase coverage for monetary authority and replay authorization"
      contains: "authoritative"
  key_links:
    - from: "supabase/functions/process-direct-sale/index.ts"
      to: "supabase/migrations/20260816000001_direct_sale_authoritative_totals.sql"
      via: "service-role RPC call with the authenticated staff ID"
      pattern: "process_direct_sale_atomic"
    - from: "process_direct_sale_atomic"
      to: "settings, products, product_modifiers, modifiers, payments"
      via: "locked server-side catalogue and billing reads before sale inserts, with non-null direct-sale discount inputs rejected before persistence"
      pattern: "FOR UPDATE|taxRatePercent"
    - from: "process_direct_sale_atomic"
      to: "p_discount_scope, p_discount_type, p_discount_value, p_discount_amount"
      via: "SQL-boundary rejection of every browser-supplied direct-sale discount field"
      pattern: "DISCOUNT_UNSUPPORTED"
---

<objective>
Close the financial-authority and replay-authorization blockers at the SQL trust boundary.

Purpose: CHK-03 and the receipt path cannot be reliable while browser-provided monetary fields decide whether a sale is paid.
Output: A new forward migration, applied schema, and live adversarial checkout coverage.

Artifacts this phase produces:
- `supabase/migrations/20260816000001_direct_sale_authoritative_totals.sql`
- strengthened `e2e/50-direct-sale-checkout.spec.ts`
</objective>

<execution_context>
@/home/widowsvail/.codex/gsd-core/workflows/execute-plan.md
@/home/widowsvail/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-direct-sale-checkout/02-CONTEXT.md
@.planning/phases/02-core-direct-sale-checkout/02-VERIFICATION.md
@.planning/phases/02-core-direct-sale-checkout/02-04-SUMMARY.md
@supabase/migrations/20260815000001_direct_sale_atomic_paid_and_shift_guard.sql
@supabase/migrations/20260810000003_drop_pool_resources.sql
@supabase/migrations/20260414000003_products_and_categories.sql
@supabase/migrations/20260419000001_settings_and_backups.sql
@e2e/helpers/supabase.ts

<interfaces>
`process_direct_sale_atomic` is service-role-only and currently accepts `p_items`, one direct-payment tuple or `p_legs`, `p_expected_total`, and discount fields. `PaymentForm` submits a tax-inclusive amount and separate tip. `products.base_price`, `product_modifiers`, `modifiers.price_delta`, and the `settings` row whose key is `billing` are the existing sources of truth.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the forward SQL contract for authoritative direct-sale totals</name>
  <read_first>.planning/phases/02-core-direct-sale-checkout/02-VERIFICATION.md; supabase/migrations/20260815000001_direct_sale_atomic_paid_and_shift_guard.sql; src/entities/settings/model/queries.ts</read_first>
  <files>supabase/migrations/20260816000001_direct_sale_authoritative_totals.sql</files>
  <decision>Approve the forward migration that makes `process_direct_sale_atomic` calculate direct-sale prices, modifier deltas, discounts, billing tax, tender requirements, and replay authorization from database state.</decision>
  <context>This changes a privileged database function and its persisted accounting behavior; undoing it after sales exist requires another migration and reconciliation of its public edge-function contract.</context>
  <options>
    <option id="authoritative-direct-sale">
      <name>Authoritative SQL totals</name>
      <pros>Closes the verified underpayment and client-price trust-boundary failures in the transaction that creates stock movement and payment records.</pros>
      <cons>Existing direct-sale requests that disagree with current catalogue or billing settings are rejected until the client is corrected.</cons>
    </option>
  </options>
  <resume-signal>Select: authoritative-direct-sale</resume-signal>
  <action>Proceed only after selecting `authoritative-direct-sale`; this preserves D-04's payment-time stock rule and D-07's catalogue-price rule without adding a separate pricing table.</action>
  <verify><automated>test -f supabase/migrations/20260815000001_direct_sale_atomic_paid_and_shift_guard.sql</automated></verify>
  <acceptance_criteria>The approved implementation path is explicit before the one-way database contract is changed.</acceptance_criteria>
  <done>The decision gate has an `authoritative-direct-sale` approval.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Derive and enforce one direct-sale payment total in the transaction</name>
  <reversibility rating="one-way">The privileged RPC's persisted accounting contract changes through a forward migration; reverting after live sales requires a compensating migration and accounting review.</reversibility>
  <read_first>supabase/migrations/20260815000001_direct_sale_atomic_paid_and_shift_guard.sql; supabase/migrations/20260810000003_drop_pool_resources.sql; supabase/migrations/20260814000002_weighted_direct_sale_price.sql; supabase/migrations/20260414000003_products_and_categories.sql; supabase/migrations/20260419000001_settings_and_backups.sql; e2e/50-direct-sale-checkout.spec.ts</read_first>
  <files>supabase/migrations/20260816000001_direct_sale_authoritative_totals.sql, e2e/50-direct-sale-checkout.spec.ts</files>
  <behavior>
    - A cash and a card request whose amount differs from the transaction's derived amount, including a card override below it, returns an error and writes neither payment nor sale rows.
    - A split request whose expected total or leg sum differs from the derived amount rolls back as one transaction.
    - Modified item prices and modifier deltas are fetched from active catalogue relationships, rather than inserted from browser values.
    - A request containing any direct-sale discount scope, type, value, or amount returns `DISCOUNT_UNSUPPORTED` and writes no payment, sale, or inventory state.
    - A replay from another cashier, closed Caja, or unowned/closed shift returns an authorization error without identifiers for an existing sale.
  </behavior>
  <action>Create the forward migration. Redefine only `process_direct_sale_atomic`; retain the proven generic payment RPCs instead of duplicating them. Lock and validate the supplied Caja and staff-owned open shift before looking up an idempotency key. For an existing key, constrain the replay record to the authenticated staff member, supplied active shift, and Caja; return a generic authorization error for a mismatch.

For each input item, lock an active product, derive packaged or gram-weighted base pricing from `products.base_price`, verify every modifier ID belongs to that product through `product_modifiers`, and sum `modifiers.price_delta` on the server. Persist those derived values, never client price or delta. Read `settings.value->>'taxRatePercent'` for the `billing` record with the existing 16-percent fallback; calculate tax from the subtotal and the payment total. Phase 1 removed the promotions tables and this phase has no server-owned discount eligibility source, so reject any non-null `p_discount_scope`, `p_discount_type`, `p_discount_value`, or `p_discount_amount` with `DISCOUNT_UNSUPPORTED` before any insert, then pass NULL discount inputs to the generic payment RPCs. Require direct `p_amount` and split `p_expected_total`/sum of leg amounts to agree with the derived result within one cent; require each cash tender to cover its own amount plus tip. Pass only the validated, derived payment inputs to the existing atomic payment function and retain the paid-tab rollback guard. Keep the existing weight bounds and payment-time atomic inventory behavior from D-04 and D-07.

Write adversarial Playwright/live-Supabase test cases first for altered item price, forged modifier delta, a non-null direct-sale discount payload, changed billing-tax amount, below-total card override, altered split expected total, and a replay under a different cashier. Have each case assert the before/after tab, payment, order-item, and inventory counts/quantities needed to prove transaction rollback; do not test only a returned error envelope. Do not run the database-backed cases in this task: Task 3 applies this migration before that proof.</action>
  <verify><automated>git diff --check -- supabase/migrations/20260816000001_direct_sale_authoritative_totals.sql e2e/50-direct-sale-checkout.spec.ts &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>The forward migration and the adversarial test cases define the required authoritative-total and replay behavior without relying on an unapplied local schema.</acceptance_criteria>
  <done>The migration and focused live-Supabase test cases are authored and type-check cleanly, ready for schema application and execution in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Apply the schema before executing database-backed verification</name>
  <precondition>The local Supabase stack is running and its migration history is reachable by the repository's Supabase CLI.</precondition>
  <read_first>supabase/migrations/20260816000001_direct_sale_authoritative_totals.sql; supabase/config.toml; e2e/helpers/requireEnv.ts</read_first>
  <files>supabase/migrations/20260816000001_direct_sale_authoritative_totals.sql</files>
  <action>Run the repository's local schema-push command to apply the new migration before executing the Playwright cases authored in Task 2. Inspect migration history and the deployed `process_direct_sale_atomic` definition after the push so the test is exercising the forward migration rather than a stale local function. Execute the focused adversarial suite only after that inspection; it must prove that forged monetary requests cannot close or mutate a sale, valid direct sale remains payable, and unauthorized replays expose no original-sale IDs. If the schema push fails, stop before behavioral verification and report the database error; do not emulate the migration through ad-hoc SQL.</action>
  <verify><automated>supabase db push --local &amp;&amp; npx playwright test e2e/50-direct-sale-checkout.spec.ts -g "authoritative|underpayment|discount|replay" &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>The local database has recorded the migration, and the focused suite proves the authoritative-total and replay behavior against its resulting function definition.</acceptance_criteria>
  <done>The schema push completes before focused end-to-end verification, and the applied function passes the live adversarial proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → process-direct-sale Edge Function → security-definer RPC | Untrusted item, modifier, tender, discount, and replay-key fields reach privileged financial writes. |
| Existing idempotency payment row → replay response | A globally unique client key may be presented by another authenticated cashier. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06-01 | Tampering | `process_direct_sale_atomic` | high | mitigate | Derive locked catalogue/modifier/tax totals, reject every direct-sale discount field without an authoritative source, and compare each tender input before writing. |
| T-02-06-02 | Elevation | idempotency replay | high | mitigate | Validate open Caja and owned shift before lookup; scope any replay to the original cashier and attribution. |
| T-02-06-03 | Repudiation | payment records | medium | mitigate | Retain the existing payment audit calls and test rollback leaves no financial record. |
| T-02-06-SC | Tampering | package installs | high | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run `supabase db push --local` before the focused live-Supabase Playwright adversarial cases, then run `npm run typecheck`.
</verification>

<success_criteria>
CHK-03's checkout payment cannot be underpaid or repriced by a browser payload, and its replay path is authorized before it returns any sale identity.
</success_criteria>

<output>
Create `.planning/phases/02-core-direct-sale-checkout/02-06-SUMMARY.md` when done.
</output>
