---
phase: 28-promotion-management-redesign
plan: 03
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - src/shared/ui/MultiSelectPicker/MultiSelectPicker.tsx
  - src/shared/ui/MultiSelectPicker/MultiSelectPicker.stories.tsx
  - src/shared/ui/MultiSelectPicker/MultiSelectPicker.test.tsx
  - src/features/manage-promotions/ui/wizard/StepScope.tsx
  - src/features/manage-promotions/model/usePromotionWizardState.ts
  - src/features/manage-promotions/ui/PromotionWizardPage.tsx
  - src/shared/lib/i18n/locales/es-MX/wAdmin.json
  - src/shared/lib/i18n/locales/en-US/wAdmin.json
autonomous: true
requirements: [D-01, D-08]

estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "The Scope step lets an admin select multiple products and multiple categories in one promotion via a search-driven multi-select picker (D-01's UI surface, backed by Task 1's promotion_targets junction table)."
    - "Checking 'Store-wide (no restriction)' clears any selected targets and disables the picker; unchecking it re-enables selection with the previous selection cleared, never silently restored stale."
    - "Forward navigation from the Scope step is blocked while zero targets are selected and Store-wide is unchecked (D-08 partial — the full 4-step gate lands in 28-04)."
    - "Selected target chips overflow onto multiple lines inside a scrollable region once they exceed roughly 5 rows, and a long product/category name truncates with a native title tooltip (UI-SPEC overflow/long-text coverage)."
  artifacts:
    - src/shared/ui/MultiSelectPicker/MultiSelectPicker.tsx
    - src/features/manage-promotions/ui/wizard/StepScope.tsx
  key_links:
    - "StepScope.tsx writes selected productIds/categoryIds into usePromotionWizardState's targets field, which Task 1's save() already threads into useMutationCreatePromotion/useMutationUpdatePromotion's promotion_targets insert"
---

<objective>
Build the Scope step of the promotion wizard: a new `MultiSelectPicker` shared primitive (search-driven,
cmdk-based, grouped Products/Categories) replacing Plan 01's hardcoded "always store-wide" placeholder
with real multi-product + multi-category selection, plus a "Store-wide" override checkbox.

Purpose: This is the first genuinely new UI surface the phase delivers on top of Plan 01's schema/
backend tracer — proving the junction-table model end-to-end through a real admin workflow (select
targets, save, see them persist).
Output: `MultiSelectPicker` (new `shared/ui` primitive with a Storybook story, per this repo's
convention for new shared/ui components), `StepScope.tsx` wired into the wizard's Scope tab, and the
wizard-state hook extended with a scope-step validity predicate.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-promotion-management-redesign/28-CONTEXT.md
@.planning/phases/28-promotion-management-redesign/28-UI-SPEC.md
@.planning/phases/28-promotion-management-redesign/28-PATTERNS.md
@src/shared/ui/CategoryTreePicker/CategoryTreePicker.tsx
@src/shared/ui/command.tsx
@src/features/manage-promotions/model/usePromotionWizardState.ts
@src/features/manage-promotions/ui/PromotionWizardPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: MultiSelectPicker shared/ui primitive</name>
  <files>src/shared/ui/MultiSelectPicker/MultiSelectPicker.tsx, src/shared/ui/MultiSelectPicker/MultiSelectPicker.stories.tsx, src/shared/ui/MultiSelectPicker/MultiSelectPicker.test.tsx</files>
  <read_first>
    src/shared/ui/CategoryTreePicker/CategoryTreePicker.tsx (full file — the contract to mirror: a
    PURE presentation component that accepts a flat items array + controlled value/onChange, does NOT
    fetch data itself, is keyboard-accessible; this one uses role="tree"/"treeitem" for single-select —
    MultiSelectPicker mirrors the shape but with role="listbox"/"option" + aria-multiselectable={true}
    and value: string[] / onChange: (ids: string[]) => void)
    src/shared/ui/command.tsx (Command/CommandInput/CommandList/CommandItem/CommandGroup/CommandEmpty —
    the cmdk-wrapped primitive; CommandGroup groups items under a heading, CommandEmpty renders when a
    search yields zero results)
    src/shared/ui/checkbox.tsx (Checkbox primitive — each row inside the picker is a checkbox + label)
    src/shared/ui/popover.tsx (Popover/PopoverTrigger/PopoverContent — houses the Command list as a
    dropdown, matching CategoryTreePicker's own popover-based precedent per 28-UI-SPEC.md)
    src/shared/lib/category-tree.ts (buildTree/TreeNode helpers CategoryTreePicker already uses for
    parentId-depth indentation — reuse for MultiSelectPicker's category rows, do not duplicate)
    .planning/phases/28-promotion-management-redesign/28-UI-SPEC.md "Component Inventory" (MultiSelectPicker
    row) and "UI Considerations" table (empty/overflow/long-text rows — CommandEmpty for the empty
    state, flex-wrap + max-h-40 overflow-y-auto scroll region for chip overflow, max-w-[12rem] truncate
    + native title attribute for long chip labels)
  </read_first>
  <action>
    Create `src/shared/ui/MultiSelectPicker/MultiSelectPicker.tsx`: a pure presentation component with
    props `products: { id: string; name: string }[]`, `categories: { id: string; name: string;
    parentId: string | null | undefined }[]`, `selectedProductIds: string[]`, `selectedCategoryIds:
    string[]`, `onChange: (next: { productIds: string[]; categoryIds: string[] }) => void`, `disabled?:
    boolean`. Render a `Popover` whose trigger is a button showing "N selected" or a placeholder, and
    whose content is a `Command` with a `CommandInput` (search), two `CommandGroup`s ("Products" /
    "Categories" — i18n headings), each `CommandItem` rendering a `Checkbox` (checked state driven by
    whether the item's id is in the relevant selected array) + the item's name (categories indented by
    `buildTree`-derived depth, same indentPx formula CategoryTreePicker uses: `depth * 20`), and a
    `CommandEmpty` showing "No matches" / "Try a different product or category name." per the
    Copywriting Contract. Clicking a `CommandItem` toggles that id in/out of the relevant array (product
    vs category, decided by which group it's in) and calls `onChange`. Below the trigger, render the
    current selection as removable `Badge` chips (`flex flex-wrap gap-2` inside a `max-h-40
    overflow-y-auto` region once wrapped content exceeds ~5 rows; each chip label `max-w-[12rem]
    truncate` with a native `title` attribute carrying the full name; an inline `×` button on each chip
    removes just that id via the same `onChange` callback). Add a top-of-file doc comment matching
    CategoryTreePicker's convention (PURE presentation component, does NOT fetch data). Create
    `MultiSelectPicker.stories.tsx` (Storybook, required for every new shared/ui component per
    CLAUDE.md) with at least: empty selection, some products selected, some categories selected, mixed
    selection with overflow (10+ chips), and a disabled state. Create `MultiSelectPicker.test.tsx`
    (Vitest + Testing Library) covering: clicking a product row adds it to selectedProductIds via
    onChange, clicking an already-selected row removes it, the search input filters visible
    CommandItems, CommandEmpty renders on a no-match search, and removing a chip via its × button calls
    onChange without the removed id.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/shared/ui/MultiSelectPicker/MultiSelectPicker.test.tsx 2>&1 | tail -30</automated>
    <fails_when>non-zero exit, or any "error TS" line, or the vitest summary reports any failed test</fails_when>
  </verify>
  <done>MultiSelectPicker exists with a Storybook story and a passing unit test suite covering select/
  deselect, search-filter, empty-state, and chip removal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire StepScope.tsx into the wizard, with scope-step validity gating</name>
  <files>src/features/manage-promotions/ui/wizard/StepScope.tsx, src/features/manage-promotions/model/usePromotionWizardState.ts, src/features/manage-promotions/ui/PromotionWizardPage.tsx, src/shared/lib/i18n/locales/es-MX/wAdmin.json, src/shared/lib/i18n/locales/en-US/wAdmin.json</files>
  <behavior>
    - isScopeStepValid(state) returns true when storeWide is true, OR when
      (selectedProductIds.length + selectedCategoryIds.length) > 0.
    - isScopeStepValid(state) returns false when storeWide is false and both selection arrays are
      empty (the D-08 "can't click Next" case for this step).
    - Checking storeWide clears both selectedProductIds and selectedCategoryIds to empty arrays.
    - Unchecking storeWide (from a previously-store-wide state) leaves both arrays empty — it does not
      restore whatever was selected before storeWide was checked (no hidden state, matches
      CategoryTreePicker's existing single-value-clear-on-deselect precedent).
  </behavior>
  <read_first>
    src/features/manage-promotions/model/usePromotionWizardState.ts (Plan 01's minimal version —
    targets always [], no scope UI wired yet; extend this hook's state shape and add the
    isScopeStepValid predicate per &lt;behavior&gt; above)
    src/features/manage-promotions/ui/PromotionWizardPage.tsx (Plan 01's minimal version — the "scope"
    TabsContent currently renders a plain placeholder note; replace it with real StepScope content)
    src/entities/product (useProducts hook — same import PromotionFormDialog.tsx used)
    src/entities/category (useCategories hook — same import PromotionFormDialog.tsx used)
    src/shared/ui/checkbox.tsx (the "Store-wide (no restriction)" toggle)
  </read_first>
  <action>
    Extend `usePromotionWizardState.ts`: add `storeWide: boolean` (default true, matching the current
    Plan-01 always-store-wide default so existing behavior doesn't regress until the admin explicitly
    picks targets), `selectedProductIds: string[]`, `selectedCategoryIds: string[]` state, and export
    `isScopeStepValid(): boolean` per &lt;behavior&gt;. Wire `save()`'s payload assembly: when
    `storeWide` is true, `targets: []`; otherwise `targets:
    [...selectedProductIds.map(id => ({productId: id, categoryId: null})),
    ...selectedCategoryIds.map(id => ({productId: null, categoryId: id}))]`. When editing an existing
    promotion, prefill `storeWide`/`selectedProductIds`/`selectedCategoryIds` from
    `promotion.targets` (empty array -> storeWide true; non-empty -> storeWide false, split by
    productId/categoryId non-null).

    Create `src/features/manage-promotions/ui/wizard/StepScope.tsx`: a "Store-wide (no restriction)"
    `Checkbox` + `FormField`-wrapped label, and — only when unchecked — the `MultiSelectPicker` (fed
    `useProducts()`/`useCategories()` data) plus an inline validation error ("Select at least one
    product or category, or mark this promotion as store-wide.", per the Copywriting Contract) shown
    when `!isScopeStepValid()` and the admin has attempted to advance. Replace `PromotionWizardPage.tsx`'s
    placeholder "scope" `TabsContent` with `<StepScope />`, and wire its forward-navigation gate (the
    `onValueChange` handler added in Plan 01's minimal Tabs shell) to also check `isScopeStepValid()`
    before allowing a jump past the scope step, per RESEARCH.md Pattern 1's `isStepValid(currentStep)`
    gate shape.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/features/manage-promotions 2>&1 | tail -30</automated>
    <fails_when>non-zero exit, or any "error TS" line, or the vitest summary reports any failed test</fails_when>
  </verify>
  <done>StepScope renders real multi-select for products/categories, the Store-wide checkbox clears
  selection and disables the picker, forward navigation past the Scope step is blocked with zero
  targets and Store-wide unchecked, and save() correctly assembles the targets payload for both the
  store-wide and multi-target cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin browser -> promotion_targets payload | Client-assembled target list, validated by Zod (PromotionTargetInputSchema, Plan 01) before insert; RLS + the DB CHECK/partial-unique constraints from Plan 01 remain the actual authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-28-06 | Tampering | Scope-step target selection payload | low | mitigate | Client-side isScopeStepValid gate + PromotionTargetInputSchema Zod validation are defense-in-depth only; Plan 01's DB CHECK constraint and partial unique indexes remain the real authority against a malformed or duplicated target list even if the UI gate is bypassed. |
</threat_model>

<verification>
Task 1's MultiSelectPicker is proven by a standalone unit test suite (select/deselect/search/empty/chip
removal) independent of the wizard. Task 2's wiring is proven by the manage-promotions feature's test
suite plus typecheck, confirming the scope-step validity predicate and the store-wide/multi-target
save-payload assembly both behave per the stated `<behavior>` cases.
</verification>

<success_criteria>
- An admin can select 2+ products and 1+ categories for one promotion via the new picker.
- Checking Store-wide clears and disables selection; unchecking it re-enables an empty selection.
- Forward navigation is blocked with zero targets and Store-wide unchecked.
- `npm run typecheck` and the manage-promotions test suite are both green.
</success_criteria>

<output>
Create `.planning/phases/28-promotion-management-redesign/28-03-SUMMARY.md` when done
</output>
