---
phase: 28-promotion-management-redesign
plan: 05
type: execute
wave: 4
depends_on: ["28-01", "28-02", "28-03", "28-04"]
files_modified:
  - e2e/payments/apply-promotion-and-custom-discount.spec.ts
  - e2e/payments/promotion-snapshot-refund-reopen.spec.ts
  - e2e/promotions/promotion-deleted-mid-cart.spec.ts
  - e2e/promotions/loose-weight-open-unit-interaction.spec.ts
  - e2e/promotions/timezone-boundary.spec.ts
  - e2e/promotions/scope-overlap-resolution.spec.ts
  - e2e/promotions/percent-field-input.spec.ts
  - e2e/infra/offline-promotion-conflict.spec.ts
  - e2e/errors/promotion-floor-guard.spec.ts
  - e2e/checkout/promotion-live-price.spec.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, folded-todo-audit-manager-pin-identity-in-remaining-rpcs]

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Every pre-Phase-28 e2e spec that seeded a promotion fixture via the old scope_type/product_id/category_id columns now seeds via the promotion_targets junction table and still passes its original assertion."
    - "e2e/promotions/percent-field-input.spec.ts drives the new wizard's Basics step (not the deleted PromotionFormDialog) and still proves the G-27-8 Part A string-buffered percent-input fix."
    - "The full e2e/promotions/ scenario matrix (both pre-existing and this phase's new specs) passes green in one run."
    - "npm run test:e2e (the full suite, not just e2e/promotions/) is green — no cross-folder regression from the schema migration or the manager-PIN RPC signature changes."
  artifacts: []
  key_links:
    - "Every fixed spec's seedPromotion()-style helper mirrors Plan 01's e2e/promotions/multi-target-scope.spec.ts seed pattern: insert into promotions without scope_type/product_id/category_id, then a separate promotion_targets insert when the scenario needs a scoped (not store-wide) promotion"
---

<objective>
Close out the phase: fix every pre-existing E2E spec whose fixture-seeding still targets the dropped
`scope_type`/`product_id`/`category_id` columns or the deleted `PromotionFormDialog`, then run the full
`e2e/promotions/` matrix and the whole `npm run test:e2e` suite to prove zero regression from this
phase's two breaking changes (the junction-table schema migration in Plan 01, and the
`reopen_tab`/`edit_paid_tab` signature changes in Plan 02).

Purpose: Plans 01-04 deliberately deferred full-suite regression repair to this closing plan, per this
repo's Sampling Rate policy (`npm run test:e2e` is only required green before `/gsd-verify-work`, not
after every wave) and matching Phase 27's own precedent (27-07 closed out the same way). This plan is
that close-out.
Output: 10 existing E2E specs updated for the new schema/UI, and a clean full-suite run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-promotion-management-redesign/28-CONTEXT.md
@e2e/promotions/multi-target-scope.spec.ts
@e2e/promotions/scope-overlap-resolution.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all 10 pre-existing promotion-fixture specs for the new schema/UI</name>
  <files>e2e/payments/apply-promotion-and-custom-discount.spec.ts, e2e/payments/promotion-snapshot-refund-reopen.spec.ts, e2e/promotions/promotion-deleted-mid-cart.spec.ts, e2e/promotions/loose-weight-open-unit-interaction.spec.ts, e2e/promotions/timezone-boundary.spec.ts, e2e/promotions/scope-overlap-resolution.spec.ts, e2e/promotions/percent-field-input.spec.ts, e2e/infra/offline-promotion-conflict.spec.ts, e2e/errors/promotion-floor-guard.spec.ts, e2e/checkout/promotion-live-price.spec.ts</files>
  <read_first>
    e2e/promotions/multi-target-scope.spec.ts (Plan 01's own new seed helper — the canonical pattern
    every fix below should mirror: insert into `promotions` without scope_type/product_id/category_id,
    then a separate `promotion_targets` insert for a scoped, non-store-wide promotion)
    e2e/promotions/scope-overlap-resolution.spec.ts lines 46-71 (`seedPromotion` — the exact old-schema
    insert shape being replaced: `scope_type: opts.productId ? 'product' : 'category', product_id:
    opts.productId ?? null, category_id: opts.categoryId ?? null` — every other file below has an
    equivalent helper to find and fix the same way)
    e2e/promotions/percent-field-input.spec.ts lines 41-50+ (drives `getByRole('dialog', { name: 'New
    Promotion' })` — the deleted PromotionFormDialog; rewrite to navigate to `/promotions/new` and
    drive the wizard's Basics step instead, keeping the exact same percent-field type-and-assert
    behavior the G-27-8 Part A regression test proves)
  </read_first>
  <action>
    In each of `e2e/payments/apply-promotion-and-custom-discount.spec.ts`,
    `e2e/payments/promotion-snapshot-refund-reopen.spec.ts`,
    `e2e/promotions/promotion-deleted-mid-cart.spec.ts`,
    `e2e/promotions/loose-weight-open-unit-interaction.spec.ts`,
    `e2e/promotions/timezone-boundary.spec.ts`, `e2e/promotions/scope-overlap-resolution.spec.ts`,
    `e2e/infra/offline-promotion-conflict.spec.ts`, `e2e/errors/promotion-floor-guard.spec.ts`, and
    `e2e/checkout/promotion-live-price.spec.ts`: locate the file's promotion-seeding helper (typically
    a local `seedPromotion`/`createPromotion`-style function calling
    `admin.from('promotions').insert({...})`), remove `scope_type`/`product_id`/`category_id` from
    that insert, and — when the scenario needs a scoped (not store-wide) promotion — add a follow-up
    `admin.from('promotion_targets').insert({ promotion_id: <new id>, product_id: ..., category_id:
    ... })` call, mirroring `multi-target-scope.spec.ts`'s pattern exactly. Preserve every file's
    original test assertions and scenario intent unchanged — this is a fixture-seeding mechanics fix,
    not a behavior-scope change. Where a file's `afterEach` cleanup deletes seeded promotions by id
    (all of them do, per the pattern already read), confirm the `promotion_targets` rows are also
    cleaned up (they are, via `ON DELETE CASCADE` on `promotion_id` from Plan 01's migration — no
    explicit cleanup code needed for the targets table itself).

    In `e2e/promotions/percent-field-input.spec.ts`: replace the `getByRole('button', { name: 'New
    Promotion' })` -> `getByRole('dialog', ...)` flow with `page.goto('/promotions')` ->
    `getByRole('button', { name: 'New Promotion' })` -> assert navigation to `/promotions/new` -> find
    the percent field on the wizard's Basics & Discount step (same field, new page instead of a
    dialog) -> clear it, type "20", assert the DOM value is exactly "20" -> advance through the
    remaining 3 steps with a store-wide target (Store-wide checkbox, default date range, no recurrence)
    -> Create Promotion -> assert the created row's `discount_value = 20` server-side, same as before.
  </action>
  <verify>
    <automated>grep -rn "scope_type\|category_id: opts\.\|product_id: opts\." e2e/ | grep -v "e2e/promotions/multi-target-scope.spec.ts" | wc -l</automated>
    <fails_when>count is greater than 0 (any remaining old-schema insert reference outside the new Plan-01 spec)</fails_when>
    <automated>npx playwright test e2e/promotions/ e2e/payments/apply-promotion-and-custom-discount.spec.ts e2e/payments/promotion-snapshot-refund-reopen.spec.ts e2e/infra/offline-promotion-conflict.spec.ts e2e/errors/promotion-floor-guard.spec.ts e2e/checkout/promotion-live-price.spec.ts</automated>
    <fails_when>non-zero exit, or any test across these files reported as failed/timed out</fails_when>
  </verify>
  <done>Zero remaining e2e references to the dropped scope_type/product_id/category_id columns outside
  the new Plan-01 spec; every fixed file's original scenario still passes; percent-field-input.spec.ts
  drives the new wizard instead of the deleted dialog and still proves the string-buffered-input fix.</done>
</task>

<task type="auto">
  <name>Task 2: Full phase-gate — e2e/promotions/ matrix + whole suite</name>
  <files>(none — verification-only task; may touch any file the phase-gate run surfaces a real regression in, per this task's action)</files>
  <read_first>
    .planning/phases/28-promotion-management-redesign/28-VALIDATION.md "Per-Task Verification Map" (the
    full list of D-01..D-12 + folded-todo test mappings this run must close out)
  </read_first>
  <action>
    Run the full `e2e/promotions/` folder (all specs — this phase's 4 new ones plus the 7 fixed
    promotions-folder ones) and confirm every test is green. Then run the complete `npm run test:e2e`
    suite and fix any remaining regression surfaced outside the files already touched in Task 1 (for
    example, a spec elsewhere in the suite that incidentally asserts against the old `Promotion` type
    shape, or against `reopen_tab`/`edit_paid_tab`'s pre-Plan-02 signature) — do not silently skip or
    `test.skip()` a failing test; fix the root cause or, if it is genuinely out of this phase's scope,
    stop and report it rather than suppressing it. Confirm `npm run typecheck && npm run lint --
    --max-warnings=0 && npm run test` are all clean as the final phase-gate.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint -- --max-warnings=0 && npm run test 2>&1 | tail -40</automated>
    <fails_when>non-zero exit from any of the three commands, or any "error TS" line, any ESLint error count above 0, or the vitest summary reports any failed test</fails_when>
    <automated>npm run test:e2e 2>&1 | tail -60</automated>
    <fails_when>non-zero exit, or the Playwright summary reports any failed test</fails_when>
  </verify>
  <done>The entire e2e/promotions/ matrix and the whole npm run test:e2e suite are green; typecheck,
  lint, and unit tests are all clean — the phase is fully proven end-to-end with zero known regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — this plan is verification-only, closing out the mitigations already
implemented in Plans 01-04.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-28-08 | Tampering | Full e2e/promotions/ + npm run test:e2e run | low | mitigate | Re-running the complete suite (not just this phase's new files) after two breaking changes (junction-table migration, RPC signature changes) confirms no other test silently regressed into a false-pass or a masked failure — closing the loop this repo's mandatory-automated-testing policy requires before any phase is considered complete. |
</threat_model>

<verification>
Task 1 is proven by a zero-match grep gate (no remaining old-schema insert references) plus a targeted
Playwright run of every fixed file. Task 2 is proven by the complete phase-gate command sequence
(typecheck, lint, unit tests, full E2E suite) all passing, matching this repo's CLAUDE.md
mandatory-automated-testing policy — no `human_needed`/manual UAT terminal state is valid for this
phase.
</verification>

<success_criteria>
- Zero e2e references to the dropped promotions.scope_type/product_id/category_id columns remain.
- e2e/promotions/percent-field-input.spec.ts passes against the new wizard.
- The full e2e/promotions/ matrix is green.
- npm run typecheck, npm run lint, npm run test, and npm run test:e2e are all green.
</success_criteria>

<output>
Create `.planning/phases/28-promotion-management-redesign/28-05-SUMMARY.md` when done
</output>
