---
phase: 28-promotion-management-redesign
plan: 04
type: execute
wave: 3
depends_on: ["28-03"]
files_modified:
  - src/shared/ui/DateRangePicker.tsx
  - src/features/manage-promotions/ui/wizard/StepValidityRecurrence.tsx
  - src/features/manage-promotions/ui/wizard/StepReview.tsx
  - src/features/manage-promotions/model/usePromotionWizardState.ts
  - src/features/manage-promotions/ui/PromotionWizardPage.tsx
  - e2e/promotions/wizard-step-validation.spec.ts
  - src/shared/lib/i18n/locales/es-MX/wAdmin.json
  - src/shared/lib/i18n/locales/en-US/wAdmin.json
autonomous: true
requirements: [D-04, D-05, D-06, D-07, D-08, D-09, D-10]

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "The Validity & Recurrence step offers forward-looking date-range presets (e.g. Next 7 Days, Next 30 Days, This Month) in addition to the existing manual date inputs — the reused Reports DateRangePicker's backward-only presets remain unchanged for Reports' own usage (D-07's forward-looking-validity requirement)."
    - "Toggling 'Recurring' on reveals 7 day-of-week checkboxes (Sun-Sat) and start/end time inputs; toggling it off clears daysOfWeek/startTime/endTime to null (D-04)."
    - "An end time before or equal to the start time is rejected inline with 'End time must be after start time' before the step can be left (D-05, same-day only)."
    - "Forward navigation is blocked on any wizard step with invalid/missing required fields; unblocked the moment the step becomes valid (D-08, now covering all 4 steps)."
    - "The Review step shows a live computed price example using evaluateBestPromotion against a real matching product for the entered configuration, or the no-match fallback copy when the scope matches zero catalog products (D-09)."
    - "Editing an existing promotion opens the wizard with all 4 steps immediately clickable via the step indicator, with no forward-only gating (D-10)."
    - statement: "The Review step's summary card remains scrollable (overflow-y-auto on the step content region, not the whole page) when recurrence + multi-target scope + the live preview are all populated at once."
      verification: backstop
  artifacts:
    - src/features/manage-promotions/ui/wizard/StepValidityRecurrence.tsx
    - src/features/manage-promotions/ui/wizard/StepReview.tsx
    - e2e/promotions/wizard-step-validation.spec.ts
  key_links:
    - "StepReview.tsx imports evaluateBestPromotion and getStoreLocalDowAndTime directly from @entities/promotion (Plan 01 Task 2's exports) to compute its live preview against the wizard's in-progress (unsaved) field state"
    - "usePromotionWizardState.ts's isStepValid(step) predicate gates PromotionWizardPage.tsx's Tabs onValueChange handler for every step in create mode, and is bypassed entirely in edit mode per D-10"
---

<objective>
Complete the promotion wizard: the Validity & Recurrence step (forward-looking date presets +
day-of-week/time-of-day recurrence), the Review step (full summary + D-09's live computed-price
preview + the final Create/Save action), and the full 4-step forward-navigation gate (D-08) plus
edit-mode free navigation (D-10).

Purpose: This closes out the wizard flow CONTEXT.md's D-07..D-10 describe, turning Plan 01's minimal
single-step entry and Plan 03's Scope step into the complete 4-step flow the phase goal specifies.
Output: `StepValidityRecurrence.tsx`, `StepReview.tsx`, an extended `DateRangePicker` with a `presets`
prop, a completed `isStepValid` predicate machine in `usePromotionWizardState.ts`, and
`wizard-step-validation.spec.ts` proving D-07/D-08/D-09/D-10 end-to-end.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-promotion-management-redesign/28-CONTEXT.md
@.planning/phases/28-promotion-management-redesign/28-UI-SPEC.md
@.planning/phases/28-promotion-management-redesign/28-RESEARCH.md
@src/shared/ui/DateRangePicker.tsx
@src/features/manage-promotions/model/usePromotionWizardState.ts
@src/features/manage-promotions/ui/PromotionWizardPage.tsx
@src/entities/promotion/model/promotion-pricing.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DateRangePicker forward-looking presets</name>
  <files>src/shared/ui/DateRangePicker.tsx, src/shared/lib/i18n/locales/es-MX/common.json, src/shared/lib/i18n/locales/en-US/common.json</files>
  <read_first>
    src/shared/ui/DateRangePicker.tsx (full file, 99 lines — the `Preset` type and `PRESETS` const at
    lines 18-57 are backward-looking-only today; the component itself already accepts arbitrary
    `fromStr`/`toStr`/`onChange`, no forking needed)
    .planning/phases/28-promotion-management-redesign/28-PATTERNS.md "DateRangePicker.tsx (extend with
    presets prop)" section (the exact generalization approach: add a `presets?: Preset[]` prop
    defaulting to the existing `PRESETS` array so Reports is unaffected)
  </read_first>
  <action>
    Add an optional `presets?: Preset[]` prop to `DateRangePickerProps`, defaulting to the existing
    `PRESETS` constant when omitted (Reports' own call sites keep compiling and behaving identically —
    do not touch any Reports call site). Export a new `PROMOTION_DATE_PRESETS: Preset[]` array from the
    same file (or a small co-located helper), built with the same `toDateStr`/`d.setDate` helpers
    already in this file, containing forward-looking options: "Today onward" (from today() to
    today()+1yr, or a sufficiently far future date — pick a concrete cap and state it in the label),
    "Next 7 Days", "Next 30 Days", "This Month" (from today to end of current month). Use the existing
    `t('dateRangePicker.<key>')` i18n pattern for each label (add the new keys to `common.json`, both
    locales — reuse the `dateRangePicker` namespace already used by `from`/`to`).
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/shared/ui 2>&1 | tail -30</automated>
    <fails_when>non-zero exit, or any "error TS" line, or the vitest summary reports any failed test</fails_when>
  </verify>
  <done>DateRangePicker accepts an optional presets prop with a forward-looking default export
  available for the wizard to pass; every existing Reports call site is unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Full isStepValid predicate machine + StepValidityRecurrence.tsx</name>
  <files>src/features/manage-promotions/ui/wizard/StepValidityRecurrence.tsx, src/features/manage-promotions/model/usePromotionWizardState.ts, src/features/manage-promotions/ui/PromotionWizardPage.tsx, src/shared/lib/i18n/locales/es-MX/wAdmin.json, src/shared/lib/i18n/locales/en-US/wAdmin.json</files>
  <behavior>
    - isValidityStepValid(state): false when endsAt is before startsAt. false when recurring is true
      and daysOfWeek is empty AND startTime/endTime are both null (a "Recurring" toggle with nothing
      configured is not a valid recurring rule). false when recurring is true, startTime and endTime
      are both set, and endTime <= startTime (D-05). true otherwise (including recurring=false, and
      recurring=true with only daysOfWeek set and no time window, or only a time window and no
      daysOfWeek restriction — both are valid partial recurrence configurations per D-04/D-05 treating
      "no day restriction" / "no time restriction" as "every day"/"all day").
    - isStepValid(step, state): dispatches to isBasicsStepValid (name non-empty, discount value in
      range — Plan 01's existing PromotionFormDialog-derived checks) / isScopeStepValid (Plan 03) /
      isValidityStepValid (this task) / true for 'review' (nothing to validate on the summary step
      itself — Save either succeeds or surfaces a server error).
    - In create mode, PromotionWizardPage's Tabs onValueChange only allows moving to a step at index
      <= furthestValidStep + 1, and furthestValidStep only advances past a step once isStepValid(step)
      is true for it.
    - In edit mode (promotion prop is non-null), every step's TabsTrigger is enabled regardless of
      furthestValidStep — no forward-gating applies at all (D-10).
  </behavior>
  <read_first>
    src/features/manage-promotions/model/usePromotionWizardState.ts (Plan 01/03's state so far —
    startsAt/endsAt/daysOfWeek/startTime/endTime fields already exist from Plan 01's minimal defaults;
    this task adds the `recurring: boolean` toggle state and the full isStepValid dispatcher)
    src/features/manage-promotions/ui/PromotionWizardPage.tsx (Plan 01's onValueChange gate currently
    only checks the basics step in create mode and has no edit-mode bypass; extend both)
    .planning/phases/28-promotion-management-redesign/28-RESEARCH.md "Pattern 1: Wizard as a controlled
    Tabs component" (the exact onValueChange/disabled gating shape, including the edit-mode
    `disabled={!isEditMode && STEP_ORDER.indexOf(step) > furthestValidStep + 1}` condition)
    src/shared/ui/switch.tsx (the "Recurring" toggle)
    src/shared/ui/DateRangePicker.tsx (Task 1's extended version — StepValidityRecurrence passes
    PROMOTION_DATE_PRESETS)
  </read_first>
  <action>
    Extend `usePromotionWizardState.ts`: add `recurring: boolean` (default false). Implement
    `isValidityStepValid()` and `isStepValid(step: Step): boolean` per &lt;behavior&gt;, replacing
    Plan 01's placeholder always-true stand-in for steps 2-4. When `recurring` is toggled off, clear
    `daysOfWeek` to `null` and `startTime`/`endTime` to `null` in the same state update (mirrors
    PromotionFormDialog's existing "switching X clears stale Y" convention). When editing an existing
    promotion, prefill `recurring` from whether `promotion.daysOfWeek`/`startTime`/`endTime` are
    non-null.

    Create `src/features/manage-promotions/ui/wizard/StepValidityRecurrence.tsx`: the `DateRangePicker`
    (unchanged existing fromStr/toStr/onChange wiring, now passed `presets={PROMOTION_DATE_PRESETS}`
    from Task 1) with an inline "End date must be on or after the start date" error per the Copywriting
    Contract when invalid; below it, a "Recurring" `Switch` that reveals — only when on — 7
    day-of-week `Checkbox`es in a single row (Sun-Sat, each toggling that day's inclusion in
    `daysOfWeek`, D-04) and two native `input[type="time"]` fields inside `FormField` pairs
    (start/end), with the "End time must be after start time" error shown inline per D-05 when invalid.

    Replace `PromotionWizardPage.tsx`'s placeholder "validity" `TabsContent` with `<StepValidityRecurrence
    />`. Rewrite the Tabs `onValueChange` handler and each `TabsTrigger`'s `disabled` prop per
    &lt;behavior&gt;'s last two bullets — in edit mode (`promotion != null`), no `disabled` is ever
    applied and `onValueChange` allows any step; in create mode, the existing forward-gate now checks
    `isStepValid` for whichever step is being left, for all three gated steps (basics/scope/validity).
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/features/manage-promotions 2>&1 | tail -30</automated>
    <fails_when>non-zero exit, or any "error TS" line, or the vitest summary reports any failed test</fails_when>
  </verify>
  <done>Recurring toggle reveals/clears day-of-week and time-window fields correctly; invalid date
  ranges and invalid time windows block forward navigation with the correct inline error copy; edit
  mode has zero forward-gating on any step.</done>
</task>

<task type="auto">
  <name>Task 3: StepReview.tsx live price preview + edit-mode step indicator + wizard-step-validation E2E</name>
  <files>src/features/manage-promotions/ui/wizard/StepReview.tsx, src/features/manage-promotions/ui/PromotionWizardPage.tsx, e2e/promotions/wizard-step-validation.spec.ts, src/shared/lib/i18n/locales/es-MX/wAdmin.json, src/shared/lib/i18n/locales/en-US/wAdmin.json</files>
  <read_first>
    src/entities/promotion/model/promotion-pricing.ts (Plan 01 Task 2's finished evaluateBestPromotion
    signature — product, activePromotions, now, expiryDiscountPercent, daysUntilExpiry,
    expiryThresholdDays, timezone — and getStoreLocalDowAndTime, both exported from
    @entities/promotion per Plan 01 Task 2)
    src/entities/product (useProducts — used to find a sample product matching the wizard's in-progress
    scope for the live preview)
    src/entities/settings (useSettings — appSettings.general.timezone, appSettings.nearExpiry.* for the
    evaluateBestPromotion call's remaining arguments)
    .planning/phases/28-promotion-management-redesign/28-UI-SPEC.md "Copywriting Contract" (Review step
    live-price-preview label: "Example: {productName} — {originalPrice} → {discountedPrice}"; no-match
    fallback: "No product currently matches this promotion's scope, so no live example is available.")
    and "UI Structure -> Step 4 — Review" (read-only summary + live preview card + final POSButton
    touchSize="xl")
    e2e/payments/apply-promotion-and-custom-discount.spec.ts (existing loginAs('admin') + PIN-flow
    pattern to reuse for driving the wizard end-to-end in the new E2E spec)
  </read_first>
  <action>
    Create `src/features/manage-promotions/ui/wizard/StepReview.tsx`: a read-only summary of every
    prior-step value (name, discount type/value, scope — reusing the same "Store-wide" / "N products, M
    categories" summary logic Plan 01's list page uses, or the actual selected names for a short list),
    date range, recurrence if set. Below the summary, a live-price-preview card: find the first product
    in `useProducts()`'s data matching the wizard's current scope (store-wide -> any product;
    otherwise -> the first product whose id is in selectedProductIds or whose categoryId is in
    selectedCategoryIds), and if one is found, call `evaluateBestPromotion` with a single-element
    `activePromotions` array built from the wizard's in-progress (unsaved) field state, the real
    `appSettings.general.timezone`, and the real near-expiry settings; render the "Example:
    {productName} — {originalPrice} → {discountedPrice}" copy when the preview call returns a match for
    that sample product's price, or the no-match fallback copy when either no product matches the scope
    or the preview call returns null (e.g. a date range that doesn't cover "now"). Give the summary
    card's content region its own `overflow-y-auto` (not the whole page) so the Review step stays
    scrollable when recurrence + multi-target scope + the live preview are all populated at once — the
    Nav bar (Back/Create/Save) stays pinned outside that scrollable region. Add the final POSButton
    (`touchSize="xl"`, "Create Promotion" / "Save Changes" per whether `promotion` is set) calling
    `usePromotionWizardState`'s `save()`.

    Replace `PromotionWizardPage.tsx`'s placeholder "review" `TabsContent` with `<StepReview />`. Make
    the step indicator (`TabsList`/`TabsTrigger`s) a clickable breadcrumb in edit mode, matching D-10 —
    this is already covered by Task 2's `disabled` rewrite; this task only needs to confirm/adjust the
    visual state classes so a completed-and-revisitable step (create mode, already past) and an
    edit-mode step (always enabled) are both styled per 28-UI-SPEC.md's "Step indicator states"
    (current = accent background/text, completed & revisitable = default/outline clickable,
    not-yet-reached in create mode = disabled/muted, all steps enabled in edit mode).

    Write `e2e/promotions/wizard-step-validation.spec.ts`: log in as admin, navigate to
    `/promotions/new`, assert clicking Next with an empty name is blocked (D-08) and the inline error
    shows; fill Basics, advance, leave Scope with Store-wide unchecked and nothing selected — assert
    Next is blocked with the scope error; select a target, advance; enter an invalid time window
    (end <= start) on the Validity step — assert Next is blocked with the time-window error; fix it and
    advance to Review — assert the live-price-preview shows a real computed discounted price matching
    what `evaluateBestPromotion` would compute for the entered configuration (cross-check the displayed
    numbers, don't just assert the card renders); click Create Promotion and assert the new promotion
    appears on `/promotions`. In a second test, edit that same promotion and assert every step's
    TabsTrigger is immediately clickable (no disabled attribute on any of the 4 triggers) without first
    passing through Basics/Scope/Validity in order (D-10).
  </action>
  <verify>
    <automated>npm run typecheck && npx playwright test e2e/promotions/wizard-step-validation.spec.ts</automated>
    <fails_when>non-zero exit, or any "error TS" line, or the test is reported as failed/timed out</fails_when>
  </verify>
  <done>The Review step shows a real, cross-checkable computed-price example (or the correct no-match
  fallback), the final Save/Create action persists the promotion, edit mode allows immediate navigation
  to any of the 4 steps, and the full create-mode forward-gate (D-08) is proven end-to-end by a real
  Playwright run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin browser -> StepReview's live preview computation | Purely client-side, non-authoritative preview using the wizard's in-progress (unsaved) field state — never itself a write path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-28-07 | Input Validation | StepValidityRecurrence's start_time/end_time fields | low | mitigate | Client-side D-05 same-day validation (end > start) mirrors Plan 01's DB CHECK constraint (promotions_recurrence_same_day) — the UI blocks invalid entry before submit, and the database rejects it independently if the client check is ever bypassed. |
</threat_model>

<verification>
Task 1 is proven by typecheck + shared/ui's test suite (no Reports regression). Task 2 is proven by
the manage-promotions feature's unit tests exercising the full isStepValid dispatcher per the stated
`<behavior>` truth table. Task 3 is proven by a real Playwright run covering the complete create-mode
forward-gated flow (D-07/D-08), the live preview's cross-checked numbers (D-09), and edit-mode's
immediate step access (D-10) in one spec file.
</verification>

<success_criteria>
- Forward-looking date presets are available in the wizard without touching Reports' own presets.
- Recurring toggle + day-of-week + time-window fields validate and persist correctly.
- Every one of the wizard's 4 steps blocks forward navigation until valid, in create mode.
- The Review step's live preview numerically matches evaluateBestPromotion's real output.
- Edit mode allows immediate navigation to any step with zero forward-gating.
- `npm run typecheck` is clean; the new E2E spec is green.
</success_criteria>

<output>
Create `.planning/phases/28-promotion-management-redesign/28-04-SUMMARY.md` when done
</output>
