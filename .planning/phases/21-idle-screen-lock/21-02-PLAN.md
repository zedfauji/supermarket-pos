---
phase: 21-idle-screen-lock
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - src/features/idle-screen-lock/model/lock-state-store.ts
  - src/features/idle-screen-lock/ui/IdleLockProvider.tsx
  - src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx
  - e2e/security/idle-lock-bypass.spec.ts
  - e2e/security/idle-lock-transactions.spec.ts
autonomous: true
requirements: [LCK-01]

estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Opening the idle-lock overlay while /pos has cart items and/or an open payment modal does not clear the cart or reset the payment modal's state — both are exactly as they were before the overlay engaged (LCK-01, D-01, no exemption for in-progress transactions)."
    - "A simulated barcode-scanner keystroke burst received while the overlay is open triggers no plugin:window|create_webview_window (Product Peek window) IPC call — CheckoutPanel's global useBarcodeScanner listener is gated on the shared lock state (Pitfall 3)."
    - "After a successful unlock, the same barcode-scanner listener resumes normal operation (a scan again opens the Product Peek window) without requiring a page reload."
    - "Pressing Escape while the overlay is open does not close it — reconfirmed at the E2E layer as defense in depth beyond Plan 21-01's onEscapeKeyDown prevention."
    - "The in-progress sale from the mid-transaction test can still be completed successfully after unlocking — the lock/unlock cycle does not corrupt transaction state."
  artifacts:
    - src/features/idle-screen-lock/model/lock-state-store.ts
    - e2e/security/idle-lock-bypass.spec.ts
    - e2e/security/idle-lock-transactions.spec.ts
  key_links:
    - "IdleLockProvider -> useLockStateStore.setState({ locked }) -> CheckoutPanel's scannerEnabled boolean reads useLockStateStore(s => s.locked) -> useBarcodeScanner({ enabled: scannerEnabled && !locked, ... }) gated false while locked"
  assumptions:
    - "[Pitfall 3 scope, flagged] src/widgets/ProductPeekWindow/ui/ProductPeekWindow.tsx's own useBarcodeScanner call is deliberately NOT gated on the main window's lock state in this phase — it runs in a separate Tauri OS window with its own JS realm (does not share this Zustand store), and RESEARCH.md's Open Question 1 explicitly recommends leaving that short-lived, mid-scan detail window un-instrumented for idle-lock v1, revisited only if usage data shows it is held open long enough to matter."
---

<objective>
Close the two gaps left open after Plan 21-01's tracer: prove D-01's "no exemption, even mid-transaction"
requirement end-to-end on a real cart + payment modal, and close Pitfall 3 (a global `window`-level
keydown listener that could otherwise keep operating — opening/updating the separate Product Peek
window — while the screen visually appears locked).

Purpose: LCK-01's own text is explicit — "no exemption for in-progress transactions (open cart, payment
modal, etc.)" — Plan 21-01 proved the mechanism on a neutral screen (/home); this plan proves the
harder, higher-severity case and removes the one bypass RESEARCH.md flagged as a real risk (Pitfall 3).
Output: a shared lock-state store any module can read without prop-drilling, `CheckoutPanel`'s scanner
gated on it, and two new E2E specs proving both closed gaps.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-idle-screen-lock/21-CONTEXT.md
@.planning/phases/21-idle-screen-lock/21-RESEARCH.md
@.planning/phases/21-idle-screen-lock/21-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared lock-state store + CheckoutPanel scanner gating (closes Pitfall 3)</name>
  <files>src/features/idle-screen-lock/model/lock-state-store.ts, src/features/idle-screen-lock/ui/IdleLockProvider.tsx, src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx, e2e/security/idle-lock-bypass.spec.ts</files>
  <read_first>
    21-RESEARCH.md "Pitfall 3" (bypass via a global keydown listener outside the dialog's focus trap) and "Open Question 1" (Product Peek window scope decision)
    src/features/idle-screen-lock/ui/IdleLockProvider.tsx (as written by Plan 21-01, Task 12) — the local `locked` useState this task replaces with the new store
    src/shared/lib/useBarcodeScanner.ts (full file) — confirms the `window.addEventListener('keydown', handler)` is genuinely global and fires regardless of dialog focus-trap state
    src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx lines 30-47 — the existing `scannerEnabled = !paymentOpen && !weightEntry.isOpen && editingWeightItemId === null` composition to extend
    src/entities/staff/model/store.ts — the Zustand `create<...>()(...)` shape to mirror for a tiny, non-persisted store (no `persist` middleware needed here)
    e2e/checkout/peek-window.spec.ts lines 12-60 — the local `scanBarcode(page, barcode)` helper (dispatches keydown events via `page.evaluate`) and the `injectPeekWindowMock`/`getPeekMockCalls` API to reuse
    e2e/helpers/tauriPeekMock.ts (full file) — `injectPeekWindowMock(page)` and `getPeekMockCalls(page, cmd?)`, in particular the `plugin:window|create_webview_window` command name
  </read_first>
  <action>
    Create src/features/idle-screen-lock/model/lock-state-store.ts: a tiny Zustand store (no persist, no middleware) useLockStateStore with shape { locked: boolean; setLocked: (locked: boolean) => void }, default locked: false — the single source of truth for "is the screen currently locked" within this window's JS realm, so any module can read it without prop-drilling from App.tsx down through the FSD layers.
    Refactor src/features/idle-screen-lock/ui/IdleLockProvider.tsx to read/write `locked` via useLockStateStore instead of its own useState from Plan 21-01 — handleIdle and handleUnlock now call useLockStateStore.getState().setLocked(true/false), and the component reads `const locked = useLockStateStore(s => s.locked)` for both the useIdleTimer(..., isAuthenticated && !locked) gate and the <IdleLockOverlay open={locked} .../> prop. Behavior is unchanged; only the state's storage location moves.
    In src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx, extend the existing scannerEnabled boolean to also require `&& !useLockStateStore(s => s.locked)`. This closes Pitfall 3: useBarcodeScanner's global window-level keydown listener would otherwise keep firing (and calling ensurePeekWindowShown, which opens/updates the Product Peek OS window) while the idle-lock overlay is visually blocking the screen — defeating the lock's purpose even though no cart mutation occurs. Deliberately do NOT touch src/widgets/ProductPeekWindow/ui/ProductPeekWindow.tsx's own useBarcodeScanner call — it runs in a separate Tauri OS window with its own JS realm (this Zustand store is not shared across windows), and RESEARCH.md's Open Question 1 explicitly recommends leaving that short-lived, mid-scan detail window un-instrumented for idle-lock v1 (recorded as a flagged assumption above, not a silent omission).
    Create e2e/security/idle-lock-bypass.spec.ts: requireIntegrationEnv(), resetTestState(), openCaja(500), injectPeekWindowMock(page), loginAs(page, 'cashier'), navigate to /pos. Seed terminal_lock_settings.lock_timeout_seconds to 15 via getServiceClient() (same pattern as idle-lock.spec.ts) and wait for the overlay to engage. While the overlay's alertdialog is visible, call a local scanBarcode(page, '1234567890123') helper (mirror the exact keydown-dispatch implementation in e2e/checkout/peek-window.spec.ts lines 22-28 — no need to seed a real product, any 4+ digit string plus Enter is enough to exercise the listener) and assert `(await getPeekMockCalls(page, 'plugin:window|create_webview_window')).length === 0`. Then unlock with a valid PIN, call scanBarcode again, and assert the call count is now greater than 0 — proving the gate is scoped to `locked`, not a permanent regression. Also assert Escape does not close the overlay (defense-in-depth re-check).
  </action>
  <verify>
    <automated>npx playwright test e2e/security/idle-lock-bypass.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/security/idle-lock-bypass.spec.ts` passes: zero `create_webview_window` calls while locked, at least one after unlock.
    - `npm run typecheck` passes with `IdleLockProvider.tsx` and `CheckoutPanel.tsx` changes included.
    - `src/widgets/ProductPeekWindow/ui/ProductPeekWindow.tsx` is unmodified by this task (grep/diff confirms) — the deliberate scope boundary documented above.
  </acceptance_criteria>
  <done>A raw barcode-scanner keystroke burst reaching the app while the screen is locked has zero externally observable effect (no peek window opens); normal scanning resumes immediately after unlock.</done>
</task>

<task type="auto">
  <name>Task 2: Mid-transaction no-exemption E2E (D-01) — cart/payment-modal state survives a lock/unlock cycle</name>
  <files>e2e/security/idle-lock-transactions.spec.ts</files>
  <read_first>
    21-CONTEXT.md "D-01" — "Everywhere," explicit rejection of a checkout carve-out during exploration
    e2e/checkout/happy-path.spec.ts — cart-building (search/add-to-cart) steps to mirror
    e2e/payments/edge-cases.spec.ts or e2e/payments/payment-pane.spec.ts — opening the payment modal, reading cart total/line items from it
    e2e/helpers/supabase.ts lines 1-30 (getServiceClient) and e2e/helpers/auth.ts (loginAs)
  </read_first>
  <action>
    Create e2e/security/idle-lock-transactions.spec.ts: resetTestState() + openCaja(500), loginAs(page, 'cashier'), navigate to /pos, add at least one real seeded product to the cart via the existing search/add flow (mirror happy-path.spec.ts), open the payment modal (mirror the existing payment-modal-opening steps in edge-cases.spec.ts/payment-pane.spec.ts) and read the displayed cart total/line count. Seed terminal_lock_settings.lock_timeout_seconds to 15 via getServiceClient() and wait for the idle-lock alertdialog to become visible ON TOP of the still-open payment modal — assert both are present in the DOM (the payment modal is not closed/unmounted by the overlay, D-01). Assert the payment modal's Confirm/Pay button is not actually clickable while the overlay is open (a bounded `.click({ timeout: 2000 })` against it should time out, since Playwright's actionability check fails on an occluded element). Unlock with a valid PIN (per D-04, any staff works here too). Assert the payment modal is STILL open showing the SAME cart total/line count as before the lock engaged — nothing was reset (D-01, no carve-out). Finally complete the payment and assert the sale succeeds end-to-end, proving the lock/unlock cycle did not corrupt the in-progress transaction.
  </action>
  <verify>
    <automated>npx playwright test e2e/security/idle-lock-transactions.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The idle-lock overlay engages even while a payment modal is open on /pos, with no exemption (D-01).
    - Cart contents and the open payment modal survive a full lock -> unlock cycle unchanged.
    - The in-progress sale can still be completed successfully after unlocking.
  </acceptance_criteria>
  <done>D-01's "no carve-outs, even mid-transaction" requirement is proven end-to-end, closing the phase's highest-severity previously-untested behavior.</done>
</task>

</tasks>

## Artifacts This Phase Produces

- `src/features/idle-screen-lock/model/lock-state-store.ts` — `useLockStateStore` (shared `{ locked, setLocked }` Zustand store, replaces `IdleLockProvider`'s local `useState` from Plan 21-01)
- `e2e/security/idle-lock-bypass.spec.ts` — Pitfall 3 bypass-resistance coverage
- `e2e/security/idle-lock-transactions.spec.ts` — D-01 mid-transaction no-exemption coverage

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Global `window`-level HID-scanner keydown listener -> cart/Peek-window mutation | An unattended, physically-scannable terminal is a real input surface even while "locked" |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-06 | Tampering | `useBarcodeScanner`'s global `window` keydown listener in `CheckoutPanel` | high | mitigate | `scannerEnabled` now also requires `!locked` (shared `useLockStateStore`), verified by Task 1's E2E asserting zero `create_webview_window` IPC calls while locked. Carried forward from Plan 21-01's threat register (T-21-06), closed here. |
| T-21-07 | Tampering | `ProductPeekWindow`'s own `useBarcodeScanner` call (separate Tauri OS window) | medium | accept | Deliberately out of scope per RESEARCH.md Open Question 1 (flagged assumption above) — a separate JS realm not sharing this store; revisit only if usage data shows the window is held open long enough to matter. |
</threat_model>

<verification>
- `npx playwright test e2e/security/idle-lock-bypass.spec.ts e2e/security/idle-lock-transactions.spec.ts` green.
- `npm run typecheck` and `npm run lint` pass.
- Full phase gate: `npx playwright test e2e/security/` (all three specs across Plan 21-01/21-02) green together.
</verification>

<success_criteria>
D-01's "no exemption, even mid-transaction" clause and Pitfall 3's bypass risk both have automated,
passing coverage, closing every gap identified in RESEARCH.md that Plan 21-01's tracer did not already
prove.
</success_criteria>

<output>
Create `.planning/phases/21-idle-screen-lock/21-02-SUMMARY.md` when done
</output>
