---
phase: 15-receipt-designer-layout-branding-logo-printing
plan: 04
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/widgets/SettingsTabsPanel/tabs/HardwareSettingsTab.tsx
  - src/shared/ui/textarea.tsx
  - src/shared/lib/i18n/locales/en-US/wAdmin.json
  - src/shared/lib/i18n/locales/es-MX/wAdmin.json
  - e2e/08-settings-receipt.spec.ts
autonomous: true
requirements: [RCPD-01]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "HardwareSettingsTab renders #receipt-headerLine2 and #receipt-footerText inputs whose onChange updates local draft state only (no network call) and whose onBlur persists via the existing patchReceipt/updateReceiptSettings mutation"
    - "A [data-testid=receipt-live-preview] element renders buildThermalReceiptText(SAMPLE_RECEIPT_DATA, locale, draftReceiptSettings) — the exact same function used for print/email/in-app receipts, not a second renderer"
    - "Typing into headerLine2/footerText updates the live preview text immediately, before any save/reload"
    - "Toggling showCashierName/showCustomerName/showReceiptNumber off removes the corresponding content from the live preview text, closing RESEARCH.md Pitfall 1's gap"
    - "Changing paperWidthChars changes the live preview's line width"
  artifacts:
    - src/widgets/SettingsTabsPanel/tabs/HardwareSettingsTab.tsx (headerLine2/footerText inputs + live preview panel)
    - src/shared/ui/textarea.tsx
  key_links:
    - "HardwareSettingsTab draft state (receipt) -> buildThermalReceiptText(SAMPLE_RECEIPT_DATA, locale, receipt) -> live preview <pre>"
  prohibitions:
    - statement: "This task must NOT import or render ReceiptPreview.tsx itself inside Settings — only its <pre> rendering fragment/class list is reused, per UI-SPEC.md and RESEARCH.md's explicit anti-pattern ('a second receipt renderer for the Settings live preview')"
      status: dismissed
      verification: backstop
    - statement: "This task must NOT add UI for receipt_settings.kds_enabled — confirmed dead field from the removed bar-pos KDS feature (RESEARCH.md Anti-Pattern: 'Skipping the dead kdsEnabled field')"
      status: dismissed
      verification: backstop
    - statement: "This task must NOT add a Save button — this tab is autosave-on-blur/on-change, matching every existing field (UI-SPEC.md Copywriting Contract: 'Primary CTA: N/A')"
      status: dismissed
      verification: backstop
    - statement: "This task must NOT add a Textarea.stories.tsx — matches this repo's existing precedent that raw shadcn primitives (input.tsx, checkbox.tsx, label.tsx) have no Storybook stories; only composite components like MoneyInput do"
      status: dismissed
      verification: backstop
---

<objective>
Add the two genuinely-missing form inputs (`headerLine2`, `footerText`) and a live, unsaved-draft-reflecting preview panel to `HardwareSettingsTab.tsx` — the other real gap RESEARCH.md's Summary identifies (`receiptSettingsTitle` card already has 6/8 fields wired; these 2 text fields have zero form inputs today despite being fully modeled/persisted). The preview reuses `buildThermalReceiptText` (Plan 01) plus `ReceiptPreview.tsx`'s exact `<pre>` rendering pattern — no second renderer, per RESEARCH.md's explicit anti-pattern and RCPD-01 success criterion 2's explicit requirement.

Purpose: Let the store owner see and edit the receipt's free-text fields, and trust the preview is exactly what will print (RESEARCH.md Pitfall 2 — preview/print divergence — is avoided by construction, since both paths call the identical function).
Output: Two new inputs, a live preview panel, and Playwright e2e coverage proving both saved-and-reloaded AND unsaved-draft states actually change rendered receipt text (not just a checkbox's checked attribute).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-receipt-designer-layout-branding-logo-printing/15-RESEARCH.md
@.planning/phases/15-receipt-designer-layout-branding-logo-printing/15-PATTERNS.md
@.planning/phases/15-receipt-designer-layout-branding-logo-printing/15-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add headerLine2/footerText inputs + live preview panel to HardwareSettingsTab</name>
  <files>src/widgets/SettingsTabsPanel/tabs/HardwareSettingsTab.tsx, src/shared/ui/textarea.tsx, src/shared/lib/i18n/locales/en-US/wAdmin.json, src/shared/lib/i18n/locales/es-MX/wAdmin.json</files>
  <read_first>
    src/widgets/SettingsTabsPanel/tabs/HardwareSettingsTab.tsx (full file — patchReceipt at lines 33-53, paper-width field at lines 119-135, checkbox list at lines 137-168)
    src/features/process-payment/ui/ReceiptPreview.tsx (the exact &lt;pre&gt; fragment to copy, lines 19 and 25-27)
    src/shared/ui/input.tsx (shadcn sibling pattern to model textarea.tsx on)
    .planning/phases/15-receipt-designer-layout-branding-logo-printing/15-UI-SPEC.md "Interaction Contract", "Live Preview Panel — Contract", and "Copywriting Contract" sections
    src/shared/lib/i18n/locales/en-US/wAdmin.json lines 382-398 (hardwareSettingsTab block to extend) and the es-MX counterpart at the same line range
  </read_first>
  <action>
    Install the new shadcn primitive via `npx shadcn@latest add textarea` (writes `src/shared/ui/textarea.tsx` — official shadcn registry, `components.json` has `"registries": {}`, no vetting gate applies). If the CLI is unavailable in this environment, hand-write `textarea.tsx` matching `input.tsx`'s exact structure: `React.forwardRef`, `cn(...)` className merge, `React.TextareaHTMLAttributes<HTMLTextAreaElement>` props type, `Textarea.displayName = 'Textarea'`. Do not add a `Textarea.stories.tsx` — this repo's raw shadcn primitives (`input.tsx`, `checkbox.tsx`, `label.tsx`) have no Storybook stories; only composite components like `MoneyInput` do.

    In HardwareSettingsTab.tsx, split `patchReceipt` per UI-SPEC.md's Interaction Contract: keep `patchReceipt(patch)` exactly as-is (still called directly by every existing checkbox/select `onChange`/`onCheckedChange`, unchanged behavior), and add a new `function applyLocal(patch: Partial<ReceiptSettings>) { if (!receipt) return; setLocalReceipt({ ...receipt, ...patch }); }` for draft-only updates (no network call).

    Import `Input` from `@shared/ui/input`, `Textarea` from `@shared/ui/textarea`, `buildThermalReceiptText` from `@shared/lib/receipt-format`, `getCurrentLocale` from `@shared/lib/i18n`, and `type { ReceiptData }` from `@shared/lib/edge-function-contracts`.

    Insert two new fields directly after the existing `paper-width` `<select>` field's closing `</div>` (~line 135) and before the checkbox `.space-y-3` block (~line 137), each using the same `<div className="space-y-1">` + `<Label htmlFor="...">` wrapper the paper-width field already uses:
    - `id="receipt-headerLine2"`, label text `t('hardwareSettingsTab.headerLine2Label')`, `<Input id="receipt-headerLine2" value={receipt.headerLine2} maxLength={48} placeholder={t('hardwareSettingsTab.headerLine2Placeholder')} onChange={e => applyLocal({ headerLine2: e.target.value })} onBlur={() => patchReceipt({ headerLine2: receipt.headerLine2 })} />`, followed by `<p className="text-sm text-muted-foreground">{t('hardwareSettingsTab.charCount', { count: receipt.headerLine2.length, max: 48 })}</p>`.
    - `id="receipt-footerText"`, label text `t('hardwareSettingsTab.footerTextLabel')`, `<Textarea id="receipt-footerText" value={receipt.footerText} maxLength={480} placeholder={t('hardwareSettingsTab.footerTextPlaceholder')} onChange={e => applyLocal({ footerText: e.target.value })} onBlur={() => patchReceipt({ footerText: receipt.footerText })} />`, followed by the same char-count `<p>` with `max: 480`.

    After the existing checkbox `.space-y-3` block (~line 168), still inside the same `space-y-4 rounded-lg border p-4` card and before its closing `</div>` (~line 169), add the live preview panel: `<h4 className="text-sm font-medium">{t('hardwareSettingsTab.previewTitle')}</h4>`, `<p className="text-sm text-muted-foreground">{t('hardwareSettingsTab.previewHelper')}</p>`, and `<pre data-testid="receipt-live-preview" className="max-h-[50vh] overflow-auto rounded-md border bg-muted/40 p-3 font-mono text-[11px] leading-tight whitespace-pre">{buildThermalReceiptText(SAMPLE_RECEIPT_DATA, getCurrentLocale(), receipt)}</pre>` — the className list is copied byte-for-byte from `ReceiptPreview.tsx:25-27` (UI-SPEC.md forbids restyling it); `data-testid="receipt-live-preview"` is new, added only for e2e queryability, not a styling change. Do NOT import or render `ReceiptPreview` itself — only its `<pre>` pattern.

    Define a module-level fixture above the component: `const SAMPLE_RECEIPT_DATA: ReceiptData = { receiptNumber: 'PREVIEW01', tabId: '00000000-0000-4000-8000-000000000000', customerName: 'Cliente', cashierName: 'Ana', barName: 'Tienda', barAddress: '', items: [{ name: 'Arroz Basmati 1kg', quantity: 2, unitPrice: 45, lineTotal: 90 }, { name: 'Aceite de Girasol 1L', quantity: 1, unitPrice: 38, lineTotal: 38, modifierNames: ['Sin bolsa'] }], subtotal: 128, tipAmount: 0, total: 128, paymentMethod: 'cash', processedAt: new Date('2026-01-01T12:00:00.000Z'), squareReceiptUrl: null, tenderedAmount: 150, changeAmount: 22 };` — a fixed/deterministic date and values (2 items, one with a modifier line, one plain; one cash tender leg with tendered/change; receipt number; cashier+customer populated), matching UI-SPEC.md's Live Preview Panel Contract sample-data requirement exactly.

    Add 7 new keys under the existing `hardwareSettingsTab` block (after the existing `autoCut` key) in both locale files:
    - en-US/wAdmin.json: `"headerLine2Label": "Header line 2 (optional)", "headerLine2Placeholder": "e.g. a tagline or slogan", "footerTextLabel": "Footer message (optional)", "footerTextPlaceholder": "e.g. return policy, thank-you note", "charCount": "{{count}}/{{max}}", "previewTitle": "Live Preview", "previewHelper": "Reflects your unsaved changes exactly as they will print."`
    - es-MX/wAdmin.json: `"headerLine2Label": "Línea de encabezado 2 (opcional)", "headerLine2Placeholder": "p. ej. un eslogan o lema", "footerTextLabel": "Mensaje de pie de página (opcional)", "footerTextPlaceholder": "p. ej. política de devoluciones, mensaje de agradecimiento", "charCount": "{{count}}/{{max}}", "previewTitle": "Vista previa en vivo", "previewHelper": "Refleja tus cambios sin guardar exactamente como se imprimirán."`
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes with the new fields/imports.
    - `npm run lint` passes (0 warnings; `i18next/no-literal-string` clean — every new string routes through `t(...)`).
    - `grep -n "data-testid=\"receipt-live-preview\"" src/widgets/SettingsTabsPanel/tabs/HardwareSettingsTab.tsx` shows exactly one match.
    - `grep -c "headerLine2Label\|footerTextLabel\|previewTitle" src/shared/lib/i18n/locales/en-US/wAdmin.json src/shared/lib/i18n/locales/es-MX/wAdmin.json` shows matches in both files.
  </acceptance_criteria>
  <done>HardwareSettingsTab has working headerLine2/footerText inputs (draft-on-change, save-on-blur) and a live preview panel rendering buildThermalReceiptText against unsaved draft settings, all copy routed through i18n.</done>
</task>

<task type="auto">
  <name>Task 2: e2e — header/footer persistence, live-preview reactivity, and toggle-driven output changes</name>
  <files>e2e/08-settings-receipt.spec.ts</files>
  <read_first>
    e2e/08-settings-receipt.spec.ts (full file — existing 5 tests and their loginAs/openCaja/goto('/settings') setup shape)
    e2e/helpers/auth.ts
    e2e/helpers/supabase.ts
  </read_first>
  <action>
    Add 4 new tests to the existing `test.describe('Receipt / Hardware Settings', ...)` block, following the exact setup shape (`loginAs`, `openCaja(540)`, `page.goto('/settings')`, `page.getByRole('tab', { name: 'Hardware' }).click()`) every existing test in this file already uses, each ending with `await logout(page);`:

    1. "Header line 2 and footer text persist after reload" — `page.locator('#receipt-headerLine2').fill('Gracias por su compra')`, then trigger blur (e.g. `.blur()` or click elsewhere), wait for the field to settle, `page.reload()`, re-navigate to Settings > Hardware, assert `page.locator('#receipt-headerLine2')` `toHaveValue('Gracias por su compra')` — same shape as the existing "Settings persist after reload" test.

    2. "Live preview reflects unsaved footer text edits before save" — `page.locator('#receipt-footerText').fill('Devoluciones en 15 dias')` WITHOUT blurring or reloading, then immediately `await expect(page.getByTestId('receipt-live-preview')).toContainText('Devoluciones en 15 dias')` — proves RCPD-01 criterion 2 (draft state, not yet saved, already reflected in the live preview).

    3. "Live preview omits cashier name when the toggle is off" — `await page.locator('#receipt-showCashierName').setChecked(false)`, then `await expect(page.getByTestId('receipt-live-preview')).not.toContainText('Ana')` (the sample fixture's cashier name) — this is the exact gap RESEARCH.md Pitfall 1 names ("any test that only checks a toggle's UI-persisted checked state... is not proof the wiring works"); this test asserts the *rendered text*, not just the checkbox, closing that gap for the first time in this spec file.

    4. "Paper width change widens the live preview's divider line" — `await page.locator('#paper-width').selectOption('32')`, then `await page.locator('#paper-width').selectOption('48')`, then `await expect(page.getByTestId('receipt-live-preview')).toContainText('-'.repeat(48))` (a 48-char divider line only appears once paper width is 48, proving the preview's line width tracks the unsaved paper-width selection).
  </action>
  <verify>
    <automated>npx playwright test e2e/08-settings-receipt.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/08-settings-receipt.spec.ts` passes all 9 tests (5 existing + 4 new) headless (`channel: 'chrome'`, per `playwright.config.ts`).
    - `grep -c "test('" e2e/08-settings-receipt.spec.ts` returns 9.
  </acceptance_criteria>
  <done>Settings > Hardware's receipt-configuration UI has automated Playwright coverage proving saved AND unsaved-draft settings changes are reflected in real rendered receipt text, not just persisted checkbox/select state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Manager/admin free-text input (headerLine2/footerText) → browser-rendered live preview `<pre>` | Same boundary as Plan 01's T-15-03, now also rendered client-side before save |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-15-04 | Information Disclosure (XSS-shaped) | `HardwareSettingsTab.tsx` new `<pre data-testid="receipt-live-preview">` | low | accept | React JSX text-node rendering (`<pre>{text}</pre>`) already HTML-escapes by construction — no `dangerouslySetInnerHTML` is introduced, identical to the existing production `ReceiptPreview.tsx` pattern this task copies. No new mitigation needed. |
| T-15-05 | Elevation of Privilege | `receipt_settings` write path via this tab | medium | accept | Already correctly enforced pre-existing (Phase 6): RLS requires manager/admin, `ProtectedAction action="manage_settings"` gates the entire tab. This task adds fields to an already-gated form; no new RBAC surface. |

## ASVS Categories

| ASVS Category | Applies | Standard Control |
|----------------|---------|--------------------|
| V5 Input Validation | yes | `maxLength={48}`/`maxLength={480}` on the two new inputs mirror the DB-enforced `VARCHAR(48)`/`VARCHAR(480)` caps (client-side UX guard; the DB constraint remains the actual backstop, per RESEARCH.md V5). |
| V4 Access Control | no (unchanged) | Tab already gated by `ProtectedAction action="manage_settings"` — unaffected by this task. |
</threat_model>

<verification>
`npm run typecheck && npm run lint` pass. `npx playwright test e2e/08-settings-receipt.spec.ts` passes all 9 tests headless.
</verification>

<success_criteria>
The store owner can edit header/footer text and every existing toggle from Settings, see an accurate live preview of unsaved changes rendered through the exact same formatter that prints/emails, and this is proven end-to-end by automated Playwright tests that assert on rendered receipt text — not merely on persisted form-control state.
</success_criteria>

<output>
Create `.planning/phases/15-receipt-designer-layout-branding-logo-printing/15-04-SUMMARY.md` when done.
</output>
