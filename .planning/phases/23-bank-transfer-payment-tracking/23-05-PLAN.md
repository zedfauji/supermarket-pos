---
phase: 23-bank-transfer-payment-tracking
plan: 05
type: execute
wave: 5
depends_on: ["23-01", "23-04"]
files_modified:
  - src/features/export-bank-transfers/model/useExportBankTransfersCsv.ts
  - src/widgets/BankTransfersList/index.tsx
  - src/shared/lib/i18n/locales/en-US/wAdmin.json
  - src/shared/lib/i18n/locales/es-MX/wAdmin.json
  - supabase/migrations/20260831000004_caja_report_bank_transfer_breakout.sql
  - src/shared/lib/domain.ts
  - src/widgets/CajaReportPanel/CajaReportPanel.tsx
  - src/shared/lib/i18n/locales/en-US/wPanels.json
  - src/shared/lib/i18n/locales/es-MX/wPanels.json
autonomous: true
requirements: [BTP-08, BTP-10]
agent_hint: Frontend Developer

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Admin can export the pending+confirmed Bank Transfers list to CSV via the Tauri-native save dialog, using rowsToCsv/sanitizeCsvCell — never a browser Blob download and never a hand-rolled CSV escaper."
    - "A pending or disputed bank-transfer sale still counts toward get_caja_report/get_payment_methods_report revenue totals immediately at checkout, exactly like cash/card, with an additional bankTransferPending breakout so admin can see how much of today's revenue is still unconfirmed."
  artifacts:
    - path: "src/features/export-bank-transfers/model/useExportBankTransfersCsv.ts"
      provides: "Tauri save()+writeFile()-based CSV export hook for the Bank Transfers list"
    - path: "supabase/migrations/20260831000004_caja_report_bank_transfer_breakout.sql"
      provides: "get_caja_report summary gains bankTransferSales + bankTransferPending fields (Pitfall 3 resolution)"
  key_links:
    - from: "useExportBankTransfersCsv"
      to: "rowsToCsv / sanitizeCsvCell (src/shared/lib/exporters/csv.ts)"
      via: "Reused verbatim, not reimplemented (CWE-1236 guard)"
      pattern: "rowsToCsv\\("
    - from: "get_caja_report"
      to: "CajaReportSummarySchema"
      via: "bankTransferSales/bankTransferPending fields flow through CajaReportSchema.parse(data) with zero query-hook changes"
      pattern: "bankTransferPending"
---

<objective>
Close the two remaining BTP requirements that are purely additive on top of the working tracer from
Plans 01-04: CSV export of the Bank Transfers list for end-of-day reconciliation (D-13/BTP-08), and the
`get_caja_report` revenue breakout RESEARCH.md's Pitfall 3 identifies as an unresolved gap (D-15/BTP-10)
— a bank-transfer sale already counts toward total revenue at checkout time (Plan 01, no change needed
there); this plan adds the missing "how much is still unconfirmed" visibility.

Purpose: these are genuinely separable, lower-risk additions once the core reconciliation loop is
proven (Plan 04) — bundling them earlier would have meant re-touching `BankTransfersList` and
`get_caja_report` before their consumers existed.

Output: `useExportBankTransfersCsv`, an "Export CSV" button on `BankTransfersList`, and
`bankTransferSales`/`bankTransferPending` fields on the caja report.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-bank-transfer-payment-tracking/23-CONTEXT.md
@.planning/phases/23-bank-transfer-payment-tracking/23-RESEARCH.md
@.planning/phases/23-bank-transfer-payment-tracking/23-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSV export on the Bank Transfers tab</name>
  <read_first>
    src/features/export-report/model/useExportReport.ts (the exact `save()`
    (`@tauri-apps/plugin-dialog`) + `writeFile()` (`@tauri-apps/plugin-fs`) + `csvToBytes(rowsToCsv(...))`
    pattern — verified real code, reuse verbatim, do NOT use a browser `Blob`/`<a download>`, which is
    what the spike's throwaway HTML did and is explicitly the wrong pattern for this Tauri app);
    src/shared/lib/exporters/csv.ts (`rowsToCsv`/`csvToBytes`/`CsvColumn` — the CWE-1236 guard to
    reuse, never reimplement a `csvCell()`-style sanitizer);
    src/widgets/BankTransfersList/index.tsx (Plan 04 — the widget this task adds a toolbar button to).
  </read_first>
  <files>
    src/features/export-bank-transfers/model/useExportBankTransfersCsv.ts,
    src/widgets/BankTransfersList/index.tsx,
    src/shared/lib/i18n/locales/en-US/wAdmin.json,
    src/shared/lib/i18n/locales/es-MX/wAdmin.json
  </files>
  <action>
    New hook `useExportBankTransfersCsv()`: accepts the currently-loaded transfer rows (pending +
    confirmed only, per D-13 — filter out disputed rows before export), maps each to a flat CSV row
    (Reference Code, Customer, Phone, Amount, Status, Created At, Confirmed By), calls
    `csvToBytes(rowsToCsv(rows, columns))`, then `save({ defaultPath:
    `bank-transfers-${dateStr}.csv`, filters: [{ name: 'CSV', extensions: ['csv'] }] })` from
    `@tauri-apps/plugin-dialog`, returning early (no-op) when the user cancels (`filePath === null`),
    else `writeFile(filePath, bytes)` from `@tauri-apps/plugin-fs` — the exact same three-import shape
    as `useExportReport.ts`. Add an "Export CSV" button to `BankTransfersList`'s toolbar (mirroring
    wherever `RefundsList`/report widgets place their own export controls, or a simple button above the
    `DataTable` if no existing toolbar slot exists), disabled when there are zero pending+confirmed
    rows. Add the new button label / toast copy to both `wAdmin.json` locale files.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rowsToCsv" src/features/export-bank-transfers/model/useExportBankTransfersCsv.ts` is
      greater than 0.
    - `grep -c "Blob\|createObjectURL" src/features/export-bank-transfers/model/useExportBankTransfersCsv.ts`
      equals 0 (never the browser-download anti-pattern).
    - `npm run lint` passes with zero warnings.
  </acceptance_criteria>
  <done>Admin can export the pending+confirmed Bank Transfers list to CSV via the Tauri-native save
  dialog, reusing rowsToCsv's CWE-1236 guard verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: get_caja_report bank-transfer revenue breakout (Pitfall 3 resolution)</name>
  <read_first>
    supabase/migrations/20260828000001_drop_tip_amount.sql lines 869-1056 (the current
    `get_caja_report` body — exact `v_cash_sales`/`v_card_sales`/`v_rappi_sales` aggregation and
    `summary` json_build_object shape to extend);
    src/shared/lib/domain.ts lines 938-949 (`CajaReportSummarySchema` — the schema to extend);
    src/entities/caja/model/queries.ts lines 345-369 (`useCajaReport` — confirms the RPC result flows
    through `CajaReportSchema.parse(data)` with no per-field mapping, so extending the schema + SQL is
    sufficient, no query-hook change needed);
    src/widgets/CajaReportPanel/CajaReportPanel.tsx lines 140-150 (the exact `{ label, value }` summary
    row array to extend for the two new fields).
  </read_first>
  <files>
    supabase/migrations/20260831000004_caja_report_bank_transfer_breakout.sql,
    src/shared/lib/domain.ts,
    src/widgets/CajaReportPanel/CajaReportPanel.tsx,
    src/shared/lib/i18n/locales/en-US/wPanels.json,
    src/shared/lib/i18n/locales/es-MX/wPanels.json
  </files>
  <action>
    New migration: `CREATE OR REPLACE FUNCTION public.get_caja_report(p_caja_id uuid)` — reproduce the
    full body from `20260828000001_drop_tip_amount.sql` lines 869-1056 verbatim, plus: a new
    `v_bank_transfer_sales NUMERIC(12,2)` variable added to the payment-aggregates SELECT (line
    ~921-930) as `COALESCE(SUM(CASE WHEN method = 'bank_transfer' THEN amount ELSE 0 END), 0)`
    alongside the existing three method sums (this does NOT change `v_total_revenue`'s unconditional
    sum, so a bank-transfer sale already counts toward total revenue immediately, unchanged); a new
    `v_bank_transfer_pending NUMERIC(12,2)` computed via a second query summing `payments.amount` for
    rows joined to `bank_transfers` where `bank_transfers.status = 'pending'` and `payments.tab_id =
    ANY(v_tab_ids)` (the "how much of today's revenue is still unconfirmed" breakout D-15 asks for);
    add both as `'bankTransferSales', v_bank_transfer_sales` and `'bankTransferPending',
    v_bank_transfer_pending` keys inside the existing `'summary', json_build_object(...)` block, right
    after `'rappiSales'`. `get_payment_methods_report` needs NO change — it already groups generically
    by `p.method`, so a `bank_transfer` row appears as its own bucket automatically (verified this
    session).

    In `domain.ts`'s `CajaReportSummarySchema`, add `bankTransferSales: MoneySchema` and
    `bankTransferPending: MoneySchema` fields, same position as the SQL. In `CajaReportPanel.tsx`, add
    two entries to the existing summary-row array (after the `rappiSales` entry): `{ label:
    t('cajaReportPanel.bankTransferSales'), value: report.summary.bankTransferSales }` and `{ label:
    t('cajaReportPanel.bankTransferPending'), value: report.summary.bankTransferPending }`. Add both
    new label keys to `wPanels.json` in both locales.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bankTransferSales\|bankTransferPending" src/shared/lib/domain.ts` is greater than or
      equal to 2.
    - `grep -c "v_total_revenue" supabase/migrations/20260831000004_caja_report_bank_transfer_breakout.sql`
      equals 1 occurrence in the aggregate SELECT and its formula is unchanged (still an unconditional
      SUM across all methods) — confirms this migration does not silently exclude pending bank
      transfers from total revenue, which would contradict D-15.
    - Apply the migration (same `supabase db push` / documented fallback as Plan 01 Task 2) and confirm
      via a direct RPC call against a caja session with at least one bank_transfer payment that
      `summary.bankTransferSales` and `summary.bankTransferPending` are both present and numeric in the
      response.
  </acceptance_criteria>
  <done>get_caja_report exposes bankTransferSales and bankTransferPending without changing
  totalRevenue's existing unconditional-sum semantics; CajaReportPanel displays both.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| BankTransfersList rows (customer_name/customer_phone) -> exported CSV file on disk | Untrusted free text crosses into a file later opened in Excel/Sheets |
| get_caja_report summary -> CajaReportPanel | Aggregate financial figures exposed to any `view_reports`-permitted role |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-11 | Tampering (CSV formula injection, CWE-1236) | useExportBankTransfersCsv | high | mitigate | Reuses `rowsToCsv`/`sanitizeCsvCell` verbatim (already implements the `=,+,-,@,\t,\r` prefix-escape and RFC-4180 quoting via `xlsx`) — never a hand-rolled `csvCell()`, which is exactly the anti-pattern RESEARCH.md flags the spike's throwaway HTML used |
| T-23-12 | Information Disclosure | get_caja_report bankTransferPending breakout | low | accept | Exposed at the same RBAC level (`view_reports`) as the existing `cashSales`/`cardSales` fields in the same report response — no new authorization boundary is needed since this is not a new access surface, only a new field on an already-gated report |

</threat_model>

<verification>
`npm run typecheck && npm run lint && npm run test`
</verification>

<success_criteria>
Admin can export the pending+confirmed Bank Transfers list to CSV via the Tauri-native save dialog with
CWE-1236-safe escaping, and the caja report shows both total bank-transfer revenue and the
still-pending portion without altering existing total-revenue semantics.
</success_criteria>

<output>
Create `.planning/phases/23-bank-transfer-payment-tracking/23-05-SUMMARY.md` when done
</output>
