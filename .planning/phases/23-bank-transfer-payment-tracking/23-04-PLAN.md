---
phase: 23-bank-transfer-payment-tracking
plan: 04
type: execute
wave: 4
depends_on: ["23-01", "23-02", "23-03"]
files_modified:
  - src/widgets/BankTransfersList/index.tsx
  - src/pages/payments/index.tsx
  - src/shared/lib/i18n/locales/en-US/pages.json
  - src/shared/lib/i18n/locales/es-MX/pages.json
  - src/shared/lib/i18n/locales/en-US/wAdmin.json
  - src/shared/lib/i18n/locales/es-MX/wAdmin.json
  - e2e/payments/bank-transfers-tab.spec.ts
autonomous: true
requirements: [BTP-03, BTP-04, BTP-05, BTP-07, BTP-09]
agent_hint: Frontend Developer

estimate:
  tokens: 55000
  raw_tokens: 110000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "A manager/admin on /payments -> Bank Transfers tab sees every pending/confirmed/disputed sale oldest-first, with reference code, customer name/phone, elapsed time, and status badge; a pending sale past 8 hours is visually flagged as stale."
    - "The full tracer happy path — cashier marks a sale pending at checkout, manager confirms it on this tab — is proven end-to-end by one real Playwright test driving both real UIs, not seeded shortcuts on either end."
    - "A cashier-role account never sees Confirm/Dispute buttons render, and is independently denied server-side if it calls the RPCs directly (BTP-09 defense in depth)."
    - "No confirm/dispute action fires without an explicit manager+ tap through ManagerPinDialog — there is no code path that auto-resolves an unambiguous-looking pending transfer (D-06/BTP-05)."
  artifacts:
    - path: "src/widgets/BankTransfersList/index.tsx"
      provides: "DataTable of pending/confirmed/disputed bank transfers with Confirm/Dispute row actions and a stale-pending visual flag"
    - path: "e2e/payments/bank-transfers-tab.spec.ts"
      provides: "The phase's core tracer E2E proof: checkout mark-pending (real UI) -> manager confirm (real UI)"
  key_links:
    - from: "src/pages/payments/index.tsx"
      to: "BankTransfersList"
      via: "New 'bankTransfers' TabsTrigger/TabsContent, third tab alongside payments/refunds"
      pattern: "value=\"bankTransfers\""
    - from: "BankTransfersList row actions"
      to: "canAccess(role, 'confirm_transfer_payment' | 'dispute_transfer_payment')"
      via: "Client-side row-action visibility gate (UX only — Plan 01's RPC role check is the real boundary)"
      pattern: "canAccess\\("
---

<objective>
Compose Plan 03's hooks and dialogs into the "Bank Transfers" tab on the existing `/payments` page
(D-11: not a new route), the one place inside the POS where a manager/admin performs the reconciliation
they've already done by eye against their own banking app outside the POS (D-05 — bank data is never
imported into this screen, only typed/selected by a human who already checked it elsewhere), and prove the phase's core tracer — checkout mark-pending through manager
confirm — end-to-end with one real Playwright test driving both real UIs. This is the "Last task: Wire"
step of interface-first ordering, and the point at which the quality gate's tracer requirement
("checkout mark-pending -> manager confirm, at minimum, verified before expansion tasks") is satisfied.

Purpose: gives the admin/manager the actual reconciliation surface the whole phase exists to replace
the paper name+phone slip with, and closes the loop the previous three plans built toward in isolation.

Output: `BankTransfersList` widget, a third `TabsTrigger`/`TabsContent` on `PaymentsPage`, and a green
end-to-end Playwright spec covering the happy path, dispute, Luhn-mismatch rejection, and RBAC denial.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-bank-transfer-payment-tracking/23-CONTEXT.md
@.planning/phases/23-bank-transfer-payment-tracking/23-RESEARCH.md
@.planning/phases/23-bank-transfer-payment-tracking/23-02-SUMMARY.md
@.planning/phases/23-bank-transfer-payment-tracking/23-03-SUMMARY.md
@.planning/spikes/005-payments-transfer-view/bank-transfers-tab.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: BankTransfersList widget + Bank Transfers tab on PaymentsPage</name>
  <read_first>
    src/widgets/RefundsList/index.tsx (full file — the exact DataTable/columns/EmptyState pattern to
    mirror, including `useTranslation('wAdmin')` and the truncated-ID display style for staff
    attribution);
    src/pages/payments/index.tsx (full file, currently 2 tabs — the exact TabsTrigger/TabsContent
    injection point for a third tab);
    src/entities/bank-transfer (from Plan 03 — usePendingTransfers/useAllTransfers, BankTransfer type);
    src/features/confirm-dispute-transfer (from Plan 03 — ConfirmTransferDialog/DisputeTransferDialog,
    canAccess-gated row actions to mirror against `src/shared/lib/rbac.ts`'s `canAccess` signature);
    .planning/spikes/005-payments-transfer-view/bank-transfers-tab.html (validated UI shape and status
    filter tabs — reference for layout only; do NOT port its Blob-based CSV export or hand-rolled
    `csvCell()`, both explicitly out of scope for this plan per RESEARCH.md's Anti-Patterns section —
    CSV export is Plan 05's job).
  </read_first>
  <files>
    src/widgets/BankTransfersList/index.tsx,
    src/pages/payments/index.tsx,
    src/shared/lib/i18n/locales/en-US/pages.json,
    src/shared/lib/i18n/locales/es-MX/pages.json,
    src/shared/lib/i18n/locales/en-US/wAdmin.json,
    src/shared/lib/i18n/locales/es-MX/wAdmin.json
  </files>
  <action>
    `BankTransfersList`: mirror `RefundsList`'s `DataTable` structure exactly (`useTranslation('wAdmin')`,
    `buildColumns(t)` factory, `EmptyState`). Default to `useAllTransfers()` with a status filter
    (pending/confirmed/disputed/all, default pending) implemented client-side over the fetched rows —
    columns: reference code (monospace `Badge`-style span), customer name + phone (two-line cell like
    `RefundsList`'s truncated-id pattern), amount (`MoneyDisplay`), elapsed time (compute
    `Date.now() - createdAt`, format as minutes/hours/days; when `status === 'pending'` and elapsed
    exceeds a named constant `STALE_PENDING_THRESHOLD_MS = 8 * 60 * 60 * 1000` — hardcoded per D-17,
    not a Settings toggle — render the elapsed cell with a distinct warning style), status
    (`Badge` variant per status), row actions (only when `status === 'pending'`: "Confirm" and
    "Dispute" buttons, each rendered only when `canAccess(currentRole, 'confirm_transfer_payment')` /
    `canAccess(currentRole, 'dispute_transfer_payment')` respectively — read the current staff role
    from the same store `ManagerPinDialog`'s callers already use — opening `ConfirmTransferDialog`/
    `DisputeTransferDialog` from Plan 03). Non-pending rows show "by {confirmedBy/disputedBy}" or the
    dispute reason, truncated-ID style, matching `RefundsList`'s existing simplicity (no extra
    profile-name JOIN).

    In `src/pages/payments/index.tsx`, add a third `TabsTrigger value="bankTransfers"` and matching
    `TabsContent` rendering `<BankTransfersList />`, using the exact same `className="flex flex-1
    overflow-hidden p-4"` as the existing `refunds` tab. Add `payments.tabs.bankTransfers` to both
    `pages.json` locale files alongside the existing `payments`/`refunds` keys. Add the new
    `bankTransfersList.*` column/empty-state/status-badge copy keys to both `wAdmin.json` locale files,
    mirroring `refundsList.*`'s exact key shape.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bankTransfers" src/pages/payments/index.tsx` is greater than 0 (third tab present).
    - `npm run lint` passes with zero warnings.
    - Both locale files for `pages.json` and `wAdmin.json` contain the same new key sets.
  </acceptance_criteria>
  <done>The Bank Transfers tab exists on /payments, lists all transfers with a stale-pending visual
  flag, and gates Confirm/Dispute row actions by RBAC client-side.</done>
</task>

<task type="auto">
  <name>Task 2: Full tracer E2E — checkout mark-pending through manager confirm/dispute</name>
  <read_first>
    e2e/payments/refund.spec.ts (full file — seeding helpers, `loginAs`/`logout` cross-role flow,
    `ManagerPinDialog` PINKeypad button-clicking convention, `requireIntegrationEnv()` gate — the
    closest existing analog for a manager-PIN-gated terminal action spec);
    e2e/checkout/bank-transfer-checkout.spec.ts (Plan 02 — the checkout half of this flow, reused here
    rather than re-derived);
    e2e/helpers/auth.ts (`loginAs`, `logout`).
  </read_first>
  <files>
    e2e/payments/bank-transfers-tab.spec.ts
  </files>
  <action>
    New spec, `requireIntegrationEnv()`-gated. Test 1 (the phase's core tracer proof): log in as
    cashier, complete a bank-transfer checkout at `/pos` (reuse the same flow Plan 02's spec drives —
    add item, select Bank Transfer, fill customer name/phone, submit), capture the displayed reference
    code from the UI; log out, log in as manager; navigate to `/payments` -> Bank Transfers tab; find
    the row for that sale (match by reference code or customer name); click Confirm; enter the captured
    code into the `ConfirmTransferDialog` input; complete the `ManagerPinDialog` PIN entry via PINKeypad
    button clicks (mirror `refund.spec.ts`'s exact button-press convention); assert the row now shows
    status Confirmed and moves off the default pending filter; assert via the service-role client that
    `bank_transfers.status = 'confirmed'` and an `audit_logs` row exists with `action =
    'payment.transfer_confirmed'`. Test 2: same checkout setup, this time click Dispute, submit with an
    empty reason -> assert the dialog blocks submission (button stays disabled, no RPC call fires);
    fill in a real reason -> assert success, `bank_transfers.status = 'disputed'`, `dispute_reason`
    stored, `audit_logs` row for `payment.transfer_disputed`. Test 3: attempt Confirm with a
    Luhn-invalid code (mutate one digit) -> assert an inline validation error renders and no RPC call
    is made (network-idle assertion or a `page.route` interception counting calls to
    `confirm_transfer_payment`) — proves BTP-05's "no auto-confirm, always an explicit correctly-typed
    tap" end to end. Test 4 (BTP-09, RBAC denial): log in as cashier, navigate to the Bank Transfers
    tab -> assert no Confirm/Dispute buttons render for any row (UI-level denial); separately, call
    `confirm_transfer_payment` directly via the cashier's authenticated Supabase client (not through the
    UI) against a seeded pending transfer -> assert the RPC itself returns an `AUTH_FORBIDDEN` error
    (server-side denial, independent of the UI gate).
  </action>
  <verify>
    <automated>npx playwright test e2e/payments/bank-transfers-tab.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 4 tests pass headless.
    - Test 1 drives real checkout UI through to real confirm UI in one continuous flow — no
      service-role shortcut on either end of the happy path (that would only prove the RPC layer
      again, which Plan 01 already did; this test's job is proving the UI wiring).
  </acceptance_criteria>
  <done>The phase's tracer requirement is satisfied: checkout mark-pending through manager confirm is
  proven end-to-end via real UI, plus dispute, Luhn-mismatch rejection, and RBAC-denial coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser render -> BankTransfersList table | Untrusted `customer_name`/`customer_phone` from the DB rendered as table cells |
| Cashier browser -> row-action visibility | Client-side-only gate; the real boundary is Plan 01's server-side RPC check |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-09 | Elevation of Privilege (UI-level) | BankTransfersList row actions | low | mitigate | `canAccess(role, 'confirm_transfer_payment'\|'dispute_transfer_payment')` hides the buttons client-side; Plan 01's server-side `auth.uid()` role check remains the actual security boundary, proven independently by this plan's Test 4 |
| T-23-10 | Tampering (rendering) | customer_name/customer_phone table cells | low | mitigate | Rendered as plain React text nodes (JSX default escaping), same as every other `DataTable` column in this codebase (e.g. `RefundsList`) — no new sink introduced |

</threat_model>

<verification>
`npm run typecheck && npm run lint && npx playwright test e2e/payments/bank-transfers-tab.spec.ts`
</verification>

<success_criteria>
The Bank Transfers tab is reachable from `/payments`, lists every transfer with the required columns
and stale-pending flag, and the phase's core tracer (checkout mark-pending -> manager confirm) is
proven end-to-end by a real, green Playwright test — satisfying this plan's tracer gate before any
further expansion (CSV export, reporting breakout) proceeds in Plan 05.
</success_criteria>

<output>
Create `.planning/phases/23-bank-transfer-payment-tracking/23-04-SUMMARY.md` when done
</output>
