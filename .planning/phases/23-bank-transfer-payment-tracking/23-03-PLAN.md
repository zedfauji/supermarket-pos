---
phase: 23-bank-transfer-payment-tracking
plan: 03
type: execute
wave: 3
depends_on: ["23-01", "23-02"]
files_modified:
  - src/shared/lib/bank-transfer-code.ts
  - src/shared/lib/bank-transfer-code.test.ts
  - src/entities/bank-transfer/model/queries.ts
  - src/entities/bank-transfer/index.ts
  - src/features/confirm-dispute-transfer/model/useConfirmTransfer.ts
  - src/features/confirm-dispute-transfer/model/useDisputeTransfer.ts
  - src/features/confirm-dispute-transfer/model/useConfirmTransfer.test.ts
  - src/features/confirm-dispute-transfer/ui/ConfirmTransferDialog.tsx
  - src/features/confirm-dispute-transfer/ui/DisputeTransferDialog.tsx
  - src/features/confirm-dispute-transfer/index.ts
  - src/shared/lib/i18n/locales/en-US/featOrders.json
  - src/shared/lib/i18n/locales/es-MX/featOrders.json
autonomous: true
requirements: [BTP-01, BTP-03, BTP-04]
agent_hint: Frontend Developer

estimate:
  tokens: 50000
  raw_tokens: 100000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "A client-side Luhn check (ported verbatim from the spike, same algorithm as the server) gives instant typo feedback on a 7-digit code before any RPC round-trip."
    - "useConfirmTransfer/useDisputeTransfer call the Plan 01 RPCs and map AUTH_FORBIDDEN/VALIDATION_ERROR/PAYMENT_ALREADY_PROCESSED into typed AppError results, mirroring useProcessRefund.ts exactly."
    - "ConfirmTransferDialog and DisputeTransferDialog use ConfirmDialog's children slot for code/reason entry — no native prompt()/confirm()/alert() anywhere (D-14)."
  artifacts:
    - path: "src/shared/lib/bank-transfer-code.ts"
      provides: "luhnCheckDigit / generateCode / isValidCode / generateUniqueCode (TS port of reference-code.cjs)"
    - path: "src/features/confirm-dispute-transfer/model/useConfirmTransfer.ts"
      provides: "TanStack mutation hook calling confirm_transfer_payment"
    - path: "src/features/confirm-dispute-transfer/model/useDisputeTransfer.ts"
      provides: "TanStack mutation hook calling dispute_transfer_payment"
    - path: "src/features/confirm-dispute-transfer/ui/ConfirmTransferDialog.tsx"
      provides: "Code-entry dialog gated by ManagerPinDialog, wired in Plan 04"
    - path: "src/features/confirm-dispute-transfer/ui/DisputeTransferDialog.tsx"
      provides: "Reason-entry dialog gated by ManagerPinDialog, wired in Plan 04"
  key_links:
    - from: "ConfirmTransferDialog"
      to: "ManagerPinDialog requiredAction=confirm_transfer_payment"
      via: "Two-step gate: code entry -> PIN confirm -> mutation fires on PIN success"
      pattern: "requiredAction=\"confirm_transfer_payment\""
---

<objective>
Build the reconciliation feature's business-logic layer — the client-side Luhn utility, the two
manager+-gated mutation hooks, and their dialogs — decoupled from the list widget that will consume
them in Plan 04. This plan is verified in isolation (unit tests for the pure logic, component tests for
the dialogs against a directly-seeded `bank_transfers` row) rather than through the full checkout ->
confirm UI round-trip, which Plan 04 proves once this plan's pieces exist.

Purpose: Interface-first ordering — Plan 04's widget/page wiring task consumes these hooks/dialogs as
finished contracts instead of building business logic and composition in the same task.

Output: `bank-transfer-code.ts` (+ test), `entities/bank-transfer/model/queries.ts`,
`features/confirm-dispute-transfer/` (hooks + dialogs).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-bank-transfer-payment-tracking/23-CONTEXT.md
@.planning/phases/23-bank-transfer-payment-tracking/23-RESEARCH.md
@.planning/phases/23-bank-transfer-payment-tracking/23-01-SUMMARY.md
@.planning/spikes/003-reference-code-design/reference-code.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port the Luhn reference-code utility to TypeScript (RED -&gt; GREEN)</name>
  <read_first>
    .planning/spikes/003-reference-code-design/reference-code.cjs (the full file — algorithm and its
    own `demo()` self-check assertions, which this task ports into real Vitest cases, not re-derives).
  </read_first>
  <behavior>
    `src/shared/lib/bank-transfer-code.test.ts`, written first:
    - `generateCode()` always returns a 7-digit string that `isValidCode()` accepts (loop 1000 times).
    - Every single-digit transcription error is caught by `isValidCode` (same mutation-sweep the
      spike's `demo()` runs: for 500 generated codes, flip each of the 6 payload digits to every other
      digit, assert 100% of mutations are rejected).
    - Adjacent-digit transposition is caught in the vast majority of cases (>85%, same threshold and
      method as the spike, over 2000 sampled transpositions), with the known `09<->90` blind spot
      explicitly reproduced and asserted (documents the real ceiling, does not overclaim).
    - `generateUniqueCode(pendingCodes)` never returns a code already in the `pendingCodes` Set.
    Run first — MUST fail (module does not exist) — RED. Then implement.
  </behavior>
  <files>
    src/shared/lib/bank-transfer-code.ts,
    src/shared/lib/bank-transfer-code.test.ts
  </files>
  <action>
    Port `luhnCheckDigit`, `generateCode`, `isValidCode`, `generateUniqueCode` from
    `reference-code.cjs` into `bank-transfer-code.ts` as named TS exports with the exact same
    algorithm and signatures (typed: `luhnCheckDigit(payloadDigits: string): number`,
    `generateCode(randomFn?: () => number): string`, `isValidCode(code: string): boolean`,
    `generateUniqueCode(pendingCodes: Set&lt;string&gt;, randomFn?: () => number): string`). This exists
    purely for instant client-side typo feedback (D-08) — the server-side PL/pgSQL port from Plan 01
    remains the sole authority; never trust this module's result as the security boundary.
  </action>
  <verify>
    <automated>npx vitest run src/shared/lib/bank-transfer-code.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The test file fails before `bank-transfer-code.ts` exists (module-not-found), then all cases pass
      after implementation.
    - `npm run typecheck` passes.
  </acceptance_criteria>
  <done>bank-transfer-code.ts exists, is verbatim-ported from the spike, and its test file is green —
  satisfies VALIDATION.md's Wave 0 requirement for BTP-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Confirm/dispute mutation hooks + entity query hooks</name>
  <read_first>
    src/features/process-refund/model/useProcessRefund.ts (full file — the exact mutation-hook shape,
    error-code mapping via `.includes()` substring checks, and query invalidation pattern to mirror);
    src/features/process-refund/model/useProcessRefund.test.ts (test structure to mirror);
    src/entities/refund/model/queries.ts (the exact `useRefunds`/query-key pattern to mirror for
    `usePendingTransfers`/`useAllTransfers`);
    src/entities/bank-transfer/model/types.ts (created in Plan 01 — the Zod-inferred types this task
    consumes, do not redefine).
  </read_first>
  <behavior>
    `src/features/confirm-dispute-transfer/model/useConfirmTransfer.test.ts`, written first (mirroring
    `useProcessRefund.test.ts`):
    - mocked `supabase.rpc('confirm_transfer_payment', ...)` success -> hook resolves `ok(true)` and
      invalidates the bank-transfer query keys.
    - mocked RPC error containing `'AUTH_FORBIDDEN'` -> hook resolves `err({ code: 'AUTH_FORBIDDEN', ... })`.
    - mocked RPC error containing `'VALIDATION_ERROR'` -> hook resolves `err({ code: 'VALIDATION_ERROR', ... })`.
    - mocked RPC error containing `'PAYMENT_ALREADY_PROCESSED'` -> hook resolves `err({ code:
      'PAYMENT_ALREADY_PROCESSED', ... })`.
    Run first — MUST fail (hook does not exist) — RED. Then implement `useConfirmTransfer` and (same
    shape, no dedicated test file needed given the near-identical mapping) `useDisputeTransfer`.
  </behavior>
  <files>
    src/features/confirm-dispute-transfer/model/useConfirmTransfer.ts,
    src/features/confirm-dispute-transfer/model/useDisputeTransfer.ts,
    src/features/confirm-dispute-transfer/model/useConfirmTransfer.test.ts,
    src/entities/bank-transfer/model/queries.ts,
    src/entities/bank-transfer/index.ts
  </files>
  <action>
    `useConfirmTransfer`: TanStack mutation hook validating input via `ConfirmTransferInputSchema`
    (from `@entities/bank-transfer`), calling `supabase.rpc('confirm_transfer_payment', {
    p_payment_id: parsed.data.paymentId, p_entered_code: parsed.data.enteredCode })` via
    `supabaseMutation`, mapping `AUTH_FORBIDDEN`/`VALIDATION_ERROR`/`PAYMENT_ALREADY_PROCESSED`
    substring matches to typed `AppError`s exactly as `useProcessRefund.ts` does for its own codes, and
    on success invalidating a new `bankTransferKeys.lists()` query key. `useDisputeTransfer`: identical
    shape calling `dispute_transfer_payment` with `p_payment_id`/`p_reason`, same error mapping plus
    the same success invalidation.

    `entities/bank-transfer/model/queries.ts`: `bankTransferKeys` (all/lists/detail, mirroring
    `refundKeys`), `usePendingTransfers()` and `useAllTransfers()` TanStack queries reading
    `bank_transfers` joined to `payments` (for `amount`/`reference_number`) and `tabs` (for
    `customer_name` via `payments.tab_id`) — select string:
    `'*, payments!inner(amount, reference_number, tab_id, tabs!inner(customer_name))'` — mapping each
    row into the `BankTransfer` shape (referenceCode from `payments.reference_number`, amount from
    `payments.amount`, customerName from the nested `tabs.customer_name`). `usePendingTransfers`
    filters `.eq('status', 'pending')`; `useAllTransfers` has no status filter, both order
    `.order('created_at', { ascending: true })` (oldest-first per D-12). `entities/bank-transfer/index.ts`:
    barrel export of the hooks + re-exported types from `./model/types`.
  </action>
  <verify>
    <automated>npx vitest run src/features/confirm-dispute-transfer/model/useConfirmTransfer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 4 test cases fail before implementation, pass after (RED then GREEN).
    - `npm run typecheck` passes.
  </acceptance_criteria>
  <done>useConfirmTransfer/useDisputeTransfer exist with typed error mapping; usePendingTransfers/
  useAllTransfers exist and compile against the joined query shape.</done>
</task>

<task type="auto">
  <name>Task 3: ConfirmTransferDialog + DisputeTransferDialog</name>
  <read_first>
    src/features/process-refund/ui/RefundSheet.tsx (lines around the `ManagerPinDialog` usage —
    exact two-step gate: user action -> open ManagerPinDialog(requiredAction) -> onSuccess fires the
    mutation);
    src/shared/ui/ConfirmDialog.tsx (full file — `children` slot, `confirmDisabled` prop for gating
    submit until the code is valid);
    src/features/manager-pin-gate/ui/ManagerPinDialog.tsx (the `{ open, onOpenChange, requiredAction,
    onSuccess }` contract).
  </read_first>
  <files>
    src/features/confirm-dispute-transfer/ui/ConfirmTransferDialog.tsx,
    src/features/confirm-dispute-transfer/ui/DisputeTransferDialog.tsx,
    src/features/confirm-dispute-transfer/index.ts,
    src/shared/lib/i18n/locales/en-US/featOrders.json,
    src/shared/lib/i18n/locales/es-MX/featOrders.json
  </files>
  <action>
    `ConfirmTransferDialog({ open, transfer, onOpenChange })`: step 1 renders `ConfirmDialog` with a
    plain shadcn `Input` (maxLength 7, numeric) in the `children` slot for the reference code; gate
    `confirmDisabled` on `!isValidCode(enteredCode)` from `bank-transfer-code.ts` (instant client-side
    typo feedback, D-08) — show an inline error string when the format is wrong but do not call the RPC
    yet. On that dialog's confirm, close it and open `ManagerPinDialog` with
    `requiredAction="confirm_transfer_payment"`; its `onSuccess` calls `useConfirmTransfer().mutateAsync({
    paymentId: transfer.paymentId, enteredCode })`, shows a `sonner` toast on success/failure (mirror
    `RefundSheet`'s `toast.error(result.error.message)`/`toast.success(...)` pattern), and closes.
    `DisputeTransferDialog({ open, transfer, onOpenChange })`: same two-step shape with a reason
    `Input`/`Textarea` (required, `confirmDisabled` on empty/whitespace-only), `requiredAction`
    `"dispute_transfer_payment"`, firing `useDisputeTransfer().mutateAsync({ paymentId: transfer.paymentId,
    reason })`. Neither dialog uses `prompt()`/`confirm()`/`alert()` anywhere (D-14). Barrel-export both
    from `features/confirm-dispute-transfer/index.ts`. Add the new copy keys (dialog titles,
    descriptions, code/reason input labels and placeholders, validation error strings, success/error
    toast copy) to both `featOrders.json` locale files, following this repo's i18n catalog rule
    (genuine es-MX content, not a literal English copy).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "prompt(\|confirm(\|alert(" src/features/confirm-dispute-transfer/ui/` returns 0 across
      both files.
    - `npm run lint` passes with zero warnings.
    - Both `featOrders.json` locale files contain the same new key set.
  </acceptance_criteria>
  <done>ConfirmTransferDialog and DisputeTransferDialog exist, wired to the Task 2 hooks via the
  ManagerPinDialog two-step gate, ready for Plan 04 to compose into the Bank Transfers list widget.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Manager/admin browser -> useConfirmTransfer/useDisputeTransfer -> RPC | Untrusted entered-code/reason text crosses into the Plan 01 RPCs via these hooks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-07 | Tampering | bank-transfer-code.ts client-side Luhn check | low | accept | This client util exists purely for instant typo feedback (D-08); it is bypassable by a hand-crafted RPC call, but the Plan 01 server-side RPC independently re-validates Luhn and the exact code match — the client check is never the security boundary |
| T-23-08 | Information Disclosure | dispute reason free text | low | accept | Same handling and exposure level as the existing `refunds.reason` field (`RefundSheet`) — no new sink introduced by mirroring that precedent |

</threat_model>

<verification>
`npm run typecheck && npm run lint && npx vitest run src/shared/lib/bank-transfer-code.test.ts src/features/confirm-dispute-transfer/model/useConfirmTransfer.test.ts`
</verification>

<success_criteria>
bank-transfer-code.ts, the two mutation hooks, the two entity query hooks, and both dialogs exist,
compile, lint clean, and their unit tests are green — ready for Plan 04 to compose into the Bank
Transfers tab.
</success_criteria>

<output>
Create `.planning/phases/23-bank-transfer-payment-tracking/23-03-SUMMARY.md` when done
</output>
