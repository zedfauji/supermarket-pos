---
phase: 19-store-local-durable-printing-service
plan: 08
type: execute
wave: 6
depends_on: ["19-02", "19-05", "19-07"]
files_modified:
  - e2e/receipts/broker-submission.spec.ts
  - e2e/receipts/unknown-status-confirm.spec.ts
  - e2e/audit/print-jobs.spec.ts
  - scripts/verify-lan-broker-reachability.ps1
autonomous: true
requirements: [PRN-01, PRN-02, PRN-03, PRN-04, PRN-05, PRN-06, PRN-07]

estimate:
  tokens: 50000
  raw_tokens: 100000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "The same job_id a mocked broker response returns from a submission is the exact value later shown in the Print Jobs table's Job ID column and in ReprintButton's status badge lookup — correlation ID propagates unbroken through every boundary this phase's E2E suite can exercise (PRN-04)."
    - "A dedicated e2e spec drives ReprintButton's full 'Did this print?' Yes/No flow end-to-end (D-05/D-06/D-07/D-08), not just the isolated component test from Plan 19-06."
    - "A dedicated e2e spec drives the Print Jobs audit tab end-to-end: filter, open detail Sheet, verify timeline renders, verify a cashier session is redirected away from /audit the same way it already is today for the Audit Log tab."
    - "Every fault case in this phase's validation strategy (broker unavailable, auth failure, invalid payload, persistence failure, stopped spooler, offline printer, retry exhaustion, duplicate idempotency key, ambiguous handoff, restart recovery) has a named, passing automated test somewhere across the broker crate's cargo test suite — this plan audits for and closes any gap rather than assuming prior plans already covered all ten."
    - { statement: "Cross-machine LAN/VPN reachability (the firewall rule from Plan 19-02 actually admitting a second real machine's traffic, not just loopback) is proven by a runnable automated script — this sandboxed planning/execution environment has no second real machine or VM available to run it against, so this truth is flagged for confirmation at real deployment rather than claimed as verified here.", verification: backstop }
  artifacts:
    - e2e/receipts/broker-submission.spec.ts
    - e2e/receipts/unknown-status-confirm.spec.ts
    - e2e/audit/print-jobs.spec.ts
    - scripts/verify-lan-broker-reachability.ps1
  key_links:
    - "e2e/receipts/broker-submission.spec.ts -> dual-global __TAURI__/__TAURI_INTERNALS__ invoke mock (never page.route(), since the broker HTTP call is Rust-side reqwest, not a browser fetch) -> asserts every one of the 5 caller files' toast/badge behavior"
    - "e2e/audit/print-jobs.spec.ts -> loginAs(page,'manager') -> /audit -> Print Jobs tab -> PrintJobsTable/PrintJobFilterBar/PrintJobDetailSheet"
  prohibitions:
    - "MUST NOT use page.route() to mock broker responses in any new Playwright spec in this plan — the broker HTTP call happens Rust-side inside the Tauri command (reqwest), never as a browser fetch(), so only the invoke() boundary is interceptable from Playwright (PRN-02/test-correctness)."
    - "MUST NOT report the cross-machine LAN reachability truth as fully verified when no second real machine was actually exercised — the script and its absence-of-a-second-machine limitation must both be stated plainly in this plan's SUMMARY, not glossed over (Pitfall 1/honesty)."
---

<objective>
Close out the phase: prove the full fault-test matrix this phase's validation strategy requires, add the
two remaining end-to-end Playwright specs (the unknown-status confirm flow and the Print Jobs audit tab,
both previously only unit/component-tested), audit for and fill any gap in the ten-case broker fault
matrix, and produce the automated cross-machine LAN/VPN reachability script Spike 001 explicitly left
open — while being honest that this sandboxed environment cannot itself exercise a second real machine.

Purpose: This is the phase's closing verification wave — every requirement (PRN-01 through PRN-07) gets
at least one end-to-end assertion here, on top of the per-plan unit/component coverage already built.
Output: Three new Playwright specs; a gap-audit pass over the broker's cargo test suite; a scripted,
runnable (but not-yet-run-against-real-hardware) LAN reachability check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-store-local-durable-printing-service/19-VALIDATION.md
@.planning/phases/19-store-local-durable-printing-service/19-RESEARCH.md
@.planning/phases/19-store-local-durable-printing-service/19-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>End-to-end broker-submission fault matrix across all five callers + correlation-ID assertion</name>
  <files>e2e/receipts/broker-submission.spec.ts</files>
  <read_first>
    e2e/receipts/print-retry-resilience.spec.ts (full file — the exact dual-global Tauri IPC mock pattern
    every new spec in this plan reuses; do not invent a second mocking approach)
    e2e/receipts/broker-test-print.spec.ts (Plan 19-01/19-01-Task-2 — the existing minimal broker mock
    spec this task's new spec extends the pattern from, for the remaining callers)
  </read_first>
  <action>
    Create e2e/receipts/broker-submission.spec.ts covering, via the dual-global __TAURI__/
    __TAURI_INTERNALS__ invoke mock (never page.route()): (1) a cash checkout (PaymentForm) where
    invoke('print_receipt') resolves {job_id: 'e2e-fixture-job-1', status: 'accepted'} — assert no toast
    appears (no-success-toast rule) and, after navigating to /audit's Print Jobs tab with the mocked
    invoke('get_print_jobs')/invoke('get_print_job') returning a job row whose job_id is the SAME
    'e2e-fixture-job-1', assert that exact ID renders in the table (the correlation-ID propagation
    truth); (2) the same checkout with invoke('print_receipt') rejecting with an Error message containing
    "broker unreachable" — assert the toast shows the exact translated 'printJobError.brokerUnreachable'
    copy; (3) with the rejection message NOT containing "broker unreachable" (a generic rejection,
    simulating a 4xx from the broker) — assert the 'printJobError.rejected' copy instead; (4) a Caja
    Print Summary click (CajaDashboard) and a Settings Hardware Test Print click (HardwareSettingsTab)
    each repeating the unreachable/rejected pair, proving all five caller files (not just PaymentForm)
    show the correct failure-class copy — reuse e2e/receipts/settings.spec.ts's existing navigation-to-
    Settings-Hardware-tab helper if one exists, otherwise write a minimal one matching this repo's
    existing e2e/receipts spec conventions.
  </action>
  <verify>
    <automated>npx playwright test e2e/receipts/broker-submission.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The spec passes and contains at least one assertion per caller (PaymentForm, CajaDashboard,
      HardwareSettingsTab) for both the broker-unreachable and job-rejected failure classes.
    - The spec contains an explicit assertion that the same fixture job_id string appears in both the
      submission mock's response and the Print Jobs table row rendered from it.
    - No `page.route()` call appears anywhere in the new spec file.
  </acceptance_criteria>
  <done>An automated, runnable proof exists that every migrated caller correctly maps broker outcomes to
  the phase's locked toast copy, and that a job's correlation ID survives from submission through to the
  audit table.</done>
</task>

<task type="auto">
  <name>End-to-end unknown-status confirm flow + Print Jobs audit tab</name>
  <files>e2e/receipts/unknown-status-confirm.spec.ts, e2e/audit/print-jobs.spec.ts</files>
  <read_first>
    .planning/phases/19-store-local-durable-printing-service/19-UI-SPEC.md (Interaction Contract section
    — the exact "Did this print?" dialog copy/behavior this spec drives through the real UI, not a mocked
    component in isolation)
    e2e/rbac/*.spec.ts (any existing spec asserting a cashier session redirects away from /audit — reuse
    that exact loginAs/navigate/expect-redirect pattern for this plan's Print Jobs tab regression check
    rather than writing a new RBAC-testing approach)
  </read_first>
  <action>
    Create e2e/receipts/unknown-status-confirm.spec.ts: drive a reprint from /payments' PaymentPane with
    invoke('print_receipt') mocked to resolve a job_id, then invoke('get_print_job') mocked to resolve
    status='unknown'; assert the amber "Needs confirmation" badge renders next to ReprintButton, clicking
    it opens the "Did this print?" dialog with the exact locked description copy, clicking "Yes, it
    printed" closes the dialog with invoke('print_receipt') NOT called a second time, and — in a second
    test case — clicking "No, print again" DOES call invoke('print_receipt') a second time (asserting the
    mock's call count) and the dialog closes afterward.

    Create e2e/audit/print-jobs.spec.ts: loginAs(page, 'manager'), navigate to /audit, assert the "Audit
    Log" tab is active by default and unchanged, click the "Print Jobs" tab, with invoke('get_print_jobs')
    mocked to return 2-3 fixture rows spanning different statuses, assert the table renders those rows
    with the correct PrintJobStatusBadge color/label per row, apply a status filter and assert
    invoke('get_print_jobs') is called with that filter, click a row and assert the detail Sheet opens
    showing its event timeline; then loginAs(page, 'cashier'), navigate to /audit directly, and assert the
    existing redirect-to-/home behavior still fires (regression check that adding the Print Jobs tab did
    not weaken the existing view_audit_log gate).
  </action>
  <verify>
    <automated>npx playwright test e2e/receipts/unknown-status-confirm.spec.ts e2e/audit/print-jobs.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both specs pass.
    - `unknown-status-confirm.spec.ts` asserts `invoke`'s mock call count for `print_receipt` explicitly
      (not just that a toast/UI element appeared) to prove "No, print again" triggers exactly one fresh
      reprint and "Yes, it printed" triggers zero.
    - `print-jobs.spec.ts` includes the cashier-redirect regression assertion.
  </acceptance_criteria>
  <done>The two previously component-only-tested flows (unknown-status confirm, Print Jobs tab) now have
  real end-to-end Playwright coverage driven through the actual app UI.</done>
</task>

<task type="auto">
  <name>Broker fault-matrix gap audit + cross-machine LAN reachability script</name>
  <files>scripts/verify-lan-broker-reachability.ps1, broker/src/http.rs, broker/src/delivery.rs, broker/src/ledger.rs</files>
  <read_first>
    .planning/phases/19-store-local-durable-printing-service/19-VALIDATION.md (Wave 0 Requirements and the
    ten-case fault matrix this task audits against)
    broker/src/ (all #[cfg(test)] mod tests blocks written across Plans 19-01, 19-02, 19-05, 19-06 — this
    task greps/reads them to confirm coverage rather than assuming it)
  </read_first>
  <action>
    Run `grep -rn "fn test_\|#\[test\]" broker/src/` (or equivalent) and cross-reference the resulting test
    names against this ten-case list: broker unavailable (client-side connect-timeout, covered by Plan
    19-01's Test 8/e2e), auth failure (Plan 19-01 Test 2), invalid payload (Plan 19-01 Test 3),
    persistence failure (Plan 19-01 Test 6), stopped spooler (Plan 19-01's spike-derived worker_tick
    retry coverage — confirm a test exercises the "RPC server is unavailable"-shaped transient-failure
    path specifically, not just a generic printer-not-found case), offline/misconfigured printer (Plan
    19-01 Test 7 / Plan 19-05's Terminal-classification Test 2), retry exhaustion (Plan 19-05 Test 3),
    duplicate idempotency key (Plan 19-01 Test 3-equivalent in the behavior block — the idempotency dedup
    test), ambiguous handoff (Plan 19-01's "unknown" reconciliation test), restart recovery (Plan 19-01
    Test 5). For any case with no test whose name/assertion clearly covers it, add exactly one missing
    #[test] to the appropriate existing module (do not create a new module for a single missing case) —
    do not silently mark it "assumed covered" if you cannot point at a specific passing test function.

    Create scripts/verify-lan-broker-reachability.ps1: a PowerShell script intended to run FROM A SECOND
    MACHINE on the same LAN/VPN as the store's POS host, taking the POS host's IP as a parameter, that
    POSTs a minimal test job to http://<pos-host-ip>:8973/health (no auth needed for /health per the
    broker's existing route) and asserts a 200 {"ok":true} response within a short timeout, then reports
    a clear PASS ("firewall rule correctly admits LAN traffic") or FAIL ("either the firewall rule is
    missing/misconfigured, or the broker is not listening on a LAN-reachable interface — re-check Plan
    19-02's netsh rule and the broker's 0.0.0.0 bind"). Document at the top of the script, in a comment,
    that this specific check could not be run during this phase's own planning/execution because no
    second real machine or bridged VM was available in this environment — running it once against real
    store hardware at deployment time is the closing step for PRN-01's LAN/VPN claim.
  </action>
  <verify>
    <automated>cd broker && cargo test</automated>
  </verify>
  <acceptance_criteria>
    - Every one of the ten fault cases in this task's list is traceable to a specific named passing test
      function (cited by file:line in the SUMMARY, not asserted generically).
    - `scripts/verify-lan-broker-reachability.ps1` exists, takes a target host parameter, and its
      top-of-file comment states plainly that it has not yet been run against real second-machine
      hardware.
  </acceptance_criteria>
  <done>The full ten-case fault matrix has named, passing automated coverage with no unverified gaps, and
  the one architectural claim this phase's spike could not itself exercise (cross-machine LAN binding) has
  a ready-to-run script plus an honest, explicit statement of what remains unverified until real
  deployment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Second LAN/VPN machine -> broker's bound network interface | the one boundary this phase's spike explicitly could not exercise (loopback-only testing) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-18 | Denial of Service | untested cross-machine firewall/bind behavior | high | accept | Accepted for this phase's automated-planning scope with an explicit flagged assumption and a ready-to-run script (scripts/verify-lan-broker-reachability.ps1) — real-hardware confirmation is a deployment-time step, not something this sandboxed environment can close; rationale: no second real machine/VM is reachable from this execution environment, and fabricating a "verified" claim would be dishonest per this repo's own no-silent-drop policy |
</threat_model>

<verification>
- `npx playwright test e2e/receipts/broker-submission.spec.ts e2e/receipts/unknown-status-confirm.spec.ts e2e/audit/print-jobs.spec.ts` — all three new specs pass.
- `cd broker && cargo test` — full broker fault-matrix suite passes with the gap-audit's additions.
- `npm run test:e2e` — full existing E2E suite still green (no regressions from this phase's changes).
</verification>

<success_criteria>
Every PRN-01 through PRN-07 requirement has at least one end-to-end automated assertion; the full
ten-case fault matrix is provably covered by name, not by assumption; and the one genuinely
unautomatable-from-here claim (cross-machine LAN reachability) is a documented script and an honest
flagged assumption rather than a silently-skipped or falsely-claimed truth.
</success_criteria>

<output>
Create `.planning/phases/19-store-local-durable-printing-service/19-08-SUMMARY.md` when done
</output>
