---
phase: 19-store-local-durable-printing-service
plan: 07
type: execute
wave: 5
depends_on: ["19-06"]
files_modified:
  - src/widgets/PrintJobsTable/PrintJobsTable.tsx
  - src/widgets/PrintJobsTable/PrintJobFilterBar.tsx
  - src/widgets/PrintJobsTable/PrintJobDetailSheet.tsx
  - src/widgets/PrintJobsTable/index.ts
  - src/pages/audit/index.tsx
  - src/pages/audit/index.test.tsx
  - src/shared/lib/i18n/locales/en-US/wAdmin.json
  - src/shared/lib/i18n/locales/es-MX/wAdmin.json
  - src/shared/lib/i18n/locales/en-US/pages.json
  - src/shared/lib/i18n/locales/es-MX/pages.json
autonomous: true
requirements: [PRN-05]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "/audit gains a 'Print Jobs' tab alongside the unchanged 'Audit Log' tab, both behind the existing view_audit_log-gated route (D-13, D-16 — no new RBAC gate)."
    - "PrintJobsTable's columns are Job ID, Origin, Printer, Status (PrintJobStatusBadge), Attempts, Created at, with row-click opening a detail Sheet and an sr-only accessible trigger per row for keyboard/screen-reader parity, mirroring AuditLogTable exactly."
    - "The detail Sheet renders an ordered event timeline (timestamp, event category, optional error, Windows job ID when assigned) instead of a before/after diff — JsonDiffViewer is never reused here."
    - "Filters (origin, printer, status, date range) use the same staged-filter-then-Apply UX as AuditLogFilterBar, via a new PrintJobFilterBar built to the same prop contract."
    - "Print Jobs tab has an empty state distinct from a filtered-no-matches state, a loading skeleton, and a load-error state (broker unreachable while loading the list), mirroring AuditLogTable's three-branch empty-state pattern exactly."
    - "A Print Jobs tab with many rows uses DataTable's existing pagination/infinite-scroll 'Load more entries' behavior unmodified — no phase-specific pagination work is needed, per UI-SPEC's explicit reuse note."
    - "Long printer names and error text truncate in the table cell (existing truncate pattern) but wrap normally (no truncation) in the detail Sheet's timeline rows, since the timeline is a vertical list, not a fixed-width table cell."
    - "A job whose payload has already been purged by Plan 19-05's retention pass does not break the detail Sheet — no payload preview is rendered by this phase's scope, so there is nothing to degrade, but the Sheet must not assume a payload field exists on the API response."
  artifacts:
    - src/widgets/PrintJobsTable/PrintJobsTable.tsx
    - src/pages/audit/index.tsx
  key_links:
    - "audit/index.tsx Tabs(defaultValue='auditLog') -> TabsContent('printJobs') -> PrintJobsTable"
    - "PrintJobsTable -> usePrintJobs(appliedFilters) (entities/print-job, Plan 19-06) -> DataTable rows"
    - "PrintJobsTable row click -> PrintJobDetailSheet(jobId) -> usePrintJob(jobId) -> event timeline"
  prohibitions:
    - "MUST NOT reuse JsonDiffViewer for the Print Jobs detail Sheet — the broker's events table is naturally a timeline (ordered list), not a two-state before/after diff, and forcing it through JsonDiffViewer would misrepresent the data shape (PRN-05/correctness)."
    - "MUST NOT gate the Print Jobs tab behind a new or different RBAC action than the existing route-level view_audit_log check — both tabs share one gate per D-16 (PRN-05/access-control)."
---

<objective>
Build the "Print Jobs" tab on the existing `/audit` page — a `PrintJobsTable` widget structurally
identical to `AuditLogTable` (same `DataTable` + staged-filter-then-Apply + click-row-opens-Sheet
composition) but reading from `entities/print-job` (Plan 19-06) and showing an event timeline instead of
a JSON diff.

Purpose: D-13 requires this to extend the existing `/audit` page's UI patterns, not become a new
standalone page; PRN-05 requires the audit surface to be queryable by time/origin/printer/job ID with
retention controls already applied server-side (Plan 19-05).
Output: `PrintJobsTable`, `PrintJobFilterBar`, `PrintJobDetailSheet` under `src/widgets/PrintJobsTable/`;
`audit/index.tsx` wrapped in a `Tabs` component with "Audit Log" and "Print Jobs".
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-store-local-durable-printing-service/19-UI-SPEC.md
@.planning/phases/19-store-local-durable-printing-service/19-PATTERNS.md
</context>

## Artifacts This Phase Produces

New symbols this plan creates:

- `src/widgets/PrintJobsTable/PrintJobFilterBar.tsx` — new component
- `src/widgets/PrintJobsTable/PrintJobDetailSheet.tsx` — new component
- `src/widgets/PrintJobsTable/PrintJobsTable.tsx` — new component
- `src/widgets/PrintJobsTable/index.ts` — new barrel export
- `src/pages/audit/index.test.tsx` — new test file (none existed before this plan)
- `src/shared/lib/i18n/locales/{en-US,es-MX}/wAdmin.json` — new `printJobsTable.*` keys
- `src/shared/lib/i18n/locales/{en-US,es-MX}/pages.json` — new `audit.tabs.{auditLog,printJobs}` keys

<tasks>

<task type="auto" tdd="true">
  <name>PrintJobFilterBar + PrintJobDetailSheet (event timeline)</name>
  <files>src/widgets/PrintJobsTable/PrintJobFilterBar.tsx, src/widgets/PrintJobsTable/PrintJobDetailSheet.tsx</files>
  <read_first>
    src/widgets/AuditLogTable/AuditLogFilterBar.tsx (full file — exact staged/onStagedChange/onApply prop
    contract and Select/date-input composition to mirror)
    src/widgets/AuditLogTable/AuditLogDetailSheet.tsx (full file — exact Sheet/SheetContent/SheetHeader
    wiring to copy; this task's Sheet body replaces JsonDiffViewer with an ordered timeline list)
    src/entities/print-job (Plan 19-06 — PrintJobFilters, PrintJobDetail, PrintJobEvent types; usePrintJob)
  </read_first>
  <behavior>
    - Test 1 (Vitest, new PrintJobFilterBar.test.tsx): selecting an origin/printer/status option and a
      date range, then clicking "Apply filters", calls onApply with exactly the staged filter object
      (mirrors AuditLogFilterBar.test.tsx's existing assertion shape).
    - Test 2 (Vitest, new PrintJobDetailSheet.test.tsx): given a PrintJobDetail with 3 events, renders 3
      timeline rows in ascending timestamp order, each showing category and (when present) the error
      text; renders without throwing when winSpoolJobId is null and when a job has zero events.
  </behavior>
  <action>
    Create src/widgets/PrintJobsTable/PrintJobFilterBar.tsx mirroring AuditLogFilterBar.tsx's exact
    structure: props { staged: PrintJobFilters, onStagedChange: (next: PrintJobFilters) => void, onApply:
    (filters: PrintJobFilters) => void }, with Select controls for origin (options: receipt, reprint,
    caja_summary, test_print, cash_drawer — the five known origin values this phase's callers use),
    printerName (free-text Input, since printer names are store-configured, not a fixed enum), status
    (the six PrintJobStatusSchema values), and the same toDateInputValue/date-range FormField pair
    AuditLogFilterBar already uses, ending in the same "Apply filters" POSButton. Reuse the omit() helper
    pattern verbatim (copy its shape, do not import it cross-module — AuditLogFilterBar.tsx doesn't export
    it today).

    Create src/widgets/PrintJobsTable/PrintJobDetailSheet.tsx mirroring AuditLogDetailSheet.tsx's Sheet/
    SheetContent/SheetHeader/SheetTitle/SheetDescription wiring exactly, but replacing the
    `<JsonDiffViewer before={row.before} after={row.after} .../>` body with an ordered list (`<ol>` or a
    `space-y-2` div) of the job's `events` array (sorted ascending by `ts`, matching the broker's own
    `ORDER BY id ASC` insertion order — no client-side re-sort needed if the API already returns them in
    order), each row rendering `{event.category}` (bold/medium weight) + `{event.ts.toLocaleString()}` +,
    when `event.detail` is non-null, the detail text in `text-sm text-muted-foreground` (matches existing
    Sheet body typography, no truncation — a vertical list, not a table cell, per this plan's must_haves).
    Show the job's `winSpoolJobId` in the Sheet header/description when non-null (mirrors
    AuditLogDetailSheet's description-line composition style). Do not reference a `payload` field
    anywhere in this component — the API response this phase ships never includes it at the list or
    detail level (Plan 19-06 didn't add it), so there is nothing to guard against being purged; do not add
    speculative payload-preview UI this phase's UI-SPEC explicitly does not require.
  </action>
  <verify>
    <automated>npx vitest run src/widgets/PrintJobsTable/PrintJobFilterBar.test.tsx src/widgets/PrintJobsTable/PrintJobDetailSheet.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests 1-2 pass.
    - `PrintJobDetailSheet.tsx` contains no import of `JsonDiffViewer`.
    - `PrintJobFilterBar.tsx`'s prop names (`staged`, `onStagedChange`, `onApply`) match
      `AuditLogFilterBar`'s exactly (same contract, so `PrintJobsTable` can compose it identically).
  </acceptance_criteria>
  <done>The filter bar and detail Sheet exist, structurally mirroring AuditLogTable's sibling components
  but rendering a timeline instead of a diff.</done>
</task>

<task type="auto" tdd="true">
  <name>PrintJobsTable widget (DataTable composition + three-branch empty state)</name>
  <files>src/widgets/PrintJobsTable/PrintJobsTable.tsx, src/widgets/PrintJobsTable/index.ts, src/shared/lib/i18n/locales/en-US/wAdmin.json, src/shared/lib/i18n/locales/es-MX/wAdmin.json</files>
  <read_first>
    src/widgets/AuditLogTable/AuditLogTable.tsx (full file — the exact DataTable/columns/openSheet/
    emptyState composition this task mirrors, including the sr-only "View diff" accessible trigger
    pattern this task adapts to "View print job {jobId} from {createdAt}")
  </read_first>
  <behavior>
    - Test 3 (Vitest, new PrintJobsTable.test.tsx): with usePrintJobs mocked to `status: 'success'` and
      zero rows and no filters applied, renders the "No print jobs yet" EmptyState; with a filter applied
      and zero rows, renders the "No print jobs match these filters" EmptyState instead.
    - Test 4 (Vitest): with usePrintJobs mocked to `status: 'error'`, renders the load-error EmptyState
      with the "Couldn't load print jobs" / broker-running hint copy.
    - Test 5 (Vitest): with usePrintJobs mocked to `status: 'pending'`, renders TableRowSkeleton rows, not
      the empty state.
    - Test 6 (Vitest): clicking a row (or its sr-only trigger) opens PrintJobDetailSheet with that row's
      data; the Job ID cell truncates a long ID visually but the sr-only trigger's aria-label contains the
      full job ID and createdAt.
  </behavior>
  <action>
    Create src/widgets/PrintJobsTable/PrintJobsTable.tsx mirroring AuditLogTable.tsx's structure exactly:
    `useState` for staged/appliedFilters/selectedRow/sheetOpen, `usePrintJobs(appliedFilters)` (Plan
    19-06) in place of `useAuditLogs`, `useMemo`-flattened `rows` from `data.pages`, an `openSheet(row)`
    function, and a `ColumnDef<PrintJob>[]` with columns: `jobId` (EntityIdCell-style truncated cell +
    sr-only Button trigger with `aria-label={t('wAdmin:printJobsTable.viewJobAriaLabel', { jobId,
    createdAt: formatDate(row.createdAt) })}`, mirroring AuditLogTable's `viewDiffAriaLabel` pattern),
    `origin` (plain text, matches AuditLogTable's `entityType` column treatment), `printerName` (plain
    text, `truncate` class for long names per this plan's must_haves), `status` (renders
    `<PrintJobStatusBadge status={row.original.status} />`, Plan 19-06), `attempts` (plain number),
    `createdAt` (`.toLocaleString()`, matches `formatAuditDate` verbatim). Reuse `DataTable`, `EmptyState`,
    `EntityIdCell`, `POSButton`, `Button`, `TableRowSkeleton` from `@shared/ui` exactly as AuditLogTable
    does — no new shared UI component. Implement the three-branch empty state exactly like AuditLogTable's
    `status === 'error'` / `filtersApplied && rows.length === 0` / `rows.length === 0` split, using the
    `wAdmin:printJobsTable.{emptyTitle,emptyBody,noMatchesTitle,noMatchesBody,loadErrorTitle,
    loadErrorDescription}` copy keys from 19-UI-SPEC.md's Copywriting Contract table. Add
    `PrintJobsTable` to a new `src/widgets/PrintJobsTable/index.ts` barrel export.

    Add the `wAdmin:printJobsTable.*` keys (columnJobId, columnOrigin, columnPrinter, columnStatus,
    columnAttempts, columnCreatedAt, viewJobAriaLabel, loadMoreEntries reused verbatim from
    auditLogTable's own key if identical copy applies, emptyTitle/emptyBody, noMatchesTitle/noMatchesBody,
    loadErrorTitle/loadErrorDescription) to `wAdmin.json` in both locales, with the exact UI-SPEC copy
    values ("No print jobs yet" / "Receipts, reprints, and reports sent to the printer will appear here."
    / "No print jobs match these filters" / "Try a different date range, origin, or printer." / "Couldn't
    load print jobs" / "Check that the print broker service is running, then try again.").
  </action>
  <verify>
    <automated>npx vitest run src/widgets/PrintJobsTable/PrintJobsTable.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests 3-6 pass.
    - `wAdmin.json` (both locales) contains every `printJobsTable.*` key referenced in the component.
    - `PrintJobsTable.tsx` imports `usePrintJobs` from `@entities/print-job`, never `useAuditLogs`.
  </acceptance_criteria>
  <done>The Print Jobs table renders, filters, paginates, and opens a detail Sheet exactly like
  AuditLogTable, backed entirely by the broker-sourced entity from Plan 19-06.</done>
</task>

<task type="auto" tdd="true">
  <name>Wrap /audit in Tabs: "Audit Log" (unchanged) + "Print Jobs" (new)</name>
  <files>src/pages/audit/index.tsx, src/pages/audit/index.test.tsx, src/shared/lib/i18n/locales/en-US/pages.json, src/shared/lib/i18n/locales/es-MX/pages.json</files>
  <read_first>
    src/pages/audit/index.tsx (current full file, 15 lines — renders AuditLogTable directly, no Tabs
    wrapper today)
    src/pages/reports/index.tsx (lines 14, 51-60 — the exact Tabs/TabsList/TabsTrigger/TabsContent
    composition pattern this task introduces to audit/index.tsx)
    src/pages/reports/ReportsPage.test.tsx (full file — the exact vi.mock-child-widgets +
    renderWithProviders(<MemoryRouter>...) + default-tab-active/tab-switch assertion pattern this task's
    new test file mirrors; no test file exists yet for src/pages/audit — grep confirmed before this plan)
  </read_first>
  <behavior>
    - Test 7 (Vitest, new src/pages/audit/index.test.tsx): with `@widgets/AuditLogTable` and
      `@widgets/PrintJobsTable` mocked to trivial data-testid stub components, rendering `AuditPage` shows
      the `AuditLogTable` stub by default and the "Audit Log" tab trigger has `data-state="active"`
      (mirrors ReportsPage.test.tsx's default-tab assertion).
    - Test 8: clicking the "Print Jobs" tab trigger shows the `PrintJobsTable` stub instead, and the
      "Audit Log" stub is no longer rendered.
  </behavior>
  <action>
    Rewrite src/pages/audit/index.tsx to wrap its existing `<AuditLogTable />` render in
    `<Tabs defaultValue="auditLog">` with two `TabsTrigger`s ("Audit Log" / "Print Jobs", via
    `pages:audit.tabs.auditLog` / `pages:audit.tabs.printJobs`) and two `TabsContent`s (`auditLog` ->
    the existing unchanged `<AuditLogTable />`, `printJobs` -> the new `<PrintJobsTable />` from this
    plan's Task 2), inside the existing `<PageContainer title={t('audit.title')} backTo="/home">` — do
    not touch `PageContainer`'s own title/props, only what it wraps. No RBAC/route change: the existing
    `AuditRoute` wrapper in `src/app/router.tsx` already gates the whole page behind `view_audit_log`
    (D-16 — confirmed by `src/app/audit-route.tsx`'s existing `can('view_audit_log')` check), so both
    tabs inherit the same gate automatically.
    Add `pages:audit.tabs.auditLog` = "Audit Log" / "Registro de Auditoría" and
    `pages:audit.tabs.printJobs` = "Print Jobs" / "Trabajos de Impresión" to `pages.json` in both locales.

    Create src/pages/audit/index.test.tsx mirroring src/pages/reports/ReportsPage.test.tsx's structure
    exactly: `vi.mock('@widgets/AuditLogTable', ...)` and `vi.mock('@widgets/PrintJobsTable', ...)` each
    returning a trivial `data-testid` stub component (avoids pulling in either widget's real
    data-fetching dependency chain), lazy-import `AuditPage` from `./index` after the mocks are
    registered, render it inside `<MemoryRouter>` via `renderWithProviders`, and assert Tests 7-8 above
    using `getByTestId` and `getByRole('tab', {name: ...})` with the same
    `toHaveAttribute('data-state', 'active')` check ReportsPage.test.tsx already uses.
  </action>
  <verify>
    <automated>npx vitest run src/pages/audit/index.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests 7-8 pass.
    - `src/pages/audit/index.tsx` renders both `AuditLogTable` and `PrintJobsTable` inside a `Tabs`
      component with `defaultValue="auditLog"`.
    - `pages.json` (both locales) contains both new `audit.tabs.*` keys.
    - No change to `src/app/audit-route.tsx` or `src/app/router.tsx` — the existing gate covers both tabs.
  </acceptance_criteria>
  <done>/audit shows both "Audit Log" (unchanged behavior) and "Print Jobs" (new) as tabs on one page,
  gated by the same existing RBAC route, proven by a real automated test file (previously none existed
  for this page).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| /audit route -> view_audit_log RBAC gate | unchanged existing boundary, now also covering the Print Jobs tab |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-17 | Elevation of Privilege | Print Jobs tab bypassing RBAC via a route-level gap | low | accept | The tab is rendered inside the same route component AuditRoute already gates — there is no separate route/URL for it to bypass through, so the existing test coverage for `AuditRoute` already covers this tab by construction |
</threat_model>

<verification>
- `npx vitest run src/widgets/PrintJobsTable src/pages/audit` — full widget and page-level coverage.
- `npm run typecheck` — no loose types across the new widget files.
- `npm run lint` — i18next/no-literal-string gate passes on every new string in these widgets/pages.
</verification>

<success_criteria>
A manager/admin can open /audit, switch to the Print Jobs tab, filter by origin/printer/status/date
range, and open any job's detail Sheet to see its full event timeline — reusing every existing
AuditLogTable UI pattern with a genuinely different, broker-backed data source underneath.
</success_criteria>

<output>
Create `.planning/phases/19-store-local-durable-printing-service/19-07-SUMMARY.md` when done
</output>
