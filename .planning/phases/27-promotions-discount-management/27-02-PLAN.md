---
phase: 27-promotions-discount-management
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - src/features/manage-promotions/model/useMutationSavePromotion.ts
  - src/features/manage-promotions/ui/PromotionFormDialog.tsx
  - src/features/manage-promotions/index.ts
  - src/pages/promotions/index.tsx
  - src/app/promotions-route.tsx
  - src/app/router.tsx
  - src/widgets/HomeDashboard/ui/HomeDashboard.tsx
  - src/widgets/SettingsTabsPanel/tabs/NearExpirySettingsTab.tsx
  - src/shared/ui/StatusBadge.tsx
  - src/shared/lib/i18n/locales/es-MX/wAdmin.json
  - src/shared/lib/i18n/locales/en-US/wAdmin.json
  - src/shared/lib/i18n/locales/es-MX/pages.json
  - src/shared/lib/i18n/locales/en-US/pages.json
autonomous: true
requirements: [PROMO-01, PROMO-02]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The /promotions list is empty-stated with EmptyState + a New Promotion CTA when there are zero promotions."
    - "The /promotions list shows DataTable's built-in loading skeleton while promotions are loading."
    - "The /promotions list's populated row shape is name, scope (target name + type badge), discount type/value, active date range, StatusBadge, row actions."
    - "The /promotions list uses DataTable's built-in searchable prop rather than pagination for overflow."
    - "Zero promotions renders EmptyState; one or many render uniform table rows with no singular/plural copy branching."
    - statement: "A promotions list fetch failure shows the toast.error copy from UI-SPEC.md's Copywriting Contract and DataTable keeps its last-known rows rendered rather than clearing to blank."
      verification: backstop
    - "Deleting a promotion's scope category/product cascades (ON DELETE CASCADE, set in Plan 01) — the list can never render a row whose scope target no longer exists, so no deleted-target fallback label is needed."
    - "The Create/Edit Promotion dialog's Save button is disabled while the mutation is pending, mirroring NearExpirySettingsTab's save() pattern."
    - "A save rejection (RLS/validation) shows toast.error(result.error.message) plus inline field validation for discount value out of the 0-100/positive range, the same two-layer pattern as every other admin form in this codebase."
    - "Switching the promotion's scope type (Product <-> Category) mid-edit in the dialog clears the previously-selected target id — a stale, type-mismatched target is never submitted (planner assumption, not explicitly locked by CONTEXT.md)."
    - "The expiry-discount rate settings field save/error path is the same save()/toast.error pattern already used by NearExpirySettingsTab's existing thresholdDays field, validated 0-100 client-side and re-validated server-side by the Zod schema on read."
  artifacts:
    - src/pages/promotions/index.tsx
    - src/app/promotions-route.tsx
    - src/features/manage-promotions/ui/PromotionFormDialog.tsx
  key_links:
    - "/promotions route -> PromotionsRoute (can('manage_promotions')) -> PromotionsPage -> DataTable(usePromotions())"
    - "HomeDashboard ITEMS tile (/promotions, requiredAction: 'manage_promotions') -> route"
    - "NearExpirySettingsTab -> useMutationUpdateSetting('near_expiry', { thresholdDays, discountPercent })"
---

<objective>
Give the admin a real place to manage promotions: a standalone `/promotions` page (admin-only route
+ Home dashboard tile), a Create/Edit Promotion dialog (product/category/subcategory scope,
percent/fixed discount, active date range), and the expiry-discount rate field on the existing
Near-Expiry settings tab.

Purpose: Plan 01 built the schema/RPC/entity-layer; this plan is the first real user-facing surface
an admin touches, and the data source (`usePromotions()`) that Plan 03 (cart display) and Plan 04
(payment screen) both consume.
Output: `/promotions` page, `manage-promotions` feature, `PromotionsRoute`, Home dashboard tile,
`NearExpirySettingsTab.discountPercent` field.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-promotions-discount-management/27-CONTEXT.md
@.planning/phases/27-promotions-discount-management/27-UI-SPEC.md
@.planning/phases/27-promotions-discount-management/27-PATTERNS.md
@src/app/audit-route.tsx
@src/app/router.tsx
@src/widgets/HomeDashboard/ui/HomeDashboard.tsx
@src/pages/suppliers/index.tsx
@src/widgets/SettingsTabsPanel/tabs/NearExpirySettingsTab.tsx
@src/shared/ui/StatusBadge.tsx
@src/shared/ui/CategoryTreePicker/CategoryTreePicker.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: `/promotions` route, page shell, and Home dashboard tile</name>
  <files>src/app/promotions-route.tsx, src/app/router.tsx, src/widgets/HomeDashboard/ui/HomeDashboard.tsx, src/pages/promotions/index.tsx</files>
  <read_first>src/app/audit-route.tsx (full file — copy this shape verbatim), src/app/router.tsx (lines 1-30, 120-150, the lazy-import + Route registration pattern), src/widgets/HomeDashboard/ui/HomeDashboard.tsx (lines 1-96, ITEMS array + DashboardItem type), src/pages/suppliers/index.tsx (PageContainer usage pattern)</read_first>
  <action>
Create src/app/promotions-route.tsx exporting `PromotionsRoute` — copy src/app/audit-route.tsx's
shape exactly, substituting `can('manage_promotions')` for `can('view_audit_log')` and toast copy
"This page is restricted to admins." (admin-only per PROMO-01, no manager fallback). Register in
router.tsx: `const PromotionsPage = lazy(() => import('../pages/promotions'));`, import
`PromotionsRoute` from './promotions-route', and add a `<Route path="/promotions" element=
{<ProtectedRoute><PromotionsRoute><PromotionsPage /></PromotionsRoute></ProtectedRoute>} />` entry
following the existing `/purchase-orders` block's exact nesting. Add a new `ITEMS` entry to
HomeDashboard.tsx: `{ path: '/promotions', labelKey: 'homeDashboard.tiles.promotions', icon: Percent
(from lucide-react — add to the existing import line), requiredAction: 'manage_promotions',
managerLabelKey: 'homeDashboard.managerLabels.admin' }`, placed after the `/settings` entry.  Create
src/pages/promotions/index.tsx as a thin route container (mirror src/pages/suppliers/index.tsx):
`PageContainer` with title from `t('promotions.title')` (pages namespace), `backTo="/home"`, an
`actions` slot rendering a POSButton "New Promotion" that opens the Create dialog (Task 2's
PromotionFormDialog), and the promotions DataTable (Task 2) as the body. Add i18n keys
`pages.promotions.title` and `homeDashboard.tiles.promotions`/`homeDashboard.managerLabels.admin`
(already exists) to both es-MX and en-US `pages.json` files.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint -- --max-warnings=0 src/app/promotions-route.tsx src/app/router.tsx src/widgets/HomeDashboard/ui/HomeDashboard.tsx src/pages/promotions/index.tsx</automated>
    <fails_when>tsc exits non-zero, or eslint reports any warning/error on the listed files</fails_when>
  </verify>
  <done>`/promotions` renders behind PromotionsRoute for an admin session and redirects a non-admin session to /home with a toast; the Home dashboard shows a "Promotions" tile only for admin-permitted sessions (a manager/cashier sees the gated-tap toast, matching the existing /settings tile's behavior).</done>
</task>

<task type="auto">
  <name>Task 2: Create/Edit Promotion dialog + promotions DataTable</name>
  <files>src/features/manage-promotions/model/useMutationSavePromotion.ts, src/features/manage-promotions/ui/PromotionFormDialog.tsx, src/features/manage-promotions/index.ts, src/pages/promotions/index.tsx, src/shared/ui/StatusBadge.tsx, src/shared/lib/i18n/locales/es-MX/wAdmin.json, src/shared/lib/i18n/locales/en-US/wAdmin.json</files>
  <read_first>src/entities/promotion/model/queries.ts (from Plan 01), src/shared/ui/CategoryTreePicker/CategoryTreePicker.tsx (full file — props: items/value/onChange/disabled/label/emptyText), src/shared/ui/StatusBadge.tsx (full file — statusConfig extension pattern), src/entities/category/model/queries.ts (useCategories), src/entities/product/index.ts (useProducts, for the product-scope target picker)</read_first>
  <action>
Extend StatusBadgeProps.status union in src/shared/ui/StatusBadge.tsx with 'promo_active' |
'promo_scheduled' | 'promo_expired' | 'promo_inactive', adding four statusConfig entries (mirror the
existing po_draft/po_received extension pattern): promo_active uses the accent/jade className
(`bg-pos-accent text-white`), promo_expired uses the destructive variant, promo_scheduled and
promo_inactive use 'secondary'/'outline'. Derive a promotion row's status client-side (not stored):
!active -> promo_inactive; active && startsAt > now -> promo_scheduled; active && now > endsAt ->
promo_expired; else promo_active. Create src/features/manage-promotions/ui/PromotionFormDialog.tsx —
a Dialog (import DialogContent/DialogHeader/DialogTitle/DialogFooter directly from
@shared/ui/dialog, not barrel) with: a scope-type button-group (Product/Category — 2 buttons, sm gap
per UI-SPEC spacing scale) that swaps which target picker renders below it (a product Select sourced
from useProducts() for 'product' scope, or CategoryTreePicker sourced from useCategories() mapped to
CategoryPickerItem for 'category' scope — a subcategory is just a category row with parentId set, so
CategoryTreePicker alone covers both PROMO-01 scope levels); switching scope type clears the
previously-selected target value. A discount-type button-group (Percent/Fixed) toggling between a
percent Input (type="number", min=0, max=100, mirrors NearExpirySettingsTab's thresholdDays input)
and a MoneyInput for the fixed value. A DateRangePicker (fromStr/endStr controlled) for the active
range. Name Input + Label/FormField scaffolding. Wire it to
src/features/manage-promotions/model/useMutationSavePromotion.ts, a thin wrapper choosing
useMutationCreatePromotion vs useMutationUpdatePromotion from entities/promotion based on whether an
id is present (mirrors src/features/manage-categories/'s create/update split). Save button is a
POSButton touchSize="large" disabled={mutation.isPending}; on rejection, toast.error(
result.error.message); on success, close the dialog and toast.success. Wire Create/Edit/Delete
entry points from the promotions DataTable in src/pages/promotions/index.tsx: DataTable columns
(name, scope target name + Badge for scope type, discount type/value formatted via
MoneyDisplay/percent text, active date range, StatusBadge using the derived status above, row
actions: an inline Switch bound to useMutationUpdatePromotion({active: !current}) for quick
toggle-off, an Edit icon-button opening PromotionFormDialog pre-filled, and a Delete icon-button
opening ConfirmDialog with the exact copy from UI-SPEC.md's Copywriting Contract ("Delete this
promotion? Sales that already used it keep their recorded discount...") before calling
useMutationDeletePromotion (add this mutation to entities/promotion/model/queries.ts if Plan 01
didn't already include a delete mutation — a plain `supabase.from('promotions').delete().eq('id',
id)`). DataTable's `isLoading`/`emptyState` props render the built-in skeleton and an EmptyState with
copy "No promotions yet" / "Create a promotion to automatically discount a product, category, or
subcategory." + a "New Promotion" action button (UI-SPEC Copywriting Contract). A fetch-failure
(usePromotions()'s resultError) triggers toast.error("Couldn't load promotions. Check your
connection and try again.") while DataTable continues rendering whatever rows it last successfully
had (do not replace `data` with an empty array on error — keep the last-known query cache value, the
default TanStack Query behavior on a background refetch failure). Export
PromotionFormDialog/useMutationSavePromotion from src/features/manage-promotions/index.ts. Add every
new copy key used above to both es-MX (byte-identical to the literal you wrote inline) and en-US
(genuine translation) wAdmin.json files, per this repo's i18next/no-literal-string lint gate.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/features/manage-promotions 2>&1 | tail -20</automated>
    <fails_when>tsc exits non-zero, or vitest reports 0 tests collected with no matching test file present (create at minimum a smoke render test for PromotionFormDialog if none exists) or any collected test fails</fails_when>
  </verify>
  <done>An admin can create a product-scoped and a category-scoped promotion via the dialog, see it appear in the DataTable with the correct derived StatusBadge, toggle it inactive via the row Switch, and delete it via the ConfirmDialog — all persisting through entities/promotion's mutations.</done>
</task>

<task type="auto">
  <name>Task 3: Expiry-discount rate field on Near-Expiry settings</name>
  <files>src/widgets/SettingsTabsPanel/tabs/NearExpirySettingsTab.tsx, src/shared/lib/i18n/locales/es-MX/wAdmin.json, src/shared/lib/i18n/locales/en-US/wAdmin.json</files>
  <read_first>src/widgets/SettingsTabsPanel/tabs/NearExpirySettingsTab.tsx (full file, current thresholdDays-only shape)</read_first>
  <action>
Extend NearExpirySettingsTab.tsx with a second field, `discountPercent` (default '15' per D-04),
mirroring the exact existing thresholdDays pattern: its own useState, its own dirty-tracking via the
same `dirty` flag (both fields share one Save button and one dirty flag, consistent with this tab
being a single settings row), an Input type="number" min={0} max={100}, and a hint paragraph
explaining it's the discount applied when a product enters the near-expiry window (D-02/D-03/D-04:
one flat global rate, admin-editable, reusing the same thresholdDays value as the near-expiry alert
— do not introduce a second threshold). The save() handler's `updateSetting.mutateAsync` payload
becomes `{ key: 'near_expiry', value: { thresholdDays: value, discountPercent: discountValue } }`
where discountValue is `Number(discountPercent)` validated 0-100 (reject the save silently, same as
the existing out-of-range thresholdDays guard, if invalid). Add the new label/hint copy keys to both
wAdmin.json locale files.
  </action>
  <verify>
    <automated>npx vitest run src/widgets/SettingsTabsPanel 2>&1 | tail -20</automated>
    <fails_when>no test file exists for NearExpirySettingsTab (create one asserting the save() payload includes discountPercent) or any collected test fails</fails_when>
  </verify>
  <done>Saving the Near-Expiry settings tab persists both thresholdDays and discountPercent onto the same `near_expiry` settings row; reloading the tab shows the previously-saved discountPercent value, defaulting to 15 for a pre-Phase-27 row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client route guard (`PromotionsRoute`) | UX-only convenience — the real authorization boundary is Plan 01's RLS `promotions_manage` policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-05 | Elevation of Privilege | `/promotions` route + Home tile client-side gating | medium | mitigate | `PromotionsRoute`/tile visibility are UX only; the actual write boundary is Plan 01's `promotions_manage` RLS policy checked server-side on every insert/update/delete regardless of which client screen issued it |
| T-27-06 | Information Disclosure | promotions SELECT policy (`USING (true)`) | low | accept | Promotion pricing rules are not sensitive data (same disposition as `products`/`categories`, already readable by any authenticated staff role in this single-store app) |
</threat_model>

<verification>
1. `npm run typecheck` passes for all files in this plan.
2. `npm run lint -- --max-warnings=0` passes for all files in this plan (i18next/no-literal-string enforced on the new UI copy).
3. `npx vitest run src/features/manage-promotions src/widgets/SettingsTabsPanel` is green.
</verification>

<success_criteria>
An admin session can navigate Home -> Promotions, create/edit/toggle/delete a promotion end to end
through the UI, and the Near-Expiry settings tab exposes and persists the new discount-rate field —
all gated correctly for non-admin sessions.
</success_criteria>

<output>
Create `.planning/phases/27-promotions-discount-management/27-02-SUMMARY.md` when done
</output>
