---
phase: 27-promotions-discount-management
plan: 13
type: execute
wave: 3
depends_on: ["27-11"]
files_modified:
  - src/entities/promotion/model/queries.ts
  - src/entities/promotion/index.ts
  - src/shared/ui/MultiSelectCombobox.tsx
  - src/shared/ui/index.ts
  - src/shared/ui/DateRangePicker.tsx
  - src/shared/ui/CategoryTreePicker/CategoryTreePicker.tsx
autonomous: true
gap_closure: true
gap_ids: [G-27-8]
requirements: [PROMO-01]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "usePromotions() (the single hook already consumed by the admin table, the cart-display preview, and the payment screen's Apply-Promotion selector) returns every promotion with productIds/categoryIds fully populated — merging junction-table rows AND any legacy single-target product_id/category_id — so every existing and new consumer sees one consistent unified shape."
    - "A new useMutationSaveWithTargets hook calls save_promotion_with_targets (27-11's atomic RPC) and is the sole write path the new wizard (plan 27-14) will use — it never writes the legacy scope_type/product_id/category_id columns."
    - "A reusable MultiSelectCombobox exists in shared/ui: search-filterable, checkbox-per-item, shows selected-count, built from the already-installed Popover+Command+Checkbox primitives — zero new npm dependencies."
    - "CategoryTreePicker gains an opt-in multiple-selection mode (array value/onChange) without changing its default single-value behavior — its one existing consumer (PromotionFormDialog, unmodified by this plan) is unaffected."
    - "DateRangePicker gains an optional forward-looking preset set via a new prop, defaulting to the existing backward-looking presets when the prop is omitted — Reports' own usage (unmodified, no prop passed) is byte-for-byte unaffected."
  artifacts:
    - src/entities/promotion/model/queries.ts
    - src/shared/ui/MultiSelectCombobox.tsx
  key_links:
    - "usePromotions() -> promotions + promotion_products + promotion_categories (3 parallel queries, merged client-side) -> Promotion.productIds/categoryIds -> every consumer (admin table, evaluateBestPromotion via 27-12, PaymentForm Apply-Promotion, and the wizard's edit-mode pre-population in 27-14)"
    - "useMutationSaveWithTargets -> save_promotion_with_targets RPC -> promotions + promotion_products + promotion_categories (atomic)"
---

<objective>
Wire the entity query/mutation layer to the new backend (plan 27-11) and build the two reusable UI
primitives the wizard (plan 27-14) needs: a generic searchable multi-select and a category-tree
multi-select mode. Also extend `DateRangePicker` with forward-looking presets so the wizard's validity
step isn't stuck with Reports' backward-only "Last 7 Days"/"This Month" (the literal "what is last 7
days, this isn't a report, it's promotions for upcoming days" complaint from the UAT report) — without
breaking Reports' own use of the same shared component.

Purpose: give plan 27-14's wizard a complete, tested set of data hooks and form controls to compose,
so that plan is pure UI wiring with minimal new logic of its own.
Output: `usePromotions()` returns the unified scope shape; `useMutationSaveWithTargets`;
`MultiSelectCombobox`; `CategoryTreePicker` multi-select mode; `DateRangePicker` forward-looking
presets.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-promotions-discount-management/27-11-PLAN.md
@src/entities/promotion/model/queries.ts
@src/shared/ui/CategoryTreePicker/CategoryTreePicker.tsx
@src/shared/ui/DateRangePicker.tsx
@src/shared/ui/popover.tsx
@src/shared/ui/command.tsx
@src/shared/ui/checkbox.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: usePromotions() returns the unified scope shape; new atomic save mutation</name>
  <files>src/entities/promotion/model/queries.ts, src/entities/promotion/index.ts</files>
  <read_first>src/entities/promotion/model/queries.ts (full file — mapPromotionRow lines 28-49, usePromotions lines 64-102, the existing useMutationCreatePromotion/useMutationUpdatePromotion — LEAVE these two unchanged, they remain the still-live PromotionFormDialog's write path until plan 27-14 deletes it), src/entities/promotion/index.ts (export barrel — add the new mutation here)</read_first>
  <behavior>
    - usePromotions()'s queryFn issues 3 parallel queries (`Promise.all`): `promotions.select('*')`
      (unchanged), `promotion_products.select('promotion_id, product_id')`,
      `promotion_categories.select('promotion_id, category_id')`. Groups the latter two by
      `promotion_id` into two `Map<string, string[]>`, and passes each promotion row's matching arrays
      (or `[]` if absent) into `mapPromotionRow`.
    - mapPromotionRow populates `productIds`/`categoryIds` as the UNION of the junction-table arrays
      AND the legacy `product_id`/`category_id` singular values (e.g. a legacy single-product
      promotion's `productIds` becomes `[row.product_id]` even though it has zero promotion_products
      rows) — every downstream consumer reads one unified array, never needing to separately check the
      legacy fields itself.
    - `recurrenceDaysOfWeek`/`recurrenceTimeStart`/`recurrenceTimeEnd` are read straight off the row
      (already plain columns from 27-11's schema migration, no merging needed).
    - A new `useMutationSaveWithTargets()` hook calls `supabase.rpc('save_promotion_with_targets', {
      p_id, p_name, p_product_ids: input.productIds, p_category_ids: input.categoryIds,
      p_discount_type, p_discount_value, p_starts_at, p_ends_at, p_recurrence_days_of_week,
      p_recurrence_time_start, p_recurrence_time_end, p_active, p_created_by })`, unwraps the
      `{ok, id, code, message}` jsonb envelope into a `Result`, and invalidates the same
      `PROMOTION_QUERY_KEY` on success (mirror `invalidatePromotionQueries`, already exported from this
      file).
  </behavior>
  <action>
Extend `usePromotions()`'s `queryFn` and `mapPromotionRow` exactly as described in `<behavior>` above.
Do NOT modify `useMutationCreatePromotion`/`useMutationUpdatePromotion`/`useMutationDeletePromotion` —
they remain unchanged and still fully functional (the still-live `PromotionFormDialog.tsx`'s write
path) until plan 27-14 retires that dialog. Add `useMutationSaveWithTargets` as a new export from this
file and from `src/entities/promotion/index.ts`.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/entities/promotion 2>&1 | tail -40</automated>
    <fails_when>tsc exits non-zero, or a new/extended queries.ts test fails (add a case asserting a legacy single-product promotion's mapped productIds equals [product_id], and a case asserting a junction-scoped promotion's mapped productIds includes every promotion_products row) or 0 tests are collected for this file</fails_when>
  </verify>
  <done>usePromotions() returns a promotion with productIds/categoryIds correctly merged from both representations; useMutationSaveWithTargets successfully creates and updates a multi-target promotion via the new RPC.</done>
</task>

<task type="auto">
  <name>Task 2: MultiSelectCombobox (new shared/ui primitive) + DateRangePicker forward-looking presets</name>
  <files>src/shared/ui/MultiSelectCombobox.tsx, src/shared/ui/index.ts, src/shared/ui/DateRangePicker.tsx</files>
  <read_first>src/shared/ui/popover.tsx, src/shared/ui/command.tsx, src/shared/ui/checkbox.tsx (all three already installed shadcn primitives — compose from these, do not add a new npm dependency), src/shared/ui/DateRangePicker.tsx (full file, 100 lines — PRESETS array lines 20-57, component lines 59-99), src/pages/reports/index.tsx (grep for DateRangePicker usage — confirm it passes no extra props today, so a new optional prop cannot break it)</read_first>
  <action>
Create `src/shared/ui/MultiSelectCombobox.tsx`: props `{ items: { id: string; label: string }[];
value: string[]; onChange: (ids: string[]) => void; placeholder?: string; disabled?: boolean;
emptyText?: string }`. Render a `Popover`/`PopoverTrigger` button showing "N selected" (or
`placeholder` when `value.length === 0`), `PopoverContent` containing a `Command`/`CommandInput`
(search box) /`CommandList`/`CommandGroup`/`CommandItem` per item, each `CommandItem` rendering a
`Checkbox` bound to `value.includes(item.id)`, toggling membership in the `value` array on click (never
closing the popover on selection — multi-select stays open until the trigger/outside click closes it,
standard shadcn multi-select-combobox pattern). Export from `src/shared/ui/index.ts`.

Extend `DateRangePickerProps` with an optional `presets?: { label: string; from: () => string; to: ()
=> string }[]` prop (the existing `Preset` type, exported). When omitted, the component behaves
IDENTICALLY to today (uses the existing hardcoded `PRESETS` array as the default) — this is the
non-breaking guarantee for Reports' unmodified usage. When provided, render the caller's presets
instead. Do not touch the free-form `from`/`to` date inputs — unchanged either way.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/shared/ui/MultiSelectCombobox.test.tsx src/shared/ui/DateRangePicker.test.tsx 2>&1 | tail -40</automated>
    <fails_when>tsc exits non-zero, no test file exists for MultiSelectCombobox (create one — checked items toggle in/out of the value array; search filters visible items), or an existing DateRangePicker test (if any) breaks, or Reports' own DateRangePicker usage changes behavior with zero props passed</fails_when>
  </verify>
  <done>MultiSelectCombobox renders, searches, and toggles selection correctly in isolation; DateRangePicker accepts an optional custom presets array with zero behavior change when omitted.</done>
</task>

<task type="auto">
  <name>Task 3: CategoryTreePicker gains an opt-in multiple-selection mode</name>
  <files>src/shared/ui/CategoryTreePicker/CategoryTreePicker.tsx</files>
  <read_first>src/shared/ui/CategoryTreePicker/CategoryTreePicker.tsx (full file — current props: items/value: string|null/onChange: (id:string|null)=>void/disabled/label/emptyText; this is the ONLY reusable category-tree UI in this codebase, worth extending rather than forking)</read_first>
  <action>
Add an opt-in `multiple` mode via a discriminated prop shape: when `multiple` is not passed (or
`false`), all existing props/behavior are byte-for-byte unchanged (PromotionFormDialog.tsx, the sole
current consumer, keeps compiling and working with zero changes). When `multiple={true}` is passed,
`value` becomes `string[]` and `onChange` becomes `(ids: string[]) => void` — each tree row renders a
`Checkbox` (reuse the same primitive `MultiSelectCombobox` composes from) instead of a radio-style
single-select highlight, toggling membership in the array; selecting a parent category does NOT
auto-select its children (explicit, one-row-at-a-time selection — the wizard's own "Scope" step is
responsible for any "select all in this category" convenience action, not this shared primitive).
Use a TypeScript discriminated union on the `multiple` prop so passing `multiple={true}` with a
non-array `value` (or vice versa) is a compile-time error, not a runtime one.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/shared/ui/CategoryTreePicker 2>&1 | tail -30</automated>
    <fails_when>tsc exits non-zero, the existing single-select test suite (if present) regresses, or no new test exists for the multiple mode (add one: checking two categories produces a 2-element array; unchecking one produces a 1-element array)</fails_when>
  </verify>
  <done>CategoryTreePicker's default (single-select) mode is unchanged for PromotionFormDialog.tsx; a new multiple={true} mode correctly toggles array membership per row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| useMutationSaveWithTargets (client) -> save_promotion_with_targets (server) | Client-side call is UX convenience only; the RPC's own manage_promotions re-check (27-11) is the real authorization boundary, unaffected by anything in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-17 | Information Disclosure | usePromotions()'s new promotion_products/promotion_categories reads | low | accept | Same disposition as the existing `promotions_select_authenticated` policy (T-27-06 from plan 27-02) — scope-target membership is not sensitive data, already readable via the promotions table itself in this single-store app |
</threat_model>

<verification>
1. `npm run typecheck` passes.
2. `npx vitest run src/entities/promotion src/shared/ui/MultiSelectCombobox.test.tsx src/shared/ui/CategoryTreePicker` is green.
3. Reports page's existing DateRangePicker usage is unaffected (visual/behavioral no-op — no props changed there).
</verification>

<success_criteria>
The full data-hook and UI-primitive layer plan 27-14's wizard needs exists and is independently tested,
with zero regression to any currently-shipped consumer of usePromotions/CategoryTreePicker/
DateRangePicker.
</success_criteria>

<output>
Create `.planning/phases/27-promotions-discount-management/27-13-SUMMARY.md` when done.
</output>
