---
phase: 27-promotions-discount-management
plan: 05
type: execute
wave: 3
depends_on: ["27-01", "27-02", "27-03", "27-04"]
files_modified:
  - src/widgets/PaymentModal/ui/PaymentForm.tsx
  - e2e/payments/promotion-snapshot-refund-reopen.spec.ts
autonomous: true
requirements: [PROMO-06]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A full refund reverses the exact discounted payments.amount that was actually charged (already net of the promotion discount), never a re-derived list-price amount — process_refund's existing behavior (reverse payments.amount as-is) needs no change for this to hold."
    - "Reopening and re-viewing a sale whose line price equals cost exactly (0% margin) renders normally — the floor guard is a checkout-time gate only and is never re-evaluated on read or reopen."
    - "order_items.promotion_id/discount_rate/discount_amount are NULL for a line with no applicable discount; reports and UI treat a NULL discount_amount as zero, never as a data/parse error."
    - "Reopened or refunded sale line items render in their original order_items insertion order, unaffected by which lines happen to carry a promotion snapshot."
    - "get_product_sales_report's margin calculation (unit_price - cost_price_snapshot) requires no code change — unit_price already stores the promotion-discounted line price (not list price, per Plan 01's RPC change), so margin is automatically correct against the discounted price once verified."
  artifacts:
    - e2e/payments/promotion-snapshot-refund-reopen.spec.ts
  key_links:
    - "order_items.promotion_id/discount_rate/discount_amount (Plan 01) -> reopened/paid-tab line-item display -> historical discount badge (read-only, sourced from stored columns, never re-evaluated)"
    - "order_items.unit_price (already discounted, Plan 01) -> get_product_sales_report RPC's existing margin formula (unchanged)"
---

<objective>
Prove PROMO-06 end to end: a promotion's discount, once snapshotted onto `order_items` at sale time
(Plan 01), survives a refund and a reopen exactly, and the existing margin report is automatically
correct against the discounted price with zero report-code changes — plus surface the historical
discount on the reopened/paid-tab line-item view so it isn't invisible after the fact.

Purpose: this is the audit-correctness half of the phase. Plans 03/04 make the discount happen at
checkout; this plan proves it stays correct after the fact, which is exactly the scenario the
existing `cost_price_snapshot` pattern was built to guarantee for cost data.
Output: a read-only discount-snapshot indicator on the reopened/paid-tab line-item view, and the
PROMO-06 E2E proof (checkout with promotion -> refund -> reopen -> margin report, all against the
same discounted sale).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-promotions-discount-management/27-CONTEXT.md
@src/widgets/PaymentModal/ui/PaymentForm.tsx
@supabase/migrations/20260825000002_get_product_sales_report_rpc.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Historical discount badge on reopened/paid-tab line items</name>
  <files>src/widgets/PaymentModal/ui/PaymentForm.tsx</files>
  <read_first>src/widgets/PaymentModal/ui/PaymentForm.tsx (the groupOrderItems/line-item rendering block used for a reopened or already-paid tab's item list), src/entities/tab/model/types.ts (OrderItem shape, confirm promotionId/discountRate/discountAmount are present after Plan 01's domain.ts change and the row-mapper that reads order_items into this type)</read_first>
  <action>
Find wherever PaymentForm.tsx (or the component it delegates to for a reopened/paid tab's read-only
item list) renders each `OrderItem` line, and add a small "X% off" indicator when
`item.discountAmount` is truthy and greater than 0 — sourced directly from the stored
`discountRate`/`discountAmount` columns, never by re-calling `evaluateBestPromotion` (a reopened
sale must show its historical discount even if the underlying promotion was since edited or
deleted, per PROMO-06). If the row-mapper that turns a raw `order_items` row into the `OrderItem`
domain type does not yet map `promotion_id`/`discount_rate`/`discount_amount` into
`promotionId`/`discountRate`/`discountAmount` (Plan 01 added the Zod fields; confirm the actual
SELECT/mapper call site also requests and maps these three columns — grep for wherever
`cost_price_snapshot` is mapped alongside `unit_price` for order_items and add the three new
columns to that same mapper), add that mapping now.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/widgets/PaymentModal 2>&1 | tail -20</automated>
    <fails_when>tsc exits non-zero, or the extended PaymentForm test suite fails (add a case asserting a reopened tab's line with a non-null discountAmount renders the historical indicator)</fails_when>
  </verify>
  <done>A reopened or already-paid tab's line-item list shows which lines carried a promotion discount and at what rate, sourced from the stored snapshot columns, not a live recomputation.</done>
</task>

<task type="auto">
  <name>Task 2: E2E proof of PROMO-06 (snapshot survives refund + reopen + margin)</name>
  <files>e2e/payments/promotion-snapshot-refund-reopen.spec.ts</files>
  <read_first>e2e/payments/ (an existing refund spec and an existing reopen spec for the fixture/assertion pattern), e2e/reports/ (an existing product-sales-report spec for the margin-assertion pattern)</read_first>
  <action>
Create e2e/payments/promotion-snapshot-refund-reopen.spec.ts: (a) seed an active promotion, complete
a direct sale for a product it discounts, then EDIT or DELETE that promotion via the admin
/promotions UI, then reopen the sale and assert the reopened line still shows the original discount
amount/rate (the historical snapshot, unaffected by the promotion's current state) — this is the
core PROMO-06 assertion; (b) process a full refund on that same sale and assert the refunded amount
equals the originally-charged discounted total, not a re-derived list-price total; (c) navigate to
the Product Sales report for the relevant date range and assert the reported margin for that
product/line reflects the discounted unit price against cost_price_snapshot, not list price against
cost (a numeric assertion comparing the report's margin figure to a hand-computed expected value
from the known fixture prices).
  </action>
  <verify>
    <automated>npx playwright test e2e/payments/promotion-snapshot-refund-reopen.spec.ts</automated>
    <fails_when>the spec reports any failed test or times out</fails_when>
  </verify>
  <done>e2e/payments/promotion-snapshot-refund-reopen.spec.ts passes headless, proving the promotion snapshot survives the promotion's own deletion, survives a refund's amount calculation, and flows correctly into the existing margin report with zero report-code changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| historical order_items read | read-only display of already-committed data; no new write path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-10 | Repudiation | historical discount visibility after promotion deletion | low | accept | Already mitigated structurally by Plan 01's `ON DELETE SET NULL` on `order_items.promotion_id` plus the numeric `discount_rate`/`discount_amount` columns surviving independently of the `promotions` row's lifecycle — this plan only adds a UI surface for what's already durable |
</threat_model>

<verification>
1. `npm run typecheck` passes.
2. `npx vitest run src/widgets/PaymentModal` is green.
3. `npx playwright test e2e/payments/promotion-snapshot-refund-reopen.spec.ts` is green.
</verification>

<success_criteria>
A promotion's discount survives its own deletion, a refund, and a reopen, exactly as recorded at
sale time — and the existing margin report is proven correct against the discounted price with zero
report-query changes, verified end to end.
</success_criteria>

<output>
Create `.planning/phases/27-promotions-discount-management/27-05-SUMMARY.md` when done
</output>
