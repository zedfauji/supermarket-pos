---
phase: "27"
slug: "promotions-discount-management"
status: draft
shadcn_initialized: true
preset: "style=radix-nova, base=radix, baseColor=neutral, cssVariables=true, iconLibrary=lucide, tailwindVersion=v4"
created: "2026-09-01"
---

# Phase 27 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | `style=radix-nova, base=radix, baseColor=neutral, cssVariables=true, iconLibrary=lucide` (`components.json`, verified via `npx shadcn info`) |
| Component library | Radix primitives (shadcn `radix-nova` style) |
| Icon library | `lucide-react` |
| Font | `--font-sans: 'Geist Variable'` (body/UI text, default everywhere) · `--font-heading: 'Playfair Display'` (page-level `<h2>` titles only, via `SectionHeader`/`PageContainer` — reused unchanged, no new decision needed this phase) |

**Project-specific note:** `components.json`'s `aliases.ui` points at `src/app/components/ui` (unused — `npx shadcn info` reports "No components installed" against that path). This project's actual convention, per `CLAUDE.md`, is to hand-place/maintain shadcn primitives directly in `src/shared/ui/` (the `shared` FSD layer), not through the CLI's default output path. Treat `src/shared/ui/` as the real install location for this phase's inventory below.

---

## Component Inventory

Enumerated by `ls src/shared/ui/*.tsx | grep -v -E '\.(test|stories)\.'` — 54 top-level files (+1, `CategoryTreePicker/`, a subdirectory component not caught by the top-level glob) — hand-maintained in `src/shared/ui/` (no package version; not distributed via `npm`), alongside `tailwindcss@4.3.3` [VERIFIED: `node_modules/tailwindcss/package.json`] — 2026-09-01.

This is a **non-exhaustive** list of known-good components for this phase — the executor may use anything `src/shared/ui/` exports; checking for a component outside this table is expected, not an exception.

| Component | Import path | Notes |
|-----------|-------------|-------|
| `PageContainer` | `@shared/ui` | `/promotions` page shell — title, `backTo="/home"`, `actions` slot for "New Promotion" CTA (mirrors `src/pages/suppliers/index.tsx` exactly) |
| `DataTable` | `@shared/ui` | Promotions list — built-in loading skeleton (`isLoading`), `emptyState` slot, `searchable` prop, row click |
| `EmptyState` | `@shared/ui` | Plugged into `DataTable`'s `emptyState` for "no promotions yet" |
| `StatusBadge` | `@shared/ui` | Extend `statusConfig` with new promotion-lifecycle keys (`promo_active`/`promo_scheduled`/`promo_expired`/`promo_inactive`) — same extension pattern as the existing `po_draft`/`po_received` entries |
| `Badge` | `@shared/ui` | Scope-type chip (Product/Category/Subcategory), discount-type chip (%/fixed), and the cart-line "X% off" indicator |
| `ConfirmDialog` | `@shared/ui` | Delete-promotion confirmation (destructive variant) |
| `Switch` | `@shared/ui/switch` (direct — not barrel-exported as a type, component is) | Inline active/inactive toggle on a promotion row; also the existing collapsible-section toggle pattern (mirrors `PaymentForm`'s `discount-toggle`) reused for the new "Apply Promotion" section |
| `CategoryTreePicker` | `@shared/ui/CategoryTreePicker` | Category **and** subcategory scope target picker — a subcategory is just a category row with `parentId` set (PROMO-01), so one picker instance covers both scope types, no second component needed |
| `DateRangePicker` | `@shared/ui` | Active date-range field (`fromStr`/`toStr` controlled) — currently used for report filters, same controlled shape fits a promotion's active range |
| `MoneyInput` | `@shared/ui` | Fixed-amount discount value entry (`discountType === 'fixed'`) |
| `Input` (type="number") | `@shared/ui` | Percent discount value entry — mirrors `NearExpirySettingsTab`'s `thresholdDays` input exactly, including its `min`/`max` pattern |
| `Select` / `SelectTrigger` / `SelectContent` / `SelectItem` | `@shared/ui/select` | "Apply existing promotion" dropdown at payment (PROMO-05) |
| `POSButton` | `@shared/ui` | All primary CTAs — `touchSize="large"` for in-dialog Save actions, `touchSize="xl"` reserved for checkout-critical actions only (not this phase's admin surfaces) |
| `MoneyDisplay` | `@shared/ui` | Discounted line price / discount-amount row in cart and at payment |
| `Label` / `FormField` | `@shared/ui` | Form field scaffolding in the Create/Edit Promotion dialog |
| `dialog.tsx` (`Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogFooter`) | `@shared/ui/dialog` | Create/Edit Promotion dialog shell (not barrel-exported — import directly) |
| `ManagerPinDialog` | `@features/manager-pin-gate` | Reused unchanged for the below-cost override gate and the ad-hoc/custom-discount gate (PROMO-05/PROMO-07) — new `requiredAction` value(s) are a planner/RBAC decision, not a visual one |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-text gaps (Badge internals, Zap indicator to price) |
| sm | 8px | Compact controls (scope/type button-group gaps, badge row gaps) |
| md | 16px | Default field spacing inside the Create/Edit Promotion dialog |
| lg | 24px | Section padding inside `PageContainer`/dialog body |
| xl | 32px | Layout gaps between the list toolbar and the table |
| 2xl | 48px | Not used this phase |
| 3xl | 64px | Not used this phase |

Exceptions: none. Touch targets on `/promotions` (admin/manager-only, not a till surface) use `POSButton`'s existing `default` (44px) or `large` (56px) sizes — `xl` (72px) is reserved for till-critical actions per existing convention and is not used on this admin page.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 (regular) | 1.5 |
| Label | 14px | 500 (medium — reused `Label` component default, `font-medium`, unchanged this phase) | 1.2 |
| Heading | 18px | 600 (semibold) | 1.2 |
| Display | 24px | 600 (semibold) | 1.2 |

New copy authored for this phase (dialog titles, hint text, badges, empty-state text) sticks to **regular 400** (body/hints) or **semibold 600** (section/dialog headings) — the codebase's 2 dominant weights. `Label`/`Button` render at their own established 500 internally via reused components; this phase does not introduce a new medium-weight text role.

- Body 14px/400: table cell text, hint text (`text-sm`), form descriptions
- Label 14px/500: form field labels (`<Label>`), reused as-is
- Heading 18px/600: dialog title (`DialogTitle`), settings sub-section title (mirrors `NearExpirySettingsTab`'s `<h2 className="text-lg font-semibold">`)
- Display 24px/600: `/promotions` page title (`SectionHeader`'s `<h2 className="font-heading text-2xl font-semibold">`, reused via `PageContainer` — no new decision)

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `var(--background)` | Page background |
| Secondary (30%) | `var(--card)` / `var(--secondary)` | Table rows, dialog surface, form section cards |
| Accent (10%) | `var(--pos-accent)` (jade) | Reserved for: primary CTA (`POSButton` default variant), the `promo_active` status badge, the "X% off" cart-line indicator badge, focus rings |
| Destructive | `var(--destructive)` / `var(--pos-danger)` | Reserved for: delete-promotion `ConfirmDialog`, the below-cost-block toast/dialog, the `promo_expired` status badge |

Accent reserved for: primary "New Promotion"/"Save"/"Apply Promotion" buttons, the `promo_active` `StatusBadge`, and the cart-line discount-applied `Badge` — nothing else on these surfaces gets accent color.

**Explicit non-reuse:** `var(--pos-warning)` (amber) is already semantically owned by the near-expiry alert (`useNearExpiryAlerts`, the existing `Badge` on `CartItem`/`HomeDashboard`). The new promotion-applied indicator on a cart line must use `pos-accent`, **not** `pos-warning`, even though PROMO-02's trigger is itself expiry-driven — otherwise a cashier cannot visually distinguish "this item is near-expiry" from "this item has a promotion applied," and the two can legitimately co-occur on the same line (expiry-triggered promotion + separate near-expiry stock alert).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "New Promotion" (`/promotions` page action, opens Create dialog) |
| Empty state heading | "No promotions yet" |
| Empty state body | "Create a promotion to automatically discount a product, category, or subcategory." + CTA button "New Promotion" (same `EmptyState.action` as the page-level CTA) |
| Error state | "Couldn't load promotions. Check your connection and try again." (list-fetch failure — `toast.error` + `DataTable` keeps rendering its last-known rows rather than clearing to blank) |
| Destructive confirmation | Delete promotion: "Delete this promotion? Sales that already used it keep their recorded discount — this only stops it from applying to future sales." (reassures the cashier/admin this is safe against PROMO-06's snapshot, not a data-loss action) |
| Destructive confirmation | Below-cost override: "This discount would sell below cost. Manager PIN required to continue." (precedes the reused `ManagerPinDialog`, whose own title/description copy — `managerPinGate.title`/`managerPinGate.description` — is unchanged) |
| Destructive confirmation | Ad-hoc/custom discount gate: reuses `ManagerPinDialog`'s existing unchanged copy — no new destructive-confirmation string needed, only a new `requiredAction` value (planner's call) |

---

## UI Considerations

Applicable state considerations resolved: 15 covered, 2 backstop, 2 unresolved

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| empty | Promotions list table (list-collection) | ✅ covered | `DataTable`'s `emptyState` renders the `EmptyState` copy above with the "New Promotion" action — see Copywriting Contract |
| loading | Promotions list table (list-collection) | ✅ covered | `DataTable`'s built-in `isLoading` skeleton rows (`TableRowSkeleton`) — no custom loading UI needed |
| populated | Promotions list table (list-collection) | ✅ covered | Standard row set: name, scope (target name + type badge), discount type/value, active date range, `StatusBadge`, row actions — same shape as `SupplierListPanel`'s table |
| overflow | Promotions list table (list-collection) | ✅ covered | `DataTable`'s built-in `searchable` prop (`SearchInput`) — a single store realistically has a handful of promotions, no pagination needed |
| zero-one-many | Promotions list table (list-collection) | ✅ covered | Zero → `EmptyState`; one/many → uniform table rows, no singular/plural copy required for a table |
| error | Promotions list table (list-collection) | 🧪 backstop | Fetch-failure copy is specified (Copywriting Contract), but the exact "keep last-known rows vs. clear" retry behavior needs an explicit test — no existing precedent component handles this exact case to copy from |
| partial | Promotions list table (list-collection) | ⚠ unresolved | A promotion whose scope target (a category) was since deleted — no decision locks the row's fallback rendering (e.g. "(deleted)" label vs. hiding the row). Planner assumption. |
| loading | Create/Edit Promotion form | ✅ covered | `POSButton` `disabled={mutation.isPending}` during save — mirrors `NearExpirySettingsTab`'s `save()` pattern exactly |
| error | Create/Edit Promotion form | ✅ covered | `toast.error(result.error.message)` on RLS/validation rejection (server) + inline `FormField` validation (client, e.g. discount value 0-100 range) — same two-layer pattern as every other admin form in this codebase |
| partial | Create/Edit Promotion form | ⚠ unresolved | Switching scope type (Product → Category) mid-edit — no decision locks whether the previously-selected target clears automatically. Planner assumption. |
| empty | "Apply Promotion" selector at payment (list-collection/interactive-control) | ✅ covered | When zero promotions are currently active, the entire "Apply Promotion" collapsible section is hidden rather than shown empty — mirrors the existing ad-hoc discount section's `discountExpanded` collapse pattern, never renders an empty picker |
| loading | "Apply Promotion" selector at payment | ✅ covered | Promotions are already resolved client-side (the same query backing PROMO-03's live cart display) before the payment screen renders — no separate loading state |
| zero-one-many | "Apply Promotion" selector at payment | ✅ covered | Native `Select` scroll handles one/many options; zero handled by the empty-state row above |
| error | "Apply Promotion" selector at payment | 🧪 backstop | If the promotions query fails, checkout must not block — ad-hoc/custom discount and no-discount paths stay available. No existing precedent covers this exact combination; needs an explicit test. |
| overflow | Cart-line promotion indicator (static-content, inside `CartItem`) | ✅ covered | Reuses the existing Zap-icon + `aria-label` pattern (`CartItem.tsx`, currently labeled `cartItem.happyHourPrice` — rename to a promotion-appropriate key, same visual treatment) |
| long-text | Cart-line promotion indicator | ✅ covered | The promotion's name is deliberately **not** shown inline on the cart line — only a compact "X% off" `Badge` + strikethrough original price (`MoneyDisplay`) — avoids long-text overflow risk by not displaying free-text there at all |
| error | Below-cost override dialog | ✅ covered | This dialog IS the error-recovery path (`BELOW_COST_REQUIRES_OVERRIDE`) — reuses `ManagerPinDialog`'s existing wrong-PIN error copy (`managerPinGate.incorrectPin`), no new error copy needed |
| loading | Below-cost override dialog | ✅ covered | Reuses `ManagerPinDialog`'s existing `isIdleOrLoading` prop/spinner state |
| error | Expiry-discount rate settings field | ✅ covered | Same `save()`/`toast.error` pattern as the existing `thresholdDays` field on `NearExpirySettingsTab`, validated 0-100 client + server |

<!-- Status vocabulary (locked by probe-core projectTruths):
     ✅ covered   → a plain truth string lifted into must_haves.truths
     🧪 backstop  → a flat scalar { statement, verification: backstop }; at verify time, no explicit
                    evidence → insufficient_spec → human_needed (never a silent pass, #1154)
     ⚠ unresolved → an explicit planner assumption (surfaced, never silently dropped)
     Rows are REPLACED (not appended) on a probe re-run — idempotent. -->

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | `dialog`, `select`, `switch`, `alert-dialog` (all already vendored in `src/shared/ui/`, no new fetch needed this phase) | not required |

No third-party registries declared for this phase — the registry vetting gate does not apply.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS
- [ ] Dimension 7 Inventory Provenance: PASS

**Approval:** pending
