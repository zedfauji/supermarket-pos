---
phase: 27-promotions-discount-management
plan: 15
type: execute
wave: 5
depends_on: ["27-12", "27-14"]
files_modified:
  - e2e/promotions/wizard-multi-target-scope.spec.ts
  - e2e/promotions/recurring-window-checkout.spec.ts
autonomous: true
gap_closure: true
gap_ids: [G-27-8]
requirements: [PROMO-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "An admin can create a promotion scoped to 2 products across 2 different categories through the real wizard UI, and both products' checkout price reflects the discount while an unrelated third product does not."
    - "An admin can create a store-wide (zero targets, 'no restriction' toggle) promotion through the wizard, and it discounts an arbitrary product never explicitly selected."
    - "A promotion restricted to a specific day-of-week and time-of-day window discounts a checkout made inside that window and does NOT discount one made outside it — proven against the real checkout RPC, not mocked."
    - "Editing an existing legacy single-target promotion (created before this gap-closure round, via the old scope model) through the new wizard correctly pre-populates its current scope and re-saves without data loss."
  artifacts:
    - e2e/promotions/wizard-multi-target-scope.spec.ts
    - e2e/promotions/recurring-window-checkout.spec.ts
  key_links:
    - "Wizard create flow -> save_promotion_with_targets -> promotion_products/promotion_categories -> process_direct_sale_atomic's multi-target matching (27-11) -> checkout total"
---

<objective>
Close out gap G-27-8 Part B with real, headless Playwright E2E coverage — per this project's mandatory
automated-testing policy (no manual UAT, no human_needed states) — proving the full stack plans
27-11..27-14 built actually works end to end: create a multi-target or store-wide or recurring-window
promotion through the real wizard screen, then check out a real cart against it and assert the correct
price.

Purpose: this is the final proof that gap G-27-8 is genuinely closed, not just individually
unit/integration-tested in isolation per plan.
Output: two new E2E spec files in `e2e/promotions/`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@e2e/payments/apply-promotion-and-custom-discount.spec.ts
@e2e/helpers/auth.ts
@e2e/helpers/supabase.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wizard-driven multi-target scope and store-wide promotion, proven at checkout</name>
  <files>e2e/promotions/wizard-multi-target-scope.spec.ts</files>
  <read_first>e2e/payments/apply-promotion-and-custom-discount.spec.ts (seedProduct/openCaja/resetTestState fixture pattern to reuse), e2e/helpers/supabase.ts (getServiceClient, findRoleStaffId), e2e directory listing under e2e/promotions/ for existing scenario-matrix conventions from Phase 27 Plan 07</read_first>
  <action>
Create `e2e/promotions/wizard-multi-target-scope.spec.ts` covering: (a) log in as admin, navigate to
`/promotions/new`, complete the wizard for a promotion scoped to 2 seeded products spanning 2 seeded
categories with a 15% discount and a validity range covering today, save, then log in as cashier and
check out a cart containing both scoped products plus one unrelated third seeded product — assert the
two scoped lines are discounted and the third is not; (b) create a store-wide promotion (name +
discount only, "no restriction" toggle checked, zero products/categories selected) through the wizard,
save, and check out an arbitrary seeded product (never explicitly selected) — assert it is discounted;
(c) edit an existing legacy single-target promotion (seed one directly via `getServiceClient()` with
the OLD shape — `product_id` set, no junction rows, matching pre-gap-closure data) through the wizard's
edit route, assert the Scope step pre-populates with that one product checked, re-save with no changes,
and assert the promotion still discounts that product at checkout afterward (no data loss from the
legacy-to-new-shape round trip).
  </action>
  <verify>
    <automated>npx playwright test e2e/promotions/wizard-multi-target-scope.spec.ts</automated>
    <fails_when>any of the three scenarios fails, times out, or the third unrelated product is incorrectly discounted in scenario (a)</fails_when>
  </verify>
  <done>All three scenarios pass headless: multi-target scope, store-wide scope, and legacy-promotion round-trip through the wizard, each proven against a real checkout total.</done>
</task>

<task type="auto">
  <name>Task 2: Recurring day-of-week / time-of-day window gates checkout pricing</name>
  <files>e2e/promotions/recurring-window-checkout.spec.ts</files>
  <read_first>e2e/promotions/wizard-multi-target-scope.spec.ts (Task 1's fixtures, reuse the seeding helpers), e2e/helpers/supabase.ts</read_first>
  <action>
Create `e2e/promotions/recurring-window-checkout.spec.ts`: seed a promotion directly via
`getServiceClient()` (bypassing the wizard UI here — this test is about checkout-time recurrence
evaluation, not wizard UI, matching Task 1's split of concerns) with `recurrence_days_of_week` set to
TODAY's real ISO day-of-week (compute it in the test from `new Date()` so this test is not flaky/
date-dependent) and `recurrence_time_start`/`recurrence_time_end` set to a window that INCLUDES the
current wall-clock time at test-run time (compute similarly — allow a generous +/-2 hour buffer around
`now` to avoid a flaky boundary failure on a slow CI run), scoped to one seeded product. Check out that
product as cashier and assert it IS discounted. Then update the same promotion's
`recurrence_days_of_week` to a day that is NOT today (e.g. `(todayIsoDow % 7) + 1`), re-check-out the
same product (a fresh cart/tab), and assert it is NOT discounted. This proves 27-11's SQL-side
recurrence matching and 27-12's client-side cart-preview matching are both live and correct against a
real transaction, not just their respective unit/integration test suites in isolation.
  </action>
  <verify>
    <automated>npx playwright test e2e/promotions/recurring-window-checkout.spec.ts</automated>
    <fails_when>either assertion fails or times out</fails_when>
  </verify>
  <done>A recurrence-windowed promotion discounts checkout inside its window and does not discount it outside — proven headless against the real checkout RPC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test fixtures (service-role seeding) | Test-only trust boundary; no production code path — service-role seeding mirrors the existing pattern every other e2e/promotions/ and e2e/payments/ spec already uses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-19 | N/A (test-only) | E2E fixtures | low | accept | No production trust boundary is introduced by this plan; both new specs exercise already-mitigated paths from plans 27-11..27-14 |
</threat_model>

<verification>
1. `npx playwright test e2e/promotions/wizard-multi-target-scope.spec.ts e2e/promotions/recurring-window-checkout.spec.ts` is green.
2. Full suite regression check: `npm run test:e2e` remains green (per this project's CLAUDE.md policy — every scenario automated, no human_needed states).
</verification>

<success_criteria>
Gap G-27-8 (both Part A and Part B) is proven closed end to end by real, headless Playwright E2E runs
— multi-target scope, store-wide scope, legacy-promotion compatibility, and recurring time-of-day/
day-of-week windows all correctly gate real checkout pricing.
</success_criteria>

<output>
Create `.planning/phases/27-promotions-discount-management/27-15-SUMMARY.md` when done.
</output>
