---
phase: 27-promotions-discount-management
plan: 04
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - src/widgets/PaymentModal/ui/PaymentForm.tsx
  - src/shared/lib/payment-processor.ts
  - src/features/checkout-sale/model/useCheckoutSale.ts
  - src/shared/lib/edge-function-contracts.ts
  - src/shared/lib/i18n/locales/es-MX/featOrders.json
  - src/shared/lib/i18n/locales/en-US/featOrders.json
  - e2e/payments/apply-promotion-and-custom-discount.spec.ts
  - e2e/errors/promotion-floor-guard.spec.ts
autonomous: true
requirements: [PROMO-05, PROMO-07]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Applying an ad-hoc/custom discount now requires a manager PIN via ManagerPinDialog (requiredAction='apply_custom_discount') before the discount section becomes usable — previously ungated on direct sale, which was a latent bug masked by the server's blanket DISCOUNT_UNSUPPORTED rejection."
    - "The discount-scope button group is retired to a single 'all' option — pool_only/consumptions_only buttons no longer render (D-10/PROMO-05)."
    - "Applying an existing promotion at payment coexists with the ad-hoc discountType/discountValue field on PaymentSchema — selecting a promotion never overwrites or clears the ad-hoc discount fields, and vice versa."
    - "The 'Apply Promotion' section is hidden entirely (not shown empty) when zero promotions are currently active — mirrors the existing discountExpanded collapse pattern, never renders an empty picker."
    - "Promotions backing the 'Apply Promotion' selector are already resolved client-side (the same usePromotions() query backing PROMO-03's live cart display) before the payment screen renders — no separate loading state."
    - "The native Select's own scroll handles one-vs-many promotion options; zero is handled by the hidden-section rule above, not by an empty-option row."
    - statement: "If the promotions query fails at the payment screen, checkout still proceeds via the ad-hoc/custom-discount and no-discount paths — a promotions fetch failure never blocks payment."
      verification: backstop
    - "The below-cost override dialog reuses ManagerPinDialog's existing wrong-PIN error copy (managerPinGate.incorrectPin) verbatim — no new error copy is introduced for this path."
    - "The below-cost override dialog reuses ManagerPinDialog's existing isIdleOrLoading spinner state verbatim."
    - "A wrong manager PIN on either the ad-hoc-discount gate or the below-cost override leaves the sale unsubmitted and re-openable; retrying with the correct PIN does not double-submit (the existing per-attempt idempotency-key pattern is unchanged)."
    - statement: "Concurrent ad-hoc-discount submissions from two terminals against the same in-progress cart never race, because each terminal's cart is local, unshared Zustand state with its own idempotency key."
      verification: backstop
  artifacts:
    - e2e/payments/apply-promotion-and-custom-discount.spec.ts
    - e2e/errors/promotion-floor-guard.spec.ts
  key_links:
    - "PaymentForm discount-toggle -> ManagerPinDialog(requiredAction='apply_custom_discount') -> discountInfoArg -> callProcessDirectSale({ discountScope:'all', discountType, discountValue, discountAmount, managerOverride: true })"
    - "process_direct_sale_atomic BELOW_COST_REQUIRES_OVERRIDE response -> ManagerPinDialog(requiredAction='apply_custom_discount') -> retry with managerOverride: true"
---

<objective>
Make the payment screen's existing (currently ungated and non-functional-on-direct-sale) ad-hoc
discount UI safe and real: gate it behind a manager PIN, retire the two bar-pos-only discount-scope
values, add an "Apply Promotion" selector for PROMO-05's "cashier applies an existing active
promotion" path, and wire the below-cost override retry flow for PROMO-07.

Purpose: Plan 01 unblocked the RPC's discount path and added `p_manager_override`; this plan is the
first place a human actually triggers it.
Output: PIN-gated ad-hoc discount section, "Apply Promotion" selector, below-cost override dialog,
E2E proof of both PROMO-05 and PROMO-07's UI paths.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-promotions-discount-management/27-CONTEXT.md
@.planning/phases/27-promotions-discount-management/27-UI-SPEC.md
@src/widgets/PaymentModal/ui/PaymentForm.tsx
@src/shared/lib/payment-processor.ts
@src/features/checkout-sale/model/useCheckoutSale.ts
@src/features/manager-pin-gate/ui/ManagerPinDialog.tsx
@src/shared/lib/edge-function-contracts.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: PIN-gate the ad-hoc discount section + retire pool_only/consumptions_only</name>
  <files>src/widgets/PaymentModal/ui/PaymentForm.tsx, src/shared/lib/payment-processor.ts, src/features/checkout-sale/model/useCheckoutSale.ts, src/shared/lib/edge-function-contracts.ts</files>
  <read_first>src/widgets/PaymentModal/ui/PaymentForm.tsx (full file — the discount-toggle/discountScope/discountType/discountValue state at lines 195-230, the discount-section JSX at ~655-780, the discountInfoArg construction at lines 346-350/485-490), src/shared/lib/payment-processor.ts (DiscountInfo interface, lines 17-22, and every processCashPayment/processCardPayment call site forwarding discountInfo), src/features/checkout-sale/model/useCheckoutSale.ts (full file — submit()'s discountInfo forwarding at lines 123-130), src/shared/lib/edge-function-contracts.ts (ProcessDirectSaleRequestSchema, lines 616-668), src/features/manager-pin-gate/ui/ManagerPinDialog.tsx (full file)</read_first>
  <action>
Remove the pool_only/consumptions_only buttons from PaymentForm.tsx's discount-scope button group
(lines ~680-701) — only render a single, non-interactive "all" label since DiscountScopeSchema is
now `z.enum(['all'])` (Plan 01); delete `setDiscountScope`/the `discountScope` useState entirely and
hardcode `'all'` wherever it's referenced (discountInfoArg's `scope`, getDiscountBase's third arg).
Add a `managerOverride` boolean state (default false) plus a `ManagerPinDialog` instance rendered
unconditionally (mirrors ManagerPinDialog's own "stays mounted, toggles open" pattern) with
`requiredAction="apply_custom_discount"`: expanding the discount section (`discountExpanded` toggle
true) now first opens this dialog instead of immediately revealing the discount fields — only on
`onSuccess` does `discountExpanded` become true and `managerOverride` become true for this payment
attempt. If the cashier collapses the section again, reset `managerOverride` to false (a fresh PIN
entry is required each time the section is re-expanded — do not cache authorization across
collapse/expand cycles within the same payment attempt). Extend `DiscountInfo` in
payment-processor.ts with `managerOverride: boolean` and thread it through every
processCashPayment/processCardPayment/processBankTransferPayment/processSplitPayment call site
(lines 40-230-ish) into `callProcessPayment`/`callProcessSplitPayment`'s existing call shape — for
the direct-sale path specifically, thread it through useCheckoutSale.ts's `submit()` into
`callProcessDirectSale`'s `managerOverride` field (added to ProcessDirectSaleRequestSchema in Plan
01 Task 1 — confirm it's present; if Plan 01 named it differently, use that exact name for
consistency rather than introducing a second field). Handle the new
`BELOW_COST_REQUIRES_OVERRIDE`/`DISCOUNT_REQUIRES_MANAGER`/`FORBIDDEN`/`INVALID_DISCOUNT_SCOPE`
error codes from the RPC in whatever error-mapping function this codebase already centralizes RPC
error codes in (grep for where `PRICE_MISMATCH`/`AMOUNT_MISMATCH` are mapped to user-facing copy and
add these alongside them, same file/pattern) — `BELOW_COST_REQUIRES_OVERRIDE` specifically should
trigger the below-cost variant of the ManagerPinDialog flow described in Task 2, not a generic error
toast.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/widgets/PaymentModal/ui/PaymentForm.test.tsx 2>&1 | tail -30</automated>
    <fails_when>tsc exits non-zero, or PaymentForm.test.tsx fails (extend it with a case asserting the discount section requires a successful ManagerPinDialog before discount fields render, and that pool_only/consumptions_only buttons no longer exist in the DOM)</fails_when>
  </verify>
  <done>Expanding the ad-hoc discount section on the payment screen always prompts ManagerPinDialog first; a correct PIN reveals the discount fields (percent/fixed only, scope fixed to 'all') and submits with managerOverride: true; an incorrect PIN leaves the section collapsed and the sale unsubmitted.</done>
  <reversibility rating="costly">D-07: PIN-gating the ad-hoc discount and below-cost override is a cashier-training contract ("below-cost or custom-override always needs a manager") — reverting to ungated later is a UX/training change, not just a code revert.</reversibility>
</task>

<task type="auto">
  <name>Task 2: "Apply Promotion" selector + below-cost override retry</name>
  <files>src/widgets/PaymentModal/ui/PaymentForm.tsx, src/shared/lib/i18n/locales/es-MX/featOrders.json, src/shared/lib/i18n/locales/en-US/featOrders.json</files>
  <read_first>src/entities/promotion/model/queries.ts (usePromotions, from Plan 01), src/widgets/PaymentModal/ui/PaymentForm.tsx (the discount-section JSX and the cash/card submit handlers from Task 1)</read_first>
  <action>
Add an "Apply Promotion" `Select`/`SelectTrigger`/`SelectContent`/`SelectItem` control (import from
@shared/ui/select directly) above the existing ad-hoc discount section, populated from
`usePromotions()` filtered to promotions currently within their active date range (client-side
`now` check, same inclusive-boundary contract as Plan 01's RPC). Render this control's containing
section only when that filtered list is non-empty (hidden entirely otherwise, per the UI-SPEC
empty-state rule — do not render an empty Select). Selecting a promotion sets a
`selectedPromotionId` state (independent of `discountType`/`discountValue` — this is the D-10
"coexists, never overwrites" requirement) and calls a new cartStore/local re-evaluation: for every
cart line matching the selected promotion's scope (product_id or category_id equality), if that
promotion's discount amount for the line beats the line's *current* discount, update the line's
unitPrice via `Math.max` semantics (never make an already-better-discounted line worse) — reuse
`evaluateBestPromotion`'s per-candidate money-amount formula rather than re-deriving it. No manager
PIN is required for this path — applying an existing active promotion is the non-ad-hoc case PROMO-
05 explicitly distinguishes from the custom-discount path. When `process_direct_sale_atomic` (Plan
01) returns `BELOW_COST_REQUIRES_OVERRIDE` from ANY payment attempt (ad-hoc discount, an applied
promotion, or their combination), surface the exact ManagerPinDialog instance from Task 1
(`requiredAction="apply_custom_discount"`) with a preceding non-blocking message using UI-SPEC's
exact copy — "This discount would sell below cost. Manager PIN required to continue." — and on a
successful PIN, resubmit the SAME payment attempt with `managerOverride: true` (reuse the last
submitted idempotency key so this is a retry, not a new sale attempt).
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/widgets/PaymentModal/ui/PaymentForm.test.tsx 2>&1 | tail -30</automated>
    <fails_when>tsc exits non-zero, or the extended PaymentForm.test.tsx fails (add a case for the below-cost retry-with-override flow and one for the Apply Promotion section being absent with zero active promotions)</fails_when>
  </verify>
  <done>A cashier can select an active promotion at payment and see it applied to matching cart lines without a PIN; a below-cost combination from any source blocks with the exact UI-SPEC copy and a manager PIN retry succeeds using the same idempotency key.</done>
</task>

<task type="auto">
  <name>Task 3: E2E proof of PROMO-05 and PROMO-07</name>
  <files>e2e/payments/apply-promotion-and-custom-discount.spec.ts, e2e/errors/promotion-floor-guard.spec.ts</files>
  <read_first>e2e/payments/ (an existing manager-PIN-gated spec for the fixture pattern), e2e/errors/ (an existing spec for the error-scenario pattern), e2e/helpers/auth.ts</read_first>
  <action>
Create e2e/payments/apply-promotion-and-custom-discount.spec.ts covering: (a) a cashier selecting an
"Apply Promotion" option at payment with no PIN prompt, and the resulting total reflecting the
discount; (b) a cashier expanding the ad-hoc discount section, being prompted for a manager PIN,
entering a wrong PIN (assert the section stays collapsed/unsubmitted), then the correct PIN
(assert the discount fields appear and the sale completes with the discount applied and
discount_scope='all' recorded); (c) both an applied promotion AND an ad-hoc discount active on the
same sale simultaneously, asserting both are reflected in the final total independently (D-10
coexistence). Create e2e/errors/promotion-floor-guard.spec.ts: seed a product whose cost_price and
an active promotion combine to price a line below cost, attempt checkout, assert the below-cost
message renders with no completed sale, enter a manager PIN, and assert the sale then completes at
the (allowed) below-cost price.
  </action>
  <verify>
    <automated>npx playwright test e2e/payments/apply-promotion-and-custom-discount.spec.ts e2e/errors/promotion-floor-guard.spec.ts</automated>
    <fails_when>either spec reports a failed test or times out</fails_when>
  </verify>
  <done>Both new specs pass headless, proving PROMO-05's apply-existing-vs-ad-hoc coexistence and PROMO-07's floor-guard-block-then-override flow end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ManagerPinDialog (client) -> RPC role re-check (server) | client PIN check is UX only; server is the real authorization boundary (Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-08 | Elevation of Privilege | client-side managerOverride flag | critical | mitigate | Setting `managerOverride: true` client-side has no effect unless the RPC's own `role_permissions` re-check for `apply_custom_discount` passes (Plan 01) — a tampered client cannot self-authorize a below-cost sale or an ad-hoc discount |
| T-27-09 | Repudiation | who authorized a below-cost/ad-hoc discount | medium | mitigate | `record_audit('promotion.apply', ...)` and the existing `payment.process` audit entry (which now carries real discount_scope/type/value/amount) together identify which staff PIN authorized the action, via ManagerPinDialog's `eligibleStaff` match feeding into the same p_staff_id already recorded on every payment |
</threat_model>

<verification>
1. `npm run typecheck` passes.
2. `npx vitest run src/widgets/PaymentModal/ui/PaymentForm.test.tsx` is green.
3. `npx playwright test e2e/payments/apply-promotion-and-custom-discount.spec.ts e2e/errors/promotion-floor-guard.spec.ts` is green.
</verification>

<success_criteria>
The payment screen's ad-hoc discount is PIN-gated and functional on a direct sale for the first
time; an existing promotion can be applied without a PIN; a below-cost combination blocks and a
manager PIN unblocks it — all proven by real Playwright checkouts.
</success_criteria>

<output>
Create `.planning/phases/27-promotions-discount-management/27-04-SUMMARY.md` when done
</output>
