# Deferred Items — Phase 27 (Promotions & Discount Management)

## From Plan 27-03

**Pre-existing test failure (out of scope — not caused by 27-03):**

- `src/widgets/HomeDashboard/ui/HomeDashboard.test.tsx` — `HomeDashboard > gated buttons show lock icon for cashier` expects `lockIcons.length` to be `8`, but the actual count is now `9`.
- **Root cause:** Plan 27-02 (`a196647 feat(27-02): /promotions route, page shell, and Home dashboard tile`) added a new admin-only-gated `/promotions` nav tile to the Home dashboard's big-box nav, which adds one more locked button for the `cashier` role in the test's fixture — the hardcoded `8` in this assertion was never updated to `9` when that tile was added.
- **Why not fixed here:** `HomeDashboard.tsx`/`HomeDashboard.test.tsx` are not in 27-03's `files_modified` list and the regression predates this plan's Task 1 (confirmed via `git log` — the failing count already diverges as of `a196647`, before any 27-03 commit). Per the executor's deviation-rules scope boundary, pre-existing failures in unrelated files are logged, not auto-fixed.
- **Suggested fix:** bump the expected `lockIcons.length` from `8` to `9` in that one assertion (one-line test-only change) — a future plan touching `HomeDashboard.test.tsx`, or a dedicated cleanup pass, should pick this up.
- **Status: CLOSED by 27-07.** Fixed as part of 27-07's Task 2 full-suite phase-gate verification (it cascaded to fail `e2e/infra/ci.spec.ts`'s "npm run test exits 0" check, blocking a green E2E run). See 27-07-SUMMARY.md.

## From Plan 27-07 (Task 2 full-suite verification)

Discovered while running the full `npm run test:e2e` suite twice back-to-back as this plan's own phase-gate requirement. All items below were confirmed via direct reproduction (isolated re-runs) and are NOT caused by anything in 27-07's own `files_modified` list.

**Fixed in 27-07 (genuine Phase 27 regressions from earlier plans, blocking a green suite):**
- `e2e/checkout/atomic-rpc-guards.spec.ts` — asserted the retired `DISCOUNT_UNSUPPORTED` hard-block; Plan 01/04 replaced it with `DISCOUNT_REQUIRES_MANAGER`. Fixed.
- `e2e/rbac/rbac.spec.ts` — hardcoded `72` switches (18 actions × 4 roles), stale since Phase 23 (which already added 2 actions unaccounted for) and further stale after Plan 01 added `manage_promotions`/`apply_custom_discount` (actual: 22 actions × 4 roles = 88). Fixed.
- `e2e/checkout/peek-window.spec.ts` — Plan 01's expiry-proximity auto-discount (PROMO-02) legitimately fires on this file's `SECONDARY_PRODUCT_NAME` fixture (its seeded `expiry_date` happens to sit inside the near-expiry threshold), discounting a total this pre-existing test asserts as undiscounted. Fixed by clearing/restoring that one fixture's `expiry_date` around the describe block.
- `e2e/payments/promotion-snapshot-refund-reopen.spec.ts` (Plan 05) — its `afterEach` never unwound `refund_items`/`order_items` before deleting its fixture products/categories, so every delete call failed silently on an FK constraint on every run since Plan 05 shipped. 14 leftover "E2E Promo-Snapshot Category/Product" rows had accumulated in the shared local catalog. Fixed the cleanup order; manually purged 13 of 14 leftover rows (1 left alone — already adopted as a live fixture by `full-day-soak.spec.ts`, unrelated to Promotions).

**Confirmed pre-existing, NOT caused by Phase 27 (reproduced in isolation, left unfixed per scope boundary):**
- `e2e/checkout/barcode-scan-search.spec.ts` "category tabs compose with search" — already documented in `.planning/STATE.md` Blockers/Concerns as pre-existing since Phase 18, no phase assigned.
- `e2e/receipts/pdf-delivery.spec.ts`, `print-retry-resilience.spec.ts` (×2), `reprint.spec.ts` — the already-documented `__TAURI_EVENT_PLUGIN_INTERNALS__.unregisterListener` mock gap (`.planning/STATE.md`: "Phase 25 not yet planned"), root-caused during Phase 24.
- `e2e/reports/report-tabs.spec.ts` "PGRST_DB_MAX_ROWS regression" — a filler-inventory-lookup fixture-setup failure (`Cannot coerce the result to a single JSON object`), unrelated to promotions pricing/RBAC.
- `e2e/soak/full-day-soak.spec.ts` — a date-string off-by-one (`expect "2026-09-14" toBe, received "2026-09-13"`) in the soak test's own relative-date computation; unrelated to any promotions/RBAC file.
- `e2e/products/categories.spec.ts` "T8: bartender sees only the Language tab" — expects the Spanish "Idioma" tab label but the pinned cashier E2E account is documented elsewhere in this codebase (`e2e/helpers/supabase.ts`) as pinned to en-US; reproduces in complete isolation (zero other tests running), a locale-pinning/test-authoring mismatch predating Phase 27 entirely (comment references "39-07").
- `e2e/inventory/near-expiry-alerts.spec.ts` "admin saves the threshold and it persists after reload" — a save-then-reload race (the test reloads immediately after clicking Save with no wait for the write to settle), unrelated to promotions; matches the same class of "success toast can render before the write is visible to a fresh read" race already called out elsewhere in this codebase (e.g. `e2e/inventory/open-units.spec.ts`'s own comment on the same phenomenon).
- `e2e/receipts/category-grouping.spec.ts` "SC-2b" — this test's own fixture-selection strategy (`pickTwoCategoryProducts`, "picks any real row, no fixture/mock products") is inherently fragile against ANY other spec's throwaway category rows in the shared local catalog; it started failing only once enough unrelated fixture categories accumulated (see the Plan 27-05 leak above) and continues intermittently as long as any other spec's own live fixture (e.g. `full-day-soak.spec.ts`) occupies a `routing: 'NONE'` category slot. Not a Promotions-specific bug — a pre-existing test-design fragility that predates and is independent of Phase 27; a real fix belongs to whoever owns `e2e/receipts/` (e.g. pin `pickTwoCategoryProducts` to a name-prefix allowlist or a dedicated fixture category instead of "any real row").
- `e2e/errors/error-scenarios-and-validation.spec.ts` ER7 and `e2e/inventory/inventory-intelligence.spec.ts` T5 — both classified "flaky" (passed on Playwright's automatic retry), not hard failures; unrelated to promotions.
- `e2e/inventory/open-units.spec.ts` — classified "flaky" in one run (a stale/duplicate "Product created" toast strict-mode violation, a UI-timing race, self-resolved on retry); unrelated to promotions.
