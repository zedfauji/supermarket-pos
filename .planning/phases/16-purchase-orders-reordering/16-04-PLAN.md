---
phase: 16-purchase-orders-reordering
plan: 04
type: execute
wave: 4
depends_on: ["16-03"]
files_modified:
  - src/features/receive-shipment/ui/ReceiveShipmentForm.tsx
  - src/features/receive-shipment/model/useReceiveShipment.ts
  - src/widgets/PurchaseOrderDetailPanel.tsx
  - src/shared/lib/i18n/locales/en-US/wAdmin.json
  - src/shared/lib/i18n/locales/es-MX/wAdmin.json
  - e2e/56-purchase-orders.spec.ts
autonomous: true
requirements: [PO-01, PO-02, PO-03]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Receiving a draft PO with a poId sets purchase_orders.status = 'received' and updates inventory.quantity_on_hand/cost_price/expiry_date in the same atomic RPC call — proven end-to-end through the running UI (16-01 already proved the RPC layer directly; this plan proves the full client path)."
    - "A cashier navigating directly to /purchase-orders is redirected to /home, AND a cashier's authenticated Supabase session gets zero rows from a direct REST SELECT on purchase_orders — both the UX-only route guard and the real RLS boundary are proven in the same spec, not just one or the other."
    - "A receive failure (including PO_ALREADY_RECEIVED) surfaces to the manager as a visible error, not a silent failure or a stuck loading state."
    - "⚠ PO-03's 'receive-in-full' edge probe did not classify into a standard category during planning. This plan's e2e Test C is the closest available closure — it proves the happy-path receive-in-full end-to-end through the UI. 16-01 Task 1 already covers the double-receive and supplier-mismatch edge cases at the RPC layer directly. No additional unclassified receive-in-full gap was identified while authoring this plan; flagged here for the phase verifier to re-examine if one surfaces during /gsd-verify-work."
  artifacts:
    - src/features/receive-shipment/ui/ReceiveShipmentForm.tsx::initialPurchaseOrder prop
    - src/features/receive-shipment/model/useReceiveShipment.ts (purchaseOrderKeys invalidation on success)
    - src/widgets/PurchaseOrderDetailPanel.tsx::Receive Shipment CTA
    - e2e/56-purchase-orders.spec.ts
  key_links:
    - "PurchaseOrderDetailPanel Receive Shipment -> ReceiveShipmentForm(initialPurchaseOrder) -> useReceiveShipment -> callReceiveShipment(poId) -> Edge Function -> receive_shipment RPC(p_po_id)"
  prohibitions:
    - "This phase's receive flow never implements partial-quantity receiving — Receive Shipment always closes the entire PO in one action; there is no 'receive partial, leave remainder open' control anywhere in ReceiveShipmentForm/PurchaseOrderDetailPanel."
---

<objective>
Close the loop on PO-03: wire the existing, unmodified `ReceiveShipmentForm` to accept an optional PO pre-fill (per 16-UI-SPEC.md's explicit "do not build a second receive form" instruction and D-04's thin-wrapper recommendation), attach it to a draft PO's detail view, and prove all four of ROADMAP Phase 16's success criteria end-to-end with a single new Playwright spec — per CLAUDE.md's mandatory-automated-testing policy, this is the phase's only verification surface for the full user-facing flow.

Purpose: Deliver receiving without duplicating receiving logic, and produce the automated evidence this project requires in place of any manual UAT.
Output: `ReceiveShipmentForm` gains `initialPurchaseOrder`, `PurchaseOrderDetailPanel` gains a Receive Shipment action, `e2e/56-purchase-orders.spec.ts`.
</objective>

## Artifacts this phase produces (this plan's slice)

- `src/features/receive-shipment/ui/ReceiveShipmentForm.tsx::initialPurchaseOrder` prop (pre-fill, disabled supplier select, `poId` carried through `receive.mutateAsync`)
- `src/features/receive-shipment/model/useReceiveShipment.ts` — `purchaseOrderKeys` invalidation added to its existing `onSuccess`
- `src/widgets/PurchaseOrderDetailPanel.tsx` — "Receive Shipment" CTA on draft POs
- `e2e/56-purchase-orders.spec.ts` — covers ROADMAP Phase 16 success criteria 1-4

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-purchase-orders-reordering/16-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReceiveShipmentForm gains optional PO pre-fill/poId, wired from PurchaseOrderDetailPanel's Receive Shipment CTA</name>
  <files>src/features/receive-shipment/ui/ReceiveShipmentForm.tsx, src/features/receive-shipment/model/useReceiveShipment.ts, src/widgets/PurchaseOrderDetailPanel.tsx, src/shared/lib/i18n/locales/en-US/wAdmin.json, src/shared/lib/i18n/locales/es-MX/wAdmin.json</files>
  <read_first>
    src/features/receive-shipment/ui/ReceiveShipmentForm.tsx (full file, this task's edit target — Line type, blank(), submit(), the supplier select)
    src/features/receive-shipment/model/useReceiveShipment.ts (full file — receive.mutateAsync(request: ReceiveShipmentRequest); ReceiveShipmentRequest already carries the optional poId field added by 16-01 Task 3)
    src/widgets/PurchaseOrderDetailPanel.tsx (16-03 Task 2's version — this task adds the Receive button only to the status === 'draft' branch, alongside the existing Edit button)
    16-UI-SPEC.md "Receive flow (thin wrapper around existing receive-shipment UX)" section (exact behavior: pre-filled supplier+lines, still editable quantities/costs/expiry, poId carried through, no second status-update call)
  </read_first>
  <action>
    In `ReceiveShipmentForm.tsx`, extend the component's props with `initialPurchaseOrder?: { id: string; supplierId: string; items: { productId: string; productName: string; quantity: number; costPrice: number }[] } | null` (default via `= null` in the destructured param). Seed `supplierId`/`lines` state lazily from it when present: `useState(() => initialPurchaseOrder?.supplierId ?? '')` and `useState<Line[]>(() => initialPurchaseOrder ? initialPurchaseOrder.items.map(i => ({ productId: i.productId, search: i.productName, quantity: i.quantity, costPrice: i.costPrice, expiryDate: null })) : [])` (expiry stays manager-fillable even when pre-filled — a PO never carries expiry per Wave 3's line-item shape, so this is the one field the manager still adds at receive time, matching how real deliveries can differ slightly from what was ordered). When `initialPurchaseOrder` is set, render the supplier `<select>` as `disabled` (the PO's supplier is fixed; 16-01's `PO_SUPPLIER_MISMATCH` guard would reject a changed supplier server-side anyway — disable it client-side to avoid a confusing failed submission). In `submit()`, include `poId: initialPurchaseOrder?.id ?? undefined` in the object passed to `receive.mutateAsync({...})` (no `useReceiveShipment.ts` signature change needed for this — it already forwards whatever `ReceiveShipmentRequest` shape it receives, and `poId` is already part of that type per 16-01 Task 3). On a `PO_ALREADY_RECEIVED` error code from the result, show the `purchaseOrderDetailPanel.alreadyReceivedError` message (via `toast.error(...)`, reading the ns explicitly since this component's own namespace is `featMgmt`) instead of the generic `receiveShipment.error` copy; any other error code keeps the existing generic message unchanged.

    In `useReceiveShipment.ts`, add `void queryClient.invalidateQueries({ queryKey: purchaseOrderKeys.all })` alongside the existing `inventoryKeys.all`/`inventoryKeys.log()` invalidations inside the `onSuccess` callback (import `purchaseOrderKeys` from `@entities/purchase-order`) — so a PO's detail view refetches and immediately shows `received` after a successful receive, with no second client-side status-update call.

    In `PurchaseOrderDetailPanel.tsx`, add a `purchaseOrderDetailPanel.receive` = "Receive Shipment" `POSButton` to the `status === 'draft'` branch (alongside the existing "Edit" button from Wave 3), opening `ReceiveShipmentForm` in a `Dialog` with `initialPurchaseOrder={{ id: po.id, supplierId: po.supplierId, items: po.items?.map(i => ({ productId: i.productId, productName: i.product?.name ?? '', quantity: i.quantity, costPrice: i.costPrice })) ?? [] }}`.

    Add to `en-US/wAdmin.json`/`es-MX/wAdmin.json`: `purchaseOrderDetailPanel.receive` = "Receive Shipment" / "Recibir mercancía"; `purchaseOrderDetailPanel.alreadyReceivedError` = "This purchase order has already been received." / "Esta orden de compra ya fue recibida."
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` and `npm run typecheck` both pass clean.
    - `grep -n "initialPurchaseOrder" src/features/receive-shipment/ui/ReceiveShipmentForm.tsx` shows the new prop.
    - `grep -n "purchaseOrderKeys" src/features/receive-shipment/model/useReceiveShipment.ts` shows the added invalidation.
  </acceptance_criteria>
  <done>A draft PO's detail view has a working Receive Shipment action that pre-fills the existing, unmodified receive form with the PO's supplier/lines, carries poId through to the already-hardened receive_shipment RPC, and the detail view reflects "received" immediately after a successful receive — with no second form and no second status-update call.</done>
</task>

<task type="auto">
  <name>Task 2: e2e/56-purchase-orders.spec.ts — create, auto-draft, receive-in-full, RBAC (ROADMAP Phase 16 success criteria 1-4)</name>
  <files>e2e/56-purchase-orders.spec.ts</files>
  <read_first>
    e2e/53-supplier-receiving.spec.ts (full file — createSupplier/openQuickAdd/service-client fixture pattern to mirror)
    e2e/22-staff-management.spec.ts lines 1-60, 140-210 (getAccessToken(page) + raw fetch REST/Edge-Function pattern for the RBAC/RLS assertion — this file's own comment documents this as a per-file-duplicated helper convention, mirror it locally)
    e2e/09-rbac.spec.ts lines 300-333 (T-RP-03/T-RP-04 route-redirect + zero-rows assertion pattern to mirror for the cashier case)
    e2e/helpers/auth.ts (loginAs, gotoAuthed)
    e2e/helpers/supabase.ts (getServiceClient, resetTestState)
  </read_first>
  <action>
    Write `e2e/56-purchase-orders.spec.ts` with `requireIntegrationEnv()`/`resetTestState()` in `beforeEach` (mirrors every other integration spec in this suite). Seed via `getServiceClient()`: a supplier, a product linked to it via `supplier_products`, and an `inventory` row with `quantity_on_hand` below `low_stock_threshold` (for the auto-draft case).

    Test A ("manager creates a purchase order manually", PO-01): `loginAs(page, 'manager')`, navigate to `/purchase-orders`, click "New Purchase Order", select the seeded supplier, add a line manually (product search + quantity + cost), click "Save Draft", assert the new row appears in the list with Draft status.

    Test B ("Suggest reorder from low stock pre-fills and is editable before save", PO-02): same manager session, open a new PO for the seeded supplier, click "Suggest reorder from low stock", assert the seeded low-stock product appears as a pre-filled line with the expected computed quantity (`lowStockThreshold - quantityOnHand`, D-07 — the seeded test product has no `unitsPerPackage`, so no rounding applies), edit the quantity, click Save Draft, assert the row is saved.

    Test C ("Receive Shipment updates stock and closes the PO", PO-03): seed a fresh draft PO directly via the service client (independent of Tests A/B) with one line item for the seeded product, log in as manager, open its detail view, click "Receive Shipment", confirm the pre-filled form, submit, then assert via `db.from('purchase_orders').select('status').eq('id', poId).single()` that `status === 'received'`, and via `db.from('inventory').select('quantity_on_hand').eq('product_id', productId).single()` that the quantity increased by the received amount.

    Test D ("cashier cannot see or reach Purchase Orders", ROADMAP success criterion 4 / D-02): `loginAs(page, 'cashier')`, `page.goto('/purchase-orders')`, assert redirect to `/home` (mirrors `T-RP-03`'s `page.waitForURL(/\/home/)` pattern). Then, still as the cashier, use a locally-duplicated `getAccessToken(page)` helper (mirrors `22-staff-management.spec.ts`'s own documented per-file-helper convention) to issue a raw `fetch(`${supabaseUrl}/rest/v1/purchase_orders`, { headers: { apikey: anonKey, Authorization: `Bearer ${token}` } })` and assert the parsed JSON array has length `0` — proving the actual RLS boundary, not only the client route guard.
  </action>
  <verify>
    <automated>npx playwright test e2e/56-purchase-orders.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 4 tests (A-D) pass.
    - `grep -n "rest/v1/purchase_orders" e2e/56-purchase-orders.spec.ts` confirms Test D exercises the raw REST endpoint (RLS), not only the UI redirect.
  </acceptance_criteria>
  <done>ROADMAP Phase 16's four success criteria are each proven by a green, headless Playwright test: manual create, auto-draft-then-edit, receive-in-full (stock+status), and cashier exclusion from both the route and the underlying RLS-protected data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Manager-authenticated client -> ReceiveShipmentForm(poId) -> receive_shipment RPC | No new boundary — reuses 16-01's already-hardened, already-threat-modeled RPC unchanged; this plan only adds a client-side pre-fill |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-07 | Repudiation | e2e Test D's RLS assertion | low | mitigate | Test D exercises the real REST endpoint with a cashier's genuine access token, not just the UI redirect — closes the gap RESEARCH.md's Pitfall 2 specifically warned an all-UI test suite would miss |
</threat_model>

<verification>
`npm run lint`, `npm run typecheck` pass clean. `npx playwright test e2e/56-purchase-orders.spec.ts` passes all 4 tests, headless, against the local dev server and self-hosted Supabase stack.
</verification>

<success_criteria>
All four ROADMAP Phase 16 success criteria are proven true end-to-end by automated, headless evidence: manual PO creation, one-click low-stock auto-draft with pre-save editing, full receiving that atomically updates stock and closes the PO via the existing receive_shipment RPC, and cashier exclusion from both the route and the underlying RLS-protected data.
</success_criteria>

<output>
Create `.planning/phases/16-purchase-orders-reordering/16-04-SUMMARY.md` when done.
</output>
