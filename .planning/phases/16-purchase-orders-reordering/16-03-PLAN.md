---
phase: 16-purchase-orders-reordering
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - src/features/create-purchase-order/ui/PurchaseOrderForm.tsx
  - src/features/create-purchase-order/index.ts
  - src/widgets/PurchaseOrderListPanel.tsx
  - src/widgets/PurchaseOrderDetailPanel.tsx
  - src/app/purchase-orders-route.tsx
  - src/app/router.tsx
  - src/widgets/HomeDashboard/ui/HomeDashboard.tsx
  - src/pages/purchase-orders/index.tsx
  - src/shared/lib/i18n/locales/en-US/featMgmt.json
  - src/shared/lib/i18n/locales/es-MX/featMgmt.json
  - src/shared/lib/i18n/locales/en-US/wAdmin.json
  - src/shared/lib/i18n/locales/es-MX/wAdmin.json
  - src/shared/lib/i18n/locales/en-US/common.json
  - src/shared/lib/i18n/locales/es-MX/common.json
  - src/shared/lib/i18n/locales/en-US/pages.json
  - src/shared/lib/i18n/locales/es-MX/pages.json
  - src/shared/lib/i18n/locales/en-US/wPanels.json
  - src/shared/lib/i18n/locales/es-MX/wPanels.json
autonomous: true
requirements: [PO-01, PO-02]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Save Draft is disabled whenever lines.length === 0 — a purchase order with zero line items can never be saved (per 16-UI-SPEC.md's disabled-submit guard)."
    - "The Save Draft submit control is disabled while its own create/update mutation is in-flight, preventing a double-click from creating two purchase orders."
    - "Two or more line items referencing the same product_id are allowed and are never merged or deduplicated — each stays its own array entry through create, edit, and save."
    - statement: "Line items are always displayed and submitted in insertion order (the order they were added), never re-sorted by name, cost, or any other key."
      verification: backstop
    - "When the low-stock suggestion query returns zero rows for the chosen supplier, the form shows the noReorderNeeded inline message instead of pre-filling any lines — manual line-add remains available."
    - "The PO list (no purchase orders at all) shows an EmptyState with a 'New Purchase Order' action, not a blank page."
    - "Line items in the create/edit form show EmptyState guidance (add manually, or suggest from low stock) when empty."
    - "The PO list and PO detail view both show skeleton loading rows while their underlying query is in flight."
    - "A save-draft failure shows an inline destructive-text error; the manager's typed lines are not discarded from the form."
    - "A PO list with many rows uses DataTable's existing pagination/scroll behavior — no phase-specific overflow handling was written for this."
    - "Long supplier names and many line items truncate/scroll rather than breaking layout, reusing the truncate + max-h-72 overflow-y-auto conventions already used elsewhere in this codebase."
  artifacts:
    - src/features/create-purchase-order/ui/PurchaseOrderForm.tsx
    - src/widgets/PurchaseOrderListPanel.tsx
    - src/widgets/PurchaseOrderDetailPanel.tsx
    - src/app/purchase-orders-route.tsx (PurchaseOrdersRoute)
    - src/pages/purchase-orders/index.tsx (route /purchase-orders)
    - HomeDashboard.tsx new tile (manage_products-gated, path /purchase-orders)
  key_links:
    - "PurchaseOrderListPanel 'New Purchase Order' -> PurchaseOrderForm (create mode) -> useMutationCreatePurchaseOrder"
    - "PurchaseOrderListPanel row click -> PurchaseOrderDetailPanel -> (draft) Edit -> PurchaseOrderForm (edit mode) -> useMutationUpdatePurchaseOrder"
    - "/home dashboard tile -> PurchaseOrdersRoute -> /purchase-orders -> PurchaseOrdersPage -> PurchaseOrderListPanel"
  prohibitions:
    - "This phase's create/edit flow never auto-submits a suggested draft — 'Suggest reorder from low stock' only replaces the in-form line list on an explicit manager click, and nothing is persisted until Save Draft is clicked (no auto-PO generation, per PROJECT.md's Out of Scope table)."
    - "The purchase order lifecycle exposed by this UI has exactly two states (draft, received) — no intermediate 'submitted'/'ordered'/approval-pending status or approval-gate control exists anywhere in PurchaseOrderForm/ListPanel/DetailPanel (no multi-level approval workflow, per PROJECT.md's Out of Scope table)."
---

<objective>
Build the manager-facing UI for PO-01 (manual create) and PO-02 (auto-draft-then-edit): a dual create/edit form with the "Suggest reorder from low stock" wiring, a searchable list, a detail view, and the route/nav wiring that makes `/purchase-orders` reachable — all per 16-UI-SPEC.md, which is APPROVED across all 6 dimensions. Receiving (PO-03) is deliberately out of scope for this plan — it lands in Wave 4 once this UI exists to attach the Receive Shipment action to.

Purpose: Deliver the full create/auto-draft/edit/browse experience, reusing every component this codebase already has (Dialog, DataTable, ConfirmDialog, EmptyState, MoneyInput, MoneyDisplay, POSButton, FormField) — no new shadcn components, per 16-UI-SPEC.md's Design System section.
Output: PurchaseOrderForm, PurchaseOrderListPanel, PurchaseOrderDetailPanel, PurchaseOrdersRoute, the /purchase-orders route + HomeDashboard tile.
</objective>

## Artifacts this phase produces (this plan's slice)

- `src/features/create-purchase-order/ui/PurchaseOrderForm.tsx` — dual create/edit, line-item grid, Suggest-reorder wiring
- `src/widgets/PurchaseOrderListPanel.tsx` — DataTable list, create dialog, delete (draft only)
- `src/widgets/PurchaseOrderDetailPanel.tsx` — read-only line items, Edit action for drafts
- `src/app/purchase-orders-route.tsx::PurchaseOrdersRoute` (mirrors `ReportsRoute`, gated `manage_products`)
- `src/pages/purchase-orders/index.tsx` — new route `/purchase-orders`
- `HomeDashboard.tsx` new tile (`FileText` icon, `requiredAction: 'manage_products'`, placed after `/suppliers`)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-purchase-orders-reordering/16-UI-SPEC.md
@.planning/phases/16-purchase-orders-reordering/16-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PurchaseOrderForm — create + edit, line-item grid, Suggest-reorder wiring (D-05/D-06 cost default)</name>
  <files>src/features/create-purchase-order/ui/PurchaseOrderForm.tsx, src/features/create-purchase-order/index.ts, src/shared/lib/i18n/locales/en-US/featMgmt.json, src/shared/lib/i18n/locales/es-MX/featMgmt.json</files>
  <read_first>
    src/features/receive-shipment/ui/ReceiveShipmentForm.tsx (full file — the line-item grid, EmptyState, product-search-match logic, and disabled-submit-guard pattern to mirror; this form drops the expiry column entirely)
    src/features/manage-suppliers/ui/SupplierForm.tsx (the dual create/edit submit dispatch pattern: `initialX ? onSubmitUpdate(...) : onSubmitCreate(...)`, and the `submitting` prop convention)
    src/entities/purchase-order (Wave 2 — useMutationCreatePurchaseOrder/useMutationUpdatePurchaseOrder, PurchaseOrderCreate/PurchaseOrderItemCreate types)
    src/features/suggest-reorder (Wave 2 — useSuggestReorder)
    src/entities/inventory (useInventory — for the D-05/D-06 cost-by-productId lookup)
    16-UI-SPEC.md "Create / auto-draft flow" and "Copywriting Contract" sections (full interaction spec + exact copy strings)
    16-PATTERNS.md "PurchaseOrderForm.tsx" section (the gap-4/pb-4 spacing divergence from ReceiveShipmentForm's legacy gap-2/pb-3 — do not copy the old spacing values)
  </read_first>
  <action>
    Create `PurchaseOrderForm.tsx` accepting props `{ initialPurchaseOrder: PurchaseOrder | null; submitting: boolean; onCancel: () => void; onSubmitCreate: (value: PurchaseOrderCreate) => void; onSubmitUpdate: (value: { id: string; supplierId: string; items: PurchaseOrderItemCreate[] }) => void }` (mirrors `SupplierForm`'s `initialSupplier`/dual-dispatch shape). Internal `Line` type: `{ productId: string; search: string; quantity: number; costPrice: number }` (identical to `ReceiveShipmentForm`'s `Line` minus `expiryDate` — per UI-SPEC's "No expiry field on PO line items" rule, since expiry is only meaningful at receive time). Seed `supplierId` from `initialPurchaseOrder?.supplierId ?? ''` and `lines` from `initialPurchaseOrder?.items?.map(i => ({ productId: i.productId, search: i.product?.name ?? '', quantity: i.quantity, costPrice: i.costPrice })) ?? []`. Reuse `useSuppliers()` and `useProductsForManagement()` exactly as `ReceiveShipmentForm` does, plus `useInventory()` for the D-05/D-06 cost lookup: when a line's product-search `<Input>` resolves to a matched product (same `products?.find(p => p.name.toLowerCase() === search.toLowerCase() || p.barcode === search || p.sku === search)` match logic as `ReceiveShipmentForm`), also set that line's `costPrice` from `inventory?.find(i => i.productId === match.id)?.costPrice ?? 0` (D-05/D-06 — 0 when the product has never been received, no blocking validation).

    Line-item grid: `grid grid-cols-1 gap-4 pb-4 md:grid-cols-5 border-b` per row (UI-SPEC's spacing divergence — NOT the `gap-2`/`pb-3` values `ReceiveShipmentForm` uses) — product search `Input`, quantity number `Input`, `MoneyInput` cost, remove `POSButton` (no expiry column). Reuse the existing `featMgmt:receiveShipment.supplier`/`.product`/`.quantity`/`.cost`/`.addLine`/`.addProduct` i18n keys verbatim for these generic field labels — do not duplicate them under a new `purchaseOrderForm.*` key, since the semantics are identical. Add a "Suggest reorder from low stock" `POSButton` (`variant="outline"`, `disabled={!supplierId || lines.length > 0}` — UI-SPEC's rule that it only works on an empty line list, to avoid a silent overwrite) that, on click, replaces `lines` wholesale with `useSuggestReorder(supplierId)`'s current `data`, mapped to `{ productId: s.productId, search: s.productName, quantity: s.quantity, costPrice: s.costPrice }` — a single confirmable click, never automatic or triggered without the manager clicking it. When `useSuggestReorder`'s `data` is an empty array, `supplierId` is set, and `lines.length === 0`, render `t('purchaseOrderForm.noReorderNeeded')` inline instead of the empty-state illustration. Otherwise, the empty-lines state reuses `EmptyState` with `Package` icon and `t('purchaseOrderForm.emptyTitle')`/`t('purchaseOrderForm.emptyBody')` (mirrors `ReceiveShipmentForm`'s empty state exactly).

    Submit `POSButton`: label `t('purchaseOrderForm.save')` = "Save Draft", `disabled={submitting || !supplierId || lines.length === 0 || lines.some(line => !line.productId || line.quantity <= 0)}`. On click, dispatch `initialPurchaseOrder ? onSubmitUpdate({ id: initialPurchaseOrder.id, supplierId, items: lines.map(({ productId, quantity, costPrice }) => ({ productId, quantity, costPrice })) }) : onSubmitCreate({ supplierId, items: lines.map(({ productId, quantity, costPrice }) => ({ productId, quantity, costPrice })) })` — array order is never re-sorted, and multiple lines sharing the same `productId` are never merged (each stays its own entry, mirroring how `ReceiveShipmentForm` never deduplicates either). On error, render `<p className="text-sm text-destructive">{t('purchaseOrderForm.saveError')}</p>` without clearing `lines`/`supplierId` state (mirrors `ReceiveShipmentForm`'s `error` state, which likewise leaves the form populated on failure). On create success, `toast.success(t('purchaseOrderForm.created'))` and call `onCancel()` — update's own success toast/close is the caller's responsibility (mirrors `SupplierForm`/`SupplierListPanel`'s existing split, where the list widget's `onSuccess` handles the update-success toast, not the form itself).

    Add to `en-US/featMgmt.json` and `es-MX/featMgmt.json`: `purchaseOrderForm.save` = "Save Draft" / "Guardar borrador"; `purchaseOrderForm.suggestReorder` = "Suggest reorder from low stock" / "Sugerir reorden por bajo stock"; `purchaseOrderForm.emptyTitle` = "No line items yet" / "Aún no hay partidas"; `purchaseOrderForm.emptyBody` = "Add a product manually, or suggest items from the low-stock list for this supplier." / "Agrega un producto manualmente o sugiere artículos de la lista de bajo stock para este proveedor."; `purchaseOrderForm.noReorderNeeded` = "This supplier has no products currently at or below their reorder point." / "Este proveedor no tiene productos actualmente en o por debajo de su punto de reorden."; `purchaseOrderForm.saveError` = "Couldn't save this purchase order. Check the line items and try again." / "No se pudo guardar esta orden de compra. Verifica las partidas e intenta de nuevo."; `purchaseOrderForm.created` = "Purchase order saved" / "Orden de compra guardada".
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` passes with 0 warnings (i18next/no-literal-string clean — every new string routes through `t(...)`).
    - `npm run typecheck` passes clean.
    - `grep -n "purchaseOrderForm" src/shared/lib/i18n/locales/en-US/featMgmt.json src/shared/lib/i18n/locales/es-MX/featMgmt.json` shows entries in both files.
  </acceptance_criteria>
  <done>PurchaseOrderForm supports both create and edit, the Suggest-reorder button replaces an empty line list from Wave 2's supplier-scoped formula in one confirmable click, cost defaults from inventory.costPrice, and Save Draft is guarded against empty/in-flight/incomplete submissions.</done>
</task>

<task type="auto">
  <name>Task 2: PurchaseOrderListPanel (DataTable) + PurchaseOrderDetailPanel (view/edit a draft, read-only when received)</name>
  <files>src/widgets/PurchaseOrderListPanel.tsx, src/widgets/PurchaseOrderDetailPanel.tsx, src/shared/lib/i18n/locales/en-US/wAdmin.json, src/shared/lib/i18n/locales/es-MX/wAdmin.json, src/shared/lib/i18n/locales/en-US/common.json, src/shared/lib/i18n/locales/es-MX/common.json</files>
  <read_first>
    src/widgets/SupplierListPanel.tsx (full file — the dialog/create/delete/ConfirmDialog composition to mirror; swap the `<ul>` for `DataTable` per UI-SPEC's List Contract, and note the "New Supplier"/delete button lives inside the widget, not the page)
    src/shared/ui/DataTable.tsx lines 1-70 (ColumnDef/DataTableProps shape, `onRowClick`/`searchable`/`emptyState` props)
    src/shared/ui/MoneyDisplay.tsx (amount prop — used for the Total cost column)
    this plan's Task 1 (PurchaseOrderForm) and Wave 2 (StatusBadge po_draft/po_received, usePurchaseOrders/usePurchaseOrder/useMutationDeletePurchaseOrder)
    16-UI-SPEC.md "List Contract" section (exact column list, the icon-only delete button's mandatory aria-label, empty/loading/populated/overflow states)
  </read_first>
  <action>
    `PurchaseOrderListPanel.tsx`: `usePurchaseOrders()` for data, `useMutationCreatePurchaseOrder`/`useMutationDeletePurchaseOrder` for actions, local state `createOpen`/`selectedId`/`deleteId`. Loading: 4x `TableRowSkeleton columns={5}` (mirrors `SupplierListPanel`'s loading branch). Render a "New Purchase Order" `POSButton` above the table (opens `createOpen`), then `<DataTable columns={columns} data={purchaseOrders ?? []} isLoading={isLoading} searchable searchPlaceholder={t('purchaseOrderListPanel.search')} onRowClick={po => setSelectedId(po.id)} emptyState={<EmptyState icon={FileText} title={t('purchaseOrderListPanel.emptyTitle')} description={t('purchaseOrderListPanel.emptyBody')} action={{ label: t('purchaseOrderListPanel.newPurchaseOrder'), onClick: () => setCreateOpen(true) }} />} />`. Define `columns: ColumnDef<PurchaseOrder>[]` with 5 entries: supplier name (`accessorFn: po => po.supplier?.name ?? ''`, cell wraps `<span className="truncate font-medium">`); status (`cell: ({ row }) => <StatusBadge status={row.original.status === 'received' ? 'po_received' : 'po_draft'} />`); line count (`accessorFn: po => po.items?.length ?? 0`); total cost (`cell` computing `po.items?.reduce((sum, i) => sum + i.quantity * i.costPrice, 0) ?? 0` through `<MoneyDisplay amount={...} />`); created date (`accessorFn: po => new Date(po.createdAt).toLocaleDateString()`); and a final actions column rendering a trash-icon `POSButton variant="destructive" size="icon" aria-label={t('purchaseOrderListPanel.delete')} onClick={e => { e.stopPropagation(); setDeleteId(po.id); }}` ONLY when `row.original.status === 'draft'` (received rows get no delete control). "New Purchase Order" opens a `Dialog` containing `<PurchaseOrderForm initialPurchaseOrder={null} submitting={create.isPending} onCancel={() => setCreateOpen(false)} onSubmitCreate={value => void create.mutateAsync(value)} onSubmitUpdate={() => undefined} />` (the create dialog never invokes `onSubmitUpdate`, pass a no-op). `ConfirmDialog` for delete mirrors `SupplierListPanel`'s exactly, with `purchaseOrderListPanel.deletePurchaseOrderTitle`/`.deletePurchaseOrderBody`/`.delete` copy. When `selectedId` is set, render `<Dialog open onOpenChange={() => setSelectedId(null)}><PurchaseOrderDetailPanel purchaseOrderId={selectedId} onClose={() => setSelectedId(null)} /></Dialog>`.

    `PurchaseOrderDetailPanel.tsx`: `usePurchaseOrder(purchaseOrderId)`, loading guard (skeleton rows), then a header (`supplier?.name`, `<StatusBadge status={po.status === 'received' ? 'po_received' : 'po_draft'} />`, created date), a read-only line-item list inside a `max-h-72 overflow-y-auto` scroll container (mirrors `ReceiveShipmentForm`'s scroll-cap convention) — product name (`truncate`), quantity, `<MoneyDisplay amount={item.costPrice} />` per row. When `po.status === 'draft'`, render an "Edit" `POSButton` that opens `PurchaseOrderForm` (via a nested `Dialog` + local `editing` boolean) with `initialPurchaseOrder={po}`, wired `onSubmitUpdate={value => void update.mutateAsync(value, { onSuccess: r => { if (r.ok) { toast.success(t('purchaseOrderForm.created', { ns: 'featMgmt' })); setEditing(false); } else toast.error(r.error.message); } })}`. When `po.status === 'received'`, render no action buttons at all — fully read-only (the Receive Shipment action is added in Wave 4, Task 1, not here).

    Add to `en-US/wAdmin.json`/`es-MX/wAdmin.json`: `purchaseOrderListPanel.newPurchaseOrder` = "New Purchase Order" / "Nueva orden de compra"; `.emptyTitle` = "No purchase orders yet" / "Aún no hay órdenes de compra"; `.emptyBody` = "Create a purchase order manually, or generate one from your low-stock list." / "Crea una orden de compra manualmente o genera una desde tu lista de bajo stock."; `.delete` = "Delete" / "Eliminar"; `.deletePurchaseOrderTitle` = "Delete this purchase order?" / "¿Eliminar esta orden de compra?"; `.deletePurchaseOrderBody` = "This draft will be permanently removed. This cannot be undone." / "Este borrador se eliminará permanentemente. Esta acción no se puede deshacer."; `.search` = "Search purchase orders" / "Buscar órdenes de compra". Add to `en-US/common.json`/`es-MX/common.json`: `statusBadge.poDraft` = "Draft" / "Borrador"; `statusBadge.poReceived` = "Received" / "Recibido".
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` and `npm run typecheck` both pass clean.
    - `grep -n "aria-label={t('purchaseOrderListPanel.delete')}" src/widgets/PurchaseOrderListPanel.tsx` confirms the icon-only delete button carries an accessible label.
    - `grep -c "po_draft\|po_received" src/widgets/PurchaseOrderListPanel.tsx src/widgets/PurchaseOrderDetailPanel.tsx` shows both files reference the StatusBadge keys added in Wave 2.
  </acceptance_criteria>
  <done>A manager can browse all purchase orders in a searchable table, open any row to view its line items, edit a draft's supplier/lines, and delete a draft — a received PO shows read-only with no edit/delete affordance.</done>
</task>

<task type="auto">
  <name>Task 3: PurchaseOrdersRoute guard + /purchase-orders route + HomeDashboard tile + page composition</name>
  <files>src/app/purchase-orders-route.tsx, src/app/router.tsx, src/widgets/HomeDashboard/ui/HomeDashboard.tsx, src/pages/purchase-orders/index.tsx, src/shared/lib/i18n/locales/en-US/pages.json, src/shared/lib/i18n/locales/es-MX/pages.json, src/shared/lib/i18n/locales/en-US/wPanels.json, src/shared/lib/i18n/locales/es-MX/wPanels.json</files>
  <read_first>
    src/app/reports-route.tsx (full 15-line file, the exact verbatim shape to copy for `PurchaseOrdersRoute`)
    src/app/router.tsx (full file — the lazy-import + `<Route>` nesting pattern for `/reports` (`ProtectedRoute` outer, `ReportsRoute` inner) to mirror exactly)
    src/pages/suppliers/index.tsx (full file — the `PageContainer`+`backTo="/home"` composition shape; this page needs no `actions` prop since "New Purchase Order" already lives inside `PurchaseOrderListPanel`)
    src/widgets/HomeDashboard/ui/HomeDashboard.tsx lines 1-88 (`ITEMS` array + lucide-react icon imports — insert the new tile immediately after the `/suppliers` entry)
    16-UI-SPEC.md "Page Placement Decision" section (icon rationale: `FileText`, not `ClipboardList`/`Package` — both already used elsewhere in this file for `/audit` and `/inventory`)
  </read_first>
  <action>
    Create `src/app/purchase-orders-route.tsx` — copy `reports-route.tsx` verbatim, renaming the component `PurchaseOrdersRoute` and the permission check to `can('manage_products')`. In `router.tsx`, add `const PurchaseOrdersPage = lazy(() => import('../pages/purchase-orders'));` alongside the existing lazy imports, and a new route entry `<Route path="/purchase-orders" element={<ProtectedRoute><PurchaseOrdersRoute><PurchaseOrdersPage /></PurchaseOrdersRoute></ProtectedRoute>} />` (mirrors the `/reports` entry's exact `ProtectedRoute` outer / feature-route-guard inner nesting). In `HomeDashboard.tsx`, add `FileText` to the `lucide-react` import list, and insert a new `ITEMS` entry immediately after the `/suppliers` object: `{ path: '/purchase-orders', labelKey: 'homeDashboard.tiles.purchaseOrders', icon: FileText, requiredAction: 'manage_products', managerLabelKey: 'homeDashboard.managerLabels.manager' }`. Create `src/pages/purchase-orders/index.tsx` mirroring `src/pages/suppliers/index.tsx`'s shape but with no `actions` prop: `export default function PurchaseOrdersPage() { const { t } = useTranslation('pages'); return (<PageContainer title={t('purchaseOrders.title')} backTo="/home"><PurchaseOrderListPanel /></PageContainer>); }`.

    Add to `en-US/pages.json`/`es-MX/pages.json`: `purchaseOrders.title` = "Purchase Orders" / "Órdenes de compra". Add to `en-US/wPanels.json`/`es-MX/wPanels.json`: `homeDashboard.tiles.purchaseOrders` = "Purchase Orders" / "Órdenes de compra" (reuse the existing `homeDashboard.managerLabels.manager` key verbatim — no new key needed for that label).
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds (proves the new lazy-loaded route/page compiles and bundles cleanly).
    - `grep -n 'path="/purchase-orders"' src/app/router.tsx` shows the new route wrapped in both `ProtectedRoute` and `PurchaseOrdersRoute`.
    - `grep -n "requiredAction: 'manage_products'" src/widgets/HomeDashboard/ui/HomeDashboard.tsx` shows the new tile's gate.
  </acceptance_criteria>
  <done>/purchase-orders is reachable from the Home dashboard (a manager+ only tile) and directly via URL, guarded by PurchaseOrdersRoute, rendering the full list/create/edit/delete flow built in Tasks 1-2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `/purchase-orders` route -> PurchaseOrdersRoute | Client-side UX gate only — the real access-control boundary is 16-01's RLS, already proven; this route guard prevents a cashier from ever seeing the page render, but is not itself the security control |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-06 | Elevation of Privilege | PurchaseOrdersRoute (client-side only) | low | accept | This route guard is UX-only by design (mirrors ReportsRoute); the actual enforcement is 16-01's RLS, already proven — a cashier who bypassed this guard via direct API calls would still get zero rows back, per T-16-01 |
</threat_model>

<verification>
`npm run lint`, `npm run typecheck`, and `npm run build` all pass clean. The full create -> suggest-reorder -> edit -> browse -> delete flow is reachable and functional in the running dev server for a manager, and unreachable for a cashier (client-side redirect).
</verification>

<success_criteria>
A manager can create a purchase order manually or via one-click low-stock suggestion, edit line items before saving, browse all purchase orders in a searchable table, and open any draft to edit or (in Wave 4) receive it — all reachable from the Home dashboard.
</success_criteria>

<output>
Create `.planning/phases/16-purchase-orders-reordering/16-03-SUMMARY.md` when done.
</output>
