---
phase: 16-purchase-orders-reordering
plan: 02
type: tdd
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/shared/lib/domain.ts
  - src/entities/purchase-order/model/reorder-quantity.ts
  - src/entities/purchase-order/model/reorder-quantity.test.ts
  - src/entities/purchase-order/model/types.ts
  - src/entities/purchase-order/model/queries.ts
  - src/entities/purchase-order/index.ts
  - src/shared/ui/StatusBadge.tsx
  - src/features/suggest-reorder/model/useSuggestReorder.ts
  - src/features/suggest-reorder/index.ts
autonomous: true
requirements: [PO-01, PO-02]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "computeReorderQuantity(quantityOnHand, lowStockThreshold, unitsPerPackage) floors at 0 when quantityOnHand >= lowStockThreshold (D-07), and rounds a positive result up to the nearest whole multiple of unitsPerPackage when set (D-08), falling back to the raw value when unitsPerPackage is null or non-positive — a product exactly at its reorder point computes a suggested quantity of 0 and is excluded from suggested line items."
    - statement: "useSuggestReorder's result list is always ordered by product name ascending (deterministic), driven by the same .order('product(name)') clause useInventory already uses elsewhere in this codebase — repeated 'Suggest reorder' clicks against an unchanged low-stock list produce identical line order."
      verification: backstop
    - statement: "useSuggestReorder returns an empty array (not an error, not undefined) when the chosen supplier has zero products at or below their reorder point."
      verification: backstop
    - statement: "Two managers concurrently creating purchase orders (for the same or different suppliers) never block or conflict — useMutationCreatePurchaseOrder is a plain RLS-gated insert (D-03) with no cross-table atomicity requirement, unlike receive_shipment."
      verification: backstop
    - "Every supabase.from(...) call added in this plan's files is wrapped in supabaseQuery/supabaseMutation (src/shared/lib/result.ts), matching entities/supplier/model/queries.ts's Result<T> pattern exactly — no unwrapped Supabase call."
  artifacts:
    - src/shared/lib/domain.ts::PurchaseOrderSchema, PurchaseOrderItemSchema, PurchaseOrderStatusSchema, PurchaseOrderCreateSchema
    - src/entities/purchase-order/model/reorder-quantity.ts::computeReorderQuantity
    - src/entities/purchase-order/model/queries.ts::purchaseOrderKeys, usePurchaseOrders, usePurchaseOrder, useMutationCreatePurchaseOrder, useMutationUpdatePurchaseOrder, useMutationDeletePurchaseOrder
    - src/entities/purchase-order/index.ts (barrel)
    - src/features/suggest-reorder/model/useSuggestReorder.ts
    - src/shared/ui/StatusBadge.tsx (po_draft/po_received statusConfig keys)
  key_links:
    - "computeReorderQuantity -> useSuggestReorder -> (Wave 3) PurchaseOrderForm's 'Suggest reorder' button"
    - "useMutationCreatePurchaseOrder -> purchase_orders insert -> purchase_order_items insert (two-step, mirrors syncSupplierProducts)"
---

<objective>
Build the entity/domain layer Purchase Orders needs: `PurchaseOrder`/`PurchaseOrderItem` Zod schemas, CRUD query hooks mirroring `entities/supplier`'s proven shape, and the two pieces PO-02's auto-draft depends on — the D-07/D-08 reorder-quantity formula (TDD, pure function, no analog in this codebase) and the supplier-scoped low-stock suggestion query (RESEARCH.md Pitfall 3: the existing store-wide `lowStockAlerts` list is insufficient on its own, it must be joined through `supplier_products`).

Purpose: Give Wave 3's UI a complete, tested data layer to consume — no business logic lives in the form components.
Output: Domain schemas, `entities/purchase-order` query hooks, `computeReorderQuantity`, `useSuggestReorder`, `StatusBadge` po_draft/po_received support.
</objective>

## Artifacts this phase produces (this plan's slice)

- `src/shared/lib/domain.ts::PurchaseOrderSchema` / `PurchaseOrderItemSchema` / `PurchaseOrderStatusSchema` / `PurchaseOrderCreateSchema`
- `src/entities/purchase-order/model/reorder-quantity.ts::computeReorderQuantity(quantityOnHand, lowStockThreshold, unitsPerPackage): number`
- `src/entities/purchase-order/model/queries.ts` — `purchaseOrderKeys`, `usePurchaseOrders()`, `usePurchaseOrder(id)`, `useMutationCreatePurchaseOrder()`, `useMutationUpdatePurchaseOrder()`, `useMutationDeletePurchaseOrder()`
- `src/entities/purchase-order/index.ts` barrel
- `src/features/suggest-reorder/model/useSuggestReorder(supplierId)`
- `src/shared/ui/StatusBadge.tsx` — `po_draft` / `po_received` keys

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-purchase-orders-reordering/16-CONTEXT.md
@.planning/phases/16-purchase-orders-reordering/16-RESEARCH.md
@.planning/phases/16-purchase-orders-reordering/16-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: computeReorderQuantity — D-07 top-up + D-08 pack-size rounding (TDD)</name>
  <files>src/entities/purchase-order/model/reorder-quantity.ts, src/entities/purchase-order/model/reorder-quantity.test.ts</files>
  <read_first>
    16-CONTEXT.md D-07/D-08 (the locked formula: raw = max(0, lowStockThreshold - quantityOnHand); round raw up to the nearest multiple of unitsPerPackage when set and raw > 0)
    16-RESEARCH.md "No Analog Found" table (confirms this is greenfield — implement directly from spec, not copied from an existing file)
    src/shared/lib/domain.ts lines 246-247 (ProductSchema.unitsPerPackage — nullable positive int, the exact type this function's third parameter must match)
  </read_first>
  <behavior>
    - computeReorderQuantity(5, 5, null) => 0 (exactly-at-threshold; D-07's floor)
    - computeReorderQuantity(3, 10, null) => 7 (raw top-up, no rounding)
    - computeReorderQuantity(3, 10, 4) => 8 (raw 7 rounds up to the next multiple of 4)
    - computeReorderQuantity(2, 10, 4) => 8 (raw 8 is already an exact multiple of 4 — must NOT over-round to 12)
    - computeReorderQuantity(10, 5, 4) => 0 (above threshold — negative raw diff floors at 0, and 0 is never rounded up to a positive multiple)
    - property test (fast-check, arbitrary non-negative integers for quantityOnHand/lowStockThreshold and a nullable positive-int unitsPerPackage): result is always >= 0; when unitsPerPackage is a positive number and result > 0, result % unitsPerPackage === 0.
  </behavior>
  <action>
    Write the RED test file first with the 5 example cases plus one `fc.assert(fc.property(...))` boundary test using `fast-check` (already a project dependency per RESEARCH.md's Standard Stack — mirrors this project's existing property-based-test convention for billing/boundary math), confirm it fails against a not-yet-created module. Then implement `export function computeReorderQuantity(quantityOnHand: number, lowStockThreshold: number, unitsPerPackage: number | null): number` in the new file: compute `const raw = Math.max(0, lowStockThreshold - quantityOnHand);` (D-07). If `raw === 0 || !unitsPerPackage || unitsPerPackage <= 0`, return `raw` unchanged (D-08's explicit fallback — the common case, most products have no case/box link per RESEARCH.md Pitfall 4). Otherwise return `Math.ceil(raw / unitsPerPackage) * unitsPerPackage` (D-08's round-up-to-nearest-case-multiple).
  </action>
  <verify>
    <automated>npx vitest run src/entities/purchase-order/model/reorder-quantity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test passes all 5 example cases plus the property test.
    - `grep -n "export function computeReorderQuantity" src/entities/purchase-order/model/reorder-quantity.ts` confirms the exact signature `(quantityOnHand: number, lowStockThreshold: number, unitsPerPackage: number | null): number`.
  </acceptance_criteria>
  <done>computeReorderQuantity implements D-07's floor-at-0 top-up and D-08's pack-size rounding exactly, proven by example and property-based tests.</done>
</task>

<task type="auto">
  <name>Task 2: PurchaseOrder domain schemas + entities/purchase-order query hooks + StatusBadge keys</name>
  <files>src/shared/lib/domain.ts, src/entities/purchase-order/model/types.ts, src/entities/purchase-order/model/queries.ts, src/entities/purchase-order/index.ts, src/shared/ui/StatusBadge.tsx</files>
  <read_first>
    src/shared/lib/domain.ts lines 541-563 (SupplierSchema/SupplierProductSchema and their z.infer type exports — the exact insertion point, right after line 562's `SupplierProduct` type export and before the "INVENTORY LOG" section, and the exact z.object/z.infer pattern to copy)
    src/entities/supplier/model/queries.ts (full file — map/row translator pair, useSuppliers/useMutationCreateSupplier/syncSupplierProducts two-step-insert pattern to mirror exactly for purchase_orders/purchase_order_items)
    src/entities/supplier/model/types.ts and src/entities/supplier/index.ts (the re-export barrel shape to copy)
    src/shared/ui/StatusBadge.tsx lines 20-29, 38-103 (StatusBadgeProps.status union type and the statusConfig Record to extend with two new keys)
    CLAUDE.md "Missing generated types workaround" section — first confirm `grep -c "purchase_orders" src/shared/lib/supabase.types.ts` is positive (16-01 already regenerated types) before writing this task's queries.ts; import `Tables`/`TablesInsert`/`TablesUpdate` normally from `@shared/lib/supabase.types`, no `as any` cast needed
  </read_first>
  <action>
    In `src/shared/lib/domain.ts`, insert a new "PURCHASE ORDERS" section immediately after the existing `SupplierProduct` type export (line 562) and before "INVENTORY LOG" starts. Add `PurchaseOrderStatusSchema = z.enum(['draft', 'received'])`. Add `PurchaseOrderItemSchema = z.object({ id: UuidSchema, purchaseOrderId: UuidSchema, productId: UuidSchema, quantity: z.number().int().positive(), costPrice: MoneySchema, product: ProductSchema.optional() })`. Add `PurchaseOrderItemCreateSchema = PurchaseOrderItemSchema.omit({ id: true, purchaseOrderId: true, product: true })`. Add `PurchaseOrderSchema = z.object({ id: UuidSchema, supplierId: UuidSchema, status: PurchaseOrderStatusSchema, createdBy: UuidSchema, receivedAt: TimestampSchema.nullable(), createdAt: TimestampSchema, updatedAt: TimestampSchema, supplier: SupplierSchema.optional(), items: z.array(PurchaseOrderItemSchema).optional() })`. Add `PurchaseOrderCreateSchema = z.object({ supplierId: UuidSchema, items: z.array(PurchaseOrderItemCreateSchema).min(1) })`. Export the four corresponding `z.infer` types: `PurchaseOrder`, `PurchaseOrderItem`, `PurchaseOrderCreate`, `PurchaseOrderStatus`.

    Create `src/entities/purchase-order/model/types.ts` re-exporting these types and schemas from `@shared/lib/domain`, mirroring `entities/supplier/model/types.ts`'s one-file re-export shape exactly.

    Create `src/entities/purchase-order/model/queries.ts` mirroring `entities/supplier/model/queries.ts`'s structure — per D-03, every mutation here is a plain RLS-gated insert/update/delete, not a new RPC or Edge Function (unlike `receive_shipment`, PO creation/editing has no cross-table atomicity requirement since stock/cost/expiry never move at create time): `purchaseOrderKeys = { all: ['purchaseOrders'] as const, detail: (id: string) => ['purchaseOrders', id] as const }`; a `mapPO(row)`/`rowPO(input)` translator pair for `purchase_orders` (snake_case<->camelCase, following the exact `map`/`row` pattern in supplier's file) and a second `mapItem(row)` translator for `purchase_order_items` (including the nested `product:products(*)` embed when present, mirroring `InventorySchema`'s optional `product` embed pattern); `usePurchaseOrders()` — `useQuery` selecting `*, supplier:suppliers(name), purchase_order_items(quantity, cost_price)` ordered by `created_at` descending, mapped through `mapPO` with the embedded items reduced for the caller's convenience; `usePurchaseOrder(id)` — `useQuery` selecting `*, supplier:suppliers(*), purchase_order_items(*, product:products(*))` for one row (`enabled: !!id`, mirrors `useSupplierProductIds`'s enabled guard); `useMutationCreatePurchaseOrder()` — `useMutation` taking `PurchaseOrderCreate`, inserting one `purchase_orders` row via `supabaseMutation`, then inserting all `purchase_order_items` rows in a second `supabaseMutation` call (two-step insert, mirrors `syncSupplierProducts`'s delete-then-insert shape but without a delete since this is a create), invalidating `purchaseOrderKeys.all` on success; `useMutationUpdatePurchaseOrder()` — `useMutation` taking `{ id: string; supplierId: string; items: PurchaseOrderItemCreate[] }`, updating the `purchase_orders.supplier_id` row, then deleting all existing `purchase_order_items` for that PO and re-inserting the new set (mirrors `syncSupplierProducts`'s delete-then-insert pattern exactly), invalidating `purchaseOrderKeys.all` and `purchaseOrderKeys.detail(id)` on success; `useMutationDeletePurchaseOrder()` — `useMutation` deleting a `purchase_orders` row by id (child `purchase_order_items` cascade via the FK's `ON DELETE CASCADE`), invalidating `purchaseOrderKeys.all` on success. Every `supabase.from(...)` call is wrapped in `supabaseQuery`/`supabaseMutation` from `@shared/lib/result`, never called unwrapped.

    Create `src/entities/purchase-order/index.ts` re-exporting `purchaseOrderKeys`, `usePurchaseOrders`, `usePurchaseOrder`, `useMutationCreatePurchaseOrder`, `useMutationUpdatePurchaseOrder`, `useMutationDeletePurchaseOrder` from `./model/queries`, and the types from `./model/types` — mirroring `entities/supplier/index.ts`'s barrel shape.

    In `src/shared/ui/StatusBadge.tsx`, extend `StatusBadgeProps.status`'s union type with `| 'po_draft' | 'po_received'`, and add two entries to the `statusConfig` Record: `po_draft: { labelKey: 'statusBadge.poDraft', variant: 'secondary' }` and `po_received: { labelKey: 'statusBadge.poReceived', variant: 'default', className: 'bg-primary text-primary-foreground hover:opacity-90' }` (mirrors the existing `paid`/`closed` treatment per 16-UI-SPEC.md's Color section — `common.json`'s corresponding `statusBadge.poDraft`/`statusBadge.poReceived` keys are added in Wave 3 Task 2 alongside the widget that first renders them).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes clean.
    - `grep -n "PurchaseOrderSchema\|PurchaseOrderItemSchema\|PurchaseOrderStatusSchema" src/shared/lib/domain.ts` shows all three.
    - `grep -n "po_draft\|po_received" src/shared/ui/StatusBadge.tsx` shows both new statusConfig keys.
    - `grep -n "supabaseQuery\|supabaseMutation" src/entities/purchase-order/model/queries.ts` shows every `supabase.from` call routed through one of the two wrappers.
  </acceptance_criteria>
  <done>PurchaseOrder/PurchaseOrderItem domain types exist, entities/purchase-order exposes usePurchaseOrders/usePurchaseOrder/create/update/delete mutation hooks mirroring entities/supplier's pattern exactly, and StatusBadge can render po_draft/po_received.</done>
</task>

<task type="auto">
  <name>Task 3: suggest-reorder — supplier-scoped low-stock query (D-07/D-08 applied, deterministic order, cost-defaulted)</name>
  <files>src/features/suggest-reorder/model/useSuggestReorder.ts, src/features/suggest-reorder/index.ts</files>
  <read_first>
    src/entities/supplier/model/queries.ts lines 81-94 (useSupplierProductIds — the exact supplier-scoped read this task starts from, reused directly rather than re-queried)
    src/entities/inventory/model/queries.ts lines 103-134 (useInventory's `.select('*, product:products(*, category:categories(*))').order('product(name)')` embed+order pattern — already proven in this codebase, reuse the `.order('product(name)')` clause verbatim)
    16-RESEARCH.md "Pitfall 3" (why the store-wide lowStockAlerts list is insufficient on its own — must be scoped through supplier_products)
    this plan's Task 1 output, src/entities/purchase-order/model/reorder-quantity.ts::computeReorderQuantity
  </read_first>
  <action>
    Implement `export function useSuggestReorder(supplierId: string | undefined)` as a `useQuery` (mirrors `useSupplierProductIds`'s `enabled: !!supplierId` guard and query-key shape, keyed `['suggestReorder', supplierId ?? 'none']`). Inside `queryFn`: call `useSupplierProductIds`'s underlying query logic to get this supplier's product ids (either call the hook's exported query directly or inline the identical `supabase.from('supplier_products').select('product_id').eq('supplier_id', supplierId)` call — do not duplicate a second independent implementation of the same lookup). With those ids, run `supabase.from('inventory').select('product_id, quantity_on_hand, low_stock_threshold, cost_price, product:products(name, units_per_package)').in('product_id', productIds).order('product(name)')` (Pitfall 3's fix — scoped to the chosen supplier's own products, mirroring `useInventory`'s proven embed+order shape). Filter to rows where `quantity_on_hand <= low_stock_threshold`. Map each surviving row through `computeReorderQuantity(row.quantity_on_hand, row.low_stock_threshold, row.product?.units_per_package ?? null)`, and filter out any row whose computed quantity is `=== 0` (a product exactly at its reorder point contributes nothing to the suggestion). Return `{ productId: string; productName: string; quantity: number; costPrice: number }[]`, with `costPrice` taken from `row.cost_price ?? 0` (D-05/D-06's default folded in here so the Wave 3 caller needs no second lookup). The `.order('product(name)')` clause alone guarantees deterministic name-ascending order — no additional client-side sort. An empty `productIds` array or zero surviving rows both naturally resolve to `[]` — the caller (Wave 3) renders the `noReorderNeeded` message in that case.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes clean.
    - `grep -n "computeReorderQuantity" src/features/suggest-reorder/model/useSuggestReorder.ts` confirms Task 1's formula is called here, not reimplemented inline.
  </acceptance_criteria>
  <done>useSuggestReorder returns a supplier-scoped, deterministically-ordered, cost-defaulted, threshold-excluded suggestion list — ready for Wave 3's form to consume as the "Suggest reorder from low stock" action's data source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client-supplied PurchaseOrderCreate (supplierId, items) -> useMutationCreatePurchaseOrder | No new boundary — enforcement is 16-01's RLS at the DB layer; this plan's code only shapes the request |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-05 | Tampering | useMutationCreatePurchaseOrder / useMutationUpdatePurchaseOrder input | low | accept | Zod schema shape validation only at this layer; the real access-control boundary is 16-01's RLS (`FOR ALL` gated `manage_products`), already proven by 16-01 Task 2 — consistent with entities/supplier's identical division of responsibility |
</threat_model>

<verification>
`npm run typecheck` passes clean across all files this plan touches. `npx vitest run src/entities/purchase-order/model/reorder-quantity.test.ts` passes all cases.
</verification>

<success_criteria>
Wave 3's UI has a complete, tested data layer to consume: domain schemas, CRUD query hooks, the D-07/D-08 reorder formula, and the supplier-scoped suggestion query — no business logic needs to live in a form component.
</success_criteria>

<output>
Create `.planning/phases/16-purchase-orders-reordering/16-02-SUMMARY.md` when done.
</output>
