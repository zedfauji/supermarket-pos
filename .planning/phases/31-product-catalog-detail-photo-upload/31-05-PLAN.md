---
phase: 31-product-catalog-detail-photo-upload
plan: 05
type: execute
wave: 4
depends_on: ["31-01", "31-02", "31-03", "31-04"]
agent_hint: Test Automation Engineer
files_modified:
  - e2e/products/product-photo-rls.spec.ts
  - e2e/checkout/peek-window.spec.ts
  - e2e/visual/46-product-dialog-baseline.spec.ts
autonomous: true
requirements: [PCAT-04]

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "A signed-in cashier — a role without `manage_products` — is denied at the database layer when it calls the Storage upload, update, and delete endpoints for the photos bucket directly, not merely hidden from the UI (PCAT-03, PCAT-04, T-31-01)."
    - "An unauthenticated client is denied on every one of those endpoints, and the bucket exposes no publicly readable URL for a stored object (T-31-04)."
    - "Phase 18's barcode-scan peek window still renders and functions unchanged for a product whose legacy image URL is set, and it is deliberately not routed through this phase's resolver — it reads the legacy field directly (PCAT-04, 31-RESEARCH.md Pitfall 4)."
    - "A product that has only an uploaded photo and no legacy image URL shows the peek window's no-image state; this is the documented, scoped-as-acceptable consequence of leaving the peek window untouched, not a regression (31-RESEARCH.md Pitfall 4)."
    - statement: "A catalog page containing a product with a 120-character name shows no horizontal page scroll and keeps the thumbnail column at its fixed 56px width (UI-SPEC catalog-table/long-text)."
      verification: backstop
    - statement: "The Details tab rendered with a 120-character product name and a 60-character category name shows no horizontal overflow inside the dialog (UI-SPEC detail-dialog-form/long-text)."
      verification: backstop
    - statement: "At 1280x800 all three rail trigger descriptions render on a single line each with no clipping of the has-errors suffix (UI-SPEC tab-rail/long-text)."
      verification: backstop
    - statement: "The Photo tab in its offline-error state shows the message fully visible below the zone with no clipping (UI-SPEC photo-area/long-text)."
      verification: backstop
  prohibitions:
    - statement: "`src/widgets/ProductPeekWindow/` must not be modified in this phase — the phase boundary requires proving it still works, not changing it (31-RESEARCH.md Pitfall 4)."
      status: active
      verification: automated
    - statement: "No verification in this phase may be a `<human-check>`, a human-verify checkpoint, or a manual UAT scenario; every criterion is an automated Playwright assertion (project policy, CLAUDE.md)."
      status: active
      verification: automated
    - statement: "The RLS-denial client must sign in with the anon key under its own isolated storage key, never reuse the service-role client — a service-role read bypasses RLS and would prove nothing."
      status: active
      verification: automated
  artifacts:
    - e2e/products/product-photo-rls.spec.ts
    - e2e/visual/46-product-dialog-baseline.spec.ts
  key_links:
    - "`createRoleScopedClient` from `e2e/helpers/rls-clients.ts` ← the anon-key signed-in client the denial assertions must use."
    - "`playwright.visual.config.ts` ← already scoped to `./e2e/visual`, so the new baseline spec is picked up by `npm run test:e2e:visual` with no config change."
---

<objective>
Close PCAT-04: prove at the database layer that a role without `manage_products` cannot write to the
photos bucket, prove Phase 18's peek window is unaffected, and wire the four visual backstops the UI
contract defers to a snapshot.

Purpose: the earlier plans prove the happy paths; this one proves the boundary holds and that nothing
outside the phase's scope moved.
Output: an RLS-denial spec, an extended peek-window spec, and one visual-regression spec.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-product-catalog-detail-photo-upload/31-RESEARCH.md
@.planning/phases/31-product-catalog-detail-photo-upload/31-UI-SPEC.md
@.planning/phases/31-product-catalog-detail-photo-upload/31-01-SUMMARY.md
@.planning/phases/31-product-catalog-detail-photo-upload/31-02-SUMMARY.md
@.planning/phases/31-product-catalog-detail-photo-upload/31-03-SUMMARY.md
@.planning/phases/31-product-catalog-detail-photo-upload/31-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove the Storage RLS boundary from a real non-privileged client</name>
  <files>
    e2e/products/product-photo-rls.spec.ts
  </files>
  <read_first>
    - e2e/helpers/rls-clients.ts (`createRoleScopedClient(role, label)` — it provisions a real auth user with a given profile role, signs in with the anon key, and returns a client plus a cleanup function)
    - e2e/helpers/supabase.ts (`getServiceClient` — ground-truth reads only, never a denial client)
    - e2e/products/product-photo-upload.spec.ts (the fixture and cleanup shape to mirror)
    - .planning/phases/31-product-catalog-detail-photo-upload/31-01-SUMMARY.md (the applied SELECT-policy scope decided in Plan 01 Task 1 — the read assertions below must match whichever variant shipped)
    - supabase/migrations/20260907000001_product_photos_storage.sql (the four policies as applied)
    - .planning/phases/31-product-catalog-detail-photo-upload/31-RESEARCH.md § "Security Domain"
  </read_first>
  <action>
New spec `e2e/products/product-photo-rls.spec.ts`. It talks to Supabase directly — no page
navigation — because the claim under test is a database-layer one, and a UI that merely hides a
button proves nothing.

Provision a cashier-role client through `createRoleScopedClient('cashier', 'photo')` and, separately,
a bare anon client with no session. Seed one product and one stored object through the service client
so there is a real target.

Assertions:
1. The cashier client's `storage.from('product-photos').upload(...)` to a fresh path returns an error,
   and a service-client list of that prefix afterwards shows the object was not created.
2. The cashier client's `storage.from('product-photos').remove([existingPath])` returns an error or
   removes nothing, and a service-client list still shows the object present.
3. The cashier client's `storage.from('product-photos').update(...)` on the existing path returns an
   error.
4. The unauthenticated anon client is denied on all three of the same calls.
5. Read scope matches whichever SELECT variant Plan 01 applied — assert the recorded behaviour
   explicitly rather than assuming: if SELECT is open to any authenticated staff, the cashier client
   can mint a signed URL and the anon client cannot; if SELECT carries the `manage_products`
   predicate, both are denied. Reference the Plan 01 SUMMARY in a comment so a future reader knows
   which branch is live and why.
6. An admin-role client through the same helper succeeds at upload and delete, proving the denials
   above are role-specific rather than a blanket failure of the whole spec's setup.

Clean up every provisioned user, product, and object.
  </action>
  <verify>
    <automated>npx playwright test e2e/products/product-photo-rls.spec.ts</automated>
    <fails_when>non-zero exit, or the summary line reports any value other than 0 for "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/products/product-photo-rls.spec.ts` reports at least 6 tests, 0 failed.
    - `grep -c "createRoleScopedClient" e2e/products/product-photo-rls.spec.ts` returns at least 2 (a cashier and an admin client).
    - Each denial assertion is paired with a service-client ground-truth read proving the object's actual state, not only the returned error.
    - The admin positive-control test passes, so a denial cannot be an artefact of broken setup.
  </acceptance_criteria>
  <precondition>`.env.local` carries `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, and the service-role key that `e2e/helpers/rls-clients.ts` and `e2e/helpers/supabase.ts` read, and the Plan 01 migration is applied to that project.</precondition>
  <done>Every Storage write endpoint for the photos bucket is proven denied for a cashier and for an anonymous caller, with an admin positive control.</done>
</task>

<task type="auto">
  <name>Task 2: Prove Phase 18's peek window is unaffected</name>
  <files>
    e2e/checkout/peek-window.spec.ts
  </files>
  <read_first>
    - e2e/checkout/peek-window.spec.ts (the whole file — its two-page BroadcastChannel harness, `scanBarcode`, its seed-product shape, and every existing assertion)
    - e2e/helpers/tauriPeekMock.ts
    - src/widgets/ProductPeekWindow/ui/ProductPeekWindow.tsx (lines 250-260 — it renders the legacy image field directly and is deliberately not routed through this phase's resolver)
    - .planning/phases/31-product-catalog-detail-photo-upload/31-RESEARCH.md § Pitfall 4
  </read_first>
  <action>
Extend the existing spec — do not restructure it and do not weaken any existing assertion. `src/widgets/ProductPeekWindow/`
itself stays untouched; the phase boundary is to prove it still works, not to change it.

Add two tests using the file's existing harness:
1. Seed a product whose legacy image URL is set and whose stored photo path is null, scan its
   barcode, and assert the peek window renders an image element sourced from that legacy URL and
   that the rest of the peek content (name, price, stock) is unchanged.
2. Seed a product whose stored photo path is set and whose legacy image URL is null, scan its
   barcode, and assert the peek window renders its no-image state while every other field still
   renders correctly. Add a comment naming this as the documented, scoped-as-acceptable consequence
   of leaving the peek window on the legacy field, with a pointer to 31-RESEARCH.md Pitfall 4 — so a
   future reader does not file it as a bug.

Record the same consequence in the plan SUMMARY so it can be carried into the project's deferred-items
list at phase close.
  </action>
  <verify>
    <automated>npx playwright test e2e/checkout/peek-window.spec.ts</automated>
    <fails_when>non-zero exit, or the summary line reports any value other than 0 for "failed"</fails_when>
    <automated>git status --porcelain -- src/widgets/ProductPeekWindow</automated>
    <fails_when>any output at all — a modified, added, or deleted file under that path violates the phase boundary</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/checkout/peek-window.spec.ts` reports at least 2 more tests than before this task and 0 failed.
    - `git diff --stat HEAD -- src/widgets/ProductPeekWindow` prints nothing.
    - `git diff HEAD -- e2e/checkout/peek-window.spec.ts` shows added test blocks and no removed `expect` calls.
    - The plan SUMMARY records the photo-only-product peek behaviour as a deferred follow-up with its rationale.
  </acceptance_criteria>
  <precondition>`.env.local` carries the E2E credentials read by `e2e/helpers/supabase.ts`, and a dev server is reachable at the base URL configured in `playwright.config.ts`.</precondition>
  <done>The peek window is proven to still work for its own data shape, and the one behavioural gap this phase deliberately leaves open is documented rather than silently absorbed.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the four visual backstops into one visual-regression spec</name>
  <files>
    e2e/visual/46-product-dialog-baseline.spec.ts
  </files>
  <read_first>
    - e2e/visual/45-visual-baseline.spec.ts (the whole file — its `gotoAuthed` / `loginAs` usage, its `waitForPageReady` stabilisation helper, its masking conventions, and its known cold-start flake note)
    - playwright.visual.config.ts (testDir is already `./e2e/visual`, so a new file there is picked up by `npm run test:e2e:visual` with no config change)
    - .planning/phases/31-product-catalog-detail-photo-upload/31-UI-SPEC.md § "UI Considerations" → the backstop summary of four items, and § "Verification Hooks" → the recommended three dialog states
    - e2e/helpers/supabase.ts (`getServiceClient`, `resetTestState`, `openCaja`)
  </read_first>
  <action>
New spec `e2e/visual/46-product-dialog-baseline.spec.ts`, mirroring `45-visual-baseline.spec.ts`'s
structure, auth flow, and stabilisation helpers rather than inventing a new convention. Viewport
1280x800, admin role, the app's default dark theme.

Seed, through the service client, one product with a 120-character name in a category with a
60-character name, plus one product with a stored photo and one without, so a single catalog page
exercises the mixed-thumbnail case.

Snapshots and paired assertions — each backstop is a snapshot plus one deterministic measurement, so
a failure says which of the two broke:
1. **Catalog with a long name.** Snapshot the catalog table. Assert the page has no horizontal scroll
   (document scroll width equals client width) and that the thumbnail column header cell reports a
   56px bounding-box width.
2. **Details tab with long name and long category.** Open that product's dialog on the Details tab and
   snapshot it. Assert the dialog content's scroll width equals its client width.
3. **Rail descriptions.** In the same 1280x800 dialog, snapshot the rail. Assert each of the three
   trigger description elements reports a bounding-box height equal to a single line of its computed
   line-height, and repeat with a forced field error so the has-errors suffix is present and still
   unclipped.
4. **Photo tab offline error.** Set the context offline, open the Photo tab, attempt an upload, and
   snapshot the panel. Assert the alert element is fully inside the dialog's bounding box.

Also capture the two states the UI contract recommends but does not gate on: the Photo tab empty and
the Photo tab populated.

Every one of these is an automated assertion — this project does not accept a human looking at a
screenshot as verification.
  </action>
  <verify>
    <automated>npm run test:e2e:visual</automated>
    <fails_when>non-zero exit, or the summary line reports any value other than 0 for "failed"; a missing baseline on the first run is written, not a pass — re-run and require a clean second run</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm run test:e2e:visual` passes on a second consecutive run with baselines already committed (the first run writes them).
    - The spec contains at least 6 snapshot assertions and at least 5 non-snapshot measurement assertions, so no backstop rests on a picture alone.
    - `git ls-files -- e2e/visual/46-product-dialog-baseline.spec.ts` lists the file and the generated baseline images are committed alongside it.
    - `grep -c "test.skip" e2e/visual/46-product-dialog-baseline.spec.ts` returns 0 — no scenario is parked behind a skip.
  </acceptance_criteria>
  <precondition>`.env.local` carries the E2E credentials read by `e2e/helpers/supabase.ts`, a dev server is reachable at the base URL configured in `playwright.visual.config.ts`, and the browser binary that config resolves is installed.</precondition>
  <done>All four UI-contract backstops are wired to a committed visual baseline plus a deterministic measurement, and the phase has no manual verification anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-privileged signed-in client → `storage.objects` | The only boundary this plan tests: whether the database, not the UI, refuses a cashier's write |
| unauthenticated client → `storage.objects` | Whether anything at all is reachable without a session |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Elevation of Privilege | `storage.objects` write policies (proof) | high | mitigate | Task 1 drives upload, update, and delete from a real cashier-role anon-key client and from an unauthenticated client, pairing every denial with a service-client ground-truth read and an admin positive control. |
| T-31-04 | Information Disclosure | private-bucket read scope (proof) | medium | mitigate | Task 1 asserts the anon client cannot mint a signed URL and that no public URL serves a stored object, matching whichever SELECT variant Plan 01 applied. |
| T-31-18 | Tampering | out-of-scope drift into Phase 18's peek window | medium | mitigate | Task 2's verify treats any working-tree change under the peek-window widget directory as a failure, so the phase boundary is enforced by a command rather than by intent. |
| T-31-19 | Repudiation | a visual backstop passing without evidence | low | mitigate | Every backstop pairs its snapshot with a deterministic measurement assertion, so a stale or auto-written baseline cannot silently carry the claim. |
| T-31-SC | Tampering | npm/pip/cargo installs | low | accept | No packages are installed by this plan. |
</threat_model>

<verification>
- `npx playwright test e2e/products/ e2e/checkout/peek-window.spec.ts` green.
- `npm run test:e2e:visual` green on a second consecutive run.
- `npm run test:e2e` green — the phase gate.
- `git status --porcelain -- src/widgets/ProductPeekWindow` empty.
</verification>

<success_criteria>
PCAT-04 is fully evidenced: the reshaped dialog's round-trip, the photo upload and replace, the RBAC
and RLS denial, and Phase 18's peek window remaining unaffected are each proven by an automated
Playwright assertion, with no manual step anywhere in the phase.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan's share)

**Tests**
- `e2e/products/product-photo-rls.spec.ts` — cashier and anonymous denial on upload, update, delete, plus an admin positive control and the read-scope assertion
- extended `e2e/checkout/peek-window.spec.ts` — legacy-image-URL product renders, photo-only product shows the no-image state
- `e2e/visual/46-product-dialog-baseline.spec.ts` and its committed baseline images — the four UI-contract backstops plus the two recommended dialog states

**Documentation**
- A deferred-items entry recording that a product with only an uploaded photo shows no image in Phase 18's peek window, with its rationale
</artifacts>

<output>
Create `.planning/phases/31-product-catalog-detail-photo-upload/31-05-SUMMARY.md` when done.
Record the peek-window behavioural gap so it can be carried into STATE.md's Deferred Items.
</output>
