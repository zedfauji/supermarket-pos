---
phase: 31-product-catalog-detail-photo-upload
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
agent_hint: Frontend Developer
files_modified:
  - src/features/manage-products/ui/ProductDetailDialog.tsx
  - src/features/manage-products/ui/tabs/ProductDetailsTab.tsx
  - src/features/manage-products/ui/tabs/ProductLinksTab.tsx
  - src/features/manage-products/model/productDialogTabs.ts
  - src/features/manage-products/model/productDialogTabs.test.ts
  - src/features/manage-products/ui/ProductForm.tsx
  - src/features/manage-products/ui/CatalogProductsTab.tsx
  - src/features/manage-products/index.ts
  - src/shared/lib/i18n/locales/es-MX/featMgmt.json
  - src/shared/lib/i18n/locales/en-US/featMgmt.json
  - e2e/products/product-management.spec.ts
  - e2e/inventory/open-units.spec.ts
autonomous: true
requirements: [PCAT-01, PCAT-02, PCAT-04]

estimate:
  tokens: 55000
  raw_tokens: 110000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "One component serves both create and edit: `ProductDetailDialog` replaces today's two separate `Dialog`s, and after a successful create the dialog stays open, the title flips to the edit title, the stock strip appears, and the Photo tab trigger becomes enabled (D-03, PCAT-01)."
    - "Every field editable in the old flat form is still editable in the reshaped dialog, and a single Save submits one `ProductUpdate` payload built exactly as `ProductForm` builds it today — the tab split adds no second save path (D-05, PCAT-02)."
    - "With zero categories the Save button stays disabled and the Category select shows the no-categories placeholder; with zero modifiers or zero suppliers the Links tab shows the existing no-modifiers / no-suppliers copy; SKU, barcode, units-per-package, parent product, and the legacy image URL all submit as null when left blank (edge probe PCAT-02/empty)."
    - "Modifier checkboxes render sorted by sort order then by name, so two modifiers with an equal sort order always appear in the same name order; parent-package options render sorted by name (edge probe PCAT-02/ordering)."
    - "Create mode: all Details and Links fields are blank, Category defaults to the first category through the existing backfill effect, the stock strip is hidden, and the Photo tab trigger is disabled carrying the locked-hint copy (UI-SPEC detail-dialog-form/empty)."
    - "While submitting, the Save label switches to the saving copy and every field, the Cancel button, and the rail triggers are disabled; edit mode opens with values already in memory and shows no field-level loading skeleton (UI-SPEC detail-dialog-form/loading)."
    - "Zod field errors render through `FormField`'s `error` prop with `role=\"alert\"` and `aria-invalid`; a server failure raises a toast and the same string as a footer-level alert line (UI-SPEC detail-dialog-form/error)."
    - "Edit mode pre-fills every field from the product row, uses a two-column grid at the `lg` breakpoint, shows the edit title, the stock strip, and an enabled Photo tab trigger (UI-SPEC detail-dialog-form/populated)."
    - "Nullable fields render as empty inputs rather than being hidden, `stock_threshold` is preserved silently through the payload, and modifier/supplier lists with zero selections render as unchecked lists rather than disappearing (UI-SPEC detail-dialog-form/partial)."
    - "The tab panel wrapper is the only scroll region inside the 80vh-capped dialog — header, rail, and footer stay pinned; the modifier and supplier lists each cap at their own scroll area; below the `lg` breakpoint the field grid collapses to one column (UI-SPEC detail-dialog-form/overflow)."
    - "Modifier and supplier lists show the existing empty copy at zero and checkbox rows at one or many, with no count-dependent copy anywhere (UI-SPEC detail-dialog-form/zero-one-many)."
    - "The rail is static markup with no data dependency and never shows a loading state; its triggers are disabled only while submitting or, for Photo, in create mode (UI-SPEC tab-rail/loading)."
    - "Any tab holding at least one field error shows an 8px destructive dot badge and its accessible label carries the has-errors suffix until the next submit clears the errors (UI-SPEC tab-rail/error, D-06)."
    - "At the `lg` breakpoint and above the rail is a fixed 13rem column of three 44px-minimum triggers; below it the rail collapses to the inherited horizontal strip (UI-SPEC tab-rail/overflow, D-08)."
    - "The stock strip wraps onto additional rows at narrow widths instead of clipping, and lives inside the pinned header rather than the scroll region (UI-SPEC stock-strip/overflow)."
    - "Stock-strip content is numeric values or fixed short badges only — no free text can enter it, so no truncation rule is required (UI-SPEC stock-strip/long-text)."
    - "Pressing Save while a field on a hidden tab is invalid switches the dialog to the first tab holding an error and moves focus to that tab's first invalid control (D-06)."
    - "Closing the dialog with unsaved field edits prompts through a destructive `ConfirmDialog` before discarding; closing with no edits closes immediately; switching tabs never prompts (D-07)."
  prohibitions:
    - statement: "The tab split must not introduce a second save path — a single submit builds one payload across all three tabs (D-05)."
      status: active
      verification: automated
    - statement: "Switching tabs must never prompt for confirmation or block on validation; tabs are one form, not wizard steps (D-07)."
      status: active
      verification: automated
    - statement: "The existing dialog accessible names and the existing barcode test id must not be renamed — other E2E specs already select on them."
      status: active
      verification: automated
    - statement: "The legacy image-URL text input must not be removed or repurposed; it moves to the Links tab and stays editable (D-13)."
      status: active
      verification: automated
    - statement: "No hardcoded UI string may be introduced in this layer — every new label, hint, and error goes through a catalog key present in both locales."
      status: active
      verification: automated
  artifacts:
    - src/features/manage-products/ui/ProductDetailDialog.tsx
    - src/features/manage-products/ui/tabs/ProductDetailsTab.tsx
    - src/features/manage-products/ui/tabs/ProductLinksTab.tsx
    - src/features/manage-products/model/productDialogTabs.ts
  key_links:
    - "`ProductDetailDialog` ← the single host for the Details, Photo, and Links panels; `CatalogProductsTab` mounts exactly one of it instead of the previous two dialogs."
    - "`fieldErrorsToTab` ← the one mapping from a Zod field-error key to the tab that owns it; the D-06 auto-switch and the rail error badges both read it."
    - "`ProductPhotoTab` (Plan 01) ← remounted from the old edit-dialog body into this dialog's Photo panel."
---

<objective>
Reshape the two separate product dialogs into one `max-w-4xl` dialog with a vertical Details / Photo /
Links rail, a read-only stock strip in the header, a single pinned Save/Cancel footer, error-driven
tab navigation, and a dirty-close guard.

Purpose: PCAT-01 and PCAT-02 — the same fields and the same single save payload, in a layout that has
room for the photo and for Phase 32's brand and pack-weight fields.
Output: `ProductDetailDialog` plus its two new field panels, with `ProductForm`'s field JSX lifted
across unchanged and both of its documented silent-validation-failure guards carried with it.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-product-catalog-detail-photo-upload/31-CONTEXT.md
@.planning/phases/31-product-catalog-detail-photo-upload/31-PATTERNS.md
@.planning/phases/31-product-catalog-detail-photo-upload/31-UI-SPEC.md
@.planning/phases/31-product-catalog-detail-photo-upload/31-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One dialog, three vertical tabs, one footer</name>
  <files>
    src/features/manage-products/ui/ProductDetailDialog.tsx,
    src/features/manage-products/ui/tabs/ProductDetailsTab.tsx,
    src/features/manage-products/ui/tabs/ProductLinksTab.tsx,
    src/features/manage-products/ui/ProductForm.tsx,
    src/features/manage-products/ui/CatalogProductsTab.tsx,
    src/features/manage-products/index.ts,
    src/shared/lib/i18n/locales/es-MX/featMgmt.json,
    src/shared/lib/i18n/locales/en-US/featMgmt.json
  </files>
  <read_first>
    - src/features/manage-products/ui/ProductForm.tsx (the whole file — every field, the submit path at lines 124-232, the categoryId backfill effect at lines 71-90, and the stock_threshold comment at lines 151-163)
    - src/features/manage-products/ui/CatalogProductsTab.tsx (lines 339-408 — the two dialogs being collapsed; lines 164-177 — the mutation callback shape to keep)
    - src/widgets/SettingsTabsPanel/index.tsx (lines 143-177 — the vertical rail composition to copy)
    - src/shared/ui/vertical-tabs.tsx (VerticalTabsTrigger props: label, icon, description, badge, plus native disabled)
    - src/shared/ui/dialog.tsx (DialogContent is position-fixed with no scroll container of its own; DialogFooter already bleeds)
    - src/shared/ui/FormField.tsx, src/shared/ui/badge.tsx, src/shared/ui/POSButton.tsx
    - .planning/phases/31-product-catalog-detail-photo-upload/31-UI-SPEC.md § "Dialog skeleton", § "Details tab", § "Links tab", § "Create-then-stay", § "Spacing Scale", § "Typography", § "Color", § "Copywriting Contract"
    - .planning/phases/31-product-catalog-detail-photo-upload/31-PATTERNS.md § ProductDetailDialog.tsx, § ProductDetailsTab / ProductLinksTab
    - .planning/phases/31-product-catalog-detail-photo-upload/31-RESEARCH.md § Pitfall 2
    - src/features/manage-products/ui/tabs/ProductPhotoTab.tsx (Plan 01's panel, remounted here)
  </read_first>
  <action>
Build `ProductDetailDialog` as the single host and move `ProductForm`'s field JSX into two panels.
Field logic, Zod parsing, and error flattening are lifted verbatim, not rewritten.

1. **`ProductDetailDialog.tsx`.** Owns all form state (name, categoryId, basePrice, sku, barcode,
   unitsPerPackageInput, parentProductIdInput, isActive, imageUrl, modifierIds, selectedSupplierIds,
   fieldErrors), moved out of `ProductForm` unchanged — including both `useEffect`s (the categoryId
   backfill with its existing eslint-disable comment, and the supplierIds sync) and both `useMemo`s
   (`sortedModifiers`, `parentPackageOptions`). `handleSubmit` is copied from
   `ProductForm.tsx:124-232`, keeping the `stock_threshold: initialProduct.stock_threshold ?? null`
   line and the `happyHourPrice: null` line — dropping either reproduces a documented silent-failure
   incident recorded in that file's own comments.

   Structure per 31-UI-SPEC.md § "Dialog skeleton": `DialogContent` with
   `max-w-4xl sm:max-w-4xl max-h-[80vh] flex flex-col overflow-hidden` and `showCloseButton`;
   `DialogHeader` with `pr-8` holding `DialogTitle` (the existing `productsTab.newProductTitle` /
   `productsTab.editProductTitle` keys — do not rename them, other specs select on them) and, in edit
   mode only, the stock strip; a `<form>` wrapping the body and footer so Enter submits from any tab;
   the body a `grid min-h-0 flex-1 gap-6 lg:grid-cols-[13rem_minmax(0,1fr)]` containing
   `VerticalTabsList` and a panel wrapper `min-h-0 overflow-y-auto`; `DialogFooter` with
   `showCloseButton={false}` holding the footer-level form-error line, a Cancel
   `POSButton variant="outline"`, and a Save `POSButton` with `focusEmphasis="high"`.

   `Tabs` is **controlled** (`value` / `onValueChange` on local `activeTab` state, not
   `defaultValue`) because Task 2 forces a switch. Three `VerticalTabsTrigger`s: `details`
   (icon `FileText`), `photo` (icon `Image`, `disabled` in create mode, description flipping between
   `productDialog.photo.lockedTitle` and `productDialog.tabs.photoDescription`), `links`
   (icon `Link2`). Each `TabsContent` gets
   `rounded-xl border border-border bg-card p-4 lg:p-6 shadow-xs`. The panel wrapper — not the
   dialog, not the rail, not the footer — carries `overflow-y-auto`.

   Stock strip (D-17), edit mode only, `data-testid="product-stock-strip"`, inside `DialogHeader`,
   `flex flex-wrap items-center gap-x-6 gap-y-2 pt-2`: an eyebrow-labelled `quantityOnHand` value in
   `text-numeric` with a warning `Badge` when a threshold exists and the quantity is at or below it,
   an eyebrow-labelled `lowStockThreshold` value falling back to the no-threshold copy, and a
   success/muted `Badge` for active/inactive. All three read fields `ProductSchema` already carries;
   add no new query.

   Create-then-stay (D-03): the dialog does **not** close after a successful create. On success it
   stores the created `Product` (`useMutationCreateProduct` already resolves `Result<Product>`) in
   local state, which flips the title, reveals the stock strip, and enables the Photo trigger. The
   existing success toast still fires.

2. **`tabs/ProductDetailsTab.tsx`.** A presentational panel receiving values, setters, `fieldErrors`,
   and `submitting`. Lift the Name, Category, Base price, SKU, Barcode, and Active-checkbox blocks
   from `ProductForm.tsx:248-319` and `374-386`, regrouped into `grid gap-4 lg:grid-cols-2` with Name
   and the Active checkbox spanning both columns. Keep `data-testid="product-form-barcode"` and the
   native `<select>` class string exactly as they are.

3. **`tabs/ProductLinksTab.tsx`.** Same shape. Lift Units-per-package, Linked-package-product, the
   legacy image-URL input (`ProductForm.tsx:360-372` — still validated by `UrlSchema`, because that
   field really is a URL unlike the Plan-01 path field), and the Modifiers and Suppliers checkbox
   wells from `ProductForm.tsx:388-450`. Change only the wells' scroll cap from `max-h-40` to
   `max-h-48` per 31-UI-SPEC.md. The image-URL row spans both columns as the last row.

4. **`ProductForm.tsx`.** Delete it and drop its re-export from
   `src/features/manage-products/index.ts` — every line now lives in the dialog and the two panels,
   and `CatalogProductsTab` is its only importer.

5. **`CatalogProductsTab.tsx`.** Replace both `Dialog` blocks (lines 339-408) with a single
   `ProductDetailDialog`, open when either the add button was pressed or a product is selected for
   edit. Keep the existing mutation callback shapes, toast strings, and the `ConfirmDialog` deactivate
   block untouched. The create branch no longer closes the dialog on success (D-03). Remove the
   direct `ProductPhotoTab` mount that Plan 01 added — the panel now lives in the dialog's Photo tab.

6. **i18n.** Add to both locale files under `manageProducts`, using the values from
   31-UI-SPEC.md § "Copywriting Contract": `productDialog.navLabel`; `productDialog.tabs.details`,
   `detailsDescription`, `photo`, `photoDescription`, `links`, `linksDescription`;
   `productDialog.tabWithErrors`; `productDialog.photo.lockedTitle`;
   `productDialog.stockStrip.onHand`, `threshold`, `noThreshold`, `lowStock`;
   `productDialog.discardTitle`, `discardDescription`, `discardConfirmLabel`, `keepEditing`;
   `productsTab.photoHeader`; `productsTab.noPhoto`; and the new value for the existing
   `productsTab.headerHelp` key. The discard keys are consumed by Task 2 and the catalog keys by
   Plan 03 — they are added here so the catalog is written once.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit, or any "error TS" line in the output</fails_when>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit, or any "error" line in the ESLint output (max-warnings is 0, so a warning also fails)</fails_when>
  </verify>
  <acceptance_criteria>
    - `git ls-files -- src/features/manage-products/ui/ProductForm.tsx` prints nothing, and `npm run typecheck` still passes — a dangling import to the deleted module would be a TS error, so this pair proves both deletion and de-referencing without a comment-sensitive grep.
    - `grep -c "product-form-barcode" src/features/manage-products/ui/tabs/ProductDetailsTab.tsx` returns 1.
    - `grep -c "stock_threshold" src/features/manage-products/ui/ProductDetailDialog.tsx` returns at least 1.
    - `grep -c "min-h-0 overflow-y-auto" src/features/manage-products/ui/ProductDetailDialog.tsx` returns 1 — exactly one scroll region, on the panel wrapper.
    - `grep -c "imageUrl" src/features/manage-products/ui/tabs/ProductLinksTab.tsx` returns at least 1 (the legacy field survives the move).
    - `grep -c "onValueChange" src/features/manage-products/ui/ProductDetailDialog.tsx` returns at least 1 (the rail is controlled, not `defaultValue`).
    - `npm run lint` passes with `i18next/no-literal-string` active on all three new files.
  </acceptance_criteria>
  <done>One dialog renders all three tabs, saves through one payload, and stays open after a create; `ProductForm.tsx` no longer exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Error-driven tab navigation and the dirty-close guard</name>
  <files>
    src/features/manage-products/model/productDialogTabs.ts,
    src/features/manage-products/model/productDialogTabs.test.ts,
    src/features/manage-products/ui/ProductDetailDialog.tsx
  </files>
  <read_first>
    - src/features/manage-products/ui/ProductDetailDialog.tsx (as written in Task 1)
    - .planning/phases/31-product-catalog-detail-photo-upload/31-UI-SPEC.md § "Error navigation (D-06)", § "Dirty close (D-07)", and the `tabWithErrors` / `discard*` / `keepEditing` copy rows
    - src/features/manage-products/ui/CatalogProductsTab.tsx (lines 410-428 — the `ConfirmDialog` prop shape to copy: open, title, description, confirmLabel, variant, isLoading, onConfirm, onCancel)
    - src/shared/ui/ConfirmDialog.tsx
    - src/shared/lib/domain.ts (the field names `ProductUpdateSchema` can produce errors for)
  </read_first>
  <behavior>
`src/features/manage-products/model/productDialogTabs.test.ts` against `productDialogTabs.ts`:
  - `tabForFieldError('name')`, `'categoryId'`, `'basePrice'`, `'sku'`, `'barcode'`, `'isActive'` → `'details'`.
  - `tabForFieldError('unitsPerPackage')`, `'parentProductId'`, `'modifiers'`, `'imageUrl'` → `'links'`.
  - `tabForFieldError('_form')` → `null` (a form-level error belongs to no tab and must not force a switch).
  - `tabForFieldError('somethingUnmapped')` → `'details'` (unmapped keys fall back to the first tab rather than being silently unreachable).
  - `firstErroringTab({})` → `null`.
  - `firstErroringTab({ imageUrl: 'x' })` → `'links'`.
  - `firstErroringTab({ imageUrl: 'x', name: 'y' })` → `'details'` — rail order decides, not object key order, so the result is stable regardless of how the error object was built.
  - `tabsWithErrors({ name: 'a', imageUrl: 'b' })` → a set containing both `'details'` and `'links'`.
  - `isProductFormDirty(initial, current)` → false for an unmodified snapshot, true when any single field differs, and false again when a field is edited and restored to its original value.
  </behavior>
  <action>
1. **`productDialogTabs.ts`** — a pure module, no React and no Supabase. Export the tab id union,
   the rail order constant (`details`, `photo`, `links`), `tabForFieldError(key)`,
   `firstErroringTab(fieldErrors)` resolving ties by rail order, `tabsWithErrors(fieldErrors)`, and
   `isProductFormDirty(initial, current)` comparing the field snapshot. The field-to-tab mapping is
   exactly the one in 31-UI-SPEC.md § "Error navigation": `name`, `categoryId`, `basePrice`, `sku`,
   `barcode`, `isActive` belong to Details; `unitsPerPackage`, `parentProductId`, `modifiers`,
   `imageUrl` belong to Links. Photo owns no form field — the photo commits at upload time, so it is
   never a target of the auto-switch.

2. **D-06 auto-switch in `ProductDetailDialog.tsx`.** After `setFieldErrors` in the submit path, if
   `firstErroringTab(next)` is non-null and differs from `activeTab`, set `activeTab` to it, then move
   focus to that panel's first `[aria-invalid="true"]` control. Pass a `badge` of
   `<span className="size-2 rounded-full bg-destructive" />` to every trigger whose tab is in
   `tabsWithErrors(fieldErrors)`, and set that trigger's `label` to
   `t('manageProducts.productDialog.tabWithErrors', { label })` so the has-errors state reaches the
   accessible name, not only the dot. Both clear on the next submit that produces no errors.

3. **D-07 dirty-close guard in `ProductDetailDialog.tsx`.** Track the initial field snapshot at open
   (and re-baseline it after a successful save, including the create-then-stay transition). On
   `onOpenChange(false)` from the X, Esc, or an outside click, and on the Cancel button, call
   `isProductFormDirty`: dirty opens a `ConfirmDialog` with `variant="destructive"` carrying
   `discardTitle` / `discardDescription` / `discardConfirmLabel` and the `keepEditing` cancel label,
   and only its confirm actually closes; not dirty closes immediately. Photo changes never count
   toward dirtiness — they are already committed to Storage and the column by the time the dialog
   closes. Tab switching is unaffected: it neither validates nor prompts.
  </action>
  <verify>
    <automated>npx vitest run src/features/manage-products/model/productDialogTabs.test.ts 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, or any line matching "FAIL" in the output, or fewer than 10 reported passing tests</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit, or any "error TS" line in the output</fails_when>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit, or any "error" line in the ESLint output</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/features/manage-products/model/productDialogTabs.test.ts` reports at least 10 passing tests, including the rail-order tie-break case and the edit-then-restore dirty case.
    - `grep -c "tabWithErrors" src/features/manage-products/ui/ProductDetailDialog.tsx` returns at least 1.
    - `grep -c "ConfirmDialog" src/features/manage-products/ui/ProductDetailDialog.tsx` returns at least 1.
    - The unit suite includes an assertion that `firstErroringTab` never returns the Photo tab id for any key in `ProductUpdateSchema`'s field set — the Photo tab owns no form field, so it can never be an auto-switch target.
  </acceptance_criteria>
  <done>An invalid hidden-tab field navigates the user to it with a visible error badge, and closing with unsaved edits asks before discarding.</done>
</task>

<task type="auto">
  <name>Task 3: Re-point the two existing product-dialog E2E specs and prove the reshape</name>
  <files>
    e2e/products/product-management.spec.ts,
    e2e/inventory/open-units.spec.ts
  </files>
  <read_first>
    - e2e/products/product-management.spec.ts (the whole file — every place it opens the product dialog, especially the `New product` dialog usage around line 171 and the happyHourPrice note around line 324)
    - e2e/inventory/open-units.spec.ts (lines 265-335 — Step 1 and Step 2 fill "Units per package" and "Linked package product" inside the create/edit dialog, and Step 2 presses "Add product" again assuming the create dialog already closed)
    - src/features/manage-products/ui/ProductDetailDialog.tsx and both tab panels (as written in Tasks 1-2)
    - .planning/phases/31-product-catalog-detail-photo-upload/31-UI-SPEC.md § "Verification Hooks"
    - e2e/helpers/auth.ts (`loginAs`, `logout`), e2e/helpers/supabase.ts (`getServiceClient`, `resetTestState`, `openCaja`)
  </read_first>
  <action>
Two existing specs drive the product dialog and both break on the reshape. Fix them first, then add
the new coverage.

1. **`e2e/inventory/open-units.spec.ts` (regression, not new coverage).** Units-per-package and
   Linked-package-product now live behind the Links tab. In Step 1 and Step 2, click
   `getByRole('tab', { name: /links/i })` inside the dialog before filling or asserting on either
   field. Also handle create-then-stay: after "Create product" the dialog no longer closes on its own,
   so Step 1 and Step 2 must close it explicitly (Cancel, which is now a no-op close because the
   snapshot was re-baselined by the successful save) before the next "Add product" click. Do not
   weaken any existing assertion — the point of this spec is unchanged.

2. **`e2e/products/product-management.spec.ts` (re-point plus new coverage).** Update any step that
   assumed a flat single-column form, then add tests for:
   - the reshaped dialog opening from the existing row Edit button with all three rail triggers
     present, Photo enabled in edit mode and disabled in create mode;
   - a full round-trip: open an existing product, change a Details field and a Links field in the
     same session, press Save once, and assert through the service client that both changed values
     persisted from that single submit;
   - create-then-stay: press "Add product", fill the required Details fields, press Create, and assert
     the dialog is still visible, the accessible name has flipped to the edit title, and the Photo
     trigger is now enabled;
   - D-06: clear the required Name field while sitting on the Links tab, press Save, and assert the
     dialog switched to the Details tab, that the Details trigger's accessible name carries the
     has-errors suffix, and that the Name field is marked `aria-invalid`;
   - D-07: edit a field, press Esc, assert the `alertdialog` with the discard title appears, choose
     keep-editing and assert the product dialog is still open with the edit intact, then re-trigger
     and confirm discard and assert it closed;
   - tab switching is free: change a field, switch tabs twice, and assert no confirmation dialog
     appeared and the changed value survived the round trip;
   - RBAC (PCAT-02): sign in as a cashier and assert the Catalog Products surface offers no
     add/edit affordance — extend the existing cashier-gating coverage in this file rather than
     replacing it.
  </action>
  <verify>
    <automated>npx playwright test e2e/products/product-management.spec.ts</automated>
    <fails_when>non-zero exit, or the summary line reports any value other than 0 for "failed"</fails_when>
    <automated>npx playwright test e2e/inventory/open-units.spec.ts</automated>
    <fails_when>non-zero exit, or the summary line reports any value other than 0 for "failed"</fails_when>
    <automated>npx playwright test e2e/products/ e2e/inventory/</automated>
    <fails_when>non-zero exit, or the summary line reports any value other than 0 for "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/inventory/open-units.spec.ts` passes with every pre-existing assertion intact (`git diff` on that file shows added navigation steps, no deleted `expect` calls).
    - `grep -c "getByRole('tab'" e2e/inventory/open-units.spec.ts` returns at least 2 (Links-tab navigation added in both steps).
    - `npx playwright test e2e/products/product-management.spec.ts` reports at least 7 more tests than before this task and 0 failed.
    - The round-trip test asserts both a Details-tab value and a Links-tab value through one service-client read after a single Save.
  </acceptance_criteria>
  <precondition>`.env.local` carries the E2E credentials read by `e2e/helpers/supabase.ts` and `e2e/helpers/auth.ts`, and a dev server is reachable at the base URL configured in `playwright.config.ts`.</precondition>
  <done>Both pre-existing product-dialog specs pass against the reshaped dialog, and the reshape's own behaviour is proven by automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → product dialog | All field values are operator input, validated client-side by Zod before the mutation |
| renderer → `products` table | The RLS policies on `products` are the authorization boundary; the dialog's visibility is UX only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-08 | Elevation of Privilege | product write access from the reshaped dialog | high | mitigate | The dialog changes layout only; the write path is the unchanged `useMutationCreateProduct` / `useMutationUpdateProduct` pair behind the existing `manage_products` RLS on `products`. Task 3 keeps and extends the cashier-denial coverage so a layout change cannot silently widen access. |
| T-31-09 | Tampering | validation bypass through a hidden tab | medium | mitigate | A single `handleSubmit` parses the whole payload with `ProductUpdateSchema` regardless of which tab is visible; D-06 surfaces the error rather than letting a hidden invalid field submit silently. |
| T-31-10 | Repudiation | unsaved edits lost without acknowledgement | low | mitigate | The D-07 dirty guard forces an explicit discard before closing. |
| T-31-SC | Tampering | npm/pip/cargo installs | low | accept | No packages are installed by this plan; every primitive it uses already exists in `src/shared/ui/` (31-UI-SPEC.md § Registry Safety, `registries: {}`). |
</threat_model>

<verification>
- `npm run typecheck` and `npm run lint` clean.
- `npx vitest run src/features/manage-products/model/productDialogTabs.test.ts` green.
- `npx playwright test e2e/products/ e2e/inventory/` green.
</verification>

<success_criteria>
Opening any product from Catalog shows one 896px dialog with a Details / Photo / Links rail, a
read-only stock strip, and one Save that persists every tab's fields in a single write; invalid
hidden fields navigate to themselves and unsaved edits cannot be lost silently.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan's share)

**Components**
- `src/features/manage-products/ui/ProductDetailDialog.tsx` — `ProductDetailDialog`
- `src/features/manage-products/ui/tabs/ProductDetailsTab.tsx` — `ProductDetailsTab`
- `src/features/manage-products/ui/tabs/ProductLinksTab.tsx` — `ProductLinksTab`

**Modules**
- `src/features/manage-products/model/productDialogTabs.ts` — `tabForFieldError`, `firstErroringTab`, `tabsWithErrors`, `isProductFormDirty`, the tab-id union, the rail-order constant

**Removed**
- `src/features/manage-products/ui/ProductForm.tsx` and its re-export from `src/features/manage-products/index.ts`

**i18n keys** (`featMgmt`, both locales, under `manageProducts`)
- `productDialog.navLabel`
- `productDialog.tabs.details`, `.detailsDescription`, `.photo`, `.photoDescription`, `.links`, `.linksDescription`
- `productDialog.tabWithErrors`
- `productDialog.photo.lockedTitle`
- `productDialog.stockStrip.onHand`, `.threshold`, `.noThreshold`, `.lowStock`
- `productDialog.discardTitle`, `.discardDescription`, `.discardConfirmLabel`, `.keepEditing`
- `productsTab.photoHeader`, `productsTab.noPhoto`, and a new value for the existing `productsTab.headerHelp`

**Test IDs**
- `product-stock-strip`

**Tests**
- `src/features/manage-products/model/productDialogTabs.test.ts`
- extended `e2e/products/product-management.spec.ts`, re-pointed `e2e/inventory/open-units.spec.ts`
</artifacts>

<output>
Create `.planning/phases/31-product-catalog-detail-photo-upload/31-02-SUMMARY.md` when done.
</output>
