---
phase: 24-tax-configuration-inclusive-exclusive-toggle
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/shared/lib/domain.ts
  - src/shared/lib/billing-settings.test.ts
  - src/entities/settings/model/queries.ts
  - supabase/migrations/20260831000005_tax_inclusive_mode.sql
  - src/shared/lib/supabase.types.ts
  - src/widgets/PaymentModal/ui/PaymentForm.tsx
  - src/widgets/PaymentModal/ui/PaymentForm.test.tsx
  - supabase/functions/_shared/tax.ts
  - supabase/functions/process-direct-sale/index.ts
  - src/shared/lib/edge-function-contracts.ts
  - src/shared/lib/receipt-format.ts
  - src/shared/lib/i18n/locales/es-MX/receipt.json
  - src/shared/lib/i18n/locales/en-US/receipt.json
  - src/shared/lib/__tests__/edge-tax.test.ts
  - e2e/checkout/tax-inclusive-mode.spec.ts
autonomous: true
requirements: [TAX-01, TAX-02, TAX-03, TAX-04, TAX-05]

estimate:
  tokens: 90000
  raw_tokens: 180000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "With taxInclusive=true (the post-migration default, D-01), completing a direct-sale checkout charges exactly the sum of catalog prices (no addition on top) and the returned receipt shows Subtotal < Total with Tax = Total - Subtotal to the cent — proven by a real Playwright checkout, not a mock."
    - "With taxInclusive=false, checkout keeps today's additive math unchanged (tax = subtotal * rate/100, total = subtotal + tax) — proven by a unit test at a nonzero rate."
    - "process_direct_sale_atomic re-reads settings.billing server-side and computes v_derived_total using the same mode as the client; the anti-tamper guard (abs(p_amount - v_derived_total) > 0.01) accepts a client amount computed under the correct mode and rejects one computed under the wrong mode."
    - "Exactly one process_direct_sale_atomic function exists in the database after the migration (no silent second overload from a signature mismatch, per Pitfall 1)."
    - "A printed/previewed direct-sale receipt shows Subtotal, Tax, and Total as three distinct lines in both modes (D-03) — never Subtotal===Total with a decorative Tax line (Pitfall 2)."
    - "No task in this plan (or anywhere in this phase) writes to historical payments/tabs rows to retroactively adjust already-overcharged tax amounts from before this migration — this is a forward-looking code fix only, per D-02's explicit call, not a financial cleanup/refund exercise."
  artifacts:
    - path: "supabase/migrations/20260831000005_tax_inclusive_mode.sql"
      provides: "CREATE OR REPLACE FUNCTION public.process_direct_sale_atomic(...) — exact current 17-arg signature preserved, mode-aware tax section"
    - path: "supabase/functions/_shared/tax.ts"
      provides: "decomposeTax(chargedAmount, taxRatePercent, taxInclusive) — pure function, shared by all 3 payment-completing edge functions"
    - path: "src/shared/lib/domain.ts"
      provides: "BillingSettingsSchema.taxInclusive: z.boolean().default(true)"
    - path: "src/shared/lib/edge-function-contracts.ts"
      provides: "ReceiptDataSchema += taxAmount/taxRatePercent/taxInclusive (all optional, MoneySchema/boolean/number)"
    - path: "e2e/checkout/tax-inclusive-mode.spec.ts"
      provides: "Real end-to-end proof: inclusive-mode checkout total unchanged, receipt shows decomposed subtotal+tax"
  key_links:
    - from: "PaymentForm.tsx useMemo (taxAmount/subtotalWithTax)"
      to: "process_direct_sale_atomic's v_tax/v_derived_total"
      via: "identical subtotal-first-then-subtract rounding discipline in both places"
      pattern: "ROUND\\(v_subtotal \\/ \\(1 \\+ v_tax_rate"
    - from: "buildSaleReceipt (process-direct-sale/index.ts)"
      to: "decomposeTax (_shared/tax.ts)"
      via: "settings.billing read + decomposeTax(chargedAmount, taxRatePercent, taxInclusive) before constructing receiptData"
      pattern: "decomposeTax\\(chargedAmount"
---

<objective>
Wire the entire mode-aware tax fix through every layer the phase touches, for one proven end-to-end
path: the direct-sale checkout, in `taxInclusive=true` mode (the confirmed real-world default, D-01).
`BillingSettingsSchema` gains `taxInclusive` (TAX-01's schema half — the admin form itself is Plan 02).
`PaymentForm.tsx`'s checkout-time tax preview becomes mode-aware, branching on the same formula as the
server (TAX-02/TAX-03) — both branches are implemented in full, not narrowed to inclusive-only, per
D-04's explicit call to keep a real toggle with both states. `process_direct_sale_atomic` is replaced
via `CREATE OR REPLACE FUNCTION`
carrying forward its exact current 17-argument signature (verified this session in
`20260831000003_bank_transfers_schema.sql:380`) with a mode-aware tax section (TAX-04) — this is the
ONLY authoritative copy of this function; the "cost-snapshot variant" CONTEXT.md named no longer exists
separately (RESEARCH.md Finding 1/Pitfall 1). `process-direct-sale`'s `buildSaleReceipt` gets the first
of three receipt-building fixes (TAX-05), via a new shared `_shared/tax.ts` helper (`process-payment`/
`process-split-payment` are fixed in Plan 03, reusing this same file). `ReceiptDataSchema` gains
`taxAmount`/`taxRatePercent`/`taxInclusive` as optional fields, `buildThermalReceiptText` renders the
new line, and both i18n locales get a `receipt.tax` key. This plan follows a RED->GREEN TDD arc: Task 1
authors every file plus a new E2E test that must currently fail (the live DB function is still the old
additive-only body); Task 2 is the [BLOCKING] schema-push task — apply the migration, regenerate
`supabase.types.ts`, and iterate until every test is green.

Purpose: every other plan in this phase (admin UI toggle, the other two edge functions, e2e/unit
fixture hardening) builds on this proven core — `_shared/tax.ts`, the live RPC formula, and the
`taxInclusive` schema field must all be correct and merged before anything else in the phase can be
verified against real behavior.

Output: `taxInclusive` schema field + live default-true DB row; mode-aware `process_direct_sale_atomic`;
mode-aware `PaymentForm.tsx` checkout preview; `_shared/tax.ts`; `process-direct-sale` receipts with a
real subtotal+tax+total breakdown; a green Playwright E2E proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-tax-configuration-inclusive-exclusive-toggle/24-CONTEXT.md
@.planning/phases/24-tax-configuration-inclusive-exclusive-toggle/24-RESEARCH.md
@.planning/phases/24-tax-configuration-inclusive-exclusive-toggle/24-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Author the mode-aware tax core + one receipt path + failing E2E proof (RED)</name>
  <read_first>
    src/shared/lib/domain.ts lines 795-821 (exact current `BillingSettingsSchema`, sibling
    `taxRatePercent` field to copy);
    src/entities/settings/model/queries.ts lines 55-80 (`DEFAULT_BILLING` constant — must gain the
    same field or a missing-settings-row fallback silently omits it);
    src/widgets/PaymentModal/ui/PaymentForm.tsx lines 172-320 (current `taxRatePercent` read at 179,
    the additive-only `taxAmount`/`subtotalWithTax`/`runningTotal` useMemo at 286-291, and every
    downstream read of `runningTotal`/`subtotalWithTax` at 293, 308, 743-777 that must keep working
    unchanged);
    src/widgets/PaymentModal/ui/PaymentForm.test.tsx lines 1-60 (the `taxRatePercent: 0` mock —
    Pitfall 5 — degenerate for both modes; add `taxInclusive` to the mock and new nonzero-rate cases);
    src/shared/lib/billing-settings.test.ts (full file — mirrors this exact `BillingSettingsSchema`
    test pattern for the new field);
    supabase/migrations/20260831000003_bank_transfers_schema.sql lines 380-527 (the ONLY live
    `process_direct_sale_atomic` body — confirmed via `grep -rl process_direct_sale_atomic
    supabase/migrations/` returning nothing newer this session; DECLARE block at 386-393, tax section
    at 477-481, anti-tamper checks at 482-490 — do not touch 482-490, only 477-481 changes);
    supabase/functions/_shared/audit.ts (cross-function Deno shared-import precedent —
    `import { recordAudit } from '../_shared/audit.ts'`, mirror this exact relative-import style for
    the new `_shared/tax.ts`);
    supabase/functions/process-direct-sale/index.ts lines 103-215 (`buildSaleReceipt` — the `admin`
    client is already its first parameter; current buggy `subtotal`/`total` construction at
    191, 201-202);
    src/shared/lib/edge-function-contracts.ts lines 40-92 (`ReceiptDataSchema` — exact optional-field
    convention to copy: `discountAmount: MoneySchema.nullable().optional()`);
    src/shared/lib/receipt-format.ts lines 195-225 (`buildThermalReceiptText`'s exact
    subtotal/total line-push order — new tax line goes between them);
    src/shared/lib/i18n/locales/es-MX/receipt.json and en-US/receipt.json lines 1-21 (`receipt.subtotal`/
    `receipt.total` sibling key convention);
    e2e/checkout/happy-path.spec.ts lines 1-70 (fixtures/helpers convention: `loginAs`, `resetTestState`,
    `openCaja`, `getServiceClient` — mirror this exact setup for the new spec; note line 49's
    `Math.round(Number(product.base_price) * 1.16 * 100) / 100` literal, which this plan does NOT fix —
    that correction belongs to Plan 04, this task only reads the file for the login/checkout flow
    pattern).
  </read_first>
  <behavior>
    New file `src/shared/lib/__tests__/edge-tax.test.ts` (mirrors the existing
    `src/shared/lib/__tests__/audit-actions.test.ts` location/naming), importing `decomposeTax` from
    `../../../../supabase/functions/_shared/tax.ts` (plain relative import — the function has zero
    imports itself, so it is importable from a Vitest `src/` test despite living under
    `supabase/functions/`, which Vitest's `include` glob does not scan for test files but can still
    resolve as an import target):
    - Case: `decomposeTax(116, 16, true)` returns `{ subtotal: 100, taxAmount: 16, total: 116 }`.
    - Case: `decomposeTax(100, 16, false)` returns `{ subtotal: 100, taxAmount: 16, total: 116 }`.
    - Property test (fast-check, already a dependency): for `chargedAmount` in `[1, 100000]` (2dp) and
      `taxRatePercent` in `[0, 100]`, both inclusive and exclusive branches satisfy
      `Math.round((subtotal + taxAmount) * 100) === Math.round(total * 100)` to the cent (Open
      Question 2 from RESEARCH.md).
    Add the identical property-based case to `PaymentForm.test.tsx` (new `describe` block) asserting
    the same invariant for the `taxAmount`/`subtotalWithTax` useMemo output at a nonzero rate, plus one
    concrete nonzero-rate exclusive-mode case (`taxRatePercent: 16, taxInclusive: false` ->
    `taxAmount === Math.round(afterDiscount * 0.16 * 100) / 100`, unchanged math, TAX-03) and one
    concrete inclusive-mode case (`taxRatePercent: 16, taxInclusive: true` -> `subtotalWithTax ===
    afterDiscount` unchanged, decomposed `taxAmount` matches the formula, TAX-02).
    New file `e2e/checkout/tax-inclusive-mode.spec.ts`: `beforeEach` calls `resetTestState()`,
    `openCaja(500)`, navigates to `/`, `loginAs(page, 'cashier')` (mirrors `happy-path.spec.ts`). One
    test: complete a cash direct-sale checkout for a known product, then read the printed/on-screen
    receipt content (or intercept the `process-direct-sale` edge function's JSON response via
    `page.waitForResponse` matching `/functions/v1/process-direct-sale` and parse
    `body.receiptData`) and assert `receiptData.total` equals the product's `base_price` (no `* 1.16`),
    `receiptData.taxAmount` is defined and > 0, and
    `Math.round((receiptData.subtotal + receiptData.taxAmount) * 100) === Math.round(receiptData.total * 100)`.
    This MUST fail right now (the live DB function is still additive-only, so `total` will be
    `base_price * 1.16`, not `base_price`) — this is the RED move.
  </behavior>
  <files>
    src/shared/lib/domain.ts,
    src/shared/lib/billing-settings.test.ts,
    src/entities/settings/model/queries.ts,
    supabase/migrations/20260831000005_tax_inclusive_mode.sql,
    src/widgets/PaymentModal/ui/PaymentForm.tsx,
    src/widgets/PaymentModal/ui/PaymentForm.test.tsx,
    supabase/functions/_shared/tax.ts,
    supabase/functions/process-direct-sale/index.ts,
    src/shared/lib/edge-function-contracts.ts,
    src/shared/lib/receipt-format.ts,
    src/shared/lib/i18n/locales/es-MX/receipt.json,
    src/shared/lib/i18n/locales/en-US/receipt.json,
    src/shared/lib/__tests__/edge-tax.test.ts,
    e2e/checkout/tax-inclusive-mode.spec.ts
  </files>
  <action>
    In `domain.ts`'s `BillingSettingsSchema`, add `taxInclusive: z.boolean().default(true)` as a
    sibling of `taxRatePercent` (D-01: default true). In `billing-settings.test.ts`, add a test
    asserting `BillingSettingsSchema.parse({ taxRatePercent: 16, paymentMethods: {...} }).taxInclusive`
    is `true` (schema default) and that `taxInclusive: false` round-trips. In `queries.ts`'s
    `DEFAULT_BILLING`, add `taxInclusive: true`.

    In `PaymentForm.tsx`, add `const taxInclusive = appSettings?.billing.taxInclusive ?? true;`
    immediately after the existing `taxRatePercent` read (line 179). Change the `taxAmount`/
    `subtotalWithTax` useMemo (lines 286-291) to branch: when `method === 'rappi'`, unchanged
    (`taxAmount: 0`, `subtotalWithTax: afterDiscount`); when `taxInclusive`, compute
    `const decomposedSubtotal = Math.round((afterDiscount / (1 + taxRatePercent / 100)) * 100) / 100;`
    then `taxAmount = Math.round((afterDiscount - decomposedSubtotal) * 100) / 100` (subtract, never
    re-derive — the rounding-discipline rule) and `subtotalWithTax = afterDiscount` (unchanged, TAX-02);
    else (exclusive, TAX-03) keep today's exact two lines unchanged
    (`taxAmount = Math.round(afterDiscount * (taxRatePercent / 100) * 100) / 100`,
    `subtotalWithTax = Math.round((afterDiscount + taxAmount) * 100) / 100`). Add `taxInclusive` to the
    memo's dependency array. Do not rename `runningTotal`/`subtotalWithTax` or touch any of their
    downstream reads (293, 308, 743-777) — this keeps the diff to the one useMemo body.

    Create `supabase/migrations/20260831000005_tax_inclusive_mode.sql` containing exactly one
    `CREATE OR REPLACE FUNCTION public.process_direct_sale_atomic(...)` statement: copy the entire
    function body verbatim from `20260831000003_bank_transfers_schema.sql` lines 380-527 (same
    17-parameter signature — `p_staff_id uuid, p_shift_id uuid, p_caja_session_id uuid, p_items jsonb,
    p_idempotency_key text, p_method text DEFAULT NULL::text, p_amount numeric DEFAULT NULL::numeric,
    p_tendered_amount numeric DEFAULT NULL::numeric, p_reference_number text DEFAULT NULL::text,
    p_legs jsonb DEFAULT NULL::jsonb, p_expected_total numeric DEFAULT NULL::numeric,
    p_discount_scope text DEFAULT NULL::text, p_discount_type text DEFAULT NULL::text,
    p_discount_value numeric DEFAULT NULL::numeric, p_discount_amount numeric DEFAULT NULL::numeric,
    p_customer_name text DEFAULT 'Walk-in'::text, p_customer_phone text DEFAULT NULL::text` — must
    match byte-for-byte, per Pitfall 1, or Postgres registers a second overload instead of replacing
    the live one), with exactly two changes: (1) in the `DECLARE` block, add `v_tax_inclusive boolean;`
    after `v_derived_total numeric;`; (2) replace the 5-line tax section (`v_subtotal := ROUND(v_subtotal, 2);`
    through `v_derived_total := ROUND(v_subtotal + v_tax, 2);`) with: keep
    `v_subtotal := ROUND(v_subtotal, 2);` as the first line, then
    `SELECT COALESCE((value->>'taxRatePercent')::numeric, 16), COALESCE((value->>'taxInclusive')::boolean, true)
    INTO v_tax_rate, v_tax_inclusive FROM settings WHERE key = 'billing';`, then
    `v_tax_rate := COALESCE(v_tax_rate, 16);`, then an `IF v_tax_inclusive THEN v_derived_total :=
    v_subtotal; v_tax := ROUND(v_subtotal - ROUND(v_subtotal / (1 + v_tax_rate / 100.0), 2), 2); ELSE
    v_tax := ROUND(v_subtotal * (v_tax_rate / 100.0), 2); v_derived_total := ROUND(v_subtotal + v_tax, 2);
    END IF;` block. The anti-tamper checks immediately after (`abs(p_amount - v_derived_total) > 0.01`
    / `abs(p_expected_total - v_derived_total) > 0.01`) are copied unchanged — no edit needed there,
    they already compare against whatever `v_derived_total` now holds.

    Create `supabase/functions/_shared/tax.ts` exporting one pure function `decomposeTax(chargedAmount:
    number, taxRatePercent: number, taxInclusive: boolean): { subtotal: number; taxAmount: number;
    total: number }`: when `taxInclusive`, `subtotal = Math.round((chargedAmount / (1 + taxRatePercent / 100)) * 100) / 100`,
    `taxAmount = Math.round((chargedAmount - subtotal) * 100) / 100` (subtract, not re-derive),
    `total = chargedAmount`; when not inclusive, `taxAmount = Math.round((chargedAmount * taxRatePercent) / (100 + taxRatePercent) * 100) / 100`,
    `subtotal = Math.round((chargedAmount - taxAmount) * 100) / 100`, `total = chargedAmount` (this
    branch backs the additive tax back out of an already-charged amount — `chargedAmount` here is
    always `process_direct_sale_atomic`'s `v_derived_total`, i.e. subtotal+tax, regardless of mode).
    No imports in this file — plain arithmetic only, so it stays importable from both the Deno runtime
    and a plain Vitest test.

    In `process-direct-sale/index.ts`'s `buildSaleReceipt`, add
    `import { decomposeTax } from '../_shared/tax.ts';` at the top of the file, mirroring the existing
    `_shared/audit.ts` import style. Rename the local `subtotal` variable (line 191) to `chargedAmount`
    (Pitfall 2 — it was never a real subtotal). Immediately after computing `chargedAmount`, add
    `const { data: billingRow } = await admin.from('settings').select('value').eq('key', 'billing').maybeSingle();`,
    then `const billing = billingRow?.value as { taxRatePercent?: number; taxInclusive?: boolean } | null;`,
    `const taxRatePercent = billing?.taxRatePercent ?? 16;`, `const taxInclusive = billing?.taxInclusive ?? true;`,
    `const { subtotal, taxAmount, total } = decomposeTax(chargedAmount, taxRatePercent, taxInclusive);`.
    Replace the returned object's `subtotal, total: subtotal,` (lines 201-202) with
    `subtotal, taxAmount, taxRatePercent, taxInclusive, total,`.

    In `edge-function-contracts.ts`'s `ReceiptDataSchema`, add three fields after `total: MoneySchema`
    (line 70): `taxAmount: MoneySchema.optional()`, `taxRatePercent: z.number().optional()`,
    `taxInclusive: z.boolean().optional()` — following the exact `discountAmount`/`tenders`
    optional-field convention already in this schema.

    In `receipt-format.ts`'s `buildThermalReceiptText` (lines 222-224), insert one new line between
    the existing `subtotal` and `total` pushes: `if (receipt.taxAmount != null) { lines.push(lineLeftRight(tr('receipt.tax'),
    formatMoneyIn(locale, receipt.taxAmount), width)); }` — reuse `formatMoneyIn` exactly as the
    neighboring lines do, no new formatting helper.

    In both `receipt.json` locale files, add `"tax": "Impuesto"` (es-MX) / `"tax": "Tax"` (en-US) as a
    sibling key to the existing `"subtotal"`/`"total"` keys inside the `receipt` object (A1 — plain,
    unambiguous wording matching the register of its neighbors).
  </action>
  <verify>
    <automated>npx vitest run src/shared/lib/__tests__/edge-tax.test.ts src/shared/lib/billing-settings.test.ts src/widgets/PaymentModal/ui/PaymentForm.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `edge-tax.test.ts` and the new `billing-settings.test.ts`/`PaymentForm.test.tsx` cases pass right
      now (they test pure functions/schema defaults, no live DB needed) — this part is GREEN already.
    - `npx playwright test e2e/checkout/tax-inclusive-mode.spec.ts` FAILS right now with the receipt's
      `total` still equal to `base_price * 1.16` (the migration has not been applied to the live
      database yet — confirms RED, not a false pass).
    - `npm run typecheck` passes (domain.ts, queries.ts, PaymentForm.tsx, edge-function-contracts.ts,
      receipt-format.ts, and the new `_shared/tax.ts`/`edge-tax.test.ts` all compile).
    - `grep -c "CREATE OR REPLACE FUNCTION public.process_direct_sale_atomic" supabase/migrations/20260831000005_tax_inclusive_mode.sql`
      equals 1.
  </acceptance_criteria>
  <done>Every file above is authored; the pure-function/schema unit tests pass; the new E2E spec exists
  and fails for the expected reason (RED — live DB still additive-only).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 [BLOCKING]: Push the migration, regenerate types, drive to GREEN</name>
  <read_first>
    .planning/phases/16-purchase-orders-reordering/16-01-PLAN.md (documented `supabase db push` /
    `docker exec supabase-db psql` fallback pattern used previously in this exact environment — reuse,
    do not re-derive);
    CLAUDE.md "Missing generated types workaround" section (the
    `npx supabase gen types typescript --local > src/shared/lib/supabase.types.ts` regeneration
    command and its documented caveats).
  </read_first>
  <files>
    src/shared/lib/supabase.types.ts
  </files>
  <action>
    Apply `supabase/migrations/20260831000005_tax_inclusive_mode.sql` to the project's Supabase
    database, preferring `supabase db push`; if that cannot reach the database, fall back to the
    documented `docker exec supabase-db psql` pattern from `16-01-PLAN.md`, applying the file and
    registering it in `supabase_migrations.schema_migrations`. Confirm with
    `SELECT version FROM supabase_migrations.schema_migrations WHERE version = '20260831000005'`.
    Verify no duplicate overload was created (Pitfall 1):
    `SELECT count(*) FROM pg_proc WHERE proname = 'process_direct_sale_atomic'` must return exactly 1.
    Regenerate `src/shared/lib/supabase.types.ts` via
    `npx supabase gen types typescript --local` (or this environment's working equivalent — see
    `16-01-SUMMARY.md` for the docker-container fallback if `--local` cannot resolve the `supabase-db`
    container from the host). Re-run Task 1's E2E spec and the full unit suite; iterate on the SQL or
    TypeScript only if a genuine defect surfaces (never by weakening an assertion) until every case
    passes — this is the GREEN move. Also confirm a pre-existing seeded `settings` row with
    `key='billing'` and no `taxInclusive` key present still resolves to `true` via the migration's
    `COALESCE(...::boolean, true)` (D-01's "no separate admin action required" — an old settings row
    predating this migration must not silently behave as exclusive-mode).
  </action>
  <verify>
    <automated>npx playwright test e2e/checkout/tax-inclusive-mode.spec.ts && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT version FROM supabase_migrations.schema_migrations WHERE version = '20260831000005'`
      returns one row.
    - `SELECT count(*) FROM pg_proc WHERE proname = 'process_direct_sale_atomic'` returns exactly `1`.
    - `npx playwright test e2e/checkout/tax-inclusive-mode.spec.ts` passes.
    - `npm run test` (full unit suite) passes.
    - `npm run typecheck` passes after `supabase.types.ts` regeneration.
  </acceptance_criteria>
  <done>Migration applied to the live database with no duplicate overload, types regenerated, and both
  the new E2E spec and the full unit suite are green (GREEN).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Authenticated staff client -> `process_direct_sale_atomic` | Untrusted `p_amount`/`p_expected_total` (computed client-side by `PaymentForm.tsx` from cached settings) crosses into a `SECURITY DEFINER` function whose anti-tamper guard is this phase's core safety net |
| `process-direct-sale` edge function -> `settings` table | Server-side read, not attacker-controlled, but a mismatched fallback default here would silently misclassify every receipt's tax mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Tampering | `process_direct_sale_atomic` anti-tamper guard vs. client formula divergence | high | mitigate | RPC re-reads `settings.billing` itself and never trusts a client-submitted mode flag (the client never sends one); both client (`PaymentForm.tsx`) and server use the identical subtotal-first-then-subtract rounding discipline; Task 1's property-based test and Task 2's E2E spec both assert `subtotal + taxAmount === total` to the cent in inclusive mode, closing RESEARCH.md Open Question 2 |
| T-24-02 | Tampering | A client submitting an amount computed under the wrong mode | medium | accept | Exclusive vs. inclusive totals differ by the full tax amount (not a rounding-scale drift), so a wrong-mode submission is rejected outright by the existing `> 0.01` tolerance, not silently accepted; residual risk is bounded to the pre-existing $0.01 anti-tamper tolerance already accepted in production for every other payment path |
| T-24-03 | Tampering | Migration signature mismatch silently creating a duplicate `process_direct_sale_atomic` overload | critical | mitigate | Task 1 copies the exact current 17-parameter signature verified this session against the live `20260831000003_bank_transfers_schema.sql`; Task 2's acceptance criteria explicitly assert `pg_proc` shows exactly one overload after the push, per Pitfall 1 |

</threat_model>

<verification>
Both tasks' `<verify>` commands must pass. Full local check before handing off to Plans 02/03:
`npm run typecheck && npm run test && npx playwright test e2e/checkout/tax-inclusive-mode.spec.ts`.
</verification>

<success_criteria>
- `BillingSettingsSchema.taxInclusive` exists, defaults to `true`, and `DEFAULT_BILLING` matches.
- `process_direct_sale_atomic` is mode-aware, exactly one overload exists in the database, and the
  anti-tamper guard accepts inclusive-mode totals unchanged from the catalog sum.
- `PaymentForm.tsx`'s checkout preview shows the correct decomposed tax in both modes.
- `process-direct-sale`'s receipts carry real `subtotal`/`taxAmount`/`total` (not `subtotal === total`).
- `buildThermalReceiptText` prints the new tax line in both es-MX and en-US.
- `e2e/checkout/tax-inclusive-mode.spec.ts` is green; `npm run test` is green.
</success_criteria>

<output>
Create `.planning/phases/24-tax-configuration-inclusive-exclusive-toggle/24-01-SUMMARY.md` when done
</output>
