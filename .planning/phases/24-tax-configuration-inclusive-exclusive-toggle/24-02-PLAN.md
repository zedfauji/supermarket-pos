---
phase: 24-tax-configuration-inclusive-exclusive-toggle
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - src/widgets/SettingsTabsPanel/tabs/BillingSettingsTab.tsx
  - src/widgets/SettingsTabsPanel/tabs/BillingSettingsTab.test.tsx
  - src/shared/lib/i18n/locales/es-MX/wAdmin.json
  - src/shared/lib/i18n/locales/en-US/wAdmin.json
autonomous: true
requirements: [TAX-01]

estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "An admin viewing Billing Settings sees a taxInclusive on/off toggle alongside the tax rate field, seeded from the live settings row."
    - "Saving Billing Settings after toggling taxInclusive persists the new value — a subsequent load reflects it, and it is never silently reset to the form's own default on save (Pitfall 6)."
  artifacts:
    - path: "src/widgets/SettingsTabsPanel/tabs/BillingSettingsTab.tsx"
      provides: "taxInclusive field threaded through BillingForm/DEFAULT_FORM/useEffect seed/save payload/JSX toggle"
  key_links:
    - from: "BillingSettingsTab.tsx save()"
      to: "updateSetting.mutateAsync({ key: 'billing', value: {...} })"
      via: "form.taxInclusive included in the wholesale value object"
      pattern: "taxInclusive: form.taxInclusive"
---

<objective>
Close TAX-01's admin-facing half: add the `taxInclusive` on/off toggle to `BillingSettingsTab.tsx`,
threaded end-to-end through the same four places `taxRatePercent` already flows through — local form
type, `DEFAULT_FORM`, the `useEffect` seed from server data, and the `save()` payload — plus a new JSX
control copying the existing `firstHourMode` two-button toggle pattern. Plan 01 already added
`taxInclusive` to `BillingSettingsSchema`/`DEFAULT_BILLING`; this plan is the thin UI layer on top,
exactly the "sibling field" pattern this file already uses three times over.

Purpose: without this plan, `taxInclusive` is only settable by hand-editing the `settings` row — no
admin can flip the store back to exclusive mode (or confirm inclusive mode is on) from the app itself,
leaving TAX-01 half-satisfied.

Output: a working, tested Billing Settings toggle; no behavior change to `taxRatePercent`/
`firstHourMode`/payment-label editing already on this tab.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-tax-configuration-inclusive-exclusive-toggle/24-CONTEXT.md
@.planning/phases/24-tax-configuration-inclusive-exclusive-toggle/24-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the taxInclusive toggle to BillingSettingsTab</name>
  <read_first>
    src/widgets/SettingsTabsPanel/tabs/BillingSettingsTab.tsx (full file, 251 lines — the exact
    `taxRatePercent`/`firstHourMode` end-to-end thread this task copies: `BillingForm` type at
    13-21, `DEFAULT_FORM` at 23-27, `useEffect` seed at 44-62, `save()` payload at 74-95, the
    `firstHourMode` two-button JSX toggle at 145-184, and the `ProtectedAction action="manage_products"`
    gate at 98-102);
    src/shared/lib/i18n/locales/es-MX/wAdmin.json lines 394-415 (`billingSettingsTab` key block —
    sibling key naming convention: `firstHourModeLabel`/`firstHourModeDescription`/`proratedLabel`/
    `fullHourLabel`).
  </read_first>
  <files>
    src/widgets/SettingsTabsPanel/tabs/BillingSettingsTab.tsx,
    src/shared/lib/i18n/locales/es-MX/wAdmin.json,
    src/shared/lib/i18n/locales/en-US/wAdmin.json
  </files>
  <action>
    Add `taxInclusive: boolean;` to the `BillingForm` type (after `firstHourMode`) and
    `taxInclusive: true` to `DEFAULT_FORM` (matching D-01's default, in case `data` hasn't loaded yet).
    In the `useEffect` seed (inside the `if (!dirty)` branch), add `taxInclusive: data.billing.taxInclusive`
    to the `setForm({...})` object. In `save()`, add `taxInclusive: form.taxInclusive` to the
    `updateSetting.mutateAsync({ key: 'billing', value: {...} })` payload object — this field MUST be
    present or every save silently resets `taxInclusive` back to whatever `DEFAULT_FORM` says
    (Pitfall 6 — the save handler upserts the whole `value` object wholesale, no partial merge).

    Add a new toggle control, placed directly below the existing tax-rate `Input` (after the closing
    `</div>` of the `settings-tax-rate` field, still inside the same `grid gap-4 md:grid-cols-2` div):
    a `Label` reading `t('billingSettingsTab.taxInclusiveLabel')`, a `POSButton` with `touchSize="large"`,
    `variant={form.taxInclusive ? 'default' : 'outline'}`, `onClick={() => { setDirty(true);
    setForm(current => ({ ...current, taxInclusive: !current.taxInclusive })); }}`, showing
    `t(form.taxInclusive ? 'billingSettingsTab.taxInclusiveOnLabel' : 'billingSettingsTab.taxInclusiveOffLabel')`,
    plus a `<p className="text-xs text-muted-foreground">` with
    `t('billingSettingsTab.taxInclusiveDescription')` underneath. Keep the section gated by the
    existing `ProtectedAction action="manage_products"` wrapper (lines 98-102) unchanged — this is a
    pre-existing gate-naming quirk shared by every field on this tab (including `taxRatePercent`
    itself); do not change it to `manage_settings` as part of this phase (RESEARCH.md Security Domain
    explicitly flags this as out of scope unless deliberately corrected, and correcting it would be an
    unrelated behavior change to the whole tab's RBAC surface, not scoped to TAX-01).

    Add four new keys under `billingSettingsTab` in both `wAdmin.json` locale files, sibling to
    `firstHourModeLabel`/`firstHourModeDescription`: `taxInclusiveLabel` ("Impuesto incluido en
    precios" / "Tax included in prices"), `taxInclusiveDescription` ("Activa esto si los precios en
    el catálogo ya incluyen el IVA (recomendado)." / "Turn this on if catalog prices already include
    tax (recommended)."), `taxInclusiveOnLabel` ("Incluido" / "Included"), `taxInclusiveOffLabel`
    ("Se agrega al pagar" / "Added at checkout").
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint -- --max-warnings=0 src/widgets/SettingsTabsPanel/tabs/BillingSettingsTab.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes (no `BillingForm`/`DEFAULT_FORM` shape mismatch).
    - `grep -c "taxInclusive" src/widgets/SettingsTabsPanel/tabs/BillingSettingsTab.tsx` is at least 5
      (type field, default, seed, save payload, JSX toggle onClick reference).
    - `npx tsc --noEmit` reports no missing i18n key usage (all four new keys referenced exist in both
      locale JSON files — verified by `npm run lint` catching an `i18next` missing-key issue if any).
  </acceptance_criteria>
  <done>The toggle renders, reflects live settings data, and its state is included in every save
  payload alongside `taxRatePercent`/`firstHourMode`/`paymentMethods`.</done>
</task>

<task type="auto">
  <name>Task 2: Test the toggle persists and never gets silently dropped from a save</name>
  <read_first>
    src/widgets/SettingsTabsPanel/tabs/BillingSettingsTab.test.tsx (full file, 117 lines — the exact
    `mockSettingsData`/`mutateAsyncMock` mock shape and the `Full Hour` toggle test at lines 70-85 to
    mirror for `taxInclusive`).
  </read_first>
  <files>
    src/widgets/SettingsTabsPanel/tabs/BillingSettingsTab.test.tsx
  </files>
  <action>
    Add `taxInclusive: true` to `mockSettingsData.billing` (the `vi.hoisted` mock object) so the
    component's `useEffect` seed has something real to read. Add three new `it` blocks: (1) renders a
    button whose accessible name matches `/Included|Tax included/i` when `mockSettingsData.billing.taxInclusive`
    is `true` (mirrors the existing "renders Prorated and Full Hour option buttons" test at lines 63-68);
    (2) clicking the toggle then clicking "Save Billing" calls `mutateAsyncMock` with
    `expect.objectContaining({ key: 'billing', value: expect.objectContaining({ taxInclusive: false }) })`
    (mirrors the `firstHourMode` save-payload test at lines 70-85 — this is the Pitfall 6 regression
    guard: if a future edit ever drops `taxInclusive` from the save payload, this test catches it);
    (3) toggling twice (on then off then on) and saving calls the mutation with `taxInclusive: true`
    (mirrors the toggle-back-and-forth test at lines 87-103).
  </action>
  <verify>
    <automated>npx vitest run src/widgets/SettingsTabsPanel/tabs/BillingSettingsTab.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All existing tests in this file still pass (no regression to `firstHourMode`/`taxRatePercent`
      coverage).
    - The 3 new test cases pass, and the save-payload assertion explicitly checks `taxInclusive` is
      present with the expected value (not just that the mutation was called).
  </acceptance_criteria>
  <done>`npx vitest run src/widgets/SettingsTabsPanel/tabs/BillingSettingsTab.test.tsx` is fully green,
  including the new taxInclusive-persistence and save-payload-completeness cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin-role browser client -> `settings` table (`key='billing'`) | Write gated client-side by `ProtectedAction action="manage_products"`; the write itself goes through `useMutationUpdateSetting`, not a new RPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-04 | Tampering | `BillingSettingsTab.save()` wholesale value overwrite | medium | mitigate | `taxInclusive` is included in the save payload from this plan's Task 1 onward, and Task 2's dedicated test asserts the field is always present in the mutation call — regressing this (dropping the field from a future edit) would silently flip live checkout behavior back to whatever `DEFAULT_FORM` says, which the new test catches |
| T-24-05 | Elevation of Privilege | `ProtectedAction action="manage_products"` gating a billing/tax field | low | accept | Pre-existing gate-naming mismatch (should arguably be `manage_settings` per CLAUDE.md's general Settings-page description) shared by the already-shipped `taxRatePercent` field on this same tab; RESEARCH.md's Security Domain section explicitly scopes correcting it out of this phase — changing it here would alter RBAC behavior for the whole tab, not just the new field |

</threat_model>

<verification>
`npx vitest run src/widgets/SettingsTabsPanel/tabs/BillingSettingsTab.test.tsx && npm run typecheck && npm run lint`
</verification>

<success_criteria>
- Billing Settings shows a working taxInclusive toggle seeded from live data.
- Every save includes `taxInclusive` in the mutation payload.
- `BillingSettingsTab.test.tsx` is fully green with the 3 new cases.
</success_criteria>

<output>
Create `.planning/phases/24-tax-configuration-inclusive-exclusive-toggle/24-02-SUMMARY.md` when done
</output>
