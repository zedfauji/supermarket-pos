---
phase: 24-tax-configuration-inclusive-exclusive-toggle
plan: 04
type: execute
wave: 3
depends_on: ["24-01", "24-03"]
files_modified:
  - e2e/helpers/tax.ts
  - e2e/checkout/atomic-rpc-guards.spec.ts
  - e2e/checkout/happy-path.spec.ts
  - e2e/soak/full-day-soak.spec.ts
  - e2e/tabs/reopen-closed-ticket.spec.ts
  - e2e/reports/report-tabs.spec.ts
  - e2e/inventory/loose-weight-hold-sale.spec.ts
  - e2e/infra/offline.spec.ts
  - e2e/receipts/reprint.spec.ts
  - src/shared/lib/receipt-format.test.ts
  - src/shared/lib/exporters/receipt-pdf.test.ts
  - src/shared/lib/email-receipt.test.ts
  - src/features/process-payment/ui/ReceiptPreview.test.tsx
  - src/features/process-payment/ui/ReceiptPreview.stories.tsx
  - src/widgets/PaymentModal/PaymentModal.test.tsx
autonomous: true
requirements: [TAX-02, TAX-03, TAX-04, TAX-05]

estimate:
  tokens: 60000
  raw_tokens: 120000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "No e2e spec anywhere in the repo computes an expected checkout total using a hardcoded additive-only formula or a *1.16 literal — every one reads the live taxInclusive/taxRatePercent settings and branches, so D-01's default flip does not silently break the suite."
    - "Every unit-test fixture that hand-builds a ReceiptData object still parses against ReceiptDataSchema and, where it asserts on rendered receipt text, reflects a real subtotal/tax/total relationship rather than degenerate subtotal===total."
    - "npm run test and the full set of tax-affected Playwright specs pass together, proving the phase's mode-aware tax fix does not regress any existing scenario."
  artifacts:
    - path: "e2e/helpers/tax.ts"
      provides: "getBillingTaxConfig(admin) + computeAuthoritativeTotal(subtotal, taxRatePercent, taxInclusive) — single shared, mode-aware helper replacing 8 duplicated/hardcoded copies"
  key_links:
    - from: "e2e/checkout/atomic-rpc-guards.spec.ts (and 7 sibling specs)"
      to: "e2e/helpers/tax.ts"
      via: "import { computeAuthoritativeTotal, getBillingTaxConfig } from '../helpers/tax'"
      pattern: "from '\\.\\./helpers/tax'"
---

<objective>
Close RESEARCH.md's Pitfall 3 regression risk across its full actual blast radius. RESEARCH.md
identified 6 e2e specs hand-rolling an additive-only `computeAuthoritativeTotal`; a direct
`grep -rl "\* 1\.16\|computeAuthoritativeTotal" e2e/` run during this phase's planning found **8**
files, not 6 — `e2e/checkout/happy-path.spec.ts` (lines 49, 211) and `e2e/receipts/reprint.spec.ts`
(line 70) also hardcode the same `* 1.16` literal and were missing from RESEARCH.md's list. All 8 are
in scope for this plan. Every one of these specs will assert the wrong expected total the moment
`taxInclusive` defaults to `true` (Plan 01's migration, already live after Wave 1) unless updated here.
This plan also closes RESEARCH.md's Pitfall 4/5/6 unit-test fixture gaps: several files hand-build
`ReceiptData`/settings-mock objects assuming `subtotal === total` or `taxRatePercent: 0`, both
degenerate for the new mode-aware code paths Plans 01 and 03 shipped.

Purpose: this is the phase's regression backstop — without it, the tax fix from Plans 01-03 ships
correct production behavior but leaves the test suite red (or worse, silently green on wrong
assertions), which this repo's CLAUDE.md testing policy treats as equivalent to the feature being
unverified.

Output: one shared `e2e/helpers/tax.ts`; 8 specs re-pointed at it; 6 unit-test fixture files updated;
a documented full-suite green run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-tax-configuration-inclusive-exclusive-toggle/24-RESEARCH.md
@.planning/phases/24-tax-configuration-inclusive-exclusive-toggle/24-PATTERNS.md
@.planning/phases/24-tax-configuration-inclusive-exclusive-toggle/24-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract e2e/helpers/tax.ts, re-point all 8 affected specs</name>
  <read_first>
    e2e/checkout/atomic-rpc-guards.spec.ts lines 1-40 (the exact `getTaxRatePercent`/
    `computeAuthoritativeTotal` helper duplicated across specs, and its call sites throughout the
    file — also lines 123 and 644 which hardcode `Math.round(Number(product.base_price) * 1.16 * 100) / 100`
    directly, bypassing the helper);
    e2e/checkout/happy-path.spec.ts lines 49 and 211 (two more `* 1.16` literals, not covered by
    RESEARCH.md's original file list — confirmed via a fresh grep this session);
    e2e/receipts/reprint.spec.ts line 70 (a third previously-missed `* 1.16` literal);
    e2e/helpers/auth.ts (the shared-helper-file precedent this task's new `e2e/helpers/tax.ts`
    mirrors — `import { loginAs } from '../helpers/auth'` is the exact import style to reuse).
  </read_first>
  <files>
    e2e/helpers/tax.ts,
    e2e/checkout/atomic-rpc-guards.spec.ts,
    e2e/checkout/happy-path.spec.ts,
    e2e/soak/full-day-soak.spec.ts,
    e2e/tabs/reopen-closed-ticket.spec.ts,
    e2e/reports/report-tabs.spec.ts,
    e2e/inventory/loose-weight-hold-sale.spec.ts,
    e2e/infra/offline.spec.ts,
    e2e/receipts/reprint.spec.ts
  </files>
  <action>
    Create `e2e/helpers/tax.ts` exporting `getBillingTaxConfig(admin: SupabaseClient): Promise<{
    taxRatePercent: number; taxInclusive: boolean }>` (reads `settings` where `key = 'billing'`,
    falls back to `{ taxRatePercent: 16, taxInclusive: true }` when the row or either field is
    missing — matching the migration's own `COALESCE(..., true)` default) and
    `computeAuthoritativeTotal(subtotal: number, taxRatePercent: number, taxInclusive: boolean): number`
    (when `taxInclusive`, returns `subtotal` unchanged — the catalog price sum IS the charged total,
    TAX-02; when not, returns the existing `Math.round((subtotal + Math.round(subtotal * (taxRatePercent / 100) * 100) / 100) * 100) / 100`
    additive formula, byte-identical to today's behavior, TAX-03).

    In each of the 8 files listed, run this exact three-step replacement: (1) delete the file's local
    `getTaxRatePercent`/`computeAuthoritativeTotal` function definitions (where present) and add
    `import { computeAuthoritativeTotal, getBillingTaxConfig } from '../helpers/tax';` (adjust the
    relative path depth per the file's folder — `../../helpers/tax` for files nested one level deeper
    than `e2e/<domain>/`, verify per file); (2) at every call site that previously did
    `const taxRatePercent = await getTaxRatePercent(admin); const total = computeAuthoritativeTotal(subtotal, taxRatePercent);`,
    change to `const { taxRatePercent, taxInclusive } = await getBillingTaxConfig(admin); const total = computeAuthoritativeTotal(subtotal, taxRatePercent, taxInclusive);`;
    (3) at every bare `Math.round(Number(product.base_price) * 1.16 * 100) / 100` literal (confirmed
    at `atomic-rpc-guards.spec.ts:123,644`, `happy-path.spec.ts:49,211`, `reprint.spec.ts:70`), replace
    with `computeAuthoritativeTotal(Number(product.base_price), (await getBillingTaxConfig(getServiceClient())).taxRatePercent, (await getBillingTaxConfig(getServiceClient())).taxInclusive)`
    or, preferably, hoist one `const { taxRatePercent, taxInclusive } = await getBillingTaxConfig(admin);`
    near the top of that test body and reuse it at both call sites in the same test — do not call
    `getBillingTaxConfig` twice per assertion when a single hoisted call covers both.

    After all 8 files are updated, run `grep -rn "\* 1\.16\|function computeAuthoritativeTotal\|function getTaxRatePercent" e2e/`
    and confirm the only remaining hits are inside `e2e/helpers/tax.ts` itself — this is the
    completeness gate proving no 9th copy was missed.
  </action>
  <verify>
    <automated>grep -rln "\* 1\.16\|function computeAuthoritativeTotal\|function getTaxRatePercent" e2e/ | grep -v "e2e/helpers/tax.ts"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command above returns empty output (no file outside `e2e/helpers/tax.ts` still
      defines or hardcodes the old formula).
    - `npx playwright test e2e/checkout/atomic-rpc-guards.spec.ts e2e/checkout/happy-path.spec.ts e2e/receipts/reprint.spec.ts`
      passes against the now-live `taxInclusive=true` default (Plan 01's migration).
  </acceptance_criteria>
  <done>One shared, mode-aware e2e tax helper exists; all 8 previously-hardcoded specs are re-pointed
  at it and pass under the new default.</done>
</task>

<task type="auto">
  <name>Task 2: Run the remaining 5 affected specs green, update unit-test fixtures</name>
  <read_first>
    src/shared/lib/receipt-format.test.ts (full file — the ~dozen fixtures at lines 23-24, 67,
    107-108, 155-156, 446, 477, 494, 510 hardcoding `subtotal: N, total: N` identical values, per
    Pitfall 4);
    src/widgets/PaymentModal/PaymentModal.test.tsx lines 25-40 (the `taxRatePercent: 0` settings mock,
    per Pitfall 5);
    src/shared/lib/edge-function-contracts.ts (the `ReceiptDataSchema` Plan 01 already extended with
    optional `taxAmount`/`taxRatePercent`/`taxInclusive` — these fixtures parse against it).
  </read_first>
  <files>
    src/shared/lib/receipt-format.test.ts,
    src/shared/lib/exporters/receipt-pdf.test.ts,
    src/shared/lib/email-receipt.test.ts,
    src/features/process-payment/ui/ReceiptPreview.test.tsx,
    src/features/process-payment/ui/ReceiptPreview.stories.tsx,
    src/widgets/PaymentModal/PaymentModal.test.tsx
  </files>
  <action>
    First run `npx playwright test e2e/soak/full-day-soak.spec.ts e2e/tabs/reopen-closed-ticket.spec.ts e2e/reports/report-tabs.spec.ts e2e/inventory/loose-weight-hold-sale.spec.ts e2e/infra/offline.spec.ts`
    (the remaining 5 of Task 1's 8 re-pointed specs, not yet run in this plan) and fix any residual
    failure by correcting the specific assertion (never by weakening it) until all 5 pass.

    In `receipt-format.test.ts`, for at least 3 of the existing `subtotal: N, total: N` fixtures
    (picking a representative spread across the file, not necessarily every one — this is a
    regression-safety pass, not a rewrite), add `taxAmount: <N * 0.16/1.16 decomposed value>` and
    change `total` to the pre-tax `subtotal` plus that `taxAmount` where the fixture's intent is an
    inclusive-mode sale (matching the new default), OR leave `subtotal === total` fixtures with
    `taxAmount` omitted entirely where the test's actual intent is unrelated to tax rendering (e.g.
    testing item-line formatting) — `buildThermalReceiptText`'s new tax line only renders `when
    receipt.taxAmount != null`, so omitting it is a valid, schema-legal way to keep an unrelated
    fixture unchanged. Add one new dedicated test asserting: given `receipt.taxAmount = 16` and
    `receipt.subtotal = 100`, the rendered thermal text contains a line with `formatMoneyIn`'s
    rendering of `16` positioned between the subtotal and total lines.

    In `PaymentModal.test.tsx`, add `taxInclusive: true` (or `false` — value is irrelevant at
    `taxRatePercent: 0`, per Pitfall 5's own reasoning, but the field must be present since it is now
    a required read in `PaymentForm.tsx`) to the settings mock object at lines 30-35.

    In `receipt-pdf.test.ts`, `email-receipt.test.ts`, `ReceiptPreview.test.tsx`, and
    `ReceiptPreview.stories.tsx`, add `taxInclusive`/`taxAmount`/`taxRatePercent` fields to any
    hand-built `ReceiptData`/settings mock object that currently omits them, following the same
    reasoning as `receipt-format.test.ts` above — only where the fixture's own assertions would
    otherwise exercise a stale, tax-line-less code path.
  </action>
  <verify>
    <automated>npx playwright test e2e/soak/full-day-soak.spec.ts e2e/tabs/reopen-closed-ticket.spec.ts e2e/reports/report-tabs.spec.ts e2e/inventory/loose-weight-hold-sale.spec.ts e2e/infra/offline.spec.ts && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - All 5 remaining Playwright specs pass under the live `taxInclusive=true` default.
    - `npm run test` (full unit suite) passes, including the new dedicated tax-line rendering
      assertion in `receipt-format.test.ts`.
  </acceptance_criteria>
  <done>Every e2e spec touched by Pitfall 3 and every unit-test fixture touched by Pitfalls 4-6 is
  updated and green.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite phase-gate proof</name>
  <read_first>
    .planning/phases/24-tax-configuration-inclusive-exclusive-toggle/24-VALIDATION.md (Sampling
    Rate / Phase Requirements -> Test Map sections — the exact commands this task's verify block
    must satisfy).
  </read_first>
  <files>
  </files>
  <action>
    Run `npm run typecheck`, `npm run lint`, `npm run test` (full unit suite), and
    `npx playwright test e2e/checkout/ e2e/receipts/ e2e/payments/ e2e/tabs/reopen-closed-ticket.spec.ts e2e/soak/full-day-soak.spec.ts e2e/reports/report-tabs.spec.ts e2e/inventory/loose-weight-hold-sale.spec.ts e2e/infra/offline.spec.ts`
    (every folder/spec this phase's must_haves across all 4 plans touch, per VALIDATION.md's
    phase-gate sampling rate) as one combined pass. If anything fails, diagnose the specific root
    cause (not a workaround) and fix it in the file it actually belongs to — do not patch the test
    to hide a real regression. Record the final green run's command output in this plan's SUMMARY.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run test && npx playwright test e2e/checkout/ e2e/receipts/ e2e/payments/ e2e/tabs/reopen-closed-ticket.spec.ts e2e/soak/full-day-soak.spec.ts e2e/reports/report-tabs.spec.ts e2e/inventory/loose-weight-hold-sale.spec.ts e2e/infra/offline.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint`, and `npm run test` all pass with zero warnings/errors.
    - Every Playwright spec in the combined command above passes in the same run.
    - `grep -rln "\* 1\.16\|function computeAuthoritativeTotal\|function getTaxRatePercent" e2e/ | grep -v "e2e/helpers/tax.ts"`
      still returns empty (no regression to Task 1's completeness gate).
  </acceptance_criteria>
  <done>The full combined verification command passes in one run — this phase's mode-aware tax fix
  (Plans 01-04) is proven correct and non-regressive across the entire affected test surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test-only changes — no new production trust boundary is introduced by this plan | All files touched are `*.test.ts`/`*.spec.ts`/`*.stories.tsx` or a new `e2e/helpers/tax.ts` test utility |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-08 | Tampering (test-formula drift masking a real regression) | `e2e/helpers/tax.ts` vs. production `_shared/tax.ts`/SQL formula | medium | mitigate | Extracting one shared, mode-aware e2e helper (instead of leaving 8 independent hardcoded copies, 2 of which RESEARCH.md itself missed) removes the specific failure mode where a future tax-rate or mode change is applied to production but only some of 8 scattered test copies are updated, letting a real overcharge regression pass CI silently in whichever spec was missed |
| T-24-09 | Repudiation (a fixture assertion silently weakened to force a pass) | Task 2/3 fixture updates | low | accept | Explicit instruction in both tasks: fix the real root cause and never patch an assertion to hide a genuine regression; enforced by human code review of the diff, no automated gate can fully prevent this class of shortcut |

</threat_model>

<verification>
`npm run typecheck && npm run lint && npm run test && npx playwright test e2e/checkout/ e2e/receipts/ e2e/payments/ e2e/tabs/ e2e/soak/ e2e/reports/report-tabs.spec.ts e2e/inventory/loose-weight-hold-sale.spec.ts e2e/infra/offline.spec.ts`
</verification>

<success_criteria>
- Zero e2e specs hardcode an additive-only or `* 1.16` tax formula anywhere outside `e2e/helpers/tax.ts`.
- All unit-test fixtures touched by Pitfalls 4-6 parse and assert correctly against the new schema shape.
- The full combined typecheck/lint/unit/e2e verification command passes in one run.
</success_criteria>

<output>
Create `.planning/phases/24-tax-configuration-inclusive-exclusive-toggle/24-04-SUMMARY.md` when done
</output>
