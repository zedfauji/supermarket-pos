---
phase: 26-multi-customer-deployment
plan: 04
type: execute
wave: 4
depends_on: ["26-03"]
files_modified:
  - customers/taj-house-of-spices/tauri.override.json
  - customers/taj-house-of-spices/icons/32x32.png
  - customers/taj-house-of-spices/icons/128x128.png
  - customers/taj-house-of-spices/icons/128x128@2x.png
  - customers/taj-house-of-spices/icons/icon.icns
  - customers/taj-house-of-spices/icons/icon.ico
  - customers/customers.json
  - scripts/verify-installer-integrity.ps1
autonomous: false
requirements: [D-15, D-16, D-17]

estimate:
  tokens: 22500
  raw_tokens: 45000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "customers/taj-house-of-spices/tauri.override.json's identifier/publisher/updater-endpoint values are byte-identical to what is hardcoded in src-tauri/tauri.conf.json today — no regeneration, no reformatting (D-16)."
    - "A real, signed installer built through the new fan-out mechanism (Plan 26-02) for Taj is produced and verified, while src-tauri/tauri.conf.json and release.yml's original publish-tauri job remain completely untouched — the old path still works exactly as before (D-17, parallel-run)."
  artifacts:
    - customers/taj-house-of-spices/tauri.override.json (byte-identical identity values)
    - customers/taj-house-of-spices/icons/* (existing icon set, reused verbatim)
    - customers/customers.json (Taj registered as an active entry with github_environment taj-house-of-spices, reusing Plan 26-01's Environment)
    - scripts/verify-installer-integrity.ps1 Check 6 (identifier/publisher/endpoint byte-for-byte comparison)
  key_links:
    - "Taj's customers.json entry's github_environment field points at the SAME 'taj-house-of-spices' Environment Plan 26-01 already created for the .env.production secrets migration — Plan 26-02's sync-customers job resolves CUSTOMER_MIRROR_PAT and VITE_SUPABASE_ANON_KEY from that one Environment, not a second one."
    - "verify-installer-integrity.ps1's new Check 6 reads customers/taj-house-of-spices/tauri.override.json and compares against the exact strings still live in src-tauri/tauri.conf.json at this point in the phase (not yet stripped — that is Plan 26-05)."
---

<objective>
Retrofit Taj House of Spices — the one real existing customer — into the new mechanism Plan 26-02
proved, without touching anything that keeps their currently-installed app working today. Create
`customers/taj-house-of-spices/tauri.override.json` and icon set with Taj's real identity values
copied byte-for-byte from `src-tauri/tauri.conf.json`, register Taj as an active `customers.json`
entry reusing Plan 26-01's `taj-house-of-spices` GitHub Environment, and produce a real signed
installer through the NEW fan-out path — while leaving `src-tauri/tauri.conf.json` and `release.yml`'s
original `publish-tauri` job completely untouched (D-17's parallel-run requirement: "core's existing
release.yml keeps working as today's fallback").

Purpose: D-16 is one-way for Taj's already-installed clients — their `identifier` determines the
Windows install location/registry keys and the updater's trust chain; getting this wrong forces every
Taj till to be reinstalled rather than auto-updated. This plan is gated by an explicit checkpoint
before touching anything identity-related, and proves the new path produces the SAME real values
Taj's install already trusts before Plan 26-05 does anything irreversible to the old path.
Output: A real signed Taj installer built through the new fan-out mechanism, verified byte-for-byte
identical in identity to today's build, with the old direct-from-core path still fully intact.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-multi-customer-deployment/26-CONTEXT.md
@.planning/phases/26-multi-customer-deployment/26-RESEARCH.md
@.planning/phases/26-multi-customer-deployment/26-PATTERNS.md
@src-tauri/tauri.conf.json
@scripts/verify-installer-integrity.ps1
@customers/customers.json
@customers/test-customer/tauri.override.json
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm D-16 before creating Taj's override file (one-way for already-installed clients)</name>
  <decision>
    Proceed with copying Taj's current `identifier` (`com.tajhouseofspices.supermarketpos`),
    `bundle.publisher` (`Taj House of Spice Supermarket POS`), and `plugins.updater.endpoints`
    (`https://github.com/zedfauji/supermarket-pos/releases/latest/download/latest.json`) verbatim from
    `src-tauri/tauri.conf.json` into `customers/taj-house-of-spices/tauri.override.json` — byte-for-
    byte, no new value invented — or stop first if there is reason to believe these values are stale?
  </decision>
  <context>
    D-15/D-16 (CONTEXT.md, locked, `Reversibility: one-way` for D-16). CONTEXT.md rates this one-way
    for Taj's already-installed clients: `identifier` determines the Windows install location/registry
    keys and the updater's trust chain, so a wrong value here would force every live Taj till to be
    reinstalled, not updated in place. Values confirmed present in `src-tauri/tauri.conf.json` at lines
    5 (`identifier`), 32 (`bundle.publisher`), 54 (`plugins.updater.endpoints[0]`) as of this session's
    RESEARCH.md/PATTERNS.md read.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed with the byte-identical copy</name>
      <pros>Values were read directly from the live tauri.conf.json this session and match
      PATTERNS.md's citation exactly; preserves Taj's live install per D-16.</pros>
      <cons>None identified — this is the only correct path per the locked decision.</cons>
    </option>
    <option id="stop">
      <name>Stop and re-verify the source values first</name>
      <pros>Guards against acting on stale values if `tauri.conf.json` changed since this session's
      read.</pros>
      <cons>Unnecessary delay if the file is unchanged, which `git diff` can confirm in seconds.</cons>
    </option>
  </options>
  <resume-signal>Type "proceed" to create the override file with the values this task reads directly
  from tauri.conf.json, or describe why the source values need re-verification first.</resume-signal>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Create Taj's override file + icons, register in customers.json, prove byte-identical via a new verify-installer-integrity.ps1 check</name>
  <files>
    customers/taj-house-of-spices/tauri.override.json, customers/taj-house-of-spices/icons/32x32.png,
    customers/taj-house-of-spices/icons/128x128.png, customers/taj-house-of-spices/icons/128x128@2x.png,
    customers/taj-house-of-spices/icons/icon.icns, customers/taj-house-of-spices/icons/icon.ico,
    customers/customers.json, scripts/verify-installer-integrity.ps1
  </files>
  <read_first>
    - src-tauri/tauri.conf.json (full file — the exact source-of-truth values for this task, read
      directly, never from a cached memory of an earlier session)
    - scripts/verify-installer-integrity.ps1 lines 78-93 (Check 2's exact fail-fast-per-check shape —
      Check 6 in this task follows the identical pattern: read, compare, `Fail` with a specific
      mismatch message, `Write-Host "OK: ..."` on match)
    - customers/test-customer/tauri.override.json (Plan 26-02's proven field shape — Taj's override
      uses the exact same four top-level keys: identifier, bundle.publisher,
      plugins.updater.endpoints, bundle.icon)
    - .planning/phases/26-multi-customer-deployment/26-PATTERNS.md "customers/taj-house-of-spices/
      tauri.override.json" section (the exact JSON shape already drafted against the live config)
  </read_first>
  <behavior>
    - Test (Check 6 in verify-installer-integrity.ps1): given `-TauriOverridePath` (defaults to
      `customers/taj-house-of-spices/tauri.override.json`) and the hardcoded expected values
      (`com.tajhouseofspices.supermarketpos`, `Taj House of Spice Supermarket POS`,
      `https://github.com/zedfauji/supermarket-pos/releases/latest/download/latest.json`), reads the
      override file via `ConvertFrom-Json` and `Fail`s with a specific mismatch message naming the
      field if `.identifier`, `.bundle.publisher`, or `.plugins.updater.endpoints[0]` differ from the
      expected value; `Write-Host "OK: ..."` on a full match.
    - RED: run the script (or a scoped invocation of just Check 6) before
      `customers/taj-house-of-spices/tauri.override.json` exists — it must `Fail` with "file not
      found" or an equivalent specific error, not a silent pass.
    - GREEN: create the override file with the correct byte-identical values — re-run, Check 6 passes.
  </behavior>
  <action>
    Add Check 6 to `scripts/verify-installer-integrity.ps1`, positioned after the existing Check 5
    (hooks.nsh check), following the exact fail-fast-per-check shape of Check 2 (lines 78-93): read
    `customers/taj-house-of-spices/tauri.override.json` via `Get-Content -Raw | ConvertFrom-Json`
    (`Fail` if the file doesn't exist), compare `.identifier` against
    `"com.tajhouseofspices.supermarketpos"`, `.bundle.publisher` against `"Taj House of Spice
    Supermarket POS"`, and `.plugins.updater.endpoints[0]` against
    `"https://github.com/zedfauji/supermarket-pos/releases/latest/download/latest.json"` — `Fail` with
    a specific "field X mismatch: got 'Y', expected 'Z'" message on the FIRST field that differs, one
    `Write-Host "OK: ..."` on full match. Create
    `customers/taj-house-of-spices/tauri.override.json` with exactly those three values plus
    `bundle.icon` listing `customers/taj-house-of-spices/icons/32x32.png`,
    `customers/taj-house-of-spices/icons/128x128.png`,
    `customers/taj-house-of-spices/icons/128x128@2x.png`,
    `customers/taj-house-of-spices/icons/icon.icns`, `customers/taj-house-of-spices/icons/icon.ico`
    (same 5-entry shape as `src-tauri/tauri.conf.json` lines 33-39) — copy the 5 files from
    `src-tauri/icons/` into `customers/taj-house-of-spices/icons/` verbatim (Taj has no distinct
    branding beyond the app's existing icon set; D-07 only requires the icons LIVE per-customer, not
    that they differ visually yet). Register Taj in `customers/customers.json` (append, do not remove
    the retained `test-customer` fixture from Plan 26-02): `name`: "taj-house-of-spices", `repo`:
    "zedfauji/supermarket-pos-taj", `status`: "active", `supabase_project_ref`:
    "mkvinyekkyennyegfoxq" (the live remote project ref, confirmed in
    `scripts/deploy-remote-backend.ps1` line 61), `deployment_mode`: "cloud", `github_environment`:
    "taj-house-of-spices" (reusing Plan 26-01's Environment — do NOT create a second Environment; add
    `CUSTOMER_MIRROR_PAT` as a new secret on that SAME Environment alongside the existing
    `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` secrets from Plan 26-01, minting a fine-grained PAT
    scoped to `contents: write` on a new `zedfauji/supermarket-pos-taj` repo you create `--private` via
    `gh repo create`). Do NOT modify `src-tauri/tauri.conf.json` or remove/alter the existing
    `publish-tauri` job in `release.yml` in this task — both must remain byte-for-byte unchanged so
    the old direct-from-core path keeps working exactly as it does today (D-17). Trigger a real
    `workflow_dispatch` of the `sync-customers` fan-out job (Plan 26-02) scoped to just the
    `taj-house-of-spices` matrix entry (or confirm it runs for all active entries including the
    retained suspended `test-customer`, which the fan-out already correctly skips) — confirm the run
    produces: (a) `zedfauji/supermarket-pos-taj`'s `main` branch matching core's HEAD SHA, (b) a real
    signed installer whose Authenticode signature thumbprint matches that run's freshly-generated cert
    (same check as `verify-installer-integrity.ps1` Check 2), and (c) `scripts/verify-installer-
    integrity.ps1` (including the new Check 6) passing in full against that installer, alongside its
    Check 4 (baked remote Supabase project ref) confirming the build used `mkvinyekkyennyegfoxq`
    correctly derived from `customers.json`'s `supabase_project_ref`, not a stale/local value.
  </action>
  <acceptance_criteria>
    - `verify-installer-integrity.ps1`'s Check 6 fails specifically (not generically) when run against
      a missing override file, and passes only when all three identity fields match exactly.
    - `customers/taj-house-of-spices/tauri.override.json`'s three identity fields are character-for-
      character identical to `src-tauri/tauri.conf.json`'s current values (diffed, not eyeballed).
    - `src-tauri/tauri.conf.json` has zero diff after this task (git diff on that specific file is
      empty).
    - `release.yml`'s `publish-tauri` job has zero diff after this task.
    - A real `workflow_dispatch` run produces a real signed installer for Taj through the NEW path,
      passing all 6 checks in `verify-installer-integrity.ps1`.
  </acceptance_criteria>
  <verify>
    <automated>git diff --stat src-tauri/tauri.conf.json .github/workflows/release.yml</automated>
    <fails_when>output is non-empty (either file shows a diff — both must remain byte-for-byte unchanged this task)</fails_when>
  </verify>
  <verify>
    <automated>pwsh -File scripts/verify-installer-integrity.ps1 -InstallerPath &lt;path-to-the-real-workflow_dispatch-built-Taj-installer&gt; -ExpectedThumbprint &lt;that-run's-cert-thumbprint&gt;</automated>
    <fails_when>non-zero exit, or output does not end with the literal "All checks passed" line</fails_when>
  </verify>
  <done>Taj's override file and icons exist with byte-identical values, Taj is registered active in customers.json reusing the existing GitHub Environment, a real signed installer built through the new path passes every check including the new Check 6, and both src-tauri/tauri.conf.json and the old publish-tauri job remain completely untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core repo (public) → Taj's mirrored repo | `git push --mirror` (Plan 26-02's mechanism) copies core's ENTIRE tree, including every customer's override file under `customers/`, into Taj's own repo. |
| `src-tauri/tauri.conf.json` (unchanged, real values) ↔ old `publish-tauri` job | This task deliberately leaves both untouched so the fallback path keeps producing correct Taj builds throughout the phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-10 | Information Disclosure | Every customer's real identity/publisher/updater-endpoint strings (including Taj's, created this task) live in `customers/<name>/tauri.override.json` in core's tree, and D-01's full-mirror pushes that ENTIRE tree — including every OTHER customer's override file — into each customer's own repo. Core is currently a PUBLIC repo (confirmed `gh api` this session), and if a customer repo (e.g. `supermarket-pos-taj`) is also public, that customer's repo audience can see every other customer's business-identity strings too. | medium | mitigate | `onboard-customer.ps1` (Plan 26-03) already defaults new customer repos to `gh repo create --private`; this task creates `zedfauji/supermarket-pos-taj` the same way (`--private`), not public. Core's own public status is a pre-existing condition not newly introduced by this phase — documented here as a residual exposure and flagged in `docs/onboarding-new-customer.md` (Plan 26-03) as a recommendation to move core private before a real customer #2 onboards, rather than silently left unaddressed. Not disposition `high`/blocking: the exposed values (identifier string, publisher name, a GitHub URL) are not credentials — the updater's actual security boundary is the Ed25519 signature/pubkey pair (RESEARCH.md ASVS V6), which this exposure does not touch. |
| T-26-11 | Tampering | An incorrect `identifier`/`publisher`/`updater.endpoints` value shipped to Taj breaks their live, already-installed production till — forces a manual reinstall instead of an in-place update | high | mitigate | D-16's byte-for-byte preservation requirement, enforced by the checkpoint:decision gate before this task and by the new Check 6 in `verify-installer-integrity.ps1` (RED/GREEN-proven, runs against every future build before it ships) — never a manual eyeball comparison. |

</threat_model>

<verification>
Both `<verify>` blocks confirm: (1) the old path's files are provably untouched (`git diff --stat`
empty), and (2) a real signed installer built through the new path passes the full, now-6-check
`verify-installer-integrity.ps1` — the same script Phase 20 already uses as this repo's pre-ship gate,
extended rather than duplicated.
</verification>

<success_criteria>
- Taj's override file/icons exist with byte-identical identity values, proven by an automated check
  (Check 6), not a manual read-through.
- `customers.json` lists Taj as active, reusing Plan 26-01's Environment for both its interim secrets
  and the new mirror-push PAT.
- A real signed Taj installer exists, built entirely through the new fan-out mechanism, with the old
  direct-from-core path still fully intact and unmodified.
</success_criteria>

<output>
Create `.planning/phases/26-multi-customer-deployment/26-04-SUMMARY.md` when done
</output>
