---
phase: 26-multi-customer-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
autonomous: true
requirements: [D-18]

estimate:
  tokens: 12000
  raw_tokens: 24000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "A fresh CI run of the tauri-build job builds the broker crate before running cargo test, so the job no longer depends on a stale broker.exe left over from a prior job on the persistent self-hosted workspace."
    - "Taj's production Supabase URL/anon key never exist as a loose plaintext file on the self-hosted runner's persistent workspace after a release job completes."
  artifacts:
    - .github/workflows/ci.yml (tauri-build job gains a broker build step)
    - .github/workflows/release.yml (gains a materialize-.env.production-from-Environment-secrets step + end-of-job cleanup)
  key_links:
    - "release.yml's new environment: { name: taj-house-of-spices } binds the job to the Environment holding VITE_SUPABASE_URL/VITE_SUPABASE_ANON_KEY, materialized to .env.production before beforeBuildCommand runs and deleted before job end."
---

<objective>
Land the two prerequisite fixes CONTEXT.md's D-18 requires before any multi-customer sync/onboarding
work is built on top of `release.yml`: (1) fix `ci.yml`'s `tauri-build` job, which today only passes
by accident because a stale `broker.exe` happens to survive on the self-hosted runner's persistent
workspace, and (2) migrate Taj House of Spices' `.env.production` credentials off a hand-placed
plaintext file and into a GitHub Environment (`taj-house-of-spices`) that Plan 26-04 will later reuse
for the same customer's mirror-push PAT.

Purpose: Both fixes are pre-existing, already-filed blockers (`.planning/todos/pending/fix-ci-tauri-build-broker-order.md`,
`.planning/todos/pending/migrate-env-production-to-github-environment.md`) that CONTEXT.md's D-18
explicitly requires sequencing as prerequisite/first-wave work — not assumed already resolved. Plan
26-02's fan-out mechanism extends this same `release.yml` file next wave, so landing these fixes here
first avoids compounding an already-broken CI job and an already-fragile secrets-handling pattern.
Output: A working `ci.yml` `tauri-build` job (broker built before `cargo test`) and a `release.yml`
that materializes `.env.production` from the new `taj-house-of-spices` GitHub Environment at build
time and cleans it up at job end — no more hand-placed plaintext secrets file.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-multi-customer-deployment/26-CONTEXT.md
@.planning/phases/26-multi-customer-deployment/26-RESEARCH.md
@.planning/todos/pending/fix-ci-tauri-build-broker-order.md
@.planning/todos/pending/migrate-env-production-to-github-environment.md
@.github/workflows/ci.yml
@.github/workflows/release.yml
@scripts/deploy-remote-backend.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ci.yml's tauri-build job — build the broker crate before cargo test</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/todos/pending/fix-ci-tauri-build-broker-order.md (full root-cause + exact fix already specified)
    - .github/workflows/ci.yml (current tauri-build job, lines 40-81 — "Rust unit tests" step at line 76-78 runs `cargo test` in `src-tauri` with no prior broker build)
    - src-tauri/tauri.conf.json line 41: `"../broker/target/release/broker.exe": "broker/broker.exe"` — the bundled resource path tauri-build's build script checks for, even under `cargo test`
  </read_first>
  <action>
    Insert a new step named "Build broker" into ci.yml's `tauri-build` job, positioned after "Install
    dependencies" (`npm ci`) and before "Rust unit tests", running `cargo build --release` with
    `working-directory: broker` (matching the todo's exact fix, no `shell:` override needed — default
    runner shell is fine for a plain `cargo build`). Do not touch the `quality` job or any other step
    in `tauri-build` — this is a single new step insertion, not a broader CI restructure. This closes
    the D-18 dependency for `fix-ci-tauri-build-broker-order.md` per CONTEXT.md's requirement to
    sequence it as first-wave work.
  </action>
  <acceptance_criteria>
    - ci.yml's tauri-build job contains a step with `run: cargo build --release` and
      `working-directory: broker`, positioned before the "Rust unit tests" step (verify by line order
      in the file).
    - No other step in ci.yml is modified or reordered.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "cargo build --release\|working-directory: broker\|Rust unit tests" .github/workflows/ci.yml</automated>
    <fails_when>the "cargo build --release" match's line number is not strictly less than the "Rust unit tests" match's line number, or either string is absent from the grep output</fails_when>
  </verify>
  <done>ci.yml's tauri-build job builds broker/ in release mode before running cargo test in src-tauri, closing the confirmed root cause of GitHub Actions run 33587195680's failure.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Taj's .env.production into a GitHub Environment, materialized at build time</name>
  <files>.github/workflows/release.yml</files>
  <precondition>A `gh` CLI session authenticated with `admin:org`/`repo`-scoped access to zedfauji/supermarket-pos is available to the operator running the one-time `gh api -X PUT repos/zedfauji/supermarket-pos/environments/taj-house-of-spices` + `gh secret set --env taj-house-of-spices` setup commands this task documents (this task edits the workflow YAML; the one-time environment/secret creation itself is an operator action using the values currently in the local, gitignored `.env.production`, matching this repo's existing `deploy-remote-backend.ps1` Section 2 pattern of reading a local secrets file directly).</precondition>
  <read_first>
    - .planning/todos/pending/migrate-env-production-to-github-environment.md (exact fix steps 1-4)
    - .github/workflows/release.yml (current file, full — steps run in order: checkout, setup node, install Rust, npm ci, generate build cert, tauri-action)
    - scripts/deploy-remote-backend.ps1 lines 162-201 ("Secrets (DEP-04)" section — the existing pattern of reading a local secrets file and setting values via CLI, and never printing secret values, only names)
  </read_first>
  <action>
    Add `environment: { name: taj-house-of-spices }` (long/object form — required so this same key can
    later become matrix-driven in Plan 26-02 without hitting the actions/runner short-form-with-
    expression bug, RESEARCH.md Pitfall 2) to the `publish-tauri` job in release.yml. Add a new step
    "Materialize .env.production from Environment secrets", positioned after "install frontend
    dependencies" and before "generate build cert", that writes `VITE_SUPABASE_URL=${{
    secrets.VITE_SUPABASE_URL }}` and `VITE_SUPABASE_ANON_KEY=${{ secrets.VITE_SUPABASE_ANON_KEY }}`
    (both read from the `taj-house-of-spices` Environment now bound to this job) to `.env.production`
    at the repo root via a `shell: pwsh` heredoc/`Set-Content`. Add a final cleanup step "Remove
    materialized .env.production" with `if: always()` that deletes `.env.production` via
    `Remove-Item -Force -ErrorAction SilentlyContinue`, positioned as the LAST step in the job — this
    repo's self-hosted runner workspace persists across jobs (RESEARCH.md Anti-Patterns, this repo's
    own `broker.exe`/locked-native-module incident history), so a materialized secrets file left behind
    is a real leak risk, not tidiness. As a one-time setup step (not part of this workflow file,
    documented in this task's action for the operator to run once): create the GitHub Environment
    `gh api -X PUT repos/zedfauji/supermarket-pos/environments/taj-house-of-spices`, then
    `gh secret set --env taj-house-of-spices VITE_SUPABASE_URL` and
    `gh secret set --env taj-house-of-spices VITE_SUPABASE_ANON_KEY`, sourcing both values from the
    local `.env.production`'s existing content, then delete that local file per the todo's step 4 once
    confirmed the workflow no longer needs to read it from disk by a human. Do not delete `.env.local`
    (self-hosted local dev stack, unrelated per the todo).
  </action>
  <acceptance_criteria>
    - release.yml's `publish-tauri` job has `environment:` with the long/object form (`name:` key), not
      a bare string.
    - A step materializes `.env.production` from `secrets.VITE_SUPABASE_URL`/`secrets.VITE_SUPABASE_ANON_KEY`
      positioned before the `tauri-action` step.
    - A cleanup step with `if: always()` removing `.env.production` exists as the last step in the job.
    - `.env.production` is not committed (already gitignored — confirmed via empty `git log -- .env.production`).
  </acceptance_criteria>
  <verify>
    <automated>grep -n "environment:\|name: taj-house-of-spices\|Materialize .env.production\|if: always()\|Remove-Item" .github/workflows/release.yml</automated>
    <fails_when>any of "environment:", "name: taj-house-of-spices", "Materialize .env.production", "if: always()", or "Remove-Item" is absent from the grep output</fails_when>
  </verify>
  <done>release.yml reads Taj's Supabase URL/anon key from the taj-house-of-spices GitHub Environment, materializes them to .env.production only for the build window, and always deletes the file at job end — closing the plaintext-credential-hygiene gap ahead of D-06's per-customer secrets pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| self-hosted runner workspace ↔ subsequent jobs | The runner's workspace persists across job runs (confirmed pattern, this repo's incident history) — anything not explicitly cleaned up is readable by whatever the NEXT job on this runner does. |
| GitHub Environment secrets ↔ job execution | Only a job whose `environment:` key names `taj-house-of-spices` can resolve that Environment's secrets — GitHub's own scoping primitive (Spike 009). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Information Disclosure | release.yml materialized `.env.production` on the persistent self-hosted runner workspace | high | mitigate | `if: always()` cleanup step removes the file as the job's last step, regardless of earlier step outcome; the file is written only inside this one job's window, never left for the next job to find. |
| T-26-02 | Elevation of Privilege | GitHub Environment `taj-house-of-spices` secret access | low | accept | GitHub's own Environment-scoping primitive already restricts secret resolution to jobs whose `environment:` key names that Environment — this is the platform boundary Spike 009 validated; no additional app-level control needed. |

</threat_model>

<verification>
Both grep-based checks above confirm the file-level changes land correctly. Because this plan only
edits CI workflow YAML (no application code path), full verification is a real CI run: push this
branch and confirm `ci.yml`'s `tauri-build` job goes green with the new broker-build step visible in
the run log, and (once the one-time Environment/secrets setup described in Task 2 is done) a
`workflow_dispatch` of `release.yml` succeeds through the materialize/cleanup steps without leaving
`.env.production` on disk afterward (`Test-Path .env.production` returns `$false` post-run).
</verification>

<success_criteria>
- ci.yml's tauri-build job builds the broker crate before running cargo test (D-18 blocker #1 closed).
- release.yml no longer depends on a hand-placed plaintext `.env.production` — it materializes the
  file from GitHub Environment secrets at build time and always deletes it at job end (D-18 blocker #2
  closed), and the same `taj-house-of-spices` Environment is now in place for Plan 26-04 to reuse for
  the mirror-push PAT.
</success_criteria>

<output>
Create `.planning/phases/26-multi-customer-deployment/26-01-SUMMARY.md` when done
</output>
