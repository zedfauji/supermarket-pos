---
phase: 18-barcode-scan-product-peek-window
plan: 03
type: execute
wave: 3
depends_on: ["18-01", "18-02"]
files_modified:
  - e2e/helpers/tauriPeekMock.ts
  - e2e/checkout/peek-window.spec.ts
autonomous: true
requirements: [PEEK-01, PEEK-02, PEEK-03, PEEK-04]

must_haves:
  truths:
    - "A real Playwright run drives two Pages in one BrowserContext (main + a simulated peek window) bridged by a BroadcastChannel-backed Tauri IPC mock — this is the closest same-process analog to Tauri's real cross-window event relay, per RESEARCH.md's Validation Architecture; this is the automated substitute for a human clicking through the app, per this project's non-negotiable testing policy."
    - "Scanning a barcode on the main page followed by opening a second page at /?window=peek&barcode=<code> (context.pages().length === 2) shows the resolved product's name, price, SKU, barcode, and stock status — PEEK-01 proven end-to-end against live Supabase data, not a fixture stub."
    - "Clicking 'Add to Cart' on a clean (non-flagged) piece-counted product, after adjusting quantity via QuantityControl, relays an add-to-cart event across the mock bus that the main window's real CheckoutPanel listener applies to the real cartStore — the main window's cart total reflects the exact quantity set in the peek window, proving Plan 18-01's listener and Plan 18-02's emit call actually interoperate (not just each side's own unit-level behavior)."
    - "Clicking 'Add to Cart' on a product flagged zero-price or low-stock shows the same confirm toast copy ProductGrid already shows, and the main window's cart stays empty until the toast's 'Add anyway' is clicked — Plan 18-02's out-of-stock/near-expiry guard prohibition is proven live here, not just by source inspection."
    - "Clicking 'Add to Cart' on a sold-by-weight product opens WeightEntryDialog inside the peek window; confirming a weight relays weightGrams (not qty) and the main window's cart shows the correct weighted line total."
    - "A barcode matching no active product renders the peek window's EmptyState ('Product not found'), with no 'Add to Cart' button rendered."
    - "Scanning a second, different barcode while the peek window is already open replaces its entire displayed content (old product's name gone, new product's name visible) with no manual close first, AND relays to the main window's search box, AND the main window's own independent scan (a third, unrelated barcode scanned directly on the main page) still updates its own search box — PEEK-04's both halves proven in one test."
    - "Clicking 'Close' with no prior interaction records a window-hide IPC call and never records an add-to-cart emit — the cart is provably unaffected."
    - "A second page opened in the same BrowserContext as an already-logged-in main page, navigated directly to the peek URL with no explicit login step of its own, renders real product content rather than a login/staff-picker screen — this closes RESEARCH.md's Assumption A1 / Pitfall 6 (cross-window Supabase session restore) with a concrete automated check instead of leaving it an untested assumption."
  artifacts:
    - e2e/helpers/tauriPeekMock.ts
    - e2e/checkout/peek-window.spec.ts
  key_links:
    - "e2e/helpers/tauriPeekMock.ts's BroadcastChannel bus -> both Pages' window.__TAURI_INTERNALS__.invoke('plugin:event|emit'/'plugin:event|listen') -> real app code (CheckoutPanel's listen(), ProductPeekWindow's emit()) unmodified from Plan 18-01/18-02"
    - "e2e/checkout/peek-window.spec.ts -> e2e/helpers/auth.ts (loginAs), e2e/helpers/supabase.ts (getServiceClient/resetTestState/openCaja), e2e/checkout/barcode-scan-search.spec.ts's existing scanBarcode()/reloadCatalog() shape (mirrored, not imported — matches this repo's existing per-file duplication convention for this small helper)"
---

<objective>
Prove Plan 18-01 and Plan 18-02's work actually interoperate, live, against real Supabase data — via Playwright, per this project's non-negotiable "automate it, never ask the user to click through" testing policy. This is the only plan in the phase that can determine whether PEEK-01 through PEEK-04 are actually satisfied, since Plans 18-01/18-02 could only Vitest-cover their own halves in isolation (18-01 had no peek-window UI yet to test against; 18-02 has no automated proof its emit calls actually reach a listening main window).

The one genuinely new E2E technique this phase needs — simulating a second Tauri OS window with a second Playwright `Page` in the same `BrowserContext`, bridged by a `BroadcastChannel`-backed IPC mock — has no existing precedent in this repo (RESEARCH.md's Validation Architecture section designed it specifically for this phase).

Purpose: close the phase with concrete, reproduced evidence per requirement, not a hopeful inference from two separately-passing unit-level plans.
Output: `e2e/helpers/tauriPeekMock.ts` (reusable mock, extending the existing `injectPrintMock` dual-global pattern), `e2e/checkout/peek-window.spec.ts` (8 tests covering PEEK-01..04 plus the session-restore assumption).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-barcode-scan-product-peek-window/18-CONTEXT.md
@.planning/phases/18-barcode-scan-product-peek-window/18-RESEARCH.md
@.planning/phases/18-barcode-scan-product-peek-window/18-UI-SPEC.md

@e2e/receipts/reprint.spec.ts
@e2e/checkout/barcode-scan-search.spec.ts
@e2e/helpers/auth.ts
@e2e/helpers/requireEnv.ts
@e2e/fixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: tauriPeekMock.ts — BroadcastChannel-backed multi-window IPC mock</name>
  <files>e2e/helpers/tauriPeekMock.ts</files>
  <read_first>
    18-RESEARCH.md "E2E: simulating a second OS window with a BroadcastChannel-backed Tauri IPC mock" Code Examples section (the complete ~55-line reference implementation to transcribe — already verified against the real compiled `@tauri-apps/api` command strings this session: `plugin:webview|create_webview_window`, `plugin:window|close`/`hide`/`show`/`set_focus`, `plugin:event|listen`/`unlisten`/`emit`/`emit_to`)
    e2e/receipts/reprint.spec.ts lines 18-42 (`injectPrintMock` — the exact dual-global `window.__TAURI__`/`window.__TAURI_INTERNALS__` shape and the `window.__lastPrintedLines`-style call-recording convention this new helper extends)
  </read_first>
  <action>
    Create `e2e/helpers/tauriPeekMock.ts` exporting `async function injectPeekWindowMock(page: Page): Promise<void>` (imports `type { Page } from '@playwright/test'`). Inside a `page.addInitScript(() => { ... })` callback (matching `injectPrintMock`'s exact structure): set `window.__TAURI__ = {}` (passes any `isTauri()`-style gate); set `window.__peekMockCalls = []` (a plain array recording every `{ cmd, args }` pair passed to `invoke`, mirroring `reprint.spec.ts`'s `__lastPrintedLines` single-value convention but as an append-only log so a test can assert on ANY call, e.g. a `plugin:window|hide`); create `const bus = new BroadcastChannel('tauri-peek-mock')` and a `Map` of registered listeners keyed by a locally incrementing id, exactly per RESEARCH.md's reference implementation (`bus.onmessage` dispatches to every registered listener whose `event` name matches the incoming message's `event`). Implement `window.__TAURI_INTERNALS__` with: `invoke(cmd, args)` — push `{ cmd, args }` onto `__peekMockCalls` first, then branch: `'plugin:event|listen'` registers the handler (keyed by `args.handler`, the transformed callback id) and resolves a locally-incrementing listener id; `'plugin:event|emit'` calls `bus.postMessage({ event: args.event, payload: args.payload })` and resolves `null`; every other command (`plugin:webview|create_webview_window`, `plugin:window|close`/`hide`/`show`/`set_focus`) resolves `null` without further side effect (already recorded in `__peekMockCalls` for assertion — window count is driven by the test itself via `context.newPage()`, not by this mock, per RESEARCH.md's explicit note); `transformCallback(callback)` and `unregisterCallback(id)` exactly as `injectPrintMock`'s existing implementation.

    Also export `async function getPeekMockCalls(page: Page, cmd?: string): Promise<{ cmd: string; args: unknown }[]>` — a `page.evaluate()` helper returning `window.__peekMockCalls` (optionally filtered to one `cmd` string), used by Task 2 to assert e.g. that a `plugin:window|hide` call was recorded after "Close" or after a successful "Add to Cart".
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes (this file is plain TypeScript, no Vitest/Playwright runtime assertion of its own — it's exercised transitively by Task 2).
    - `grep -n "BroadcastChannel" e2e/helpers/tauriPeekMock.ts` confirms the cross-page bridge is present.
    - `grep -n "__peekMockCalls" e2e/helpers/tauriPeekMock.ts` confirms both the recorder and its `getPeekMockCalls` accessor exist.
  </acceptance_criteria>
  <done>A reusable, typed Playwright helper exists that lets two Pages in the same BrowserContext stand in for Tauri's real main+peek windows, relaying emit/listen calls exactly as the real IPC layer would, with every invoke() call recorded for test assertions.</done>
</task>

<task type="auto">
  <name>Task 2: peek-window.spec.ts — PEEK-01..04 end-to-end, plus the session-restore assumption</name>
  <files>e2e/checkout/peek-window.spec.ts</files>
  <read_first>
    e2e/checkout/barcode-scan-search.spec.ts (full file, already read this session — `scanBarcode()`'s exact keydown-dispatch shape at lines 10-16, `reloadCatalog()`, the `beforeEach` login+checkout-navigation shape at lines 25-37, the zero-price/sold-by-weight toggle-then-restore-in-`finally` seeding pattern at lines 54-90 and 180-208; the three named seed products this spec already relies on: `Haldiram's Aloo Bhujia 200g` (PRIMARY), `Parle-G Biscuits 200g` (SECONDARY), `MDH Garam Masala 100g` (CATEGORY) — reuse these same three rather than inventing new fixture names)
    18-UI-SPEC.md Copywriting Contract table (exact copy strings to assert against: "Add to Cart", "Close", "Product not found", the `notFoundBody`/`stockCount`/`unitWeight` strings)
    Task 1's output, `e2e/helpers/tauriPeekMock.ts` (`injectPeekWindowMock`, `getPeekMockCalls`)
    e2e/helpers/auth.ts (`loginAs`) and e2e/helpers/requireEnv.ts (`requireIntegrationEnv`)
  </read_first>
  <action>
    Create `e2e/checkout/peek-window.spec.ts`. Define a local `scanBarcode(page, barcode)` helper identical in shape to `barcode-scan-search.spec.ts`'s own (per-file duplication is this repo's existing convention for this small helper, per PATTERNS.md/RESEARCH.md — not worth centralizing for a third use). `test.describe('Barcode scan product peek window (PEEK-01..04)', ...)` with a `beforeEach` mirroring `barcode-scan-search.spec.ts`'s exactly: `requireIntegrationEnv()`, `resetTestState()`, `openCaja(500)`, `injectPeekWindowMock(page)`, `page.goto('/')`, `loginAs(page, 'cashier')`, click checkout, assert `/\/pos$/`.

    Implement 8 tests:

    1. **"scanning a barcode and opening the peek window shows full product detail (PEEK-01)"** — fetch `Haldiram's Aloo Bhujia 200g`'s `id, name, base_price, sku, barcode` via `getServiceClient()`; `scanBarcode(page, product.barcode)`; open `const peekPage = await context.newPage()`, `injectPeekWindowMock(peekPage)`, `peekPage.goto(\`/?window=peek&barcode=${product.barcode}\`)`; assert `peekPage` shows the product's name (heading), a price string containing `product.base_price.toFixed(2)`, the SKU value, the barcode value, and stock-status text (e.g. "In stock" or the `stockCount` copy); assert `context.pages().length === 2`.

    2. **"Add to Cart with an adjusted quantity relays to the real main-window cart (PEEK-02/PEEK-03)"** — use `Parle-G Biscuits 200g` (a clean, non-flagged product — confirm via a quick `getServiceClient()` read that its `base_price > 0` and stock is healthy, matching this spec's other tests' assumption); open `peekPage` at its barcode; click the quantity `+` button twice (default 1 → 3, via the `QuantityControl`'s "Increase quantity" `aria-label`, per `src/shared/ui/QuantityControl.tsx`); click "Add to Cart"; assert (via `getPeekMockCalls(peekPage, 'plugin:window|hide')`) a hide call was recorded; on the main `page`, assert the cart area contains the product's name and a total/line amount consistent with `3 * base_price` (formatted to 2 decimals, same substring-match style `reprint.spec.ts` already uses for money assertions).

    3. **"a zero-price product still gates through the risky-add confirm toast (prohibition: no guard bypass)"** — toggle `base_price: 0` on `MDH Garam Masala 100g` via the service client (restore in `finally`, mirroring `barcode-scan-search.spec.ts`'s existing pattern exactly); open `peekPage` at its barcode; click "Add to Cart"; assert the same `"... is priced at $0"` toast copy renders in `peekPage`; assert the main `page`'s cart area does NOT yet show the product; click `/^add anyway$/i` in `peekPage`; assert the main `page`'s cart now shows it.

    4. **"a sold-by-weight product opens WeightEntryDialog and relays a weighted line (PEEK-02 weight path)"** — toggle `sold_by_weight: true` on `Haldiram's Aloo Bhujia 200g` (restore `false` in `finally`, mirroring the existing toggle pattern); open `peekPage` at its barcode; assert the peek window shows the `unitWeight` copy ("Sold by weight (kg)"); click "Add to Cart"; assert `peekPage` shows the "Enter weight" heading; enter a weight via the dialog's own numeric keypad buttons (e.g. click "1", ".", "5" for 1.5kg — mirrors `WeightEntryDialog`'s own keypad, unmodified per D-05); click the dialog's own Confirm button (`t('featOrders:weightEntry.confirm')` = "Add to cart"); assert the main `page`'s cart shows a line whose amount matches `1.5 * base_price` (2-decimal formatted).

    5. **"an unmatched barcode shows Product not found, no Add to Cart rendered"** — open `peekPage` at `/?window=peek&barcode=9999999999999`; assert `peekPage` shows "Product not found" and the `notFoundBody` copy; assert no "Add to Cart" button is present; assert "Close" is present.

    6. **"rescanning a different barcode replaces peek content and relays to main, while main's own independent scan still fires (PEEK-04)"** — open `peekPage` at PRIMARY's barcode, assert its name visible; `scanBarcode(peekPage, SECONDARY.barcode)`; assert `peekPage` now shows SECONDARY's name and PRIMARY's name is gone; assert the main `page`'s search input value updated to SECONDARY's barcode (relay); then `scanBarcode(page, CATEGORY_PRODUCT.barcode)` directly on the main page; assert the main `page`'s search input now shows `CATEGORY_PRODUCT`'s barcode — proving the main window's own local scan-to-search path still fires completely independently of the peek relay.

    7. **"Close dismisses with zero cart mutation"** — open `peekPage` at any clean product's barcode; click "Close"; assert (via `getPeekMockCalls`) a `plugin:window|hide` call was recorded and NO `plugin:event|emit` call carrying `event: 'add-to-cart'` was ever recorded; assert the main `page`'s cart is still empty.

    8. **"a second window in the same browser context restores the session without a fresh login (closes RESEARCH.md Assumption A1 / Pitfall 6)"** — `const freshPeekPage = await context.newPage()` (same `context` as the already-logged-in main `page` — no `loginAs(freshPeekPage, ...)` call at all); `injectPeekWindowMock(freshPeekPage)`; `freshPeekPage.goto(\`/?window=peek&barcode=${primaryProduct.barcode}\`)`; assert the product's name renders (NOT the "who are you"/PIN staff-picker heading) — proving Supabase's `localStorage`-persisted session is shared across same-context pages exactly as it will be across two real `WebviewWindow`-backed same-origin webviews.

    Every seed/mutation test that toggles a product field restores it in a `finally` block, mirroring `barcode-scan-search.spec.ts`'s existing convention exactly (never leave `base_price`/`sold_by_weight` mutated for a later test in the suite).
  </action>
  <verify>
    <automated>npx playwright test e2e/checkout/peek-window.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/checkout/peek-window.spec.ts` passes all 8 tests.
    - `npx playwright test e2e/checkout/` (the whole folder) still passes — regression check that this new spec's mock injection and any shared-fixture mutation doesn't destabilize `barcode-scan-search.spec.ts`, `happy-path.spec.ts`, `modifier-notes.spec.ts`, or `atomic-rpc-guards.spec.ts`.
    - `grep -c "getProductRiskFlag\|useConfirmRiskyAdd" src/widgets/ProductPeekWindow/ui/ProductPeekWindow.tsx` (source assertion, re-run here as the phase-closing regression guard) still shows the guard imported from `entities/product/model`, not reimplemented — confirms Plan 18-02 wasn't silently altered between plans.
  </acceptance_criteria>
  <done>PEEK-01 through PEEK-04 are proven end-to-end against live Supabase data via real Playwright browser automation — window-count, full product detail, guarded add-to-cart (both piece and weight paths), rescan-replaces-content with main's independent listener still firing, Close's zero-mutation guarantee, and cross-window session restore are all reproduced, not inferred.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's slice)

- `e2e/helpers/tauriPeekMock.ts` — new Playwright helper: `injectPeekWindowMock(page)`, `getPeekMockCalls(page, cmd?)`.
- `e2e/checkout/peek-window.spec.ts` — new spec file, 8 tests covering PEEK-01..04 and the cross-window session-restore assumption.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Playwright mock vs. real Tauri IPC | The `BroadcastChannel` bridge faithfully simulates event *content and relay logic* but not genuinely OS-level facts (a real second taskbar entry, real window decorations, real OS focus-stealing) — this is the one documented gap, matching this project's existing CLAUDE.md carve-out for the native Tauri window shell. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-07 | Information Disclosure | Test-only mock recording every `invoke()` call onto `window.__peekMockCalls` | low | accept | Test/dev-only code path (`e2e/helpers/`), never shipped in the production bundle — no different in kind from the existing `__lastPrintedLines` pattern already in `e2e/receipts/reprint.spec.ts`. |

</threat_model>

<verification>
`npx playwright test e2e/checkout/peek-window.spec.ts` passes all 8 tests. `npx playwright test e2e/checkout/` (full folder) passes with no regression. As the phase-closing gate, `npm run test:e2e` (full suite) should be run before `/gsd-verify-work` per RESEARCH.md's Sampling Rate — a new capability grant or a new IPC-mock helper is exactly the kind of change that could destabilize an unrelated Tauri-mocked spec elsewhere in the suite.
</verification>

<success_criteria>
Every one of PEEK-01 through PEEK-04's success criteria (per ROADMAP.md Phase 18) has a corresponding passing, reproducible Playwright test — no `human_needed` state, no manual click-through step, per this project's non-negotiable testing policy.
</success_criteria>

<output>
Create `.planning/phases/18-barcode-scan-product-peek-window/18-03-SUMMARY.md` when done
</output>
