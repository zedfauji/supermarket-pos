---
phase: 18-barcode-scan-product-peek-window
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/main.tsx
  - src/app/PeekApp.tsx
  - src/widgets/ProductPeekWindow/ui/ProductPeekWindow.tsx
  - src/shared/lib/i18n/locales/en-US/wPanels.json
  - src/shared/lib/i18n/locales/es-MX/wPanels.json
autonomous: true
requirements: [PEEK-01, PEEK-02, PEEK-03, PEEK-04]

must_haves:
  truths:
    - "Visiting the app with ?window=peek in the URL query string mounts PeekApp (not App/Router) — the same #root, same globals.css bundle, same AppConfigProvider->Providers stack, no Router."
    - "ProductPeekWindow reads the barcode query param on first mount and calls useLookupProductByBarcode().lookup(code.trim()) — the scanned code is trimmed before the exact-match query so scanner whitespace/newline artifacts never cause a false not-found (specless-fallback PEEK-01/encoding, backstop)."
    - "ProductPeekWindow shows name, size/unit (soldByWeight ? 'Sold by weight (kg)' : 'Sold by piece' — there is no separate kg/g/L enum in ProductSchema, RESEARCH.md Pitfall 5), photo (or an ImageOff fallback with an sr-only caption when imageUrl is null), price (MoneyDisplay), current inventory (StatusBadge inv_in_stock/inv_low_stock/inv_out_of_stock + a stockCount line), SKU, and barcode for the resolved product (PEEK-01) — all using the existing StatusBadge tri-state thresholds unchanged (specless-fallback PEEK-01/boundary, covered)."
    - "A product missing SKU and/or barcode renders an em dash (—) in that detail row, not a blank cell (18-UI-SPEC.md UI Considerations, partial, covered)."
    - "A long product name truncates with a title attribute for full-text hover, matching ProductCard's existing treatment (18-UI-SPEC.md UI Considerations, long-text, covered)."
    - "A scanned barcode matching no active product renders EmptyState (icon PackageSearch, 'Product not found' title, notFoundBody description, no action button, Close-only footer) — 18-UI-SPEC.md UI Considerations, empty/static-content, covered."
    - "The peek window's own product fetch is treated as a frequent (not rare) loading state — CardSkeleton renders while lookup() is in flight on every open/rescan, since this is a separate JS/query-cache context that never reads the main window's warm cache (18-UI-SPEC.md UI Considerations, loading, covered)."
    - "An unhandled exception from the lookup() call itself (not a resolved null) renders the distinct loadError copy, Close-only footer, no retry button (18-UI-SPEC.md UI Considerations, error, covered). Documented limitation: useLookupProductByBarcode collapses a genuine query failure and a real not-found into the same resolved null — so an actual RLS/auth-session failure (RESEARCH.md Pitfall 6) will present as 'Product not found', not 'Couldn't load product details'; Plan 18-03's E2E session-restore test is what actually catches a real auth failure, not this UI branch."
    - "The piece-counted path renders QuantityControl (min=1, max=99, default 1, unmodified) before 'Add to Cart' commits; requesting the exact currently-available quantity is allowed — the peek window adds no new stock-vs-request comparison beyond the existing getProductRiskFlag low-stock-threshold check it reuses verbatim (specless-fallback PEEK-02/boundary + PEEK-02/adjacency, both covered)."
    - "Every 'Add to Cart' commit path — piece stepper and the sold-by-weight WeightEntryDialog overlay alike — first calls getProductRiskFlag(product) and, if flagged, gates through useConfirmRiskyAdd(...) exactly as ProductGrid.selectProduct does today (same toast copy, same Infinity duration, same 44px action-button minimum) before ever emitting add-to-cart."
    - "All local UI state (piece-count stepper value, any half-entered weight) is keyed on product.id via a React key prop on the populated-detail subtree, so it resets to default the instant the displayed product changes on a rescan — no stale quantity carries between products (PEEK-04, specless-fallback PEEK-02/precision + UI-SPEC rescan-replaces-content, both covered). PEEK-02/ordering is judged not applicable — the peek window displays exactly one product, never an ordered/sortable collection, so no tie-break rule is needed (explicit flagged assumption, not a silent drop)."
    - "The piece-counted commit path emits ADD_TO_CART_EVENT with {product, qty} and hides (never destroys) the window; the sold-by-weight path opens WeightEntryDialog (mode='add') with its new onConfirm override supplying {product, weightGrams} to the same event, then hides the window — 'Close' in the footer hides the window with zero event emissions."
    - "The peek window's own product query never selects or displays a cost/supplier price field — only product.basePrice (the existing sale price) is fetched via useLookupProductByBarcode's unmodified .select()."
    - "The peek window mounts its own <Toaster/> (sonner) inside PeekApp, since useConfirmRiskyAdd's toast needs a Toaster in the same React root it's called from — a separate render tree from the main window's own Toaster."
  prohibitions:
    - "MUST NOT let 'Add to Cart' in the peek window bypass the existing out-of-stock/near-expiry confirmation guard — the peek window's add path must trigger the same getProductRiskFlag/useConfirmRiskyAdd gate ProductGrid's add path triggers, not a silent add (PEEK-02/safety)."
    - "MUST NOT display supplier/cost price to any session in the peek window — only the sale price (product.basePrice) is shown; cost price stays manager/admin-only elsewhere in this app and is never fetched into this window's product query (PEEK-01/privacy)."
  artifacts:
    - src/main.tsx (?window=peek branch)
    - src/app/PeekApp.tsx
    - src/widgets/ProductPeekWindow/ui/ProductPeekWindow.tsx
  key_links:
    - "main.tsx (?window=peek) -> PeekApp -> Providers (QueryClientProvider) -> ProductPeekWindow"
    - "ProductPeekWindow mount -> new URLSearchParams(location.search).get('barcode') -> useLookupProductByBarcode().lookup(code.trim())"
    - "ProductPeekWindow's own useBarcodeScanner -> local re-lookup (PEEK-04) + emit(BARCODE_SCANNED_EVENT, {code}) (relay to main, D-01/D-02, satisfied by 18-01)"
    - "ProductPeekWindow commit -> getProductRiskFlag/useConfirmRiskyAdd (reused verbatim) -> emit(ADD_TO_CART_EVENT, payload) -> getCurrentWebviewWindow().hide()"
    - "WeightEntryDialog onConfirm (18-01) -> emit(ADD_TO_CART_EVENT, {product, weightGrams}) -> hide()"
---

<objective>
Build the peek window's own React tree end to end: the `?window=peek` bootstrap branch, its provider shell, and the full `ProductPeekWindow` widget that composes existing checkout building blocks (per D-05 — no duplicated guard/dialog logic) into the layout `18-UI-SPEC.md` specifies. This is the plan that actually satisfies PEEK-01 through PEEK-04's user-visible behavior; Plan 18-01 already made the main window ready to open/relay/receive, Plan 18-03 proves this plan's output live via Playwright.

Purpose: give the cashier a real, inspectable product-detail window with a working qty/weight entry and guard reuse, reachable from a barcode scan.
Output: `PeekApp.tsx`, `ProductPeekWindow.tsx`, the `main.tsx` bootstrap branch, and the remaining `wPanels:productPeekPanel.*` copy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-barcode-scan-product-peek-window/18-CONTEXT.md
@.planning/phases/18-barcode-scan-product-peek-window/18-RESEARCH.md
@.planning/phases/18-barcode-scan-product-peek-window/18-PATTERNS.md
@.planning/phases/18-barcode-scan-product-peek-window/18-UI-SPEC.md

@src/main.tsx
@src/app/App.tsx
@src/app/providers.tsx
@src/app/AppConfigProvider.tsx
@src/widgets/ProductGrid/ui/ProductGrid.tsx
@src/entities/product/ui/ProductCard.tsx
@src/features/lookup-product-by-barcode/model/useLookupProductByBarcode.ts
@src/features/open-product-peek-window/model/useProductPeekWindow.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap branch — main.tsx, PeekApp.tsx, remaining productPeekPanel i18n copy</name>
  <files>src/main.tsx, src/app/PeekApp.tsx, src/shared/lib/i18n/locales/en-US/wPanels.json, src/shared/lib/i18n/locales/es-MX/wPanels.json</files>
  <read_first>
    src/main.tsx (full 16-line file, already read — the exact insertion point)
    src/app/App.tsx (full 23-line file, already read — the provider stack to mirror minus Router/OfflineBanner/ClockDriftBanner)
    18-RESEARCH.md "Peek window entry branching (main.tsx)" and "PeekApp.tsx shell" Code Examples (both already verified against this exact file this session)
    src/app/providers.tsx (full file, already read this session — confirms Providers wraps QueryClientProvider + mounts CajaListener/OfflineQueueProcessor/UpdaterProvider unconditionally; there is no lighter-weight peek-only variant in this codebase)
    src/app/AppConfigProvider.tsx (full file, already read — confirms it already gates its Tauri `invoke('get_runtime_config')` call behind `window.__TAURI_INTERNALS__` and is safe to mount a second time in a second window)
  </read_first>
  <action>
    In `src/main.tsx`, add `import { PeekApp } from './app/PeekApp';` alongside the existing `App` import, compute `const isPeek = new URLSearchParams(window.location.search).get('window') === 'peek';` before the `createRoot(rootEl).render(...)` call, and change the rendered element to `{isPeek ? <PeekApp /> : <App />}`. Keep the `#root` null-check and the `import './app/globals.css'` line exactly as they are — both windows share one CSS bundle and one root-element contract.

    Create `src/app/PeekApp.tsx` mirroring `App.tsx`'s exact provider order (`ErrorBoundary` → `AppConfigProvider` → `Toaster` → `Providers`) but: drop `OfflineBanner` (no offline-cart concept in the peek window, D-04 — it never touches the cart directly) and `ClockDriftBanner` (no payment/caja concerns here); keep `<Toaster richColors position="top-right" />` (this window's own `useConfirmRiskyAdd` toast needs a `Toaster` mounted in the same React root it fires from); replace `<Router />` with `<ProductPeekWindow />` (imported from `@widgets/ProductPeekWindow/ui/ProductPeekWindow`, created in Task 2 of this plan). Reuse `<Providers>` as-is (ponytail: it also mounts `CajaListener`/`OfflineQueueProcessor`/`UpdaterProvider`, none of which this window strictly needs — a peek-only provider subset would be a second maintained abstraction for a two-window app; at most this briefly duplicates a Realtime subscription or an update-check across the two windows, harmless in this closed single-process desktop app — add a stripped variant only if this measurably causes a problem).

    In both `wPanels.json` locale files, extend the `"productPeekPanel"` object (created in Plan 18-01 with just `windowTitle`) with the remaining keys: `"addToCart": "Add to Cart"` / `"Agregar al carrito"`; `"notFoundBody": "No product matches this barcode. Scan again or check the code."` / `"Ningún producto coincide con este código de barras. Escanea de nuevo o verifica el código."`; `"loadError": "Couldn't load product details. Close this window and try again."` / `"No se pudieron cargar los detalles del producto. Cierra esta ventana e inténtalo de nuevo."`; `"noPhoto": "No photo available"` / `"Sin foto disponible"`; `"unitWeight": "Sold by weight (kg)"` / `"Se vende por peso (kg)"`; `"unitPiece": "Sold by piece"` / `"Se vende por pieza"`; `"stockCount": "{{count}} in stock"` / `"{{count}} en existencia"`; `"skuLabel": "SKU"` / `"SKU"`; `"barcodeLabel": "Barcode"` / `"Código de barras"`.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "window=peek\|isPeek" src/main.tsx` shows the branch.
    - `grep -n "ProductPeekWindow" src/app/PeekApp.tsx` confirms it's rendered in place of `Router`.
    - `grep -c '"addToCart"\|"notFoundBody"\|"loadError"\|"noPhoto"\|"unitWeight"\|"unitPiece"\|"stockCount"\|"skuLabel"\|"barcodeLabel"' src/shared/lib/i18n/locales/en-US/wPanels.json` returns `9` (all keys present alongside Plan 18-01's `windowTitle`).
    - `npm run typecheck` passes (Task 2's `ProductPeekWindow` component doesn't need to exist yet for this file to typecheck against its own import — confirm this task's diff alone doesn't break the build if Task 2 hasn't landed; if it does fail purely because the import target is missing, proceed to Task 2 immediately within this same plan before running the final verification).
  </acceptance_criteria>
  <done>Navigating to /?window=peek mounts a dedicated PeekApp tree (same providers minus Router/OfflineBanner/ClockDriftBanner) instead of the main App; all productPeekPanel copy keys exist in both locales.</done>
</task>

<task type="auto">
  <name>Task 2: ProductPeekWindow — full widget (fetch, own scanner+relay, risky-add guard reuse, piece/weight paths, all UI states)</name>
  <files>src/widgets/ProductPeekWindow/ui/ProductPeekWindow.tsx</files>
  <read_first>
    18-UI-SPEC.md in full (already loaded this session) — Interaction Contract's Layout steps 1-7, the Sold-by-weight path section (including the now-resolved "Flag for planner" callout), Risky-add guard section, Rescan-replaces-content section, Not-found/Loading/Error state sections, and the full Copywriting Contract table
    src/widgets/ProductGrid/ui/ProductGrid.tsx lines 56-68 (`selectProduct`'s exact commit/guard shape — the pattern to mirror for both the piece and weight commit paths)
    src/entities/product/ui/ProductCard.tsx lines 48-62 (category-dot + name + price markup to reuse verbatim, with UI-SPEC's stated size overrides: name becomes `text-2xl font-heading font-semibold truncate`, `MoneyDisplay size="xl" className="font-semibold"`)
    src/entities/product/model/productRiskFlag.ts and useConfirmRiskyAdd.ts (both full files, already read — call exactly as ProductGrid does: `const flag = getProductRiskFlag(product); if (flag) { confirmRiskyAdd(flag, product, commit); return; } commit();`)
    src/entities/inventory/ui/InventoryRow.tsx lines 37-42 (`stockTier` — NOT exported, operates on `Inventory` not `Product`; inline a 3-line `Product`-typed equivalent using the same comparisons: quantityOnHand === 0 -> 'inv_out_of_stock'; quantityOnHand <= lowStockThreshold -> 'inv_low_stock'; else 'inv_in_stock'; treat an undefined quantityOnHand/lowStockThreshold as 'inv_in_stock', i.e. no badge downgrade when there's no inventory row to compare)
    src/shared/ui/QuantityControl.tsx, StatusBadge.tsx, EmptyState.tsx, LoadingSkeletons.tsx (CardSkeleton), MoneyDisplay.tsx, POSButton.tsx (all full files, already read this session — exact prop shapes)
    src/features/lookup-product-by-barcode/model/useLookupProductByBarcode.ts (full file, already read — `lookup(code): Promise<Product | null>`, already filters `is_active=true` and already selects `inventory(quantity_on_hand, low_stock_threshold)` but never a cost/supplier price column)
    src/features/add-loose-weight-item/ui/WeightEntryDialog.tsx (already modified by Plan 18-01 — use its new `onConfirm` prop)
    src/shared/lib/useBarcodeScanner.ts (full file, already read — same `{ onScan, enabled }` shape, third instantiation, no modification needed)
    src/features/open-product-peek-window/model/useProductPeekWindow.ts (Plan 18-01's `BARCODE_SCANNED_EVENT`, `ADD_TO_CART_EVENT`, `AddToCartPayload`)
    node_modules/@tauri-apps/api/webviewWindow.js (`getCurrentWebviewWindow` export, confirmed this session — used for `.hide()`)
  </read_first>
  <action>
    Create `src/widgets/ProductPeekWindow/ui/ProductPeekWindow.tsx`. Top-level component `ProductPeekWindow`: uses `useTranslation('wPanels')`, `const { lookup } = useLookupProductByBarcode()`, `const confirmRiskyAdd = useConfirmRiskyAdd()`. State: `const [product, setProduct] = useState<Product | null | undefined>(undefined)` (`undefined` = loading, `null` = not-found-or-error-resolved-null, a `Product` = populated) and `const [hasError, setHasError] = useState(false)`. A `loadProduct` callback (wrapped in `useCallback`) that sets `hasError` false and `product` to `undefined` before calling, then `try { const result = await lookup(code.trim()); setProduct(result); } catch { setHasError(true); }` — the trim is the encoding backstop (specless-fallback PEEK-01/encoding); the try/catch is a defensive last resort since `useLookupProductByBarcode`'s own implementation already swallows Supabase errors into a resolved `null` (document this exact limitation inline as a code comment: an actual RLS/auth-session failure will therefore present as "not found", not "load error" — Plan 18-03's dedicated session-restore E2E test is what actually catches that failure mode). On mount, `useEffect(() => { const code = new URLSearchParams(window.location.search).get('barcode'); if (code) { void loadProduct(code); } else { setProduct(null); } }, [loadProduct])` (defensive: `ensurePeekWindowShown` always supplies a barcode, so the `else` branch should not occur in practice).

    Instantiate the peek window's own `useBarcodeScanner({ onScan: code => { void loadProduct(code); void emit(BARCODE_SCANNED_EVENT, { code }); } })` (imports `emit` from `@tauri-apps/api/event`) — this is PEEK-04's "rescan replaces content" (local re-lookup) plus the D-01/D-02 relay to main in one call, no `enabled` gate needed (the peek window has no payment/weight-dialog concept blocking its own scanner the way `CheckoutPanel` does).

    `const handleClose = () => { void getCurrentWebviewWindow().hide(); };` (imported from `@tauri-apps/api/webviewWindow`) — used by every state's Close button; never `.close()`/`.destroy()` (D-03).

    Render branch: `if (hasError) return <ErrorStateView onClose={handleClose} />;` else `if (product === undefined) return <LoadingStateView onClose={handleClose} />;` else `if (product === null) return <NotFoundStateView onClose={handleClose} />;` else render the populated detail view keyed by `product.id` (a `key={product.id}` prop on that subtree — the React remount-on-key-change idiom resets any local qty/weight-dialog-open state the instant the displayed product changes, satisfying the rescan-reset requirement with no manual `useEffect` reset needed).

    The populated subtree (call it `PeekProductDetail`, a child component receiving `product`, `onClose`, `confirmRiskyAdd`) implements UI-SPEC's 7-step layout: (1) photo hero, `aspect-square rounded-lg border bg-muted max-w-[240px]` centered — `product.imageUrl` present renders an `<img src={product.imageUrl} className="object-contain" />`, else a centered `ImageOff` icon (`h-10 w-10 text-muted-foreground`, `aria-hidden`) plus an `sr-only` span with `t('productPeekPanel.noPhoto')`; (2) category row — `product.category`'s color dot + name, mirroring `ProductCard.tsx` lines 48-55 verbatim (no `useCategories()` call needed — `product.category` is already populated by `useLookupProductByBarcode`'s `.select()`, which embeds `category:categories(*)`); (3) `<h3 title={product.name} className="w-full truncate text-2xl font-heading font-semibold">{product.name}</h3>`; (4) `<MoneyDisplay amount={product.basePrice} size="xl" className="font-semibold" />`; (5) detail panel `rounded-md border bg-muted/40 p-3` with label/value rows for SKU (`product.sku ?? '—'`), Barcode (`product.barcode ?? '—'`), Unit (`product.soldByWeight ? t('productPeekPanel.unitWeight') : t('productPeekPanel.unitPiece')`), Stock (inline `productStockTier(product)` function feeding `<StatusBadge status={...} />` plus `t('productPeekPanel.stockCount', { count: product.quantityOnHand ?? 0 })`); (6) for `!product.soldByWeight` only, a `QuantityControl` with local `const [qty, setQty] = useState(1)` (no min/max override — component defaults of 1/99 already match UI-SPEC); (7) footer `border-t bg-muted/50 p-4 flex gap-2 justify-end` with `POSButton variant="outline" touchSize="large"` "Close" (`t('common:actions.close')`, calling `onClose`) and `POSButton touchSize="xl"` "Add to Cart" (`t('productPeekPanel.addToCart')` for the piece path; for the weight path this button's own click still triggers the risk-gated commit below, but the dialog that then opens supplies its own `t('featOrders:weightEntry.confirm')`-labelled Confirm button per UI-SPEC — the outer footer button's own label stays `addToCart` either way since it's what the cashier clicks first).

    Commit logic (mirrors `ProductGrid.selectProduct` exactly): `const commit = () => { if (product.soldByWeight) { setWeightDialogOpen(true); } else { void emit(ADD_TO_CART_EVENT, { product, qty }); onClose(); } };` then `const handleAddToCart = () => { const flag = getProductRiskFlag(product); if (flag) { confirmRiskyAdd(flag, product, commit); return; } commit(); };` bound to the footer's "Add to Cart" button. When `soldByWeight` and the risk gate has already passed (or there was no flag), render `<WeightEntryDialog open={weightDialogOpen} onOpenChange={setWeightDialogOpen} product={product} mode="add" onConfirm={weightGrams => { void emit(ADD_TO_CART_EVENT, { product, weightGrams }); onClose(); }} />` as an overlay on top of this same content (not conditionally unmounting the detail view underneath it) — its own Confirm/Cancel buttons become the effective primary/secondary actions for that path, matching UI-SPEC's explicit instruction not to duplicate the outer footer underneath it.

    `LoadingStateView`: `<CardSkeleton height={360} />` plus a slim `Close`-only footer. `NotFoundStateView`: `<EmptyState icon={PackageSearch} title={t('featOrders:scanBarcodeToCart.productNotFound')} description={t('productPeekPanel.notFoundBody')} />` plus `Close`-only footer. `ErrorStateView`: centered `<p className="text-sm text-destructive" role="alert">{t('productPeekPanel.loadError')}</p>` plus `Close`-only footer, no retry button (rescanning is the retry path).

    Mount a `<Toaster richColors position="top-right" />`? No — `PeekApp.tsx` (Task 1) already mounts one at the app-shell level; do not add a second one here.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` and `npm run lint` both pass clean (confirms the `i18next/no-literal-string` gate finds no unwrapped literal in this new `widgets/` file, and FSD boundary rules are respected — no import of `@features/lookup-product-by-barcode` etc. from a disallowed layer).
    - `grep -n "getProductRiskFlag\|useConfirmRiskyAdd" src/widgets/ProductPeekWindow/ui/ProductPeekWindow.tsx` shows both imported and called from the shared `entities/product/model` location — never re-implemented inline (D-05 regression guard).
    - `grep -c "cost_price\|costPrice" src/widgets/ProductPeekWindow/ui/ProductPeekWindow.tsx` returns `0` (privacy prohibition — no cost/supplier price ever referenced in this file).
    - `grep -n "key={product.id}" src/widgets/ProductPeekWindow/ui/ProductPeekWindow.tsx` confirms the remount-on-rescan mechanism is present.
  </acceptance_criteria>
  <done>ProductPeekWindow renders every UI-SPEC state (loading/error/not-found/populated), reuses every existing checkout building block verbatim (risk guard, weight dialog, quantity control, status badge), and both commit paths correctly emit ADD_TO_CART_EVENT and hide the window after passing the risky-add guard.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's slice)

- `src/main.tsx` — `?window=peek` bootstrap branch (modified).
- `src/app/PeekApp.tsx` — new provider shell for the peek window.
- `src/widgets/ProductPeekWindow/ui/ProductPeekWindow.tsx` — new widget, the peek window's entire content.
- `wPanels:productPeekPanel.{addToCart,notFoundBody,loadError,noPhoto,unitWeight,unitPiece,stockCount,skuLabel,barcodeLabel}` i18n keys (en-US + es-MX), extending Plan 18-01's `windowTitle`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Window-creation URL query string → ProductPeekWindow | Untrusted input crosses here — the URL carries only a raw scanned barcode string, never a serialized product object. |
| Peek window → Supabase (authenticated read) | `useLookupProductByBarcode`'s existing parameterized, `is_active`-filtered query — no new table, no new RLS policy, no write path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-04 | Tampering | `ProductPeekWindow`'s `?barcode=` URL param | medium | mitigate | The peek window never trusts a serialized product passed via the URL — it re-derives trusted data itself by calling the same parameterized `useLookupProductByBarcode().lookup()` every other checkout surface uses, filtered to `is_active = true`. |
| T-18-05 | Information Disclosure | Product detail rendered to any authenticated session | low | mitigate | Only `product.basePrice` (the existing sale price) is fetched/rendered — `useLookupProductByBarcode`'s `.select()` never includes `inventory.cost_price` or any supplier field, verified by this plan's `grep -c` acceptance criterion. |
| T-18-06 | Elevation of Privilege | "Add to Cart" bypassing the out-of-stock/near-expiry guard | medium | mitigate | Both commit paths (piece and sold-by-weight) route through `getProductRiskFlag`/`useConfirmRiskyAdd` before any `ADD_TO_CART_EVENT` emission, mirroring `ProductGrid.selectProduct` exactly — no separate, potentially-divergent guard implementation. |

</threat_model>

<verification>
`npm run typecheck && npm run lint` pass clean. Manual visual confirmation is deferred to Plan 18-03's Playwright spec per this project's automated-only testing policy — no `checkpoint:human-verify` step in this plan.
</verification>

<success_criteria>
Scanning a barcode (once Plan 18-01's main-side wiring calls `ensurePeekWindowShown`) opens a window whose entire content — loading, error, not-found, and populated states — is real, reuses every existing checkout building block, and correctly emits `add-to-cart`/relays `barcode-scanned` per Plan 18-01's shared event contract.
</success_criteria>

<output>
Create `.planning/phases/18-barcode-scan-product-peek-window/18-02-SUMMARY.md` when done
</output>
