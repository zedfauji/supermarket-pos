---
phase: 18-barcode-scan-product-peek-window
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/features/add-loose-weight-item/ui/WeightEntryDialog.tsx
  - src/features/add-loose-weight-item/ui/WeightEntryDialog.test.tsx
  - src/features/open-product-peek-window/model/useProductPeekWindow.ts
  - src/features/open-product-peek-window/model/useProductPeekWindow.test.ts
  - src-tauri/capabilities/default.json
  - src/shared/lib/i18n/locales/en-US/wPanels.json
  - src/shared/lib/i18n/locales/es-MX/wPanels.json
  - src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx
  - src/widgets/CheckoutPanel/ui/CheckoutPanel.test.tsx
autonomous: true
requirements: [PEEK-02, PEEK-03, PEEK-04]

must_haves:
  truths:
    - "WeightEntryDialog's confirm() with no onConfirm prop passed behaves exactly as before: mode='add' calls addWeightedItem(product, weightGrams); mode='edit'+tempId calls updateWeightedItem(tempId, weightGrams) — both existing call sites (ProductGrid.tsx, CheckoutPanel.tsx) are unaffected, zero behavior change."
    - "WeightEntryDialog's confirm() with an onConfirm prop passed calls onConfirm(weightGrams) exactly once instead of addWeightedItem/updateWeightedItem, and still calls onOpenChange(false) — this resolves 18-UI-SPEC.md's flagged unresolved 'Weight-path cart-mutation wiring' UI Consideration (the peek window has no access to the main window's cartStore, D-04)."
    - "An invalid weight (weightGrams <= 0 or > 50,000) never calls onConfirm, addWeightedItem, or updateWeightedItem regardless of whether onConfirm is passed — confirm() is a no-op (PEEK-02 boundary reuse, unchanged from today)."
    - "ensurePeekWindowShown(code) checks WebviewWindow.getByLabel('peek') first: when it resolves null, it constructs exactly one new WebviewWindow('peek', {...}) with url `/?window=peek&barcode=<encodeURIComponent(code)>`; when it resolves an existing window, it calls that window's show() then setFocus() then emit('barcode-scanned', {code}) instead of constructing a second window — no duplicate-window-creation throw on a rapid rescan (RESEARCH.md Pitfall 4)."
    - "CheckoutPanel's existing local barcode-scan handler (onScan) keeps its own setSearch(code) call unchanged and additionally calls ensurePeekWindowShown(code) — a scan on the main window still populates search in one action with no added step (fast-checkout regression guard)."
    - "CheckoutPanel listens for the 'barcode-scanned' Tauri event and calls setSearch(event.payload.code) — identical to its own local onScan behavior — so a rescan captured by the peek window (which has OS focus while open) still updates the main window's search box (PEEK-04: 'the main /pos window's own scan-to-search listener continues to fire independently on the same scan event')."
    - "CheckoutPanel listens for the 'add-to-cart' Tauri event: a payload carrying weightGrams calls addWeightedItem(product, weightGrams) exactly once; a payload carrying qty (or no qty, defaulting to 1) calls the existing addItem(product, []) action exactly qty times, reusing addItem's existing same-product quantity-accumulation behavior — no new cartStore action is added."
    - "src-tauri/capabilities/default.json grants the 'peek' window label exactly 5 new permissions (core:webview:allow-create-webview-window, core:window:allow-close, core:window:allow-hide, core:window:allow-show, core:window:allow-set-focus) in addition to the existing core:default/opener:default/dialog:allow-save/fs:allow-write-file/notification:default/updater:default/process:allow-restart set — no broader core:webview:default or core:window:default grant is added (RESEARCH.md Security Domain: minimum-permission requirement)."
  artifacts:
    - src/features/open-product-peek-window/model/useProductPeekWindow.ts::PEEK_WINDOW_LABEL,BARCODE_SCANNED_EVENT,ADD_TO_CART_EVENT,BarcodeScannedPayload,AddToCartPayload,ensurePeekWindowShown
    - src/features/open-product-peek-window/model/useProductPeekWindow.test.ts
    - src/features/add-loose-weight-item/ui/WeightEntryDialog.tsx::WeightEntryDialogProps.onConfirm
    - src-tauri/capabilities/default.json (windows: ["main","peek"], +5 permissions)
  key_links:
    - "CheckoutPanel.onScan(code) -> setSearch(code) [unchanged] + ensurePeekWindowShown(code) -> WebviewWindow.getByLabel('peek') -> (new WebviewWindow | show+setFocus+emit)"
    - "CheckoutPanel listen(BARCODE_SCANNED_EVENT) -> setSearch(code)"
    - "CheckoutPanel listen(ADD_TO_CART_EVENT) -> cartStore.addItem (looped) / addWeightedItem"
    - "WeightEntryDialog.confirm() -> onConfirm?.(weightGrams) ?? addWeightedItem/updateWeightedItem"
---

<assumption_delta_decision>
Detector: `assumption-delta scan "18"` fired on pluralization ("second [window]") and chosen ("choose to add-or-skip").

noun: "app window" — singular (main only) becoming plural (main + peek) for the first time in this codebase.
decision: no-change
rationale: CONTEXT.md D-03 already locks a single persistent, fixed-label ("peek") second window, created lazily and reused (not a general N-window model). REQUIREMENTS.md's v1.4 scope note is explicit: "v1 is /pos-only. Rollout to other screens is deferred — see seed barcode-peek-global-rollout." A generalized window-registry/manager abstraction would serve a rollout this phase explicitly excludes. The hardcoded two-window model (literal labels `"main"`/`"peek"`, no registry data structure) is correct for v1 — promote to a registry only if/when a third window type is actually requested in a future phase.
</assumption_delta_decision>

<objective>
Make the main `/pos` window ready to open, relay scans to, and receive cart-adds from a second Tauri window, before that second window's own UI exists (Plan 18-02) or is E2E-proven (Plan 18-03). Three independent pieces, wired together: (1) `WeightEntryDialog` gets an optional `onConfirm` override so the peek window can reuse it without writing to the wrong (peek-local) cart store instance — this resolves 18-UI-SPEC.md's one flagged open question; (2) `ensurePeekWindowShown`, the race-free window-open/reuse function (RESEARCH.md Pattern 1) plus the exact 5-permission capability grant it needs (Pattern 4); (3) `CheckoutPanel` wired to call it on every scan and to listen for the two cross-window events (`barcode-scanned` relay, `add-to-cart` commit).

Purpose: every subsequent plan builds on a main window that already knows how to open the peek window and receive its two event types — Plan 18-02 can focus entirely on the peek window's own React tree.
Output: `WeightEntryDialog.onConfirm`, `useProductPeekWindow.ts` (`ensurePeekWindowShown` + shared event-name/payload constants), the capability grant, and `CheckoutPanel`'s scan/relay/add-to-cart wiring — all Vitest-covered where mockable, since the peek window's own UI (needed for a live E2E proof) doesn't exist until Plan 18-02.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-barcode-scan-product-peek-window/18-CONTEXT.md
@.planning/phases/18-barcode-scan-product-peek-window/18-RESEARCH.md
@.planning/phases/18-barcode-scan-product-peek-window/18-PATTERNS.md
@.planning/phases/18-barcode-scan-product-peek-window/18-UI-SPEC.md

@src/features/add-loose-weight-item/ui/WeightEntryDialog.tsx
@src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx
@src/widgets/CheckoutPanel/ui/CheckoutPanel.test.tsx
@src-tauri/capabilities/default.json
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: WeightEntryDialog — optional onConfirm override (TDD)</name>
  <files>src/features/add-loose-weight-item/ui/WeightEntryDialog.tsx, src/features/add-loose-weight-item/ui/WeightEntryDialog.test.tsx</files>
  <read_first>
    src/features/add-loose-weight-item/ui/WeightEntryDialog.tsx (full file, already read this session — note the exact current `confirm()` body at lines 69-74 and the `WeightEntryDialogProps` interface at lines 11-18)
    18-RESEARCH.md "Pattern 3: WeightEntryDialog optional confirm override" (exact diff shape, and why: peek window's own bundle has a different Zustand module instance — D-04)
    18-UI-SPEC.md "Flag for planner" callout in the Interaction Contract's "Sold-by-weight path" section, and the one `⚠ unresolved` row in "## UI Considerations" — this task resolves both
    src/entities/tab/model/cartStore.ts lines 21-27 (addWeightedItem/updateWeightedItem signatures — confirms this task does not touch the store itself)
  </read_first>
  <behavior>
    - `confirm()` with mode='add', no `onConfirm` passed, valid weight (e.g. 500g) → `addWeightedItem(product, 500)` called once, `updateWeightedItem` never called, `onOpenChange(false)` called once (regression: unchanged from today).
    - `confirm()` with mode='edit', `tempId` set, no `onConfirm` passed, valid weight → `updateWeightedItem(tempId, weightGrams)` called once, `addWeightedItem` never called, `onOpenChange(false)` called once (regression: unchanged from today).
    - `confirm()` with `onConfirm` passed (either mode), valid weight → `onConfirm(weightGrams)` called once with the exact gram value, `addWeightedItem`/`updateWeightedItem` never called, `onOpenChange(false)` called once.
    - `confirm()` with an invalid weight (0g, or 50,001g) — with or without `onConfirm` passed — calls none of `onConfirm`/`addWeightedItem`/`updateWeightedItem`/`onOpenChange` (no-op, matches existing `isValid` guard).
  </behavior>
  <action>
    Write `WeightEntryDialog.test.tsx` first (RED) rendering `WeightEntryDialog` with `@testing-library/react`, mocking `useCartStore` (`vi.mock('@entities/tab/model/cartStore', ...)` returning `vi.fn()` spies for `addWeightedItem`/`updateWeightedItem`, plus the real exported `calcWeightedLineTotal` re-implemented inline or imported from the actual module — check whether the module needs partial-mocking via `vi.importActual` so `calcWeightedLineTotal` still works for the helper-text render). Drive the keypad via the component's own numeric `Button`s (e.g. click "5", "0", "0" to reach 500g) or via the existing global-keydown handler (`fireEvent.keyDown(window, { key: '5' })` etc., matching the component's own keydown-driven input path) before clicking the Confirm button (`getByRole('button', { name: /add to cart|save weight/i })`). Confirm the test fails against the current (no `onConfirm` prop) implementation for the third case before touching the component.

    Then (GREEN) add `onConfirm?: (weightGrams: number) => void;` to `WeightEntryDialogProps` (after `tempId?: string;`) and destructure it in the component's props. In `confirm()`, change the branch to: if `onConfirm` is defined, call `onConfirm(weightGrams)`; else if `mode === 'edit' && tempId`, call `updateWeightedItem(tempId, weightGrams)`; else call `addWeightedItem(product, weightGrams)`. Keep `onOpenChange(false)` as the unconditional last step, guarded by the existing `if (!isValid) return;` at the top — no other line in the file changes. Do not touch `ProductGrid.tsx` or `CheckoutPanel.tsx`'s existing `<WeightEntryDialog ... />` usages — neither passes `onConfirm`, so both keep their current store-writing behavior unchanged (Plan 18-02 is the first caller to pass it).
  </action>
  <verify>
    <automated>npx vitest run src/features/add-loose-weight-item/ui/WeightEntryDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/features/add-loose-weight-item/ui/WeightEntryDialog.test.tsx` passes all 4 cases.
    - `grep -n "onConfirm" src/features/add-loose-weight-item/ui/WeightEntryDialog.tsx` shows the prop declared on `WeightEntryDialogProps` and referenced inside `confirm()`.
    - `npm run typecheck` passes (confirms the additive-only prop shape is compatible with `exactOptionalPropertyTypes` and both existing unmodified call sites).
  </acceptance_criteria>
  <done>WeightEntryDialog accepts an optional onConfirm override that fully replaces its default cartStore-writing behavior when supplied, with zero change to either existing caller — the UI-SPEC's one flagged open question is resolved.</done>
  <reversibility rating="reversible">Additive, backward-compatible prop — removing it later is a one-line revert with no data/migration impact.</reversibility>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: ensurePeekWindowShown (race-free window open/reuse) + capability grant + window-title i18n key (TDD)</name>
  <files>src/features/open-product-peek-window/model/useProductPeekWindow.ts, src/features/open-product-peek-window/model/useProductPeekWindow.test.ts, src-tauri/capabilities/default.json, src/shared/lib/i18n/locales/en-US/wPanels.json, src/shared/lib/i18n/locales/es-MX/wPanels.json</files>
  <read_first>
    18-RESEARCH.md "Pattern 1: Lazy window creation with race-free first-payload delivery" (the exact function body to implement, and why an `emit()` right after construction races the new window's bootstrap — Pitfall 1) and "Pattern 4: Capability grant" (the exact JSON shape and the exact 5 verified permission identifiers)
    18-PATTERNS.md sections "`src/features/open-product-peek-window/...`" and "`src-tauri/capabilities/default.json` (modified)" (both already extracted, line-sourced excerpts)
    src-tauri/capabilities/default.json (full current 17-line file, already read this session)
    node_modules/@tauri-apps/api/webviewWindow.js (confirmed this session: exports `WebviewWindow`, `getAllWebviewWindows`, `getCurrentWebviewWindow`; `WebviewWindow.getByLabel` is a `static async` method)
    node_modules/@tauri-apps/api/event.js (confirmed this session: `emit(event, payload)` is `async function emit(event, payload) { await invoke('plugin:event|emit', { event, payload }); }` — no other side effects to mock beyond `invoke`, which this task's own test bypasses entirely by mocking the whole `@tauri-apps/api/event` module)
    src/shared/lib/pos-printer.ts lines 1-9 (the exact `import i18n from '@shared/lib/i18n'; ... i18n.t('featOrders:printer...')` pattern — the precedent for calling `i18n.t()` from a plain async function outside any component, since `ensurePeekWindowShown` cannot use the `useTranslation()` hook)
    src/shared/lib/i18n/locales/en-US/wPanels.json and es-MX/wPanels.json (full files, already read — insertion point: add a new top-level `"productPeekPanel"` object; this task adds only the `windowTitle` key, Plan 18-02 extends the same object with the rest)
  </read_first>
  <behavior>
    - `WebviewWindow.getByLabel('peek')` resolves `null` → `ensurePeekWindowShown('7501234567890')` constructs exactly one `WebviewWindow` instance, called with `('peek', { url, title, width: 480, height: 720, minWidth: 400, minHeight: 600, resizable: true, center: true })` where `url === '/?window=peek&barcode=7501234567890'` and `title` equals the translated `wPanels:productPeekPanel.windowTitle` value; `emit` is never called.
    - A barcode needing URL-encoding (e.g. `'123 456'`) → the constructed `url` contains `barcode=123%20456` (confirms `encodeURIComponent`, not a raw template interpolation).
    - `WebviewWindow.getByLabel('peek')` resolves an existing mock window object (with `show`/`setFocus` spies) → `ensurePeekWindowShown('code2')` calls `.show()` then `.setFocus()` on that object, then calls `emit('barcode-scanned', { code: 'code2' })` exactly once; the `WebviewWindow` constructor is NOT invoked a second time (Pitfall 4 regression guard — a rapid rescan never throws Tauri's unique-label error).
  </behavior>
  <action>
    Write `useProductPeekWindow.test.ts` first (RED). Mock `@tauri-apps/api/webviewWindow` with a factory returning `{ WebviewWindow: <a vi.fn() constructor mock with a static getByLabel property, itself a vi.fn(), attached via Object.assign or direct property assignment on the mock function> }`, and mock `@tauri-apps/api/event` with `{ emit: vi.fn().mockResolvedValue(undefined) }`. Confirm the test fails because the module doesn't exist yet.

    Then (GREEN) create `src/features/open-product-peek-window/model/useProductPeekWindow.ts` exporting: `export const PEEK_WINDOW_LABEL = 'peek';`, `export const BARCODE_SCANNED_EVENT = 'barcode-scanned';`, `export const ADD_TO_CART_EVENT = 'add-to-cart';` (three shared string constants — Plan 18-02's `ProductPeekWindow.tsx` imports these same three constants rather than re-typing the literals, so the two sides of each event channel can never drift; this is a lighter-weight alternative to a dedicated `tauriPeekEvents.ts` wrapper file, which 18-PATTERNS.md flagged as a YAGNI candidate — skipped here, add a dedicated wrapper only if a third event/consumer appears later). Export `export interface BarcodeScannedPayload { code: string }` and `export interface AddToCartPayload { product: Product; qty?: number; weightGrams?: number }` (import `Product` from `@shared/lib/domain`). Implement `export async function ensurePeekWindowShown(code: string): Promise<void>` exactly per RESEARCH.md Pattern 1: `const existing = await WebviewWindow.getByLabel(PEEK_WINDOW_LABEL);`; if `existing === null`, construct `new WebviewWindow(PEEK_WINDOW_LABEL, { url: \`/?window=peek&barcode=${encodeURIComponent(code)}\`, title: i18n.t('wPanels:productPeekPanel.windowTitle'), width: 480, height: 720, minWidth: 400, minHeight: 600, resizable: true, center: true })` and `return;`; else `await existing.show(); await existing.setFocus(); await emit(BARCODE_SCANNED_EVENT, { code });`. Import `i18n` (default export) from `@shared/lib/i18n`, `WebviewWindow` from `@tauri-apps/api/webviewWindow`, `emit` from `@tauri-apps/api/event`.

    In `src-tauri/capabilities/default.json`: change `"windows": ["main"]` to `"windows": ["main", "peek"]`; change `"description": "Capability for the main window"` to `"description": "Capability for the main and peek windows"`; insert exactly 5 new entries into the `"permissions"` array immediately after `"core:default"` (before `"opener:default"`): `"core:webview:allow-create-webview-window"`, `"core:window:allow-close"`, `"core:window:allow-hide"`, `"core:window:allow-show"`, `"core:window:allow-set-focus"`. Leave the existing 6 permission entries (`core:default`, `opener:default`, `dialog:allow-save`, `fs:allow-write-file`, `notification:default`, `updater:default`, `process:allow-restart`) untouched and in their current relative order.

    In both `src/shared/lib/i18n/locales/en-US/wPanels.json` and `es-MX/wPanels.json`, add a new top-level object `"productPeekPanel"` (after the existing `"employeeSelector"` key, following the file's existing flat-top-level-key convention) containing only `"windowTitle": "Product Details"` (en-US) / `"windowTitle": "Detalles del producto"` (es-MX) for this task — Plan 18-02 extends this same object with the remaining keys the widget needs.
  </action>
  <verify>
    <automated>npx vitest run src/features/open-product-peek-window/model/useProductPeekWindow.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/features/open-product-peek-window/model/useProductPeekWindow.test.ts` passes all 3 cases.
    - `grep -n '"windows"' -A2 src-tauri/capabilities/default.json` shows `"main"` and `"peek"` both present.
    - `grep -c "core:webview:allow-create-webview-window\|core:window:allow-close\|core:window:allow-hide\|core:window:allow-show\|core:window:allow-set-focus" src-tauri/capabilities/default.json` returns `5`.
    - `grep -n "productPeekPanel" src/shared/lib/i18n/locales/en-US/wPanels.json src/shared/lib/i18n/locales/es-MX/wPanels.json` shows the new key in both files.
    - `npm run typecheck` passes.
  </acceptance_criteria>
  <done>ensurePeekWindowShown correctly branches between first-open (URL-param delivery, no race) and reuse (show/focus/relay), Vitest-covered with mocked Tauri modules; the capability grant adds exactly the 5 documented permissions for the new "peek" window label; the window's native title bar copy is locale-aware from creation time.</done>
  <reversibility rating="reversible">Capability entries and the new module are additive; reverting is a clean file-level revert with no schema/data impact.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Wire CheckoutPanel — open peek on scan, relay rescans, receive add-to-cart</name>
  <files>src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx, src/widgets/CheckoutPanel/ui/CheckoutPanel.test.tsx</files>
  <read_first>
    src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx (full file, already read — exact current `onScan` callback at lines 33-38, and the existing `addItem`/`items`/`removeItem`/etc. selector block at lines 41-49)
    src/widgets/CheckoutPanel/ui/CheckoutPanel.test.tsx (full file, already read — the existing `vi.mock(...)` calls at the top, e.g. for `@entities/product`, `@widgets/ProductGrid/ui/ProductGrid`, `sonner`; the `scanAndFlush`/`scanKeys` helpers)
    18-RESEARCH.md "Pattern 2: Bidirectional single-channel scan relay" (main-side code block, "CheckoutPanel.tsx — additive, does not touch the existing useBarcodeScanner call")
    18-PATTERNS.md "`src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx` (modified)" section and "Cart mutation API (destination for `add-to-cart` event, unchanged)" under Shared Patterns
    This plan's Task 2 output: `src/features/open-product-peek-window/model/useProductPeekWindow.ts` (`ensurePeekWindowShown`, `BARCODE_SCANNED_EVENT`, `ADD_TO_CART_EVENT`, `AddToCartPayload`)
    src/entities/tab/model/cartStore.ts lines 21, 24 (`addItem`/`addWeightedItem` signatures — confirms the loop-N-times approach for `qty` needs no new store action)
  </read_first>
  <action>
    In `CheckoutPanel.tsx`, import `listen` from `@tauri-apps/api/event` and `ensurePeekWindowShown`, `BARCODE_SCANNED_EVENT`, `ADD_TO_CART_EVENT`, and the `AddToCartPayload` type from `@features/open-product-peek-window/model/useProductPeekWindow`. Change the existing `onScan: code => { setSearch(code); }` to `onScan: code => { setSearch(code); void ensurePeekWindowShown(code); }` — the local scan behavior is otherwise completely unchanged (still gated by the existing `scannerEnabled` check, still a single `useBarcodeScanner` call).

    Add one new `useEffect(() => { ... }, [])` (after the existing `useBarcodeScanner` call) that subscribes to both events and cleans up both on unmount: `listen<{ code: string }>(BARCODE_SCANNED_EVENT, event => { setSearch(event.payload.code); })` (identical effect to the local `onScan`'s `setSearch` call — PEEK-04's "main window's own scan-to-search listener continues to fire" requirement), and `listen<AddToCartPayload>(ADD_TO_CART_EVENT, event => { const { product, qty, weightGrams } = event.payload; if (weightGrams != null) { addWeightedItem(product, weightGrams); } else { const times = qty ?? 1; for (let i = 0; i < times; i += 1) { addItem(product, []); } } })`. Since `addItem`'s existing same-product+no-modifiers accumulation logic increments quantity by 1 per call, looping `times` calls is the correct, no-new-store-action way to add a peek-window-set quantity in one shot (documented simplification — skip a dedicated `addItemWithQuantity` store action; add one only if a future caller needs a single atomic N-unit add rather than N additive ones). Store both `listen()` calls' returned unlisten-promises and call both inside the effect's cleanup function, mirroring the exact `void unlistenPromise.then(unlisten => { unlisten(); });` shape from RESEARCH.md's code example. Both `addItem` and `addWeightedItem` (destructured from `useCartStore` a few lines above the effect) are stable Zustand action references, so the empty `[]` dependency array is correct — no ref-wrapping needed (same stability guarantee `useBarcodeScanner`'s own `onScanRef` pattern exists to work around for props that DO change identity per render, which these do not).

    In `CheckoutPanel.test.tsx`, add two new `vi.mock(...)` calls near the top (alongside the existing ones): `vi.mock('@tauri-apps/api/event', () => ({ listen: vi.fn().mockResolvedValue(() => undefined) }));` and `vi.mock('@features/open-product-peek-window/model/useProductPeekWindow', () => ({ ensurePeekWindowShown: vi.fn().mockResolvedValue(undefined), BARCODE_SCANNED_EVENT: 'barcode-scanned', ADD_TO_CART_EVENT: 'add-to-cart' }));` — this is required because the test's `renderWithProviders(<CheckoutPanel />)` calls now exercise the real `@tauri-apps/api/event` import chain (which needs `transformCallback` from `@tauri-apps/api/core`, not provided by the project's global `test-setup.ts` mock of that module) and the real `ensurePeekWindowShown` (which would otherwise try to construct a real `WebviewWindow` in jsdom). Do not modify `src/shared/lib/test-setup.ts` — this mock is scoped to the one test file whose subject under test changed, matching this file's existing per-test-file `vi.mock` granularity (e.g. its own `@widgets/ProductGrid/ui/ProductGrid` and `@features/checkout-sale/model/useCheckoutSale` mocks) rather than widening the global mock's blast radius to every other test in the suite.
  </action>
  <verify>
    <automated>npx vitest run src/widgets/CheckoutPanel/ui/CheckoutPanel.test.tsx && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/widgets/CheckoutPanel/ui/CheckoutPanel.test.tsx` passes all 4 pre-existing tests unmodified (regression check — scanning still only populates search, never adds to cart, and the payment/weight-dialog scanner-disable gates are untouched).
    - `grep -n "ensurePeekWindowShown\|listen(" src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx` shows both the `onScan` call site and the two `listen(...)` subscriptions.
    - `npm run typecheck` passes.
  </acceptance_criteria>
  <done>CheckoutPanel opens/reuses the peek window on every scan without changing its own scan-to-search behavior, and correctly receives both cross-window event types — the main window half of PEEK-03/PEEK-04 is complete and regression-proven against the pre-existing test suite.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's slice)

- `src/features/open-product-peek-window/model/useProductPeekWindow.ts` — new module: `PEEK_WINDOW_LABEL`, `BARCODE_SCANNED_EVENT`, `ADD_TO_CART_EVENT` string constants; `BarcodeScannedPayload`, `AddToCartPayload` interfaces; `ensurePeekWindowShown(code: string): Promise<void>`.
- `src/features/open-product-peek-window/model/useProductPeekWindow.test.ts` — new Vitest file.
- `WeightEntryDialogProps.onConfirm` — new optional prop on `src/features/add-loose-weight-item/ui/WeightEntryDialog.tsx`.
- `src-tauri/capabilities/default.json` — `windows: ["main", "peek"]`, +5 permission entries.
- `wPanels:productPeekPanel.windowTitle` i18n key (en-US + es-MX) — extended by Plan 18-02.
- Two new `listen()` subscriptions and one modified `onScan` call inside `src/widgets/CheckoutPanel/ui/CheckoutPanel.tsx`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Tauri capability manifest → window runtime permissions | `src-tauri/capabilities/default.json` is the sole declarative gate on what the new "peek" window label may do at the OS/webview level — no code can bypass it. |
| Cross-window Tauri event bus (`barcode-scanned`, `add-to-cart`) | Process-internal only (this app's own two windows); not network-exposed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Elevation of Privilege | `src-tauri/capabilities/default.json` new "peek" window grant | medium | mitigate | Exactly 5 permissions added (`allow-create-webview-window`, `allow-close`, `allow-hide`, `allow-show`, `allow-set-focus`) — never the broader `core:webview:default`/`core:window:default` bundles. Verified by this plan's `grep -c` acceptance criterion (must equal exactly 5, not more). |
| T-18-02 | Tampering | `add-to-cart` / `barcode-scanned` Tauri events (process-internal) | low | accept | Only this app's own two windows exist in this closed desktop app; Tauri's event system is process-internal, not network-exposed. No additional mitigation beyond what's already true of the existing single-window cart-mutation path (per RESEARCH.md Security Domain). |
| T-18-03 | Denial of Service | Rapid re-scan calling `ensurePeekWindowShown` before the prior call settles | low | mitigate | `WebviewWindow.getByLabel` is checked before every construction (Task 2), so a burst of scans never throws Tauri's unique-label error — Vitest-covered regression guard. |

</threat_model>

<verification>
`npx vitest run src/features/add-loose-weight-item/ui/WeightEntryDialog.test.tsx src/features/open-product-peek-window/model/useProductPeekWindow.test.ts src/widgets/CheckoutPanel/ui/CheckoutPanel.test.tsx` all pass. `npm run typecheck` passes clean across every file this plan touches.
</verification>

<success_criteria>
The main `/pos` window can open/reuse a "peek" window (once Plan 18-02 gives it something real to load), correctly relays rescans back into its own search box, and correctly applies an `add-to-cart` event to the real cart — all proven by Vitest against mocked Tauri APIs, since the peek window's own UI (needed for a live E2E proof) doesn't exist until Plan 18-02.
</success_criteria>

<output>
Create `.planning/phases/18-barcode-scan-product-peek-window/18-01-SUMMARY.md` when done
</output>
