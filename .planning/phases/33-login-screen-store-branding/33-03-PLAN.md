---
phase: 33-login-screen-store-branding
plan: 03
type: execute
wave: 3
depends_on: ["33-01", "33-02"]
autonomous: true
agent_hint: Frontend Developer
requirements: [STORE-01, STORE-02, STORE-03]
files_modified:
  - src/entities/settings/ui/StoreLogoImage.tsx
  - src/entities/settings/index.ts
  - src/widgets/SettingsTabsPanel/tabs/GeneralSettingsTab.tsx
  - src/pages/login/index.tsx
  - src/shared/lib/i18n/locales/es-MX/wAdmin.json
  - src/shared/lib/i18n/locales/en-US/wAdmin.json
  - src/shared/lib/i18n/locales/es-MX/pages.json
  - src/shared/lib/i18n/locales/en-US/pages.json
  - e2e/settings/store-branding.spec.ts
  - e2e/settings/store-branding-rls.spec.ts
  - e2e/visual/login-branding-baseline.spec.ts

estimate:
  tokens: 46000
  raw_tokens: 92000
  tasks: 3
  confidence: high

must_haves:
  truths:
    # --- Phase goal (logo half) ---
    - "An admin can pick, drag or paste an image file into Settings -> General and see it become the login screen's hero logo without editing anything else."
    - "The login screen's hero logo is rendered at 128px with object-contain inside a padded, light-backdrop tile, never cropped."
    # --- $COVERAGE probe rows ---
    - "STORE-02/concurrency: the login page holds no Realtime subscription to settings; a logo replaced while a login screen is already open is picked up on that page's next mount or refetch, not pushed live, and the stale render is never a broken image."
    - "STORE-03/empty: the visual-regression spec captures the unconfigured (no name, no logo) login aside as its own named baseline, not only the configured one."
    - "STORE-03/encoding: the visual baselines pin an ASCII fixture store name so the PNG diff is deterministic across machines; non-Latin name rendering is asserted textually in the functional spec instead, where it is a string comparison rather than a pixel comparison."
    - "STORE-03/idempotency: every branding spec restores the settings row and removes any object it uploaded in afterAll, so a second consecutive run passes with no manual cleanup."
    - "STORE-03/concurrency: each spec that mutates the singleton general settings row declares test.describe.serial — required in e2e/visual/ because playwright.visual.config.ts sets fullyParallel true, unlike the functional config which pins workers to 1."
    # --- UI-SPEC ## UI Considerations: login-hero-branding ---
    - "login-hero-branding/loading: while useSettings() is pending the unconfigured branch renders (no skeleton), and once general has resolved with a storeLogoPath the hero tile shows a size-32 rounded-3xl Skeleton rather than reverting to the 44px tile."
    - "login-hero-branding/error: when the logo image fails to load with a storeLogoPath present, the hero tile shows a centered ImageOff at size-8 inside the same 128px frame and the store name still renders."
    - "login-hero-branding/populated: name and logo both set renders a 128px object-contain logo plus the store name at text-3xl/600, with the terminal-id caption unchanged."
    - "login-hero-branding/partial: storeName set with no logo renders a hero-size ShoppingBasket (128px frame, not the 44px tile) beside the real name; a logo with no storeName renders the hero logo beside the generic t('login.brand') string, and neither combination crashes."
    # --- UI-SPEC ## UI Considerations: settings-logo-upload ---
    - "settings-logo-upload/empty: with no logo stored, the control is a dashed 80px tile with an ImagePlus icon, a single outline Choose-file button and the format hint below it."
    - "settings-logo-upload/loading: the three in-flight states are distinct — resolving, processing/resize, and uploading — and the file input and both buttons are disabled in all three."
    - "settings-logo-upload/error: validation, upload, link, remove, offline and image-load failures each render their own keyed inline role=alert string plus a matching error toast, and the tile returns to its previous state rather than a broken one."
    - "settings-logo-upload/populated: a stored logo renders in a solid-border 80px tile with object-contain, beside an outline Replace button and a destructive Remove button."
    - "settings-logo-upload/overflow: the tile is overflow-hidden with object-contain so an oversized source is contained rather than clipped mid-subject, and the button row wraps instead of overflowing on narrow widths."
    - "settings-logo-upload/zero-one-many: not applicable — a single logo slot, never a collection; no count or plural copy exists in this control."
    - "settings-logo-upload/long-text: hint and error strings sit in a flex column beside the fixed tile and wrap; no validation message is truncated."
  prohibitions:
    - "The receipt-only base64 logo field and its rendering component must not be altered, overwritten or repurposed to serve the store logo."
    - "The mobile login header and the unconfigured fallback block must not be enlarged, rebranded or redesigned."
    - "No human/manual click-through may be substituted for an automated Playwright assertion of this phase's behaviour."
  artifacts:
    - path: "src/entities/settings/ui/StoreLogoImage.tsx"
      provides: "Store-logo image with its own fallback passthrough, reading general.storeLogoPath"
      contains: "login-store-logo"
    - path: "e2e/settings/store-branding-rls.spec.ts"
      provides: "Database-level proof that cashier and anonymous writes to the bucket are denied"
    - path: "e2e/visual/login-branding-baseline.spec.ts"
      provides: "Configured and unconfigured login-aside baselines plus a hero-size measurement"
  key_links:
    - from: "src/pages/login/index.tsx"
      to: "src/entities/settings/ui/StoreLogoImage.tsx"
      via: "hero tile renders StoreLogoImage with a ShoppingBasket fallback"
      pattern: "StoreLogoImage"
    - from: "src/widgets/SettingsTabsPanel/tabs/GeneralSettingsTab.tsx"
      to: "src/entities/settings/model/useStoreLogoUpload.ts"
      via: "upload/remove mutations invoked from the logo control"
      pattern: "useStoreLogoUpload|useRemoveStoreLogo"
    - from: "src/entities/settings/ui/StoreLogoImage.tsx"
      to: "src/entities/settings/model/store-logo-file.ts"
      via: "resolves the stored object key to a renderable URL"
      pattern: "storeLogoPublicUrl|useStoreLogoUrl"
---

<objective>
Expand the proven tracer into the full feature: an upload control in Settings, a hero
logo on the login screen, and the automated coverage STORE-03 demands — functional,
RLS-boundary and visual-regression.

Purpose: the logo half of STORE-01/STORE-02, and all of STORE-03.
Output: `StoreLogoImage`, the `GeneralSettingsTab` logo control, the reshaped hero, 24
new i18n keys across four catalogs, and three Playwright specs.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/33-login-screen-store-branding/33-CONTEXT.md
@.planning/phases/33-login-screen-store-branding/33-PATTERNS.md
@.planning/phases/33-login-screen-store-branding/33-UI-SPEC.md
@.planning/phases/33-login-screen-store-branding/33-01-SUMMARY.md
@.planning/phases/33-login-screen-store-branding/33-02-SUMMARY.md
</context>

## Artifacts this phase produces

See the table in `33-01-PLAN.md` § "Artifacts this phase produces" — it enumerates every
symbol, bucket, policy, migration filename, i18n key and testid created across all three
plans of Phase 33. The symbols introduced *by this plan* are the `StoreLogoImage`
component, the `login.logoAlt` i18n key, the 22 `generalSettingsTab.logo*` i18n keys
listed in `33-UI-SPEC.md` § Copywriting Contract, the testids
`login-store-logo`, `settings-store-logo-dropzone`, `settings-store-logo-file-input`,
`settings-store-logo-preview`, `settings-store-logo-replace`,
`settings-store-logo-remove`, and the two new spec files.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Store-logo upload control in Settings -> General</name>
  <files>src/widgets/SettingsTabsPanel/tabs/GeneralSettingsTab.tsx, src/shared/lib/i18n/locales/es-MX/wAdmin.json, src/shared/lib/i18n/locales/en-US/wAdmin.json</files>
  <read_first>
    - `src/widgets/SettingsTabsPanel/tabs/GeneralSettingsTab.tsx` (whole file, as left by plan 01)
    - `src/features/manage-products/ui/tabs/ProductPhotoTab.tsx` (the accept attribute and hidden file input at ~lines 35 and 252-260, the keyboard-accessible drop zone at ~lines 91-96 and 193-201, the drag handlers at ~lines 152-172, the in-flight overlay at ~lines 241-250, the `errorCopyFor(error: AppError)` switch at ~lines 60-84, and the remove `ConfirmDialog` at ~lines 305-317)
    - `src/entities/settings/model/useStoreLogoUpload.ts` and `store-logo-file.ts` (plan 02's exports and their input shapes)
    - `.planning/phases/33-login-screen-store-branding/33-UI-SPEC.md` § "Layout & Interaction Contract" §2 and the full § "Copywriting Contract"
    - `src/shared/lib/i18n/locales/es-MX/wAdmin.json` and `.../en-US/wAdmin.json` (the `generalSettingsTab` block)
    - `src/shared/lib/result.ts` (the `PHOTO_*` AppErrorCodes the error switch maps)
  </read_first>
  <behavior>
    Testable expectations for this control, asserted by task 3's Playwright cases rather
    than a component unit test (this is DOM-and-network glue, not pure logic):
    - With no logo stored, `settings-store-logo-dropzone` is present, `settings-store-logo-remove` is absent.
    - After attaching a valid PNG to `settings-store-logo-file-input`, `settings-store-logo-preview` becomes visible and both `settings-store-logo-replace` and `settings-store-logo-remove` appear.
    - Attaching an `image/heic` file renders an inline `role="alert"` carrying the unsupported-format copy and leaves the tile in its prior state.
    - Clicking Remove opens an `alertdialog` whose title is the remove-confirm copy; confirming clears the preview and returns the control to its empty state.
    - Editing the store-name field and pressing Save after a logo upload leaves `general.storeLogoPath` unchanged (asserted via a service-client read).
  </behavior>
  <action>
    Add a new `md:col-span-2` row to `GeneralSettingsTab`'s existing `grid gap-4 md:grid-cols-2`,
    positioned directly below the `storeName` field — in this tab, not a new dedicated Branding
    tab (D-11), so every store-identity field stays in one place with no new tab or i18n
    namespace. Follow 33-UI-SPEC.md §2's markup contract exactly: a `Label` bound to `generalSettingsTab.logoLabel`, then a
    `flex flex-wrap items-center gap-4` row holding an 80px tile and a button column.

    The tile is `size-20 shrink-0 rounded-xl` with `overflow-hidden`, `border-2 border-dashed
    border-border-strong bg-muted p-2` when empty and `border border-border bg-card` (solid)
    when populated, plus `transition-colors duration-150`. It is a single tab stop —
    `role="button" tabIndex={0}` with an `aria-label` from `generalSettingsTab.logoChooseFile`
    and an Enter/Space `onKeyDown` that opens the picker — and carries
    `data-testid="settings-store-logo-dropzone"`. Empty renders `ImagePlus` at `size-6
    text-muted-foreground`; populated renders an `<img data-testid="settings-store-logo-preview"
    class="max-h-full max-w-full object-contain">`; drag-over swaps the border and background to
    `border-brand bg-brand-soft/80`. Lift the drag handlers and the
    `firstImageFromDataTransfer` extraction from `ProductPhotoTab`, and lift its hidden
    `<input type="file" className="sr-only">` including the `event.target.value = ''` reset in
    the change handler — without that reset, re-picking the same file after an error fires no
    change event. Give the input `data-testid="settings-store-logo-file-input"` and an `accept`
    attribute built by joining `ACCEPTED_LOGO_MIME_TYPES`.

    The button column is a `flex flex-col gap-2`: a `flex flex-wrap gap-2` row of `POSButton`s
    with `touchSize="default"` — `variant="outline"` carrying `logoChooseFile` when empty or
    `logoReplace` with an `Upload` icon and `data-testid="settings-store-logo-replace"` when
    populated, plus a `variant="destructive"` `logoRemove` with a `Trash2` icon and
    `data-testid="settings-store-logo-remove"` shown only when populated — then a
    `text-sm text-muted-foreground` hint from `logoHint`, then the inline error slot as a
    `<p role="alert" class="text-sm text-destructive">` rendered only when an error is held.

    Wire the states from 33-UI-SPEC.md §2's state table: processing shows a centred
    `LoadingSpinner` in the tile with the hint text swapped to `logoProcessing`; uploading dims
    the tile under an `absolute inset-0 bg-popover/70` overlay with the hint swapped to
    `logoUploading`; both disable the file input and every button. A populated row whose URL has
    not resolved yet shows `Skeleton className="size-20 rounded-xl"` in place of the tile — under
    plan 02's public-URL resolution this state is unreachable because the URL is built
    synchronously, so render it defensively and note that in a comment rather than deleting the
    branch. An `<img>` `onError` shows `ImageOff` at `size-5 text-muted-foreground` in the tile
    while leaving Replace and Remove enabled so the admin can recover.

    Port `errorCopyFor(error: AppError)` from `ProductPhotoTab` as a switch over AppErrorCode,
    mapping each to a `generalSettingsTab.logoError*` key: unsupported type, too large, decode,
    upload, link, remove, offline and load. Show the same string both inline and through
    `toast.error`, matching the tab's existing save-error pattern. Success paths fire
    `toast.success` with `logoUploaded` / `logoRemoved`. Remove opens a `ConfirmDialog`
    with `variant="destructive"` and the four `logoRemoveConfirm*` keys.

    The logo commits on its own action, immediately — it is deliberately **not** folded into the
    Save-General dirty-state flow, because the underlying mutation is a Storage upload plus a
    settings-blob read-merge-write independent of the text form state. After a successful upload
    or remove, refresh the form's `storeLogoPath` from the refetched snapshot so a later Save
    does not write a stale path back over the new one.

    Add all 22 `generalSettingsTab.logo*` keys to both `src/shared/lib/i18n/locales/es-MX/wAdmin.json`
    and `.../en-US/wAdmin.json`, with the exact es-MX and en-US values given in 33-UI-SPEC.md's
    Copywriting Contract table — including the interpolation placeholders on the error strings.
    Both catalogs must gain the same key set; a key present in only one locale is a defect.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit, or any "error TS" line in the output</fails_when>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit, or any "error" line in the ESLint output — an `i18next/no-literal-string` hit here means a hardcoded UI string slipped into the widget</fails_when>
    <automated>node -e "const a=require('./src/shared/lib/i18n/locales/es-MX/wAdmin.json').generalSettingsTab,b=require('./src/shared/lib/i18n/locales/en-US/wAdmin.json').generalSettingsTab;const ka=Object.keys(a).filter(k=>k.startsWith('logo')).sort(),kb=Object.keys(b).filter(k=>k.startsWith('logo')).sort();console.log(ka.length,kb.length,JSON.stringify(ka)===JSON.stringify(kb))"</automated>
    <fails_when>prints a count below 22 for either locale, or prints `false` for the key-set comparison</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "settings-store-logo-dropzone\|settings-store-logo-file-input\|settings-store-logo-preview\|settings-store-logo-replace\|settings-store-logo-remove" src/widgets/SettingsTabsPanel/tabs/GeneralSettingsTab.tsx` is at least 5.
    - `grep -c "useStoreLogoUpload\|useRemoveStoreLogo" src/widgets/SettingsTabsPanel/tabs/GeneralSettingsTab.tsx` is at least 2.
    - `grep -c "ConfirmDialog" src/widgets/SettingsTabsPanel/tabs/GeneralSettingsTab.tsx` is at least 2 (import plus usage).
    - `grep -c 'role="alert"' src/widgets/SettingsTabsPanel/tabs/GeneralSettingsTab.tsx` is at least 1.
    - `grep -c "ProtectedAction" src/widgets/SettingsTabsPanel/tabs/GeneralSettingsTab.tsx` is still at least 2 — per D-12 the logo control sits inside the existing admin-only `manage_settings` gate and introduces no second gate and no new permission.
    - `grep -c "logoDataUrl\|useReceiptSettings" src/widgets/SettingsTabsPanel/tabs/GeneralSettingsTab.tsx` is 0 — the receipt logo is untouched by this control.
    - The node key-parity check prints 22, 22, true.
    - `git diff --name-only` for this task does not list `src/widgets/LogoImage/index.tsx` or any `receipt_settings` migration.
  </acceptance_criteria>
  <done>An admin can choose, drag or paste a logo in Settings -> General, see it preview inline, replace it, and remove it behind a destructive confirm — all in both locales, all behind the existing `manage_settings` gate.</done>
</task>

<task type="auto">
  <name>Task 2: StoreLogoImage and the login hero logo</name>
  <files>src/entities/settings/ui/StoreLogoImage.tsx, src/entities/settings/index.ts, src/pages/login/index.tsx, src/shared/lib/i18n/locales/es-MX/pages.json, src/shared/lib/i18n/locales/en-US/pages.json</files>
  <read_first>
    - `src/widgets/LogoImage/index.tsx` (whole file, 25 lines — the props contract and fallback passthrough being mirrored; this file must NOT be modified)
    - `src/pages/login/index.tsx` (whole file, as left by plan 01)
    - `src/entities/settings/model/store-logo-file.ts` (plan 02's URL resolution export)
    - `src/entities/settings/index.ts` (the barrel the new component is exported through)
    - `src/features/manage-products/ui/tabs/ProductPhotoTab.tsx` (~lines 210-230 — the skeleton / ImageOff / real-image three-way render)
    - `.planning/phases/33-login-screen-store-branding/33-UI-SPEC.md` § "Layout & Interaction Contract" §1 and its loading/error paragraphs
    - `src/shared/lib/i18n/locales/es-MX/pages.json` and `.../en-US/pages.json` (the `login` block)
  </read_first>
  <action>
    Create `src/entities/settings/ui/StoreLogoImage.tsx`, a new sibling concept to
    `LogoImage` — not an edit of it. `LogoImage` is hardcoded to the receipt settings'
    base64 data URL and stays receipt-only (33-RESEARCH.md Pitfall 3); overloading one
    component with two unrelated sources and two rendering strategies is the failure this
    separation exists to prevent.

    Mirror `LogoImage`'s props contract — `className`, `alt`, `fallback: ReactNode` — and its
    widget-owns-its-data habit: the component calls `useSettings()` itself and reads
    `general.storeLogoPath` rather than making `LoginPage` prop-drill. Resolve the path to a URL
    through plan 02's export. Return the `fallback` when there is no stored path. Render
    `<img data-testid="login-store-logo" class="max-h-full max-w-full object-contain">` — `object-contain`,
    never `object-cover`, because a cropped logo is a broken logo. On the image's `onError`,
    render a centred `ImageOff` at `size-8` rather than the caller's fallback, so a broken object
    is visibly distinct from "no logo configured". If plan 02 chose the private-bucket option, add
    the signing-in-flight branch rendering the `Skeleton` the caller sizes through `className`.
    Export it from `src/entities/settings/index.ts`.

    In `src/pages/login/index.tsx`, replace the placeholder `ShoppingBasket` inside the hero
    tile that plan 01 left there with `<StoreLogoImage>`, passing
    `alt={t('login.logoAlt', { name: storeName })}`, `className="max-h-full max-w-full object-contain"`,
    and `fallback={<ShoppingBasket className="size-8" aria-hidden="true" />}` — the hero-size
    fallback icon, not the 44px one, because in this branch the tile is already at hero
    dimensions (33-UI-SPEC.md §1's partial state). Keep the branch condition on `storeName`
    being non-empty: a logo with no name still renders the hero tile beside `t('login.brand')`,
    and neither half missing may crash. Leave the unconfigured branch, the `lg:hidden` mobile
    header, the date/clock/tagline block and the two-child `justify-between` aside structure
    exactly as plan 01 left them.

    Add `login.logoAlt` to both `src/shared/lib/i18n/locales/es-MX/pages.json` and
    `.../en-US/pages.json` with the values `Logotipo de {{name}}` and `Logo of {{name}}`. Do not
    touch `login.brand`, `login.terminal`, `login.tagline` or `common.logoAlt` — `common.logoAlt`
    stays bound to the receipt-only component.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit, or any "error TS" line in the output</fails_when>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit, or any "error" line in the ESLint output — a `boundaries` hit here means the new component was placed where `pages/` cannot import it</fails_when>
    <automated>git diff --name-only -- src/widgets/LogoImage/index.tsx | wc -l</automated>
    <fails_when>prints anything other than 0 — the receipt logo component must be untouched</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/entities/settings/ui/StoreLogoImage.tsx` exists and `grep -c "login-store-logo" src/entities/settings/ui/StoreLogoImage.tsx` is 1.
    - `grep -c "object-contain" src/entities/settings/ui/StoreLogoImage.tsx` is at least 1 and `grep -c "object-cover" src/entities/settings/ui/StoreLogoImage.tsx` is 0.
    - `grep -c "useReceiptSettings\|logoDataUrl" src/entities/settings/ui/StoreLogoImage.tsx` is 0.
    - `grep -c "StoreLogoImage" src/entities/settings/index.ts` is at least 1.
    - `grep -c "StoreLogoImage" src/pages/login/index.tsx` is at least 2 (import plus usage).
    - `grep -c "LogoImage" src/pages/login/index.tsx` counts the mobile-header usage still present — the mobile block still imports and renders the receipt `LogoImage`.
    - `grep -c "size-10" src/pages/login/index.tsx` is still 1 and `grep -c "lg:hidden" src/pages/login/index.tsx` is still 1.
    - `grep -c "logoAlt" src/shared/lib/i18n/locales/es-MX/pages.json src/shared/lib/i18n/locales/en-US/pages.json` totals 2.
    - `git diff --name-only` for this task does not list `src/widgets/LogoImage/index.tsx`.
  </acceptance_criteria>
  <done>The configured login screen renders the uploaded store logo at hero size with a contained fit, falls back to a hero-size icon when only the name is set, and shows `ImageOff` when a stored object cannot load — with the receipt logo component byte-identical.</done>
</task>

<task type="tdd">
  <name>Task 3: STORE-03 coverage — functional upload flow, RLS boundary, visual baselines</name>
  <files>e2e/settings/store-branding.spec.ts, e2e/settings/store-branding-rls.spec.ts, e2e/visual/login-branding-baseline.spec.ts</files>
  <read_first>
    - `e2e/settings/store-branding.spec.ts` (as written in plan 01 — this task extends it)
    - `e2e/products/product-photo-upload.spec.ts` (~lines 90-190 — the in-page canvas helper that mints a `File` and the `setInputFiles` / `DataTransfer` attach patterns; no on-disk fixture is used or needed)
    - `e2e/products/product-photo-rls.spec.ts` (whole file, 247 lines — the header rationale, `createAnonClient`, `fakePhotoBytes`, the `test.describe.serial` + `beforeAll` shared-client structure, and the service-client ground-truth read paired with every denial)
    - `e2e/visual/46-product-dialog-baseline.spec.ts` (~lines 1-70 — the header conventions, the untracked-baseline note, the imports and the `waitForPageReady` stabilisation helper)
    - `playwright.visual.config.ts` (note `fullyParallel: true` — unlike the functional config, which pins `workers: 1`)
    - `e2e/helpers/rls-clients.ts` (`createRoleScopedClient`) and `e2e/helpers/supabase.ts` (`getServiceClient`)
  </read_first>
  <behavior>
    Write each case red first, then make it green.

    **`e2e/settings/store-branding.spec.ts` (extend):** log in as admin, open Settings ->
    General, attach a generated PNG to `settings-store-logo-file-input`, assert
    `settings-store-logo-preview` becomes visible, assert via a service-client read that
    `general.storeLogoPath` is now a non-empty string matching `store/<uuid>.<ext>` while
    `storeName`, `address`, `timezone`, `currency` and `receiptFooterText` still hold their
    seeded values. Then sign out, visit `/login` unauthenticated, and assert
    `login-store-logo` is visible with a non-empty `src`. Then: attach an `image/heic` file
    and assert the inline `role="alert"` carries the unsupported-format copy and
    `general.storeLogoPath` is unchanged. Then: Remove behind the `alertdialog` confirm, and
    assert `general.storeLogoPath` is null and `/login` is back to the hero-size icon fallback.
    Then: edit the store name, Save, and assert `general.storeLogoPath` survived the text save.
    `afterAll` restores the settings row and deletes any object the spec uploaded.

    **`e2e/settings/store-branding-rls.spec.ts` (new):** a cashier-scoped client's
    `storage.from('store-branding').upload(...)` is denied; an anonymous client's upload is
    denied; an anonymous client's `update` and `remove` against an existing object are denied;
    each denial is paired with a service-client ground-truth read proving the object was not
    written or not changed. Add the settings-table half of the boundary too, since plan 01
    widened it: an anonymous `select` on `settings` filtered to `key='general'` returns exactly
    one row, and the same client filtered to `key='billing'` (and `key='near_expiry'`) returns
    zero rows.

    **`e2e/visual/login-branding-baseline.spec.ts` (new):** two named baselines of the
    `hidden lg:flex` aside at 1280x800 — configured (an ASCII fixture store name plus an
    uploaded logo) and unconfigured (both cleared). Pair the configured screenshot with a
    deterministic measurement, per the convention in `46-product-dialog-baseline.spec.ts`:
    assert the hero tile's `boundingBox()` width and height are at least 120px, so "hero-size,
    dominant" (D-04) is a testable claim and a failure says which of the two broke.
  </behavior>
  <action>
    Extend one spec and create two.

    For the functional extension, generate the image in-page exactly as
    `product-photo-upload.spec.ts` does — a canvas drawn to a blob, wrapped in a `File`, handed
    to the input — rather than committing a binary fixture. Reuse `loginAs(page, 'admin')` from
    `e2e/helpers/auth.ts` for the Settings half; the `/login` assertions must run with no
    session, so sign out or use a fresh context before them. Keep the whole file in one
    `test.describe.serial` block: every case writes the same singleton settings row.

    For the RLS spec, copy `product-photo-rls.spec.ts`'s structure wholesale — its header
    paragraph explaining why it talks to Supabase directly rather than through the UI (a UI that
    merely hides a button proves nothing), its `createAnonClient` with an isolated
    `storageKey`, its tiny deterministic byte buffer (RLS denial fires before any content
    validation would), and its `beforeAll` provisioning of role-scoped clients once because
    `createRoleScopedClient` costs seconds per call. Substitute the bucket name and the
    permission under test. Assert on the error, not merely on the absence of success: a denial
    must surface an error object, never a silent no-op.

    For the visual spec, mirror `46-product-dialog-baseline.spec.ts`'s header including its note
    that `.gitignore` excludes `e2e/visual/**/*-snapshots/`, so baselines are local
    regenerated-on-first-run artifacts and are not committed alongside the spec. This page is
    pre-auth, so it needs neither `loginAs` nor `openCaja` — only the service client in
    `beforeAll` to write and clear `settings.general`, then a plain `page.goto('/login')` per
    case. Mask the date/live-clock block before every screenshot: `LiveTimeDisplay` ticks every
    second and the date label rolls over daily, so an unmasked baseline is guaranteed to flake.
    Declare `test.describe.serial` — `playwright.visual.config.ts` sets `fullyParallel: true`,
    so without it the configured and unconfigured cases would race each other writing the same
    settings row. `afterAll` restores the row.
  </action>
  <verify>
    <automated>npx playwright test e2e/settings/store-branding.spec.ts e2e/settings/store-branding-rls.spec.ts</automated>
    <fails_when>non-zero exit, or a "0 passed" / any non-zero "failed" count in the summary line</fails_when>
    <automated>npm run test:e2e:visual</automated>
    <fails_when>non-zero exit, or any non-zero "failed" count; a first run that only writes new baselines and reports them as written is not a pass — re-run and require a green diff</fails_when>
    <automated>grep -c "describe.serial" e2e/visual/login-branding-baseline.spec.ts</automated>
    <fails_when>prints 0 — the visual config runs fully parallel and both cases mutate the same settings row</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/settings/store-branding.spec.ts e2e/settings/store-branding-rls.spec.ts` reports 0 failed and at least 10 passed in total.
    - Running that same command a second time with no manual cleanup reports the same pass count — the specs restore their own state.
    - `npm run test:e2e:visual` is green on a re-run after baselines exist.
    - `grep -c "store-branding" e2e/settings/store-branding-rls.spec.ts` is at least 1 and `grep -c "createRoleScopedClient" e2e/settings/store-branding-rls.spec.ts` is at least 1.
    - `grep -c "key.*billing\|'billing'" e2e/settings/store-branding-rls.spec.ts` is at least 1 — the anon settings grant is proven to be scoped, not blanket.
    - `grep -c "mask" e2e/visual/login-branding-baseline.spec.ts` is at least 1 — the ticking clock is masked.
    - `grep -c "boundingBox" e2e/visual/login-branding-baseline.spec.ts` is at least 1 — the hero-size claim is measured, not only screenshotted.
    - `grep -rc "test.skip\|human_needed\|manually verify" e2e/settings/store-branding.spec.ts e2e/settings/store-branding-rls.spec.ts e2e/visual/login-branding-baseline.spec.ts` totals 0.
  </acceptance_criteria>
  <done>All three STORE-03 surfaces are automated and green headless: the configured/unconfigured functional flow including upload, the database-level RLS boundary on both the bucket and the widened settings read, and the paired visual baseline plus hero-size measurement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated browser → rendered store logo | An admin-supplied image is rendered on a pre-auth screen. |
| unauthenticated browser → `settings` (`key='general'`) | Store name and address are readable without a session (introduced in plan 01, asserted here). |
| admin browser → Storage write via the new control | The UI gate is UX only; RLS is the authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-08 | Spoofing | Admin-uploaded image rendered on the pre-auth login screen | low | accept | The image is controllable only by a holder of `manage_settings`, who can already change every other branding surface. It is rendered through `<img>` only — never `dangerouslySetInnerHTML` — and `image/svg+xml` is absent from both the client allow-list and the bucket's `allowed_mime_types`, so no script-bearing format can reach the page. |
| T-33-09 | Information Disclosure | Store name and address readable by an anonymous caller | low | accept | Only `key='general'` is exposed, and its contents are printed on every customer receipt and posted on the store door. `store-branding-rls.spec.ts` asserts the grant does not extend to `billing` or `near_expiry`. |
| T-33-05 | Elevation of Privilege | Direct Storage write bypassing the UI gate | high | mitigate | Verified here rather than assumed: `store-branding-rls.spec.ts` proves cashier and anonymous `upload`/`update`/`remove` are all denied by Postgres, each paired with a service-client ground-truth read. |
| T-33-11 | Denial of Service | A broken or deleted logo object blocking the login screen | medium | mitigate | `StoreLogoImage`'s `onError` degrades to a centred `ImageOff` inside the same hero frame; the store name and the PIN form render regardless, so a bad object never prevents sign-in. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed by this phase (33-RESEARCH.md § Standard Stack), so the package-legitimacy gate has no applicable rows. |
</threat_model>

<verification>
- `npm run typecheck` and `npm run lint` green.
- `npx playwright test e2e/settings/store-branding.spec.ts e2e/settings/store-branding-rls.spec.ts` green, and green again on an immediate re-run.
- `npm run test:e2e:visual` green on a re-run after baselines exist.
- `src/widgets/LogoImage/index.tsx` and the `receipt_settings` schema are unchanged across the whole phase.
</verification>

<success_criteria>
An admin uploads a logo in Settings -> General, signs out, and the login screen shows that
logo hero-sized beside the store's name — with the unconfigured fallback, the RLS boundary
and the visual baselines all proven by headless Playwright, and no human asked to click
anything.
</success_criteria>

<output>
Create `.planning/phases/33-login-screen-store-branding/33-03-SUMMARY.md` when done.
</output>
