---
phase: 17-e2e-suite-overhaul
plan: 04
type: execute
wave: 2
depends_on: ["17-01", "17-02", "17-03"]
files_modified:
  - e2e/checkout/happy-path.spec.ts
  - e2e/checkout/atomic-rpc-guards.spec.ts
  - e2e/checkout/barcode-scan-search.spec.ts
  - e2e/50-direct-sale-checkout.spec.ts
  - e2e/51-barcode-scan-search.spec.ts
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04]

estimate:
  tokens: 22500
  raw_tokens: 45000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "A cashier scanning/searching an Indian grocery product, paying cash/card/split, produces a completed sale with UI confirmation AND a matching stock_movements/payments DB row (D-10)"
    - "process_direct_sale_atomic's existing adversarial coverage (tampered price, zero/partial payment, wrong caja/shift state, forged modifiers, cross-cashier replay) survives the rewrite intact (D-11)"
    - "The new e2e/checkout/ folder runs green against the Wave 1 foundation: agent-browser Chrome-for-Testing binary, Indian seed catalog, db-assertions.ts helpers, folder-path VERIFICATION_REPORT.md classification"
  artifacts:
    - e2e/checkout/happy-path.spec.ts
    - e2e/checkout/atomic-rpc-guards.spec.ts
    - e2e/checkout/barcode-scan-search.spec.ts
  key_links:
    - "e2e/checkout/*.spec.ts -> e2e/helpers/db-assertions.ts assertStockMovement/assertPaymentRecorded -> stock_movements/payments tables"
    - "e2e/checkout/*.spec.ts -> scripts/seed-dev-data.ts's Indian catalog (Plan 17-02) -> products table"
---

<objective>
**TRACER.** Rewrite `e2e/checkout/` — the app's single most central user flow — fully end-to-end first, before any other domain folder in this phase. This is the one slice that exercises every piece of Wave 1's foundation at once: the new folder/naming convention (D-06/D-07), the agent-browser Chrome-for-Testing launch target (D-16), the Indian grocery seed catalog (D-01/D-02), the shared `db-assertions.ts` helpers (D-12), and `global-teardown.ts`'s folder-path classification. If anything in Wave 1 is wrong, this plan is where it surfaces first — on one folder, not on forty-nine.

`e2e/50-direct-sale-checkout.spec.ts` (901 lines) and `e2e/51-barcode-scan-search.spec.ts` (503 lines) are both already confirmed bar-pos-clean (not on the TEST-01 remnant list) and already comprehensive — including extensive existing adversarial coverage of `process_direct_sale_atomic` (tampered price, zero/partial payment, wrong caja/shift state, forged modifiers, cross-cashier replay, idempotency-key reuse) that already satisfies D-11 for this RPC. The work here is: move + split into descriptively-named files, swap every hardcoded `'Budweiser'` reference (23+ occurrences) for a real Indian catalog product, and adopt the new `db-assertions.ts` helpers where they're a clean fit alongside the file's own existing inline checks (do not rewrite adversarial assertions that already work).

Purpose: prove the whole Wave 1 foundation on the app's core flow before Wave 3 fans out across the other 17 domain folders.
Output: `e2e/checkout/happy-path.spec.ts`, `e2e/checkout/atomic-rpc-guards.spec.ts`, `e2e/checkout/barcode-scan-search.spec.ts`; the two original root files deleted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/17-e2e-suite-overhaul/17-CONTEXT.md
@.planning/phases/17-e2e-suite-overhaul/17-RESEARCH.md
@.planning/phases/17-e2e-suite-overhaul/17-PATTERNS.md
@.planning/phases/17-e2e-suite-overhaul/17-01-SUMMARY.md
@.planning/phases/17-e2e-suite-overhaul/17-02-SUMMARY.md
@.planning/phases/17-e2e-suite-overhaul/17-03-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: e2e/checkout/happy-path.spec.ts — cash/card/split checkout, DB-verified, Indian fixtures</name>
  <files>e2e/checkout/happy-path.spec.ts, e2e/50-direct-sale-checkout.spec.ts</files>
  <read_first>
    e2e/50-direct-sale-checkout.spec.ts — full file; the 7 tests this task moves are: `cash payment creates one paid sale and decrements stock`, `cancelling before payment preserves the cart`, `offline before checkout submit shows blocking dialog instead of hanging`, `Try Again after reconnecting completes the sale`, `Cancel on offline dialog returns to cart without submitting`, `card payment creates one paid sale and decrements stock`, `split cash and card payment creates two grouped payments and decrements stock once` (lines ~117-406 in the current file)
    e2e/helpers/db-assertions.ts (Plan 17-01's output — `assertStockMovement`, `assertPaymentRecorded`)
    scripts/seed-dev-data.ts (Plan 17-02's output — pick ONE simple, single-barcode packaged-goods product from the new Indian catalog as this file's primary test product, e.g. a snack or tea/coffee item with a round, easy-to-assert `base_price`)
    e2e/helpers/supabase.ts (Plan 17-03's output — confirm `seedOpenTab`'s `productName` is now required; this file likely seeds via direct UI scan/search, not `seedOpenTab`, but check)
    e2e/fixtures.ts, e2e/helpers/auth.ts, e2e/helpers/requireEnv.ts (unchanged base patterns — `test`/`expect` import, `loginAs`, `requireIntegrationEnv`)
  </read_first>
  <action>
    Create `e2e/checkout/happy-path.spec.ts` containing the 7 tests listed in `<read_first>`, moved verbatim in structure but with every `'Budweiser'` product-name literal (and its associated `base_price` `6.50` and any dependent computed totals/tax assertions) replaced by the chosen Indian catalog product and its real `base_price` from `scripts/seed-dev-data.ts` — recompute every hardcoded expected-total/tax assertion in the moved tests against the new price, do not just swap the name and leave stale price math. Update the relative imports (`./fixtures` → `../fixtures`, `./helpers/auth` → `../helpers/auth`, etc. — one extra `../` level for the new subfolder). Add ONE new `assertStockMovement(productId, -1, 'sale')` and `assertPaymentRecorded(tabId, expectedAmount, 'cash')`-style call (using `e2e/helpers/db-assertions.ts`) to the `cash payment creates one paid sale and decrements stock` test, alongside its existing UI/inventory assertions, per D-10 — this is the concrete "UI assertion AND DB assertion" proof point for the whole phase's D-10 requirement.
    Delete these 7 tests from `e2e/50-direct-sale-checkout.spec.ts` (the remainder moves in Task 2).
  </action>
  <verify>
    <automated>npx playwright test e2e/checkout/happy-path.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Budweiser" e2e/checkout/happy-path.spec.ts` returns 0
    - `grep -c "assertStockMovement\|assertPaymentRecorded" e2e/checkout/happy-path.spec.ts` returns 1 or more
    - `npx playwright test e2e/checkout/happy-path.spec.ts` passes with 0 failures
    - The Playwright run's browser process resolves to agent-browser's Chrome-for-Testing binary, not system Chrome (confirm via `npx playwright test e2e/checkout/happy-path.spec.ts --reporter=list 2>&1 | grep -i chrome` or by checking `page.context().browser()?.version()` against the pinned CfT version if easily assertable)
  </acceptance_criteria>
  <done>e2e/checkout/happy-path.spec.ts exists, passes end-to-end against the Indian catalog with a real DB assertion, and proves the Wave 1 foundation (config, seed data, db-assertions.ts) works together on the app's most-used flow.</done>
</task>

<task type="auto">
  <name>Task 2: e2e/checkout/atomic-rpc-guards.spec.ts + e2e/checkout/barcode-scan-search.spec.ts — expand from the proven slice</name>
  <files>e2e/checkout/atomic-rpc-guards.spec.ts, e2e/checkout/barcode-scan-search.spec.ts, e2e/50-direct-sale-checkout.spec.ts, e2e/51-barcode-scan-search.spec.ts</files>
  <read_first>
    e2e/50-direct-sale-checkout.spec.ts — the remaining tests after Task 1's 7 are moved out: `reuses an idempotency key...`, `snapshots inventory cost_price server-side...`, `reuses a split idempotency key...`, `rejects a tampered price...`, `rejects a zero-amount cash payment...`, `rejects a partial cash payment...`, `rejects a sale against a caja session that is not open`, `rejects a sale against a shift that is not open`, `rejects a card override below the derived total...`, `rejects a non-null direct-sale discount payload...`, `rejects a forged zero modifier delta...`, `rejects a modifier not linked to the item product...`, `rejects a split expected total below the derived total...`, `replay from a different cashier is rejected...`, `original-cashier replay through the edge function...`, `cross-cashier replay through the edge function...`, `split payment returns one truthful sale-level receipt...` (lines ~406-901 in the pre-Task-1 file) — this is D-11's existing `process_direct_sale_atomic` forced-failure coverage; keep every test's existing "writes no rows" inline assertion logic intact, just rehome the file and its product-name literals
    e2e/51-barcode-scan-search.spec.ex — full file (503 lines), confirmed bar-pos-clean, needs the same product-name/folder treatment as Task 1
  </read_first>
  <action>
    Create `e2e/checkout/atomic-rpc-guards.spec.ts` from `e2e/50-direct-sale-checkout.spec.ts`'s remaining tests (listed in `<read_first>`), same product-name-swap and import-path treatment as Task 1. Delete `e2e/50-direct-sale-checkout.spec.ts` entirely once both new files contain its full content (verify no test was dropped: original file's `test(` count must equal the sum of `happy-path.spec.ts`'s and `atomic-rpc-guards.spec.ts`'s `test(` counts).
    Create `e2e/checkout/barcode-scan-search.spec.ts` from `e2e/51-barcode-scan-search.spec.ts` in full, with the same product-name swap (use the Task 1 primary product plus at least one additional Indian catalog product for multi-product scan/search coverage) and import-path fix. Delete `e2e/51-barcode-scan-search.spec.ts`.
  </action>
  <verify>
    <automated>npx playwright test e2e/checkout/ --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `test -f e2e/50-direct-sale-checkout.spec.ts` fails (file deleted)
    - `test -f e2e/51-barcode-scan-search.spec.ts` fails (file deleted)
    - `grep -c "Budweiser\|Corona" e2e/checkout/*.spec.ts` returns 0
    - `grep -c "rejects a" e2e/checkout/atomic-rpc-guards.spec.ts` returns 10 or more (D-11 adversarial coverage preserved)
    - `npx playwright test e2e/checkout/` passes with 0 failures
  </acceptance_criteria>
  <done>e2e/checkout/ contains 3 files (happy-path, atomic-rpc-guards, barcode-scan-search), all green, all using Indian catalog fixtures, the two original root files are gone, and `process_direct_sale_atomic`'s existing forced-failure coverage (D-11) is fully preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → `process_direct_sale_atomic` RPC | The primary state-changing boundary this folder exercises — already extensively adversarially tested (tampered price, forged modifiers, cross-cashier replay); this plan preserves that coverage, does not weaken it |
| Test harness → Postgres (service-role, D-10 assertion) | `assertStockMovement`/`assertPaymentRecorded` read ground truth after the UI action completes — read-only in this plan's usage, no new write-path risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-17-06 | Tampering | `process_direct_sale_atomic` adversarial test coverage | high | mitigate | Task 2's acceptance criteria requires a `test(` count reconciliation (original file count == sum of both new files) and a `grep -c "rejects a"` floor, specifically to catch an accidental silent drop of an adversarial test during the file split — the exact failure mode this threat register entry exists to prevent. |
| T-17-07 | Repudiation | Idempotency-key reuse tests (`reuses an idempotency key without creating a second payment...`) | medium | mitigate | Preserved verbatim in the move (Task 2) — these tests are the existing proof that a retried/duplicated client request cannot double-charge or double-decrement stock. |

</threat_model>

<verification>
- `npx playwright test e2e/checkout/` passes with 0 failures.
- `test( ` count reconciliation confirms zero tests silently dropped during the split.
- `VERIFICATION_REPORT.md` (generated by `global-teardown.ts`, Plan 17-03) shows a `Checkout` row with a nonzero total, not folded into `Other`.
</verification>

<success_criteria>
`e2e/checkout/` is fully rewritten, green, uses the Indian grocery catalog, includes at least one D-10 DB assertion and preserves all existing D-11 `process_direct_sale_atomic` adversarial coverage — proving Wave 1's foundation works before Wave 3 fans out to the remaining 17 domain folders.
</success_criteria>

<artifacts_produced>
- `e2e/checkout/happy-path.spec.ts` (new)
- `e2e/checkout/atomic-rpc-guards.spec.ts` (new)
- `e2e/checkout/barcode-scan-search.spec.ts` (new)
- `e2e/50-direct-sale-checkout.spec.ts`, `e2e/51-barcode-scan-search.spec.ts` (deleted)
</artifacts_produced>

<output>
Create `.planning/phases/17-e2e-suite-overhaul/17-04-SUMMARY.md` when done
</output>
