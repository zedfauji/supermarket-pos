---
phase: 17-e2e-suite-overhaul
plan: 10
type: execute
wave: 3
depends_on: ["17-04"]
files_modified:
  - e2e/receipts/settings.spec.ts
  - e2e/receipts/category-grouping.spec.ts
  - e2e/receipts/print-retry-resilience.spec.ts
  - e2e/receipts/reprint.spec.ts
  - e2e/receipts/pdf-delivery.spec.ts
  - e2e/08-settings-receipt.spec.ts
  - e2e/49-receipt-category-grouping.spec.ts
  - e2e/59-receipt-print-retry-resilience.spec.ts
  - e2e/60-reprint-receipt.spec.ts
  - e2e/61-receipt-pdf-delivery.spec.ts
autonomous: true
requirements: [TEST-01, TEST-02]

estimate:
  tokens: 17500
  raw_tokens: 35000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "SC-4's dead KDS-bar cross-check is deleted outright, not left as test.skip (D-08 — the KDS bar board was deleted wholesale, no substitute surface exists)"
    - "SC-2b's live receipt category/modifier-grouping coverage survives the move, updated to reference the new catalog's illustrative modifier"
  artifacts:
    - e2e/receipts/settings.spec.ts
    - e2e/receipts/category-grouping.spec.ts
    - e2e/receipts/print-retry-resilience.spec.ts
    - e2e/receipts/reprint.spec.ts
    - e2e/receipts/pdf-delivery.spec.ts
---

<objective>
Rewrite `e2e/receipts/` (5 source files). `59`/`60`/`61` (print-retry-resilience, reprint, PDF-delivery — all shipped by v1.2 Phase 13) are confirmed bar-pos-clean and need only a folder move + fixture swap. `08-settings-receipt.spec.ts` is similarly clean. `49-receipt-category-grouping.spec.ts` is the one hybrid split (Pitfall 3): its `SC-2b` test is live, current receipt-category-grouping coverage that must survive; its `SC-4` test is a permanently-`test.skip`'d cross-check against the KDS bar board, which was deleted wholesale in Phase 1 with no substitute surface — delete it outright per D-08, not leave it quarantined.

Purpose: this is the lowest-drama domain to close out among the mid-wave plans — most of it is a clean move; the one real decision (SC-4) is already fully evidenced by the file's own header comment.
Output: `e2e/receipts/settings.spec.ts`, `category-grouping.spec.ts`, `print-retry-resilience.spec.ts`, `reprint.spec.ts`, `pdf-delivery.spec.ts`; 5 original root files deleted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-e2e-suite-overhaul/17-CONTEXT.md
@.planning/phases/17-e2e-suite-overhaul/17-RESEARCH.md
@.planning/phases/17-e2e-suite-overhaul/17-PATTERNS.md
@.planning/phases/17-e2e-suite-overhaul/17-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e/receipts/settings.spec.ts + category-grouping.spec.ts (Pitfall 3 split)</name>
  <files>e2e/receipts/settings.spec.ts, e2e/receipts/category-grouping.spec.ts, e2e/08-settings-receipt.spec.ts, e2e/49-receipt-category-grouping.spec.ts</files>
  <read_first>
    e2e/08-settings-receipt.spec.ts (full file, 151 lines — no bar-pos remnants, standard move)
    e2e/49-receipt-category-grouping.spec.ts (full file, 308 lines — confirmed this session: `SC-2b` is live current coverage using `pickTwoCategoryProducts`/`seedTwoCategoryTabWithModifier` — generic, not coupled to specific product names, currently references `MODIFIER_EXTRA_LIME_NAME`; `SC-4` is unconditionally `test.skip`'d with an explicit in-file admission the KDS bar board it compares against "was deleted wholesale" with "no remaining UI surface" — delete outright per D-08, this is the exact anti-pattern named in 17-RESEARCH.md's Anti-Patterns section)
    scripts/seed-dev-data.ts (Plan 17-02's output — confirms the actual illustrative modifier name seeded, e.g. `Extra Spicy` on a snack/pickle product — use that name in place of `MODIFIER_EXTRA_LIME_NAME`)
  </read_first>
  <action>
    Create `e2e/receipts/settings.spec.ts` from `e2e/08-settings-receipt.spec.ts` (full move, fix imports, swap fixture names).
    Create `e2e/receipts/category-grouping.spec.ts` containing ONLY `SC-2b` from `e2e/49-receipt-category-grouping.spec.ts`, updated to reference the Indian catalog's actual illustrative modifier name (from Plan 17-02) instead of `MODIFIER_EXTRA_LIME_NAME`, with two products from different categories (matching `pickTwoCategoryProducts`'s existing category-agnostic selection logic — no change needed there beyond confirming it still resolves against the new catalog's categories). Delete `SC-4` entirely (do not port, do not leave `test.skip`).
    Delete `e2e/08-settings-receipt.spec.ts` and `e2e/49-receipt-category-grouping.spec.ts`.
  </action>
  <verify>
    <automated>npx playwright test e2e/receipts/settings.spec.ts e2e/receipts/category-grouping.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ic "kds" e2e/receipts/category-grouping.spec.ts` returns 0
    - `grep -c "test.skip(" e2e/receipts/category-grouping.spec.ts` returns 0
    - `grep -c "SC-2b" e2e/receipts/category-grouping.spec.ts` returns 1
    - `test -f e2e/08-settings-receipt.spec.ts` fails, `test -f e2e/49-receipt-category-grouping.spec.ts` fails
    - `npx playwright test e2e/receipts/settings.spec.ts e2e/receipts/category-grouping.spec.ts` passes with 0 failures
  </acceptance_criteria>
  <done>Both files created, SC-4's dead KDS cross-check gone, SC-2b passing against Indian fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: e2e/receipts/print-retry-resilience.spec.ts + reprint.spec.ts + pdf-delivery.spec.ts</name>
  <files>e2e/receipts/print-retry-resilience.spec.ts, e2e/receipts/reprint.spec.ts, e2e/receipts/pdf-delivery.spec.ts, e2e/59-receipt-print-retry-resilience.spec.ts, e2e/60-reprint-receipt.spec.ts, e2e/61-receipt-pdf-delivery.spec.ts</files>
  <read_first>
    e2e/59-receipt-print-retry-resilience.spec.ts (148 lines), e2e/60-reprint-receipt.spec.ts (152 lines), e2e/61-receipt-pdf-delivery.spec.ts (197 lines) — all 3 shipped by v1.2 Phase 13 (`.planning/milestones/v1.2-phases/13-*`), confirmed bar-pos-clean, no skips
  </read_first>
  <action>
    Move all 3 files into `e2e/receipts/`, fixing imports and swapping fixture product names for the Indian catalog.
    Delete the 3 original root files.
  </action>
  <verify>
    <automated>npx playwright test e2e/receipts/print-retry-resilience.spec.ts e2e/receipts/reprint.spec.ts e2e/receipts/pdf-delivery.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `test -f e2e/59-receipt-print-retry-resilience.spec.ts` fails, same for the other 2 originals
    - `npx playwright test e2e/receipts/print-retry-resilience.spec.ts e2e/receipts/reprint.spec.ts e2e/receipts/pdf-delivery.spec.ts` passes with 0 failures
  </acceptance_criteria>
  <done>All 3 files moved with Indian fixtures, green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Reports/receipts (mostly read-only display of already-recorded payment data) | No new state-changing surface introduced by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-17-16 | Repudiation | Receipt content accuracy (category/modifier grouping) | low | accept | Read-only rendering of already-recorded order data — no write-path risk; SC-2b's existing assertion coverage is preserved. |

</threat_model>

<verification>
- `npx playwright test e2e/receipts/` passes with 0 failures.
- `VERIFICATION_REPORT.md` shows a `Receipts` row.
</verification>

<success_criteria>
`e2e/receipts/` contains 5 fully-passing files, the dead KDS cross-check is gone, live category-grouping coverage preserved.
</success_criteria>

<artifacts_produced>
- `e2e/receipts/settings.spec.ts`, `category-grouping.spec.ts`, `print-retry-resilience.spec.ts`, `reprint.spec.ts`, `pdf-delivery.spec.ts` (new)
- `e2e/08-settings-receipt.spec.ts`, `49-receipt-category-grouping.spec.ts`, `59-receipt-print-retry-resilience.spec.ts`, `60-reprint-receipt.spec.ts`, `61-receipt-pdf-delivery.spec.ts` (deleted)
</artifacts_produced>

<output>
Create `.planning/phases/17-e2e-suite-overhaul/17-10-SUMMARY.md` when done
</output>
