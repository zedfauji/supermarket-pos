---
phase: 17-e2e-suite-overhaul
plan: 15
type: execute
wave: 3
depends_on: ["17-04"]
files_modified:
  - e2e/settings/i18n-locale-switch.spec.ts
  - e2e/settings/backup-restore.spec.ts
  - e2e/infra/ci.spec.ts
  - e2e/infra/infrastructure.spec.ts
  - e2e/infra/tauri-build.spec.ts
  - e2e/infra/updater.spec.ts
  - e2e/infra/offline.spec.ts
  - e2e/soak/full-day-soak.spec.ts
  - e2e/46-i18n-locale-switch.spec.ts
  - e2e/56-settings-backup-restore.spec.ts
  - e2e/01-ci.spec.ts
  - e2e/12-infrastructure.spec.ts
  - e2e/13-tauri-build.spec.ts
  - e2e/18-updater.spec.ts
  - e2e/14-manual-stubs.spec.ts
  - e2e/11-offline.spec.ts
  - e2e/55-full-day-soak.spec.ts
autonomous: true
requirements: [TEST-01, TEST-02]

estimate:
  tokens: 22500
  raw_tokens: 45000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "14-manual-stubs.spec.ts (4 manual-verification test.skip stubs) is deleted per CLAUDE.md's non-negotiable no-manual-verification policy — 3 stubs are redundant with existing automated coverage, the 4th's genuine hardware/native-runtime limitation is documented in CLAUDE.md's E2E section rather than left as a placeholder test file"
    - "11-offline.spec.ts's real offline-queue coverage (order queued/syncs on reconnect) is rewritten against the current /pos UI, not left permanently skipped; T4's pool-session-targeting test is deleted outright (the feature it tested no longer exists, independent of its own already-documented Playwright limitation)"
  artifacts:
    - e2e/infra/offline.spec.ts
---

<objective>
Rewrite `e2e/settings/`, `e2e/infra/`, and `e2e/soak/` (7 source files + 1 deletion). Most of this is clean folder-move work (`i18n-locale-switch`, `backup-restore`, `ci`, `infrastructure`, `tauri-build`, `updater`, `full-day-soak` — all confirmed bar-pos-clean and skip-free). Two real items: delete `14-manual-stubs.spec.ts` outright (a standing CLAUDE.md policy violation — 4 unconditionally-skipped "manual verification" stubs, exactly what this project's testing policy forbids), and rewrite `11-offline.spec.ts`'s real offline-queue coverage against the current UI (its `T4` targets the now-fully-dead pool-session domain and should be deleted, not merely left with its existing Playwright-limitation excuse).

Purpose: close the suite's last explicit "have a human verify this" violation, and restore the offline-queue's genuine cross-cutting reliability coverage.
Output: `e2e/settings/i18n-locale-switch.spec.ts`, `backup-restore.spec.ts`; `e2e/infra/ci.spec.ts`, `infrastructure.spec.ts`, `tauri-build.spec.ts`, `updater.spec.ts`, `offline.spec.ts`; `e2e/soak/full-day-soak.spec.ts`; 8 original root files removed (7 moved, `14-manual-stubs.spec.ts` deleted outright).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/17-e2e-suite-overhaul/17-CONTEXT.md
@.planning/phases/17-e2e-suite-overhaul/17-RESEARCH.md
@.planning/phases/17-e2e-suite-overhaul/17-PATTERNS.md
@.planning/phases/17-e2e-suite-overhaul/17-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e/settings/i18n-locale-switch.spec.ts + backup-restore.spec.ts</name>
  <files>e2e/settings/i18n-locale-switch.spec.ts, e2e/settings/backup-restore.spec.ts, e2e/46-i18n-locale-switch.spec.ts, e2e/56-settings-backup-restore.spec.ts</files>
  <read_first>
    e2e/46-i18n-locale-switch.spec.ts (full file, 280 lines — no bar-pos remnants, no skips, standard move)
    e2e/56-settings-backup-restore.spec.ts (full file, 32 lines — no bar-pos remnants, no skips, standard move)
  </read_first>
  <action>
    Move both files into `e2e/settings/`, fixing imports and swapping any hardcoded fixture names for the Indian catalog.
    Delete both original root files.
  </action>
  <verify>
    <automated>npx playwright test e2e/settings/ --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `test -f e2e/46-i18n-locale-switch.spec.ts` fails, `test -f e2e/56-settings-backup-restore.spec.ts` fails
    - `npx playwright test e2e/settings/` passes with 0 failures
  </acceptance_criteria>
  <done>Both files moved, green.</done>
</task>

<task type="auto">
  <name>Task 2: e2e/infra/ci.spec.ts + infrastructure.spec.ts + tauri-build.spec.ts + updater.spec.ts, delete 14-manual-stubs.spec.ts</name>
  <files>e2e/infra/ci.spec.ts, e2e/infra/infrastructure.spec.ts, e2e/infra/tauri-build.spec.ts, e2e/infra/updater.spec.ts, e2e/01-ci.spec.ts, e2e/12-infrastructure.spec.ts, e2e/13-tauri-build.spec.ts, e2e/18-updater.spec.ts, e2e/14-manual-stubs.spec.ts</files>
  <read_first>
    e2e/01-ci.spec.ts (29 lines), e2e/12-infrastructure.spec.ts (45 lines), e2e/13-tauri-build.spec.ts (30 lines), e2e/18-updater.spec.ts (72 lines) — all confirmed bar-pos-clean, skip-free, standard moves
    e2e/14-manual-stubs.spec.ts (full file, 31 lines — confirmed this session: 4 unconditional `test.skip(true, 'Manual: ...')` stubs, a direct violation of CLAUDE.md's "Testing & Verification Policy — NON-NEGOTIABLE" section; 3 of the 4 stubs' own skip messages name an already-automated equivalent — "Zustand store spot-check... covered in CI Checks" duplicates `01-ci.spec.ts`; "Tauri production build" duplicates `13-tauri-build.spec.ts`; the remaining stub is a native Tauri window + physical PIN keypad + Supabase devtools console check, the closest thing in this suite to a genuinely hardware/native-runtime-only check)
    CLAUDE.md's "Testing & Verification Policy" section — the exact carve-out language for a genuinely un-automatable check ("must say so explicitly with a reason, not leaving a silent test.skip")
  </read_first>
  <action>
    Move the 4 clean files into `e2e/infra/`, fixing imports and fixture names.
    Delete `e2e/14-manual-stubs.spec.ts` entirely — 3 of its 4 stubs are confirmed redundant with `e2e/infra/ci.spec.ts`/`tauri-build.spec.ts`, and the 4th (native Tauri window + physical PIN keypad + Supabase devtools console check) is a genuine CLAUDE.md carve-out case (hardware/native-shell-only, no Playwright-against-browser-mode equivalent) — its accepted-limitation reasoning belongs in CLAUDE.md's rewritten E2E section (Plan 17-17, D-09), not as a placeholder test file in this suite.
  </action>
  <verify>
    <automated>npx playwright test e2e/infra/ci.spec.ts e2e/infra/infrastructure.spec.ts e2e/infra/tauri-build.spec.ts e2e/infra/updater.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `test -f e2e/14-manual-stubs.spec.ts` fails (deleted, not moved)
    - `test -f e2e/01-ci.spec.ts` fails, same for the other 3 originals
    - `npx playwright test e2e/infra/ci.spec.ts e2e/infra/infrastructure.spec.ts e2e/infra/tauri-build.spec.ts e2e/infra/updater.spec.ts` passes with 0 failures
  </acceptance_criteria>
  <done>4 files moved; 14-manual-stubs.spec.ts's policy violation resolved by deletion, with its one genuine exception flagged for CLAUDE.md documentation in Plan 17-17.</done>
</task>

<task type="auto">
  <name>Task 3: e2e/infra/offline.spec.ts (rewritten) + e2e/soak/full-day-soak.spec.ts</name>
  <files>e2e/infra/offline.spec.ts, e2e/soak/full-day-soak.spec.ts, e2e/11-offline.spec.ts, e2e/55-full-day-soak.spec.ts</files>
  <read_first>
    e2e/11-offline.spec.ts (full file, 116 lines — confirmed this session: `Order queued while offline — no error toast` and `Order syncs on reconnect` are Bucket-B, permanently skipped against the deleted `/pos` New-Tab flow, targeting the still-live `tabsStore.offlineQueue`/`OfflineQueueProcessor`; `T4: offline pool session start` is a runtime-conditional self-skip citing a real Playwright limitation, but its SUBJECT (pool session start) no longer exists in the app at all — delete this test outright regardless of its already-documented excuse; `T5: three offline actions...` is also Bucket-B, same rewrite treatment as the first two)
    e2e/checkout/happy-path.spec.ts (Plan 17-04's output — current `/pos` scan/cart interaction pattern to drive while `page.context().setOffline(true)`)
    e2e/55-full-day-soak.spec.ts (full file, 540 lines — no bar-pos remnants, no skips, standard move)
    src/features/*offline* or wherever `tabsStore.offlineQueue`/`OfflineQueueProcessor`/`OfflineBanner` live (CLAUDE.md's "Offline Queue" section) — confirm the current offline-cached-data indicator text (`/offline.*cached data/i` may or may not still be the literal current copy)
  </read_first>
  <action>
    Delete `T4: offline pool session start — syncs after reconnect` entirely (its subject no longer exists).
    Rewrite `Order queued while offline — no error toast`, `Order syncs on reconnect`, and `T5: three offline actions...` to drive the CURRENT `/pos` scan/cart flow (per `e2e/checkout/happy-path.spec.ts`) while offline instead of the deleted New-Tab flow — go offline via `page.context().setOffline(true)`, add item(s) to cart via scan/search, attempt checkout, assert no error toast and the action enters `tabsStore.offlineQueue`, go back online, assert sync completes (poll `getOrderCount`/an equivalent current DB check via `e2e/helpers/db-assertions.ts` if a cleaner assertion exists than the old `getOrderCount` helper). Un-skip all 3 (`test.skip` → `test`).
    Move the result to `e2e/infra/offline.spec.ts`, fixing imports and fixture names for the Indian catalog.
    Move `e2e/55-full-day-soak.spec.ts` to `e2e/soak/full-day-soak.spec.ts`, fixing imports and fixture names.
    Delete both original root files.
  </action>
  <verify>
    <automated>npx playwright test e2e/infra/offline.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ic "pool session" e2e/infra/offline.spec.ts` returns 0
    - `grep -c "test.skip('" e2e/infra/offline.spec.ts` returns 0
    - `test -f e2e/11-offline.spec.ts` fails, `test -f e2e/55-full-day-soak.spec.ts` fails
    - `npx playwright test e2e/infra/offline.spec.ts` passes with 0 failures
  </acceptance_criteria>
  <done>Offline-queue coverage (add-to-cart-while-offline, sync-on-reconnect, multi-action) passes for real against the current UI; the dead pool-session test is gone; full-day-soak moved clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Offline mutation queue → sync-on-reconnect | Data-integrity boundary — a queued action must eventually land exactly once, not be lost or duplicated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-17-21 | Repudiation | Manual-verification stubs standing in for real coverage | high | mitigate | Task 2 deletes `14-manual-stubs.spec.ts` entirely — closing this project's own explicitly-named policy violation. |
| T-17-22 | Tampering | Offline queue sync-on-reconnect (previously permanently skipped) | high | mitigate | Task 3 restores real coverage of this data-integrity property against the current UI. |

</threat_model>

<verification>
- `npx playwright test e2e/settings/ e2e/infra/ e2e/soak/` passes with 0 failures.
- `test -f e2e/14-manual-stubs.spec.ts` fails.
</verification>

<success_criteria>
8 files resolved: 7 clean moves, the manual-verification policy violation deleted, offline-queue coverage restored against the current UI.
</success_criteria>

<artifacts_produced>
- `e2e/settings/i18n-locale-switch.spec.ts`, `backup-restore.spec.ts` (new)
- `e2e/infra/ci.spec.ts`, `infrastructure.spec.ts`, `tauri-build.spec.ts`, `updater.spec.ts`, `offline.spec.ts` (new)
- `e2e/soak/full-day-soak.spec.ts` (new)
- `e2e/46-i18n-locale-switch.spec.ts`, `56-settings-backup-restore.spec.ts`, `01-ci.spec.ts`, `12-infrastructure.spec.ts`, `13-tauri-build.spec.ts`, `18-updater.spec.ts`, `14-manual-stubs.spec.ts`, `11-offline.spec.ts`, `55-full-day-soak.spec.ts` (deleted)
</artifacts_produced>

<output>
Create `.planning/phases/17-e2e-suite-overhaul/17-15-SUMMARY.md` when done
</output>
