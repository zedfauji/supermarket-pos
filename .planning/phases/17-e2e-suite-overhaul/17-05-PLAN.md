---
phase: 17-e2e-suite-overhaul
plan: 05
type: execute
wave: 3
depends_on: ["17-04"]
files_modified:
  - e2e/payments/core-payments.spec.ts
  - e2e/payments/payment-pane.spec.ts
  - e2e/payments/edge-cases.spec.ts
  - e2e/payments/refund.spec.ts
  - e2e/payments/split-payment.spec.ts
  - e2e/05-payments.spec.ts
  - e2e/17-payment-pane.spec.ts
  - e2e/23-payment-edge-cases.spec.ts
  - e2e/35-refund.spec.ts
  - e2e/41-split-payment.spec.ts
autonomous: true
requirements: [TEST-01, TEST-02]

estimate:
  tokens: 21000
  raw_tokens: 42000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "e2e/payments/ contains zero pool-table/Rappi references (T4 in payment-pane, PE5 in edge-cases deleted per D-08)"
    - "process_refund's existing REFUND_EXCEEDS_ORIGINAL forced-failure coverage survives the move (D-11)"
    - "The 3 permanently test.skip'd split-payment tests (T1-T3) are un-skipped and passing — a live policy violation per CLAUDE.md's no-manual/no-skipped-verification mandate"
  artifacts:
    - e2e/payments/core-payments.spec.ts
    - e2e/payments/payment-pane.spec.ts
    - e2e/payments/edge-cases.spec.ts
    - e2e/payments/refund.spec.ts
    - e2e/payments/split-payment.spec.ts
  key_links:
    - "e2e/payments/refund.spec.ts -> process_refund RPC -> assertStockMovement('refund')/assertPaymentRecorded"
    - "e2e/payments/split-payment.spec.ts -> process-split-payment edge function -> process_split_payment_atomic RPC (both confirmed still deployed/live)"
---

<objective>
Rewrite `e2e/payments/` (5 source files): strip the 2 confirmed bar-pos-only tests (a pool-table badge test, a Rappi payment-method test), swap Indian catalog fixtures throughout, add D-10 DB assertions to the state-changing flows that lack them, and — the substantive fix in this plan — un-skip and get passing the 3 permanently-disabled split-payment tests in `41-split-payment.spec.ts`, which is a standing violation of this project's own CLAUDE.md "no skipped/manual verification" policy against a feature (`process-split-payment` edge function + `process_split_payment_atomic` RPC) confirmed still live and deployed.

Purpose: payments is the second-most state-changing domain after checkout; get its DB-assertion coverage and its one real automation gap (split-payment) closed here.
Output: `e2e/payments/core-payments.spec.ts`, `payment-pane.spec.ts`, `edge-cases.spec.ts`, `refund.spec.ts`, `split-payment.spec.ts`; the 5 original root files deleted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/17-e2e-suite-overhaul/17-CONTEXT.md
@.planning/phases/17-e2e-suite-overhaul/17-RESEARCH.md
@.planning/phases/17-e2e-suite-overhaul/17-PATTERNS.md
@.planning/phases/17-e2e-suite-overhaul/17-01-SUMMARY.md
@.planning/phases/17-e2e-suite-overhaul/17-02-SUMMARY.md
@.planning/phases/17-e2e-suite-overhaul/17-03-SUMMARY.md
@.planning/phases/17-e2e-suite-overhaul/17-04-SUMMARY.md
@src/shared/lib/edge-function-contracts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e/payments/core-payments.spec.ts + payment-pane.spec.ts</name>
  <files>e2e/payments/core-payments.spec.ts, e2e/payments/payment-pane.spec.ts, e2e/05-payments.spec.ts, e2e/17-payment-pane.spec.ts</files>
  <read_first>
    e2e/05-payments.spec.ts (full file, 273 lines — confirmed this session: ALL 8 tests are unconditionally `test.skip('title', fn)`'d, each with an in-file comment "D-07: /pos was deleted in Phase 1 (Plan 01-11)... coverage becomes valid again once Phase 2 lands a checkout entry point" — Phase 2 has since shipped direct-sale checkout, but these tests still drive the OLD tab-opening flow (`New Tab` → `Place Order` → `Close Tab / Pay` → `ProcessPayment` modal), not the current scan/cart/pay UI)
    e2e/checkout/happy-path.spec.ts and e2e/checkout/atomic-rpc-guards.spec.ts (Plan 17-04's output — already cover `cash payment creates one paid sale and decrements stock`, `card payment creates one paid sale and decrements stock`, and split payment against the CURRENT UI — likely supersedes most or all of 05-payments.spec.ts's 8 stale-UI tests)
    e2e/17-payment-pane.spec.ts (full file, 483 lines — confirmed this session: test `T4: pool table tab shows Pool badge in the card` (lines ~250-283) queries the dropped `pool_tables`/`pool_sessions` tables directly — delete this whole test, not just the query)
  </read_first>
  <action>
    For `e2e/05-payments.spec.ts`: compare each of its 8 skipped tests' intent against `e2e/checkout/happy-path.spec.ts`/`atomic-rpc-guards.spec.ts`'s already-passing coverage (Plan 17-04). For each test whose assertion is genuinely covered there (expect this to be most or all 8 — "Cash payment — change calculation/completes", "Card payment button label", "Card payment — no cash drawer in console", and T5-T8's exact-cash/underpayment/tip/discount cases), do NOT port it — this whole file is very likely a pre-direct-sale-checkout fossil fully superseded by the tracer. For any test whose specific assertion truly has no equivalent (e.g. a precise change-due dollar format, a discount-application check not present elsewhere), port ONLY that assertion into `e2e/payments/core-payments.spec.ts` (new file, current-UI-driven, not the old tab-opening flow) rather than un-skipping the old test against a UI flow that plausibly no longer exists in that shape. Delete `e2e/05-payments.spec.ts` in full once this comparison is done and documented in the plan SUMMARY.
    Create `e2e/payments/payment-pane.spec.ts` from `e2e/17-payment-pane.spec.ts`, deleting the entire `T4: pool table tab shows Pool badge in the card` test (per D-08 — genuinely bar-pos-only, the pool-table/pool-session tables it queries were dropped in Phase 1), keeping every other test, fixing imports, swapping fixture product names.
    Delete `e2e/17-payment-pane.spec.ts`.
  </action>
  <verify>
    <automated>npx playwright test e2e/payments/payment-pane.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -icE "pool_tables|pool_sessions|pool table" e2e/payments/payment-pane.spec.ts` returns 0
    - `test -f e2e/05-payments.spec.ts` fails, `test -f e2e/17-payment-pane.spec.ts` fails (both deleted)
    - The plan SUMMARY documents the 05-payments.spec.ts coverage comparison (which of the 8 tests were confirmed superseded vs. ported)
    - `npx playwright test e2e/payments/payment-pane.spec.ts` passes with 0 failures; if `e2e/payments/core-payments.spec.ts` was created (a genuine gap found), it passes too
  </acceptance_criteria>
  <done>05-payments.spec.ts's coverage is confirmed superseded (or the genuine remainder ported to a new current-UI-driven file) and deleted; payment-pane.spec.ts moved with its bar-pos test deleted; both green.</done>
</task>

<task type="auto">
  <name>Task 2: e2e/payments/edge-cases.spec.ts + refund.spec.ts — D-08 delete + D-10 DB assertions</name>
  <files>e2e/payments/edge-cases.spec.ts, e2e/payments/refund.spec.ts, e2e/23-payment-edge-cases.spec.ts, e2e/35-refund.spec.ts</files>
  <read_first>
    e2e/23-payment-edge-cases.spec.ts (full file, 267 lines — confirmed this session: `PE5: Rappi payment method — no open_cash_drawer in logs` (lines ~234-267) targets a `payment-btn-rappi` test id that does not exist in the current payment methods (cash/card/split only) — delete this whole test)
    e2e/35-refund.spec.ts (full file, 559 lines — confirmed this session: `T5: REFUND_EXCEEDS_ORIGINAL blocks double-refund of fully-refunded payment` is the existing D-11 forced-failure coverage for `process_refund` — preserve verbatim)
    e2e/helpers/db-assertions.ts (Plan 17-01's output — `assertStockMovement`, `assertPaymentRecorded`)
  </read_first>
  <action>
    Create `e2e/payments/edge-cases.spec.ts` from `e2e/23-payment-edge-cases.spec.ts`, deleting the entire `PE5: Rappi payment method` test (per D-08), fixing imports, swapping fixture product names for the Indian catalog.
    Create `e2e/payments/refund.spec.ts` from `e2e/35-refund.spec.ts` verbatim (no bar-pos remnants, no tests to delete), fixing imports and fixture product names. Add an `assertStockMovement(productId, expectedDelta, 'refund')` call (using `e2e/helpers/db-assertions.ts`) to `T1-T4: process refund on 2 items with manager PIN` alongside its existing assertions, and confirm `T6: refund remaining 3 items with restock=false — no stock ledger change` already asserts the ABSENCE of a stock movement — if it does not, add that DB-level negative assertion too (D-10 requires a direct DB assertion for this state-changing flow, both the positive-restock and no-restock paths).
    Delete `e2e/23-payment-edge-cases.spec.ts` and `e2e/35-refund.spec.ts`.
  </action>
  <verify>
    <automated>npx playwright test e2e/payments/edge-cases.spec.ts e2e/payments/refund.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ic "rappi" e2e/payments/edge-cases.spec.ts` returns 0
    - `grep -c "REFUND_EXCEEDS_ORIGINAL" e2e/payments/refund.spec.ts` returns 1 or more (D-11 preserved)
    - `grep -c "assertStockMovement" e2e/payments/refund.spec.ts` returns 1 or more
    - `npx playwright test e2e/payments/edge-cases.spec.ts e2e/payments/refund.spec.ts` passes with 0 failures
  </acceptance_criteria>
  <done>Both files moved, Rappi test deleted, refund's forced-failure coverage preserved, DB assertions added per D-10.</done>
</task>

<task type="auto">
  <name>Task 3: e2e/payments/split-payment.spec.ts — un-skip and debug to green (policy fix)</name>
  <files>e2e/payments/split-payment.spec.ts, e2e/41-split-payment.spec.ts</files>
  <read_first>
    e2e/41-split-payment.spec.ts (full file, 365 lines — confirmed this session: all 3 tests use Playwright's unconditional `test.skip('title', fn)` form, not a conditional runtime skip — this is a standing violation of CLAUDE.md's "no `<human-check>`... no case where 'have a human click it' is acceptable" policy, except here it's "never run at all" rather than manual, which the policy's spirit equally forbids)
    src/shared/lib/edge-function-contracts.ts (`process-split-payment` request/response contract, lines ~745-841 — confirms this edge function is still implemented and deployed)
    src/shared/lib/supabase.types.ts (`process_split_payment_atomic` RPC, confirms columns `payment_group_id`/`split_index` on `payments` still exist)
    e2e/checkout/happy-path.spec.ts's split-payment test (Plan 17-04's `split cash and card payment creates two grouped payments...`) — a DIFFERENT, already-passing split mechanism (direct-sale checkout's built-in split via `process_direct_sale_atomic`); this file's split flow is a SECOND, separate mechanism (splitting an ALREADY-OPEN tab's payment via the dedicated `process-split-payment` edge function) — do not conflate the two or assume Plan 17-04's coverage makes this file redundant
  </read_first>
  <action>
    Create `e2e/payments/split-payment.spec.ts` from `e2e/41-split-payment.spec.ts`, changing all 3 `test.skip('T1...', ...)` / `test.skip('T2...', ...)` / `test.skip('T3...', ...)` calls to plain `test('T1...', ...)` / etc. Run the suite; debug each test to a real passing state against the live `process-split-payment` edge function + `process_split_payment_atomic` RPC — the test bodies are already fully written (not stubs), so this is fixing real failures (selector drift, stale UI text, timing), not authoring from scratch. Swap fixture product names for the Indian catalog. Fix imports for the new folder depth.
    If, after genuine debugging effort, a specific test proves un-automatable for a reason CLAUDE.md's carve-out actually covers (hardware-only), it must be documented with an explicit written reason in the test file itself, not left as a silent `test.skip` — but the default expectation, given these are UI+RPC flows with no hardware dependency, is that all 3 become green.
    Delete `e2e/41-split-payment.spec.ts`.
  </action>
  <verify>
    <automated>npx playwright test e2e/payments/split-payment.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test.skip(" e2e/payments/split-payment.spec.ts` returns 0 (or, if a specific test is kept skipped, an explicit written justification comment sits directly above that one call, citing CLAUDE.md's carve-out by name)
    - `npx playwright test e2e/payments/split-payment.spec.ts` passes with 0 failures (or documents the one accepted exception per the criterion above)
    - `test -f e2e/41-split-payment.spec.ts` fails (deleted)
  </acceptance_criteria>
  <done>e2e/payments/split-payment.spec.ts's 3 tests run for real against the live split-payment edge function/RPC, closing a standing CLAUDE.md policy violation, or the one genuinely un-automatable case is explicitly documented rather than silently skipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → `process_refund` RPC | Manager-PIN-gated state-changing boundary; D-11 coverage (`REFUND_EXCEEDS_ORIGINAL`) preserved verbatim in this plan |
| Browser → `process-split-payment` edge function → `process_split_payment_atomic` RPC | Second state-changing payment path, previously untested in CI/local runs (permanently skipped) — this plan is the first time it gets real automated coverage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-17-08 | Repudiation | `process_refund`'s double-refund guard | high | mitigate | `REFUND_EXCEEDS_ORIGINAL` test preserved verbatim (Task 2), plus a new `assertStockMovement` DB check confirms no phantom restock on a rejected double-refund attempt. |
| T-17-09 | Tampering | `process_split_payment_atomic` — previously zero automated coverage | high | mitigate | Task 3 closes this gap entirely; until this plan, a regression in split-payment's atomicity would have shipped undetected since its only test suite was permanently disabled. |

</threat_model>

<verification>
- `npx playwright test e2e/payments/` passes with 0 failures.
- `grep -c "test.skip(" e2e/payments/split-payment.spec.ts` returns 0 (or a documented exception).
- `VERIFICATION_REPORT.md` shows a `Payments` row with a nonzero total.
</verification>

<success_criteria>
`e2e/payments/` is fully rewritten with zero bar-pos references, D-11 refund coverage intact, D-10 DB assertions added, and the previously-disabled split-payment suite genuinely running and passing.
</success_criteria>

<artifacts_produced>
- `e2e/payments/core-payments.spec.ts`, `payment-pane.spec.ts`, `edge-cases.spec.ts`, `refund.spec.ts`, `split-payment.spec.ts` (new)
- `e2e/05-payments.spec.ts`, `17-payment-pane.spec.ts`, `23-payment-edge-cases.spec.ts`, `35-refund.spec.ts`, `41-split-payment.spec.ts` (deleted)
</artifacts_produced>

<output>
Create `.planning/phases/17-e2e-suite-overhaul/17-05-SUMMARY.md` when done
</output>
