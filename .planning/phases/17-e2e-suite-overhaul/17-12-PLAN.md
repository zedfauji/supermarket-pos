---
phase: 17-e2e-suite-overhaul
plan: 12
type: execute
wave: 3
depends_on: ["17-04"]
files_modified:
  - e2e/products/product-management.spec.ts
  - e2e/products/categories.spec.ts
  - e2e/checkout/modifier-notes.spec.ts
  - e2e/21-product-management.spec.ts
  - e2e/31-categories.spec.ts
  - e2e/18-modifier-notes-kds.spec.ts
autonomous: true
requirements: [TEST-01, TEST-02]

estimate:
  tokens: 19000
  raw_tokens: 38000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "PM4/PM7's stale 'no other surface renders a live product catalog grid' premise is corrected — /pos genuinely does again (direct-sale checkout), both tests un-skipped and rewritten against it"
    - "Modifier sheet (T1/T2) coverage survives, rewritten against the current /pos UI — still a self-contained page.route() mock, no real DB fixture dependency"
    - "KDS-only content in 18-modifier-notes-kds.spec.ts is deleted; T4's already-documented Tauri-IPC limitation is preserved as-is (legitimate CLAUDE.md carve-out)"
  artifacts:
    - e2e/products/product-management.spec.ts
    - e2e/products/categories.spec.ts
    - e2e/checkout/modifier-notes.spec.ts
---

<objective>
Rewrite `e2e/products/` (2 source files) and split `18-modifier-notes-kds.spec.ts` (Pitfall 3 hybrid) into `e2e/checkout/modifier-notes.spec.ts`. `21-product-management.spec.ts`'s `PM4`/`PM7` were skipped on a premise ("no other surface renders a live product catalog grid") that is now stale — `/pos` genuinely renders exactly that grid again via direct-sale checkout — un-skip and rewrite both. `31-categories.spec.ts` is already clean (its own in-file comment documents a prior cleanup removing the one `combo_eligible`-on-categories test that never should have existed; the remaining `combo_eligible` matches are historical comment text, not live dead code) — plain move. `18-modifier-notes-kds.spec.ts`'s `T1`/`T2` use a self-contained `page.route()` product mock (no real seeded product dependency) and are similarly Bucket-B-stale on navigation only — rewrite against current `/pos`; `T4` keeps its already-legitimate, already-documented Tauri-IPC-unavailable skip.

Purpose: two of this plan's three files were skipped on premises that are now provably false (Phase 2 shipped `/pos` again) — this is exactly the "confirm before deleting/skipping" discipline the phase-wide audit exists to apply.
Output: `e2e/products/product-management.spec.ts`, `categories.spec.ts`; `e2e/checkout/modifier-notes.spec.ts`; 3 original root files deleted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-e2e-suite-overhaul/17-CONTEXT.md
@.planning/phases/17-e2e-suite-overhaul/17-RESEARCH.md
@.planning/phases/17-e2e-suite-overhaul/17-PATTERNS.md
@.planning/phases/17-e2e-suite-overhaul/17-02-SUMMARY.md
@.planning/phases/17-e2e-suite-overhaul/17-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e/products/product-management.spec.ts + categories.spec.ts</name>
  <files>e2e/products/product-management.spec.ts, e2e/products/categories.spec.ts, e2e/21-product-management.spec.ts, e2e/31-categories.spec.ts</files>
  <read_first>
    e2e/21-product-management.spec.ts (full file, 353 lines — confirmed this session: `PM4: TestProduct-E2E visible in /pos product grid` and `PM7: set is_active=false — product disappears from /pos grid` are both unconditionally `test.skip`'d with a stale premise ("no other surface renders a live, is_active-filtered product catalog with 'Select {name}' buttons") — `/pos`'s direct-sale checkout product grid is exactly that surface now)
    e2e/checkout/happy-path.spec.ts, e2e/checkout/barcode-scan-search.spec.ts (Plan 17-04's output — the current product-grid interaction pattern: how a product is selected/searched on the current `/pos`)
    e2e/31-categories.spec.ts (full file, 376 lines — confirmed this session: the only `combo_eligible` matches are inside a comment block documenting a PRIOR removal of a test that incorrectly assumed `categories.combo_eligible` existed (it never did — the column lives on `products`, per the migration cited in that comment) — this file has no live dead-schema references, plain move)
  </read_first>
  <action>
    In `e2e/21-product-management.spec.ts`: rewrite `PM4` and `PM7` to drive the CURRENT `/pos` product grid (per `e2e/checkout/happy-path.spec.ts`'s pattern — search/scan the seeded test product, confirm it's selectable; toggle `is_active=false` via the service-role client as before, confirm the product disappears from the grid/search results). Un-skip both (`test.skip` → `test`).
    Move the result to `e2e/products/product-management.spec.ts`, fixing imports and using the standard `TEST_PRODUCT` self-contained seed/cleanup pattern this file already uses (no Indian-catalog dependency needed — it seeds/deletes its own test product per test, per the existing `cleanupTestData` afterEach).
    Move `31-categories.spec.ts` to `e2e/products/categories.spec.ts` verbatim, fixing imports only (category names like "Beers"/"Regular"/"Corona" in this file are the TEST's own throwaway fixture categories, not the app's seeded catalog — leave them as-is unless they collide with the new Indian catalog's category names; if they do, rename to avoid collision).
    Delete both original root files.
  </action>
  <verify>
    <automated>npx playwright test e2e/products/product-management.spec.ts e2e/products/categories.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test.skip('" e2e/products/product-management.spec.ts` returns 0
    - `grep -c "route removed" e2e/products/product-management.spec.ts` returns 0 (stale premise corrected, not just relabeled)
    - `test -f e2e/21-product-management.spec.ts` fails, `test -f e2e/31-categories.spec.ts` fails
    - `npx playwright test e2e/products/product-management.spec.ts e2e/products/categories.spec.ts` passes with 0 failures
  </acceptance_criteria>
  <done>PM4/PM7 pass for real against the current /pos grid; categories.spec.ts moved clean.</done>
</task>

<task type="auto">
  <name>Task 2: e2e/checkout/modifier-notes.spec.ts — hybrid split (Pitfall 3)</name>
  <files>e2e/checkout/modifier-notes.spec.ts, e2e/18-modifier-notes-kds.spec.ts</files>
  <read_first>
    e2e/18-modifier-notes-kds.spec.ts (full file, 261 lines — confirmed this session: `T1`/`T2` inject a MOCK product via `page.route()` interception — `MOCK_PRODUCT_NAME = 'Alitas E2E'`, self-contained, no dependency on real seeded catalog rows — only their navigation (old `/pos` New-Tab flow) is stale; `T4` already has a legitimate, well-documented `test.skip` for Tauri-IPC-unavailable-in-browser-mode, matching CLAUDE.md's own carve-out — keep as-is verbatim, do not touch)
    e2e/checkout/happy-path.spec.ts (Plan 17-04's output — current `/pos` navigation pattern to replace the old New-Tab flow with)
  </read_first>
  <action>
    Rewrite `T1: modifier sheet opens when tapping a product with modifiers` and `T2: select modifier and add note — cart reflects both` to navigate the CURRENT `/pos` flow (scan/search → cart, per Plan 17-04) up to the point of tapping the mock-intercepted product, keeping the `page.route()` interception logic itself unchanged (it is already correct and self-contained). Un-skip both. Rename `MOCK_PRODUCT_NAME` from `'Alitas E2E'` to an Indian-catalog-flavored synthetic name (e.g. `'Test Snack E2E'`) for naming consistency — this is cosmetic only, the mock is synthetic and does not read from the seeded catalog.
    Create `e2e/checkout/modifier-notes.spec.ts` containing the rewritten `T1`/`T2` plus `T4` UNCHANGED (its skip reason remains valid — Tauri IPC genuinely isn't reachable in Playwright browser mode). Fix imports for the new folder path.
    Delete `e2e/18-modifier-notes-kds.spec.ts` in full (its content now fully accounted for: T1/T2/T4 moved, no other tests existed in this file per the earlier confirmed test count of 1 real + T1/T2/T4).
  </action>
  <verify>
    <automated>npx playwright test e2e/checkout/modifier-notes.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ic "kds" e2e/checkout/modifier-notes.spec.ts` returns 0
    - `grep -c "test.skip('" e2e/checkout/modifier-notes.spec.ts` returns 0 (T1/T2 un-skipped; T4's runtime-conditional `test.skip(true, ...)` inside an unconditional `test(...)` is a DIFFERENT, legitimate form and is not what this gate targets)
    - `grep -c "Tauri IPC" e2e/checkout/modifier-notes.spec.ts` returns 1 or more (T4's justification preserved)
    - `test -f e2e/18-modifier-notes-kds.spec.ts` fails
    - `npx playwright test e2e/checkout/modifier-notes.spec.ts` passes with 0 failures
  </acceptance_criteria>
  <done>T1/T2 pass for real against the current /pos UI; T4's legitimate documented exception is preserved unchanged; the original hybrid file is fully split and deleted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → `page.route()` mocked product response (T1/T2) | Test-only interception, no real network/DB boundary crossed — low-risk by construction |
| Browser → `products.is_active` toggle (PM7) | State-changing via service-role seed helper, UI-verified via grid absence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-17-18 | Tampering | `PM4`/`PM7`'s stale-premise correction | medium | mitigate | Acceptance criteria's `grep -c "route removed"` gate specifically catches a shallow "relabel but still skip" fix, forcing the premise correction to be real. |

</threat_model>

<verification>
- `npx playwright test e2e/products/ e2e/checkout/modifier-notes.spec.ts` passes with 0 failures.
- `VERIFICATION_REPORT.md` shows a `Products` row.
</verification>

<success_criteria>
Both stale-premise product-grid tests pass for real; modifier-sheet coverage survives with a current-UI rewrite; the legitimate Tauri-IPC exception is untouched; the hybrid KDS file is fully split and gone.
</success_criteria>

<artifacts_produced>
- `e2e/products/product-management.spec.ts`, `categories.spec.ts` (new)
- `e2e/checkout/modifier-notes.spec.ts` (new)
- `e2e/21-product-management.spec.ts`, `31-categories.spec.ts`, `18-modifier-notes-kds.spec.ts` (deleted)
</artifacts_produced>

<output>
Create `.planning/phases/17-e2e-suite-overhaul/17-12-SUMMARY.md` when done
</output>
