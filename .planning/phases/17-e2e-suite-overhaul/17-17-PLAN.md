---
phase: 17-e2e-suite-overhaul
plan: 17
type: execute
wave: 4
depends_on: ["17-05", "17-06", "17-07", "17-08", "17-09", "17-10", "17-11", "17-12", "17-13", "17-14", "17-15", "17-16"]
files_modified:
  - e2e/ai/agent-chat.spec.ts
  - CLAUDE.md
autonomous: true
requirements: [TEST-01, TEST-02, TEST-04]

estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Every current feature/widget/entity in src/features, src/widgets, src/entities has at least one corresponding e2e spec somewhere in the rewritten folder tree — confirmed by an explicit cross-reference, not assumed"
    - "The agent-chat (AI vision pipeline) feature — confirmed this session to have zero existing e2e coverage across all 50 original spec files — has real coverage"
    - "A repo-wide grep for bar-pos domain terms across e2e/**/*.spec.ts returns zero matches (TEST-01's final gate)"
    - "CLAUDE.md's E2E Test Suite section documents the new folder structure, not the stale flat 43/50-file list"
    - "npm run test:e2e (full suite) passes green"
  artifacts:
    - e2e/ai/agent-chat.spec.ts
---

<objective>
Phase wrap-up: close the last confirmed TEST-02 coverage gap (`agent-chat` / AI vision pipeline — zero existing e2e coverage anywhere in the original 50-file suite), run the systematic feature/widget/entity cross-reference the whole phase has been building toward, and finish TEST-01/TEST-04/D-09's closing requirements — the final bar-pos grep gate, the CLAUDE.md documentation rewrite, and a full green suite run.

Purpose: this is the phase's own goal-backward verification step, executed as a real plan rather than left to a separate verification pass.
Output: `e2e/ai/agent-chat.spec.ts` (new); `CLAUDE.md`'s E2E section rewritten; a documented gap-audit; a passing full suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/17-e2e-suite-overhaul/17-CONTEXT.md
@.planning/phases/17-e2e-suite-overhaul/17-RESEARCH.md
@.planning/phases/17-e2e-suite-overhaul/17-PATTERNS.md
@.planning/phases/17-e2e-suite-overhaul/17-01-SUMMARY.md
@.planning/phases/17-e2e-suite-overhaul/17-02-SUMMARY.md
@.planning/phases/17-e2e-suite-overhaul/17-03-SUMMARY.md
@.planning/phases/17-e2e-suite-overhaul/17-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TEST-02 coverage gap audit + e2e/ai/agent-chat.spec.ts</name>
  <files>e2e/ai/agent-chat.spec.ts</files>
  <read_first>
    src/features/agent-chat (the AI vision pipeline feature — confirmed this session, via `ls src/features/`, to exist with zero corresponding e2e spec found across any of this phase's 50 audited original files)
    src/shared/lib/agent/{brain,vision}.ts — the extraction pipeline this feature drives (image/PDF/CSV → Anthropic extraction → confirm → bulk product import, per CLAUDE.md's "AI vision pipeline" section)
    `ls src/features/ src/widgets/ src/entities/` — the full current FSD inventory (confirmed this session: 38 `features/`, 33 `widgets/`, cross-reference `src/entities/` too) to check against the final `e2e/` folder tree this phase produced (Plans 17-04 through 17-16)
  </read_first>
  <action>
    Enumerate every directory under `src/features/`, `src/widgets/`, `src/entities/` and confirm each has at least one corresponding test somewhere in the rewritten `e2e/` tree (a feature can be covered indirectly — e.g. `checkout-sale` is covered by `e2e/checkout/happy-path.spec.ts` even though the file isn't named `checkout-sale.spec.ts`). Document the full cross-reference in the plan SUMMARY (a simple two-column list: FSD unit → covering spec file(s), or "GAP" if none found).
    For `agent-chat` (the one gap already confirmed this session): create `e2e/ai/agent-chat.spec.ts` covering at minimum the confirm-then-import flow (upload an image/PDF/CSV fixture → extraction results shown for confirmation → confirm → products created) — mock the Anthropic API call via `page.route()` (do not make a real external AI API call from a test) rather than asserting on live extraction accuracy, since extraction quality is explicitly out of this phase's scope (only the pipeline's UI/DB wiring is under test here).
    For any OTHER genuine gap found during the enumeration (not already known/deferred), add minimal-but-real coverage the same way — a real Playwright test, not a stub. If a gap is found that's genuinely out of scope for this phase (e.g. requires live hardware), document it explicitly in the SUMMARY rather than silently closing the plan without addressing it.
  </action>
  <verify>
    <automated>npx playwright test e2e/ai/agent-chat.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - The plan SUMMARY contains the full FSD-unit-to-spec-file cross-reference table
    - `e2e/ai/agent-chat.spec.ts` exists and covers the confirm-then-import flow
    - `grep -c "api.anthropic.com" e2e/ai/agent-chat.spec.ts` returns 0 (mocked via `page.route()`, no real external API call from a test)
    - `npx playwright test e2e/ai/agent-chat.spec.ts` passes with 0 failures
  </acceptance_criteria>
  <done>Every current FSD unit is confirmed covered (directly or indirectly) or the gap is explicitly documented; agent-chat's previously-zero coverage is closed.</done>
</task>

<task type="auto">
  <name>Task 2: Final TEST-01 grep gate, CLAUDE.md rewrite (D-09), full suite run</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md's "E2E Test Suite" section (current — lists all files flat, needs a full rewrite to document the new folder structure)
    e2e/ directory listing (`find e2e -name '*.spec.ts' | sort`) — the final, authoritative post-rewrite file inventory to document
  </read_first>
  <action>
    Run the TEST-01 grep gate across the entire rewritten suite: `grep -rlE "pool_tables|pool-tables|/rappi|rappi_orders|/kitchen-prep|/waitlist|waitlist_entries|combo_eligible|is_combo|kds_status|KDS|promotion" e2e/**/*.spec.ts e2e/helpers/*.ts` — this MUST return zero files; if any match survives, fix it before proceeding (do not treat a lingering match as acceptable).
    Rewrite CLAUDE.md's "E2E Test Suite" section to document the new folder structure (list each `e2e/<folder>/` with a one-line description of what it covers, replacing the old flat 43-file list), per D-09. Add the one accepted manual-verification exception (from Plan 17-15's `14-manual-stubs.spec.ts` deletion — the native Tauri window + physical PIN keypad check) as an explicitly-documented, reasoned exception per CLAUDE.md's own carve-out language, not a vague gap.
    Run the full suite: `npm run test:e2e`. Fix any failures surfaced by running everything together (a cross-file interaction the per-plan targeted runs wouldn't have caught) before declaring the phase complete.
  </action>
  <verify>
    <automated>npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rlE "pool_tables|pool-tables|/rappi|rappi_orders|/kitchen-prep|/waitlist|waitlist_entries|combo_eligible|is_combo|kds_status|KDS|promotion" e2e/**/*.spec.ts e2e/helpers/*.ts` returns empty (exit 1, no matches)
    - CLAUDE.md's E2E Test Suite section lists folder paths, not a flat filename list
    - `npm run test:e2e` passes with 0 failures
    - `grep -n "headless" playwright.config.ts` confirms `headless: true` is still the default (TEST-04)
  </acceptance_criteria>
  <done>Zero bar-pos references remain anywhere in the suite, CLAUDE.md accurately documents the new structure, and the full suite is green end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test harness → Anthropic API (agent-chat coverage) | Must be mocked via `page.route()`, never a real external call from an automated test — cost, flakiness, and secret-handling risk otherwise |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-17-24 | Information Disclosure / Cost | `e2e/ai/agent-chat.spec.ts` calling the real Anthropic API | high | mitigate | Acceptance criteria's `grep -c "api.anthropic.com"` gate (must be 0) enforces mocking; this also sidesteps needing a real `ANTHROPIC_API_KEY` in the E2E environment for a test run. |
| T-17-25 | Repudiation | Full-suite cross-file interaction failures not caught by per-plan targeted runs | medium | mitigate | Task 2's `npm run test:e2e` full run is the final gate specifically to catch this class of issue before the phase is declared done. |

</threat_model>

<verification>
- TEST-01 grep gate: zero matches.
- `npm run test:e2e`: 0 failures.
- CLAUDE.md's E2E section reflects the new structure.
- FSD-unit-to-spec cross-reference documented in the SUMMARY.
</verification>

<success_criteria>
The phase goal is fully met: zero bar-pos references anywhere in `e2e/`, comprehensive current-feature coverage (confirmed by cross-reference, agent-chat gap closed), agent-browser Chrome-for-Testing as the launch target, headless-by-default preserved, `test:e2e:ui` available, and CLAUDE.md accurately documents all of it.
</success_criteria>

<artifacts_produced>
- `e2e/ai/agent-chat.spec.ts` (new)
- `CLAUDE.md` (E2E Test Suite section rewritten)
</artifacts_produced>

<output>
Create `.planning/phases/17-e2e-suite-overhaul/17-17-SUMMARY.md` when done
</output>
