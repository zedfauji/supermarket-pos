---
phase: 17-e2e-suite-overhaul
plan: 13
type: execute
wave: 3
depends_on: ["17-04"]
files_modified:
  - e2e/audit/audit-logs.spec.ts
  - e2e/audit/entity-id-crosslink.spec.ts
  - e2e/tabs/edit-paid-tab.spec.ts
  - e2e/tabs/reopen-closed-ticket.spec.ts
  - e2e/tabs/concurrent-edits.spec.ts
  - e2e/38-audit-logs.spec.ts
  - e2e/58-entity-id-crosslink.spec.ts
  - e2e/47-edit-paid-tab.spec.ts
  - e2e/48-reopen-closed-ticket.spec.ts
  - e2e/39-concurrent-edits.spec.ts
autonomous: true
requirements: [TEST-01, TEST-02]

estimate:
  tokens: 19000
  raw_tokens: 38000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "39-concurrent-edits.spec.ts's optimistic-concurrency (tabs.version race) coverage is retargeted to a real current UI surface that guards tabs.version, not left permanently skipped against deleted TabDrawer/TabCard components"
  artifacts:
    - e2e/audit/audit-logs.spec.ts
    - e2e/audit/entity-id-crosslink.spec.ts
    - e2e/tabs/edit-paid-tab.spec.ts
    - e2e/tabs/reopen-closed-ticket.spec.ts
    - e2e/tabs/concurrent-edits.spec.ts
---

<objective>
Rewrite `e2e/audit/` (2 files, both confirmed bar-pos-clean and skip-free — plain moves) and `e2e/tabs/` (3 files). `47-edit-paid-tab.spec.ts` and `48-reopen-closed-ticket.spec.ts` are clean, plain moves. `39-concurrent-edits.spec.ts`'s one test is permanently skipped: it targeted `TabDrawer`/`TabCard`'s "Close Tab" flow, both deleted in Phase 1 alongside `/pos` — but its underlying mechanism (`bumpTabVersion()`'s intercept-the-PATCH-then-bump-the-DB race simulation, proving a `STALE_VERSION` conflict surfaces as a toast instead of silent last-write-wins) is still a real, valuable concurrency guarantee to verify against whichever current UI surface still guards `tabs.version` on save.

Purpose: optimistic-concurrency correctness (no silent last-write-wins on a tab edit) is a real data-integrity property worth keeping tested, even though its original UI target is gone.
Output: `e2e/audit/audit-logs.spec.ts`, `entity-id-crosslink.spec.ts`; `e2e/tabs/edit-paid-tab.spec.ts`, `reopen-closed-ticket.spec.ts`, `concurrent-edits.spec.ts`; 5 original root files deleted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-e2e-suite-overhaul/17-CONTEXT.md
@.planning/phases/17-e2e-suite-overhaul/17-RESEARCH.md
@.planning/phases/17-e2e-suite-overhaul/17-PATTERNS.md
@.planning/phases/17-e2e-suite-overhaul/17-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e/audit/audit-logs.spec.ts + entity-id-crosslink.spec.ts</name>
  <files>e2e/audit/audit-logs.spec.ts, e2e/audit/entity-id-crosslink.spec.ts, e2e/38-audit-logs.spec.ts, e2e/58-entity-id-crosslink.spec.ts</files>
  <read_first>
    e2e/38-audit-logs.spec.ts (full file, 401 lines — no bar-pos remnants, no skips, standard move)
    e2e/58-entity-id-crosslink.spec.ts (full file, 64 lines — no bar-pos remnants, no skips, standard move)
  </read_first>
  <action>
    Move both files into `e2e/audit/`, fixing imports and swapping fixture product/entity names for the Indian catalog where relevant.
    Delete both original root files.
  </action>
  <verify>
    <automated>npx playwright test e2e/audit/ --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `test -f e2e/38-audit-logs.spec.ts` fails, `test -f e2e/58-entity-id-crosslink.spec.ts` fails
    - `npx playwright test e2e/audit/` passes with 0 failures
  </acceptance_criteria>
  <done>Both files moved, green.</done>
</task>

<task type="auto">
  <name>Task 2: e2e/tabs/ — edit-paid-tab, reopen-closed-ticket, concurrent-edits (retargeted)</name>
  <files>e2e/tabs/edit-paid-tab.spec.ts, e2e/tabs/reopen-closed-ticket.spec.ts, e2e/tabs/concurrent-edits.spec.ts, e2e/47-edit-paid-tab.spec.ts, e2e/48-reopen-closed-ticket.spec.ts, e2e/39-concurrent-edits.spec.ts</files>
  <read_first>
    e2e/47-edit-paid-tab.spec.ts (full file, 362 lines — confirmed this session: `manager opens EditPaidTabDialog from a paid tab, passes the PIN gate, edits an item and reason, and saves` is a real, current, save-guarded flow — the natural retarget candidate for the version-race test)
    e2e/48-reopen-closed-ticket.spec.ts (full file, 520 lines — no bar-pos remnants, no skips, standard move)
    e2e/39-concurrent-edits.spec.ts (full file, 102 lines — confirmed this session: its one test is unconditionally skipped, targeting the deleted `TabDrawer`/`TabCard` "Close Tab" flow; its mechanism — intercept the save `PATCH` via `page.route()`, bump `tabs.version` out from under it via `bumpTabVersion()` (from `e2e/helpers/supabase.ts`), release the stale PATCH, assert a `STALE_VERSION` conflict toast instead of silent last-write-wins — is reusable against any current UI action that performs a version-guarded `tabs` UPDATE)
    src/features/edit-paid-tab (or wherever `EditPaidTabDialog`'s save mutation lives) — confirm it reads-then-guards `tabs.version` the same way `useCloseTab` used to, before assuming it's the right retarget
  </read_first>
  <action>
    Move `e2e/47-edit-paid-tab.spec.ts` to `e2e/tabs/edit-paid-tab.spec.ts` and `e2e/48-reopen-closed-ticket.spec.ts` to `e2e/tabs/reopen-closed-ticket.spec.ts`, fixing imports and fixture names, no other changes.
    Rewrite `e2e/39-concurrent-edits.spec.ts`'s one test to retarget the version-race mechanism at `EditPaidTabDialog`'s save action (open a paid tab via `/payments`, open `EditPaidTabDialog`, make an edit, intercept the save request via `page.route()`, bump `tabs.version` via `bumpTabVersion()` before releasing the intercepted request, assert the resulting conflict is surfaced as a toast and the dialog does not silently succeed) — confirm via `<read_first>` that this mutation is genuinely version-guarded before committing to this retarget; if it is not, pick the closest current alternative that is (e.g. `ReopenTabDialog`'s save) and document the choice. Un-skip (`test.skip` → `test`), rename the test title to reflect the new target surface.
    Move the result to `e2e/tabs/concurrent-edits.spec.ts`, fixing imports and fixture names.
    Delete all 3 original root files.
  </action>
  <verify>
    <automated>npx playwright test e2e/tabs/ --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TabDrawer\|TabCard" e2e/tabs/concurrent-edits.spec.ts` returns 0
    - `grep -c "test.skip('" e2e/tabs/concurrent-edits.spec.ts` returns 0
    - `grep -c "bumpTabVersion" e2e/tabs/concurrent-edits.spec.ts` returns 1 or more (race-simulation mechanism preserved)
    - `test -f e2e/47-edit-paid-tab.spec.ts` fails, `test -f e2e/48-reopen-closed-ticket.spec.ts` fails, `test -f e2e/39-concurrent-edits.spec.ts` fails
    - `npx playwright test e2e/tabs/` passes with 0 failures
  </acceptance_criteria>
  <done>3 files moved; the version-conflict test retargeted to a real current version-guarded UI action and passing for real, not permanently skipped against deleted components.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Two concurrent POS terminals editing the same tab | The exact race `bumpTabVersion()`/the retargeted test exists to prove is handled (conflict surfaced, not silent last-write-wins) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-17-19 | Tampering | Optimistic-concurrency guard on `tabs.version` | high | mitigate | Task 2 restores real coverage of this property (previously permanently skipped) against whichever current UI surface is genuinely version-guarded. |

</threat_model>

<verification>
- `npx playwright test e2e/audit/ e2e/tabs/` passes with 0 failures.
- `VERIFICATION_REPORT.md` shows `Audit` and `Tabs` rows.
</verification>

<success_criteria>
5 files moved, the version-conflict test retargeted and passing for real against a live surface.
</success_criteria>

<artifacts_produced>
- `e2e/audit/audit-logs.spec.ts`, `entity-id-crosslink.spec.ts` (new)
- `e2e/tabs/edit-paid-tab.spec.ts`, `reopen-closed-ticket.spec.ts`, `concurrent-edits.spec.ts` (new)
- `e2e/38-audit-logs.spec.ts`, `58-entity-id-crosslink.spec.ts`, `47-edit-paid-tab.spec.ts`, `48-reopen-closed-ticket.spec.ts`, `39-concurrent-edits.spec.ts` (deleted)
</artifacts_produced>

<output>
Create `.planning/phases/17-e2e-suite-overhaul/17-13-SUMMARY.md` when done
</output>
