---
phase: 17-e2e-suite-overhaul
plan: 06
type: execute
wave: 3
depends_on: ["17-04"]
files_modified:
  - e2e/caja/session-management.spec.ts
  - e2e/caja/entries.spec.ts
  - e2e/02-caja.spec.ts
  - e2e/19-caja-entries.spec.ts
  - e2e/23-caja-entries.spec.ts
autonomous: true
requirements: [TEST-01, TEST-02]

estimate:
  tokens: 14000
  raw_tokens: 28000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "The 4 permanently-skipped caja open/close tests in 02-caja.spec.ts are un-skipped and passing (policy fix, matches Plan 17-05's split-payment precedent)"
    - "19-caja-entries.spec.ts's 7 always-self-skipping 'UI not implemented' stubs are removed — fully superseded by 23-caja-entries.spec.ts's real, passing equivalents"
    - "close_caja_session's existing 'cannot close with open tabs' guard becomes real D-11 forced-failure coverage once un-skipped"
  artifacts:
    - e2e/caja/session-management.spec.ts
    - e2e/caja/entries.spec.ts
  key_links:
    - "e2e/caja/entries.spec.ts -> e2e/helpers/db-assertions.ts assertCajaEntry -> caja_entries table"
---

<objective>
Rewrite `e2e/caja/` from 3 source files down to 2: un-skip and debug `02-caja.spec.ts`'s 4 permanently-disabled core caja-session tests to green (same policy fix pattern as Plan 17-05's split-payment), and delete `19-caja-entries.spec.ts` outright — its 7 tests are all unconditional `test.skip(true, 'UI not implemented — EXPECTED FAIL...')` stubs for functionality `23-caja-entries.spec.ts` already covers for real, unskipped, and passing.

Purpose: close two more standing CLAUDE.md policy gaps (permanently-skipped tests) and eliminate a dead-weight duplicate file in the same pass as the folder move.
Output: `e2e/caja/session-management.spec.ts`, `e2e/caja/entries.spec.ts`; all 3 original root files removed (2 moved+fixed, 1 deleted outright).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/17-e2e-suite-overhaul/17-CONTEXT.md
@.planning/phases/17-e2e-suite-overhaul/17-RESEARCH.md
@.planning/phases/17-e2e-suite-overhaul/17-PATTERNS.md
@.planning/phases/17-e2e-suite-overhaul/17-01-SUMMARY.md
@.planning/phases/17-e2e-suite-overhaul/17-02-SUMMARY.md
@.planning/phases/17-e2e-suite-overhaul/17-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e/caja/session-management.spec.ts — un-skip and debug caja open/close (policy fix)</name>
  <files>e2e/caja/session-management.spec.ts, e2e/02-caja.spec.ts</files>
  <read_first>
    e2e/02-caja.spec.ts (full file, 155 lines — confirmed this session: `POS is active after caja open`, `Cannot close caja with open tabs`, `Manager closes caja`, `Pending total shows open-tab revenue after creating a tab with an item` are all unconditional `test.skip(...)`; only `Print Summary button is visible when caja is open` and `5-card summary is hidden when caja is closed` currently run)
    e2e/helpers/supabase.ts — `openCaja()` (the service-role shortcut every other spec's `beforeEach` uses for setup) vs. the UI-driven open/close flow this file is meant to test directly — these are deliberately different code paths, do not replace the UI-driven test with a call to the `openCaja()` helper
  </read_first>
  <action>
    Create `e2e/caja/session-management.spec.ts` from `e2e/02-caja.spec.ts`. Change all 4 `test.skip('...', fn)` calls to `test('...', fn)` and debug each to a real passing state against the current `/caja` (or wherever the current UI lives — confirm the actual route/component) UI — these are core, definitely-implemented features (open caja, close caja, pending-total display), so the expectation is selector/timing drift, not missing functionality. `Cannot close caja with open tabs` is this domain's D-11 forced-failure coverage for `close_caja_session` — once passing, no separate new forced-failure test is needed for this RPC. Fix relative imports for the new folder depth; swap any hardcoded bar-food product/fixture names for the Indian catalog.
    Delete `e2e/02-caja.spec.ts`.
  </action>
  <verify>
    <automated>npx playwright test e2e/caja/session-management.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test.skip(" e2e/caja/session-management.spec.ts` returns 0 (or an explicit documented exception per CLAUDE.md's carve-out, same bar as Plan 17-05 Task 3)
    - `npx playwright test e2e/caja/session-management.spec.ts` passes with 0 failures
    - `test -f e2e/02-caja.spec.ts` fails (deleted)
  </acceptance_criteria>
  <done>All 6 tests in this file run for real (4 newly un-skipped and debugged to green), closing a standing policy gap on the app's core caja-session flow.</done>
</task>

<task type="auto">
  <name>Task 2: e2e/caja/entries.spec.ts — delete superseded duplicate, keep the real one, add D-10 assertion</name>
  <files>e2e/caja/entries.spec.ts, e2e/19-caja-entries.spec.ts, e2e/23-caja-entries.spec.ts</files>
  <read_first>
    e2e/19-caja-entries.spec.ts (full file, 227 lines — confirmed this session: all 7 tests, `CE1` through `CE7`, self-skip via `test.skip(true, 'UI not implemented — EXPECTED FAIL: ...')` inside the test body — this file predates the caja-entries UI actually shipping and was never cleaned up)
    e2e/23-caja-entries.spec.ts (full file, 202 lines — confirmed this session: 7 real, unskipped tests covering the same ground — expense/income registration, dashboard balance effect, closing-report visibility, RBAC (cashier cannot see the button), and both form-validation cases — this is the file that superseded 19-caja-entries.spec.ts)
    e2e/helpers/db-assertions.ts (Plan 17-01's output — `assertCajaEntry`)
  </read_first>
  <action>
    Confirm `e2e/23-caja-entries.spec.ts`'s 7 tests fully cover `e2e/19-caja-entries.spec.ts`'s 7 `CE1`-`CE7` intents (expense/income registration, empty-concept validation, zero-amount validation, appears-in-reports) — if a genuine gap exists (e.g. the exact-200-char-concept boundary case `CE4` has no equivalent), port ONLY that missing case into the new file rather than the whole stub file. Delete `e2e/19-caja-entries.spec.ts` in full — it is dead-weight duplicate coverage, not a rewrite candidate.
    Create `e2e/caja/entries.spec.ts` from `e2e/23-caja-entries.spec.ts`, fixing imports and swapping fixture names for the Indian catalog. Add an `assertCajaEntry(cajaSessionId, 'expense', 25)`-style call (using `e2e/helpers/db-assertions.ts`) to `Manager registers an expense during open caja`, per D-10.
    Delete `e2e/23-caja-entries.spec.ts` (its content now lives at the new path).
  </action>
  <verify>
    <automated>npx playwright test e2e/caja/entries.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `test -f e2e/19-caja-entries.spec.ts` fails (deleted)
    - `test -f e2e/23-caja-entries.spec.ts` fails (deleted)
    - `grep -c "assertCajaEntry" e2e/caja/entries.spec.ts` returns 1 or more
    - `npx playwright test e2e/caja/entries.spec.ts` passes with 0 failures
  </acceptance_criteria>
  <done>e2e/caja/entries.spec.ts is the single source of caja-entries coverage, DB-verified per D-10, with the duplicate stub file gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → `close_caja_session` RPC | State-changing, guarded against closing with open tabs — previously untested (permanently skipped), now real coverage via Task 1 |
| Test harness → Postgres (service-role, D-10 assertion) | `assertCajaEntry` reads ground truth after a UI-driven expense/income entry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-17-10 | Repudiation | `close_caja_session`'s open-tabs guard | high | mitigate | Task 1 turns this from a permanently-skipped assertion into real, running coverage — closing this domain's D-11 gap. |
| T-17-11 | Tampering | Duplicate/dead `19-caja-entries.spec.ts` masking whether real coverage exists | medium | mitigate | Task 2 explicitly confirms `23-caja-entries.spec.ts` covers the same ground before deleting the stub file, preventing an accidental net coverage loss. |

</threat_model>

<verification>
- `npx playwright test e2e/caja/` passes with 0 failures.
- `grep -c "test.skip(" e2e/caja/*.spec.ts` returns 0 across both files (or documented exceptions).
- `VERIFICATION_REPORT.md` shows a `Caja` row with a nonzero total.
</verification>

<success_criteria>
`e2e/caja/` contains exactly 2 files, both fully passing, both DB-verified per D-10 where state-changing, with the duplicate stub file and the permanently-skipped core-flow tests both resolved.
</success_criteria>

<artifacts_produced>
- `e2e/caja/session-management.spec.ts`, `e2e/caja/entries.spec.ts` (new)
- `e2e/02-caja.spec.ts`, `e2e/19-caja-entries.spec.ts`, `e2e/23-caja-entries.spec.ts` (deleted)
</artifacts_produced>

<output>
Create `.planning/phases/17-e2e-suite-overhaul/17-06-SUMMARY.md` when done
</output>
