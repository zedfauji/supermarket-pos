---
phase: 17-e2e-suite-overhaul
plan: 07
type: execute
wave: 3
depends_on: ["17-04"]
files_modified:
  - e2e/inventory/inventory-management.spec.ts
  - e2e/inventory/inventory-intelligence.spec.ts
  - e2e/inventory/near-expiry-alerts.spec.ts
  - e2e/inventory/open-units.spec.ts
  - e2e/inventory/loose-weight-hold-sale.spec.ts
  - e2e/10-inventory.spec.ts
  - e2e/27-inventory-intelligence.spec.ts
  - e2e/54-near-expiry-alerts.spec.ts
  - e2e/49-open-units.spec.ts
  - e2e/52-loose-weight-hold-sale.spec.ts
autonomous: true
requirements: [TEST-01, TEST-02]

estimate:
  tokens: 19000
  raw_tokens: 38000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "10-inventory.spec.ts's 2 low-stock-alert tests target a real current low-stock UI surface, or are removed with a documented reason if `widgets/LowStockAlert` is confirmed genuinely orphaned (zero callers anywhere in pages/widgets/app as of this planning session)"
    - "49-open-units.spec.ts's monolithic skipped checklist test is confirmed redundant with its own file's earlier 'salvaged' split coverage before being deleted, not silently dropped"
    - "e2e/inventory/open-units.spec.ts and loose-weight-hold-sale.spec.ts keep whatever non-Indian/generic fixtures they need for unit-type mechanics, per D-05 — not required to use the Indian catalog"
  artifacts:
    - e2e/inventory/inventory-management.spec.ts
    - e2e/inventory/inventory-intelligence.spec.ts
    - e2e/inventory/near-expiry-alerts.spec.ts
    - e2e/inventory/open-units.spec.ts
    - e2e/inventory/loose-weight-hold-sale.spec.ts
---

<objective>
Rewrite `e2e/inventory/` (5 source files): resolve `10-inventory.spec.ts`'s 2 permanently-skipped low-stock-alert tests (confirmed this session — `widgets/LowStockAlert/index.tsx` has zero current callers anywhere in `src/pages`/`src/widgets`/`src/app`, a genuinely orphaned component, not just a stale skip reason), confirm-then-delete `49-open-units.spec.ts`'s monolithic skipped checklist test, and move the remaining 3 files with Indian-catalog fixtures (except `open-units`/`loose-weight-hold-sale`, which keep their existing generic fixtures per D-05).

Purpose: close the low-stock-alert coverage gap for real (retarget to whatever current UI surface actually shows low stock, or document its absence) rather than leaving 2 permanently-skipped tests pointing at dead code.
Output: `e2e/inventory/inventory-management.spec.ts`, `inventory-intelligence.spec.ts`, `near-expiry-alerts.spec.ts`, `open-units.spec.ts`, `loose-weight-hold-sale.spec.ts`; 5 original root files deleted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/17-e2e-suite-overhaul/17-CONTEXT.md
@.planning/phases/17-e2e-suite-overhaul/17-RESEARCH.md
@.planning/phases/17-e2e-suite-overhaul/17-PATTERNS.md
@.planning/phases/17-e2e-suite-overhaul/17-02-SUMMARY.md
@.planning/phases/17-e2e-suite-overhaul/17-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e/inventory/inventory-management.spec.ts + inventory-intelligence.spec.ts + near-expiry-alerts.spec.ts</name>
  <files>e2e/inventory/inventory-management.spec.ts, e2e/inventory/inventory-intelligence.spec.ts, e2e/inventory/near-expiry-alerts.spec.ts, e2e/10-inventory.spec.ts, e2e/27-inventory-intelligence.spec.ts, e2e/54-near-expiry-alerts.spec.ts</files>
  <read_first>
    e2e/10-inventory.spec.ts (full file, 278 lines — confirmed this session: `Low stock alert visible to manager` and `Low stock alert hidden from bartender` are unconditionally `test.skip`'d against `widgets/LowStockAlert`, which — confirmed by a targeted `grep -rln "LowStockAlert" src/pages src/widgets src/app` this session, excluding the widget's own `index.tsx` — has ZERO current callers anywhere in the app)
    src/widgets/LowStockAlert/index.tsx (the orphaned widget itself — check its props/query to understand what it was meant to show)
    src/pages/InventoryPage.tsx or equivalent, and any Home-dashboard widget referencing low stock (CLAUDE.md: "low-stock list driven by per-product reorder point" is described as part of the Inventory page's feature set — find where that actually renders today)
    e2e/27-inventory-intelligence.spec.ts, e2e/54-near-expiry-alerts.spec.ts (full files — no bar-pos remnants confirmed, standard move + fixture-swap)
    scripts/seed-dev-data.ts (Plan 17-02's output — Indian catalog + expiry-date spread, needed for near-expiry-alerts assertions)
  </read_first>
  <action>
    For the 2 low-stock-alert tests in `10-inventory.spec.ts`: determine whether the app has ANY current low-stock UI surface (a badge, count, or filtered list — on `/inventory`, `/home`, or elsewhere) that renders the same underlying data `widgets/LowStockAlert` was meant to show. If yes, rewrite both tests to target that real current surface, preserving each test's original assertion intent (visible to manager, hidden/absent for cashier). If genuinely no such surface exists anywhere in the current app (confirm via search, don't assume), delete both tests with a one-line comment explaining the orphaned-widget finding — do not leave them silently skipped.
    Create `e2e/inventory/inventory-management.spec.ts` from `10-inventory.spec.ts`'s remaining tests, `e2e/inventory/inventory-intelligence.spec.ts` from `27-inventory-intelligence.spec.ts`, and `e2e/inventory/near-expiry-alerts.spec.ts` from `54-near-expiry-alerts.spec.ts` — fix imports, swap fixture product names for the Indian catalog (near-expiry-alerts specifically should reference products whose seeded `expiry_date` falls inside vs. outside the 14-day alert window, per Plan 17-02's varied-expiry seeding).
    Delete the 3 original root files.
  </action>
  <verify>
    <automated>npx playwright test e2e/inventory/inventory-management.spec.ts e2e/inventory/inventory-intelligence.spec.ts e2e/inventory/near-expiry-alerts.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test.skip('" e2e/inventory/inventory-management.spec.ts` returns 0
    - Either both low-stock-alert tests pass against a real current UI surface, or both are absent with a documented one-line reason in their place
    - `test -f e2e/10-inventory.spec.ts` fails, same for the other 2 originals
    - `npx playwright test e2e/inventory/inventory-management.spec.ts e2e/inventory/inventory-intelligence.spec.ts e2e/inventory/near-expiry-alerts.spec.ts` passes with 0 failures
  </acceptance_criteria>
  <done>3 files moved with Indian fixtures; low-stock-alert coverage either resurrected against a real surface or explicitly documented as absent, never left silently skipped.</done>
</task>

<task type="auto">
  <name>Task 2: e2e/inventory/open-units.spec.ts + loose-weight-hold-sale.spec.ts (D-05 — generic fixtures kept)</name>
  <files>e2e/inventory/open-units.spec.ts, e2e/inventory/loose-weight-hold-sale.spec.ts, e2e/49-open-units.spec.ts, e2e/52-loose-weight-hold-sale.spec.ts</files>
  <read_first>
    e2e/49-open-units.spec.ts (full file, 1062 lines — the largest spec in the suite; confirmed this session: the final test is a monolithic, unconditionally-`test.skip`'d checklist ("configure, stock, open, sell, exhaust, override, correct/void, RBAC tiers, D-06, audit") whose own comment says it's "kept verbatim... so it can be restored... see the split-rationale comment above the previous test for what's salvaged vs. blocked" — read that split-rationale comment and the test immediately preceding this one to confirm the split already covers this checklist's ground before deleting)
    e2e/52-loose-weight-hold-sale.spec.ts (full file, 421 lines — no skips, no bar-pos remnants, standard move keeping its existing fixtures per D-05)
  </read_first>
  <action>
    In `e2e/49-open-units.spec.ts`: read the "split-rationale" comment and the test immediately above the final monolithic skipped test. If that earlier test's coverage genuinely supersedes the monolithic checklist (expected, per the file's own comment), delete the monolithic skipped test entirely. If a genuine gap remains (some named requirement — SC-1..SC-4, D-02/D-03/D-05/D-06/D-07/D-08/D-09/D-10/D-11/D-12 — isn't covered by the earlier test), port only that missing piece into the earlier test or a new small test, not the whole monolithic block.
    Move the remainder of `e2e/49-open-units.spec.ts` to `e2e/inventory/open-units.spec.ts` and all of `e2e/52-loose-weight-hold-sale.spec.ts` to `e2e/inventory/loose-weight-hold-sale.spec.ts`, fixing imports only — per D-05, do NOT swap these files' product fixtures for the Indian catalog; they keep whatever case/piece and kg/g test products they already use for unit-type mechanics.
    Delete both original root files.
  </action>
  <verify>
    <automated>npx playwright test e2e/inventory/open-units.spec.ts e2e/inventory/loose-weight-hold-sale.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test.skip('" e2e/inventory/open-units.spec.ts` returns 0
    - The plan SUMMARY documents whether the monolithic checklist was confirmed redundant-and-deleted, or partially ported
    - `test -f e2e/49-open-units.spec.ts` fails, `test -f e2e/52-loose-weight-hold-sale.spec.ts` fails
    - `npx playwright test e2e/inventory/open-units.spec.ts e2e/inventory/loose-weight-hold-sale.spec.ts` passes with 0 failures
  </acceptance_criteria>
  <done>Both files moved, generic fixtures preserved per D-05, monolithic dead skip resolved with documented reasoning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → inventory adjustment RPCs | `adjust-inventory`/`physical-count` state-changing flows already have D-10 UI+DB coverage in the existing `10-inventory.spec.ts` content (unchanged by this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-17-12 | Information Disclosure | Low-stock data visible to unauthorized roles | medium | mitigate | If the low-stock-alert tests are resurrected against a real surface, both the "visible to manager" and "hidden from cashier" cases are preserved — this is an RBAC-adjacent visibility boundary, not a merely cosmetic check. |

</threat_model>

<verification>
- `npx playwright test e2e/inventory/` passes with 0 failures.
- `VERIFICATION_REPORT.md` shows an `Inventory` row with a nonzero total.
</verification>

<success_criteria>
`e2e/inventory/` contains 5 fully-passing files, the low-stock-alert gap is resolved (not silently skipped), the monolithic dead skip in open-units is resolved with documented reasoning, and D-05's fixture-preservation rule is honored for open-units/loose-weight.
</success_criteria>

<artifacts_produced>
- `e2e/inventory/inventory-management.spec.ts`, `inventory-intelligence.spec.ts`, `near-expiry-alerts.spec.ts`, `open-units.spec.ts`, `loose-weight-hold-sale.spec.ts` (new)
- `e2e/10-inventory.spec.ts`, `27-inventory-intelligence.spec.ts`, `54-near-expiry-alerts.spec.ts`, `49-open-units.spec.ts`, `52-loose-weight-hold-sale.spec.ts` (deleted)
</artifacts_produced>

<output>
Create `.planning/phases/17-e2e-suite-overhaul/17-07-SUMMARY.md` when done
</output>
