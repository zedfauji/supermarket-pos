---
phase: 17-e2e-suite-overhaul
plan: 16
type: execute
wave: 3
depends_on: ["17-04"]
files_modified:
  - e2e/visual/45-visual-baseline.spec.ts
autonomous: true
requirements: [TEST-01, TEST-02]

estimate:
  tokens: 19000
  raw_tokens: 38000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "e2e/visual/45-visual-baseline.spec.ts's route list matches the CURRENT src/app/router.tsx registration exactly — zero dropped-route entries (pool-tables, kds, kds-bar, kitchen-prep, waitlist, rappi)"
    - "playwright.visual.config.ts is untouched — this plan is content-only, per the explicit phase-boundary decision resolving RESEARCH.md's Open Question 2"
  artifacts:
    - e2e/visual/45-visual-baseline.spec.ts
---

<objective>
Rewrite `e2e/visual/45-visual-baseline.spec.ts` — confirmed this session as the single heaviest bar-pos-content file in the whole suite (30+ matches across `pool_tables`/`pool-tables`, `/rappi`, `kds`, `/kitchen-prep`, `/waitlist`). Its entire route/screenshot list is built against the pre-Phase-1 17-route registration, none of which matches the current `src/app/router.tsx`. **Scope decision (resolving RESEARCH.md's Open Question 2 explicitly, as the planner):** this file's CONTENT is in scope for TEST-01's audit — the phase boundary's "does not touch... test:e2e:visual's separate config" refers only to `playwright.visual.config.ts`, not this spec's content.

Purpose: this is the file most likely to be silently forgotten (runs under a separate config, not the main `npm run test:e2e`) — resolve it explicitly, in this phase, per D-08's spirit.
Output: `e2e/visual/45-visual-baseline.spec.ts` rebuilt against the current route registration; `playwright.visual.config.ts` untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/17-e2e-suite-overhaul/17-CONTEXT.md
@.planning/phases/17-e2e-suite-overhaul/17-RESEARCH.md
@.planning/phases/17-e2e-suite-overhaul/17-02-SUMMARY.md
@src/app/router.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the route/screenshot list against the current router</name>
  <files>e2e/visual/45-visual-baseline.spec.ts</files>
  <read_first>
    e2e/visual/45-visual-baseline.spec.ts (full file, 371 lines — confirmed this session: built entirely around a stale "17 registered routes" comment and a `seedOccupiedTableDirect()` helper that queries the dropped `pool_tables`/`pool_sessions` tables, plus a `seedKdsFoodOrder` import (already deleted from `e2e/helpers/supabase.ts` in Plan 17-03) — this file will not even typecheck until its imports are fixed)
    src/app/router.tsx (the CURRENT, authoritative route registration — build the new route list from this file directly, with each route's role-gating per CLAUDE.md's Routes table: `/home`, `/pos`, `/inventory` (manager+), `/staff`, `/suppliers`, `/reports`, `/settings` (admin), `/payments`, `/login`, `/rbac` (admin), `/audit`, `/edit-history`, plus `/purchase-orders` (Phase 16, manager+) — confirm the exact current list and role gates directly from the router file, do not assume this list is exhaustive or exactly correct)
    e2e/helpers/auth.ts (`gotoAuthed`, `loginAs`, role-gating helpers already used elsewhere in this file)
  </read_first>
  <action>
    Delete `seedOccupiedTableDirect()` entirely (dropped tables) and its one call site (`pool-tables idle grid — admin, cashier, manager...` test — delete this whole test, the route it screenshots no longer exists). Fix the `seedKdsFoodOrder` import (removed in Plan 17-03) — remove it and any test that depended on it.
    Rebuild the admin/cashier/manager route lists (the "remaining N routes" tests) from `src/app/router.tsx`'s actual current registration and role gates, replacing every dropped-route entry (`/pool-tables`, `/kds`, `/kds-bar`, `/kitchen-prep`, `/waitlist`, `/rappi`) with the routes that now exist instead (`/purchase-orders`, `/edit-history`, etc. — confirm the full current set from the router file). Update the header comment's route count and route-registration description to match.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -icE "pool_tables|pool-tables|/rappi|/kds|/kitchen-prep|/waitlist" e2e/visual/45-visual-baseline.spec.ts` returns 0
    - `grep -c "seedKdsFoodOrder\|seedOccupiedTableDirect" e2e/visual/45-visual-baseline.spec.ts` returns 0
    - Every `page.goto('/...')` literal in this file corresponds to a route that exists in `src/app/router.tsx` (manual cross-check, documented in the plan SUMMARY)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>The file's route list exactly matches the current router registration, zero dropped-route references remain, and the file typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: Generate new baseline screenshots and verify the suite passes</name>
  <files>e2e/visual/45-visual-baseline.spec.ts</files>
  <read_first>
    playwright.visual.config.ts (unchanged by this plan — confirms the `test:e2e:visual` command and snapshot directory)
  </read_first>
  <action>
    Run the visual suite with `--update-snapshots` to generate new baseline images for every route in Task 1's rebuilt list (the old baselines, keyed by the old route slugs, are now orphaned and should be removed if the snapshot tooling doesn't already prune them). Run the suite again WITHOUT `--update-snapshots` to confirm it passes against the freshly-generated baselines (a real green run, not just "images were written").
  </action>
  <verify>
    <automated>npm run test:e2e:visual</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:e2e:visual -- --update-snapshots` completes without error
    - `npm run test:e2e:visual` (no update flag, second run) passes with 0 failures
    - No orphaned baseline image files remain for deleted routes (e.g. `*pool-tables*.png`, `*kds*.png`)
  </acceptance_criteria>
  <done>Fresh, current baseline screenshots exist and the visual regression suite passes green against them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Screenshot content → role-gated route access | The admin/cashier/manager route lists this file exercises double as a lightweight RBAC-visibility smoke check |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-17-23 | Elevation of Privilege | Role-gated route screenshot list drifting from real router gates | medium | mitigate | Task 1's acceptance criteria requires a documented manual cross-check against `src/app/router.tsx`'s actual current gates, not an assumed/copied list. |

</threat_model>

<verification>
- `npm run typecheck` exits 0.
- `npm run test:e2e:visual` passes with 0 failures against fresh baselines.
</verification>

<success_criteria>
e2e/visual/45-visual-baseline.spec.ts's route list exactly matches the current app, zero bar-pos content remains, fresh baselines pass.
</success_criteria>

<artifacts_produced>
- `e2e/visual/45-visual-baseline.spec.ts` (rewritten in place)
- New baseline screenshot files under the visual config's snapshot directory
</artifacts_produced>

<output>
Create `.planning/phases/17-e2e-suite-overhaul/17-16-SUMMARY.md` when done
</output>
