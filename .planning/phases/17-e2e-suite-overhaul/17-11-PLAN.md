---
phase: 17-e2e-suite-overhaul
plan: 11
type: execute
wave: 3
depends_on: ["17-04", "17-03"]
files_modified:
  - e2e/rbac/rbac.spec.ts
  - e2e/rbac/staff-management.spec.ts
  - e2e/rbac/rls-boundary.spec.ts
  - e2e/09-rbac.spec.ts
  - e2e/22-staff-management.spec.ts
autonomous: true
requirements: [TEST-01, TEST-02]

estimate:
  tokens: 19500
  raw_tokens: 39000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "09-rbac.spec.ts's Rappi/kds-dead tests (T6, kds refs) are deleted; T7 (delete-tab) and T10 (caja-on-pos) are deleted per their own confirmed-stale premises; the 2 Bucket-B close-tab-pay tests are rewritten against the current checkout UI"
    - "A representative RLS-boundary check exists: a cashier-role signed-in client is denied a manager+-gated mutation, verified via a real Postgres error, never via a service-role client (D-13)"
  artifacts:
    - e2e/rbac/rls-boundary.spec.ts
  key_links:
    - "e2e/rbac/rls-boundary.spec.ts -> e2e/helpers/rls-clients.ts createRoleScopedClient -> anon-key signed-in client -> RLS denial"
---

<objective>
Rewrite `e2e/rbac/` (2 source files) and add `rls-boundary.spec.ts`, this phase's dedicated D-13 representative RLS-boundary coverage. `09-rbac.spec.ts` needs the most work of the two: delete `T6: Kitchen user is blocked from /rappi page` (dead route/table), delete `T7: admin deletes a tab` (its own comment confirms no delete-tab UI exists anywhere in the current app — nothing to retarget), delete `T10: bartender on /pos — caja open/close` (its own comment confirms the premise no longer applies — caja controls live on `/staff`, never `/pos`, and `/pos` now redirects nowhere near this test's assumption), and rewrite `Bartender Close Tab / Pay button is present` + `Manager can Close Tab / Pay` against the CURRENT direct-sale checkout UI (per Plan 17-04's proven interaction pattern) instead of the deleted old tab-opening flow.

Purpose: RBAC is the domain where a stale test silently passing (or silently absent) has the highest security cost — get the deletions right (confirmed-dead, not just skipped) and the live checkout-permission checks rewritten for real.
Output: `e2e/rbac/rbac.spec.ts`, `staff-management.spec.ts`, `rls-boundary.spec.ts`; 2 original root files deleted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/17-e2e-suite-overhaul/17-CONTEXT.md
@.planning/phases/17-e2e-suite-overhaul/17-RESEARCH.md
@.planning/phases/17-e2e-suite-overhaul/17-PATTERNS.md
@.planning/phases/17-e2e-suite-overhaul/17-03-SUMMARY.md
@.planning/phases/17-e2e-suite-overhaul/17-04-SUMMARY.md
@src/shared/lib/rbac.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e/rbac/rbac.spec.ts + staff-management.spec.ts</name>
  <files>e2e/rbac/rbac.spec.ts, e2e/rbac/staff-management.spec.ts, e2e/09-rbac.spec.ts, e2e/22-staff-management.spec.ts</files>
  <read_first>
    e2e/09-rbac.spec.ts (full file, 392 lines — confirmed this session: `T6: Kitchen user is blocked from /rappi page` targets the dropped `/rappi` route/`rappi_orders` table (delete); `T7: admin deletes a tab` is unconditionally skipped with its own comment confirming "no delete-tab UI exists anywhere else in the app... only the RBAC action definition remains... zero UI consumers" (delete, nothing to retarget); `T10: bartender on /pos — caja open/close` is unconditionally skipped with its own comment confirming "caja controls live on /staff (CajaDashboard), never on /pos... this test's premise... no longer has a page to check against" (delete); `Bartender Close Tab / Pay button is present` and `Manager can Close Tab / Pay` are unconditionally skipped "Bucket B" tests (same `/pos` New-Tab-flow staleness as Plan 17-05's `05-payments.spec.ts`) — rewrite against the CURRENT direct-sale checkout UI; `Bartender cannot access Reports route` and `void-order control is absent from every screen it could plausibly appear on` are already real, passing, current tests — preserve verbatim
    e2e/checkout/happy-path.spec.ts (Plan 17-04's output — the proven current scan/cart/pay interaction pattern to rewrite the 2 Bucket-B tests against; for the RBAC-specific assertion, the relevant fact is whether the pay-button/flow is reachable and PIN-gated appropriately per role, not the exact old "New Tab → Place Order" mechanics)
    e2e/22-staff-management.spec.ts (full file, 363 lines — no bar-pos remnants, standard move)
    src/shared/lib/rbac.ts (current role set — `cashier < manager < admin`, non-hierarchical `kitchen` — confirm role names used in rewritten tests match exactly; the old file's `bartender`-flavored test titles refer to the current `cashier` role, per this repo's Phase 1 rename, D-16)
  </read_first>
  <action>
    Delete `T6`, `T7`, `T10` from `09-rbac.spec.ts` entirely.
    Rewrite `Bartender Close Tab / Pay button is present (requires PIN verification)` and `Manager can Close Tab / Pay` to drive the current `/pos` scan/cart/pay flow (per `e2e/checkout/happy-path.spec.ts`'s pattern), asserting the same underlying RBAC fact each test originally intended: a `cashier`-role session can reach and complete the pay step (has `close_tab`/`process_payment`-equivalent access without needing a manager PIN for their own sale), while cross-checking that manager-only mutations elsewhere still require the PIN gate. Rename both tests to drop the now-outdated "Close Tab / Pay button" framing if the current UI doesn't literally have that control (use the actual current control's accessible name instead).
    Move the result to `e2e/rbac/rbac.spec.ts`, fixing imports and fixture names.
    Move `22-staff-management.spec.ts` to `e2e/rbac/staff-management.spec.ts` verbatim, fixing imports and fixture names.
    Delete both original root files.
  </action>
  <verify>
    <automated>npx playwright test e2e/rbac/rbac.spec.ts e2e/rbac/staff-management.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -icE "rappi|/rappi" e2e/rbac/rbac.spec.ts` returns 0
    - `grep -c "test.skip('" e2e/rbac/rbac.spec.ts` returns 0
    - `grep -c "void-order control is absent" e2e/rbac/rbac.spec.ts` returns 1 (preserved verbatim)
    - `test -f e2e/09-rbac.spec.ts` fails, `test -f e2e/22-staff-management.spec.ts` fails
    - `npx playwright test e2e/rbac/rbac.spec.ts e2e/rbac/staff-management.spec.ts` passes with 0 failures
  </acceptance_criteria>
  <done>3 dead/stale-premise tests removed, 2 Bucket-B tests rewritten against current UI, all other coverage preserved and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: e2e/rbac/rls-boundary.spec.ts — D-13 representative RLS checks (new file)</name>
  <files>e2e/rbac/rls-boundary.spec.ts</files>
  <read_first>
    e2e/helpers/rls-clients.ts (Plan 17-03's output — `createRoleScopedClient(role, label)`)
    src/entities/purchase-order/model/purchase-orders-rls.integration.test.ts (the proven RLS-denial pattern to mirror — dual-client, distinct storageKey, `.insert()`/`.update()` attempt asserted to fail with an RLS-shaped error, not silently no-op)
    src/shared/lib/rbac.ts (RBAC action list — pick 2-3 representative manager+/admin-only mutations for this spec: e.g. `manage_purchase_orders`-equivalent, `process_refund`, `manage_caja`)
  </read_first>
  <behavior>
    - Test 1: a `cashier`-role client (via `createRoleScopedClient('cashier', ...)`) attempting to `insert` a `purchase_orders` row is rejected with an RLS-shaped error (Postgres `42501` or PostgREST's equivalent denial), never silently succeeding.
    - Test 2: a `cashier`-role client attempting to `update` a `caja_sessions` row (e.g. closing a session) is rejected the same way.
    - Test 3 (sanity/negative control): a `manager`-role client performing the SAME mutation as Test 1 succeeds — proving the denial in Test 1 is role-specific, not a broken query.
    - Every denial assertion uses the `cashier`/`manager` clients from `createRoleScopedClient()` — never `getServiceClient()`, which would bypass RLS and produce a false-negative pass (the exact threat this file exists to prevent).
  </behavior>
  <action>
    Create `e2e/rbac/rls-boundary.spec.ts` with the 3 tests described in `<behavior>`, calling each client's `cleanup()` in `afterEach`/`afterAll`. Import `createRoleScopedClient` from `../helpers/rls-clients` and `requireIntegrationEnv` as usual.
  </action>
  <verify>
    <automated>npx playwright test e2e/rbac/rls-boundary.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getServiceClient" e2e/rbac/rls-boundary.spec.ts` returns 0 (D-13 — service-role never used for a denial assertion)
    - `grep -c "createRoleScopedClient" e2e/rbac/rls-boundary.spec.ts` returns 3 or more
    - `npx playwright test e2e/rbac/rls-boundary.spec.ts` passes with 0 failures
  </acceptance_criteria>
  <done>e2e/rbac/rls-boundary.spec.ts exists, passes, and is the phase's concrete D-13 deliverable — a cashier is genuinely denied by Postgres RLS on 2 manager+-gated mutations, proven with a real signed-in client, not a service-role shortcut.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| cashier role → manager+-gated RPCs/tables | The exact boundary Task 2 exists to verify |
| Test harness → Postgres (anon + signed-in, per Plan 17-03's `rls-clients.ts`) | The only sanctioned client for a denial assertion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-17-01 | Elevation of Privilege (false negative) | `e2e/rbac/rls-boundary.spec.ts` | high | mitigate | Acceptance criteria's `grep -c "getServiceClient"` gate (must be 0) directly enforces this plan cannot ship the exact anti-pattern the threat register warns about — a service-role-backed "denial" test that always passes regardless of real RLS behavior. |
| T-17-17 | Elevation of Privilege | `T7`/`T10` deletion in `09-rbac.spec.ts` | medium | accept | Both confirmed genuinely dead (no UI surface, stale premise) by their own in-file comments before deletion — not a coverage reduction, since neither ever ran (permanently skipped). |

</threat_model>

<verification>
- `npx playwright test e2e/rbac/` passes with 0 failures.
- `grep -c "getServiceClient" e2e/rbac/rls-boundary.spec.ts` returns 0.
- `VERIFICATION_REPORT.md` shows an `Rbac` row.
</verification>

<success_criteria>
`e2e/rbac/` contains 3 fully-passing files: dead/stale tests removed, Bucket-B tests rewritten against current UI, and this phase's D-13 RLS-boundary deliverable is real and correctly constructed.
</success_criteria>

<artifacts_produced>
- `e2e/rbac/rbac.spec.ts`, `staff-management.spec.ts`, `rls-boundary.spec.ts` (new)
- `e2e/09-rbac.spec.ts`, `22-staff-management.spec.ts` (deleted)
</artifacts_produced>

<output>
Create `.planning/phases/17-e2e-suite-overhaul/17-11-SUMMARY.md` when done
</output>
