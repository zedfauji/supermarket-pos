---
phase: 17-e2e-suite-overhaul
plan: 09
type: execute
wave: 3
depends_on: ["17-04"]
files_modified:
  - e2e/reports/report-tabs.spec.ts
  - e2e/reports/product-sales.spec.ts
  - e2e/reports/export.spec.ts
  - e2e/reports/discount-and-revenue.spec.ts
  - e2e/07-reports.spec.ts
  - e2e/19-product-sales-report.spec.ts
  - e2e/25-export-reports.spec.ts
  - e2e/20-sprint2-revenue.spec.ts
autonomous: true
requirements: [TEST-01, TEST-02]

estimate:
  tokens: 27500
  raw_tokens: 55000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "e2e/reports/ contains zero pool-table/Rappi references — the removal test no longer needs a pool table at all (it only ever needed an open tab with 2+ items)"
    - "20-sprint2-revenue.spec.ts's discount tests (D1/D3/D4) target PaymentModal's still-live discount-section, reached via /payments (PaymentPane), not the deleted /pos New-Tab flow"
    - "D2 ('Pool Only' scope) and D5 (promotions banner) are deleted, not resurrected — both are genuinely dead bar-pos concepts"
  artifacts:
    - e2e/reports/report-tabs.spec.ts
    - e2e/reports/product-sales.spec.ts
    - e2e/reports/export.spec.ts
    - e2e/reports/discount-and-revenue.spec.ts
---

<objective>
Rewrite `e2e/reports/` (4 source files, including the suite's largest file at 1557 lines). Most of `07-reports.spec.ts`'s ~34 tests are already current and valid (every `ReportsPage` tab they exercise — session, products, hourly, categories, payment-methods, inventory-analytics, staff, voids, deletions-pre/post, refunds-reg — confirmed still live in `src/pages/reports/index.tsx`); the actual required fixes are narrow: strip "rappi" from one revenue-breakdown assertion, and replace the pool-table-dependent removal-test seed helper with a plain open-tab seed (the test never needed a pool table — it only needs an open tab with 2+ order items). `20-sprint2-revenue.spec.ts` needs a real split: its discount tests are still valid against `PaymentModal` (now reached via `/payments`, not the deleted `/pos`), its pool-scope and promotions-banner tests are genuinely dead.

Purpose: this file is the single riskiest rewrite in the whole phase by line count — get it right with surgical, evidence-backed fixes rather than a wholesale rewrite that risks losing 30+ working tests.
Output: `e2e/reports/report-tabs.spec.ts`, `product-sales.spec.ts`, `export.spec.ts`, `discount-and-revenue.spec.ts`; 4 original root files deleted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/17-e2e-suite-overhaul/17-CONTEXT.md
@.planning/phases/17-e2e-suite-overhaul/17-RESEARCH.md
@.planning/phases/17-e2e-suite-overhaul/17-PATTERNS.md
@.planning/phases/17-e2e-suite-overhaul/17-02-SUMMARY.md
@.planning/phases/17-e2e-suite-overhaul/17-05-SUMMARY.md
@src/pages/reports/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e/reports/report-tabs.spec.ts — surgical fixes to the 1557-line file</name>
  <files>e2e/reports/report-tabs.spec.ts, e2e/07-reports.spec.ts</files>
  <read_first>
    e2e/07-reports.spec.ts (full file, 1557 lines — confirmed this session: `Revenue breakdown shows cash, card, rappi` (~line 547) asserts on a "rappi" payment-method breakdown that no longer exists (payment methods are cash/card/split only); `seedRemovableItem()` (~lines 405-465) queries the dropped `pool_tables`/`pool_sessions` tables purely as scaffolding for `Phase 24: bartender-initiated reason-required removal succeeds...` (~line 1237), which actually only needs an open tab with 2+ order items, not a pool table at all — every other test in this file targets a `ReportsPage` tab (`session`, `products`, `hourly`, `categories`, `payment-methods`, `inventory-analytics`, `staff`, `voids`, `deletions-pre`, `deletions-post`, `refunds-reg`) confirmed still live in `src/pages/reports/index.tsx`)
    src/pages/reports/index.tsx (confirms the current tab set — cross-check every test's target tab exists; if any test targets a tab NOT in this file's `TabsTrigger` list, flag and resolve rather than assuming)
    src/features/remove-tab-item/useRemoveTabItem.ts, src/features/remove-tab-item/ui/RemoveTabItemDialog.tsx (the current, live "remove item" feature this test should actually exercise)
    e2e/helpers/supabase.ts (Plan 17-03's output — `seedOpenTab()` pattern to model the replacement seed helper on, now requiring an explicit `productName`)
  </read_first>
  <action>
    Rename the test `Revenue breakdown shows cash, card, rappi` to `Revenue breakdown shows cash and card` and remove its "rappi" assertion (leave cash/card assertions intact).
    Replace `seedRemovableItem()` with a plain seed function that creates an open tab with 2 Indian-catalog order items directly via the service-role client (mirroring `seedOpenTab()`'s pattern, extended to insert 2 order_items instead of 1) — delete every `pool_tables`/`pool_sessions` query from this helper entirely; the test using it (`Phase 24: bartender-initiated reason-required removal succeeds...`) only needs `page.goto('/payments')` → open the seeded tab → use `RemoveTabItemDialog`'s reason-required removal flow, not `/pool-tables/:tableId`.
    Move the full file to `e2e/reports/report-tabs.spec.ts`, fixing imports and swapping every hardcoded bar-food product name literal for Indian catalog products (there will be many — this is the bulk of the mechanical work in this task).
    Delete `e2e/07-reports.spec.ts`.
  </action>
  <verify>
    <automated>npx playwright test e2e/reports/report-tabs.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -icE "pool_tables|pool_sessions|/pool-tables|rappi" e2e/reports/report-tabs.spec.ts` returns 0
    - `grep -c "seedRemovableItem" e2e/reports/report-tabs.spec.ts` returns 0 (renamed/replaced) or, if the name is kept, the function body contains zero pool-table queries
    - Test count is preserved: `grep -c "test(" e2e/reports/report-tabs.spec.ts` is within 1-2 of the original file's `test(` count (accounting for the rename, not a deletion)
    - `test -f e2e/07-reports.spec.ts` fails (deleted)
    - `npx playwright test e2e/reports/report-tabs.spec.ts` passes with 0 failures
  </acceptance_criteria>
  <done>e2e/reports/report-tabs.spec.ts is bar-pos-clean, all ~34 tests preserved and passing against Indian fixtures, the removal test exercises the current RemoveTabItemDialog flow with no pool-table dependency.</done>
  <reversibility rating="costly">1557-line file, the largest single rewrite in the phase — costly to get wrong (re-review is expensive), not one-way (git revert restores fully). Surgical-fix approach (not wholesale rewrite) is the mitigation.</reversibility>
</task>

<task type="auto">
  <name>Task 2: e2e/reports/product-sales.spec.ts + export.spec.ts</name>
  <files>e2e/reports/product-sales.spec.ts, e2e/reports/export.spec.ts, e2e/19-product-sales-report.spec.ts, e2e/25-export-reports.spec.ts</files>
  <read_first>
    e2e/19-product-sales-report.spec.ts (full file, 75 lines — no bar-pos remnants, standard move)
    e2e/25-export-reports.spec.ts (full file, 357 lines — no bar-pos remnants, standard move)
  </read_first>
  <action>
    Move both files to `e2e/reports/product-sales.spec.ts` and `e2e/reports/export.spec.ts`, fixing imports and swapping fixture product names for the Indian catalog.
    Delete both original root files.
  </action>
  <verify>
    <automated>npx playwright test e2e/reports/product-sales.spec.ts e2e/reports/export.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `test -f e2e/19-product-sales-report.spec.ts` fails, `test -f e2e/25-export-reports.spec.ts` fails
    - `npx playwright test e2e/reports/product-sales.spec.ts e2e/reports/export.spec.ts` passes with 0 failures
  </acceptance_criteria>
  <done>Both files moved with Indian fixtures, green.</done>
</task>

<task type="auto">
  <name>Task 3: e2e/reports/discount-and-revenue.spec.ts — split live discount UI from dead pool/promotions</name>
  <files>e2e/reports/discount-and-revenue.spec.ts, e2e/20-sprint2-revenue.spec.ts</files>
  <read_first>
    e2e/20-sprint2-revenue.spec.ts (full file, 172 lines — confirmed this session: `D1` (discount section visible), `D3` (Fixed $5 discount), `D4` (10% percent discount) all target `PaymentModal`'s `discount-section`/`discount-type-fixed`/`discount-applied-label` testids via `page.goto('/pos')`'s OLD New-Tab flow — confirmed this session that `PaymentModal` is still live, still has these exact testids (`src/widgets/PaymentModal/PaymentModal.test.tsx`), and is now mounted inside `PaymentPane` (reached via `/payments`, not `/pos`); `D2` (switching scope to "Pool Only") and `D5` (active promotions banner) are genuinely dead bar-pos concepts — no `pool_only` discount scope or promotions banner exists in the current app)
    src/widgets/PaymentPane/ui/PaymentPane.tsx (confirms how `PaymentModal` is currently reached — the unlock/select-tab-then-pay flow)
    e2e/payments/payment-pane.spec.ts (Plan 17-05's output — the established current interaction pattern for reaching a payable tab via `/payments`, reuse this navigation approach rather than inventing a new one)
  </read_first>
  <action>
    Delete `D2: switching scope to Pool Only changes the active button` and `D5: active promotions banner present on POS...` entirely (per D-08 — genuinely dead).
    Rewrite `D1`, `D3`, `D4` to reach `PaymentModal` via `/payments` → `PaymentPane` (per Plan 17-05's established pattern) instead of the deleted `/pos` New-Tab flow, preserving each test's original discount-UI assertion (`discount-section` visible, Fixed $5 → `discount-applied-label` shows "5.00", 10% percent → `discount-applied-label` + `discount-row` with a negative sign). Un-skip these 3 (`test.skip` → `test`).
    Move the result to `e2e/reports/discount-and-revenue.spec.ts`, fixing imports and swapping fixture product names.
    Delete `e2e/20-sprint2-revenue.spec.ts`.
  </action>
  <verify>
    <automated>npx playwright test e2e/reports/discount-and-revenue.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -icE "pool.only|promotion" e2e/reports/discount-and-revenue.spec.ts` returns 0
    - `grep -c "test.skip(" e2e/reports/discount-and-revenue.spec.ts` returns 0
    - `grep -c "'/pos'" e2e/reports/discount-and-revenue.spec.ts` returns 0 (D1/D3/D4 now reach PaymentModal via /payments)
    - `test -f e2e/20-sprint2-revenue.spec.ts` fails
    - `npx playwright test e2e/reports/discount-and-revenue.spec.ts` passes with 0 failures
  </acceptance_criteria>
  <done>D1/D3/D4 pass for real against PaymentModal via the current /payments path; D2/D5 removed as genuinely dead.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Reports (read-only) | Per D-10, reports/list-view flows stay UI-only — no new DB assertions needed in this domain |
| Browser → discount application in PaymentModal | Discount fields feed `payments.discount_amount/discount_scope/discount_type/discount_value` — D1/D3/D4's resurrection closes a real coverage gap on this write path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-17-14 | Tampering | 1557-line file test-count preservation | high | mitigate | Task 1's acceptance criteria explicitly requires a `test(` count check against the original file, specifically to catch an accidental silent test drop during the largest single rewrite in this phase. |
| T-17-15 | Repudiation | Discount application (D1/D3/D4) | medium | mitigate | Previously permanently skipped, now real coverage of a real payment-record-mutating UI path. |

</threat_model>

<verification>
- `npx playwright test e2e/reports/` passes with 0 failures.
- Task 1's test-count-preservation check passes.
- `VERIFICATION_REPORT.md` shows a `Reports` row with a nonzero total close to the pre-rewrite count.
</verification>

<success_criteria>
`e2e/reports/` is fully rewritten, all previously-working tests preserved, discount coverage resurrected against the current UI, dead pool/promotions tests removed.
</success_criteria>

<artifacts_produced>
- `e2e/reports/report-tabs.spec.ts`, `product-sales.spec.ts`, `export.spec.ts`, `discount-and-revenue.spec.ts` (new)
- `e2e/07-reports.spec.ts`, `19-product-sales-report.spec.ts`, `25-export-reports.spec.ts`, `20-sprint2-revenue.spec.ts` (deleted)
</artifacts_produced>

<output>
Create `.planning/phases/17-e2e-suite-overhaul/17-09-SUMMARY.md` when done
</output>
