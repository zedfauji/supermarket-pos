---
phase: 17-e2e-suite-overhaul
plan: 14
type: execute
wave: 3
depends_on: ["17-04"]
files_modified:
  - e2e/home/home-navigation.spec.ts
  - e2e/a11y/focus-tab-order.spec.ts
  - e2e/errors/error-scenarios-and-validation.spec.ts
  - e2e/15-home-navigation.spec.ts
  - e2e/44-focus-tab-order.spec.ts
  - e2e/20-error-scenarios.spec.ts
  - e2e/26-field-validation.spec.ts
autonomous: true
requirements: [TEST-01, TEST-02]

estimate:
  tokens: 21000
  raw_tokens: 42000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "15-home-navigation.spec.ts's removed-route redirect test no longer names specific bar-pos routes as fixture data"
    - "44-focus-tab-order.spec.ts's permanently-gone Surface (a) is either deleted or repointed at a live PIN-gated flow, not left as a dead test.skip"
    - "20-error-scenarios.spec.ts's ER1 (dead pool-session premise) is deleted; ER3/ER4/ER6 are rewritten against current UI or confirmed redundant with existing coverage"
    - "26-field-validation.spec.ts's FV1-FV5 (tab-name/order-notes validation) are retargeted to a real current equivalent field or confirmed-and-documented as obsolete if direct-sale checkout genuinely has no such field"
  artifacts:
    - e2e/home/home-navigation.spec.ts
    - e2e/a11y/focus-tab-order.spec.ts
    - e2e/errors/error-scenarios-and-validation.spec.ts
---

<objective>
Rewrite 4 cross-cutting files: `15-home-navigation.spec.ts` (genericize a bar-pos-route fixture list), `44-focus-tab-order.spec.ts` (resolve its own already-documented dead Surface (a)), and `20-error-scenarios.spec.ts` + `26-field-validation.spec.ts` (both carry the same "Bucket B — /pos deleted, no stub until Phase 2" staleness this phase has already resolved several times over — ER3/ER4/ER6 and FV1-FV5 need the same current-UI-retarget-or-confirmed-obsolete treatment).

Purpose: close out the last of this phase's `/pos`-deletion-era skipped tests in one pass, since all 4 files share the same root cause and resolution pattern already proven in Plans 17-05/17-07/17-09/17-11/17-12.
Output: `e2e/home/home-navigation.spec.ts`, `e2e/a11y/focus-tab-order.spec.ts`, `e2e/errors/error-scenarios-and-validation.spec.ts`; 4 original root files deleted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/17-e2e-suite-overhaul/17-CONTEXT.md
@.planning/phases/17-e2e-suite-overhaul/17-RESEARCH.md
@.planning/phases/17-e2e-suite-overhaul/17-PATTERNS.md
@.planning/phases/17-e2e-suite-overhaul/17-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e/home/home-navigation.spec.ts — genericize the removed-route fixture list</name>
  <files>e2e/home/home-navigation.spec.ts, e2e/15-home-navigation.spec.ts</files>
  <read_first>
    e2e/15-home-navigation.spec.ts (full file, 198 lines — confirmed this session: `removedPaths = ['/pos', '/pool-tables', '/pool-tables/abc123', '/kds', '/kds-bar', '/kitchen-prep', '/waitlist', '/rappi']` is used to parametrize `visiting removed route {path} redirects to /home with the dashboard rendered` — CRITICAL: `/pos` is itself in this list, but `/pos` is NOT removed anymore (direct-sale checkout lives there) — this entry is now factually wrong, not just stale-named, and must be dropped or the test will fail)
  </read_first>
  <action>
    Remove `/pos` from `removedPaths` (it is a real, live route). Replace the remaining bar-pos-specific route strings (`/pool-tables`, `/pool-tables/abc123`, `/kds`, `/kds-bar`, `/kitchen-prep`, `/waitlist`, `/rappi`) with generic nonexistent-path literals (e.g. `/nonexistent-page-xyz`, `/some-random-path-123`) that still prove the router's catch-all redirect-to-`/home` behavior, without perpetuating specific bar-pos route names as fixture data in a rewritten suite that's supposed to have zero such references. Keep at least 2-3 generic paths for parametrized coverage breadth.
    Move the result to `e2e/home/home-navigation.spec.ts`, fixing imports.
    Delete `e2e/15-home-navigation.spec.ts`.
  </action>
  <verify>
    <automated>npx playwright test e2e/home/home-navigation.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -icE "pool-tables|/kds|/kitchen-prep|/waitlist|/rappi" e2e/home/home-navigation.spec.ts` returns 0
    - `grep -c "'/pos'" e2e/home/home-navigation.spec.ts` returns 0 (removed from the removed-routes list — it's a live route)
    - `test -f e2e/15-home-navigation.spec.ts` fails
    - `npx playwright test e2e/home/home-navigation.spec.ts` passes with 0 failures
  </acceptance_criteria>
  <done>The redirect-catch-all test still proves real router behavior with generic nonexistent paths, with zero bar-pos route names and no factually-wrong `/pos` entry.</done>
</task>

<task type="auto">
  <name>Task 2: e2e/a11y/focus-tab-order.spec.ts — resolve Surface (a)</name>
  <files>e2e/a11y/focus-tab-order.spec.ts, e2e/44-focus-tab-order.spec.ts</files>
  <read_first>
    e2e/44-focus-tab-order.spec.ts (full file, 119 lines — confirmed this session: Surface (a)'s own extensive header comment already concludes pool-tables are "explicitly out of scope forever" and explicitly notes ManagerPinDialog's general Tab-order behavior "is not orphaned... still exercised by several live PIN-gated flows (RefundSheet, VoidOpenUnitDialog, ReopenTabDialog, EditPaidTabDialog, etc.)" and that "re-pointing this surface at one of those is... left to whichever future plan wants that coverage" — this plan is that future plan; note `VoidOpenUnitDialog` may itself be void-order-adjacent (Phase 5 removed void-order end-to-end) — verify it still exists before choosing it as the retarget)
  </read_first>
  <action>
    Delete Surface (a)'s `test.skip('A: ManagerPinDialog Tab order follows the visual keypad layout...')` block and its permanently-gone framing. Add a new test exercising `ManagerPinDialog`'s Tab order (1-9, 0, Backspace, Cancel — same assertions the deleted test intended) via ONE of the still-live PIN-gated flows the file's own comment names (`RefundSheet`, `ReopenTabDialog`, or `EditPaidTabDialog` — prefer whichever is simplest to reach in a beforeEach-light way; confirm `VoidOpenUnitDialog` still exists before considering it). Keep Surfaces (b) and (c) unchanged (already live and passing).
    Move the result to `e2e/a11y/focus-tab-order.spec.ts`, fixing imports.
    Delete `e2e/44-focus-tab-order.spec.ts`.
  </action>
  <verify>
    <automated>npx playwright test e2e/a11y/focus-tab-order.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test.skip('A:" e2e/a11y/focus-tab-order.spec.ts` returns 0
    - `grep -c "ManagerPinDialog" e2e/a11y/focus-tab-order.spec.ts` returns 1 or more (Tab-order coverage restored via a live surface)
    - `test -f e2e/44-focus-tab-order.spec.ts` fails
    - `npx playwright test e2e/a11y/focus-tab-order.spec.ts` passes with 0 failures
  </acceptance_criteria>
  <done>ManagerPinDialog Tab-order coverage restored via a live PIN-gated flow, closing the gap the file's own comment explicitly left open.</done>
</task>

<task type="auto">
  <name>Task 3: e2e/errors/error-scenarios-and-validation.spec.ts — merge and resolve remaining Bucket-B skips</name>
  <files>e2e/errors/error-scenarios-and-validation.spec.ts, e2e/20-error-scenarios.spec.ts, e2e/26-field-validation.spec.ts</files>
  <read_first>
    e2e/20-error-scenarios.spec.ts (full file, 335 lines — confirmed this session: `ER1: paying tab with active pool session shows error` targets the fully-dropped pool-session domain (delete outright); `ER3: place order without open caja`, `ER4: adding item to a paid tab`, `ER6: ordering out-of-stock product` are Bucket-B, permanently skipped against the old `/pos` New-Tab flow — before rewriting, check whether `e2e/checkout/atomic-rpc-guards.spec.ts`'s `rejects a sale against a caja session that is not open` (Plan 17-04) already covers ER3's RPC-level intent; `ER7`/`ER8` are already real/passing, preserve verbatim)
    e2e/26-field-validation.spec.ts (full file, 306 lines — confirmed this session: `FV1`-`FV3` target the deleted Open Tab dialog's customer-name field validation, `FV4`-`FV5` target the deleted dialog's order-notes field validation — investigate whether the current direct-sale checkout flow has ANY equivalent user-typed customer-name or order-notes field (check `entities/product/model/queries.ts`/checkout components); `FV6`-`FV9` are already real (FV6 has an already-legitimate, already-documented runtime skip for a known `MoneyInput` coercion gap — leave as-is)
    e2e/checkout/happy-path.spec.ts, e2e/checkout/atomic-rpc-guards.spec.ts (Plan 17-04's output)
  </read_first>
  <action>
    Delete `ER1` entirely (per D-08 — pool-session domain fully dropped).
    For `ER3`: if `e2e/checkout/atomic-rpc-guards.spec.ts`'s existing `rejects a sale against a caja session that is not open` already covers the same underlying guard, delete `ER3` as redundant (note this in the SUMMARY); otherwise rewrite it against the current UI (attempting to complete a sale with no open caja should surface a blocking UI state or `CAJA_CLOSED` error).
    For `ER4` and `ER6`: rewrite against the current `/pos` scan/cart UI (per `e2e/checkout/happy-path.spec.ts`'s pattern) — `ER4`'s real current equivalent is likely "editing/adding to an already-completed sale is not possible from the checkout screen, only via the dedicated edit-paid-tab flow" (may end up redundant with `e2e/tabs/edit-paid-tab.spec.ts`'s RBAC gate — if so, delete as redundant and document); `ER6` (out-of-stock product shows an error/is blocked when added to cart) is a live, valuable check — rewrite it directly.
    For `FV1`-`FV5`: determine whether the current direct-sale checkout flow has an equivalent user-typed customer-name or order-notes field. If yes, retarget the length/empty-value validation assertions at that real field. If no such field exists in the current flow at all (a real possibility — direct-sale checkout may not need a customer name for an anonymous walk-in sale), delete these 5 tests with a one-line documented reason (not a silent skip) rather than forcing a retarget onto a feature that doesn't exist.
    Merge everything into `e2e/errors/error-scenarios-and-validation.spec.ts` (two `test.describe` blocks, one per original file's remaining content, keeping `ER7`'s "No Caja" `test.describe` structure intact since it depends on a different `beforeEach`), fixing imports and fixture names.
    Delete both original root files.
  </action>
  <verify>
    <automated>npx playwright test e2e/errors/error-scenarios-and-validation.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -icE "pool_tables|pool-tables|pool session" e2e/errors/error-scenarios-and-validation.spec.ts` returns 0
    - `grep -c "test.skip('" e2e/errors/error-scenarios-and-validation.spec.ts` returns 0 (FV6's legitimate runtime-conditional skip and ER8's legitimate "not an E2E concern" runtime skip are the DIFFERENT `test('title', () => test.skip(...))` form and are not what this gate targets)
    - The plan SUMMARY documents each of ER3/ER4/FV1-FV5's disposition (rewritten vs. confirmed-redundant-and-deleted vs. confirmed-obsolete-and-deleted)
    - `test -f e2e/20-error-scenarios.spec.ts` fails, `test -f e2e/26-field-validation.spec.ts` fails
    - `npx playwright test e2e/errors/error-scenarios-and-validation.spec.ts` passes with 0 failures
  </acceptance_criteria>
  <done>All Bucket-B skips in both files resolved (rewritten, confirmed-redundant, or confirmed-obsolete — never left silently skipped), merged into one file, green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| CAJA_CLOSED / out-of-stock guards on the sale-creation path | Real business-rule boundaries this task restores coverage for |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-17-20 | Denial of Service (business logic) | Out-of-stock / closed-caja guards on checkout | medium | mitigate | ER3/ER6's resolution (rewrite or confirmed-redundant) ensures this domain's coverage is either real or explicitly known to be covered elsewhere — never silently absent. |

</threat_model>

<verification>
- `npx playwright test e2e/home/ e2e/a11y/ e2e/errors/` passes with 0 failures.
- Plan SUMMARY documents every disposition decision made in Task 3.
</verification>

<success_criteria>
4 files fully resolved: no bar-pos route names as fixture data, no dead Surface (a), no remaining Bucket-B skips left silently unresolved.
</success_criteria>

<artifacts_produced>
- `e2e/home/home-navigation.spec.ts`, `e2e/a11y/focus-tab-order.spec.ts`, `e2e/errors/error-scenarios-and-validation.spec.ts` (new)
- `e2e/15-home-navigation.spec.ts`, `44-focus-tab-order.spec.ts`, `20-error-scenarios.spec.ts`, `26-field-validation.spec.ts` (deleted)
</artifacts_produced>

<output>
Create `.planning/phases/17-e2e-suite-overhaul/17-14-SUMMARY.md` when done
</output>
