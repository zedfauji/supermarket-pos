---
phase: 17-e2e-suite-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seed-dev-data.ts
  - supabase/seed.sql
autonomous: true
requirements: [TEST-02]

estimate:
  tokens: 17500
  raw_tokens: 35000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Dev environment, manual testing, and E2E share one Indian-grocery product catalog (D-01)"
    - "The catalog covers every category from CLAUDE.md's product description — masalas, atta/rice/dals, snacks, pickles/papads, ghee/oil, tea/coffee, frozen, sweets, ready-to-eat — with 3-6 realistic SKUs each (D-02)"
    - "Every seeded product is a single-barcoded packaged unit (no case/piece or loose-weight items in this general catalog) (D-04)"
  artifacts:
    - scripts/seed-dev-data.ts (rewritten content, same idempotent-upsert shape)
    - supabase/seed.sql (rewritten content)
  key_links:
    - "scripts/seed-dev-data.ts upsertProduct()/ensureInventory() -> products/inventory tables consumed by every Wave 2+ e2e spec's Indian fixtures"
---

<objective>
Replace the current Spanish bar-food catalog (Budweiser/Corona/Margarita/pool-table `resources`) in `scripts/seed-dev-data.ts` and `supabase/seed.sql` with a realistic ~30-50 SKU Indian grocery catalog (D-01/D-02), so dev environment, manual testing, and every Wave 2+ E2E spec in this phase read from one source of truth. This is Wave-1 foundation work — nearly every later plan's Indian-fixture assertions depend on this catalog existing with predictable, greppable product/category names.

Purpose: eliminate the current mismatch between what the app actually sells (Indian grocery) and what dev/test data pretends it sells (bar food), and delete dead code that writes to a `resources` table dropped in Phase 1.
Output: `scripts/seed-dev-data.ts` and `supabase/seed.sql` rewritten with Indian grocery content; the `resources`/pool-table seeding block removed entirely from both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-e2e-suite-overhaul/17-CONTEXT.md
@.planning/phases/17-e2e-suite-overhaul/17-RESEARCH.md
@.planning/phases/17-e2e-suite-overhaul/17-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/seed-dev-data.ts — Indian grocery catalog (remote dev/E2E seed)</name>
  <files>scripts/seed-dev-data.ts</files>
  <read_first>
    scripts/seed-dev-data.ts (current full content — confirmed this session: the `ensureResource()` function at lines 143-166 writes to a `resources` table that was dropped in Phase 1 (`supabase/migrations/20260810000003_drop_pool_resources.sql`, confirmed by `e2e/44-focus-tab-order.spec.ts`'s header comment) — this script currently errors on that insert step and must have that whole block deleted, not just its call sites)
    src/shared/lib/supabase.types.ts — confirmed live column shapes: `categories` (name, color, sort_order, routing [`CategoryRoutingSchema`, default `'NONE'`], parent_id — `happy_hour_start`/`happy_hour_end` are DEPRECATED/always-null, do not set them), `products` (name, category_id, base_price, sku, is_active, sold_by_weight, barcode, stock_threshold, units_per_package, parent_product_id — `happy_hour_price` is DEPRECATED/always-null, do not set it), `inventory` (product_id, quantity_on_hand, low_stock_threshold, cost_price, expiry_date, unit)
    .planning/PROJECT.md — product category list (masalas, atta/rice/dals, snacks, pickles/papads, ghee/oil, tea/coffee, frozen items, ready-to-eat, sweets)
  </read_first>
  <action>
    Delete the `ensureResource()` function (current lines ~143-166) and its 5 call sites (current lines ~236-241, "Resources (pool tables)..." section) entirely — the `resources` table does not exist in the live schema.
    Replace the `upsertCategory`/`upsertProduct`/`ensureInventory` call data with a 9-category Indian grocery catalog, 3-6 SKUs per category (27-54 products total), covering: Masalas, Atta/Rice/Dals, Snacks, Pickles/Papads, Ghee/Oil, Tea/Coffee, Frozen, Ready-to-Eat, Sweets. Use realistic brand-style product names and 13-digit numeric barcode literals (GS1-India-shaped, prefix `890`, e.g. `'8901030800001'`, incrementing per product — plain literals only, do NOT compute an EAN-13 check digit, per RESEARCH.md's Don't-Hand-Roll finding). Representative anchor SKUs to follow (author the remainder in the same style/quantity): Masalas — "MDH Garam Masala 100g", "Everest Chana Masala 100g", "Shan Biryani Masala 50g"; Atta/Rice/Dals — "Aashirvaad Atta 5kg", "India Gate Basmati Rice 5kg", "Toor Dal 1kg"; Snacks — "Haldiram's Aloo Bhujia 200g", "Parle-G Biscuits 200g"; Pickles/Papads — "Mother's Recipe Mango Pickle 400g", "Lijjat Papad 200g"; Ghee/Oil — "Amul Pure Ghee 500ml", "Fortune Sunflower Oil 1L"; Tea/Coffee — "Tata Tea Gold 250g", "Bru Instant Coffee 100g"; Frozen — "McCain Frozen Peas 500g", "Godrej Yummiez Frozen Paratha 400g"; Ready-to-Eat — "MTR Poha 80g", "Haldiram's Ready to Eat Rajma 285g"; Sweets — "Haldiram's Soan Papdi 250g", "Bikaji Gulab Jamun 1kg".
    Set `sold_by_weight: false` and leave `units_per_package`/`parent_product_id` null for every product in this general catalog (D-04 — packaged goods only, no case/piece or loose-weight items here). Set `is_active: true` on all. Set realistic `base_price` values per category (e.g. masala pouches ~45-120, atta 5kg ~280-350, rice 5kg ~450-600, snacks ~35-60, pickles ~90-150, ghee 500ml ~280-350, tea/coffee 250g/100g ~90-140, frozen ~120-180, ready-to-eat ~40-90, sweets ~150-300 — these are plain numeric `base_price` values; currency/locale formatting is unaffected per D-03).
    Replace `ensureInventory()` call data with realistic `quantity_on_hand` (20-150), `low_stock_threshold` (10-20), plus new arguments/fields for `cost_price` (70-85% of `base_price`) and `expiry_date` (compute `new Date(Date.now() + N * 24 * 60 * 60 * 1000).toISOString().slice(0, 10)` with `N` varying 60-365 days per product so near-expiry-alert tests (`e2e/inventory/near-expiry-alerts.spec.ts`, planned in a later wave) have some products inside the 14-day alert window and most outside it — extend `ensureInventory()`'s signature to accept these two new fields, matching the existing "insert-only, never overwrite live stock" contract).
    Replace the minimal Modifiers section: keep `upsertModifier()`/`linkModifier()` as-is (mechanism, not content) but attach at most 1-2 illustrative modifiers (e.g., `{ name: 'Extra Spicy', price_delta: 0 }`) to a single snack/pickle product — packaged Indian grocery goods have no realistic per-item modifier use case beyond this thin smoke-level example (resolves RESEARCH.md Open Question 1).
    Update the file's header doc comment (currently describes bar-pos content and the "NEVER touches an existing resources row" pool-table caveat) to describe the Indian grocery catalog and remove the now-false `resources`/pool-table paragraph entirely (not just leave it stale).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "resources" scripts/seed-dev-data.ts` returns 0
    - `grep -c "ensureResource" scripts/seed-dev-data.ts` returns 0
    - `grep -c "happy_hour" scripts/seed-dev-data.ts` returns 0
    - Product count inserted is between 27 and 54 (`grep -c "await upsertProduct(" scripts/seed-dev-data.ts` in that range)
    - Category count is 9 (`grep -c "await upsertCategory(" scripts/seed-dev-data.ts` returns 9)
    - Every product name is a real Indian grocery item name, not a bar-food name (`grep -icE "budweiser|corona|margarita|mojito|whiskey|vodka shot|old fashioned" scripts/seed-dev-data.ts` returns 0)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>`scripts/seed-dev-data.ts` seeds a 9-category, 27-54-SKU Indian grocery catalog with realistic barcodes/prices/expiry dates/stock levels, contains zero `resources`/pool-table code, and remains idempotent (matches existing upsert-by-natural-key contract).</done>
  <reversibility rating="costly">Per CONTEXT.md D-01 — touches every spec that queries products by name/category, and any dev-environment manual-testing habits built around the old bar-food catalog. Already a locked, user-approved decision (not a fresh one-way choice requiring a new checkpoint) — git history fully preserves the prior content if ever needed.</reversibility>
</task>

<task type="auto">
  <name>Task 2: supabase/seed.sql — mirror catalog for local `supabase start` stack</name>
  <files>supabase/seed.sql</files>
  <read_first>
    supabase/seed.sql (current full content, 79 lines — confirmed this session: still uses the literal `'bartender'` role value and the same bar-food category/product/modifier rows as `seed-dev-data.ts`'s pre-rewrite content)
    scripts/seed-dev-data.ts (Task 1's output — this file's product/category/modifier content must mirror it, adjusted only for raw SQL `INSERT` syntax and fixed UUID literals in place of the TS upsert helpers)
    supabase/config.toml (`sql_paths = ["./seed.sql"]` confirms this file backs local `supabase start` only, separate from the remote seed Task 1 targets)
  </read_first>
  <action>
    Replace the `categories`/`products`/`modifiers` `INSERT` blocks with the same Indian grocery catalog authored in Task 1 (same names/prices/barcodes — SQL literal `INSERT ... VALUES` rows with fixed UUID literals in the same `'1000000X-0000-...'`/`'2000000X-0000-...'` numbering scheme already used in this file, extended to cover the full 27-54 product set). Do not change the `profiles` staff-seed block or the `role` enum value `'bartender'` in this file — this file's staff role literal is out of this phase's boundary (role rename is a completed prior-phase concern, not this rewrite's scope; confirm current schema still accepts `'bartender'` as a valid `role` enum value before assuming otherwise — if it does not, use the current valid literal from `src/shared/lib/rbac.ts`'s role union instead).
    Add `INSERT` rows for the `inventory` table (currently absent from this file entirely — confirmed by its 79-line length with no `inventory` block) mirroring Task 1's `quantity_on_hand`/`low_stock_threshold`/`cost_price`/`expiry_date` per product, using `CURRENT_DATE + INTERVAL 'N days'` for `expiry_date` (SQL has no direct JS `Date.now()` equivalent — use the interval form).
  </action>
  <verify>
    <automated>grep -c "INSERT INTO public.products" supabase/seed.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -icE "budweiser|corona|margarita|mojito|whiskey shot|old fashioned" supabase/seed.sql` returns 0
    - `grep -c "INSERT INTO public.inventory" supabase/seed.sql` returns 1 or more (new block added)
    - Product names in this file's `INSERT` values match the names authored in Task 1's `scripts/seed-dev-data.ts` (same catalog, mirrored)
  </acceptance_criteria>
  <done>`supabase/seed.sql` mirrors the same Indian grocery catalog as `scripts/seed-dev-data.ts`, plus a new `inventory` seed block, so a local `supabase start` stack and the remote dev/E2E environment present identical fixture data.</done>
  <reversibility rating="costly">Same D-01 rationale as Task 1 — mirrored content, same already-locked decision.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Seed script → Postgres (service-role) | `scripts/seed-dev-data.ts` writes via `SUPABASE_SERVICE_ROLE_KEY` (already gated: file exits with an actionable error if the env var is missing, unchanged by this plan) |
| `supabase/seed.sql` → local-only Postgres | Runs only against a local `supabase start` stack per `supabase/config.toml`, never against the remote/production project |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-17-04 | Tampering | `scripts/seed-dev-data.ts`'s idempotent upsert contract | low | accept | Content changes only (bar-food → Indian grocery); the existing "match by natural key, never overwrite live `quantity_on_hand`/`resources` occupancy" safety contract is preserved for every field this plan touches — no new overwrite risk introduced. |
| T-17-05 | Information Disclosure | Realistic-looking barcodes/prices in fixture data | low | accept | Fixture-only synthetic data (13-digit literals, no real GS1 registration lookup performed or implied), no real-world product/vendor data exposed. |

</threat_model>

<verification>
- `npm run typecheck` exits 0 after Task 1.
- Both files contain zero bar-food product names and zero `resources`/pool-table references.
- The full behavioral proof (seed data actually working end-to-end against real checkout/inventory flows) happens in Wave 2's tracer plan (17-04), which is the first plan to run a spec against this catalog.
</verification>

<success_criteria>
`scripts/seed-dev-data.ts` and `supabase/seed.sql` both seed the same ~30-50 SKU, 9-category Indian grocery catalog with realistic barcodes/prices/stock/expiry data, contain zero bar-food or dropped-`resources`-table references, and remain idempotent.
</success_criteria>

<artifacts_produced>
- `scripts/seed-dev-data.ts`: rewritten catalog content, `ensureResource()` and its call sites removed, `ensureInventory()` signature extended with `cost_price`/`expiry_date` parameters
- `supabase/seed.sql`: rewritten `categories`/`products`/`modifiers` `INSERT` content, new `inventory` `INSERT` block
</artifacts_produced>

<output>
Create `.planning/phases/17-e2e-suite-overhaul/17-02-SUMMARY.md` when done
</output>
