---
phase: 17-e2e-suite-overhaul
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/helpers/supabase.ts
  - e2e/helpers/rls-clients.ts
  - e2e/global-teardown.ts
autonomous: true
requirements: [TEST-01, TEST-02]

estimate:
  tokens: 19000
  raw_tokens: 38000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Zero bar-pos-domain references remain in e2e/helpers/supabase.ts (TEST-01)"
    - "`resetTestState()` — called in every spec's `beforeEach` — no longer queries tables dropped in Phase 1 (`resources`, `pool_sessions`, `ingredients`, `recipe_items`, `prep_productions`) or a nonexistent column (`order_items.kds_status`)"
    - "A representative RLS-boundary check can sign in as a specific role and assert denial without ever using the service-role client for that assertion (D-13)"
    - "VERIFICATION_REPORT.md's per-domain breakdown reflects the new folder taxonomy, not a collapsed 'Other' bucket (Pitfall 1)"
  artifacts:
    - e2e/helpers/rls-clients.ts
  key_links:
    - "e2e/global-teardown.ts classify() -> folder path segment (no more filename-regex SUITE_MAP)"
    - "e2e/helpers/rls-clients.ts createRoleScopedClient() -> db.auth.admin.createUser() + signInWithPassword() (mirrors src/entities/purchase-order/model/purchase-orders-rls.integration.test.ts)"
---

<objective>
Clean out the dead bar-pos-era code hiding inside `e2e/helpers/supabase.ts` — most critically `resetTestState()`, which runs in nearly every spec's `beforeEach` and currently queries at least four dropped tables/columns — add the shared RLS-boundary role-scoped-client helper D-13 needs, and rewrite `global-teardown.ts`'s suite classification from a 26-entry filename regex map to folder-path-based classification, so the D-06 folder restructure (starting Wave 2) doesn't silently collapse every rewritten spec's results into an undifferentiated "Other" bucket.

Purpose: `e2e/helpers/supabase.ts` is the single most load-bearing file in the whole suite (imported by nearly every spec) — get its dead-table/dead-column removal right once, here, in Wave 1, before any spec that depends on it is rewritten.
Output: `e2e/helpers/supabase.ts` with dead functions/blocks removed; new `e2e/helpers/rls-clients.ts`; `e2e/global-teardown.ts` classifying by folder path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/17-e2e-suite-overhaul/17-CONTEXT.md
@.planning/phases/17-e2e-suite-overhaul/17-RESEARCH.md
@.planning/phases/17-e2e-suite-overhaul/17-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e/helpers/supabase.ts — strip dead bar-pos code (all confirmed this session)</name>
  <files>e2e/helpers/supabase.ts</files>
  <read_first>
    e2e/helpers/supabase.ts — READ THE ENTIRE FILE (~900 lines) before editing; this task's find list below was confirmed by a full read this planning session, but exact line numbers will have shifted by the time you edit
    src/shared/lib/rbac.ts — confirms the current role enum (`cashier < manager < admin`, plus non-hierarchical `kitchen`) if any role-literal audit is needed
    CLAUDE.md — "Bar/pool-parlour-era tables... were dropped in Phase 1" list (confirms `pool_sessions`, `resources`/`pool_tables`, `ingredients`, `recipe_items`, `prep_productions` no longer exist) and "there is no `kds_status` column" fact from 17-RESEARCH.md Pitfall 3
  </read_first>
  <action>
    In `resetTestState()`: delete the `resources` update block (the "pool_tables was renamed to `resources`..." comment + query), delete the `pool_sessions` select/`bumpVersionedRows` block, delete the `order_items.kds_status` reset block, delete the entire `ingredients`/`prep_productions`/`recipe_items` E2E-fixture cleanup block (the "Prep productions reference ingredients..." through "Clean up E2E test ingredients..." comment section). Change the hardcoded `.eq('name', 'Budweiser')`/`.in('name', ['Budweiser', 'Corona'])` `sold_by_weight`/`quantity_on_hand` reset blocks to apply unconditionally to every product (no `.eq('name', ...)`/`.in('name', ...)` filter) — remove the coupling to specific bar-food product names entirely rather than swapping in specific Indian product names, since which product(s) later waves' loose-weight/barcode specs use is not yet fixed at this point in the phase.
    Delete these whole functions in full: `getPoolSessionStartedAt`, `getLatestStoppedPoolChargeForTab`, `getOccupiedPoolTableIds`, `seedRappiOrder`, `seedKdsFoodOrder`, `getKdsItemStatus`, `resetPrepIngredientStock` (the last one's own doc comment already confirms it was written for the already-deleted `e2e/21-prep.spec.ts` — fully orphaned).
    In `bumpVersionedRows()`'s `table` parameter union type, remove `'pool_sessions'` — leave `'tabs' | 'caja_sessions'`.
    In `seedOpenTab()`, change `const productName = opts.productName ?? 'Budweiser';` so `productName` is a required parameter (remove the optional `?`/default fallback on `opts.productName` in the function's parameter type and this line) — every caller must now pass an explicit product name. Do NOT go update spec-file call sites in this task; that happens naturally as each spec is rewritten in a later wave (a caller passing no `productName` will get a TypeScript error, which is the intended forcing function).
    Leave `getLatestInventoryLog()` untouched — confirmed this session it already queries the correct `stock_movements` table (Pitfall 2 does not apply here; this one function was already fixed in a prior phase, only its name is confusing).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -icE "resources|pool_sessions|prep_productions|recipe_items|kds_status|rappi_orders" e2e/helpers/supabase.ts` returns 0
    - `grep -c "function getPoolSessionStartedAt\|function getLatestStoppedPoolChargeForTab\|function getOccupiedPoolTableIds\|function seedRappiOrder\|function seedKdsFoodOrder\|function getKdsItemStatus\|function resetPrepIngredientStock" e2e/helpers/supabase.ts` returns 0
    - `grep -c "'Budweiser'\|'Corona'" e2e/helpers/supabase.ts` returns 0
    - `grep -c "from('stock_movements')" e2e/helpers/supabase.ts` returns 1 or more (confirms `getLatestInventoryLog` survived untouched)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>`e2e/helpers/supabase.ts` contains zero references to any table/column dropped in Phase 1, `resetTestState()` no longer breaks against the live schema, and `seedOpenTab()` no longer defaults to a bar-food product name.</done>
  <reversibility rating="costly">Every spec in the suite transitively depends on `resetTestState()` — a mistake here breaks every spec's `beforeEach` at once. Rated costly, not one-way (git revert fully restores); this is mechanical dead-code removal implementing already-locked D-08's spirit for the shared helper file, not a fresh judgment call needing a new checkpoint.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: e2e/helpers/rls-clients.ts — role-scoped client helper (D-13)</name>
  <files>e2e/helpers/rls-clients.ts</files>
  <read_first>
    src/entities/purchase-order/model/purchase-orders-rls.integration.test.ts (lines 1-119, full temp-user-creation + `signInWithPassword` + distinct-`storageKey` pattern this helper must mirror — Vitest, not Playwright, but the client-construction shape is directly reusable)
    e2e/helpers/supabase.ts — `getUrl()`/`getServiceKey()`/`getServiceClient()` pattern (env var reads to reuse, not duplicate)
    17-RESEARCH.md — "Pattern 2: RLS-boundary check via role-scoped client (D-13)" and Security Domain's "Known Threat Patterns" table
  </read_first>
  <behavior>
    - `createRoleScopedClient(role: 'cashier' | 'manager' | 'admin' | 'kitchen', label: string): Promise<{ client: SupabaseClient; userId: string; cleanup: () => Promise<void> }>` — creates a temporary Supabase Auth user (via `getServiceClient().auth.admin.createUser()`, unique email `__e2e_rls_${label}_${Date.now()}@test.local`, a fixed test password), upserts a matching `profiles` row with the given `role` and a distinct 6-digit `pin`, signs in a fresh anon-key `SupabaseClient` (constructed with `auth: { persistSession: false, autoRefreshToken: false, storageKey: 'e2e-rls-' + label + '-' + Date.now() }`) via `signInWithPassword`, and returns that signed-in client plus a `cleanup()` closure that deletes the temp auth user (`getServiceClient().auth.admin.deleteUser(userId)`) — profile row cascade-deletes with it (mirror the FK-cleanup ordering already documented in `deleteTestStaff()` if the auth user delete alone does not cascade the profile).
    - Throws a descriptive `Error` if user creation, profile upsert, or sign-in fails at any step (fail loud, not silent).
    - `VITE_SUPABASE_ANON_KEY` is read from `process.env` (new env dependency for this file only — mirror `getUrl()`'s throw-if-missing pattern for it).
  </behavior>
  <action>
    Create `e2e/helpers/rls-clients.ts` exporting `createRoleScopedClient` as described in `<behavior>`, importing `getServiceClient` from `./supabase` for the admin-side operations (never duplicate the service-role client construction). This is the ONLY sanctioned way any Wave 2+ spec constructs a role-scoped, anon-key, signed-in client for a "should be denied" RLS assertion — never `getServiceClient()` for that assertion (Security Domain finding: using the service-role client there would produce a false-negative pass, since RLS is bypassed entirely).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/helpers/rls-clients.ts` exports `createRoleScopedClient`
    - `grep -c "getServiceClient" e2e/helpers/rls-clients.ts` returns 1 or more (reused, not reimplemented)
    - `grep -c "storageKey" e2e/helpers/rls-clients.ts` returns 1 or more (distinct per-client storage key present, per the load-bearing requirement documented in PATTERNS.md)
    - `grep -c "SUPABASE_SERVICE_ROLE_KEY" e2e/helpers/rls-clients.ts` returns 0 (key handling stays inside `getServiceClient()`, not duplicated here)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>e2e/helpers/rls-clients.ts exists, typechecks, and exposes `createRoleScopedClient()` ready for Wave 3's `e2e/rbac/rls-boundary.spec.ts` (D-13) and any forced-failure RLS assertions in other Wave 3 plans to import.</done>
</task>

<task type="auto">
  <name>Task 3: e2e/global-teardown.ts — classify by folder path, not filename regex (Pitfall 1)</name>
  <files>e2e/global-teardown.ts</files>
  <read_first>
    e2e/global-teardown.ts (full current file, 236 lines — confirmed this session: `SUITE_MAP` is a 26-entry `{match: RegExp, label: string}[]` anchored on numeric-prefixed basenames like `/^02-caja/`, consumed by `classify()` via `path.basename(file)` matching, with `counts` pre-populated from `SUITE_MAP` before any results are read)
    17-RESEARCH.md — "Pitfall 1: global-teardown.ts's SUITE_MAP silently collapses after the D-06 rename" and "Don't Hand-Roll" table's "Suite/report grouping after D-06" row (group by folder path, not a maintained regex-to-label map)
  </read_first>
  <action>
    Delete the `SUITE_MAP` array entirely. Rewrite `classify(file: string): string` to derive the label directly from the spec file's folder path instead of a maintained list: normalize `file` to forward-slash separators, split on `/`, find the segment immediately after `e2e` (or after the results-file's own root prefix — inspect how `file`/`specFile` values are actually shaped in `e2e-results/results.json` before writing this, since Playwright's JSON reporter may emit either an absolute path or a path relative to `testDir`), and title-case that segment (`checkout` → `Checkout`, `purchase-orders` → `Purchase Orders`) as the label. Files with no folder segment before the basename (i.e. directly under `e2e/`, which should not occur for real specs after the D-06 restructure) fall back to `'Other'`; files under `e2e/visual/` map to `'Visual Regression'`.
    Since labels are no longer known upfront, change `counts` from being pre-populated by iterating `SUITE_MAP` to being populated lazily: in `onTest(file, status)`, call `classify(file)` and `counts.get(label) ?? counts.set(label, { pass: 0, fail: 0, skip: 0, total: 0 }).get(label)!` (create-on-first-use). Change the summary-table-printing loop (currently `for (const { label } of SUITE_MAP)`) to iterate `[...counts.keys()].sort()` instead, keeping the existing `'TOTAL'` row logic and the separate `'Other'`-only-if-nonzero handling (now just one more key in the sorted iteration, no special-cased block needed).
  </action>
  <verify>
    <automated>npx tsx e2e/global-teardown.ts 2>&1 | tail -5 || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SUITE_MAP" e2e/global-teardown.ts` returns 0
    - `grep -c "/^01-ci/\|/^02-caja/" e2e/global-teardown.ts` returns 0
    - `classify()`'s new implementation contains no numeric-prefix-anchored regex literals
    - Running teardown against a stale/missing `e2e-results/results.json` still produces the existing graceful fallback report (no crash) — verify by temporarily renaming or confirming absence of `e2e-results/results.json` before running the automated check
  </acceptance_criteria>
  <done>`e2e/global-teardown.ts` classifies test results by spec folder path, with no maintained filename-to-label map — Wave 2+'s folder-restructured specs will report into correctly-labeled buckets in `VERIFICATION_REPORT.md` instead of collapsing into "Other".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test harness → Postgres (service-role) | `resetTestState()` and every other `supabase.ts` helper bypass RLS via `SUPABASE_SERVICE_ROLE_KEY` — valid for setup/teardown, never for asserting an RLS denial |
| Test harness → Postgres (anon + signed-in, new in this plan) | `rls-clients.ts`'s `createRoleScopedClient()` is the only sanctioned client for a "should be denied" assertion — it carries a real role-scoped JWT, not service-role bypass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-17-01 | Elevation of Privilege (false negative) | `e2e/helpers/rls-clients.ts` and every future D-13 spec that imports it | high | mitigate | `createRoleScopedClient()` always signs in via `signInWithPassword` against the anon key — it never returns a service-role client, so a spec importing this helper cannot accidentally produce a false-negative "denied" assertion the way a raw `getServiceClient()` call would. |
| T-17-02 | Information Disclosure | Temp RLS-test auth users created by `createRoleScopedClient()` | medium | mitigate | Each call's `cleanup()` deletes the temp auth user via `getServiceClient().auth.admin.deleteUser()`; specs that import this helper are responsible for calling `cleanup()` in `afterEach`/`afterAll` (documented in the function's own JSDoc), same lifecycle discipline as the existing `deleteTestStaff()` pattern. |
| T-17-03 | Tampering | `resetTestState()`'s broadened (name-filter-removed) `sold_by_weight`/`quantity_on_hand` reset | low | accept | Removing the `.eq('name', 'Budweiser')` filter makes the reset apply store-wide instead of to two named products — this is intentionally MORE consistent (every product starts from the documented default each test run), not a new risk; no production data is touched, this only runs against E2E/dev Supabase projects gated by `requireIntegrationEnv()`. |

</threat_model>

<verification>
- `npm run typecheck` exits 0 after all three tasks.
- `grep` gates in each task's acceptance_criteria confirm zero dead-table/column references remain.
- Full behavioral proof (every spec's `beforeEach` actually working end-to-end against the live schema) happens starting with Wave 2's tracer plan (17-04) — this plan's own verification is necessarily static/typecheck-level, since `resetTestState()` is exercised for real only once specs run.
</verification>

<success_criteria>
`e2e/helpers/supabase.ts` no longer queries any table/column dropped in Phase 1. `e2e/helpers/rls-clients.ts` exists and provides the one sanctioned role-scoped-client construction path for D-13 checks. `e2e/global-teardown.ts` classifies results by folder path with no maintained filename map, ready for the D-06 restructure starting Wave 2.
</success_criteria>

<artifacts_produced>
- `e2e/helpers/supabase.ts`: 7 dead functions removed, `resetTestState()` and `seedOpenTab()` rewritten, `bumpVersionedRows()`'s table union narrowed
- `e2e/helpers/rls-clients.ts` (new file): exports `createRoleScopedClient(role, label)`
- `e2e/global-teardown.ts`: `SUITE_MAP` removed, `classify()` rewritten to folder-path-based, `counts` populated lazily
</artifacts_produced>

<output>
Create `.planning/phases/17-e2e-suite-overhaul/17-03-SUMMARY.md` when done
</output>
