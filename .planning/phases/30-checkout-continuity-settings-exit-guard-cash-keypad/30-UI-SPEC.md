---
phase: 30
slug: checkout-continuity-settings-exit-guard-cash-keypad
status: approved
shadcn_initialized: true
preset: radix-nova
created: 2026-09-08
reviewed_at: 2026-09-08T15:24:03.1572646-06:00
---

# Phase 30 — UI Design Contract

> Visual and interaction contract for Checkout continuity, guarded Settings exits, and cash
> tender entry. Generated by gsd-ui-researcher; verified by gsd-ui-checker.

---

## Design System

| Property          | Value                                                                                          |
| ----------------- | ---------------------------------------------------------------------------------------------- |
| Tool              | Existing shadcn configuration; reuse repository primitives only                                |
| Preset            | `radix-nova`, neutral base, subtle menu accent                                                 |
| Component library | Existing Radix-backed `AlertDialog`, `Popover`, `Tabs`, `ScrollArea`, and form primitives      |
| Icon library      | Lucide; `AlertTriangle`, `RefreshCw`, `Loader2`, `Delete`, `X`, and `ShoppingBag` where needed |
| Font              | Geist Variable; system sans-serif fallback                                                     |

Source: `components.json`, `npx shadcn info`, `src/app/globals.css`, and existing shared UI. No
component is installed or copied for this phase.

### Reuse Contract

| Need                            | Existing primitive/pattern                      | Contract                                                                                              |
| ------------------------------- | ----------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| Blocking decisions              | `@shared/ui/alert-dialog`                       | Reuse its overlay, focus trap, 24px padding, 16px radius, and 56px actions.                           |
| Status and failure messaging    | `@shared/ui/alert`                              | Use `warning`, `destructive`, `success`, or `info`; never encode state by color alone.                |
| Saved basket and receipt values | `ScrollArea`, `MoneyDisplay`, `Badge`           | Keep currency tabular and show full item identity and quantity.                                       |
| Tender entry                    | `MoneyInput`, `POSButton`, `@shared/ui/popover` | One controlled composite; screen and hardware input share one amount buffer.                          |
| Key layout                      | `PINKeypad`, `WeightEntryDialog`                | Reuse the three-column, large-target visual rhythm, not their global key handlers.                    |
| Settings exit                   | Existing Settings tabs plus `AlertDialog`       | One application-level dialog; individual forms continue to own values, validation, Save, and Discard. |

Do not add a second design system, a second modal primitive, or a new keypad dependency.

---

## Spacing Scale

Declared values (all multiples of 4):

| Token | Value | Phase usage                                                  |
| ----- | ----- | ------------------------------------------------------------ |
| xs    | 4px   | Icon-to-label gaps and compact status metadata               |
| sm    | 8px   | Key gaps, inline actions, and basket-row internal spacing    |
| md    | 16px  | Default field, card, and section spacing                     |
| lg    | 24px  | Dialog padding and separation between major dialog regions   |
| xl    | 32px  | Compact-viewport edge allowance and large content separation |
| 2xl   | 48px  | Recovery illustration/status breathing room only             |
| 3xl   | 64px  | Maximum page-level separation; do not use inside dialogs     |

Exceptions: interactive dimensions follow the existing POS accessibility contract rather than the
spacing scale: 44px minimum target, 56px keypad and dialog actions, and 72px existing primary Pay
action. Keypad width is 320px maximum and always keeps a 16px viewport gutter.

### Surface Measurements

- Resume-sale dialog: 24px padding, 24px major gap, 8px basket-row gap, `max-width: 768px`, and
  `max-height: calc(100dvh - 32px)`.
- Payment-status recovery: 24px padding and `max-width: 576px`; receipt uses the existing receipt
  surface after reconciliation.
- Settings exit dialog: existing `max-width: 512px`, 24px padding, and 8px action gaps.
- Cash keypad popover: 12px outer padding, 8px key gaps, 56px minimum key height, 320px maximum
  width, and 4px anchor offset.

---

## Typography

Phase additions use exactly four sizes and two weights. Existing shared primitives may retain their
internal typography.

| Role                            | Size | Weight | Line Height |
| ------------------------------- | ---- | ------ | ----------- |
| Body                            | 14px | 400    | 1.5         |
| Eyebrow / compact label         | 12px | 600    | 1.25        |
| Dialog / section heading        | 18px | 600    | 1.2         |
| Tender / recovery total display | 36px | 600    | 1.0         |

- Currency, quantity, keypad digits, and status totals use tabular lining numerals via
  `text-numeric`/`tabular-nums`.
- Basket product names wrap to two lines in the restore dialog; they are never ellipsized there.
- References and translated status copy wrap naturally; opaque identifiers are never displayed.
- Uppercase 12px eyebrow labels use the established `0.12em` tracking. Body copy does not use
  letter spacing.

---

## Color

Use the existing semantic variables in both color schemes; do not introduce literal colors.

| Role            | Exact token and current values                                                                     | Usage                                                                                                    |
| --------------- | -------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| Dominant (60%)  | `--background`: light `oklch(0.975 0.004 250)`, dark `oklch(0.155 0.01 260)`                       | Page beneath overlays and Checkout background                                                            |
| Secondary (30%) | `--card` / `--popover`: light `oklch(1 0 0)`, dark `oklch(0.2 0.01 260)` / `oklch(0.215 0.01 260)` | Dialogs, basket rows, keypad, and Settings form surfaces                                                 |
| Accent (10%)    | `--brand`: light `oklch(0.55 0.12 195)`, dark `oklch(0.76 0.11 195)`                               | Resume Sale, Save and Continue, Retry Status Check, focused tender ring, and reconciliation spinner only |
| Warning         | `--warning` and `--warning-soft`                                                                   | Changed saved/current values and unresolved payment status                                               |
| Success         | `--success` and `--success-soft`                                                                   | Confirmed complete status and fully resolved conflict state only                                         |
| Destructive     | `--destructive` and `--destructive-soft`                                                           | Discard Sale, Discard Changes, Remove Item, and inline save/payment failures only                        |

Accent is reserved for: the Resume Sale action, successful-exit Save action, payment-status Retry,
cash-input focus ring, and active reconciliation indicator. Exact/quick-tender, Stay, and ordinary
keypad digits remain neutral outline controls. Every warning, error, and success also has an icon
and text label.

---

## Copywriting Contract

The English copy below is canonical for intent. Add matching keys to both `en-US` and `es-MX` locale
catalogs; never inline these strings in components.

| Element                  | Copy                                                                                                                            |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------- |
| Primary CTA              | **Resume Sale**                                                                                                                 |
| Empty state heading      | **Your cart is empty** (reuse existing Checkout copy)                                                                           |
| Empty state body         | **Scan or search for a product to add it here.** (reuse existing Checkout copy)                                                 |
| Recovery load            | **Checking payment status** — “We're confirming whether this payment completed before another charge can be attempted.”         |
| Recovery error           | **We couldn't confirm the payment status.** “Check the connection, then try again. Payment and cart controls will stay locked.” |
| Settings save error      | **Couldn't save changes.** “Check the highlighted fields or try again. Your changes are still here.”                            |
| Destructive confirmation | **Discard Sale** — “Remove this saved cart and Checkout details from this terminal? This can't be undone.”                      |

### Required Labels and Messages

| Surface  | Element             | English                                                                                               | Spanish (es-MX)                                                                                                 |
| -------- | ------------------- | ----------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| Resume   | Title               | Resume unfinished sale?                                                                               | ¿Reanudar la venta sin terminar?                                                                                |
| Resume   | Description         | This terminal has an unfinished sale from the current register session.                               | Esta terminal tiene una venta sin terminar de la sesión de caja actual.                                         |
| Resume   | Basket heading      | Saved basket                                                                                          | Carrito guardado                                                                                                |
| Resume   | Checkout heading    | Saved Checkout details                                                                                | Detalles de cobro guardados                                                                                     |
| Resume   | Primary action      | Resume Sale                                                                                           | Reanudar venta                                                                                                  |
| Resume   | Destructive action  | Discard Sale                                                                                          | Descartar venta                                                                                                 |
| Conflict | Heading             | Review restored sale                                                                                  | Revisar venta restaurada                                                                                        |
| Conflict | Guidance            | Some saved details changed. Review every highlighted item before payment.                             | Algunos datos guardados cambiaron. Revisa cada artículo marcado antes de cobrar.                                |
| Conflict | Value labels        | Saved / Current                                                                                       | Guardado / Actual                                                                                               |
| Conflict | Resolve action      | Accept Current Values                                                                                 | Aceptar valores actuales                                                                                        |
| Conflict | Unavailable         | Product unavailable — remove this item to continue.                                                   | Producto no disponible — quita este artículo para continuar.                                                    |
| Conflict | Remove action       | Remove Item                                                                                           | Quitar artículo                                                                                                 |
| Recovery | Loading title       | Checking payment status                                                                               | Verificando estado del pago                                                                                     |
| Recovery | Completed           | Payment confirmed                                                                                     | Pago confirmado                                                                                                 |
| Recovery | Incomplete          | Payment not completed                                                                                 | Pago no completado                                                                                              |
| Recovery | Incomplete guidance | Review the restored Checkout and press Pay when ready. The original payment reference will be reused. | Revisa el cobro restaurado y presiona Pagar cuando estés listo. Se reutilizará la referencia original del pago. |
| Recovery | Unknown title       | We couldn't confirm the payment status                                                                | No pudimos confirmar el estado del pago                                                                         |
| Recovery | Retry action        | Retry Status Check                                                                                    | Volver a verificar                                                                                              |
| Settings | Title               | Unsaved changes                                                                                       | Cambios sin guardar                                                                                             |
| Settings | Description         | Save or discard your changes before leaving this settings section.                                    | Guarda o descarta tus cambios antes de salir de esta sección de configuración.                                  |
| Settings | Save                | Save and Continue                                                                                     | Guardar y continuar                                                                                             |
| Settings | Saving              | Saving…                                                                                               | Guardando…                                                                                                      |
| Settings | Retry save          | Retry Save                                                                                            | Reintentar guardado                                                                                             |
| Settings | Discard             | Discard Changes                                                                                       | Descartar cambios                                                                                               |
| Settings | Stay                | Stay Here                                                                                             | Permanecer aquí                                                                                                 |
| Keypad   | Group name          | Cash amount keypad                                                                                    | Teclado de monto en efectivo                                                                                    |
| Keypad   | Clear               | Clear amount                                                                                          | Borrar monto                                                                                                    |
| Keypad   | Backspace           | Delete last digit                                                                                     | Borrar último dígito                                                                                            |
| Keypad   | Decimal             | Decimal point                                                                                         | Punto decimal                                                                                                   |
| Keypad   | Digit               | Key {{digit}}                                                                                         | Tecla {{digit}}                                                                                                 |

Do not expose the persisted idempotency key, internal recovery state names, raw backend errors,
PINs, or credentials in copy, logs, or accessible names.

---

## Interaction Contract

### 1. Saved Sale Handoff

1. After authentication and current-caja validation, a valid saved sale with Checkout open displays
   a modal `AlertDialog` before Checkout content is mounted or revealed.
2. The header shows the title and current caja/session context without naming the previous cashier.
   The body contains the full saved basket, saved total, payment method, tendered amount, split
   allocations, promotion/custom discount, and applicable customer/reference fields. Omit empty
   optional rows; never render secret fields.
3. Each basket row shows full product name, quantity or weight, saved unit price, and saved line
   total. The footer repeats the saved sale total. A long basket scrolls inside the body while the
   title and actions remain visible.
4. **Resume Sale** is the single brand action and receives initial focus. **Discard Sale** is
   destructive. Escape and outside click do nothing because the decision is mandatory; no close
   control is rendered.
5. Resume opens Checkout with the saved draft. Discard clears the active sale and draft, returns to
   the ordinary empty Checkout, and restores focus to product search.
6. A saved cart whose Checkout-open marker is absent restores directly to the selling screen without
   this dialog. A malformed, obsolete, partial, caja-mismatched, or empty envelope fails closed to
   the existing empty-cart state and never flashes persisted content.

### 2. Restored-Value Review

- After Resume, compare the saved snapshot with current product, price/promotion, and stock data.
  Checkout remains visible, but Pay and cart mutation controls stay disabled until comparison ends.
- If nothing changed, show a compact success/info notice, announce “Restored sale ready,” and enable
  normal editing and payment.
- Changed rows receive a warning border and a **Needs review** badge. A review panel presents Saved
  and Current values side by side, then **Accept Current Values** and **Remove Item**. Acceptance
  applies the current price/promotion/stock decision explicitly and clears that row's warning.
- If the product or inventory record is unavailable, show Current as **Unavailable** and offer only
  **Remove Item**. Never offer acceptance without an authoritative current value.
- Keep the existing Pay action disabled while any conflict remains. State and copy—not a manager
  bypass—explain why.

### 3. Submitted Payment Recovery

| State             | Visual contract                                                                                                                                                                   | Available actions                                                                      |
| ----------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| Reconciling       | Blocking centered status surface with spinner, saved basket summary, total, and `aria-live="polite"` copy. Cart and payment UI are inert and visually dimmed beneath the overlay. | App close only; no Escape, outside dismissal, edit, Pay, or Discard.                   |
| Completed         | Transition directly to the existing receipt view with a brief “Payment confirmed” live announcement.                                                                              | Existing Print, Email, PDF, and Done actions; never trigger Print automatically.       |
| Incomplete        | Restore Checkout fields exactly and show an info banner that the original payment reference will be reused.                                                                       | Normal review plus the existing explicit Pay action; no automatic retry.               |
| Unknown / offline | Replace the spinner with warning icon, actionable copy, and persistent saved summary.                                                                                             | **Retry Status Check** and app close only; no Pay, edit, Discard, or manager override. |

Retry keeps the same dialog and stable layout; it swaps the warning block for the spinner so focus
does not jump. Disable Retry and label it **Checking…** during the request. Backend details never
appear in the message.

### 4. Settings Exit Guard

1. Intercept dirty Settings tab changes, route navigation, sign-out, and Tauri close before any
   navigation/sign-out/close side effect occurs. Browser reload/window close uses only the native
   browser warning.
2. Show one `AlertDialog` titled **Unsaved changes** with exactly three actions:
   **Save and Continue** (brand), **Discard Changes** (destructive), and **Stay Here** (outline).
   Initial focus is **Stay Here**, the safest action.
3. Escape, outside click, and the 44px top-right close control all invoke **Stay Here**: cancel the
   pending exit, preserve values, and return focus to the control that attempted the exit.
4. Save changes its label to **Saving…**, adds a spinner, and disables all three actions until the
   owning mutation settles. Success closes the dialog and continues the original exit exactly once.
5. Failed validation or mutation leaves the dialog open, preserves every field, renders the
   destructive alert above the footer, focuses the first invalid field for validation failures or
   the alert for mutation failures, and changes Save to **Retry Save**.
6. Discard resets the mounted form, clears event-dirty state, and continues the original exit once.
   Stay cancels it. Repeated navigation/close attempts never replace the first pending target.
7. A form remains dirty after any edit until explicit Save or Discard, even when the current value
   equals its original value. Backup remains read-only/action-only and does not register a draft.

### 5. Cash Tender Keypad

- Use one controlled, non-modal `Popover` anchored to each cash **Amount tendered** input: the main
  cash field and each split-cash tendered field. Do not attach it to split allocation, discount,
  card, transfer, or reference fields.
- Focusing or activating the cash field opens the popover. Pointer use inside the popover keeps the
  input composite active. Escape or focus leaving the entire composite closes it and returns focus
  to the amount field when Escape initiated the close.
- Key order is the familiar three-column layout: `7 8 9`, `4 5 6`, `1 2 3`, then `Clear 0 .`, with
  Backspace spanning all three columns. Every key is at least 56px high. Clear and Backspace include
  text/accessibility labels; the backspace icon is decorative.
- Digits append as normal decimal text: `1`, `0`, `0` displays `100.00`; the decimal key starts or
  continues cents, permits only one decimal separator, and limits the fractional part to two digits.
  Empty/Clear normalizes to `0.00` without an error.
- Existing Exact and quick-tender controls remain directly below the field. Selecting one arms
  replacement: the next keypad or physical digit replaces the preset, then later digits append.
  Decimal, Backspace, or Clear edit the selected preset rather than submitting it.
- While the keypad is open and its amount input owns focus, top-row/numpad digits, decimal,
  Backspace, and Delete dispatch the same intents as pointer keys. Delete means Clear. Prevent Enter
  from submitting or activating Pay. Do not install a page-global keypad handler.
- During payment processing, the input, popover keys, Exact, and quick-tender actions are all
  disabled. An already-open popover remains visible but inert, preventing layout/focus jumps.
- The current amount is announced politely after pointer input but not duplicated for native input
  typing. The visual input remains the single accessible value field.

---

## Responsive and Motion Contract

- At 1024px and wider, keep the existing two-column PaymentForm. Position the keypad below and
  start-aligned to the tender input; Radix collision handling may flip it upward, but it must not
  cover the Pay action or saved-order summary.
- Below 640px, dialogs use a 16px viewport gutter, stack actions to full width, and constrain basket
  content to the remaining vertical space. The keypad stays a popover, never a separate modal or
  permanently visible panel, with width `min(320px, calc(100vw - 32px))`.
- Basket rows change from side-by-side saved/current columns to stacked labeled values below 480px.
  Action labels wrap to two lines without reducing target height.
- Reuse existing 150–250ms fade/scale transitions. State changes must remain understandable with
  `prefers-reduced-motion`; reduced motion removes translation/scale and retains opacity only.
- Do not animate totals, progress through recovery states, or the dismissal of destructive errors.

---

## Accessibility Contract

- Every custom dialog has a programmatic title and description. Mandatory recovery dialogs trap
  focus and cannot be dismissed to an unsafe state.
- On dialog close, return focus to the initiating Settings tab/link/control; after saved-sale
  Discard, focus product search; after Resume, focus the first actionable Checkout control.
- Reconciliation changes use `role="status"`/`aria-live="polite"`; save/recovery failures use
  `role="alert"`. Do not announce the continuously formatted amount twice.
- Icon-only controls have translated accessible names and at least 44px targets. All keypad buttons
  use `type="button"` and remain reachable in row-major Tab order.
- Warning, success, and destructive states combine semantic copy, icons, and existing tokens; color
  never carries meaning alone.
- Automated keyboard coverage must include Tab order, Escape behavior, Enter suppression, top-row
  and numpad digits, decimal, Backspace, Delete, Clear, quick-tender replacement, focus return, and
  disabled processing state in both locales.

---

## UI Considerations

> Populated by the ui-phase UI-consideration probe and lifted by plan-phase. Copy references the
> Copywriting Contract rather than duplicating source strings.

Applicable state considerations resolved: **32 covered, 0 backstop, 0 unresolved**.

| Category          | Element(s)            | Status     | Resolution / Reason                                                                                                                      |
| ----------------- | --------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| Empty             | Saved Sale Handoff    | ✅ covered | Empty, malformed, obsolete, caja-mismatched, or invalid saved sales render no recovery surface and fall back to the existing empty cart. |
| Loading           | Saved Sale Handoff    | ✅ covered | Persisted content stays hidden until authentication and current-caja validation finish; the dialog appears only after validation.        |
| Error             | Saved Sale Handoff    | ✅ covered | Validation or payload failure fails closed without flashing persisted content or exposing backend details.                               |
| Populated         | Saved Sale Handoff    | ✅ covered | Show every safe saved product, quantity or weight, unit price, line total, sale total, and applicable Checkout detail.                   |
| Partial           | Saved Sale Handoff    | ✅ covered | Omit absent optional Checkout rows; missing required sale data invalidates the whole envelope and fails closed.                          |
| Overflow          | Saved Sale Handoff    | ✅ covered | Long baskets scroll inside the body while the dialog title and actions remain visible within the viewport-height cap.                    |
| Zero / one / many | Saved Sale Handoff    | ✅ covered | Zero valid lines produces no recovery dialog; one and many lines use the same localized list, and long lists scroll.                     |
| Long text         | Saved Sale Handoff    | ✅ covered | Product names wrap to two lines; translated copy and references wrap safely; action labels may use two lines.                            |
| Empty             | Restored-Value Review | ✅ covered | Zero changed rows shows a compact ready notice and enables normal editing and payment.                                                   |
| Loading           | Restored-Value Review | ✅ covered | Checkout stays visible during comparison, but Pay and cart mutation controls remain disabled until it finishes.                          |
| Error             | Restored-Value Review | ✅ covered | Missing authoritative product or inventory data is shown as Unavailable and exposes only Remove Item.                                    |
| Populated         | Restored-Value Review | ✅ covered | Each conflict shows warning treatment, Saved and Current values, and the applicable accept or remove action.                             |
| Partial           | Restored-Value Review | ✅ covered | A row without an authoritative current value cannot be accepted and must be removed before Checkout continues.                           |
| Overflow          | Restored-Value Review | ✅ covered | Review content remains viewport-bound; saved/current columns stack below 480px, and long collections scroll.                             |
| Zero / one / many | Restored-Value Review | ✅ covered | Zero conflicts shows ready; one and many use the same review rows, with Pay disabled until every row resolves.                           |
| Long text         | Restored-Value Review | ✅ covered | Names, values, guidance, and translated actions wrap without hiding identity, meaning, or touch targets.                                 |
| Loading           | Payment Recovery      | ✅ covered | Reconciliation shows a stable blocking spinner/status surface with the saved summary while cart and payment controls are inert.          |
| Error             | Payment Recovery      | ✅ covered | Unknown or offline status shows the documented warning and Retry action while Pay, edit, and discard remain unavailable.                 |
| Overflow          | Payment Recovery      | ✅ covered | The recovery surface is viewport-constrained and its saved summary scrolls without displacing status or permitted actions.               |
| Long text         | Payment Recovery      | ✅ covered | Recovery messages and translations wrap naturally, actions may use two lines, and internal identifiers are never displayed.              |
| Empty             | Settings Exit Guard   | ✅ covered | A form with no registered dirty event shows no guard; Backup never registers a draft.                                                    |
| Loading           | Settings Exit Guard   | ✅ covered | Saving shows the documented in-flight label and spinner and disables all three actions until the mutation settles.                       |
| Error             | Settings Exit Guard   | ✅ covered | Validation or mutation failure keeps the dialog open, preserves values, shows the documented alert, and enables Retry Save.              |
| Partial           | Settings Exit Guard   | ✅ covered | Partially invalid forms preserve all fields and focus the first invalid field; mutation failures focus the alert.                        |
| Overflow          | Settings Exit Guard   | ✅ covered | Below 640px the dialog keeps a 16px viewport gutter, constrains content vertically, and stacks full-width actions.                       |
| Long text         | Settings Exit Guard   | ✅ covered | Translated descriptions, errors, and actions wrap naturally without reducing action target height.                                       |
| Empty             | Cash Tender Keypad    | ✅ covered | An empty or cleared tender buffer normalizes to `0.00` without showing an error.                                                         |
| Loading           | Cash Tender Keypad    | ✅ covered | During processing the open keypad stays visible but inert, and all tender input and shortcut controls are disabled.                      |
| Error             | Cash Tender Keypad    | ✅ covered | Extra decimal separators or fractional digits are rejected without a modal; validation stays in existing Checkout placement.             |
| Partial           | Cash Tender Keypad    | ✅ covered | The buffer permits one decimal separator and two fractional digits while preserving the valid prefix already entered.                    |
| Overflow          | Cash Tender Keypad    | ✅ covered | Radix collision handling keeps the keypad within the viewport gutter and away from Pay and the saved-order summary.                      |
| Long text         | Cash Tender Keypad    | ✅ covered | Visible labels may wrap; translated accessible names remain complete while targets retain their required height.                         |

---

## Registry Safety

| Registry        | Blocks Used                           | Safety Gate                                                               |
| --------------- | ------------------------------------- | ------------------------------------------------------------------------- |
| shadcn official | None; reuse existing local primitives | Configuration verified with `npx shadcn info` — 2026-09-08                |
| Third-party     | None                                  | `components.json` registries are empty — no vetting required — 2026-09-08 |

No registry source enters the phase. If implementation later proposes a registry block, stop and run
`shadcn view` plus the required network/environment/eval/dynamic-import review before changing this
contract.

---

## Automated Visual and Interaction Acceptance

- Headless Playwright proves saved-sale Resume/Discard, long-basket scrolling, per-line conflict
  review, unavailable-line removal, every payment-recovery outcome, and no automatic receipt print.
- Component tests prove the Settings dialog's three actions, initial/focus-return behavior,
  Escape/outside/close-as-Stay, disabled save state, failed-save alert, and one-shot continuation.
- Component and Playwright tests prove the keypad layout, pointer/keyboard parity, split-cash reuse,
  two-decimal behavior, preset replacement, Enter suppression, translated names, and processing lock.
- Assertions cover light and dark semantic classes/tokens without screenshot-only acceptance. Any
  screenshots are supporting evidence; behavior remains machine-asserted.

---

## Source Decisions

- `30-CONTEXT.md`: D-01 through D-20 are locked and directly define recovery, Settings exit, and
  keypad behavior.
- `30-RESEARCH.md`: reuse existing Zustand/Zod, React Router/Tauri, Radix AlertDialog/Popover,
  MoneyInput/PINKeypad, and receipt flows; no dependency change.
- `REQUIREMENTS.md`: CART-01..04, SET-01..02, and KEYPAD-01 require automated Vitest and Playwright
  coverage.
- `components.json` and `src/app/globals.css`: existing radix-nova/Counter visual language, neutral
  surfaces, teal brand accent, semantic warning/success/destructive tokens, Geist, and reduced motion.

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** approved
