---
phase: 32-brand-entity-pack-weight-catalog-attributes
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
agent_hint: Frontend Developer
files_modified:
  - src/features/manage-products/ui/CatalogProductsTab.tsx
  - src/widgets/ProductGrid/ui/ProductGrid.tsx
  - src/shared/lib/i18n/locales/es-MX/featMgmt.json
  - src/shared/lib/i18n/locales/en-US/featMgmt.json
  - src/shared/lib/i18n/locales/es-MX/wPanels.json
  - src/shared/lib/i18n/locales/en-US/wPanels.json
  - e2e/products/product-management.spec.ts
  - e2e/checkout/product-grid-brand-weight-filters.spec.ts
autonomous: true
requirements: [BRND-04, BRND-05]

estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "The admin Catalog -> Products table can be filtered by brand and by weight unit via dropdown <select>s above the table (D-11), composing with the existing text search -- selecting a brand narrows the table to only that brand's products."
    - "The POS checkout grid (/pos) shows brand and weight-unit dropdown filters below the existing category tab strip (D-12); selecting a brand narrows the grid to that brand's products WITHIN the currently active category tab (AND logic -- category selection and the new dropdowns compose, neither replaces the other)."
    - "CategoryTabs itself is untouched by this phase (D-12) -- the primary category selector's markup and behavior are identical to before this plan."
  artifacts:
    - src/features/manage-products/ui/CatalogProductsTab.tsx (brand/weight filter toolbar)
    - src/widgets/ProductGrid/ui/ProductGrid.tsx (brand/weight secondary filters)
    - e2e/checkout/product-grid-brand-weight-filters.spec.ts
  key_links:
    - "useBrands() (Plan 01, entities/brand) <- consumed by both this plan's filter dropdowns to populate options."
    - "Product.brandId/weightUnit (Plan 01, ProductSchema) <- read by both filter predicates; without Plan 01 these fields do not exist on Product."
---

<objective>
Add brand and weight-unit filter dropdowns to both catalog-browsing surfaces this phase's CONTEXT.md
requires (D-10): the admin Catalog product table and the POS checkout grid. Client-side filtering
only, composing with (not replacing) each surface's existing search/category mechanism.

Purpose: BRND-04 requires filtering on both surfaces, and D-10 explicitly rejected picking just one.
This plan is pure UI expansion over the brand/weight data Plan 01 already made real -- no schema work.
Output: a working brand/weight-unit filter on the admin Catalog table and on the POS checkout grid.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-brand-entity-pack-weight-catalog-attributes/32-CONTEXT.md
@.planning/phases/32-brand-entity-pack-weight-catalog-attributes/32-RESEARCH.md
@.planning/phases/32-brand-entity-pack-weight-catalog-attributes/32-PATTERNS.md
@.planning/phases/32-brand-entity-pack-weight-catalog-attributes/32-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Brand/weight-unit filter dropdowns on the admin Catalog table</name>
  <files>
    src/features/manage-products/ui/CatalogProductsTab.tsx,
    src/shared/lib/i18n/locales/es-MX/featMgmt.json,
    src/shared/lib/i18n/locales/en-US/featMgmt.json,
    e2e/products/product-management.spec.ts
  </files>
  <read_first>
    - src/features/manage-products/ui/CatalogProductsTab.tsx (full file -- current DataTable usage at lines 459-469, the inline category-editor <select> markup at lines 117-118 to mirror for the filter selects)
    - src/shared/ui/DataTable.tsx (the `toolbar` prop, lines 49, 100-102 -- the exact no-new-component insertion point, D-11)
    - .planning/phases/32-brand-entity-pack-weight-catalog-attributes/32-PATTERNS.md (section "CatalogProductsTab.tsx (modified -- brand/weight filter dropdowns)")
    - src/entities/brand/index.ts (Plan 01 -- `useBrands`)
    - e2e/products/product-management.spec.ts (lines 1-111 -- getServiceClient/seedTestProduct/navigateToProductsSettingsTab helpers)
  </read_first>
  <action>
1. **`src/features/manage-products/ui/CatalogProductsTab.tsx`.** Import `useBrands` from
   `@entities/brand`. Add `const { data: brands } = useBrands();` and two `useState<string>('')`
   values, `brandFilter`/`weightUnitFilter` (empty string = "all", matching the native `<select>`
   convention already used elsewhere in this file). Before passing `data` to `<DataTable>`, filter
   the existing `products ?? []` array: `const filteredProducts = (products ?? []).filter(p =>
   (brandFilter === '' || p.brandId === brandFilter) && (weightUnitFilter === '' || p.weightUnit ===
   weightUnitFilter));` and pass `data={filteredProducts}` in place of `data={products ?? []}`. Pass
   a `toolbar` prop containing two native `<select>`s (same className as the existing inline
   category-editor `<select>` at lines 117-118): the brand select with a leading `<option
   value="">{t('manageProducts.productsTab.allBrands')}</option>` then `brands?.map(b => <option
   key={b.id} value={b.id}>{b.name}</option>)` (already alphabetical -- `useBrands()` orders by
   name); the weight-unit select with a leading `<option
   value="">{t('manageProducts.productsTab.allWeightUnits')}</option>` then the four literal unit
   options `g`/`kg`/`lb`/`oz`. This is D-11 -- no new filter-panel component, plain `useState` + array
   `.filter()`, and the existing `DataTable searchable` text search still applies on top of
   `filteredProducts`.

2. **i18n.** Add to `manageProducts.productsTab` in both `featMgmt.json` locales:
   `brandFilterLabel`, `allBrands`, `weightUnitFilterLabel`, `allWeightUnits` (used as `<select>`
   `aria-label`s and the leading "all" option text).

3. **`e2e/products/product-management.spec.ts`.** Add one test: seed two brands via the service
   client (`admin.from('brands').insert(...)`), seed two products via `seedTestProduct`-style
   upserts referencing each brand respectively, open Catalog -> Products, select one brand from the
   new filter dropdown, and assert only that brand's product row is visible (`toHaveCount` on the row
   locator) while the sibling product row is absent. Clean up the seeded brands/products in the
   test's own cleanup (both `beforeEach`/`afterEach` and an explicit brand delete after the products
   that reference them are removed, since D-02's RESTRICT would otherwise block the brand cleanup).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit, or any "error TS" line in the output</fails_when>
    <automated>npx playwright test e2e/products/product-management.spec.ts</automated>
    <fails_when>non-zero exit, or the summary line reports any value other than 0 for "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "toolbar" src/features/manage-products/ui/CatalogProductsTab.tsx` returns at least 1 (the DataTable `toolbar` prop is used).
    - The new filter E2E test asserts exactly one product row visible after selecting a brand, and zero rows for a brand that has no matching product.
    - `git diff --name-only HEAD -- src/entities/product/ui/CategoryTabs.tsx` prints nothing (this task never touches the POS category selector -- that stays Task 2's boundary, D-12).
  </acceptance_criteria>
  <done>An admin narrows the Catalog product table by brand and by weight unit via two new dropdowns above the table, composing with the existing search box; proven by a real E2E test.</done>
</task>

<task type="auto">
  <name>Task 2: Brand/weight-unit filter dropdowns on the POS checkout grid</name>
  <files>
    src/widgets/ProductGrid/ui/ProductGrid.tsx,
    src/shared/lib/i18n/locales/es-MX/wPanels.json,
    src/shared/lib/i18n/locales/en-US/wPanels.json,
    e2e/checkout/product-grid-brand-weight-filters.spec.ts
  </files>
  <read_first>
    - src/widgets/ProductGrid/ui/ProductGrid.tsx (full file -- the current activeCategory/matches filter predicate, lines 36, 47-55, and the CategoryTabs render site at lines 94-101)
    - src/entities/product/ui/CategoryTabs.tsx (must stay untouched, D-12 -- receives no new props)
    - .planning/phases/32-brand-entity-pack-weight-catalog-attributes/32-PATTERNS.md (section "ProductGrid.tsx (modified -- secondary brand/weight filters, D-12)")
    - src/entities/brand/index.ts (Plan 01 -- `useBrands`)
    - e2e/helpers/supabase.ts (getServiceClient, openCaja, resetTestState)
    - e2e/helpers/auth.ts (loginAs)
    - e2e/products/product-management.spec.ts (lines 1-45 -- the seeding/cleanup style to mirror in a new checkout spec file)
  </read_first>
  <action>
1. **`src/widgets/ProductGrid/ui/ProductGrid.tsx`.** Import `useBrands` from `@entities/brand`. Add
   `const { data: brands = [] } = useBrands();` and two `useState<string | null>(null)` values,
   `activeBrand`/`activeWeightUnit` (same shape as the existing `activeCategory` state). AND them
   into the existing `matches` filter predicate alongside `activeCategory` and the search `query`
   (D-12 -- narrows within the active category, does not replace it): `(activeBrand === null ||
   product.brandId === activeBrand) && (activeWeightUnit === null || product.weightUnit ===
   activeWeightUnit)`. Render two native `<select>`s (same styling convention as the category-select
   markup used elsewhere in this codebase) in a `<div>` directly below the existing `<CategoryTabs
   .../>` element and above the `matches`-rendering scroll container -- `CategoryTabs` itself receives
   no new props and is not otherwise touched (D-12). Each select's first option is an "all" sentinel
   (`value=""`, mapped to `null` on change) followed by the brand list / the four literal units.

2. **i18n.** Add to `checkoutPanel` in both `wPanels.json` locales: `brandFilterLabel`, `allBrands`,
   `weightUnitFilterLabel`, `allWeightUnits`.

3. **`e2e/checkout/product-grid-brand-weight-filters.spec.ts`** (new file). Mirror the
   login/caja-open/seed pattern used elsewhere in this suite: `requireIntegrationEnv()`,
   `resetTestState()`, `openCaja(<amount>)`, `loginAs(page, 'cashier')`, `goto('/pos')`. Seed two
   products via the service client with different `brand_id`s in the SAME category (to prove the
   AND-composition, not just brand alone), select that category's tab, then select one brand from the
   new grid dropdown, and assert only that brand's product card is visible in the grid while the
   sibling (same category, different brand) is not. Add a second assertion proving the weight-unit
   dropdown narrows independently of the brand dropdown. Clean up seeded brands/products in
   `afterEach` (delete products before their referencing brand, per D-02's RESTRICT).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit, or any "error TS" line in the output</fails_when>
    <automated>npx playwright test e2e/checkout/product-grid-brand-weight-filters.spec.ts</automated>
    <fails_when>non-zero exit, or the summary line reports any value other than 0 for "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only HEAD -- src/entities/product/ui/CategoryTabs.tsx` prints nothing (D-12 -- `CategoryTabs` stays untouched).
    - The new E2E spec asserts the AND-composition: a same-category, different-brand sibling product disappears from the grid when the brand filter is applied while the active category tab is unchanged.
    - `npx playwright test e2e/checkout/product-grid-brand-weight-filters.spec.ts` reports at least 2 tests, 0 failed.
  </acceptance_criteria>
  <done>A cashier/manager on /pos narrows the product grid by brand and weight unit, composing with the active category tab (never replacing it); CategoryTabs itself is unmodified; proven by a real E2E test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side filter state (both surfaces) | Purely a display-narrowing UI over data already fetched under existing RLS read policies -- no new read/write boundary is introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-07 | Information Disclosure | brand/weight filter dropdown data source | low | accept | Filter options come from `useBrands()`/the already-fetched `products` array, both already readable under existing RLS (brand read policy from Plan 01, products read policy pre-existing) -- no new data exposure, purely a client-side narrowing UI. |
| T-32-08 | Tampering | client-side filter state manipulation via devtools | low | accept | Filtering is a display-only convenience; a manipulated filter state can only show/hide rows already present in the session's authorized dataset, not fetch anything new -- the underlying RLS-gated fetch is unchanged. |
| T-32-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new packages this plan. |
</threat_model>

<verification>
- `npm run typecheck` clean.
- `npx playwright test e2e/products/product-management.spec.ts e2e/checkout/product-grid-brand-weight-filters.spec.ts` green.
</verification>

<success_criteria>
Both the admin Catalog table and the POS checkout grid can be filtered by brand and weight unit,
composing with existing search/category selection rather than replacing it; proven by automated
tests.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 02's share)

**UI**
- Brand/weight-unit filter `toolbar` on `CatalogProductsTab.tsx`'s `DataTable`
- Brand/weight-unit secondary filter dropdowns below `CategoryTabs` in `ProductGrid.tsx`

**i18n keys** (`featMgmt.manageProducts.productsTab`, `wPanels.checkoutPanel`, both `es-MX`/`en-US`)
- `brandFilterLabel`, `allBrands`, `weightUnitFilterLabel`, `allWeightUnits` (in each namespace)

**Tests**
- `e2e/checkout/product-grid-brand-weight-filters.spec.ts` (new)
- `e2e/products/product-management.spec.ts` (extended)
</artifacts>

<output>
Create `.planning/phases/32-brand-entity-pack-weight-catalog-attributes/32-02-SUMMARY.md` when done.
</output>
