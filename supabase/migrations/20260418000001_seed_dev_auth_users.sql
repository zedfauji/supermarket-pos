-- VOIDED: Dev auth users already exist (created by setup:dev-users script on 2026-04-16).
-- This migration was applied but created duplicates — data was cleaned up manually.
-- Left as a no-op so migration history stays intact.
SELECT 1;
